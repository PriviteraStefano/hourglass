---
quick_id: 260825-route-tree-gitignore
description: Untrack generated TanStack route tree (CONCERNS #5)
date: 2026-08-25
status: complete
---

# Quick Task 260825-route-tree-gitignore

## Plan

Address CONCERNS.md #5 "Committed generated route tree".

Task: add `web/src/routeTree.gen.ts` to `.gitignore` and `git rm --cached` it so the
TanStack Router generated file is no longer tracked. It is regenerated by the router plugin
on `bun run dev` / `bun run build`, so CI continues to work; this only removes noisy diffs.

> Note: CONCERNS.md #4 (hand-maintained test mocks) is deferred — regenerating mocks with
> mockgen requires migrating the two test files that depend on the hand-written fakes'
> in-memory state. Flagged for a deliberate follow-up, not silently changed.

## Verify

- `.gitignore` contains `web/src/routeTree.gen.ts`
- `git ls-files web/src/routeTree.gen.ts` returns nothing (untracked)
- File still present on disk (build regenerates it)
