---
phase: 01-authorization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/app/org-switcher.tsx
  - web/src/components/layout/sidebar.tsx
  - web/src/routes/_authenticated/index.tsx
  - web/src/api/auth.ts
autonomous: true
requirements: [AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "OrgSwitcher dropdown shows real organizations from /auth/memberships"
    - "Clicking an organization in OrgSwitcher switches org and refreshes all data"
    - "Sidebar OrgSwitcher does not pass a hardcoded organizations prop"
    - "Accessing / redirects to /time-entries instead of showing Dashboard placeholder"
    - "Password reset API response type has no optional code field"
  artifacts:
    - path: "web/src/components/app/org-switcher.tsx"
      provides: "Self-fetched memberships via useSuspenseQuery, wire org switch mutation"
      contains: "membershipsQueryOpts"
    - path: "web/src/components/layout/sidebar.tsx"
      provides: "OrgSwitcher used without organizations prop"
      contains: "<OrgSwitcher/>"
    - path: "web/src/routes/_authenticated/index.tsx"
      provides: "Redirect to /time-entries instead of Dashboard"
      contains: "Navigate"
    - path: "web/src/api/auth.ts"
      provides: "requestPasswordResetMutationOpts response type without code field"
      contains: "message: string"
  key_links:
    - from: "org-switcher.tsx"
      to: "AuthApis.membershipsQueryOpts"
      via: "useSuspenseQuery"
    - from: "org-switcher.tsx"
      to: "AuthApis.switchOrganizationMutationOpts"
      via: "useMutation with client.clear()"
    - from: "sidebar.tsx"
      to: "org-switcher.tsx"
      via: "<OrgSwitcher/> (no organizations prop)"
---

<objective>
**Frontend auth integration** (D-03, D-04, D-05, D-06, D-07, D-11) — wire real data into OrgSwitcher, create `/` redirect to `/time-entries`, fix password reset API type.

**Purpose:** Enable org switching with full data refresh (D-03/D-04/D-05), implement minimal landing-page redirect (D-06/D-07), and update the password-reset API contract (D-11).

**Output:** Modified `org-switcher.tsx`, `sidebar.tsx`, `auth.ts`, and `_authenticated/index.tsx`.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-authorization/01-CONTEXT.md
@.planning/phases/01-authorization/01-PATTERNS.md

@web/src/components/app/org-switcher.tsx
@web/src/components/layout/sidebar.tsx
@web/src/components/app/profile-menu.tsx
@web/src/routes/_authenticated/index.tsx
@web/src/api/auth.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Fix OrgSwitcher to fetch real memberships and wire org switch mutation + fix sidebar</name>

<files>
- web/src/components/app/org-switcher.tsx
- web/src/components/layout/sidebar.tsx
</files>

<read_first>
@web/src/components/app/org-switcher.tsx
  — Current signature: `export function OrgSwitcher({organizations}: {organizations: Array<Organization>})`
  — Current uses `AuthApis.profileQueryOpts` for current org (line 20)
  — Dropdown maps over `organizations` prop (line 52) — currently empty
  — onClick is empty (line 56)
@web/src/components/layout/sidebar.tsx
  — Line 60: `<OrgSwitcher organizations={[]}/>` — remove array prop
@web/src/components/app/profile-menu.tsx
  — Reference pattern: self-contained component using useSuspenseQuery + useMutation
@web/src/api/auth.ts
  — membershipsQueryOpts (lines 129-133) — returns `{ memberships: Array<{membership, organization}> }`
  — switchOrganizationMutationOpts (lines 119-127) — POST with `organization_id`, onSuccess sets query data
  — onSuccess pattern uses `client.setQueryData` (4th arg destructured `{client}`)
</read_first>

<action>

**A. Rewrite `org-switcher.tsx`** (D-03, D-04, D-05):

1. **Remove the `organizations` prop entirely** (D-03). The component will fetch its own data.

2. **Add imports:** Add `useMutation` from `@tanstack/react-query` and `useNavigate` from `@tanstack/react-router`. Remove the `Organization` type import since it's no longer a prop.

3. **Add data fetching:** Inside the component, add:
   - `const {data: membershipsData} = useSuspenseQuery(AuthApis.membershipsQueryOpts)` to fetch real memberships
   - `const queryClient = useQueryClient()` — need to import `useQueryClient`
   - `const {mutateAsync: switchOrg} = useMutation(AuthApis.switchOrganizationMutationOpts)`
   - `const navigate = useNavigate()`

4. **Replace the dropdown mapping** (lines 52-62): Instead of `organizations.map(...)`, map over `membershipsData.memberships.map(...)`.

5. **Wire the switch onClick** (D-04, D-05 — replace the empty `onClick={() => {}}` on line 56): On click, call `await switchOrg({ organization_id: membership.organization.id })`, then `queryClient.clear()` to refresh all data for the new org context. Also refetch `['auth', 'me']` since the profile organization changes (D-05).

6. **Adjust the display:** Each dropdown item should show `membership.organization.name` with a secondary `membership.membership.role` label below it (dimmed text-xs). Keep the same styling patterns as the current dropdown.

**B. Fix `sidebar.tsx`** (D-03):

Change line 60 from `<OrgSwitcher organizations={[]}/>` to `<OrgSwitcher/>`. Remove the `organizations` prop entirely since OrgSwitcher now self-fetches its data.

</action>

<verify>
<automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build</automated>
Build must pass with no TypeScript errors. The route tree will be auto-generated by the Vite plugin.
</verify>

<done>
- OrgSwitcher has no `organizations` prop
- OrgSwitcher fetches memberships via `useSuspenseQuery(AuthApis.membershipsQueryOpts)`
- Org switch mutation wires to dropdown `onClick` with `queryClient.clear()` after success
- Sidebar renders `<OrgSwitcher/>` without the empty organizations array
- `bun run build` passes with zero TypeScript errors
</done>
</task>

<task type="auto">
<name>Task 2: Create `/` redirect to `/time-entries` + fix password reset API response type</name>

<files>
- web/src/routes/_authenticated/index.tsx
- web/src/api/auth.ts
</files>

<read_first>
@web/src/routes/_authenticated/index.tsx
  — Currently renders: `Dashboard` heading with welcome text (lines 3-12)
  — Route ID: `/_authenticated/`
@web/src/api/auth.ts
  — requestPasswordResetMutationOpts (lines 97-103): response type has `code?: string`
@web/src/routes/_auth/login/-components/login-form.tsx
  — Pattern: navigate({to: '/'}) after login (line 48) — this already targets `/`
</read_first>

<action>

**A. Replace `/` route with redirect** (D-06, D-07 — `_authenticated/index.tsx`):

Replace the entire file content. Import `createFileRoute` and `Navigate` from `@tanstack/react-router`. The component should render `<Navigate to="/time-entries" replace />`. This implements the minimal redirect approach (D-07, no Dashboard page in v0.1).

The route ID stays `/_authenticated/` — no file path changes needed.

After login/register, the `navigate({to: '/'})` calls already in `login-form.tsx` (line 48) and `register-form.tsx` (line 69) will redirect to `/`, which immediately redirects to `/time-entries` (per D-06).

**B. Fix password reset API response type** (D-11 — `auth.ts`):

Change line 99 from `api<{ message: string; code?: string }>` to `api<{ message: string }>`. Remove the `code?: string` from the type parameter. The backend already does not return the code in the response body (confirmed in PATTERNS.md), so the frontend type should not expect it.

</action>

<verify>
<automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build</automated>
Build must pass with no TypeScript errors.
</verify>

<done>
- `_authenticated/index.tsx` renders `<Navigate to="/time-entries" replace />`
- `requestPasswordResetMutationOpts` response type is `{ message: string }` without `code?`
- `bun run build` passes with zero TypeScript errors
- Login form navigates to `/` (already working via `navigate({to: '/'})`)
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Org switch and password reset requests from frontend components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | OrgSwitcher | mitigate | POST /auth/switch-organization is protected by auth middleware; token rotation on switch |
| T-02-02 | Info Disclosure | Password reset mutation | mitigate | Response type changed from `{message, code?}` to `{message}` — no code surface in frontend per D-11 |
| T-02-03 | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>

### Automated

```bash
cd /Users/stefanoprivitera/Projects/hourglass/web
bun run build
```

### Structural checks (grep-based)

```bash
# Verify no remaining hardcoded organizations prop in sidebar
grep -c 'organizations={\[\]}' web/src/components/layout/sidebar.tsx
# Expected: 0

# Verify OrgSwitcher no longer has the organizations prop
grep -c 'organizations:' web/src/components/app/org-switcher.tsx | grep -v 'organization'
# Expected: 0

# Verify password reset type no longer has code?
grep 'code?' web/src/api/auth.ts
# Expected: no match
```

</verification>

<success_criteria>

1. OrgSwitcher fetches real memberships from /auth/memberships (D-03 satisfied)
2. Org switch works: call mutation → `queryClient.clear()` → all data refreshes (D-04, D-05)
3. Sidebar renders OrgSwitcher without empty `organizations` prop
4. `/` route redirects to `/time-entries` (D-06, D-07 satisfied)
5. Password reset mutation type has no `code?` field (D-11 satisfied)
6. Frontend build passes with zero TypeScript errors

</success_criteria>

<output>

Create `.planning/phases/01-authorization/01-02-SUMMARY.md` when done.

</output>
