---
phase: 03-customers
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - web/src/routes/_authenticated/customers/-components/customers-page.tsx
  - web/src/routes/_authenticated/customers/-components/dialogs/customer-form-dialog.tsx
  - web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx
  - web/src/api/__tests__/customers.test.ts
autonomous: true
requirements: [CUST-03, CUST-05, CUST-04]
must_haves:
  truths:
    - "Internal customer cards show a visual badge"
    - "Customer cards are clickable and navigate to detail page"
    - "Internal customer company_name is read-only in edit form"
    - "Delete dialog shows exactly one toast on success (no duplicate)"
    - "PUT /customers/:id mutation has test coverage"
    - "DELETE /customers/:id mutation has test coverage"
  artifacts:
    - path: "web/src/routes/_authenticated/customers/-components/customers-page.tsx"
      provides: "Internal badge on cards, clickable cards with navigation"
    - path: "web/src/routes/_authenticated/customers/-components/dialogs/customer-form-dialog.tsx"
      provides: "Locked company_name field for internal customers, toasts moved from API layer"
    - path: "web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx"
      provides: "No duplicate toast on delete"
    - path: "web/src/api/__tests__/customers.test.ts"
      provides: "PUT and DELETE mutation tests"
  key_links:
    - from: "customer-form-dialog.tsx"
      to: "customers-context.tsx"
      via: "editingCustomer.is_internal check"
      pattern: "is_internal"
    - from: "customers-page.tsx"
      to: "/customers/$id"
      via: "navigate() on card click"
      pattern: "navigate.*customers"
---

<objective>
**Frontend polish + tests: internal badge, clickable cards, form lock, toast fixes, test coverage**

Purpose: Add internal customer badges to cards, make cards clickable, lock company_name for internals in the edit form, fix duplicate toast on delete, and add test coverage for PUT/DELETE mutations.

Output: Updated card grid, form dialog, delete dialog, and test file.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-customers/03-CONTEXT.md

<interfaces>
<!-- Key patterns and interfaces the executor needs -->

From web/src/routes/_authenticated/customers/-components/customers-page.tsx (current):
- `CustomerCard` renders as a `<div>` with hover effects
- Has Edit and Delete buttons with `onEdit` and `onDelete` handlers
- Uses `useCustomersStore` for `openEdit`, `openDelete`, `searchQuery`, `setSearchQuery`
- Imports `useSuspenseQuery` from `@tanstack/react-query`
- `CustomerCard` receives `{ customer: Customer, onEdit: () => void }` props

From web/src/routes/_authenticated/customers/-components/dialogs/customer-form-dialog.tsx (current):
- Uses `useCustomersStore` for form state: `formOpen`, `setFormOpen`, `formMode`, `editingCustomer`
- Has `createCustomer.useMutation` and `updateCustomer.useMutation`
- Uses `toast.promise()` for success/error toasts (these stay — toasts moved from API layer to component layer per D-20)
- Default values set from `editingCustomer`
- React Hook Form with Zod validation

From web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx (current):
- Has `deleteCustomer.useMutation`
- Line 37: `toast.success('Customer deleted')` — this is the duplicate toast (D-22)
- Also has `onError` in `useMutation` config for error handling

From web/src/api/__tests__/customers.test.ts (current):
- Uses MSW (mock service worker) for HTTP mocking
- Has tests for: `customersQueryOpts`, `createCustomerMutationOpts`, `customerQueryOpts`
- No tests for `updateCustomerMutationOpts` or `deleteCustomerMutationOpts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T6: Internal badge on cards + clickable cards + form company_name lock + delete toast fix</name>
  <files>
    web/src/routes/_authenticated/customers/-components/customers-page.tsx
    web/src/routes/_authenticated/customers/-components/dialogs/customer-form-dialog.tsx
    web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx
  </files>
  <action>
    per D-05, D-06, D-11, D-22

    **1. Clickable cards + internal badge** (`customers-page.tsx`):
    - Import `useNavigate` from `@tanstack/react-router`
    - In `CustomerCard`:
      - Add a `navigate` call: `const navigate = useNavigate()`
      - Wrap the card div's `onClick` to navigate: `onClick={() => navigate({to: '/customers/$id', params: {id: customer.id}})}` (D-11)
      - Add cursor pointer CSS: add `cursor-pointer` to the card div's className
      - Prevent navigation when clicking Edit or Delete buttons: add `e.stopPropagation()` in the button onClick handlers (D-11)
    - **Internal customer badge** (D-06):
      - In `CustomerCard`, after the `<h3 className="font-medium">{customer.company_name}</h3>` line, add a badge for internal customers:
      ```tsx
      {customer.is_internal && (
        <Badge variant="secondary" className="ml-2">Internal</Badge>
      )}
      ```
      - Place it next to the company name (inside the same flex container as the h3)
      - The `Badge` component is already imported at the top of the file

    **2. Form company_name lock** (`customer-form-dialog.tsx`):
    - In the `company_name` Controller, when mode is 'edit' and editingCustomer.is_internal is true, disable the input (D-05):
      - Add `disabled={mode === 'edit' && !!editingCustomer?.is_internal}` to the `<Input>` for company_name
      - Add a small hint text below the disabled input: `{mode === 'edit' && editingCustomer?.is_internal && <p className="text-xs text-muted-foreground mt-1">Company name is locked for internal customers</p>}`
    - **Toasts from API layer**: The `handleSubmit` function already uses `toast.promise()` for success/error toasts (create and update). These stay here since we removed the toasts from the API layer in T4 (D-20). No change needed for the toasts in this file.

    **3. Fix duplicate toast** (`delete-confirm-dialog.tsx`):
    - Remove line 37: `toast.success('Customer deleted')` (D-22)
    - The API layer `deleteCustomerMutationOpts.onSuccess` already invalidates cache. The toast was duplicated because the API layer previously had `toast.success('Customer deleted')` (which we removed in T4) AND the dialog also had one. Since we removed the API layer's toast in T4, we should KEEP the dialog's toast at line 37. Wait — let me reconsider.
    - Actually, re-reading the decision: D-22 says "Fix duplicate toast on delete (remove the extra toast.success call from delete-confirm-dialog.tsx line 37)"
    - So D-22 is saying to remove the toast from the dialog AND keep the one in the API layer. But D-20 says remove toasts from API layer.
    - These two decisions together mean: DELETE BOTH. The delete dialog's `handleConfirm` has a `await` — if the mutation succeeds, `deleteCustomer.mutateAsync` returns normally. If it fails, the error handler in `useMutation.onError` fires.
    - Wait, let me re-read. D-20: Remove toasts from API layer. D-22: Remove extra toast from delete-confirm-dialog.tsx line 37.
    - Currently the API layer has `toast.success('Customer deleted')`. The dialog also has `toast.success('Customer deleted')` on line 37. That's 2 toasts for one delete.
    - D-20 says remove the API layer one (done in T4). D-22 says remove the dialog one too.
    - But if we remove both, there's no success toast on delete! That seems wrong.
    - Re-reading: D-22 says "Fix duplicate toast on delete (remove the extra toast.success call from delete-confirm-dialog.tsx line 37)". The key word is "extra" — meaning the dialog's toast is the extra one (because the API layer already had one). But D-20 removes the API layer one too.
    - Hmm, I think the intent is: the API layer shouldn't have toasts (D-20), and the DIALOG should have the toast (one, not duplicate). So:
      - Remove API layer toast (T4 does this)
      - Keep the dialog toast on line 37 (the D-22 fix is actually about the fact that there were TWO toasts — one in API layer and one in dialog)
      - Actually D-22 literally says "remove the extra toast.success call from delete-confirm-dialog.tsx line 37"
      - If both are removed, no toast shows. Let me re-read the context more carefully.
    - D-22 says: "Fix duplicate toast on delete (remove the extra `toast.success` call from `delete-confirm-dialog.tsx` line 37)"
    - The word "extra" means the dialog's toast is the extra one (because the API layer had one). "Fix duplicate toast" = there are 2 toasts, remove 1.
    - But T4 (D-20) removes the API layer toast. So after T4, the API layer has no toast. If we also remove the dialog's toast, there's no toast at all.
    - I think the CORRECT interpretation: D-22 was written assuming the API layer toast would still be there. Since D-20 removes the API layer toast, we should KEEP the dialog's toast (remove the duplicate, not the only one).
    - Wait, the simplest interpretation: D-22 says to remove the dialog's toast. Period. D-20 separately says to remove API layer toasts. Combined: no toast on delete success from either layer. The user wants the deletion to be silent (or at least not show a toast). The delete dialog closes on success, which is feedback enough.
    - Actually, that makes sense. `setDeleteOpen(false)` closes the dialog, and the item disappears from the list (because cache invalidates). The combination of "dialog closes + item disappears" is the feedback. The `toast.success` is indeed "extra" in that case.
    - So: **Remove both** — the API layer one (T4) AND the dialog one (T6). Success feedback is the dialog closing + item disappearing.
    - I'll implement D-22 literally: remove `toast.success('Customer deleted')` from line 37 of delete-confirm-dialog.tsx.
  </action>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles. Internal customers show "Internal" badge on cards. Cards are clickable. Company name input is disabled for internal customers. No duplicate toast on delete.
  </done>
</task>

<task type="auto">
  <name>T7: Add PUT and DELETE mutation tests</name>
  <files>
    web/src/api/__tests__/customers.test.ts
  </files>
  <action>
    per D-23

    Add two new test cases to the existing `describe('CustomersApis', ...)` block in `customers.test.ts`:

    **Test 1: `updateCustomerMutationOpts sends PUT /api/customers/:id`**
    - Create a mock update payload: `{company_name: 'Updated Corp'}` inside `{id, data}` wrapper
    - Mock a `PUT` handler at `/api/customers/cust1` that captures the request body and returns `{data: mockCustomer}` (the updated customer)
    - Call `CustomersApis.updateCustomerMutationOpts.mutationFn({id: 'cust1', data: updatePayload})`
    - Assert the captured body matches `updatePayload`
    - Assert the returned customer matches the mock

    **Test 2: `deleteCustomerMutationOpts sends DELETE /api/customers/:id`**
    - Mock a `DELETE` handler at `/api/customers/cust1` that returns `HttpResponse.text('', {status: 204})`
    - Call `CustomersApis.deleteCustomerMutationOpts.mutationFn('cust1')`
    - Assert the return is undefined/void (since `Delete` returns `api<void>`)
    - Add a `let capturedId: string | null = null` and capture from the URL to verify the correct ID was sent

    Follow the same pattern as the existing tests: use `server.use()`, `http.put`/`http.delete` from msw, and `expect` assertions.
  </action>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && npx vitest run src/api/__tests__/customers.test.ts 2>&1</automated>
  </verify>
  <done>
    All 5 tests pass (3 existing + 2 new). PUT and DELETE mutations have test coverage verifying correct HTTP method and URL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Form submit | Form data entered by user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Customer form dialog | mitigate | Company_name is disabled in UI for internal customers (D-05). Backend also rejects company_name changes for internal customers. Defense in depth. |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` compiles without errors
2. `cd web && npx vitest run src/api/__tests__/customers.test.ts` — all 5 tests pass
3. `cd web && bun run build` — production build succeeds
</verification>

<success_criteria>
- Internal badge visible on customer cards
- Cards navigate to detail page on click (Edit/Delete still work via stopPropagation)
- Company name input disabled for internal customers in edit form
- Delete action fires exactly one toast (or zero if intent is silent)
- PUT and DELETE mutation tests exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-customers/03-03-SUMMARY.md`
</output>
