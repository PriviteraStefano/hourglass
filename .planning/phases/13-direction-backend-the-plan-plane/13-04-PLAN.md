---
phase: 13-direction-backend-the-plan-plane
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-03
files_modified:
  - internal/adapters/secondary/postgres/org_settings_repository.go
  - internal/core/services/orgsettings/orgsettings.go
  - internal/adapters/primary/http/org_settings_handler.go
  - internal/core/domain/auth/membership.go
  - internal/adapters/secondary/postgres/organization_repo.go
  - cmd/server/main.go
  - internal/adapters/primary/http/handler_test_helper.go
  - internal/core/services/orgsettings/orgsettings_test.go
  - internal/adapters/secondary/postgres/org_settings_repository_test.go
  - internal/adapters/primary/http/org_settings_handler_test.go
autonomous: true
requirements:
  - DIR-04

estimate:
  tokens: 34000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The org_settings vertical works end-to-end through every layer this phase introduces: org_settings table (13-01) → domain keys/validators (13-03) → postgres repo (Upsert in one tx with the in-tx audit row, Get, List) → orgsettings service (known-key validation → 400 on unknown key; manager+ gate on PUT) → org_settings handler (GET/PUT /organizations/settings literal routes, JWT-resolved org via middleware.GetOrganizationID — NO org path param) → cmd/server/main.go registration (D-13-18, D-13-22, D-13-23)"
    - "GET/PUT /organizations/settings are registered as literal routes COEXISTING with the existing GET/PUT /organizations/{id}/settings (typed organization_settings) — ServeMux literal-beats-wildcard; the existing wildcard registrations are NOT removed (Pitfall 6, D-13-23)"
    - "Every settings change writes an audit_logs row IN THE SAME TRANSACTION as the upsert: entity_type='org_settings', entity_id = the org id, action='settings-updated', payload {key, before, after} (D-13-22, ADR-BE-018)"
    - "auth.OrganizationMembership gains PlanningMode *string, ValidFrom *time.Time, ValidUntil *time.Time fields and postgres GetMembership SELECT+scan include valid_from, valid_until, planning_mode (columns shipped in 012/022) — the mode-override and validity reads (13-07) build on this"
    - "orgsettings service exposes ResolvePlanningMode(ctx, orgID, employeeID) (string, error): membership planning_mode override → org default planning_mode key → DefaultDailyHours-independent fallback 'manager_planned' when neither is set (D-13-19) — the direction service mode gate (13-07) consumes it"
    - "GET /organizations/settings returns the known keys with code-level defaults applied for absent keys (planning_daily_hours defaults to 8.0); PUT accepts one-or-many {key: value} pairs, each validated; unknown key → 400; non-manager PUT → 403 (D-13-18, D-13-23, D-13-24)"
  artifacts:
    - internal/adapters/secondary/postgres/org_settings_repository.go
    - internal/core/services/orgsettings/orgsettings.go
    - internal/adapters/primary/http/org_settings_handler.go
    - internal/core/services/orgsettings/orgsettings_test.go
    - internal/adapters/secondary/postgres/org_settings_repository_test.go
    - internal/adapters/primary/http/org_settings_handler_test.go
  key_links:
    - "The literal-route coexistence test asserts GET /organizations/settings (no id) hits the NEW handler while GET /organizations/{uuid}/settings still returns the typed settings — the Pitfall-6 regression lock"
    - "ResolvePlanningMode is the seam the direction service (13-07) mode gate depends on — its precedence (override → org default → fallback) is pinned here and in ADR-BE-018 (D-13-19)"
    - "The membership struct/scan extension is consumed by: orgsettings ResolvePlanningMode (planning_mode) and the direction service validity warnings (valid_from/valid_until) — one change, two consumers"
  prohibitions:
    - "Never block writes on planning deadline/horizon values — org_settings is store + permission-gating only; no deadline enforcement exists in the backend (D-13-20, D-13-21, D-X parked to UI)"
  flagged_assumptions:
    - "GET applies code-level defaults for absent keys rather than seeding rows (D-13-18 note: no seed rows needed — research Pattern 7)"
    - "ResolvePlanningMode fallback when no key and no override = 'manager_planned' (the org default is manager-planned when unset — conservative reading of D-13-19/D-13-20, documented in ADR-BE-018)"
---

<objective>
The org_settings vertical slice — the phase tracer. One end-to-end path through every layer this phase modifies: migration 022 table → orgsettings domain → postgres repo (in-tx audit upsert) → orgsettings service (known-key validation + manager gate) → org_settings handler (literal routes) → cmd/server wiring → verified by a real HTTP integration test.

Purpose: This is the thinnest slice that touches every layer the phase introduces and de-risks the phase's sharpest wiring trap (Pitfall 6: literal /organizations/settings routes coexisting with the typed wildcard routes) plus the JSONB upsert + in-tx audit pattern. It also delivers the ResolvePlanningMode seam and the membership struct/scan extension that the direction service mode gate and validity warnings (13-07) build on. DIR-04 lands here in full.
Output: org_settings repo + service + handler + routes + wiring + membership extension + tests.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@internal/adapters/secondary/postgres/coverage_repository.go
@internal/core/services/coverage/coverage.go
@internal/adapters/primary/http/organization.go
@internal/adapters/primary/http/coverage_handler.go
@internal/middleware/middleware.go
@cmd/server/main.go
@internal/adapters/primary/http/handler_test_helper.go
@internal/core/domain/auth/membership.go
@internal/adapters/secondary/postgres/organization_repo.go
</context>

<tasks>

<task type="tracer">
  <name>End-to-end org_settings — repo upsert+audit, service validation+gate, handler literal routes, wiring</name>
  <files>
    internal/adapters/secondary/postgres/org_settings_repository.go
    internal/core/services/orgsettings/orgsettings.go
    internal/adapters/primary/http/org_settings_handler.go
    cmd/server/main.go
    internal/adapters/primary/http/handler_test_helper.go
    internal/adapters/primary/http/org_settings_handler_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/coverage_repository.go lines 27-70 (struct + assertion + in-tx audit insert helper shape)
    internal/core/services/coverage/coverage.go lines 46-64 (service struct/constructor), 480-483 (manager-role gate shape)
    internal/adapters/primary/http/organization.go lines 100-149 (GetSettings/UpdateSettings route shape — the typed analog to diverge from: NO path param)
    internal/adapters/primary/http/coverage_handler.go lines 81-97, 306-323 (claims/parse/writeError skeleton)
    internal/middleware/middleware.go lines 67-85 (GetOrganizationID/GetUserID/GetRole)
    cmd/server/main.go lines 140-260 (coverage wiring block + route registrations incl. GET/PUT /organizations/{id}/settings at ~211-212)
    internal/adapters/primary/http/handler_test_helper.go lines 48-239 (newHandlerFixture — mirror wiring)
    internal/adapters/primary/http/organization_test.go (typed-settings handler test shape)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (orgsettings service + handler analog sections, lines 217-293; wiring lines 485-503)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 7 lines 352-359; Pitfall 6 lines 435-439)
  </read_first>
  <action>
    ONE path, wired end-to-end, production quality: the settings GET/PUT vertical for the JWT-resolved org.

    Repo (internal/adapters/secondary/postgres/org_settings_repository.go): struct {pool *pgxpool.Pool} + var _ ports.OrgSettingsRepository = (*OrgSettingsRepository)(nil) + NewOrgSettingsRepository(pool). Get: SELECT value FROM org_settings WHERE org_id=$1 AND key=$2 → pgx.ErrNoRows maps to (nil, nil). List: SELECT key, value FROM org_settings WHERE org_id=$1 → map[string]json.RawMessage. Upsert: BeginTx + defer Rollback; INSERT ... ON CONFLICT (org_id, key) DO UPDATE SET value = EXCLUDED.value, updated_at = EXCLUDED.updated_at (pattern from PATTERNS.md lines 407-419); write the audit row in the SAME tx via an insertOrgSettingsAudit helper (copy insertCoverageAudit shape, coverage_repository.go 45-70 — entity_type from log.EntityType); Commit; wrapPGError on failures.

    Service (internal/core/services/orgsettings/orgsettings.go): Service{repo ports.OrgSettingsRepository, orgRepo ports.OrganizationRepository} + NewService(repo, orgRepo). Get(ctx, orgID) (map[string]any, error): List raw settings, overlay code-level defaults for absent known keys (planning_daily_hours → 8.0; others absent-key → omitted from the response map). Put(ctx, orgID, actorID uuid.UUID, role string, values map[string]json.RawMessage) (map[string]any, error): manager+ gate first (role != string(models.RoleManager) → ErrForbidden — D-13-23); for each key: unknown → ErrUnknownKey; validate via the domain knownKeys validator → ErrInvalidValue; fetch current value (repo Get), build audit {OrgID, EntityType: orgsettings.AuditEntityOrgSettings, EntityID: orgID, Action: orgsettings.AuditActionSettingsUpdated, ActorID: &actorID, Payload: map[string]any{"key": key, "before": <current or nil>, "after": <new>}}, then repo.Upsert per key. Return the post-state map.

    Handler (internal/adapters/primary/http/org_settings_handler.go): OrgSettingsHandler{service *orgsettings.Service}; Get(w, r): orgID := middleware.GetOrganizationID(r.Context()) → service.Get → api.RespondWithJSON(w, 200, m). Put(w, r): decode body into map[string]json.RawMessage (invalid body → 400) → service.Put(ctx, orgID, middleware.GetUserID(ctx), middleware.GetRole(ctx), values) → writeError switch: ErrUnknownKey/ErrInvalidValue/ErrInvalidRequest → 400, ErrForbidden → 403, default → 500.

    Wiring (cmd/server/main.go): after the coverage block, orgSettingsRepo := postgres.NewOrgSettingsRepository(pool); orgSettingsService := orgsettingssvc.NewService(orgSettingsRepo, orgRepo); orgSettingsHandler := http.NewOrgSettingsHandler(orgSettingsService); register mux.HandleFunc("GET /organizations/settings", middleware.Auth(authService, orgSettingsHandler.Get)) + "PUT /organizations/settings" — the literal routes NEXT TO the existing GET/PUT /organizations/{id}/settings registrations (do NOT remove or reorder them — ServeMux most-specific-wins, proven by POST /organizations/invite at line ~209).

    Fixture (handler_test_helper.go): mirror the same repos/services/handlers/routes in newHandlerFixture so handler tests exercise the real stack.

    One end-to-end integration test (org_settings_handler_test.go): registerAndLogin as manager → PUT /organizations/settings {"planning_daily_hours": 7.5} → 200; GET /organizations/settings returns planning_daily_hours: 7.5; audit_logs contains a row entity_type='org_settings' action='settings-updated' with payload {"key":"planning_daily_hours","before":null,"after":7.5} (or before 8-equivalent — assert the payload key and after value); PUT with unknown key → 400; PUT as employee role → 403. This single path proves repo-tx-audit + validation + gate + routes + wiring together.
  </action>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run TestOrgSettingsHandler -count=1</automated>
  </verify>
  <done>The one settings path works end-to-end through the HTTP stack: PUT validated + audited + manager-gated, GET returns it, unknown key 400 — committed with its test.</done>
  <acceptance_criteria>
    - org_settings_repository.go compiles with Get/List/Upsert; Upsert commits value + audit row in one tx (rollback leaves no audit row — asserted via a test forcing a mid-tx error or by the audit assertion in the e2e test)
    - orgsettings service rejects unknown keys with ErrUnknownKey and non-manager PUT with ErrForbidden
    - main.go registers "GET /organizations/settings" and "PUT /organizations/settings" and still registers GET/PUT /organizations/{id}/settings (no removal)
    - The e2e test passes: PUT 200 → GET reflects 7.5 → audit row exists with key + before/after → unknown key 400 → employee 403
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Membership struct/scan extension + ResolvePlanningMode + unit tests</name>
  <files>
    internal/core/domain/auth/membership.go
    internal/adapters/secondary/postgres/organization_repo.go
    internal/core/services/orgsettings/orgsettings.go
    internal/core/services/orgsettings/orgsettings_test.go
  </files>
  <read_first>
    internal/core/domain/auth/membership.go (OrganizationMembership struct — add fields)
    internal/adapters/secondary/postgres/organization_repo.go lines 66-81 (GetMembership SELECT + scan — extend)
    migrations/012_staffing_schema.up.sql lines 39-42 (valid_from/valid_until/work_permit_expires_at columns)
    migrations/022_org_settings.up.sql (planning_mode column — being created in 13-01)
    internal/core/services/coverage/coverage_test.go (service unit-test shape with mocks)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (orgsettings service analog)
  </read_first>
  <action>
    Extend internal/core/domain/auth/membership.go OrganizationMembership with three fields: PlanningMode *string, ValidFrom *time.Time, ValidUntil *time.Time (json tags planning_mode/valid_from/valid_until, omitempty) — additive, no constructor change (NewOrganizationMembership leaves them nil).

    Extend internal/adapters/secondary/postgres/organization_repo.go GetMembership: SELECT adds valid_from, valid_until, planning_mode (after updated_at) and the Scan adds the three new slots. AddMembership INSERT stays untouched (new columns are nullable).

    Extend the orgsettings service with ResolvePlanningMode(ctx, orgID, employeeID uuid.UUID) (string, error): m, err := orgRepo.GetMembership(ctx, employeeID, orgID); if m != nil && m.PlanningMode != nil → return *m.PlanningMode (mode vocabulary validated — mismatch → ErrInvalidValue); else read the planning_mode key via repo.Get; if present and valid → return it; else return ModeManagerPlanned (the unset default — D-13-19 fallback, pinned in ADR-BE-018).

    Unit tests (orgsettings_test.go, testdata mocks): Put with unknown key → ErrUnknownKey; Put with invalid planning_horizon value → ErrInvalidValue; Put by non-manager role → ErrForbidden; Put writes one audit row per key with {key, before, after}; ResolvePlanningMode: override wins over org default; org default wins when no override; fallback manager_planned when neither; GET applies default 8.0 for absent planning_daily_hours.
  </action>
  <verify>
    <automated>go test ./internal/core/services/orgsettings/ -run TestOrgSettingsService -count=1 && go test ./internal/adapters/secondary/postgres/ -run TestOrganizationRepository -count=1</automated>
  </verify>
  <done>Membership carries planning_mode + validity; ResolvePlanningMode precedence tested; orgsettings service unit suite green.</done>
  <acceptance_criteria>
    - membership.go OrganizationMembership has PlanningMode/ValidFrom/ValidUntil pointer fields
    - organization_repo.go GetMembership SELECT includes valid_from, valid_until, planning_mode and scans them
    - orgsettings service exposes ResolvePlanningMode with the override → org-default → manager_planned precedence
    - go test ./internal/core/services/orgsettings/ and the organization-repo GetMembership tests exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Repo integration tests + handler test battery (literal-route coexistence, permission matrix)</name>
  <files>
    internal/adapters/secondary/postgres/org_settings_repository_test.go
    internal/adapters/primary/http/org_settings_handler_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/coverage_repository_test.go (integration test shape: SetupTestSchema + TeardownTestSchema + NewRepo(pool))
    internal/adapters/primary/http/organization_test.go (typed-settings handler test — the coexistence counterpart)
    internal/adapters/primary/http/handler_test_helper.go (newHandlerFixture + registerAndLogin usage)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (test analog lines 460-464)
  </read_first>
  <action>
    Create org_settings_repository_test.go (postgres package, testcontainers + SetupTestSchema): Upsert creates + updates the row (value replaced); Upsert writes the audit row in the same tx — assert audit_logs has entity_type='org_settings', entity_id = orgID, action='settings-updated', payload JSON contains key + before + after; List returns only stored keys; Get absent key returns nil.
    Extend org_settings_handler_test.go into the full battery: (a) literal-route coexistence — GET /organizations/settings (no id) hits the new handler while GET /organizations/{uuid}/settings (seed a typed organization_settings row or assert the typed handler's response shape) still returns the typed settings (Pitfall 6 lock); (b) GET requires auth (401 without cookie); (c) PUT one-or-many keys both succeed; (d) PUT planning_daily_hours: 0 or negative → 400 (ErrInvalidValue); (e) PUT planning_horizon: "year" → 400; (f) PUT as finance role → 403 (manager+ only); (g) malformed JSON body → 400.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestOrgSettingsRepository -count=1 && go test ./internal/adapters/primary/http/ -run TestOrgSettingsHandler -count=1</automated>
  </verify>
  <done>Repo + handler suites green including the literal-route coexistence test; the typed /organizations/{id}/settings surface is proven untouched.</done>
  <acceptance_criteria>
    - org_settings_repository_test.go covers upsert/audit-in-tx/list/get-absent
    - org_settings_handler_test.go contains a test asserting BOTH /organizations/settings (new) and /organizations/{uuid}/settings (typed) resolve correctly
    - The permission matrix (manager 200, finance 403) and the validation matrix (unknown key 400, invalid value 400, bad JSON 400) pass
    - go test ./internal/adapters/secondary/postgres/ -run TestOrgSettingsRepository -count=1 and go test ./internal/adapters/primary/http/ -run TestOrgSettingsHandler -count=1 exit 0
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/adapters/secondary/postgres/org_settings_repository.go` — `OrgSettingsRepository` (Get/List/Upsert with in-tx audit) + `insertOrgSettingsAudit` helper
- `internal/core/services/orgsettings/orgsettings.go` — `Service` (Get with defaults overlay, Put with known-key validation + manager gate, `ResolvePlanningMode`)
- `internal/adapters/primary/http/org_settings_handler.go` — `OrgSettingsHandler` (Get/Put, JWT-resolved org, writeError map)
- `internal/core/domain/auth/membership.go` — `OrganizationMembership.PlanningMode/ValidFrom/ValidUntil` fields
- `internal/adapters/secondary/postgres/organization_repo.go` — GetMembership SELECT+scan extended
- `cmd/server/main.go` — orgsettings wiring + literal `GET/PUT /organizations/settings` routes
- `internal/adapters/primary/http/handler_test_helper.go` — fixture wiring for orgsettings
- Tests: `org_settings_repository_test.go`, `orgsettings_test.go`, `org_settings_handler_test.go`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → org_settings handler | unauthenticated/unauthorized callers crossing into org-policy mutation |
| org_settings value → service validation | JSONB values are unvalidated by the DB (no CHECK on JSONB — D-13-18); validation exists only at the service boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Elevation of privilege | PUT /organizations/settings | high | mitigate | manager+ gate in the service (role != manager → ErrForbidden → 403, D-13-23) BEFORE any validation or write; asserted for finance role in Task 3 (f); JWT-resolved org from middleware claims — no path-param org spoofing |
| T-13-11 | Tampering | org_settings values | high | mitigate | Known-key validation at the service boundary (unknown key → 400, per-key validator → 400) is the ONLY gate (CHECK on JSONB infeasible — D-13-18); Task 3 asserts unknown-key/invalid-value/negative-hours rejection; settings writes are manager+-gated per T-13-10 |
| T-13-12 | Repudiation | settings changes | medium | mitigate | Every PUT writes the audit row in the SAME tx as the upsert (D-13-22) — a failed upsert rolls back the audit, no silent unlogged change; before/after payload pinned in ADR-BE-018 |
| T-13-13 | Tampering | route collision | medium | mitigate | Literal routes registered beside the typed wildcard (ServeMux most-specific-wins, in-repo precedent); coexistence asserted by the Task 3 literal-route test — the typed surface is proven untouched (Pitfall 6) |

No high-severity unmitigated threats: T-13-10/T-13-11 are mitigated by the service gates and asserted by the test battery. No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go test ./internal/core/services/orgsettings/ ./internal/adapters/secondary/postgres/ ./internal/adapters/primary/http/ -run 'TestOrgSettings|TestOrganizationRepository' -count=1` exits 0
- `go build ./...` compiles (wiring + fixture + all call sites)
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- The org_settings vertical is proven end-to-end: HTTP → validation → tx upsert + in-tx audit → read-back
- The literal routes coexist with the typed wildcard routes (Pitfall 6 closed)
- ResolvePlanningMode is available for the direction service mode gate (13-07)
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-04-SUMMARY.md` when done
</output>
