---
phase: 13-direction-backend-the-plan-plane
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hourglass-vault/decisions/project/ADR-P-015 — Direction, The Plan Plane.md
  - hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md
  - hourglass-vault/decisions/_index.md
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
  - DIR-04
  - DIR-05
  - DIR-06

estimate:
  tokens: 14000
  raw_tokens: 8000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ADR-P-015 exists in the vault decisions folder, drafted from research Part 14/15 (D-R..D-AA + R4 resolution), recording: one direction entity with derived mode, per-day rows with no intra-day ordering, lifecycle draft→active→superseded/cancelled with derived done/lapsed/claimed, WG claim model with hours-based Σ-consumption, org-configurable policy, direction-coverage read-model (DIR-01..06)"
    - "ADR-BE-018 exists, drafted from the ADR-BE-017 template, pinning: status vocabulary draft/active/superseded/cancelled; derived-state vocab done/lapsed/claimed + claim spectrum not_claimed/partially_claimed/fully_claimed; audit vocabulary entity_type='direction' with actions created/activated/cancelled/superseded/claimed/unclaimed and entity_type='org_settings' with action settings-updated + before/after payload (entity_id = org id); settings key vocabulary planning_daily_hours (default 8)/planning_deadline/planning_horizon (day|week|month)/planning_mode (manager_planned|self_planned); claim over-subscription lock (FOR UPDATE on the WG row, in-tx Σ in cents); est_hours DECIMAL(8,2) (A1, A2, A7)"
    - "ADR-BE-018 records the assumption-delta decisions: (1) identity noun = direction row id — 'multiple rows may share a day' is a grouping convention, no UNIQUE constraint, decision no-change on the identity model; (2) origin fallback = add-alongside read-path derivation — stored refs stay authoritative, accepted debt is transient drift between stored refs and derived refs, mitigated by never writing back and by origin_type remaining the discriminator; (3) planning policy = promoted to first-class data (org_settings key/value + membership planning_mode override) — config became a modeling decision (D-13-18/19)"
    - "The vault decisions _index.md gains entries for both new ADRs"
  artifacts:
    - hourglass-vault/decisions/project/ADR-P-015 — Direction, The Plan Plane.md
    - hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md
  key_links:
    - "ADR-BE-018's vocabulary pins are the contract later plans (13-03 domain constants, 13-04 settings keys, 13-05 audit rows) compile against — the domain constants are exported so repo/service/handler can never drift (Patterns A1)"
    - "ADR-P-015 cross-references ADR-P-008 (absence windows + validity consumed by DIR-05) and ADR-P-012 (direction vs coverage-allocation terminology convention)"
  flagged_assumptions:
    - "A1 (audit vocabulary) + A2 (est_hours DECIMAL(8,2)) + A4 (fallback trigger = OriginType == nil) + A5 (WG-scope predicate = same-org AND (activity == anchor OR anchor in ancestry)) + A6 (unit scope = unit + descendants' members) + A7 (settings key vocabulary) + A8 (claim rows draft + queued, copy priority/due_date) + A9 (strict mode-gate reading) + A10 (WG-row creation gate = BE-014 on the anchored activity) are all pinned in ADR-BE-018 as recorded decisions with their alternatives documented"
    - "Probe-unclassified rows for DIR-02..06 (specless fallback) surface as flagged assumptions here and in their implementing plans: DIR-02 activation mechanics (explicit activate endpoint — OQ1), DIR-03 claim-row status/mode (draft + queued — OQ4), DIR-04 mode-gate exclusivity (strict reading — OQ7), DIR-05 lapsed predicate (no non-deleted entries any status — OQ2), DIR-06 period envelope (period_start+period_end pair, no pagination — OQ5)"
  prohibitions:
    - "Never store derived states (done/lapsed/claimed/claim-spectrum) as columns or nightly jobs — computed on read only (D-13-09, D-V)"
    - "Never introduce a dedicated supersede endpoint or in-place edits of planned_date/est_hours — the plan is mutable as a chain of rows via create-with-supersedes_id only (D-13-04, D-13-08)"
    - "Never read only confirmed absence windows — the warning path reads declared+confirmed until Phase 14 tightens (D-13-29)"
---

<objective>
Record the plan plane's two ADRs: ADR-P-015 (the direction plane — project-level) and ADR-BE-018 (the schema/audit/vocabulary encoding — backend-level, from the ADR-BE-017 template), plus the vault index update.

Purpose: Per the milestone convention ("each backend phase drafts its ADR + BE encoding ADR"), every vocabulary/mechanism pin (status matrix, derived-state semantics, claim spectrum, audit vocabulary, settings keys, claim lock, identity model) lands in a recorded decision BEFORE code lands — the domain constants in plan 13-03 and the audit rows in plan 13-05 compile against these pins.
Output: Two ADR files in the vault + _index.md entries.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md
@hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions The Coverage-Allocation Ledger.md
@hourglass-vault/decisions/_index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft ADR-P-015 (Direction — The Plan Plane)</name>
  <files>
    hourglass-vault/decisions/project/ADR-P-015 — Direction, The Plan Plane.md
  </files>
  <read_first>
    .planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md (decisions D-13-01..34 — the record to encode)
    hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions The Coverage-Allocation Ledger.md (project-ADR template + three-plane terminology)
    hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md (consumed by DIR-05 — cross-reference)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (State of the Art lines 590-603; Assumptions Log A1-A10)
  </read_first>
  <action>
    Create ADR-P-015 in the vault decisions/project folder following the ADR-P-012 template structure (Status: Proposed; Context; Decision; Consequences). Content: the direction plane as the third plane of direction → facts → coverage (P-012 convention); one direction entity with mode derived from planned_date (D-R); per-day rows always, partial days first-class, multiple rows may share a day, no intra-day ordering (D-W, D-AA); est_hours semantics per mode (required on scheduled, optional queued budget) with hard per-row validation and soft per-day warnings (D-13-02/03); immutable rows + supersede-only writes, the plan is mutable as a chain (D-13-04/08); lifecycle draft → active → superseded/cancelled with derived done/lapsed/claimed, never stored, no nightly jobs (D-V, D-13-07/09); WG-direction queued-only + hours-based split claims with Σ-consumption, claim = derived row via origin_direction_id (D-T, D-13-11..16); org policy org-configurable (deadline/horizon/mode) stored not enforced (D-X, D-13-18..21); scheduler warning overlay from P-008 windows + employment validity, never blocks (D-Y, D-13-28..31); direction-coverage read-model planned vs capacity per employee/period with uncovered-day surfacing (D-Z, D-13-24..27); origin fallback from first direction record (R4 resolution, D-13-32..34). Cross-reference ADR-P-008 and ADR-P-012. Record the three assumption-delta decisions (identity no-change; fallback add-alongside; planning policy promoted) in the Decision section.
  </action>
  <verify>
    <automated>ls "hourglass-vault/decisions/project/ADR-P-015 — Direction, The Plan Plane.md"</automated>
  </verify>
  <done>ADR-P-015 exists, drafted from the Part 14/15 record of truth with all D-13-01..34 decisions encoded.</done>
  <acceptance_criteria>
    - File "hourglass-vault/decisions/project/ADR-P-015 — Direction, The Plan Plane.md" exists with Status, Context, Decision, Consequences sections
    - The Decision section covers: derived mode, per-day multiplicity, supersede chain, derived states, claim model with Σ-consumption, org policy storage-not-enforcement, warning overlay, coverage read-model, origin fallback
    - The Decision section records the three assumption-delta decisions (identity no-change / fallback add-alongside / policy promoted) with one-line rationales
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Draft ADR-BE-018 (Direction & Org Settings Encoding) + vault index update</name>
  <files>
    hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md
    hourglass-vault/decisions/_index.md
  </files>
  <read_first>
    hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md (the template — status/vocab/audit/settings pin format)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 2 lines 289-304 — audit vocabulary pin; Pattern 7 lines 352-359 — settings key vocabulary; A1/A2/A4-A10)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (audit vocabulary constants section — the exported-constant contract)
    hourglass-vault/decisions/_index.md (entry format)
  </read_first>
  <action>
    Create ADR-BE-018 in the vault decisions/backend folder from the ADR-BE-017 template (Context; Decisions; Schema encoding; Security; Consequences). Pin exactly: (1) status vocabulary draft/active/superseded/cancelled with the matrix (draft→active, draft|active→cancelled, superseded reachable ONLY via create-with-supersedes_id — no transition endpoint); (2) derived-state vocabulary done (terminal-activity CTE re-anchored at activities.id, semantic inversion of the ticket EXISTS), lapsed (past planned_date/due_date AND no non-deleted time entries on the subtree — any status, OQ2/A3), claimed spectrum not_claimed/partially_claimed/fully_claimed (fully only when budget set and Σ == budget — D-13-15); (3) audit vocabulary entity_type='direction' actions created/activated/cancelled/superseded/claimed/unclaimed and entity_type='org_settings' action settings-updated with payload {key, before, after}, entity_id = the org id (audit_logs.entity_id is UUID NOT NULL — 017:18); (4) settings key vocabulary planning_daily_hours (number, default 8.0 — capacity denominator D-13-24), planning_deadline (date), planning_horizon (day|week|month, stored not enforced D-13-21), planning_mode (manager_planned|self_planned, org default + membership override D-13-19); (5) claim over-subscription closure: WG row locked FOR UPDATE, Σ claims in cents (math.Round(h*100) — coverage precedent) re-checked in-tx, 409 ErrClaimOverBudget, uncapped when est_hours NULL (D-13-13/14); (6) est_hours DECIMAL(8,2) mirroring time_entries.hours (A2); (7) settings CRUD: GET/PUT /organizations/settings literal routes, JWT-resolved org, manager+ gate, unknown key → 400 (D-13-23); (8) assumptions A1/A4/A5/A6/A8/A9/A10 pinned as recorded decisions with alternatives (trigger predicate OriginType == nil; WG-scope predicate same-org AND (activity == wg.SubprojectID OR anchor in GetAncestry(activityID)); unit scope = unit + descendants' members; claim rows draft + queued copying priority/due_date; strict mode-gate reading; WG-row creation gate = routing.ResolveManagerStage on the anchored activity). Update hourglass-vault/decisions/_index.md with entries for ADR-P-015 and ADR-BE-018 following the existing entry format.
  </action>
  <verify>
    <automated>grep -c "ADR-BE-018" hourglass-vault/decisions/_index.md</automated>
  </verify>
  <done>ADR-BE-018 exists with all eight pins; _index.md lists both new ADRs.</done>
  <acceptance_criteria>
    - File "hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md" exists
    - ADR-BE-018 contains the exact vocabularies: status values draft/active/superseded/cancelled; audit actions created/activated/cancelled/superseded/claimed/unclaimed + settings-updated; keys planning_daily_hours/planning_deadline/planning_horizon/planning_mode; modes manager_planned/self_planned
    - ADR-BE-018 records the claim lock (FOR UPDATE + in-tx Σ in cents) and the 8 assumption pins (A1/A2/A4/A5/A6/A7/A8/A9/A10)
    - hourglass-vault/decisions/_index.md contains lines referencing both "ADR-P-015" and "ADR-BE-018"
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `hourglass-vault/decisions/project/ADR-P-015 — Direction, The Plan Plane.md` — project ADR (Status: Proposed): direction plane, per-day rows, supersede chain, derived states, claim model, org policy, warnings, coverage read-model, origin fallback + assumption-delta decisions
- `hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md` — BE ADR: status/derived/claim-spectrum/audit/settings vocabularies, claim lock, est_hours scale, route shape, 8 assumption pins
- `hourglass-vault/decisions/_index.md` — two new entries

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR text → later code | no runtime boundary; the risk is vocabulary drift between the recorded pins and the code that implements them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | ADR-BE-018 vocabularies | medium | mitigate | All vocabularies are re-declared as exported Go constants in plan 13-03's domain packages (Patterns: "Exported so repo/service/handler can never drift") — the ADR is the spec, the constants are the compile-time enforcement |
| T-13-06 | Repudiation | audit vocabulary | low | mitigate | action names pinned verbatim (created/activated/cancelled/superseded/claimed/unclaimed/settings-updated) so history reads (Phase 19) filter deterministically |

No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- Both ADR files exist and are self-consistent with 13-CONTEXT.md D-13-01..34
- ADR-BE-018 vocabularies match the domain constants plan (13-03) and the audit inserts plan (13-05) exactly
</verification>

<success_criteria>
- ADR-P-015 + ADR-BE-018 drafted per milestone convention and indexed in the vault
- Every D-13 decision and every research assumption (A1-A10) is recorded with its alternative documented
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-02-SUMMARY.md` when done
</output>
