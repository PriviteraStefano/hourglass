---
phase: 13-direction-backend-the-plan-plane
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/services/direction/direction.go
  - internal/core/services/direction/direction_test.go
  - internal/core/services/testdata/mock_direction_repo.go
  - internal/adapters/primary/http/direction_handler_test.go
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03]
user_setup: []
gap_closure: true
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Supersede-on-create writes TWO audit rows in the same tx: 'created' (entity_id = new row) + 'superseded' (entity_id = flipped target) — CR-02 closed, DIR-02 audit-first via BE-012 restored (port contract direction_repository.go:27-29, ADR-BE-018 §1/§3)"
    - "POST /direction/claims with a user-targeted row id returns 404 with the error envelope — no panic, no dropped connection — CR-01 closed (DIR-03 claim robustness; repo wg_id IS NOT NULL predicate mirrored at the service)"
    - "est_hours > 999999.99 is rejected with ErrInvalidHours -> 400; 999999.99 is accepted; client input never reaches the repo DECIMAL(8,2) overflow (PG 22003) — WR-02 closed (D-13-03 absurd-values pin, T-13-32 never-a-500 boundary contract)"
    - "Phase 13 stays backend-only: the server-emitted contracts (warning objects, error envelope, required-reason destructive writes) are unchanged — 13-UI-SPEC UI Considerations all dismissed, none applicable (lift rule honored)"
  artifacts:
    - internal/core/services/direction/direction.go (audits slice in Create, Claim nil guard, wholeCent ceiling)
    - internal/core/services/direction/direction_test.go (regression subtests for all three fixes)
    - internal/core/services/testdata/mock_direction_repo.go (Claim user-row guard)
    - internal/adapters/primary/http/direction_handler_test.go (superseded-audit DB check, claims-404, est_hours-400 subtests)
  key_links:
    - "Service.Create audits slice -> repo.Create in-tx insertDirectionAudit loop (direction_repository.go:242-246) now carries both rows on supersede"
    - "Service.Claim -> wgRepo.ListMembers is reached only when wg.WgID != nil; the repo's lock predicate (wg_id IS NOT NULL, direction_repository.go:441) is mirrored at the service"
    - "wholeCent -> DECIMAL(8,2) column boundary: the ceiling keeps every accepted value insertable (no 22003)"
  prohibitions:
    - statement: "Supersede-on-create must never drop the 'superseded' audit event for the flipped target — audit-first via BE-012 is the DIR-02 contract"
      status: unverified
      flagged: true
    - statement: "Client input (row ids, est_hours) must never cause a server panic or a 500 response — invalid input maps to 404/400"
      status: unverified
      flagged: true
---

<objective>
Close gaps CR-02 (missing 'superseded' audit row on supersede-on-create, DIR-02),
CR-01 (nil-deref panic in Service.Claim on user-targeted rows, DIR-03) and WR-02
(no upper bound in wholeCent -> DECIMAL(8,2) overflow -> 500, D-13-03/T-13-32) at
the service boundary, each with regression tests at the service unit level AND at
the HTTP boundary (testcontainers integration).

Purpose: the three defects are the critical + first-warning gaps of 13-VERIFICATION.md;
Phase 19 history filters need the 'superseded' audit event, the claims endpoint
must never crash on invalid input, and client input must never produce a 500.

Output: hardened `Service.Create`/`Service.Claim`/`wholeCent` in
`internal/core/services/direction/direction.go`, matching mock hardening, and the
regression batteries in `direction_test.go` + `direction_handler_test.go`.
</objective>

<assumption_delta_decision>
Detector fired on ROADMAP prose terms ("multiple" per-day rows, "fallback" origin
refs, "configurable" policy). Identity-model question: the primary noun remains
the **direction row id** — decision **no-change** (per-day multiplicity is a
grouping convention; already pinned in ADR-BE-018 §9.1 and cycle-tested in
migration 021; origin fallback = add-alongside §9.2; planning policy = first-class
data §9.3). This gap-closure run introduces no identity transition.
</assumption_delta_decision>

## Artifacts this phase produces

New symbols this phase creates or modifies (source-grounding reference for plan review):

- `internal/core/services/direction/direction.go`:
  - `maxEstHours` — NEW file-scoped `const float64 = 999999.99` (DECIMAL(8,2) ceiling, ADR-BE-018 §6)
  - `wholeCent(hours float64) bool` — MODIFIED: adds the `hours <= maxEstHours` upper bound
  - `Service.Create` — MODIFIED: builds a named `audits` slice (created [+ superseded when req.SupersedesID != nil]) handed to `s.repo.Create`
  - `Service.Claim` — MODIFIED: `if wg.WgID == nil { return nil, directiondomain.ErrDirectionNotFound }` guard before `wgRepo.ListMembers`
- `internal/core/services/testdata/mock_direction_repo.go` — `MockDirectionRepo.Claim` MODIFIED: `wg.WgID == nil` -> `ErrDirectionNotFound` (mirrors the repo `wg_id IS NOT NULL` predicate)
- Test symbols (new subtests):
  - `direction_test.go`: TestService_Create supersede-audit subtest, absurd-hours + ceiling subtests; TestService_Claim user-row 404 subtest + 1000000 invalid-hours case
  - `direction_handler_test.go`: TestDirectionHandler subtests — supersede writes both audit_logs rows (pool queries), claims-on-user-row -> 404, est_hours 1000000 -> 400

No new exported API surface, no new endpoints, no new dependencies.

<schema_push_gate>
No schema files in scope (no migrations, no ORM). Go + raw SQL migrations
project — no ORM push command applies. 13-09/13-10 touch no `.up.sql`/`.down.sql`
pairs; the gate is skipped by construction.
</schema_push_gate>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-direction-backend-the-plan-plane/13-VERIFICATION.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-UI-SPEC.md

# The five gap fixes build on the executed 13-01..08 code — read current state, never assume
@internal/core/services/direction/direction.go
@internal/core/services/testdata/mock_direction_repo.go
@internal/core/ports/direction_repository.go
@internal/core/domain/direction/direction.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CR-02 — supersede-on-create writes the 'superseded' audit row (tracer slice: service -> repo tx -> audit_logs)</name>
  <files>
    internal/core/services/direction/direction.go
    internal/core/services/direction/direction_test.go
    internal/adapters/primary/http/direction_handler_test.go
  </files>
  <read_first>
    internal/core/services/direction/direction.go (current Create, lines 199-325 — the audits literal at 302-310)
    internal/core/ports/direction_repository.go (Create contract doc, lines 27-29 and 40-48: "two audit rows: created + superseded")
    internal/core/domain/direction/direction.go (audit vocabulary, lines 155-168: AuditEntityDirection / AuditActionCreated / AuditActionSuperseded)
    internal/adapters/primary/http/direction_handler_test.go (subtest shape at lines 283-305 — the existing supersede chain subtest; h.f.Pool is available for direct SQL)
  </read_first>
  <behavior>
    - Test 1 (service unit): Create with req.SupersedesID set (target seeded draft in the mock) -> f.dirRepo.Audits has EXACTLY 2 rows; Audits[0].Action == AuditActionCreated && Audits[0].EntityID == created.ID; Audits[1].Action == AuditActionSuperseded && Audits[1].EntityID == *req.SupersedesID && Audits[1].EntityType == AuditEntityDirection; both ActorID == actorID; both Payload nil (13-05 repo-test contract)
    - Test 2 (service unit): Create without SupersedesID -> exactly 1 audit row (created) — existing assertion patterns at direction_test.go:182-189 stay green
    - Test 3 (handler integration): POST /direction with supersedes_id -> SELECT COUNT(*) FROM audit_logs WHERE entity_type='direction' AND entity_id=$targetID AND action='superseded' returns 1 AND ... AND entity_id=$newID AND action='created' returns 1 (fixture pool query, mirroring countDirectionAudits at direction_repository_test.go:92)
  </behavior>
  <action>
    In Service.Create (direction.go:302-310) replace the single-element audit literal passed to s.repo.Create with a named slice: always build the 'created' row {OrgID: orgID, EntityType: directiondomain.AuditEntityDirection, EntityID: row.ID, Action: directiondomain.AuditActionCreated, ActorID: &actor, CreatedAt: time.Now().UTC()} (nil Payload); when req.SupersedesID != nil append a SECOND row {OrgID: orgID, EntityType: directiondomain.AuditEntityDirection, EntityID: *req.SupersedesID, Action: directiondomain.AuditActionSuperseded, ActorID: &actor, CreatedAt: time.Now().UTC()} — EntityID is the TARGET row id (the row being flipped to superseded), per the port doc "created + superseded" contract. Pass the slice to s.repo.Create. DO NOT modify the postgres repo: it already iterates the audits slice in-tx (direction_repository.go:242-246) and writes both rows (TestDirectionRepository_Create_Supersede proves it, direction_repository_test.go:209-252).
    In direction_test.go add the two unit subtests under TestService_Create (seed the supersede target as a draft row via f.seedDirectionRow before calling Create with req.SupersedesID; assert against f.dirRepo.Audits). In direction_handler_test.go add a new subtest under TestDirectionHandler: create row1 (200), create row2 with supersedes_id=row1 (200), then assert both audit_logs rows via h.f.Pool queries with action='superseded' (entity_id = row1) and action='created' (entity_id = row2) — the DB check proves the full slice reached the tx. Do NOT alter the existing supersede subtest at lines 283-305.
  </action>
  <verify>
    <automated>go test ./internal/core/services/direction/ -run "TestService_Create" -count=1 && go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - direction.go: s.repo.Create receives an audits slice that is length 2 exactly when req.SupersedesID != nil, with Audits[1].Action == directiondomain.AuditActionSuperseded and Audits[1].EntityID == *req.SupersedesID
    - direction_test.go: the new supersede subtest asserts require.Len(t, f.dirRepo.Audits, 2) with the created-then-superseded ordering and entity ids above
    - direction_handler_test.go: the new subtest's two pool queries return 1 row each (superseded on the target, created on the new row)
    - `go test ./internal/core/services/direction/ -run "TestService_Create" -count=1` exits 0; `go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1` exits 0
  </acceptance_criteria>
  <done>Supersede-on-create writes both audit rows end-to-end (service -> repo tx -> audit_logs); regression tests prove it at unit and HTTP levels.</done>
  <reversibility rating="reversible">Appending an audit row is additive behavior; removing it restores the prior state with no schema impact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-01 — nil-WgID guard in Service.Claim (404, never a panic) + mock mirror</name>
  <files>
    internal/core/services/direction/direction.go
    internal/core/services/direction/direction_test.go
    internal/core/services/testdata/mock_direction_repo.go
    internal/adapters/primary/http/direction_handler_test.go
  </files>
  <read_first>
    internal/core/services/direction/direction.go (Service.Claim, lines 454-489 — the deref at line 462)
    internal/core/services/testdata/mock_direction_repo.go (Claim, lines 162-198)
    internal/adapters/secondary/postgres/direction_repository.go (Claim lock predicate "AND wg_id IS NOT NULL", lines 439-442 — the authoritative shape the service must mirror)
    internal/adapters/primary/http/direction_handler_test.go (ClaimRequest shape at lines 61-66; the non-member claim subtest at 189-207 as the WG-row baseline)
  </read_first>
  <behavior>
    - Test 1 (service unit): Claim on a user-targeted row (DirectedTo set, WgID nil, status active) -> returns ErrDirectionNotFound; a trap installed on f.wgRepo.ListMembers proves ListMembers is NEVER called (no deref path remains)
    - Test 2 (handler integration): create a self-direction row (200), POST /direction/claims with that row's id {"wg_row_id": id, "est_hours": 4} -> 404 envelope decoded (the connection stays up; a panic would fail the json decode and the test)
  </behavior>
  <action>
    In Service.Claim (direction.go:454-465): after the Get + status fast-fail (wg.Status != StatusActive -> ErrWgRowNotActive) and BEFORE s.wgRepo.ListMembers(ctx, *wg.WgID), insert: if wg.WgID == nil { return nil, directiondomain.ErrDirectionNotFound } — this mirrors the repo's lock predicate wg_id IS NOT NULL (direction_repository.go:441), which returns ErrDirectionNotFound for user rows; the service must fail identically instead of dereferencing nil.
    In MockDirectionRepo.Claim (mock_direction_repo.go:162-198): after the map lookup + org check (ErrDirectionNotFound on missing/cross-org) add: if wg.WgID == nil { return nil, directiondomain.ErrDirectionNotFound } — the mock then mirrors the authoritative repo shape and a future unit test can never silently claim a user row through the mock.
    In direction_test.go add the unit subtest under TestService_Claim (seed the user-targeted row via f.seedDirectionRow with WgID nil + StatusActive; install f.wgRepo.ListMembersFn as a trap; assert ErrDirectionNotFound and the trap not set). In direction_handler_test.go add a subtest under TestDirectionHandler: login the owner, POST /direction self-directed (200), then POST /direction/claims with that row id -> require 404 (doJSON decodes the envelope — a server panic would drop the connection and fail the decode).
  </action>
  <verify>
    <automated>go test ./internal/core/services/direction/ -run "TestService_Claim" -count=1 && go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - direction.go: Service.Claim returns ErrDirectionNotFound before any deref when wg.WgID == nil (guard sits between the status fast-fail and ListMembers)
    - mock_direction_repo.go: MockDirectionRepo.Claim returns ErrDirectionNotFound for a stored user-targeted row
    - direction_test.go: the new subtest asserts ErrorIs(err, ErrDirectionNotFound) and that the ListMembers trap never fired
    - direction_handler_test.go: the new claims-on-user-row subtest asserts http.StatusNotFound
    - `go test ./internal/core/services/direction/ -run "TestService_Claim" -count=1` exits 0; `go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1` exits 0
  </acceptance_criteria>
  <done>Any authenticated user can POST /direction/claims with a user-targeted row id and receive 404 — no panic, no dropped connection; the mock mirrors the repo predicate.</done>
  <reversibility rating="reversible">Guard-only change; removing it restores the previous (buggy) behavior with no other impact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-02 — wholeCent upper bound (DECIMAL(8,2) ceiling) so absurd hours are 400, never 500</name>
  <files>
    internal/core/services/direction/direction.go
    internal/core/services/direction/direction_test.go
    internal/adapters/primary/http/direction_handler_test.go
  </files>
  <read_first>
    internal/core/services/direction/direction.go (wholeCent, lines 113-118; its two call sites: Create line 206, Claim line 476)
    internal/adapters/secondary/postgres/direction_repository.go (est_hours DECIMAL(8,2) column insert at lines 213-221; Claim estHours <= 0 fast-fail at 419-423 — the repo boundary has no upper bound)
    internal/adapters/primary/http/direction_handler_test.go (the est_hours 0/sub-cent rejection precedent does not exist at the handler; add the boundary subtest next to the sentinel mapping subtest at lines 262-277)
  </read_first>
  <behavior>
    - Test 1 (service unit): Create with est_hours 1000000 -> ErrInvalidHours (previously passed the gate and died in the repo as PG 22003 -> 500)
    - Test 2 (service unit): Create with est_hours 999999.99 -> succeeds (whole-cent: 999999.99*100 rounds exactly to 99999999 in float64; within the DECIMAL(8,2) ceiling)
    - Test 3 (service unit): Claim with estHours 1000000 -> ErrInvalidHours (wholeCent is shared)
    - Test 4 (handler integration): POST /direction with est_hours 1000000 -> 400 (not 500)
  </behavior>
  <action>
    In direction.go define a file-scoped constant near wholeCent: const maxEstHours = 999999.99 (the DECIMAL(8,2) ceiling per ADR-BE-018 §6) and change wholeCent's return to: hours &gt; 0 && hours &lt;= maxEstHours &amp;&amp; math.Round(hours*100) == hours*100. Both call sites (Create line 206, Claim line 476) inherit the ceiling — no other change needed. Do NOT touch wrapPGError (postgres.go:16-33): with the service ceiling the 22003 path is unreachable for client input; a global 22003 mapping would alter behavior of unrelated repos (time entries).
    In direction_test.go add the three unit subtests: to TestService_Create (est_hours 1000000 -> ErrInvalidHours; est_hours 999999.99 with planned_date -> created != nil), to TestService_Claim (estHours 1000000 -> ErrInvalidHours — extend the existing invalid-hours loop subtest at lines 640-648 with 1000000). In direction_handler_test.go add a subtest: POST /direction self-directed with "est_hours":1000000 -> require 400.
  </action>
  <verify>
    <automated>go test ./internal/core/services/direction/ -run "TestService_Create|TestService_Claim" -count=1 && go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - direction.go: wholeCent rejects hours > 999999.99 (const maxEstHours = 999999.99 present)
    - direction_test.go: Create 1000000 -> ErrInvalidHours; Create 999999.99 succeeds; Claim 1000000 -> ErrInvalidHours
    - direction_handler_test.go: est_hours 1000000 -> http.StatusBadRequest
    - `go test ./internal/core/services/direction/ -run "TestService_Create|TestService_Claim" -count=1` exits 0; `go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1` exits 0
  </acceptance_criteria>
  <done>No client-supplied est_hours value can reach the repo DECIMAL(8,2) overflow; absurd values map to ErrInvalidHours -> 400 at both create and claim.</done>
  <reversibility rating="reversible">A validation boundary change; lowering/raising the ceiling is a one-line edit with no schema impact.</reversibility>
</task>

</tasks>

<flagged_assumptions>
Specless edge-probe (10 items, all "unresolved" — no plain SPEC exists; only UI-SPEC).
Each row surfaced with the accepted answer from the executed implementation + tests
(never a silent drop):

- DIR-01 adjacency: "two rows exactly equal/just touching a day" -> rows neither merge nor collide: per-day multiplicity is the model (D-W/D-AA); no UNIQUE on the (employee, activity, day) tuple; migration 021 cycle test asserts two rows sharing the day both insert (direction_ontology_migrations_test.go).
- DIR-01 empty: single-element/null input -> XOR fast-fail rejects empty targets (both nil or both set -> ErrInvalidTarget, direction.go:201-203); a queued row without est_hours is legal (budget optional, D-AA; test "queued self-row without est_hours is allowed").
- DIR-01 ordering: equal keys -> stable output: ListPlan ORDER BY planned_date ASC NULLS LAST, priority ASC NULLS LAST, due_date ASC NULLS LAST, created_at ASC (direction_repository.go:653; 13-06 ordering test).
- DIR-01 idempotency: running twice -> every create inserts a NEW row (no upsert); supersede chains are append-only; a second supersede of the same target fails in-tx under lock (ErrInvalidTransition, Pitfall 4 test).
- DIR-01 concurrency: parallel supersedes/claims serialize on the target FOR UPDATE lock; the claim Sigma guard bounds over-subscription (TestDirectionClaim_Concurrent battery, direction_repository_test.go).
- DIR-02..06 unclassified: manual review found no additional spec-less edge in this gap-closure scope; behaviors are pinned by ADR-P-015/ADR-BE-018 and the executed 13-01..08 suites (all green in 13-VERIFICATION.md spot-checks).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | untrusted JSON: est_hours values, row ids, directed_to ids cross here (T-13-32 boundary contract) |
| API -> service -> repo | user-targeted rows reach the claim path; supersede targets carry audit obligations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13g-01 | DoS | Service.Claim nil deref (direction.go:462) | high | mitigate | nil-WgID guard -> ErrDirectionNotFound before ListMembers (Task 2); regression 404 test at the HTTP boundary |
| T-13g-02 | Integrity | Service.Create supersede audit (direction.go:303-310) | high | mitigate | append the 'superseded' audit row in the same slice/tx (Task 1); DB-level regression asserts the row exists |
| T-13g-03 | DoS | wholeCent -> DECIMAL(8,2) overflow 22003 -> 500 (direction.go:116-118) | medium | mitigate | maxEstHours = 999999.99 ceiling maps absurd input to ErrInvalidHours -> 400 (Task 3) |
| T-13g-04 | Tampering | directed_to cross-org refs (Create gate) | high | mitigate | PLAN 13-10 Task 1: orgRepo.GetMembership active-member check before the mode gate (ADR-BE-018 §Security) |
| T-13g-05 | Repudiation | Unclaim audit action drift ('cancelled' vs pinned 'unclaimed') | medium | mitigate | PLAN 13-10 Task 2: Unclaim writes AuditActionUnclaimed; port doc + repo test aligned to ADR-BE-018 §3 |

No third-party package installs in this gap-closure scope — package legitimacy gate (T-*-SC) N/A.
</threat_model>

<verification>
- `go test ./internal/core/services/direction/ -count=1` — full service package (existing 8 tests + new regressions)
- `go test ./internal/adapters/primary/http/ -run "TestDirectionHandler" -count=1` — HTTP boundary incl. the three new subtests (testcontainers Postgres)
- `go test ./internal/adapters/secondary/postgres/ -run "TestDirectionRepository" -count=1` — repo mutator suite stays green (no repo change; proves the contract compatibility)
- `go build ./...` and `go vet ./internal/core/services/direction/ ./internal/adapters/primary/http/ ./internal/adapters/secondary/postgres/` exit 0
</verification>

<success_criteria>
- CR-02: supersede-on-create writes both 'created' + 'superseded' audit rows in one tx (service slice -> repo in-tx loop); Phase 19 history filters on 'superseded' return real events
- CR-01: POST /direction/claims with a user-targeted row id returns 404 — no panic, connection stays up; mock mirrors the repo predicate
- WR-02: est_hours >= 1,000,000 maps to 400 (ErrInvalidHours) at create AND claim; 999999.99 accepted; no 22003 path reachable from client input
- All three fixes carry service-unit AND HTTP-boundary regressions; full suite green
- 13-UI-SPEC UI Considerations: none applicable (backend-only) — no UI truth dropped (see must_haves.truths row 4)
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-09-SUMMARY.md` when done
</output>
