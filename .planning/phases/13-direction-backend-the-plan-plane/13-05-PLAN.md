---
phase: 13-direction-backend-the-plan-plane
plan: 05
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-03
files_modified:
  - internal/adapters/secondary/postgres/direction_repository.go
  - internal/adapters/secondary/postgres/direction_repository_test.go
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03

estimate:
  tokens: 38000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "direction_repository.go implements the mutator half of ports.DirectionRepository: Create (plain insert + supersede-on-create in ONE tx: lock the supersedes target SELECT ... FOR UPDATE, re-check status draft|active in-tx (CR-01), INSERT the new row, UPDATE the target to 'superseded' with a status-precondition backstop, write BOTH the created and superseded audit rows in the same tx, commit), Activate, Cancel (reason required — the repo refuses nil/empty reason), Claim, Unclaim-by-cancel (D-13-04, D-13-07, D-13-08, D-13-10)"
    - "Claim tx: lock the WG row `SELECT est_hours, status, directed_by FROM direction WHERE id=$1 AND org_id=$2 AND wg_id IS NOT NULL FOR UPDATE`; in-tx re-checks: WG row active (ErrWgRowNotActive), claimant is a WG member (working_group_members SQL join), claimed estHours > 0; when the WG budget is set, Σ claimed in CENTS (math.Round(h*100)) via `SELECT COALESCE(SUM(est_hours),0) FROM direction WHERE origin_direction_id=$1 AND status <> 'cancelled'` re-checked in-tx — over budget → ErrClaimOverBudget (409); uncapped when est_hours IS NULL (D-13-12, D-13-13, D-13-14, CR-01)"
    - "Claim row insert: directed_by = the WG row's creator (attribution preserved, D-13-11), directed_to = claimant, origin_direction_id = WG row id, same activity_id, est_hours = claimed amount, status 'draft', planned_date NULL (queued), priority + due_date copied from the WG row (A8/OQ4); claimed audit row written in the same tx"
    - "Cancel: `SELECT status FROM direction WHERE id=$1 AND org_id=$2 FOR UPDATE`; in-tx re-check draft|active via the domain matrix (ErrInvalidTransition otherwise); UPDATE with status-precondition backstop; cancelled audit row in-tx. Unclaim = Cancel on a claim row (origin_direction_id IS NOT NULL) with the same reason requirement (D-13-16)"
    - "Every mutator uses BeginTx + defer Rollback + in-tx insertDirectionAudit (coverage insertAudit shape, caller-controlled entity_type) + Commit — no fire-and-forget audit (BE-012); scanDirectionRow normalizes the 7 nullable columns (planned_date/est_hours/priority/due_date/supersedes_id/origin_direction_id/reason)"
    - "Idempotency edge (probe DIR-01): a second create with the same supersedes_id fails in-tx with ErrInvalidTransition (target already superseded); a second activate on an active row fails with ErrInvalidTransition — no silent double-transition (Pitfall 4)"
    - "Concurrency edge (probe DIR-01): the concurrent-claims test mirrors ticket_repository_test.go:418-506 — N goroutines claim the last budget hours; exactly the budget-bounded set commits; over-subscription never commits (CR-01 closure)"
  artifacts:
    - internal/adapters/secondary/postgres/direction_repository.go
    - internal/adapters/secondary/postgres/direction_repository_test.go
  key_links:
    - "The Create tx is the only supersede channel (D-13-08) — there is no UpdateState/transition endpoint to superseded; the in-tx re-check + status-precondition UPDATE backstop close the CR-01 TOCTOU (ticket_repository.go:360-378 pattern)"
    - "The Claim Σ guard re-runs INSIDE the mutator tx under the WG-row FOR UPDATE lock — pool-level checks are fast-fail UX only; the lock serializes concurrent claims (Pitfall 1)"
    - "direction_repository.go is the same file the read-models plan (13-06) extends — this plan owns the file for its wave; 13-06 follows"
  flagged_assumptions:
    - "Claim requires the WG row to be 'active' (not draft) — the pinned reading of D-13-07/D-13-13 (research Common Operation 2), recorded in ADR-BE-018"
    - "Superseding a row already superseded → ErrInvalidTransition (chain rewrite blocked, Pitfall 4); a cancelled row is also not supersedable"
    - "Claim rows land in 'draft' + queued mode copying priority/due_date (A8, OQ4 — the claimant schedules via the normal supersede chain)"
  prohibitions:
    - "Never rewrite the plan fact: no in-place UPDATE of planned_date/est_hours exists anywhere in this repo — replanning is create-with-supersedes_id only; superseded/cancelled rows are never updated or deleted (D-13-04, D-13-08)"
---

<objective>
The correctness layer of the direction plane: the postgres repository's mutator transactions — create (plain + supersede-on-create), activate, cancel, claim (with the Σ-consumption lock), unclaim — all with in-tx audit rows per BE-012 and the CR-01 FOR UPDATE re-validation closure.

Purpose: The lifecycle matrix (D-13-07) and the claim over-subscription invariant (D-13-13) are enforced repo-authoritatively inside transactions — the same closure that fixed TICK-02/TICK-04's TOCTOU in Phase 11. Pool-level checks (service, 13-07) are fast-fail UX only.
Output: direction_repository.go (mutators) + integration tests incl. the concurrent-claim battery.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@internal/adapters/secondary/postgres/ticket_repository.go
@internal/adapters/secondary/postgres/coverage_repository.go
@internal/core/domain/direction/direction.go
@internal/core/ports/direction_repository.go
@internal/adapters/secondary/postgres/ticket_repository_test.go
@internal/adapters/secondary/postgres/exported_test_helpers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo foundation — scan helper, audit helper, Create with supersede-on-create tx</name>
  <files>
    internal/adapters/secondary/postgres/direction_repository.go
    internal/adapters/secondary/postgres/direction_repository_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/ticket_repository.go lines 27-65 (struct + assertion + scanTicketRow nullable-normalization), 239-305 (Dismiss/UpdateState BeginTx + FOR UPDATE + in-tx re-check + backstop + audit skeleton)
    internal/adapters/secondary/postgres/coverage_repository.go lines 45-70 (insertCoverageAudit — copy shape as insertDirectionAudit)
    internal/core/domain/direction/direction.go (matrix + audit constants)
    internal/core/ports/direction_repository.go (pinned signatures)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 3 lines 306-319; Common Operation 1 lines 469-490; Pitfall 4)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (repo analog lines 295-403)
  </read_first>
  <action>
    Create internal/adapters/secondary/postgres/direction_repository.go: struct {pool *pgxpool.Pool} + var _ ports.DirectionRepository = (*DirectionRepository)(nil) + NewDirectionRepository(pool). Implement scanDirectionRow (normalizes the 7 nullable columns into locals then assigns — ticket scan shape) + insertDirectionAudit (copy of insertCoverageAudit — caller-controlled EntityType) + a getByIDTx helper used after mutators (SELECT ... WHERE id=$1 AND org_id=$2 inside the caller tx).

    Create(ctx, orgID, d *direction.Direction, supersedesID *uuid.UUID, audits []*audit.AuditLog) (*direction.Direction, error): BeginTx + defer Rollback. If supersedesID != nil: SELECT status FROM direction WHERE id=$1 AND org_id=$2 FOR UPDATE → pgx.ErrNoRows → ErrDirectionNotFound; status not draft|active → ErrInvalidTransition (in-tx authoritative — CR-01). INSERT the new row (all D-13-01 columns; status defaults 'draft' — the caller may pass d.Status for claim rows which also come through... note: claim rows use the Claim method, not Create). UPDATE direction SET status='superseded', updated_at=NOW() WHERE id=$1 AND status=$2 (status-precondition backstop — 0 rows affected → ErrInvalidTransition). Write EVERY audit in audits (created + superseded) in-tx. Commit. Return the created row (re-read via getByIDTx).

    Integration tests (direction_repository_test.go, testcontainers + SetupTestSchema + TeardownTestSchema + seedDirectionRow/seedOrg/seedUser/seedActivity): (a) plain Create stores + returns the row with draft status; (b) Create with supersedesID: target flips to superseded in the same tx, both audit rows exist (entity_type='direction', actions created + superseded), the new row's supersedes_id points at the target; (c) create-with-supersedes on an ALREADY superseded target → ErrInvalidTransition (idempotency edge, probe DIR-01); (d) create-with-supersedes on a cancelled target → ErrInvalidTransition; (e) Create with target in another org → ErrDirectionNotFound (org-scoped lock).
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestDirectionRepository_Create|TestDirectionRepository_Supersede' -count=1</automated>
  </verify>
  <done>Create + supersede-on-create proven in one tx with audit rows and the CR-01 re-check; chain-rewrite attempts rejected.</done>
  <acceptance_criteria>
    - direction_repository.go compiles against ports.DirectionRepository (var _ assertion)
    - The supersede tx locks the target FOR UPDATE, re-checks draft|active in-tx, writes created+superseded audit rows, and the target's status is 'superseded' after commit
    - Second supersede of the same target returns ErrInvalidTransition (no chain rewrite)
    - Cross-org supersede target returns ErrDirectionNotFound
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Activate + Cancel txs (matrix re-validation under lock, reason required, audit)</name>
  <files>
    internal/adapters/secondary/postgres/direction_repository.go
    internal/adapters/secondary/postgres/direction_repository_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/ticket_repository.go lines 239-305 (the state-update tx skeleton this task copies twice)
    internal/core/domain/direction/direction.go (CanTransition/IsTerminalStatus)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 2 lines 289-304; Pitfall 4)
  </read_first>
  <action>
    Implement Activate(ctx, orgID, id uuid.UUID, audit *audit.AuditLog): BeginTx; SELECT status FROM direction WHERE id=$1 AND org_id=$2 FOR UPDATE; ErrNoRows → ErrDirectionNotFound; !CanTransition(status, StatusActive) → ErrInvalidTransition (in-tx); UPDATE direction SET status='active', updated_at=NOW() WHERE id=$1 AND status=$2 (backstop; 0 rows → ErrDirectionNotFound); insert audit (action 'activated') in-tx; Commit; re-read via getByIDTx.

    Implement Cancel(ctx, orgID, id uuid.UUID, reason string, audit *audit.AuditLog): reason empty → ErrCancelReasonRequired (fast-fail at the repo boundary — the DB CHECK is the second line); BeginTx; SELECT status ... FOR UPDATE; !CanTransition(status, StatusCancelled) → ErrInvalidTransition; UPDATE ... SET status='cancelled', reason=$reason ...; insert audit (action 'cancelled', payload {"reason": reason}) in-tx; Commit; re-read.

    Tests: (a) draft → activate → active, audit row 'activated' written; (b) activate twice → second returns ErrInvalidTransition (idempotency edge); (c) activate a cancelled row → ErrInvalidTransition; (d) draft → cancel with reason → cancelled + audit with reason payload; (e) cancel with empty reason → ErrCancelReasonRequired; (f) cancel an already-cancelled row → ErrInvalidTransition; (g) cross-org id → ErrDirectionNotFound.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestDirectionRepository_Activate|TestDirectionRepository_Cancel' -count=1</automated>
  </verify>
  <done>Activate/Cancel enforce the matrix under FOR UPDATE with the backstop; reason-less cancel rejected; audits written in-tx.</done>
  <acceptance_criteria>
    - Activate only permits draft→active; Cancel only draft|active→cancelled with non-empty reason — both re-validated against the LOCKED row with a status-precondition UPDATE backstop
    - Audit rows exist with actions 'activated'/'cancelled' (cancelled carries the reason payload)
    - Terminal rows reject further transitions (ErrInvalidTransition)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Claim tx (WG-row lock + Σ cents guard + membership re-check) + Unclaim + concurrent battery</name>
  <files>
    internal/adapters/secondary/postgres/direction_repository.go
    internal/adapters/secondary/postgres/direction_repository_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/coverage_repository.go lines 105-178 (ReplaceAllocations — FOR UPDATE + in-tx Σ cents + audit shape)
    internal/adapters/secondary/postgres/ticket_repository_test.go lines 414-509 (the concurrent battery template to mirror)
    internal/core/domain/direction/errors.go (ErrClaimOverBudget/ErrNotWgMember/ErrWgRowNotActive)
    migrations/014_ticket_schema.up.sql + migrations/000_full_schema.up.sql (working_group_members table name + columns for the in-tx membership check)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 5 lines 329-339; Common Operation 2 lines 492-524; Pitfall 1)
  </read_first>
  <action>
    Implement Claim(ctx, orgID, wgRowID, claimantID uuid.UUID, estHours float64, audit *audit.AuditLog): estHours <= 0 → ErrInvalidHours (fast-fail). BeginTx; SELECT est_hours, status, directed_by, activity_id, priority, due_date, wg_id FROM direction WHERE id=$1 AND org_id=$2 AND wg_id IS NOT NULL FOR UPDATE → ErrNoRows → ErrDirectionNotFound; status != 'active' → ErrWgRowNotActive; membership re-check in-tx: SELECT EXISTS (SELECT 1 FROM working_group_members WHERE working_group_id=$1 AND user_id=$2) → false → ErrNotWgMember (D-13-12 — authoritative in-tx); if est_hours IS NOT NULL (budget set): Σ claimed in cents — SELECT COALESCE(SUM(est_hours),0)::numeric FROM direction WHERE origin_direction_id=$1 AND status <> 'cancelled' — read as numeric string and roundCents-style compare: claimedCents + round(estHours*100) > round(budget*100) → ErrClaimOverBudget (D-13-13, CR-01); INSERT claim row: directed_by = locked directed_by (attribution, D-13-11), directed_to = claimantID, wg_id NULL, activity_id = locked activity_id, planned_date NULL (queued-only), est_hours = estHours, priority + due_date copied from the locked WG row (A8), status 'draft'; insert audit (action 'claimed', payload {wg_row_id, est_hours}); Commit; re-read.

    Implement Unclaim via Cancel with the reason requirement (D-13-16) — Cancel already enforces reason + matrix on claim rows (origin_direction_id IS NOT NULL rows are draft — the matrix draft→cancelled covers it); the service (13-07) enforces "unclaim only on claim rows" as fast-fail, and the repo Cancel additionally accepts an optional guard: if the row's origin_direction_id IS NULL → ErrInvalidRequest when called through the unclaim path (use a dedicated Unclaim(ctx, orgID, claimRowID, reason, audit) wrapper method on the repo that reuses the cancel tx internals with that guard).

    Concurrent battery (mirror ticket_repository_test.go:418-506): seed one WG row with est_hours = 8.00 and N=5 members; start channel + buffered results; 5 goroutines each claim 2.00h; collect outcomes; assert exactly the commits that keep Σ ≤ 8.00 (first-wins determinism: 4 succeed + 1 ErrClaimOverBudget when all are serialized by the lock — assert Σ claimed in the table == 8.00 and the number of claim rows == 4, i.e. over-subscription never commits). Also: claim on a row with NULL budget — two 10h claims both succeed (uncapped, D-13-14); unclaim returns hours — claim 8h, unclaim with reason, claim 8h again succeeds (Σ-derived return, D-13-16).
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestDirectionClaim_Concurrent|TestDirectionRepository_Claim|TestDirectionRepository_Unclaim' -count=1</automated>
  </verify>
  <done>Claim/unclaim proven with the Σ guard under the WG-row lock; the concurrent battery shows over-subscription never commits.</done>
  <acceptance_criteria>
    - The claim tx locks the WG row FOR UPDATE and re-checks status/membership/Σ in-tx — pool-level nothing is authoritative
    - Σ claimed (status <> 'cancelled' claim rows) never exceeds the WG budget in any committed state — the concurrent test asserts Σ == 8.00 with exactly 4 claim rows
    - Uncapped claims (NULL budget) both succeed; unclaim frees hours (re-claim succeeds)
    - Claim audit rows (action 'claimed') and unclaim audit rows (action 'cancelled' with reason) exist in-tx
    - Non-member claimant → ErrNotWgMember; claim on a draft WG row → ErrWgRowNotActive
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/adapters/secondary/postgres/direction_repository.go` — `DirectionRepository` (mutators): `Create` (supersede-on-create tx), `Activate`, `Cancel`, `Claim` (Σ guard under FOR UPDATE), `Unclaim`; helpers `scanDirectionRow`, `insertDirectionAudit`, `getByIDTx`
- `internal/adapters/secondary/postgres/direction_repository_test.go` — integration tests: create/supersede chain + idempotency, activate/cancel matrix, claim/unclaim Σ semantics, `TestDirectionClaim_Concurrent` battery

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → repo mutator | the tx boundary where check-then-act races live (CR-01) |
| DB → audit_logs | in-tx append-only event writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-14 | Tampering | claim over-subscription race | high | mitigate | WG row locked SELECT ... FOR UPDATE; Σ claims re-computed in CENTS in-tx under the lock; over budget → ErrClaimOverBudget (409) — the CR-01 closure (Pitfall 1); proven by the concurrent battery asserting Σ == budget with bounded commits |
| T-13-15 | Tampering | lifecycle bypass | high | mitigate | Every mutator re-validates the matrix against the LOCKED row + a status-precondition UPDATE backstop (0 rows → error); supersede targets re-checked draft\|active in-tx; terminal rows immutable (Pitfall 4); asserted in Tasks 1-2 tests |
| T-13-16 | Tampering | chain rewrite / plan-fact rewrite | high | mitigate | No in-place UPDATE of planned_date/est_hours exists anywhere; the only mutation channels are activate/cancel/supersede-flip with the status precondition; superseded/cancelled rows are never targets again (ErrInvalidTransition) |
| T-13-17 | Repudiation | audit loss | medium | mitigate | All audit rows (created/activated/cancelled/superseded/claimed) are written IN THE SAME tx as the state change — rollback on audit failure; no fire-and-forget (BE-012) |
| T-13-18 | Spoofing | cross-org mutation | medium | mitigate | Every lock/re-check query is scoped AND org_id=$N; cross-org ids return ErrDirectionNotFound (no existence oracle) |

No high-severity unmitigated threats: T-13-14/15/16 are mitigated in-tx and proven by the test battery. No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestDirection' -count=1` exits 0
- `go build ./...` compiles
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- Supersede-on-create, activate, cancel, claim, unclaim all execute as single transactions with in-tx audit rows
- The concurrent-claims battery proves over-subscription never commits (CR-01 closure)
- No update/delete surface exists for plan facts — the chain is append-only
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-05-SUMMARY.md` when done
</output>
