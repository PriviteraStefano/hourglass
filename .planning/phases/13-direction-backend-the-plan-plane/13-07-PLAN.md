---
phase: 13-direction-backend-the-plan-plane
plan: 07
type: execute
wave: 4
depends_on:
  - 13-04
  - 13-05
  - 13-06
files_modified:
  - internal/core/services/direction/direction.go
  - internal/core/services/direction/direction_test.go
  - internal/core/services/testdata/mock_direction_repo.go
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
  - DIR-05
  - DIR-06

estimate:
  tokens: 40000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Create implements the D-13-20 mode gate + D-S self-direction + BE-014 manager gate: self-direction (directed_to == actor) allowed per the resolved mode; manager-planned targets → routing.ResolveManagerStage(ctx, orgID, activityID, unitID, directed_to) with the coverage gate shape (RoleGated → role must be manager; else actor in ApproverIDs) — unitID = the target employee's primary unit via unitRepo.ListMembershipsForUser (strict reading, A9/OQ7); service fast-fails the XOR target shape, est_hours <= 0 or non-whole-cent → ErrInvalidHours, same-org activity check (D-13-03, D-13-05)"
    - "WG-row create (wg_id set): queued-only enforced by the schema; service fast-fails planned_date set on a WG row (D-13-17); WG-scope predicate: activity same-org AND (activity == wg.SubprojectID OR wg.SubprojectID in activityRepo.GetAncestry(activityID)) (A5, Pitfall 9); creation gate = routing.ResolveManagerStage on the WG's ANCHORED activity (A10/OQ8)"
    - "Create with supersedes_id → the repo supersede tx; the service fast-fails supersede of a row the pool-read shows non-draft/non-active (repo re-check is authoritative — CR-01); create returns the row + computed warnings (D-13-03)"
    - "Activate/Cancel orchestrate the repo mutators with pool-level matrix fast-fail (UX only — the repo re-validates under lock); Cancel requires a reason (ErrCancelReasonRequired before the repo call — D-13-10)"
    - "Claim orchestration: WG row must be active + claimant is a WG member (wgRepo.ListMembers) + estHours > 0 as pool-level fast-fail (D-13-12/13 — the repo's in-tx re-check under FOR UPDATE is authoritative); Unclaim is Cancel-on-claim-row with reason (D-13-16)"
    - "The service computes warnings as a pure function over repo data: per (employee, day) — away (full absence window covers the day), partial (partial-day permit), over-capacity (Σ est_hours > capacity), invalid (day outside membership valid_from/valid_until via orgRepo.GetMembership); message pre-rendered server-side as {Type} {date-range-or-day} (\"Away 10–21 Aug\", \"Partial 14 Aug\", \"Over capacity 16 Aug\", \"Outside validity period\") per the 13-UI-SPEC contract — warnings never block writes (D-13-28, D-13-30, D-13-31)"
    - "Coverage assembly: resolve the employee set by scope — employee (scope_id is the employee), unit (unitRepo.GetDescendants + ListMembers for the unit AND descendants — A6/OQ6), wg (wgRepo.ListMembers); split validity-outside employees (orgRepo.GetMembership valid_from/valid_until) — they get the invalid warning and are excluded from uncovered surfacing (D-13-31); repo.Coverage over the valid set; fully-absent days (capacity 0) excluded from uncovered surfacing; response = rows + period totals + warnings (D-13-25, D-13-26)"
  artifacts:
    - internal/core/services/direction/direction.go
    - internal/core/services/direction/direction_test.go
  key_links:
    - "The mode gate consumes orgsettings.Service.ResolvePlanningMode (13-04) — override → org default → manager_planned; the service constructor takes orgsettingsSvc + routingSvc, mirroring the coverage service's routing dependency (D-G parity)"
    - "Warnings ride BOTH the create response and the read-model responses (D-13-03, D-13-28) — the pure warning fn is shared, never duplicated"
    - "The read-model response shape (rows + totals + warnings) is the Phase 19 contract from 13-UI-SPEC — field names pinned in the domain structs (13-03)"
  flagged_assumptions:
    - "Strict mode gate (A9/OQ7): manager-planned → only managers create for that employee; self-planned → only the employee creates own rows. If a hybrid (self-direction always allowed) is wanted later, it is an additive loosening — recorded in ADR-BE-018"
    - "Warning message formats pinned verbatim: away = 'Away {date-range-or-day}', partial = 'Partial {day}', over-capacity = 'Over capacity {day}', invalid = 'Outside validity period' (13-UI-SPEC Copywriting Contract — Phase 19 renders message verbatim)"
    - "The create gate resolves the target employee's unit via ListMembershipsForUser primary membership; when the employee has no membership, the routing fallback degrades to the role-gated terminal resolution (proposer pattern, house style)"
  prohibitions:
    - "Never block a write on warnings, deadlines, horizons, or over-capacity — the backend never enforces soft policy (D-13-03, D-13-20, D-13-21, D-13-28, D-X parked)"
---

<objective>
The direction service: create orchestration (mode gate + BE-014 routing gate + WG-scope predicate + validation), lifecycle orchestration (activate/cancel with matrix fast-fail), claim orchestration (membership + Σ fast-fail feeding the authoritative repo tx), the warning pure-function overlay, and the coverage read-model assembly.

Purpose: The service owns all invariants and gates (D-13-03/05/12/17/20/28..31); the repo owns the transactions. This is where permission policy, soft-warning computation, and the DIR-06 scope resolution meet.
Output: direction service + unit tests against the testdata mocks.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@internal/core/services/ticket/ticket.go
@internal/core/services/coverage/coverage.go
@internal/core/services/orgsettings/orgsettings.go
@internal/core/services/routing/routing.go
@internal/core/domain/direction/direction.go
@internal/core/domain/direction/errors.go
@internal/core/ports/direction_repository.go
@internal/core/services/testdata/mock_direction_repo.go
@internal/core/services/testdata/mock_org_settings_repo.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create orchestration — mode gate, routing gate, WG-scope predicate, validation, warnings-in-response</name>
  <files>
    internal/core/services/direction/direction.go
    internal/core/services/direction/direction_test.go
  </files>
  <read_first>
    internal/core/services/coverage/coverage.go lines 340-369 (whole-cent rejection + D-08 routing gate shape to copy), 46-64 (constructor shape)
    internal/core/services/routing/routing.go lines 57-104 (ResolveManagerStage — how unitID feeds the fallback)
    internal/core/services/orgsettings/orgsettings.go (ResolvePlanningMode signature from 13-04)
    internal/core/domain/direction/direction.go + errors.go (matrix + sentinels)
    internal/core/domain/working_group/working_group.go (SubprojectID anchor)
    internal/core/ports/unit_repository.go (ListMembershipsForUser for the primary-unit resolution)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 3 lines 306-319; OQ7/OQ8; A9/A10)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (service analog lines 151-215)
  </read_first>
  <action>
    Build internal/core/services/direction/direction.go: Service{repo ports.DirectionRepository, activityRepo ports.ActivityRepository, wgRepo ports.WorkingGroupRepository, unitRepo ports.UnitRepository, orgRepo ports.OrganizationRepository, orgSettings *orgsettings.Service, routing *routing.Service} + NewService with those deps (coverage constructor shape).

    Create(ctx, orgID, actorID uuid.UUID, role string, req *CreateDirectionRequest) (*direction.Direction, []direction.Warning, error) where CreateDirectionRequest mirrors the D-13-01 write fields (DirectedTo *uuid.UUID, WgID *uuid.UUID, ActivityID uuid.UUID, PlannedDate *time.Time, EstHours *float64, Priority *int, DueDate *time.Time, SupersedesID *uuid.UUID). Order: (1) XOR fast-fail — exactly one of DirectedTo/WgID set, else ErrInvalidTarget (D-13-05); (2) est_hours validation — when set: > 0 AND whole-cent (math.Round(h*100) == h*100 — coverage pattern) else ErrInvalidHours (D-13-03); scheduled shape — PlannedDate set requires EstHours set, else ErrInvalidHours (D-13-02); (3) same-org activity: activityRepo.Get → OrgID != orgID → ErrInvalidRequest (D-02 pattern); (4) WG rows: PlannedDate set → ErrInvalidRequest (queued-only, D-13-17); wgRepo.GetByID → same-org + scope predicate (activity == wg.SubprojectID OR wg.SubprojectID in activityRepo.GetAncestry(activityID)) else ErrInvalidRequest (A5, Pitfall 9); (5) permission gate: if DirectedTo != nil: if *DirectedTo == actorID → self-direction allowed subject to the mode gate: mode, err := s.orgSettings.ResolvePlanningMode(ctx, orgID, *DirectedTo); if mode == ModeManagerPlanned → the actor must still pass the routing gate below (manager creates for the employee — strict reading A9); if mode == ModeSelfPlanned AND actor != *DirectedTo → ErrForbidden. Manager path (DirectedTo != actor OR WG rows): unitID := primary unit of DirectedTo via unitRepo.ListMembershipsForUser (fallback uuid.Nil — routing degrades to role-gated); res, err := s.routing.ResolveManagerStage(ctx, orgID, activityID, unitID, directedTo); gate shape verbatim from coverage: !res.RoleGated && !contains(res.ApproverIDs, actor) → ErrForbidden; res.RoleGated && role != string(models.RoleManager) → ErrForbidden. WG rows use the ANCHORED activity (wg.SubprojectID) for the routing call (A10). Self-direction with mode self_planned → no routing call (D-S — no approval); (6) audit DTOs built service-side (ticket pattern): created audit with payload {mode, planned_date, est_hours, supersedes_id}; (7) repo.Create (supersedesID passed when set — plus the superseded audit row built service-side); (8) warnings := s.computeWarnings(ctx, orgID, employee, plannedDate) (Task 3's pure fn) — returned with the row (D-13-03); warnings NEVER cause rejection.

    Unit tests (direction_test.go with MockDirectionRepo/MockOrgSettingsRepo + seeded activity/WG/unit mocks): self-direction self_planned mode → Create succeeds without a routing call (assert via mock routing absence or approver-set empty); self-direction in manager_planned mode → passes only when the actor is in the approver set / manager role; employee creating for a colleague in self_planned mode → ErrForbidden; XOR violation → ErrInvalidTarget; est_hours 0 / negative / 1.005 → ErrInvalidHours; scheduled without est_hours → ErrInvalidHours; WG row with planned_date → ErrInvalidRequest; WG row for an activity outside the WG scope → ErrInvalidRequest; supersede target already superseded (mock pool-read) → ErrInvalidTransition fast-fail; cross-org activity → ErrInvalidRequest; create returns warnings slice (empty-warning happy path).
  </action>
  <verify>
    <automated>go test ./internal/core/services/direction/ -run TestService_Create -count=1</automated>
  </verify>
  <done>Create enforces XOR/hours/mode/routing/WG-scope gates in the right order, delegates to the repo tx, returns warnings without blocking.</done>
  <acceptance_criteria>
    - The gate order is: XOR → hours → same-org → WG-scope → mode → routing (coverage shape verbatim)
    - Self-direction self_planned requires NO routing call; manager_planned self-direction passes only via approver-set/role
    - WG-row creation routes on the anchored activity and enforces the scope predicate
    - Every validation failure maps to its sentinel (ErrInvalidTarget/ErrInvalidHours/ErrInvalidRequest/ErrForbidden/ErrInvalidTransition)
    - The response carries the created row + warnings; warnings never reject
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lifecycle + claim orchestration (activate/cancel/claim/unclaim)</name>
  <files>
    internal/core/services/direction/direction.go
    internal/core/services/direction/direction_test.go
  </files>
  <read_first>
    internal/core/services/ticket/ticket.go lines 200-246 (pool-level fast-fail pattern + audit DTO building)
    internal/core/domain/direction/direction.go (matrix)
    internal/core/ports/working_group_repository.go (ListMembers for the membership fast-fail)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 2, Pattern 5)
  </read_first>
  <action>
    Activate(ctx, orgID, actorID, role string, id uuid.UUID) (*direction.Direction, error): pool-read the row (repo via a Get method — add Get(ctx, orgID, id) to the repo if not already present; use getByIDTx or a pool Get), fast-fail CanTransition(status, active) → ErrInvalidTransition (the repo re-validates under lock — CR-01); permission: actor must be the row's DirectedBy (creator) OR pass the manager gate (routing on the row's activity — managers may activate); build the activated audit; repo.Activate.

    Cancel(ctx, orgID, actorID, role string, id uuid.UUID, reason string) (*direction.Direction, error): reason empty → ErrCancelReasonRequired (D-13-10); pool-read + matrix fast-fail; permission same shape as Activate (creator or manager gate); cancelled audit with {reason}; repo.Cancel.

    Claim(ctx, orgID, actorID uuid.UUID, role string, wgRowID uuid.UUID, estHours float64) (*direction.Direction, error): pool fast-fails: WG row exists + is active (ErrWgRowNotActive) + wgRepo.ListMembers contains actorID (ErrNotWgMember — D-13-12) + estHours > 0 (ErrInvalidHours); build claimed audit {wg_row_id, est_hours}; repo.Claim (the in-tx Σ + lock are authoritative — D-13-13).

    Unclaim(ctx, orgID, actorID uuid.UUID, role string, claimRowID uuid.UUID, reason string) (*direction.Direction, error): pool-read: the row has OriginDirectionID != nil (else ErrInvalidRequest — only claim rows are unclaimable, D-13-16); reason required (ErrCancelReasonRequired); actor == DirectedTo (the claimant) or creator or manager; unclaimed audit (action 'cancelled' with reason); repo.Unclaim.

    Tests: matrix fast-fails before the repo (draft→active ok; cancelled→active ErrInvalidTransition); cancel without reason rejected before the repo call; claim by a non-member → ErrNotWgMember; claim on a draft WG row → ErrWgRowNotActive; unclaim on a non-claim row → ErrInvalidRequest; audit payloads carry {reason}/{wg_row_id, est_hours} as pinned in ADR-BE-018; all mutators return the repo's stored row.
  </action>
  <verify>
    <automated>go test ./internal/core/services/direction/ -run 'TestService_Activate|TestService_Cancel|TestService_Claim|TestService_Unclaim' -count=1</automated>
  </verify>
  <done>Lifecycle + claims orchestrated with correct fast-fail ordering and audit DTOs; the repo remains the authoritative layer.</done>
  <acceptance_criteria>
    - Activate/Cancel fast-fail the matrix at the pool level BEFORE the repo call; cancel requires a non-empty reason
    - Claim checks membership + active status + positive hours as fast-fail; Unclaim only on origin_direction_id rows with a reason
    - Audit DTO payloads match the ADR-BE-018 vocabulary (actions activated/cancelled/claimed; reason + wg_row_id + est_hours payloads)
    - Pool-level failures map to the pinned sentinels (ErrInvalidTransition/ErrCancelReasonRequired/ErrNotWgMember/ErrWgRowNotActive/ErrInvalidRequest)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Warning pure function + ListPlan/Coverage read-model assembly</name>
  <files>
    internal/core/services/direction/direction.go
    internal/core/services/direction/direction_test.go
  </files>
  <read_first>
    internal/core/domain/direction/direction.go (Warning/CoverageRow/PlanRow structs + type constants)
    .planning/phases/13-direction-backend-the-plan-plane/13-UI-SPEC.md (Copywriting Contract lines 86-93 — verbatim message formats; API Data Contracts)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 6 lines 341-350; Common Operation 4 lines 544-557; D-13-24..27, D-13-31)
    internal/core/ports/unit_repository.go (GetDescendants/ListMembers/ListMembersByUnitIDs for scope resolution)
    internal/core/ports/working_group_repository.go (ListMembers)
  </read_first>
  <action>
    computeWarnings(ctx, orgID uuid.UUID, employeeIDs []uuid.UUID, start, end time.Time) ([]direction.Warning, error): pure-function core (warningsForEmployee) over repo data — fetch AbsenceWindows(orgID, employeeIDs, start, end) + orgRepo.GetMembership per employee (validity). Per (employee, day): membership valid_from/valid_until excludes the day → Warning{Type: invalid, Message: "Outside validity period"} (D-13-31); else full absence window covering the day (hours IS NULL) → {away, "Away {day}"} (message format: use the window's day — for a multi-day window the UI contract example is "Away 10–21 Aug": format the CONTIGUOUS absence range per window as "Away {2 Jan}-{3 Jan}" or "Away {2 Jan}" for single-day — pin: day format "2 Jan" style via the pinned layout, ranges joined with an en dash); partial window (hours set) covering the day → {partial, "Partial {day}"}; plus over-capacity from the Coverage rows (planned > capacity) → {over-capacity, "Over capacity {day}"}. Keep the message pre-rendered server-side — Phase 19 renders verbatim. The invalid warning path also excludes the employee from uncovered surfacing (handled in Coverage assembly).

    ListPlan(ctx, orgID, actorID, role string, employeeID *uuid.UUID, periodStart, periodEnd time.Time) (PlanResponse, error): org-scoped read (manager+ or the employee's own view — reads are org-internal; gate: employeeID != nil && *employeeID != actorID && role != manager → ErrForbidden); rows := repo.ListPlan(...); warnings := computeWarnings over the rows' employees/days; PlanResponse{rows, warnings}.

    Coverage(ctx, orgID, actorID, role string, scope, scopeID string, periodStart, periodEnd time.Time) (CoverageResponse, error): scope validation (employee|unit|wg else ErrInvalidRequest — D-13-25); employee set: employee → [scopeID]; unit → unitRepo.GetDescendants(scopeID) + ListMembers(unitID) for the unit AND each descendant (A6 — ListMembersByUnitIDs for the descendant set); wg → wgRepo.ListMembers(scopeID); per employee orgRepo.GetMembership → validity split: invalid employees get the invalid warning and are dropped from the repo call (D-13-31); rows := repo.Coverage(valid employees, period); filter: exclude days with capacity == 0 (fully-absent — away) from the UNCOVERED surfacing, but keep the row data for warnings (D-13-26); period totals per employee (Σ planned, Σ capacity, Σ gap); CoverageResponse{rows, totals, warnings}. Manager+ gate for unit/wg scopes (role != manager → ErrForbidden); employee scope allows self-view.

    Tests (mocks): invalid employee (valid_until in the past) → invalid warning + absent from rows; away day (capacity 0) excluded from uncovered rows but warning present; partial day warning; over-capacity warning when planned > capacity; unit scope aggregates descendant members; wg scope aggregates members; period totals correct; non-manager requesting unit scope → ErrForbidden; scope=unknown → ErrInvalidRequest; warning message formats match the pinned strings exactly.
  </action>
  <verify>
    <automated>go test ./internal/core/services/direction/ -run 'TestService_Warnings|TestService_ListPlan|TestService_Coverage' -count=1</automated>
  </verify>
  <done>Warnings derive from absence/validity/coverage data with pinned messages; ListPlan/Coverage assemble scope-resolved read-models with invalid/away exclusion rules.</done>
  <acceptance_criteria>
    - Warning types are the closed set away|partial|over-capacity|invalid with the exact pinned message formats ("Away 10–21 Aug", "Partial 14 Aug", "Over capacity 16 Aug", "Outside validity period")
    - Validity-outside employees get invalid warnings and never appear in uncovered surfacing; fully-absent days (capacity 0) are excluded from uncovered rows
    - Unit scope = the unit + descendant units' members; WG scope = WG members; employee scope = the one employee
    - CoverageResponse carries rows + period totals + warnings; scope=unit|wg requires role manager
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/core/services/direction/direction.go` — `Service` (deps: directionRepo, activityRepo, wgRepo, unitRepo, orgRepo, orgSettingsSvc, routingSvc) with methods `Create` (gate chain + warnings), `Activate`, `Cancel`, `Claim`, `Unclaim`, `ListPlan`, `Coverage`; internal `computeWarnings`/`warningsForEmployee` pure fn; types `CreateDirectionRequest`, `PlanResponse`, `CoverageResponse`
- `internal/core/services/direction/direction_test.go` — unit suites: create gates, lifecycle, claims, warnings, read-model assembly
- `internal/core/services/testdata/mock_direction_repo.go` — extended with `Get` behavior + seeded read-model data setters as needed

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP actor → direction service | unauthenticated/unauthorized direction writes and org read-models cross here |
| service → repo | pool-level fast-fails cross to the authoritative tx layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-23 | Elevation of privilege | mode-gate bypass | high | mitigate | Strict reading (A9): self_planned mode → only the employee creates own rows; manager_planned → the routing gate (BE-014 approver set / role-gated manager) decides — tested per branch in Task 1; settings side is manager+-gated (13-04 T-13-10) so the mode itself cannot be tampered into permissiveness |
| T-13-24 | Elevation of privilege | unauthorized direction creation (subtree/WG reach) | high | mitigate | Manager-directed rows reuse routing.ResolveManagerStage (coverage gate shape verbatim — no re-implementation, D-G parity); WG rows route on the anchored activity; self-direction is the only bypass and it is mode-gated (D-S/D-13-20) |
| T-13-25 | Tampering | WG-scope smuggling (cross-WG/cross-org activity) | high | mitigate | Same-org check + scope predicate (activity == wg.SubprojectID OR anchor in GetAncestry(activityID)) on WG-row create AND the repo re-locks org-scoped in the claim tx (13-05); asserted in Task 1 |
| T-13-26 | Information disclosure | read-model scoping | medium | mitigate | ListPlan: employee filter restricted to self or manager; Coverage: unit/wg scopes manager-only, employee scope self-view; org-scoped repo queries underneath |
| T-13-27 | Tampering | soft-policy enforcement drift | medium | mitigate | computeWarnings is the ONLY channel for warnings; no code path can reject a write for away/partial/over-capacity/invalid — asserted by the create tests (warnings returned, never errors) |

No high-severity unmitigated threats: T-13-23/24/25 are mitigated by the gate chain and asserted in Task 1/2 tests. No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go test ./internal/core/services/direction/ -count=1` exits 0
- `go build ./...` compiles (constructor + wiring call sites still compile — handler fixture updated in 13-08)
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- Create/activate/cancel/claim/unclaim are fully orchestrated with the pinned gate chain and audit DTOs
- Warnings are a shared pure function with the UI-SPEC verbatim message formats, never blocking
- The coverage read-model resolves scopes, excludes invalid/away days, and delivers rows + totals + warnings
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-07-SUMMARY.md` when done
</output>
