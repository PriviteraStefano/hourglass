---
phase: 13-direction-backend-the-plan-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/021_direction_rows.up.sql
  - migrations/021_direction_rows.down.sql
  - migrations/022_org_settings.up.sql
  - migrations/022_org_settings.down.sql
  - internal/adapters/secondary/postgres/exported_test_helpers.go
  - internal/adapters/secondary/postgres/direction_ontology_migrations_test.go
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
  - DIR-04

estimate:
  tokens: 26000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Migration 021 creates table `direction` (id UUID PK, org_id NOT NULL REFERENCES organizations, directed_by NOT NULL REFERENCES users, directed_to UUID REFERENCES users, wg_id UUID REFERENCES working_groups, activity_id NOT NULL REFERENCES activities, planned_date DATE nullable, est_hours DECIMAL(8,2) nullable — same shape as time_entries.hours, priority INT nullable, due_date DATE nullable, status VARCHAR(20) NOT NULL DEFAULT 'draft', supersedes_id UUID REFERENCES direction, origin_direction_id UUID REFERENCES direction, reason TEXT, created_at/updated_at TIMESTAMPTZ) with exactly these constraints: direction_target_check (XOR — explicit IS [NOT] NULL both sides, no 3VL guard needed on a new table), direction_wg_queued_check (wg_id IS NULL OR planned_date IS NULL), direction_est_hours_check (est_hours IS NULL OR est_hours > 0), direction_scheduled_hours_check (planned_date IS NULL OR est_hours IS NOT NULL), direction_status_check (IN 'draft','active','superseded','cancelled'), direction_cancel_reason_check (status <> 'cancelled' OR reason IS NOT NULL); indexes (org_id, directed_to, planned_date), (org_id, wg_id), (activity_id, created_at) for the origin fallback, (supersedes_id) (DIR-01, DIR-02, DIR-03, D-13-01, D-13-02, D-13-05, D-13-06, D-13-10, D-13-17)"
    - "Identity model (assumption-delta: 'multiple rows may share a day'): the direction ROW (its UUID id) is the identity noun; there is NO UNIQUE constraint on (org_id, directed_to, activity_id, planned_date) — multiple rows sharing a day is legal, no merge/collision semantics; ordering within a day is undefined (no intra-day ordering). The cycle test asserts two rows for the same employee/activity/day both insert (D-W, D-AA)"
    - "Migration 022 creates table `org_settings` (org_id UUID NOT NULL REFERENCES organizations, key VARCHAR(50) NOT NULL, value JSONB NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (org_id, key)) + ALTER TABLE organization_memberships ADD COLUMN planning_mode VARCHAR(20) (nullable override, no backfill — NULL falls back to the org default) (DIR-04, D-13-18, D-13-19)"
    - "TeardownTestSchema drops direction and org_settings in dependency order (direction before working_groups/activities; org_settings before organizations) (Pitfall 8)"
    - "Cycle tests TestMigration021/022 pass up → down → up with functional assertions: targetless row rejected (23514, direction_target_check), WG row with planned_date rejected (direction_wg_queued_check), est_hours <= 0 rejected, scheduled row without est_hours rejected, cancelled row without reason rejected, two rows sharing (directed_to, activity_id, planned_date) BOTH accepted (identity = row id)"
  artifacts:
    - migrations/021_direction_rows.up.sql
    - migrations/021_direction_rows.down.sql
    - migrations/022_org_settings.up.sql
    - migrations/022_org_settings.down.sql
    - internal/adapters/secondary/postgres/direction_ontology_migrations_test.go
  key_links:
    - "021 supersedes_id + origin_direction_id are self-FKs (references direction(id)) — chain history is never deleted; cancelled is terminal, there is NO is_deleted soft-delete column (D-13-04/08/10)"
    - "021 activity_id FK to activities — same-org refs are enforced at the service layer (org_id column on the row), mirroring the house pattern; the CHECKs are the shape guards"
    - "022 org_settings JSONB value has NO schema CHECK (CHECK on JSONB infeasible) — the known-key vocabulary is enforced in code (D-13-18); the cycle test proves the shape, the service proves the vocabulary"
  prohibitions:
    - "Never add is_deleted / soft-delete columns or UPDATE/DELETE surfaces on direction rows — cancelled is the terminal state and the supersede chain is append-only history (D-13-04, D-13-08, D-13-10)"
    - "Never add typed columns to organization_settings (the legacy typed table) for new policy keys — new keys are data rows in org_settings key/value JSONB (D-13-18, user decision)"
  flagged_assumptions:
    - "est_hours scale pinned at DECIMAL(8,2) mirroring time_entries.hours exactly (A2 — planner discretion, research verified 000:278)"
    - "No UNIQUE constraint anywhere on direction — the only identity is the row id; per-day storage is a grouping convention, not uniqueness (assumption-delta pluralization decision: no-change on identity, recorded in plan 13-02)"
---

<objective>
Schema foundation for the plan plane: two append-only migrations (021 direction rows, 022 org_settings + planning_mode override) with up/down pairs per ADR-BE-004, the shared test teardown list updated, and self-seeding cycle tests proving every CHECK constraint — including the identity-model assertion that multiple direction rows may share a day.

Purpose: Every later direction task (repo, service, handler) compiles and tests against a verified schema. The XOR/queued-only/est_hours CHECKs and the per-day-multiplicity identity model (D-W, D-AA) are the riskiest schema claims in the phase — the cycle tests are the end-to-end proof that runs before any code touches the tables.
Output: migrations 021/022 (.up/.down), updated `TeardownTestSchema`, `direction_ontology_migrations_test.go` with TestMigration021/022.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@migrations/014_ticket_schema.up.sql
@migrations/015_activity_origins.up.sql
@migrations/017_audit_logs.up.sql
@migrations/012_staffing_schema.up.sql
@internal/adapters/secondary/postgres/exported_test_helpers.go
@internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migrations 021 (direction rows) + 022 (org_settings + planning_mode) as up/down pairs</name>
  <files>
    migrations/021_direction_rows.up.sql
    migrations/021_direction_rows.down.sql
    migrations/022_org_settings.up.sql
    migrations/022_org_settings.down.sql
  </files>
  <read_first>
    migrations/014_ticket_schema.up.sql (table + status-vocab CHECK + index block shape)
    migrations/015_activity_origins.up.sql (XOR CHECK with explicit IS [NOT] NULL, lines 36-47)
    migrations/012_staffing_schema.up.sql (ALTER TABLE organization_memberships ADD COLUMN block, lines 39-42)
    migrations/000_full_schema.up.sql line 270-286 (time_entries.hours DECIMAL(8,2) CHECK > 0 — est_hours mirror)
    migrations/019_coverage_allocations.down.sql (DROP TABLE IF EXISTS ... CASCADE form)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 1 lines 246-287 — recommended DDL; Pitfall 2 lines 411-415 — 3VL rule; Pitfall 3 — est_hours semantics)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (migrations analog section, lines 421-436)
  </read_first>
  <action>
    Create four files following ADR-BE-004 numbering (next free numbers are 021 and 022; the 007 gap is frozen — do not renumber).

    Migration 021 up: CREATE TABLE direction with columns id UUID PRIMARY KEY DEFAULT gen_random_uuid(), org_id UUID NOT NULL REFERENCES organizations(id), directed_by UUID NOT NULL REFERENCES users(id), directed_to UUID REFERENCES users(id), wg_id UUID REFERENCES working_groups(id), activity_id UUID NOT NULL REFERENCES activities(id), planned_date DATE (NULL = queued, D-R), est_hours DECIMAL(8,2) (mirrors time_entries.hours exactly — D-13-02), priority INT, due_date DATE, status VARCHAR(20) NOT NULL DEFAULT 'draft', supersedes_id UUID REFERENCES direction(id), origin_direction_id UUID REFERENCES direction(id), reason TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(). Then five ALTER TABLE ADD CONSTRAINT statements with exactly these names and shapes (new table — no legacy rows — so pin both sides of the XOR with explicit IS [NOT] NULL, NO 3VL guard per research Pitfall 2): direction_target_check CHECK ((directed_to IS NOT NULL AND wg_id IS NULL) OR (directed_to IS NULL AND wg_id IS NOT NULL)) per D-13-05; direction_wg_queued_check CHECK (wg_id IS NULL OR planned_date IS NULL) per D-13-17; direction_est_hours_check CHECK (est_hours IS NULL OR est_hours > 0) per D-13-02/03; direction_scheduled_hours_check CHECK (planned_date IS NULL OR est_hours IS NOT NULL) per D-13-02; direction_status_check CHECK (status IN ('draft','active','superseded','cancelled')) per D-13-07; direction_cancel_reason_check CHECK (status <> 'cancelled' OR reason IS NOT NULL) per D-13-10 (this form fails on NULL reason — never NULL-satisfiable). Indexes: idx_direction_org_employee_day ON direction(org_id, directed_to, planned_date); idx_direction_org_wg ON direction(org_id, wg_id); idx_direction_activity_created ON direction(activity_id, created_at) (origin fallback read, D-13-33); idx_direction_supersedes ON direction(supersedes_id). Deliberately NO UNIQUE constraint on (org_id, directed_to, activity_id, planned_date) — multiple rows may share a day; the row id is the only identity (D-W, D-AA).

    Migration 021 down: DROP TABLE IF EXISTS direction CASCADE;

    Migration 022 up: CREATE TABLE org_settings (org_id UUID NOT NULL REFERENCES organizations(id), key VARCHAR(50) NOT NULL, value JSONB NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (org_id, key)) per D-13-18 — no CHECK on JSONB (vocabulary enforced in code); then ALTER TABLE organization_memberships ADD COLUMN planning_mode VARCHAR(20) per D-13-19 — nullable override, no backfill, no CHECK (vocabulary enforced in code per D-13-18 house rule).

    Migration 022 down: ALTER TABLE organization_memberships DROP COLUMN IF EXISTS planning_mode; then DROP TABLE IF EXISTS org_settings CASCADE;
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration021|TestMigration022' -count=1</automated>
  </verify>
  <done>All four migration files exist with the constraint names above; both cycle tests pass (tests land in Task 2 — if missing on first run, the automated check targets Task 2's tests after they land).</done>
  <acceptance_criteria>
    - migrations/021_direction_rows.up.sql contains table `direction` with all columns: id, org_id, directed_by, directed_to, wg_id, activity_id, planned_date, est_hours, priority, due_date, status, supersedes_id, origin_direction_id, reason, created_at, updated_at
    - migrations/021_direction_rows.up.sql declares all six constraints by name: direction_target_check (XOR with explicit IS [NOT] NULL on both sides), direction_wg_queued_check, direction_est_hours_check, direction_scheduled_hours_check, direction_status_check, direction_cancel_reason_check
    - migrations/021_direction_rows.up.sql has `est_hours DECIMAL(8,2)` and NO UNIQUE constraint on any (employee, activity, day) tuple
    - migrations/021_direction_rows.up.sql declares the four indexes idx_direction_org_employee_day, idx_direction_org_wg, idx_direction_activity_created, idx_direction_supersedes
    - migrations/022_org_settings.up.sql creates org_settings with PRIMARY KEY (org_id, key) and value JSONB NOT NULL, and contains `ADD COLUMN planning_mode VARCHAR(20)`
    - migrations/022_org_settings.down.sql drops planning_mode column BEFORE dropping org_settings table
    - No file in migrations/ is renumbered; numbering continues at 021
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Teardown list update + cycle tests TestMigration021/022 (self-seed, CHECK + identity assertions)</name>
  <files>
    internal/adapters/secondary/postgres/exported_test_helpers.go
    internal/adapters/secondary/postgres/direction_ontology_migrations_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/exported_test_helpers.go (TeardownTestSchema lines 79-127; seed helpers — seedOrg/seedUser/seedActivity/seedWorkingGroup shapes)
    internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go (TestMigration018..020 self-seed cycle shape — the direct analog)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Common Operation 6 lines 570-588 — cycle-test skeleton; Pitfall 8)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (test-file analog section, lines 438-464)
  </read_first>
  <action>
    In exported_test_helpers.go, extend the TeardownTestSchema table list with exactly two entries: "direction" before "working_groups" (FK from direction.wg_id) and "org_settings" before "organizations" (dependency order, Pitfall 8). Add a seedDirectionRow helper following the seedTimeEntry shape (inserts a direction row with the seeded org/user/activity; returns the row id) for later plans' tests.

    Create direction_ontology_migrations_test.go with two tests following the TestMigration018..020 self-seed shape (applyMigrations for pre-state + readMigration + seedOrg/seedUser/seedActivity + direct SQL — never scripts/seed_demo.sql, Phase 11 house rule):
    - TestMigration021_DirectionRows_UpDownUpCycle: apply 022 as pre-state; seed org + user (as directed_by) + second user (as directed_to) + activity; UP → apply 021; functional assertions: (a) INSERT a valid scheduled row (directed_to set, wg_id NULL, planned_date + est_hours) succeeds; (b) INSERT a second row with the SAME directed_to + activity_id + planned_date succeeds (identity = row id — the D-W multiplicity assertion); (c) INSERT targetless row (both directed_to and wg_id NULL) fails with pgErr.Code "23514" and constraint name direction_target_check; (d) INSERT WG row with planned_date set fails with direction_wg_queued_check; (e) INSERT scheduled row without est_hours fails with direction_scheduled_hours_check; (f) INSERT est_hours = 0 fails with direction_est_hours_check; (g) INSERT cancelled row without reason fails with direction_cancel_reason_check; (h) queued row (planned_date NULL, est_hours NULL) succeeds; DOWN → assertTableExists(direction, false); UP again → green.
    - TestMigration022_OrgSettings_UpDownUpCycle: UP → org_settings exists with PK(org_id, key) (assertPrimaryKey via pg_constraint), INSERT row + UPDATE same key via ON CONFLICT semantics works (value replaced), information_schema shows organization_memberships.planning_mode column; DOWN → planning_mode column gone AND org_settings table gone; UP again → green.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration021|TestMigration022' -count=1</automated>
  </verify>
  <done>Both cycle tests pass; teardown list contains direction + org_settings in dependency order; seedDirectionRow helper exists.</done>
  <acceptance_criteria>
    - exported_test_helpers.go TeardownTestSchema list contains "direction" (before "working_groups") and "org_settings" (before "organizations")
    - exported_test_helpers.go exports a seedDirectionRow helper
    - direction_ontology_migrations_test.go exists with funcs TestMigration021_DirectionRows_UpDownUpCycle and TestMigration022_OrgSettings_UpDownUpCycle
    - TestMigration021 asserts pgErr.Code "23514" with constraint names direction_target_check, direction_wg_queued_check, direction_scheduled_hours_check, direction_est_hours_check, direction_cancel_reason_check AND asserts two same-day rows for the same employee+activity both insert successfully
    - go test ./internal/adapters/secondary/postgres/ -run 'TestMigration021|TestMigration022' -count=1 exits 0
    - No test loads scripts/seed_demo.sql
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `migrations/021_direction_rows.up.sql` / `.down.sql` — table `direction` (id, org_id, directed_by, directed_to, wg_id, activity_id, planned_date, est_hours, priority, due_date, status, supersedes_id, origin_direction_id, reason, created_at, updated_at) + constraints `direction_target_check` (XOR), `direction_wg_queued_check`, `direction_est_hours_check`, `direction_scheduled_hours_check`, `direction_status_check`, `direction_cancel_reason_check` + indexes `idx_direction_org_employee_day`, `idx_direction_org_wg`, `idx_direction_activity_created`, `idx_direction_supersedes`
- `migrations/022_org_settings.up.sql` / `.down.sql` — table `org_settings` (org_id, key, value JSONB, updated_at, PK(org_id,key)) + `organization_memberships.planning_mode VARCHAR(20)` nullable
- `internal/adapters/secondary/postgres/direction_ontology_migrations_test.go` — `TestMigration021_DirectionRows_UpDownUpCycle`, `TestMigration022_OrgSettings_UpDownUpCycle`
- `exported_test_helpers.go` teardown entries: `direction`, `org_settings`; new helper `seedDirectionRow`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → PostgreSQL | repo-owned append-only SQL; the risk is constraint correctness (NULL bypass, missing vocab), not injection (parameterized migration runner) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | direction CHECKs | high | mitigate | XOR pinned with explicit IS [NOT] NULL both sides (never NULL-satisfiable, research Pitfall 2); queued-only/est_hours/scheduled/reason CHECKs use never-NULL-satisfiable forms; all asserted functionally in TestMigration021 (failing-insert assertions, not DDL text) |
| T-13-02 | Tampering | direction vocabulary | medium | mitigate | status CHECK IN ('draft','active','superseded','cancelled') closed set (D-13-07); no other free-form vocab columns in 021 |
| T-13-03 | Tampering | org_settings shape | medium | mitigate | PK(org_id, key) is the identity; JSONB value has no CHECK by design (D-13-18) — the vocabulary gate is the code-level known-key validation in plan 13-04, noted here as the compensating control |
| T-13-04 | Tampering | supersede/claim self-FKs | medium | mitigate | supersedes_id/origin_direction_id reference direction(id) — chain rows are never deleted (no is_deleted column exists); cancelled is terminal |

No high-severity unmitigated threats: T-13-01 is mitigated by Task 2's failing-insert cycle assertions. No package installs this phase (Go module set unchanged — verified in RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestMigration021|TestMigration022' -count=1` exits 0
- `go build ./...` compiles (test package wired)
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- Both migrations apply, cycle, and down cleanly (ADR-BE-004)
- The identity model is proven: multiple rows sharing a day insert successfully; every CHECK rejects its violation (23514 + named constraint)
- Teardown drops direction + org_settings in dependency order (Pitfall 8 closed)
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-01-SUMMARY.md` when done
</output>
