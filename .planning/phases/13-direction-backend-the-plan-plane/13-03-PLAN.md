---
phase: 13-direction-backend-the-plan-plane
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/domain/direction/direction.go
  - internal/core/domain/direction/errors.go
  - internal/core/domain/orgsettings/orgsettings.go
  - internal/core/domain/orgsettings/errors.go
  - internal/core/ports/direction_repository.go
  - internal/core/ports/org_settings_repository.go
  - internal/core/services/testdata/mock_direction_repo.go
  - internal/core/services/testdata/mock_org_settings_repo.go
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
  - DIR-04
  - DIR-06

estimate:
  tokens: 30000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "domain/direction defines the Direction entity (ID, OrgID, DirectedBy, DirectedTo *uuid.UUID, WgID *uuid.UUID, ActivityID, PlannedDate *time.Time, EstHours *float64, Priority *int, DueDate *time.Time, Status, SupersedesID *uuid.UUID, OriginDirectionID *uuid.UUID, Reason *string, CreatedAt, UpdatedAt) with status constants StatusDraft/StatusActive/StatusSuperseded/StatusCancelled and the pinned transition matrix (draft→active, draft→cancelled, active→cancelled; superseded reachable only via create) + CanTransition + IsTerminalStatus (superseded|cancelled) (D-13-01, D-13-07)"
    - "domain/direction defines the derived-state vocabulary constants (done/lapsed/claimed), the claim-spectrum constants (not_claimed/partially_claimed/fully_claimed), the Warning struct {Type, Message} with closed type set away|partial|over-capacity|invalid, the CoverageRow struct (EmployeeID, Date, Capacity, Planned, Gap) and PlanRow struct (row + derived states) per D-13-09/15/26/27/30"
    - "domain/direction/errors.go defines sentinels ErrDirectionNotFound, ErrInvalidRequest, ErrForbidden, ErrInvalidTransition, ErrClaimOverBudget, ErrNotWgMember, ErrWgRowNotActive, ErrCancelReasonRequired, ErrInvalidHours, ErrInvalidTarget + JSONNames map (house style, coverage/errors.go shape)"
    - "domain/orgsettings defines key constants KeyPlanningDailyHours/KeyPlanningDeadline/KeyPlanningHorizon/KeyPlanningMode, mode vocabulary manager_planned|self_planned, horizon vocabulary day|week|month, DefaultDailyHours = 8.0, a knownKeys validator map, and errors ErrUnknownKey/ErrInvalidValue/ErrForbidden + JSONNames (D-13-18, D-13-24)"
    - "ports/direction_repository.go declares the DirectionRepository interface: Create (with supersedesID + audits — supersede-on-create in one tx), Get (pool-level single-row read for service fast-fails), Activate, Cancel (reason + audit), Claim (wgRowID + claimant + estHours + audit — Σ guard under lock), ListPlan (employeeID *uuid.UUID optional), Coverage (employeeIDs slice — scope resolved by the service), AbsenceWindows, FirstDirectionRefs; every mutator takes its audit row(s) and writes them IN THE SAME TRANSACTION (ticket port shape)"
    - "ports/org_settings_repository.go declares OrgSettingsRepository: Get (nil when absent), List, Upsert (in-tx audit, D-13-22)"
    - "testdata MockDirectionRepo + MockOrgSettingsRepo exist with the mutex+map+GetFn-override+Audits-capture pattern and compile-time assertions `var _ ports.X = (*MockX)(nil)` — every other plan compiles against them"
  artifacts:
    - internal/core/domain/direction/direction.go
    - internal/core/domain/direction/errors.go
    - internal/core/domain/orgsettings/orgsettings.go
    - internal/core/domain/orgsettings/errors.go
    - internal/core/ports/direction_repository.go
    - internal/core/ports/org_settings_repository.go
    - internal/core/services/testdata/mock_direction_repo.go
    - internal/core/services/testdata/mock_org_settings_repo.go
  key_links:
    - "The DirectionRepository port is the compile-time contract between the postgres repo (13-05/13-06), the service (13-07), the activity service's origin-fallback dependency (13-08) and the testdata mocks — its signatures are pinned here and must not change later"
    - "Exported audit constants (AuditEntityDirection + AuditActionCreated/Activated/Cancelled/Superseded/Claimed/Unclaimed; AuditEntityOrgSettings + AuditActionSettingsUpdated) mirror the coverage constants block so the repo (13-05) and service (13-07) can never drift from the ADR-BE-018 vocabulary"
    - "Warning struct + CoverageRow + PlanRow shapes are the UI-SPEC API-data-contract consumables for Phase 19 (13-UI-SPEC API Data Contracts section) — field names and closed sets are pinned here"
  flagged_assumptions:
    - "Coverage port signature takes an employeeIDs slice (scope resolution lives in the service per D-13-25 'aggregation differs only in which-employees resolution'); the alternative (scope params into the repo) was rejected to keep the repo free of unit/WG membership queries (OQ5, A6)"
    - "Claim port signature: single claim insert with estHours; the WG row's est_hours budget read + Σ re-check happen inside the repo tx (CR-01, D-13-13)"
    - "FirstDirectionRefs returns nil when no non-cancelled row exists (D-13-33); DirectionRefs carries AssignedBy/AssignedTo only — never the other origin shapes (D-13-33/34)"
  prohibitions:
    - "Never store derived states on the Direction entity — done/lapsed/claimed/claim-spectrum are read-model fields only (D-13-09, D-V)"
---

<objective>
The compile-time contracts of the phase: direction domain (entity, matrix, vocabularies, errors), orgsettings domain (key vocabulary + validators), both repository ports, and the testdata mocks every other plan compiles against.

Purpose: Contracts first — the repo (13-05/06), service (13-07), handlers (13-08) and mocks all compile against pinned signatures; the exported audit/status/key constants implement the ADR-BE-018 vocabulary with zero drift.
Output: 6 new Go files (2 domains, 2 ports, 2 mocks).
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@internal/core/domain/ticket/ticket.go
@internal/core/domain/coverage/coverage.go
@internal/core/domain/coverage/errors.go
@internal/core/ports/ticket_repository.go
@internal/core/ports/coverage_repository.go
@internal/core/services/testdata/mock_ticket_repo.go
@internal/core/domain/activity/activity.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Direction domain — entity, matrix, vocabularies, errors</name>
  <files>
    internal/core/domain/direction/direction.go
    internal/core/domain/direction/errors.go
  </files>
  <read_first>
    internal/core/domain/ticket/ticket.go (whole file — entity + matrix + CanTransition + derived-on-read precedent)
    internal/core/domain/coverage/errors.go (whole file — sentinel + JSONNames shape)
    internal/core/domain/coverage/coverage.go lines 119-153 (audit vocabulary constants block)
    internal/core/domain/activity/activity.go (DirectionRefs consumed by the origin fallback — AssignedBy/AssignedTo fields)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (domain analog section lines 44-108)
    .planning/phases/13-direction-backend-the-plan-plane/13-UI-SPEC.md (API Data Contracts — status/derived/claim-spectrum/warning closed sets)
  </read_first>
  <action>
    Create internal/core/domain/direction/direction.go: the Direction struct with exactly the D-13-01 fields (ID, OrgID, DirectedBy uuid.UUID, DirectedTo *uuid.UUID, WgID *uuid.UUID, ActivityID uuid.UUID, PlannedDate *time.Time, EstHours *float64, Priority *int, DueDate *time.Time, Status string, SupersedesID *uuid.UUID, OriginDirectionID *uuid.UUID, Reason *string, CreatedAt, UpdatedAt time.Time) with json tags matching the D-13-01 column names. Status constants StatusDraft/StatusActive/StatusSuperseded/StatusCancelled. The pinned matrix as an unexported map + exported CanTransition(from, to string) bool and IsTerminalStatus(s string) bool (superseded + cancelled terminal); superseded is NOT in the matrix (reachable only via Create-with-supersedes_id per D-13-08). Derived-state vocabulary constants (StatusDerivedDone = "done", StatusDerivedLapsed = "lapsed", StatusDerivedClaimed = "claimed") + claim-spectrum constants (ClaimStateNotClaimed/PartiallyClaimed/FullyClaimed = "not_claimed"/"partially_claimed"/"fully_claimed") per D-13-09/15. The Warning struct {Type string `json:"type"`, Message string `json:"message"`} with the closed type constants WarningAway/WarningPartial/WarningOverCapacity/WarningInvalid = away/partial/over-capacity/invalid (UI-SPEC contract — message pre-rendered server-side "{Type} {date-range-or-day}"). The CoverageRow struct {EmployeeID uuid.UUID, Date time.Time, Capacity float64, Planned float64, Gap float64} (D-13-26). The PlanRow struct embedding the Direction row plus derived-state fields (Done, Lapsed bool; ClaimState string for WG rows) (D-13-27). The DirectionRefs struct {AssignedBy, AssignedTo *uuid.UUID} for the origin fallback (D-13-32). The exported audit vocabulary constants mirroring the coverage block: AuditEntityDirection = "direction", AuditActionCreated/Activated/Cancelled/Superseded/Claimed/Unclaimed = created/activated/cancelled/superseded/claimed/unclaimed (ADR-BE-018 pin).
    Create internal/core/domain/direction/errors.go from the coverage/errors.go shape: ErrDirectionNotFound, ErrInvalidRequest, ErrForbidden, ErrInvalidTransition, ErrClaimOverBudget (→ 409), ErrNotWgMember (→ 403), ErrWgRowNotActive, ErrCancelReasonRequired, ErrInvalidHours, ErrInvalidTarget + the JSONNames map.
  </action>
  <verify>
    <automated>go build ./internal/core/domain/direction/ && go vet ./internal/core/domain/direction/</automated>
  </verify>
  <done>Direction domain compiles with entity, matrix, vocabularies, warning/coverage/plan structs, DirectionRefs and errors.</done>
  <acceptance_criteria>
    - internal/core/domain/direction/direction.go defines the Direction struct with all 15 D-13-01 fields and json tags
    - CanTransition("draft","active") == true; CanTransition("active","superseded") == false (supersede is create-only); IsTerminalStatus("superseded") == true; IsTerminalStatus("cancelled") == true
    - direction.go exports the claim-spectrum constants and the Warning type constants away/partial/over-capacity/invalid
    - direction.go exports AuditEntityDirection + the six action constants with ADR-BE-018 strings
    - internal/core/domain/direction/errors.go defines the 10 sentinels + JSONNames
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Orgsettings domain + both repository ports</name>
  <files>
    internal/core/domain/orgsettings/orgsettings.go
    internal/core/domain/orgsettings/errors.go
    internal/core/ports/direction_repository.go
    internal/core/ports/org_settings_repository.go
  </files>
  <read_first>
    internal/core/domain/coverage/coverage.go lines 119-153 (vocabulary const block shape)
    internal/core/ports/ticket_repository.go (port doc-comment + mutator-signature shape)
    internal/core/ports/coverage_repository.go (read-model method shape)
    internal/core/ports/audit_log_repository.go (tiny-port shape for org_settings)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (ports analog section lines 116-149)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 7 — settings key vocabulary)
  </read_first>
  <action>
    Create internal/core/domain/orgsettings/orgsettings.go: key constants KeyPlanningDailyHours = "planning_daily_hours", KeyPlanningDeadline = "planning_deadline", KeyPlanningHorizon = "planning_horizon", KeyPlanningMode = "planning_mode"; mode vocabulary ModeManagerPlanned/ModeSelfPlanned = "manager_planned"/"self_planned" (D-13-19); horizon vocabulary HorizonDay/HorizonWeek/HorizonMonth = "day"/"week"/"month" (D-13-21); DefaultDailyHours = 8.0 (D-13-24). A knownKeys validator mechanism: map[string]func(json.RawMessage) error with per-key validation — planning_daily_hours: JSON number > 0; planning_deadline: JSON string parseable as 2006-01-02; planning_horizon: one of day|week|month; planning_mode: one of manager_planned|self_planned (D-13-18 code-enforced vocabulary). Export AuditActionSettingsUpdated = "settings-updated" + AuditEntityOrgSettings = "org_settings" (ADR-BE-018). Create internal/core/domain/orgsettings/errors.go: ErrUnknownKey, ErrInvalidValue, ErrForbidden, ErrNotFound + JSONNames.
    Create internal/core/ports/direction_repository.go with doc comments declaring: Get(ctx, orgID, id uuid.UUID) (*direction.Direction, error) — pool-level single-row read (pgx.ErrNoRows → ErrDirectionNotFound) used by the service for Activate/Cancel/Claim/Unclaim fast-fails (13-07) and by the supersede fast-fail; Create(ctx context.Context, orgID uuid.UUID, d *direction.Direction, supersedesID *uuid.UUID, audits []*audit.AuditLog) (*direction.Direction, error) — supersede-on-create in one tx (D-13-08); Activate(ctx, orgID, id uuid.UUID, audit *audit.AuditLog) (*direction.Direction, error); Cancel(ctx, orgID, id uuid.UUID, reason string, audit *audit.AuditLog) (*direction.Direction, error); Claim(ctx, orgID, wgRowID, claimantID uuid.UUID, estHours float64, audit *audit.AuditLog) (*direction.Direction, error) — the Σ guard under the WG-row FOR UPDATE lock (D-13-13); ListPlan(ctx, orgID uuid.UUID, employeeID *uuid.UUID, periodStart, periodEnd time.Time) ([]direction.PlanRow, error) — read-model + derived states (D-13-27); Coverage(ctx, orgID uuid.UUID, employeeIDs []uuid.UUID, periodStart, periodEnd time.Time) ([]direction.CoverageRow, error) — DIR-06 (employee set resolved by the service); AbsenceWindows(ctx, orgID uuid.UUID, employeeIDs []uuid.UUID, start, end time.Time) ([]direction.AbsenceWindow, error) — declared+confirmed P-008 windows (D-13-29) for the service warning computation; FirstDirectionRefs(ctx, orgID, activityID uuid.UUID) (*direction.DirectionRefs, error) — the origin-fallback port the activity service consumes (D-13-32/33). Define the direction.AbsenceWindow struct (EmployeeID, Kind, StartsOn, EndsOn, Hours *float64 — partial-day) in the direction domain. Create internal/core/ports/org_settings_repository.go: Get(ctx, orgID uuid.UUID, key string) (json.RawMessage, error) (nil when absent), List(ctx, orgID uuid.UUID) (map[string]json.RawMessage, error), Upsert(ctx, orgID uuid.UUID, key string, value json.RawMessage, audit *audit.AuditLog) error — in-tx audit (D-13-22).
  </action>
  <verify>
    <automated>go build ./internal/core/domain/orgsettings/ ./internal/core/ports/ && go vet ./internal/core/domain/orgsettings/ ./internal/core/ports/</automated>
  </verify>
  <done>Orgsettings domain + both ports compile; the port signatures are the pinned contracts for plans 13-04..13-08.</done>
  <acceptance_criteria>
    - orgsettings.go defines the four key constants, two mode constants, three horizon constants, DefaultDailyHours = 8.0 and a knownKeys validator covering all four keys
    - orgsettings.go exports AuditEntityOrgSettings + AuditActionSettingsUpdated
    - ports/direction_repository.go declares the 9 methods Get/Create/Activate/Cancel/Claim/ListPlan/Coverage/AbsenceWindows/FirstDirectionRefs with the exact signatures above, every mutator taking audit parameter(s) for in-tx writes
    - ports/org_settings_repository.go declares Get/List/Upsert with the signatures above
    - domain/direction defines AbsenceWindow with EmployeeID/Kind/StartsOn/EndsOn/Hours
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Testdata mocks — MockDirectionRepo + MockOrgSettingsRepo</name>
  <files>
    internal/core/services/testdata/mock_direction_repo.go
    internal/core/services/testdata/mock_org_settings_repo.go
  </files>
  <read_first>
    internal/core/services/testdata/mock_ticket_repo.go (whole file — mutex+map+GetFn override+Audits capture pattern)
    internal/core/services/testdata/mocks_test.go (mock contract tests shape)
    internal/core/ports/direction_repository.go (being created this plan)
    internal/core/ports/org_settings_repository.go (being created this plan)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (testdata mocks analog, lines 464-465 + mock pattern note)
  </read_first>
  <action>
    Create mock_direction_repo.go implementing ports.DirectionRepository: struct with sync.Mutex, in-memory map[string]*direction.Direction keyed by id, a Claims []*direction.Direction capture slice, a GetFn-style override field per method (GetFn/CreateFn/ActivateFn/CancelFn/ClaimFn/ListPlanFn/CoverageFn/AbsenceWindowsFn/FirstDirectionRefsFn), and Audits []*audit.AuditLog capture (every mutator appends its audit). Default Get behavior: look up the map by id (org-scoped check), return the row; missing → ErrDirectionNotFound (service fast-fails in 13-07 rely on this). Default Create behavior: assign ID if empty, store, return; default Claim: store the claim row and append to Claims; default ListPlan/Coverage/AbsenceWindows: return configured stub data (settable fields or return empty slice). Compile-time assertion var _ ports.DirectionRepository = (*MockDirectionRepo)(nil). Helper setters for the read-methods (SetPlanRows/SetCoverageRows/SetAbsenceWindows/SetDirectionRefs) so the service unit tests (13-07) control outputs.
    Create mock_org_settings_repo.go implementing ports.OrgSettingsRepository: mutex + map[string]json.RawMessage + UpsertErr/GetFn override + Audits capture. Default Upsert stores + appends audit; Get returns nil when absent. Compile-time assertion var _ ports.OrgSettingsRepository = (*MockOrgSettingsRepo)(nil).
  </action>
  <verify>
    <automated>go build ./internal/core/services/testdata/ && go test ./internal/core/services/testdata/ -run TestMockContracts -count=1</automated>
  </verify>
  <done>Both mocks compile, satisfy their ports (compile-time assertions), and capture audits for service tests.</done>
  <acceptance_criteria>
    - mock_direction_repo.go implements all 9 port methods with per-method Fn overrides + Audits capture; `var _ ports.DirectionRepository = (*MockDirectionRepo)(nil)` compiles
    - mock_org_settings_repo.go implements Get/List/Upsert with Fn overrides + Audits capture; `var _ ports.OrgSettingsRepository = (*MockOrgSettingsRepo)(nil)` compiles
    - go build ./internal/core/services/testdata/ exits 0 (all existing mock call sites intact)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/core/domain/direction/direction.go` — `Direction` (15 D-13-01 fields), status constants, `transitionMatrix` + `CanTransition`/`IsTerminalStatus`, derived-state constants (`done`/`lapsed`/`claimed`), claim-spectrum constants, `Warning{Type,Message}` + 4 warning-type constants, `CoverageRow{EmployeeID,Date,Capacity,Planned,Gap}`, `PlanRow`, `DirectionRefs{AssignedBy,AssignedTo}`, `AbsenceWindow{EmployeeID,Kind,StartsOn,EndsOn,Hours}`, audit constants `AuditEntityDirection` + 6 actions
- `internal/core/domain/direction/errors.go` — 10 sentinels + `JSONNames`
- `internal/core/domain/orgsettings/orgsettings.go` — 4 key constants, 2 mode constants, 3 horizon constants, `DefaultDailyHours = 8.0`, `knownKeys` validators, `AuditEntityOrgSettings` + `AuditActionSettingsUpdated`
- `internal/core/domain/orgsettings/errors.go` — `ErrUnknownKey`/`ErrInvalidValue`/`ErrForbidden`/`ErrNotFound` + `JSONNames`
- `internal/core/ports/direction_repository.go` — interface `DirectionRepository` (9 methods: Get/Create/Activate/Cancel/Claim/ListPlan/Coverage/AbsenceWindows/FirstDirectionRefs)
- `internal/core/ports/org_settings_repository.go` — interface `OrgSettingsRepository` (Get/List/Upsert)
- `internal/core/services/testdata/mock_direction_repo.go` — `MockDirectionRepo`
- `internal/core/services/testdata/mock_org_settings_repo.go` — `MockOrgSettingsRepo`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain/port contract → all later plans | compile-time boundary; the risk is contract drift, not runtime injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Tampering | audit/status/key vocabularies | medium | mitigate | All vocabularies are exported constants (ADR-BE-018 pins) — repos/services/handlers reference constants, never string literals; drift becomes a compile error |
| T-13-08 | Tampering | port contract | medium | mitigate | MockDirectionRepo/MockOrgSettingsRepo carry compile-time `var _ ports.X = (*MockX)(nil)` assertions — a port signature change breaks the build, forcing a deliberate contract review |
| T-13-09 | Elevation of privilege | ErrClaimOverBudget sentinel | low | mitigate | Sentinel is the only 409 channel for claim over-subscription; JSONNames keeps the wire name stable for the handler map (13-08) |

No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go build ./...` compiles (domains + ports + mocks + all existing call sites)
- `go test ./internal/core/services/testdata/ -count=1` passes
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- Direction + orgsettings domains encode the ADR-BE-018 vocabularies as exported constants
- Both ports are pinned; all later plans compile against them without signature changes
- Testdata mocks are contract-complete for the service unit tests (13-07)
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-03-SUMMARY.md` when done
</output>
