---
phase: 13-direction-backend-the-plan-plane
plan: 06
type: execute
wave: 3
depends_on:
  - 13-05
files_modified:
  - internal/adapters/secondary/postgres/direction_repository.go
  - internal/adapters/secondary/postgres/direction_repository_test.go
autonomous: true
requirements:
  - DIR-02
  - DIR-05
  - DIR-06

estimate:
  tokens: 34000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ListPlan(ctx, orgID, employeeID *uuid.UUID, periodStart, periodEnd) returns plan/queue rows for the period with derived states computed ON READ, never stored: done (linked activity terminal — the Phase 11 recursive CTE re-anchored at activities.id with the same non-terminal predicate status IN draft/submitted/pending_manager/pending_finance AND is_deleted=false, semantic inversion: NOT EXISTS), lapsed (planned_date/due_date in the past AND no non-deleted time entries ANY status on the subtree — OQ2/A3), claimed spectrum for WG rows (not_claimed → partially_claimed → fully_claimed; fully only when the WG budget is set and Σ == budget — D-13-09, D-13-15)"
    - "Only draft|active rows participate in ListPlan/Coverage — superseded and cancelled rows are history (D-13-08); queued ordering is priority ASC then due_date ASC with a created_at tiebreak (D-13-06, probe DIR-01 ordering edge — stable order for equal keys)"
    - "Coverage(ctx, orgID, employeeIDs, periodStart, periodEnd) computes per (employee, day): capacity = planning_daily_hours (org_settings value, default 8.0 — subquery with COALESCE fallback) − absence hours that day (full window hours IS NULL → capacity 0; partial window → capacity -= hours, floored at 0 — Pitfall 10), planned = Σ est_hours of draft|active scheduled rows for that employee/day, gap = capacity − planned (can be 0 or negative — over-capacity); returns CoverageRow{EmployeeID, Date, Capacity, Planned, Gap} (D-13-24, D-13-26)"
    - "AbsenceWindows(ctx, orgID, employeeIDs, start, end) returns availability_windows rows with status IN ('declared','confirmed') — BOTH statuses per D-13-29 (Phase 14 tightens), including kind + hours + starts_on + ends_on for service-side warning message formatting"
    - "FirstDirectionRefs(ctx, orgID, activityID) returns the manager-assignment-shaped refs (assigned_by = directed_by, assigned_to = directed_to) of the EARLIEST created_at non-cancelled direction row for the activity (idx_direction_activity_created); nil when no such row — refs stay empty (D-13-33); org-scoped query"
    - "Coverage excludes fully-absent days from... no — the repo returns all days' rows; the SERVICE excludes fully-absent days (capacity 0) and validity-outside employees from uncovered surfacing (D-13-26, D-13-31 — the service owns the surfacing policy, the repo owns the math)"
  artifacts:
    - internal/adapters/secondary/postgres/direction_repository.go (read-model methods)
    - internal/adapters/secondary/postgres/direction_repository_test.go (read-model tests)
  key_links:
    - "The derived-done CTE is the SAME subtree walk the ticket dismissal guard uses — re-anchored at activities.id with the semantic inversion; the lapsed predicate adds 'no non-deleted entries any status' (OQ2)"
    - "Coverage capacity reads org_settings.planning_daily_hours directly in SQL (COALESCE subquery default 8.0) — no service round-trip for the daily-hours value (D-13-24)"
    - "FirstDirectionRefs is the seam the activity-service origin fallback (13-08) consumes; its nil-when-none contract is the D-13-34 'refs stay empty' behavior"
  flagged_assumptions:
    - "lapsed predicate pinned: no non-deleted time entries of ANY status on the activity subtree (A3, OQ2 recommendation) — recorded in ADR-BE-018"
    - "period handling: period_start/period_end are DATE-inclusive bounds (row planned_date >= $start AND <= $end); the service parses the query strings at the handler boundary (OQ5)"
    - "AbsenceWindows returns declared+confirmed (D-13-29); the warning-type derivation (away/partial/over-capacity/invalid) happens in the service (13-07) from this data + validity"
  prohibitions:
    - "Never store derived states or add a consumed/full flag — done/lapsed/claimed/spectrum are computed in these read queries only (D-13-09, D-13-14/15)"
---

<objective>
The read side of the direction repository: ListPlan with derived states (done/lapsed/claimed spectrum), the Coverage read-model (planned vs capacity per employee/day), AbsenceWindows (declared+confirmed) feeding the warning overlay, and FirstDirectionRefs for the origin fallback.

Purpose: Derived states are computed on read, never stored, no nightly jobs (D-V); the coverage math (capacity = daily hours − absence hours) and the terminal/lapsed CTEs are the read-model correctness core the service (13-07) and Phase 19 surfaces consume.
Output: four read methods on direction_repository.go + integration tests.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@internal/adapters/secondary/postgres/direction_repository.go
@internal/adapters/secondary/postgres/ticket_repository.go
@internal/adapters/secondary/postgres/coverage_repository.go
@internal/core/domain/direction/direction.go
@internal/core/ports/direction_repository.go
@migrations/012_staffing_schema.up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: ListPlan + derived states (done CTE re-anchored, lapsed, claim spectrum)</name>
  <files>
    internal/adapters/secondary/postgres/direction_repository.go
    internal/adapters/secondary/postgres/direction_repository_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/ticket_repository.go lines 816-861 (terminal-activity CTE pool + Tx variants — re-anchor at activities.id, invert the EXISTS)
    internal/adapters/secondary/postgres/coverage_repository.go lines 215-252, 297-299 (read-model shape + roundCents)
    internal/core/domain/direction/direction.go (PlanRow + derived-state constants)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 4 lines 321-327; Common Operation 3 lines 526-543; Pitfall 7)
  </read_first>
  <action>
    Implement ListPlan(ctx, orgID uuid.UUID, employeeID *uuid.UUID, periodStart, periodEnd time.Time) ([]direction.PlanRow, error): SELECT the direction rows (org-scoped; directed_to = $employeeID when employeeID != nil; planned_date within [periodStart, periodEnd] — for queued rows (planned_date NULL) also include rows whose due_date falls in the period OR no date at all (the queue is part of the plan view — include queued rows with due_date in range or NULL due_date); status IN ('draft','active')). Order: scheduled rows by planned_date; queued rows by priority ASC NULLS LAST then due_date ASC NULLS LAST then created_at (stable tiebreak — probe DIR-01 ordering edge).

    Derived states per row (computed in one pass per activity set): done — one query per DISTINCT activity_id of the returned rows using the re-anchored CTE: WITH RECURSIVE subtree AS (SELECT id FROM activities WHERE id=$1 UNION ALL SELECT a.id FROM activities a JOIN subtree s ON a.parent_id=s.id) SELECT NOT EXISTS (SELECT 1 FROM time_entries te WHERE te.is_deleted=false AND te.status IN ('draft','submitted','pending_manager','pending_finance') AND te.activity_id IN (SELECT id FROM subtree)) → done = true. lapsed — same subtree plus: planned_date (or due_date when queued) < today AND NOT EXISTS (any non-deleted time entry on the subtree — ANY status, A3). claimed spectrum for WG rows (wg_id IS NOT NULL): Σ est_hours of claim rows (origin_direction_id = row id, status <> 'cancelled') vs the row's est_hours: budget NULL → ClaimStateNotClaimed unless Σ > 0 → PartiallyClaimed; budget set → Σ == 0 → NotClaimed, 0 < Σ < budget → PartiallyClaimed, Σ == budget → FullyClaimed (D-13-15). Populate PlanRow{Done, Lapsed bool, ClaimState string}.

    Tests: (a) rows for the period with a terminal-activity row (seed time entries in non-terminal statuses on the subtree → done=false; none → done=true — seed entries via direct SQL or seedTimeEntry); (b) lapsed: past planned_date + no entries → lapsed=true; past planned_date + a draft entry on the subtree → lapsed=false (A3 pin); (c) WG row spectrum: no claims → not_claimed; claim 3 of 8 → partially_claimed; claim 5 more → fully_claimed; cancelled claim frees the Σ; (d) queued ordering: priority 2 before priority 1? No — priority LOWER = higher (D-13-06): priority 1 first, then 2; equal priorities ordered by due_date; equal both → created_at; (e) superseded + cancelled rows absent from the result.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestDirectionRepository_ListPlan|TestDirectionRepository_DerivedStates' -count=1</automated>
  </verify>
  <done>ListPlan returns draft|active rows with correct done/lapsed/claimed-spectrum derivation, stable ordering, history rows excluded.</done>
  <acceptance_criteria>
    - The done CTE is anchored at activities.id with the exact non-terminal predicate from the ticket CTE (semantic inversion NOT EXISTS)
    - lapsed requires NO non-deleted entries of any status on the subtree (a draft entry kills lapsed)
    - Claim spectrum derives correctly incl. fully_claimed only when budget set and Σ == budget; cancelled claims release their hours
    - Ordering: priority ASC (lower first) → due_date ASC → created_at; superseded/cancelled rows never appear
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Coverage read-model + AbsenceWindows</name>
  <files>
    internal/adapters/secondary/postgres/direction_repository.go
    internal/adapters/secondary/postgres/direction_repository_test.go
  </files>
  <read_first>
    migrations/012_staffing_schema.up.sql lines 15-30 (availability_windows columns + vocabularies)
    internal/adapters/secondary/postgres/coverage_repository.go lines 215-252 (per-day grouping read-model shape)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 6 lines 341-350; Pitfall 7 lines 441-445; Pitfall 10 lines 459-463)
    .planning/phases/13-direction-backend-the-plan-plane/13-UI-SPEC.md (coverage read-model shape contract)
  </read_first>
  <action>
    Implement Coverage(ctx, orgID uuid.UUID, employeeIDs []uuid.UUID, periodStart, periodEnd time.Time) ([]direction.CoverageRow, error): generate the day series for the period (date_trunc/generate_series over the bounds, or a Go-side day loop over [start..end]); for each (employee, day): capacity = (SELECT COALESCE(value::text::numeric, 8.0) FROM org_settings WHERE org_id=$1 AND key='planning_daily_hours') − absence hours — absence reduction per day: full windows (hours IS NULL, status IN declared/confirmed, starts_on <= day <= ends_on) → capacity 0 (away); partial windows (hours set) → capacity -= hours (floored at 0 — Pitfall 10: never negative, and a zeroed day is an away-day, not over-capacity); planned = Σ est_hours of rows status IN ('draft','active') AND directed_to = employee AND planned_date = day; gap = capacity − planned (may be negative — over-capacity). One row per (employee, day) for every day in the period (zero rows still surfaced — D-13-26 including 0 planned). Implement per-day SQL with a LEFT JOIN from generate_series or a single query grouped by employee+day with COALESCE(planned,0).

    Implement AbsenceWindows(ctx, orgID uuid.UUID, employeeIDs []uuid.UUID, start, end time.Time) ([]direction.AbsenceWindow, error): SELECT employee_id (column name per 012 — verify: availability_windows.employee_id), kind, starts_on, ends_on, hours FROM availability_windows WHERE org_id=$1 AND employee_id = ANY($2) AND status IN ('declared','confirmed') AND starts_on <= $4 AND ends_on >= $3 — BOTH statuses (D-13-29). Map to direction.AbsenceWindow{EmployeeID, Kind, StartsOn, EndsOn, Hours}.

    Tests: (a) capacity math — 8.0 default: no absences → capacity 8.0; a full holiday window (hours NULL) on one day → capacity 0; a partial permit (hours 3.5) → capacity 4.5; (b) multi-day window reduces each covered day once (Pitfall 10); overlapping windows both subtract; (c) planned Σ per day from two rows sharing the day (multiplicity) sums; superseded/cancelled rows excluded; (d) an employee with no direction → row with planned 0 and gap == capacity (uncovered day surfaced — D-13-26); (e) custom planning_daily_hours=7.5 via org_settings row changes capacity (COALESCE fallback covered); (f) AbsenceWindows returns declared AND confirmed rows, org-scoped.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestDirectionRepository_Coverage|TestDirectionRepository_Warnings|TestDirectionRepository_AbsenceWindows' -count=1</automated>
  </verify>
  <done>Coverage math proven per day incl. absence-aware capacity and planned Σ; AbsenceWindows returns declared+confirmed rows.</done>
  <acceptance_criteria>
    - capacity = planning_daily_hours (COALESCE 8.0) − absence hours; full absence zeroes the day; partial reduces; floor at 0 (never negative capacity)
    - planned counts only draft|active rows for the employee on that exact day; Σ of multiple rows per day sums
    - A zero-planned day is surfaced (gap == capacity) — the D-13-26 uncovered-day shape
    - AbsenceWindows includes status 'declared' AND 'confirmed' rows within the window overlap, org-scoped
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: FirstDirectionRefs (origin fallback lookup) + tests</name>
  <files>
    internal/adapters/secondary/postgres/direction_repository.go
    internal/adapters/secondary/postgres/direction_repository_test.go
  </files>
  <read_first>
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 8 lines 361-364 — the fallback contract; D-13-32..34)
    internal/core/domain/direction/direction.go (DirectionRefs struct)
    internal/adapters/secondary/postgres/ticket_repository.go (small single-query method shape)
  </read_first>
  <action>
    Implement FirstDirectionRefs(ctx, orgID, activityID uuid.UUID) (*direction.DirectionRefs, error): SELECT directed_by, directed_to FROM direction WHERE org_id=$1 AND activity_id=$2 AND status <> 'cancelled' ORDER BY created_at ASC, id ASC LIMIT 1 (idx_direction_activity_created serves it) → pgx.ErrNoRows → (nil, nil) (refs stay empty — D-13-33); else return &DirectionRefs{AssignedBy: &directed_by, AssignedTo: directed_to} — manager-assignment shape ONLY, never proposed_by/reviewed_by/ticket_id (D-13-33). Never writes anything (D-13-34 — read-only by construction).

    Tests: (a) activity with two rows — the earliest created_at non-cancelled row wins; (b) a cancelled earliest row is skipped in favor of a later non-cancelled row; (c) no rows at all → (nil, nil); (d) rows exist but all cancelled → (nil, nil); (e) rows for another org → (nil, nil) (org-scoped); (f) a WG row (wg_id set, directed_to NULL) → AssignedBy set, AssignedTo nil (the fallback shape carries what exists).
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestDirectionRepository_FirstDirectionRefs' -count=1</automated>
  </verify>
  <done>FirstDirectionRefs resolves earliest non-cancelled refs org-scoped; nil contracts hold; never writes.</done>
  <acceptance_criteria>
    - Earliest created_at non-cancelled row supplies assigned_by/assigned_to; cancelled rows skipped; none → nil
    - The query is org-scoped and read-only (no INSERT/UPDATE path in the method)
    - The DirectionRefs shape carries only the manager-assignment pair
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/adapters/secondary/postgres/direction_repository.go` — read-model methods: `ListPlan` (derived done/lapsed/claimed-spectrum), `Coverage` (per employee/day capacity/planned/gap), `AbsenceWindows` (declared+confirmed), `FirstDirectionRefs`; internal `terminalActivitySubtree`/`hasAnyEntries` CTE helpers
- `internal/adapters/secondary/postgres/direction_repository_test.go` — read-model integration tests: derived states, ordering, coverage capacity math, absence windows, fallback refs

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-model queries → PostgreSQL | the risk is wrong-row counting (statuses/scopes/absence math), not injection (parameterized) |
| FirstDirectionRefs → activity read path | derived refs must never be written back (D-13-34) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-19 | Tampering | coverage row selection | high | mitigate | Eligibility predicate pinned: status IN ('draft','active') AND planned_date = day AND directed_to = employee — superseded/cancelled history rows can never inflate planned Σ (Pitfall 7); asserted in Task 2 tests (superseded row excluded) |
| T-13-20 | Tampering | capacity math | high | mitigate | Full absence zeroes the day, partial reduces by hours, floor at 0 (Pitfall 10 — a zeroed day is 'away', never negative capacity that would fake over-capacity); multi-day windows subtract once per day; asserted in Task 2 tests |
| T-13-21 | Information disclosure | FirstDirectionRefs | medium | mitigate | Org-scoped query (org_id = $1) — the fallback cannot leak another org's direction data into the activity read path; nil-on-no-rows leaks no existence oracle beyond the activity's own visibility |
| T-13-22 | Tampering | derived-state accuracy | medium | mitigate | Derived states computed from the same CTE semantics as the ticket dismissal guard (single subtree walk, non-terminal predicate verbatim — semantic inversion documented); lapsed predicate pinned (A3) so read-model output is deterministic |

No high-severity unmitigated threats: T-13-19/20 are mitigated by the pinned predicates + integration assertions. No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestDirection' -count=1` exits 0 (mutators + read-models together)
- `go build ./...` compiles
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- ListPlan derives done/lapsed/claimed on read with deterministic predicates; history rows never appear
- Coverage math (capacity − absences, planned Σ, gap) proven per day incl. the fully-absent-day and zero-planned-day shapes
- FirstDirectionRefs fulfills the D-13-32..34 contract for the 13-08 fallback wiring
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-06-SUMMARY.md` when done
</output>
