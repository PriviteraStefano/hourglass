---
phase: 13-direction-backend-the-plan-plane
plan: 08
type: execute
wave: 5
depends_on:
  - 13-04
  - 13-06
  - 13-07
files_modified:
  - internal/adapters/primary/http/direction_handler.go
  - internal/adapters/primary/http/direction_handler_test.go
  - cmd/server/main.go
  - internal/adapters/primary/http/handler_test_helper.go
  - internal/core/services/activity/activity.go
  - internal/core/services/activity/activity_origin_fallback_test.go
  - internal/core/services/activity/activity_test.go
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
  - DIR-05
  - DIR-06
  - FND-04

estimate:
  tokens: 40000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The seven direction routes are registered in cmd/server/main.go with middleware.Auth: POST /direction, POST /direction/{id}/activate, POST /direction/{id}/cancel, POST /direction/claims, POST /direction/claims/{id}/cancel, GET /direction, GET /direction/coverage (Go 1.22+ method patterns, kebab-case, coverage-wiring style) (DIR-01..06)"
    - "The origin fallback is wired into the activity read path: activity service GetByID + List, when OriginType == nil, call the refs provider (FirstDirectionRefs) and set AssignedBy/AssignedTo on the response — derived, NEVER written back, origin_type stays NULL (D-13-32..34, A4/OQ3); the activity service constructor gains the refs-provider parameter and ALL call sites (cmd/server/main.go, handler_test_helper.go, activity service tests, testdata mocks) compile"
    - "Contract test: an activity with empty origin refs + one seeded direction row → GET /activities response carries derived assigned_by/assigned_to; no direction rows → refs stay empty; stored origin refs (origin_type set) stay authoritative — the fallback never overrides them (Pitfall 5, D-13-34)"
    - "POST /direction parses the create request (directed_to XOR wg_id, activity_id, planned_date, est_hours, priority, due_date, supersedes_id), calls the service, returns 200 {row, warnings} — the warnings ride the create response (D-13-03); sentinel map: ErrDirectionNotFound → 404, ErrInvalidRequest/ErrInvalidHours/ErrInvalidTarget/ErrCancelReasonRequired → 400, ErrForbidden/ErrNotWgMember → 403, ErrInvalidTransition/ErrClaimOverBudget/ErrWgRowNotActive → 409, default → 500 (coverage_handler writeError shape)"
    - "POST /direction/{id}/activate + /{id}/cancel (body {reason}) + POST /direction/claims (body {wg_row_id, est_hours}) + POST /direction/claims/{id}/cancel (body {reason}) + GET /direction (employee_id + period_start + period_end params) + GET /direction/coverage (scope + scope_id + period_start + period_end params) all parse + map sentinels + envelope via pkg/api (D-13-10, D-13-16, D-13-25, OQ5)"
    - "Handler integration tests prove the permission matrix end-to-end: self-direction 200; non-manager creating for another employee in manager_planned mode → 403; claim by non-member → 403; claim over budget → 409; activate twice → 409; unauthenticated → 401; create response includes the warnings array"
  artifacts:
    - internal/adapters/primary/http/direction_handler.go
    - internal/adapters/primary/http/direction_handler_test.go
    - internal/core/services/activity/activity_origin_fallback_test.go
  key_links:
    - "cmd/server/main.go wires directionRepo → directionService (orgsettings + routing deps) → directionHandler + 7 routes, AND passes directionRepo into the activityService constructor (fallback seam) — one wiring task, Pitfall 5 closed"
    - "handler_test_helper.go mirrors the same wiring so handler tests exercise the real stack; the activityService fixture line updates with the new constructor"
    - "The fallback predicate is OriginType == nil (A4); refs derived from the earliest non-cancelled direction row via FirstDirectionRefs (13-06)"
  flagged_assumptions:
    - "POST /direction/{id}/activate is the explicit activation endpoint (OQ1 recommendation — one audit row per transition, matrix stays simple; create-with-planned_date does NOT auto-activate)"
    - "GET /direction requires period_start + period_end (2006-01-02 strings parsed at the boundary — OQ5); employee_id is optional and means the org-wide default view — which the service gate (13-07 T3) restricts to managers: non-managers MUST pass their own employee_id or get 403; GET /direction/coverage requires scope + scope_id + period bounds, and non-managers may only use scope=employee with scope_id == self (13-07 T3)"
    - "The create response shape is {row, warnings} (D-13-03); read responses are {rows/coverage rows + totals, warnings} per the 13-UI-SPEC contract"
  prohibitions:
    - "Never write derived origin refs back to activities — the fallback is read-only derivation; stored refs (origin_type set) stay authoritative (D-13-34)"
---

<objective>
The HTTP surface of the plan plane + the origin fallback wiring: the direction handler (7 routes), the cmd/server wiring (direction + orgsettings services already in 13-04 + activity service constructor with the fallback seam), and the activity read-path origin fallback with its contract test.

Purpose: The phase's endpoints become real, and FND-04's promise lands — activities with empty origin refs derive manager-assignment refs from the first direction record, read-only. This closes the phase: DIR-01..06 all reachable over HTTP.
Output: direction_handler.go + tests, wiring, activity service fallback + tests.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md
@internal/adapters/primary/http/coverage_handler.go
@internal/adapters/primary/http/direction_handler.go
@cmd/server/main.go
@internal/adapters/primary/http/handler_test_helper.go
@internal/core/services/activity/activity.go
@internal/core/services/activity/activity_test.go
@internal/core/services/testdata/mocks.go
@internal/core/ports/direction_repository.go
@internal/adapters/secondary/postgres/direction_repository.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Origin fallback in the activity read path (service + tests + mocks)</name>
  <files>
    internal/core/services/activity/activity.go
    internal/core/services/activity/activity_origin_fallback_test.go
    internal/core/services/activity/activity_test.go
    internal/core/services/testdata/mocks.go
  </files>
  <read_first>
    internal/core/services/activity/activity.go lines 35-90 (Service struct + NewService + List + GetByID — the constructor change site)
    internal/core/domain/activity/activity.go lines 79-97 (origin fields + ActivityResponse)
    internal/core/services/activity/activity_test.go (NewService call sites to update)
    internal/core/services/testdata/mocks.go (MockActivityRepo constructor shape)
    internal/core/ports/direction_repository.go (FirstDirectionRefs signature)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (Pattern 8 lines 361-364; Pitfall 5 lines 429-433; A4/OQ3)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (activity MODIFY analog lines 466-483)
  </read_first>
  <action>
    Modify internal/core/services/activity/activity.go: Service gains a `directionRefs ports.DirectionRepository` field (the full port is the simplest wiring seam — the mock in testdata satisfies it); NewService gains the parameter (append after ticketRepo, before routing). Wrap the read path: in GetByID (line ~70) and List (line ~54) — after the repo returns the activity/response: if a.OriginType == nil (A4 predicate — no origin set at all): refs, err := s.directionRefs.FirstDirectionRefs(ctx, orgID, activityID); err → return err; refs != nil → a.AssignedBy, a.AssignedTo = refs.AssignedBy, refs.AssignedTo (derived — never written back, D-13-34; origin_type stays NULL — OQ3). List applies the same enrichment per row (batch: call FirstDirectionRefs per activity with empty origin_type; acceptable volume for v0.2 org-scoped lists).

    Update ALL NewService call sites: internal/core/services/activity/activity_test.go + the other activity service test files (activity_origin_test.go, activity_proposal_test.go, activity_beneficiary_unit_test.go) — pass a MockDirectionRepo (testdata) as the new arg; testdata/mocks.go MockActivityRepo unchanged (it is the activity REPO mock; the direction refs come from MockDirectionRepo which already implements FirstDirectionRefs).

    Create activity_origin_fallback_test.go (unit, mocks): (a) activity with OriginType nil + MockDirectionRepo returning refs → GetByID response carries assigned_by/assigned_to; (b) OriginType nil + FirstDirectionRefs nil → refs stay empty; (c) OriginType = 'customer_ticket' set → fallback NOT invoked (stored refs authoritative — assert FirstDirectionRefs not called); (d) List enriches every empty-origin row.
  </action>
  <verify>
    <automated>go test ./internal/core/services/activity/ -run 'TestActivityOriginFallback' -count=1</automated>
  </verify>
  <done>The fallback is implemented at the service layer with the A4 predicate; all activity-service call sites compile with the new constructor; contract tests prove derivation + authority.</done>
  <acceptance_criteria>
    - activity.go NewService has the directionRefs parameter and GetByID/List apply the OriginType == nil predicate with derived AssignedBy/AssignedTo (no write-back)
    - All NewService call sites in the activity package + testdata compile with the new argument
    - activity_origin_fallback_test.go proves: derived refs appear, empty refs stay empty, stored origin_type blocks the fallback, List enriches per-row
    - go build ./internal/core/services/activity/ ./internal/core/services/testdata/ exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: cmd/server + fixture wiring (direction service/handler/routes + activityService constructor)</name>
  <files>
    cmd/server/main.go
    internal/adapters/primary/http/handler_test_helper.go
  </files>
  <read_first>
    cmd/server/main.go lines 140-260 (coverage wiring block + route registrations incl. the literal /organizations/settings routes from 13-04 + GET/PUT /organizations/{id}/settings)
    cmd/server/main.go line ~146 (activityService := activitysvc.NewService(...) — gains the direction repo arg)
    internal/adapters/primary/http/handler_test_helper.go lines 48-101 (fixture repos/services blocks — mirror the main.go wiring)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (wiring lines 485-503; fixture analog)
  </read_first>
  <action>
    cmd/server/main.go: after the orgsettings block (13-04): directionRepo := postgres.NewDirectionRepository(pool); directionService := directionsvc.NewService(directionRepo, activityRepo, wgRepo, unitRepo, orgRepo, orgSettingsService, routingSvc) — constructor per 13-07's pin; directionHandler := http.NewDirectionHandler(directionService); activityService := activitysvc.NewService(activityRepo, contractRepo, unitRepo, orgRepo, ticketRepo, directionRepo, routingSvc) — the fallback seam (Pitfall 5 — one task, compile-forced). Register the seven routes with middleware.Auth: "POST /direction", "POST /direction/{id}/activate", "POST /direction/{id}/cancel", "POST /direction/claims", "POST /direction/claims/{id}/cancel", "GET /direction", "GET /direction/coverage". Do NOT touch the /organizations/settings + /organizations/{id}/settings registrations (13-04/Pitfall 6).

    handler_test_helper.go: mirror the same wiring in newHandlerFixture — directionRepo/service/handler + the seven routes + update the activityService fixture line with the directionRepo arg (line ~85) so handler tests exercise the real stack.
  </action>
  <verify>
    <automated>go build ./... && go vet ./cmd/server/ ./internal/adapters/primary/http/</automated>
  </verify>
  <done>Everything compiles: direction + activity fallback wired end-to-end in both main.go and the fixture.</done>
  <acceptance_criteria>
    - cmd/server/main.go constructs directionService with the 13-07 constructor and registers all seven routes with middleware.Auth
    - cmd/server/main.go activityService call includes directionRepo (fallback seam)
    - handler_test_helper.go newHandlerFixture mirrors the wiring (direction routes live)
    - go build ./... exits 0; go vet clean on the touched packages
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Direction handler + integration test battery (permission matrix, envelope, sentinels, warnings in create)</name>
  <files>
    internal/adapters/primary/http/direction_handler.go
    internal/adapters/primary/http/direction_handler_test.go
  </files>
  <read_first>
    internal/adapters/primary/http/coverage_handler.go (whole file — claims/parse/writeError skeleton lines 81-97, 306-333)
    internal/adapters/primary/http/coverage_handler_test.go (handler integration test shape with newHandlerFixture + registerAndLogin)
    internal/middleware/middleware.go lines 67-85 (GetOrganizationID/GetUserID/GetRole)
    .planning/phases/13-direction-backend-the-plan-plane/13-PATTERNS.md (handler analog lines 229-293)
    .planning/phases/13-direction-backend-the-plan-plane/13-RESEARCH.md (discretion pins: route list; Common Operation 4 — warnings in create response)
  </read_first>
  <action>
    Create internal/adapters/primary/http/direction_handler.go: DirectionHandler{service *directionsvc.Service} + NewDirectionHandler. Routes as thin adapters (coverage_handler skeleton — ctx claims, uuid.Parse for PathValue ids, json decode, service call, RespondWithJSON/RespondWithError): Create (POST /direction — body DirectedTo/WgID/ActivityID/PlannedDate/EstHours/Priority/DueDate/SupersedesID; 200 with {row, warnings}), Activate (POST /direction/{id}/activate), Cancel (POST /direction/{id}/cancel — body {reason}), Claim (POST /direction/claims — body {wg_row_id, est_hours}), Unclaim (POST /direction/claims/{id}/cancel — body {reason}), ListPlan (GET /direction — query employee_id optional + period_start + period_end required, parsed as 2006-01-02 → time), Coverage (GET /direction/coverage — scope + scope_id + period_start + period_end required). writeError switch (coverage shape): ErrDirectionNotFound → 404; ErrInvalidRequest/ErrInvalidHours/ErrInvalidTarget/ErrCancelReasonRequired → 400; ErrForbidden/ErrNotWgMember → 403; ErrInvalidTransition/ErrClaimOverBudget/ErrWgRowNotActive → 409; default → 500.

    Create direction_handler_test.go (integration, newHandlerFixture — real PG stack): (a) permission matrix — self-direction create 200 + response contains warnings array; employee creating for a colleague in manager_planned org → 403; WG claim by non-member → 403; claim over budget → 409; activate a cancelled row → 409; unauthenticated → 401; (b) sentinel mapping — unknown direction id activate → 404; cancel without reason → 400; malformed body → 400; (c) create-with-supersedes → 200 and the superseded target visible as history via GET /direction (status superseded absent from rows); superseding a claim-row target (claim → create-with-supersedes scheduling the claim row) → 200 with origin_direction_id carried — the repo tx (13-05) owns the carry, the handler body needs no special field beyond supersedes_id; (d) GET /direction requires period bounds → 400 when missing; (e) read gates at the HTTP boundary: non-manager GET /direction WITHOUT employee_id → 403 (org-wide view is manager-only); non-manager GET /direction with employee_id = another employee → 403; non-manager GET /direction with employee_id = self → 200; non-manager GET /direction/coverage scope=employee scope_id=other → 403; non-manager scope=employee scope_id=self → 200; (f) GET /direction/coverage with scope=employee + valid period → 200 with rows + totals + warnings; scope=unknown → 400; (g) the fallback contract at the HTTP boundary: activity with empty origin refs + a seeded direction row → GET /activities response carries derived assigned_by/assigned_to (Pitfall 5 lock — the FND-04 e2e).
  </action>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run TestDirectionHandler -count=1</automated>
  </verify>
  <done>The full HTTP surface works end-to-end with the permission/validation matrix and the fallback contract proven at the API boundary.</done>
  <acceptance_criteria>
    - direction_handler.go implements the 7 routes with the coverage-handler skeleton and the pinned writeError map (404/400/403/409/500)
    - direction_handler_test.go covers: self-direction 200 with warnings, 403 cross-employee manager_planned create, 403 non-member claim, 409 over-budget claim, 409 double-activate, 401 unauthenticated, 404 unknown id, 400 reason-less cancel, 400 missing period bounds, coverage 200 rows+totals+warnings, scope=unknown 400, non-manager org-wide GET /direction → 403, non-manager employee_id=other → 403, employee_id=self → 200, non-manager coverage scope_id != self → 403, scope_id == self → 200
    - The HTTP-boundary fallback test: GET /activities derives assigned_by/assigned_to for an empty-origin activity with a seeded direction row; stays empty without rows
    - go test ./internal/adapters/primary/http/ -run TestDirectionHandler -count=1 exits 0
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/adapters/primary/http/direction_handler.go` — `DirectionHandler` (Create/Activate/Cancel/Claim/Unclaim/ListPlan/Coverage) + `writeError` sentinel map
- `internal/adapters/primary/http/direction_handler_test.go` — integration battery (permission matrix, sentinels, warnings, coverage, fallback e2e)
- `internal/core/services/activity/activity.go` — `Service.directionRefs` field + `NewService` param + GetByID/List fallback enrichment
- `internal/core/services/activity/activity_origin_fallback_test.go` — fallback unit contract tests
- `cmd/server/main.go` — direction wiring + 7 routes + activityService constructor update
- `internal/adapters/primary/http/handler_test_helper.go` — fixture wiring mirror

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → direction handler | unauthenticated/unauthorized direction writes and org read-models cross here |
| GET /activities → origin fallback | derived refs enter the activity read response — must never be persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-28 | Elevation of privilege | direction endpoints | high | mitigate | Every route is middleware.Auth-wrapped; the service gate chain (13-07 T-13-23/24) is the authorization core; the handler adds nothing permissive; the integration battery asserts 401/403 paths end-to-end |
| T-13-29 | Tampering | sentinel mapping leakage | medium | mitigate | writeError maps every sentinel to a fixed status (404/400/403/409/500); ErrClaimOverBudget/ErrInvalidTransition surface as 409 not 500 — asserted in the battery (over-budget claim, double-activate) |
| T-13-30 | Tampering | derived-ref write-back | high | mitigate | The fallback sets response fields only (D-13-34); no repo write exists in the path; the contract test proves stored origin_type blocks derivation and nothing is persisted (read-only by construction) |
| T-13-31 | Information disclosure | GET /direction + /coverage scoping | medium | mitigate | Service-level read gates under Auth (13-07 T-13-26): ListPlan employee filter self-or-manager with the omitted employee_id org-wide view manager-only; Coverage unit/wg scopes manager-only and employee scope non-manager requires scope_id == actorID; handler passes claims through without widening; the 403 battery (org-wide read, other-employee id, other-id coverage scope) asserts the gates at the HTTP boundary |
| T-13-32 | Tampering | input parsing | medium | mitigate | uuid.Parse failures → 400; period strings parsed as 2006-01-02 → 400 on malformed input; JSON decode errors → 400 — no 500 path for client input |

No high-severity unmitigated threats: T-13-28/30 mitigated by Auth + service gates + contract tests. No package installs this phase (Go module set unchanged — RESEARCH.md Package Legitimacy Audit), so the T-13-SC package-legitimacy row is not applicable.
</threat_model>

<verification>
- `go test ./internal/adapters/primary/http/ -run TestDirectionHandler -count=1` exits 0
- `go test ./internal/core/services/activity/ -count=1` exits 0
- `go build ./...` + `go vet ./...` clean
- Phase gate: full suite green — `make test`
</verification>

<success_criteria>
- All seven direction routes live and pass the permission/validation battery
- The origin fallback is proven at both the service and HTTP boundaries (FND-04 lands; Pitfall 5 closed)
- The whole phase compiles and the full suite is green — DIR-01..06 reachable over HTTP
</success_criteria>

<output>
Create `.planning/phases/13-direction-backend-the-plan-plane/13-08-SUMMARY.md` when done
</output>
