---
phase: 13
slug: 13-direction-backend-the-plan-plane
status: draft
shadcn_initialized: true
preset: base-mira
created: 2026-08-08
updated: 2026-08-08
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Phase disposition: BACKEND-ONLY — no UI deliverables.** Phase boundary locked in `13-CONTEXT.md` ("Backend-only phase (no UI)"). This contract therefore (a) freezes the existing design system state so the phase introduces **zero** frontend changes, and (b) pins the API-level data contracts (status vocabularies, warning objects, read-model shapes, settings keys) that the Phase 19 Direction Surfaces will render verbatim. The executor must NOT create or modify any frontend files; the auditor should verify "no UI change" as the correct outcome.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (detected: `web/components.json`) |
| Preset | `base-mira` style, base color `olive`, CSS variables (`web/src/index.css`), Tailwind v4 (`@import "tailwindcss"`, no tailwind.config) |
| Component library | base-ui (shadcn `style: base-mira`; components alias `@base-ui/react`, ui alias `@/components/ui`; 55 components present as of 2026-08-08) |
| Icon library | lucide (`components.json` → `"iconLibrary": "lucide"`, `lucide-react ^1.27.0`) |
| Font | Inter variable (`@fontsource-variable/inter`); display fonts Martian Mono 200 / Geist Mono 300 (Google Fonts imports in `index.css`) already wired via `.font-display` / `.font-text` |

**Change status: FROZEN.** No tokens, components, or fonts are added, changed, or removed this phase. Phase 15 (UX Foundation) is the designated token/component-freeze phase; Phase 19 consumes this contract's API shapes.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none — **no spacing decisions or usage occur in this phase** (no UI surface). Scale recorded for continuity so Phase 19 plans against the same tokens. Source: Tailwind v4 default scale, no project overrides detected (verified `web/src/index.css` 2026-08-08).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (text-base) | 400 | 1.5 |
| Label | 14px (text-sm) | 400 | 1.4 |
| Heading | 20px (text-xl) | 600 | 1.2 |
| Display | 28px | 600 | 1.2 |

Contract weights: 400 (regular) + 600 (semibold) only. Existing decorative mono weights (Martian Mono 200 / Geist Mono 300) remain untouched as-is.

**Change status: FROZEN** — no typography work this phase. Contract recorded so Phase 19 UI (scheduler calendar, direction queue, coverage grid) typesets against the same roles. Source: Tailwind v4 defaults + `index.css` variables (verified 2026-08-08; no user input needed).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` white / `--base-950` dark | Page background, surfaces |
| Secondary (30%) | `--card` white / `--secondary` `--base-100`, sidebar `--base-100` | Cards, sidebar, nav |
| Accent (10%) | `--primary` `--primary-400` (olive, oklch 0.765 0.177 163.22) | Primary buttons, active nav, focus ring (`--ring`), selection |
| Destructive | `--destructive` oklch(0.577 0.245 27.325) | Destructive actions only (light); dark-mode variant already defined |

Accent reserved for: primary actions, active navigation state, focus/selection rings. Never used for status semantics — **status colors are Phase 15's semantic-token job (UXFD-01)**, not this phase's.

**Change status: FROZEN** — no color work this phase. Values verified against `web/src/index.css` `:root` (light) + `.dark` block 2026-08-08; dark-mode tokens already present. The derived-state vocabulary in the API Data Contracts section below is the future input to Phase 15's status palette, recorded here so no backend phase introduces ad-hoc hex values.

---

## Copywriting Contract

Phase 13 has **no UI copy** (no surface). The following are server-emitted strings the Phase 19 UI renders verbatim — the executor pins these formats in the API contract so future surfaces display them without transformation.

| Element | Copy |
|---------|------|
| Primary CTA | N/A — no UI surface this phase |
| Empty state heading | N/A — no UI surface this phase |
| Empty state body | N/A — no UI surface this phase |
| Warning message (DIR-05 / D-13-30) | Structured warning objects: `{ type, message }` where `message` is pre-rendered server-side: `"Away 10–21 Aug"` (type `away`), `"Partial 14 Aug"` (type `partial`), `"Over capacity 16 Aug"` (type `over-capacity`). Types closed set: `away` \| `partial` \| `over-capacity`. Message = `{Type} {date-range-or-day}` — UI renders `message` verbatim, never re-formats. Format sourced from REQUIREMENTS.md DIR-05 ("warns at plan time — 'away 10–21 Aug'") and ROADMAP Phase 19 SC2 (same example) |
| Validity warning (D-13-31) | Type `invalid` (outside employment validity) — surfaced on read-model, employee never flagged uncovered; message format `"Outside validity period"` |
| Error state (house style) | `{ "error": "<problem description>" }` via `pkg/api` envelope; sentinel errors → 400/403/409 with descriptive message; client auto-refresh on 401 already handled by `web/src/lib/api.ts` — no UI work needed |
| Destructive confirmation | N/A for UI. Backend contract: cancellation of a direction row (D-13-10) and unclaim (D-13-16) **require a reason** — API rejects reason-less destructive writes with 400. Future UI (Phase 19) must present a required-reason confirm dialog; the API enforces the invariant server-side regardless |

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe (Step 9.5) and lifted by plan-phase's
> `## UI Considerations` lift rule. Shape-rooted UI *state* coverage (empty / loading / error /
> populated / partial / overflow / zero-one-many / long-text).

Applicable state considerations resolved: 0 covered, 0 backstop, 0 unresolved — **none applicable**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| — | — | ✅ covered | No UI elements exist in this phase — backend-only (CONTEXT.md phase boundary). The 8-category taxonomy's relevance filter raises zero considerations for an empty element list; silence is resolved by the phase-boundary reason string. Future UI states (direction queue empty/loading, coverage grid overflow, long activity titles in scheduler cells, claim zero-one-many) are deferred to Phase 15/19 probes, where elements actually exist |

<!-- Status vocabulary (locked by probe-core projectTruths):
     ✅ covered   → a plain truth string lifted into must_haves.truths
     🧪 backstop  → a flat scalar { statement, verification: backstop }; at verify time, no explicit
                    evidence → insufficient_spec → human_needed (never a silent pass, #1154)
     ⚠ unresolved → an explicit planner assumption (surfaced, never silently dropped)
     Rows are REPLACED (not appended) on a probe re-run — idempotent. -->

---

## API Data Contracts with UI Destiny (Phase 19 inputs)

Pinned here because the Phase 19 surfaces (scheduler calendar, direction queue, coverage read-model, org-policy settings) will consume these shapes. The backend phase MUST emit these exact vocabularies; Phase 15 maps them to StatusBadge variants.

| Contract | Values (closed sets) | Source |
|----------|----------------------|--------|
| Direction row status | `draft` → `active` → `superseded` \| `cancelled` | D-13-07 |
| Derived states (never stored, computed on read) | `done`, `lapsed`, `claimed` | D-13-09 |
| Claim spectrum (derived) | `not_claimed` → `partially_claimed` → `fully_claimed` | D-13-15 |
| Warning types | `away` \| `partial` \| `over-capacity` \| `invalid` (validity) | D-13-30, D-13-31 |
| Planning mode | `manager_planned` \| `self_planned` (org default + per-employee override) | D-13-19 |
| Coverage read-model shape (per employee/period) | `(employee, date, capacity, planned, gap)` + period totals; fully-absent days excluded from uncovered surfacing | D-13-26 |
| Scope params | `scope=employee\|unit\|wg` + `scope_id` + `period` | D-13-25 |
| Org settings endpoints | `GET/PUT /organizations/settings` `{key: value}` JSONB, manager+-gated, JWT-resolved org, additive keys | D-13-23 |
| Settings key vocabulary | `planning_daily_hours` (default 8; capacity denominator) + any other keys validated in code per known key — API returns 400 on unknown keys | D-13-24, D-13-18 |
| est_hours semantics | Scheduled rows: `est_hours` required, `> 0` (service rejects ≤ 0 with 400). Queued rows: optional — row-level total budget for the activity. Σ est_hours over day capacity = soft warning, never a rejection | D-13-02, D-13-03 |
| Supersede chain | Replanning creates a new row with `supersedes_id`; the superseded row flips to `superseded` in the same transaction. Superseded rows keep their audit trail as history — no separate supersede endpoint | D-13-08 |
| Claim consumption rule | Σ claimed ≤ WG row `est_hours` enforced under tx lock; uncapped when WG budget absent; unclaim = cancel claim row with reason | D-13-13, D-13-14, D-13-16 |

**Phase 19 consumption map** (how each contract above lands in a surface):

| Phase 19 surface | Consumes |
|------------------|----------|
| Scheduler calendar (SURF-07) | Direction row status, est_hours semantics (drop pre-fills from remaining day capacity), warning types overlay (`away`/`partial`/`over-capacity`), validity `invalid` on read-model |
| Direction queue (SURF-08) | Row status + derived states (`done`/`lapsed`/`claimed`), claim spectrum, supersede chain (history rendering), priority + due_date ordering |
| Coverage read-model view (SURF-08) | Coverage read-model shape, scope params, planning mode |
| Org policy config UI (D-X, Phase 19 SC5) | Settings key vocabulary + planning mode |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none added this phase (55 components already installed and untouched) | not applicable — no new registry items |
| Third-party | none — `components.json` `"registries": {}` | not applicable — no third-party registry declared |

Vetting gate result: no third-party registries declared, therefore no `shadcn view` scan required. `2026-08-08` (update run) — re-verified `web/components.json` still contains an empty `registries` object and no new items were added since the baseline pass.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** {pending / approved YYYY-MM-DD}
