---
plan: 08-02
title: Frontend Completion — List Views, Customers Route, Error Boundaries
wave: 1
depends_on: []
files_modified:
  - web/src/routes/_authenticated/time-entries/index.tsx
  - web/src/components/time-entries/time-entries-page.tsx
  - web/src/components/expenses/expenses-page.tsx
  - web/src/components/shared/entries-table.tsx
  - web/src/routes/_authenticated/customers/index.tsx
  - web/src/routes/_authenticated/customers/$id.tsx
  - web/src/components/layout/sidebar.tsx
  - web/src/routes/_authenticated.tsx
  - web/src/components/layout/route-error.tsx
autonomous: true
requirements: [P0-2, P0-3, P0-4]
---

# Plan 08-02 — Frontend Completion

Closes the three frontend P0 findings (P0-2 list views, P0-3 customers index route, P0-4 error boundaries). Independent of Plan 01 — runs in the same wave. The `/customers` index route unblocks manual smoke for Plans 03/04; the shared table component should exist before the list tabs are filled in (build order within this plan, not cross-plan coupling).

<task id="1">
<type>execute</type>

<action>
Create the `/customers` index route (P0-3). The list page component already exists but is unreachable — only `/customers/$id` has a route file.

- Add `web/src/routes/_authenticated/customers/index.tsx` following the directory-per-route convention (ADR-FE-017), rendering the existing customers list page component
- Wire a route loader with `client.ensureQueryData(customersApis.list())` per ADR-FE-004, mirroring an existing list route (contracts is the closest analog)
- Keep `$id.tsx` where it is (directory layout: `customers/index.tsx` + `customers/$id.tsx`)
- Verify `sidebar.tsx` Customers link points to `/customers` (it was built for Phase 3; fix the `to` prop if it points elsewhere)
- Add the route to the generated tree by running the dev server / route-gen (TanStack Router file-based codegen)
</action>

<read_first>
- `web/src/routes/_authenticated/contracts/index.tsx` (list-route + loader pattern to mirror)
- `web/src/components/customers/` (existing list page component — its export name and props)
- `web/src/lib/api/customers.ts` (customersApis list queryOptions)
- `web/src/components/layout/sidebar.tsx` (Management group Customers link)
- ADR-FE-004 (route-level data loading), ADR-FE-017 (routing patterns)
</read_first>

<acceptance_criteria>
- `web/src/routes/_authenticated/customers/index.tsx` exists and lazy-loads the customers list component
- Navigating to `/customers` renders the list with data (no 404, no blank screen)
- Sidebar Customers link resolves to `/customers`
- `customers.spec.ts` Playwright suite (or a new minimal spec) covers: sidebar → /customers → list visible → click row → detail at `/customers/$id`
- `bun run build` (or `npm run build`) regenerates `routeTree.gen.ts` including the new route without type errors
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>

<action>
Implement the time-entries list view (P0-2, part 1). The `TabsContent value="list"` in the time-entries page is a placeholder comment — replace it with a real, filterable table built on the shared `EntriesTable` shell (task 4 builds it first if this task lands earlier in your queue; coordinate on the shared component's API).

- Columns: date, project (name), description (truncated), hours, status badge (draft/submitted/pending_manager/pending_finance/approved/rejected), actions (view detail)
- Filters: status multi-select + date-range picker, driven through `validateSearch` Zod params per ADR-FE-017 so filter state is URL-shareable
- Data: reuse the existing time-entries list API hook + query key `['time-entries', ...]` (ADR-FE-003); do not invent a new endpoint
- Client-side pagination per ADR-FE-018 conventions (page size 25)
- Empty state: friendly message + "new entry" affordance (matches calendar tab tone)
- Row click → opens the existing EntryDetail surface (same component the calendar tab uses)
</action>

<read_first>
- `web/src/components/time-entries/time-entries-page.tsx` (tabs structure, placeholder location)
- `web/src/routes/_authenticated/time-entries/index.tsx` (existing validateSearch + loaderDeps pattern)
- `web/src/lib/api/time-entries.ts` (list queryOptions + response shape)
- `web/src/components/time-entries/entry-detail.tsx` (detail surface to open on row click)
- `web/src/components/ui/table.tsx` + existing badge components (shadcn primitives, ADR-FE-010)
</read_first>

<acceptance_criteria>
- List tab renders a populated table (no placeholder comment remains in source)
- Status filter narrows rows; the URL query string updates and a page reload restores the filter
- Status badge renders all six workflow states with distinct styling
- Row click opens EntryDetail for the clicked entry
- `npm run test` — new component/API tests for the list pass; existing time-entries tests stay green
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>

<action>
Implement the expenses list view (P0-2, part 2) on the same shared table shell.

- Columns: date, project (name), category (9 categories), description (truncated), amount (+currency), status badge, receipt indicator (paperclip icon when a receipt is attached), actions (view detail)
- Filters: status multi-select + date range + category select, URL-driven via validateSearch as in time entries
- Reuse existing expenses list API hook + `['expenses', ...]` query key
- Row click → existing ExpenseDetail surface
- km/mileage rows show distance in the description or a tooltip (don't add a column)
</action>

<read_first>
- `web/src/components/expenses/expenses-page.tsx` (placeholder location)
- `web/src/routes/_authenticated/expenses/index.tsx` (validateSearch pattern)
- `web/src/lib/api/expenses.ts` (list queryOptions + response shape incl. receipt + category fields)
- `web/src/components/expenses/expense-detail.tsx` (detail surface)
- Task 2's shared table component API (consistency)
</read_first>

<acceptance_criteria>
- List tab renders a populated table (no placeholder comment remains in source)
- Category + status + date filters work and persist in the URL
- Rows with receipts show the indicator; mileage rows surface distance
- Row click opens ExpenseDetail
- Expenses tests pass (existing + new)
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>

<action>
Extract the shared entries-table shell used by tasks 2–3 (build first if scheduling allows). Also enforce consistent table conventions.

- `web/src/components/shared/entries-table.tsx`: generic table component accepting column defs, rows, status-badge renderer, empty-state props; built on shadcn `Table` primitives
- Shared status-badge component mapping the six workflow states → label + variant (used by both lists)
- Shared date-range + status filter controls (URL-param agnostic — parent pages own the search-schema wiring)
- Placement follows ADR-FE-001 taxonomy: shared cross-domain components under `components/shared/` (create the dir; note the audit U7/U8 taxonomy debt is explicitly out of scope)
</action>

<read_first>
- ADR-FE-001 (project structure), ADR-FE-010 (shadcn primitives)
- `web/src/components/ui/table.tsx`, `badge.tsx`, `select.tsx`, `popover.tsx`, `calendar.tsx` (available primitives)
- The two page components (consumers' needs)
</read_first>

<acceptance_criteria>
- `components/shared/entries-table.tsx` exists and is imported by both list views (no duplicated table markup between them)
- Status badge is a single shared component used by both lists
- TypeScript strict: table is generic over row type; no `any`
- Both lists render identically-shaped markup (visual parity check in browser)
</acceptance_criteria>
</task>

<task id="5">
<type>execute</type>

<action>
Add route error boundaries (P0-4) per ADR-FE-014. Today a failed loader crashes to a blank screen.

- Create `web/src/components/layout/route-error.tsx`: full-width error panel showing the error message, a "Try again" button (re-runs the loader via TanStack Router's reset), and a "Go to Today/home" link
- Register it as `errorComponent` on the `_authenticated.tsx` layout route (covers all authenticated pages by default)
- Per-route overrides ONLY where recovery differs: login/register (auth layout) get a slimmer variant; everything else inherits the layout boundary
- Keep `pendingComponent` behavior unchanged (that's P1 skeletons, out of scope)
- Boundary must log to console in dev
</action>

<read_first>
- `web/src/routes/_authenticated.tsx` (layout route — current pendingComponent wiring)
- TanStack Router error-component API (version in `package.json` — check docs for `errorComponent` + `reset` semantics for this version)
- `web/src/routes/_auth/` (auth layout for the slim variant)
- ADR-FE-014 (error handling & boundaries)
</read_first>

<acceptance_criteria>
- `route-error.tsx` exists with retry + home-link affordances
- `_authenticated.tsx` declares `errorComponent`; source assertion passes
- Forced-error test: temporarily throw in a loader (or MSW 500 on a query) → error panel renders, "Try again" re-attempts, no blank screen — cover with a Playwright or Vitest test using MSW
- Auth pages get the slim variant
- All existing E2E specs still pass (no regression from layout change)
</acceptance_criteria>
</task>

<verification>
- `npm run build` + `npm run test` green (Vitest)
- Playwright suite green, including new customers-route and error-boundary coverage
- Manual smoke: `/customers` list loads; time-entries and expenses List tabs show real data with working filters; killing the API mid-load renders the error panel with working retry
</verification>

<must_haves>
- `/customers` is reachable and renders its list (P0-3 closed)
- Both List tabs render real, filterable data (P0-2 closed)
- A failed loader on any authenticated route shows a recoverable error panel, never a blank screen (P0-4 closed)
</must_haves>

<artifacts_produced>
- `web/src/routes/_authenticated/customers/index.tsx`
- `web/src/components/shared/entries-table.tsx` + shared status badge + filter controls
- `web/src/components/layout/route-error.tsx` (+ slim auth variant)
- Filled list tabs in `time-entries-page.tsx` and `expenses-page.tsx`
- `errorComponent` wiring in `_authenticated.tsx`
</artifacts_produced>
