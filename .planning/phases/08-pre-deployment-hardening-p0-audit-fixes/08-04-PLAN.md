---
plan: 08-04
title: Frontend E2E & Smoke Verification
wave: 2
depends_on: [08-02]
files_modified:
  - e2e/customers.spec.ts
  - e2e/time-entries.spec.ts
  - e2e/expenses.spec.ts
  - e2e/error-boundary.spec.ts
  - web/src/components/shared/entries-table.test.tsx
  - hourglass-vault/research/2026-07-28 — Pre-Deployment Audit — Hourglass v0.1.md
  - hourglass-vault/00-Index.md
autonomous: true
requirements: [P0-2, P0-3, P0-4]
---

# Plan 08-04 — Frontend E2E & Smoke Verification

Proves the Plan 02 fixes in the browser: real list views, the reachable customers page, and error boundaries that recover. Closes the phase by updating the audit's P0 table to "Fixed" — the phase definition of done.

<task id="1">
<type>execute</type>

<action>
List-view E2E for time entries and expenses (P0-2 verification).

- Seed a dataset spanning all six workflow states + multiple categories (reuse existing seed helpers from Phase 0/6 specs)
- time-entries.spec.ts: List tab shows seeded rows; status filter narrows; URL reflects filter; reload restores filter; row click opens detail
- expenses.spec.ts: List tab shows rows; category + status filters compose; receipt indicator renders on receipt-backed rows; mileage row surfaces distance
- Component test `entries-table.test.tsx`: shared table renders columns/rows/empty state; status badge maps all six states
</action>

<read_first>
- `e2e/time-entries.spec.ts`, `e2e/expenses.spec.ts` (existing specs + seed helpers)
- Plan 02 tasks 2–4 implementation (table + filters)
- Vitest + Testing Library conventions (ADR-FE-011)
</read_first>

<acceptance_criteria>
- New E2E cases pass: `npx playwright test time-entries expenses`
- `entries-table.test.tsx` passes under `npm run test`
- Filter-state round-trip through the URL is asserted in at least one spec per page
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>

<action>
Customers route E2E (P0-3 verification).

- customers.spec.ts: sidebar → Customers → `/customers` loads the seeded list; search narrows; click-through opens `/customers/$id` detail
- Internal customer renders its badge + locked fields (Phase 3 behavior preserved)
- Deep-link: fresh browser session directly to `/customers` renders (auth redirect → login → lands back on `/customers`)
</action>

<read_first>
- `e2e/customers.spec.ts` (existing spec — extend, don't rewrite)
- Plan 02 task 1 route wiring
</read_first>

<acceptance_criteria>
- All cases pass; no 404/blank renders in trace
- Deep-link redirect target verified in the spec
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>

<action>
Error-boundary E2E (P0-4 verification).

- error-boundary.spec.ts: intercept a list API with a 500 → error panel renders (message + Try again + home link)
- Remove the intercept, click Try again → data loads successfully
- Home link navigates to the landing route
- Auth-page slim variant: force a failure on login page loader → slim panel renders
</action>

<read_first>
- Plan 02 task 5 implementation (route-error component + wiring)
- Playwright route-interception patterns used in existing specs
</read_first>

<acceptance_criteria>
- Spec passes, including the recover-after-retry path (proves the router reset works)
- No blank screen at any point in the trace (assert document body non-empty before panel assertion)
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>

<action>
Close the audit loop (phase definition of done).

- Update `hourglass-vault/research/2026-07-28 — Pre-Deployment Audit — Hourglass v0.1.md` §6: confirm the P0 table reflects final state — P0-1/P0-6 already read Fixed (pre-audit, per the 2026-07-31 Corrections section); mark P0-2…P0-5 ✅ Fixed with one-line evidence links (component, test) once verified
- Update `hourglass-vault/00-Index.md`: current-state note that v0.1 P0 gate is closed; Phase 8 complete
- Keep the note's historical content intact — findings and the Corrections section stay as written; only per-row status annotations change
</action>

<read_first>
- The audit note §6 P0 table (exact rows to annotate)
- `hourglass-vault/00-Index.md` (status sections)
</read_first>

<acceptance_criteria>
- All six P0 rows reflect final state (P0-1/P0-6: Fixed pre-audit; P0-2…P0-5: Fixed this phase, with evidence)
- 00-Index reflects the closed gate
- Vault commits land on the vault's Git sync (vault is Git-synced per project convention)
</acceptance_criteria>
</task>

<verification>
- Full Playwright suite green (`npx playwright test`)
- `npm run test` + `go test ./...` green (phase-wide final gate)
- Audit P0 table fully annotated
</verification>

<must_haves>
- Browser-proven list views, customers route, and error recovery
- Phase 8 closes with the audit's P0 gate marked Fixed
</must_haves>

<artifacts_produced>
- Extended E2E specs (list views, customers, error boundary)
- `entries-table.test.tsx`
- Audit note + 00-Index status updates
</artifacts_produced>
