---
phase: 16-integrity-repair
plan: 01
type: execute
wave: 1
depends_on: [12-coverage-backend, 14-availability-backend]
files_modified: []
autonomous: false
requirements: []
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 8
  confidence: medium
must_haves:
  truths:
    - "Employee coverage read currently requires manager|finance role; Phase 16 adds an employee self-read path over their own allocations (no manager/finance scope grant)."
    - "Phase 12 multi-row allocation smoke + concurrent period-close smoke are unrun; they are executed and recorded, or explicitly waived with evidence (not silently dropped)."
    - "POST /expenses omits unit_id: postgres repo already inserts unit_id (expense_repository.go:197,204) but the handler/service Create path never maps it from the request."
    - "SetReceiptURL(ctx, expenseID, receiptURL) authorizes nothing; the actor + org must be checked before the URL write (expense.go:538)."
    - "Capacity unit/WG scope IDs (WR-05) are not org-guarded; org isolation must be added to the capacity read/write paths."
    - "Rate limiter raises a key's stored limit permanently to the highest tier seen in the window (ratelimit.go:85-86) and keys anonymous traffic on RemoteAddr IP (ratelimit.go:47-50), collapsing proxy-shared clients into one bucket."
  artifacts:
    - path: "internal/core/services/expense/expense.go"
      provides: "unit_id mapping in Create + actor/org auth in SetReceiptURL"
    - path: "internal/middleware/ratelimit.go"
      provides: "no permanent limit inflation + per-client anonymous keys"
    - path: "internal/adapters/secondary/postgres/coverage_repository.go"
      provides: "employee self-coverage read + org-isolated capacity scope"
  key_links:
    - from: "internal/core/services/expense/expense.go"
      to: "internal/adapters/secondary/postgres/expense_repository.go"
      via: "Create maps unit_id; SetReceiptURL guards by org"
      pattern: "repair"
    - from: "internal/middleware/ratelimit.go"
      to: "internal/middleware/ratelimit_test.go"
      via: "regression tests for the two limiter defects"
      pattern: "repair"
---

<objective>
Repair-only close of v0.2. Land the Phase 12 leftovers (employee own-coverage read, two unrun smokes), the selected Known Bugs (expense `unit_id`, receipt auth, WR-05 capacity org-isolation, two rate-limiter defects). No UI. No Availability Frontend. No chips/fields/Today composition. No sketch. Do not reuse the deleted "Availability Frontend" name or its requirements.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 16: Integrity Repair, lines 284-310)
@internal/core/services/coverage/coverage.go
@internal/adapters/secondary/postgres/coverage_repository.go
@internal/core/services/expense/expense.go
@internal/adapters/primary/http/expense.go
@internal/adapters/secondary/postgres/expense_repository.go
@internal/middleware/ratelimit.go
@internal/middleware/ratelimit_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Employee own-coverage read</name>
  <read_first>
    - internal/core/services/coverage/coverage.go — current manager|finance-only read path
    - internal/adapters/secondary/postgres/coverage_repository.go — add a self-scoped query
  </read_first>
  <action>
    Add a read path that lets an employee read coverage allocations on their own entries (current reads are manager|finance only). Scope strictly to user_id = actor and org_id = membership; no broadening of manager/finance views. Add a backend test asserting an employee sees only their own allocations and is denied cross-user reads.
  </action>
  <verify>
    <automated>go test ./internal/core/services/coverage/... ./internal/adapters/secondary/postgres/... -run Coverage</automated>
  </verify>
  <acceptance_criteria>
    - An employee can read coverage allocations on their own entries
    - Manager|finance read behavior is unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase 12 multi-row allocation smoke</name>
  <read_first>
    - internal/core/services/coverage/coverage.go — allocation ledger
    - .planning/phases/12-coverage-backend-the-allocation-loop/ (leftover note)
  </read_first>
  <action>
    Run the multi-row allocation smoke (allocate across several rows in one period, assert ledger totals and to-cover queue consistency). Record pass/fail + evidence in the plan SUMMARY. If the environment cannot run it, waive with explicit evidence (not silently skipped).
  </action>
  <verify>
    <automated>go test ./internal/... -run MultiRowAllocation</automated>
  </verify>
  <acceptance_criteria>
    - Smoke run is recorded as pass, or explicitly waived with evidence
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase 12 concurrent period-close smoke</name>
  <read_first>
    - internal/core/services/coverage/coverage.go — period-close mechanics
  </read_first>
  <action>
    Run the concurrent period-close smoke (two closes racing on the same period; assert exactly-once finalization / no double-snapshot). Record pass/fail + evidence. Waive with evidence only if not runnable.
  </action>
  <verify>
    <automated>go test ./internal/... -run ConcurrentPeriodClose</automated>
  </verify>
  <acceptance_criteria>
    - Smoke run is recorded as pass, or explicitly waived with evidence
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: POST /expenses sets unit_id</name>
  <read_first>
    - internal/adapters/primary/http/expense.go — Create handler
    - internal/core/services/expense/expense.go — Create service
    - internal/adapters/secondary/postgres/expense_repository.go:197,204 — repo already inserts unit_id
  </read_first>
  <action>
    Map unit_id from the expense create request through the handler and service into the repo (repo column + INSERT already exist). Add/adjust a test asserting the persisted expense carries the supplied unit_id.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/... -run Expense</automated>
  </verify>
  <acceptance_criteria>
    - POST /expenses persists the requested unit_id (previously dropped)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Receipt upload authorizes actor/org</name>
  <read_first>
    - internal/adapters/primary/http/expense.go:538 — SetReceiptURL call
    - internal/core/services/expense/expense.go — SetReceiptURL signature (expenseID, receiptURL only)
  </read_first>
  <action>
    Authorize the actor + org before writing the receipt URL: load the expense, assert expense.org_id == actor membership org and the actor owns or is authorized for the expense. Reject otherwise with 403. Add a test covering unauthorized actor/org.
  </action>
  <verify>
    <automated>go test ./internal/core/services/expense/... ./internal/adapters/primary/http/... -run Receipt</automated>
  </verify>
  <acceptance_criteria>
    - SetReceiptURL refuses writes when actor/org is not authorized (previously accepted any expense ID + URL)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Capacity unit/WG scope org-isolation (WR-05)</name>
  <read_first>
    - internal/core/services/availability/... or capacity paths — unit/WG scope IDs
    - .planning/ROADMAP.md:220 — WR-05 open warning
  </read_first>
  <action>
    Add org isolation to capacity unit/WG scope reads/writes so a scope ID cannot be resolved across orgs. Add a test asserting cross-org scope access is denied.
  </action>
  <verify>
    <automated>go test ./internal/... -run CapacityScope</automated>
  </verify>
  <acceptance_criteria>
    - Capacity unit/WG scope IDs are organization-isolated (WR-05 closed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Rate limiter — no permanent limit inflation</name>
  <read_first>
    - internal/middleware/ratelimit.go:85-86 — `if limit > info.limit { info.limit = limit }`
    - internal/middleware/ratelimit_test.go
  </read_first>
  <action>
    Remove the permanent stored-limit inflation: a key's limit must reflect the tier of the current request, not the highest tier ever seen in the window. Add a regression test: an anonymous key that briefly presents an auth tier must not keep the higher limit after reverting to anonymous.
  </action>
  <verify>
    <automated>go test ./internal/middleware/... -run RateLimit</automated>
  </verify>
  <acceptance_criteria>
    - A key's stored limit is not permanently raised to the highest tier seen in the window
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Rate limiter — distinct anonymous buckets behind proxy</name>
  <read_first>
    - internal/middleware/ratelimit.go:47-50 — getClientKey keys on RemoteAddr IP
    - internal/middleware/ratelimit_test.go
  </read_first>
  <action>
    Stop collapsing all anonymous proxy traffic into one bucket. Key anonymous clients on a proxy-aware identifier (e.g., X-Forwarded-For first hop with fallback to RemoteAddr), keeping per-client buckets. Add a regression test with a shared RemoteAddr + distinct XFF headers.
  </action>
  <verify>
    <automated>go test ./internal/middleware/... -run RateLimit</automated>
  </verify>
  <acceptance_criteria>
    - Anonymous traffic behind a proxy is not collapsed into one shared bucket
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Per-task `go test` paths above (backend only)
- Full suite green: `make test` (go test ./...)
- No UI changes; no sketch; no new endpoints beyond the repaired paths
</verification>

<success_criteria>
- All 8 ROADMAP Phase 16 success criteria (lines 291-298) are TRUE
- WR-05 closed; two rate-limiter defects fixed with regression tests
- No presentation/sketch/surface work introduced
</success_criteria>

<output>
Create `.planning/phases/16-integrity-repair/16-01-SUMMARY.md` when done
</output>
