---
phase: 17-design-language-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/LANGUAGE.md
autonomous: true
requirements:
  - DL-01
estimate:
  tokens: 22000
  raw_tokens: 11000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A design-language contract document exists at docs/design/LANGUAGE.md and is positioned as the source of truth for all later presentation work (DL-01, SC1).
    - The contract covers type, color, density, motion, and status vocabulary — not only the Phase 15 token dump (SC2).
    - Phase 15 frozen components are listed as inputs with gaps called out rather than silently reused (SC3).
    - No UI implementation, no sketches, and no route work were performed (SC4).
  artifacts:
    - docs/design/LANGUAGE.md exists with the exact section order from D-17-20.
    - web/src/index.css and web/components.json are cited by repo-root path only, never duplicated as oklch/hex/px tables.
  key_links:
    - LANGUAGE.md wins on type/color/density/motion/status vocabulary authority over later docs (D-17-09).
    - LANGUAGE.md points at the live value stores web/src/index.css and web/components.json by repo-root citation (D-17-18, D-17-19).
---

<objective>
Author the design-language contract at `docs/design/LANGUAGE.md` — the durable source of truth for type, color, density, motion, and status vocabulary across all later presentation work (DL-01). Per D-17-01 this is architected product language under `docs/design/`, not a Phase 15 restoration. Per D-17-46 the plan MUST NOT modify `web/src/index.css`, `web/components.json`, any `web/src` component, `15-UI-SPEC.md`, or create `docs/design/workflows/`. Per D-17-47/D-17-48 no implementation, sketches, or route work occurs; chrome/workflow/composition belong to Phases 18/19/20.

Purpose: establish the authoritative vocabulary so later contracts (CHROME, workflows, COMPOSITION) and later GSD UI-SPEC files add only surface/layout/copy/composition and MUST NOT override LANGUAGE.md (D-17-09, D-17-10).
Output: docs/design/LANGUAGE.md (the single new file for this plan).
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-design-language-contract/17-CONTEXT.md
@web/src/index.css
@web/components.json
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/design/LANGUAGE.md full contract</name>
  <files>docs/design/LANGUAGE.md</files>
  <read_first>
    - docs/design/LANGUAGE.md
    - .planning/phases/17-design-language-contract/17-CONTEXT.md
    - web/src/index.css
    - web/components.json
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md
  </read_first>
  <action>
    Create `docs/design/LANGUAGE.md` from scratch following the exact section order from D-17-20: Title → Purpose → changelog → foundations → overlay → do/don't → pointers → not-in-this-file → 15-UI-SPEC note → Gaps.

    One dual-audience file, no agent-only section (D-17-02). Use RFC 2119 selectively: `MUST` / `MUST NOT` for hard conformance, `SHOULD` for defaults, rationale in prose (D-17-06). State in Purpose that roles and MUST/SHOULD rules live in this file while concrete values live in CSS; CSS wins on values, LANGUAGE.md wins on meaning/usage; do not invent CSS tokens and do not duplicate oklch/hex tables (D-17-04). State the authority stack: LANGUAGE.md wins on type/color/density/motion/status vocabulary; later docs (CHROME, workflows, COMPOSITION) and later GSD UI-SPEC files may only add surface/layout/copy/composition and MUST NOT override it (D-17-09); only later GSD docs/planning phases may amend LANGUAGE.md, implementation/job-cluster work MUST NOT (D-17-10). One short line: amend in place with the compact changelog, no version fork (D-17-05).

    changelog (D-17-21): compact dated entries keyed by GSD phase, latest first, no semver, no Keep-a-Changelog headings. First entry exactly: `2026-08-26 · Phase 17 · Added foundations`.

    foundations — five vocabularies, each as English role names + MUST/SHOULD + a CSS pointer by repo-root citation only (never a wrapped table of values) (D-17-18):
    - Type (D-17-25..D-17-28): English roles only, never name Inter/Martian Mono/Geist Mono as the language. Roles: `display` (live `--display-family`/`--display-weight`/`.font-display`), `text` (live `--text-family`/`--text-weight`/`.font-text`), and `ui` (the default Inter / `html { font-sans }` face). `display` and `text` point at web/src/index.css; `ui` is named as meaning (default sans/UI face) and recorded as a Gap below (D-17-27) — do NOT pretend a `--ui` token exists. Do not dump Phase 15's four-size/two-weight table (D-17-28).
    - Color (D-17-29..D-17-32): interaction/chrome roles only, never status. One line per product-wide shadcn role (English name + job), naming `*-foreground` only where the pair exists, pointing at web/src/index.css, never naming `--base-*`/`--primary-*` ramps, `sidebar-*`, or `chart-*`. Inventory exactly: `background` (page/canvas), `foreground` (default text), `card`/`card-foreground`, `popover`/`popover-foreground`, `primary`/`primary-foreground`, `secondary`/`secondary-foreground`, `muted`/`muted-foreground`, `accent`/`accent-foreground` (interaction/navigation, never status), `destructive` (destructive action, never status; no `destructive-foreground` pair in live CSS, do not invent one), `border`, `input`, `ring`. State `accent` is interaction/navigation never status and `destructive` is an action role not domain `danger`; MUST NOT map `danger` to `destructive` (D-17-32).
    - Density (D-17-33..D-17-35): 4px spacing rhythm, compact default; CSS owns values; do not copy the Phase 15 xs..3xl pixel table; record the density Gap below. `--radius` is a value, not a density role — do not catalog it (D-17-35).
    - Motion (D-17-36..D-17-39): English roles `duration` (motion length) and `easing` (motion curve); MUST NOT catalog animations/transitions/component motion recipes. Live web/src/index.css has no duration/easing tokens — record the two motion Gaps below. MUST include the rule that UI MUST honor `prefers-reduced-motion` (D-17-39) — that is a rule, not a token, and not a Gap line; do not invent a reduced-motion CSS identifier.
    - Status vocabulary (D-17-40..D-17-44): roles `neutral`, `info`, `success`, `warning`, `danger` as semantic meaning separate from interaction/chrome tokens. MUST NOT use interaction tokens (`primary`, `accent`, `destructive`, `muted`) as status and MUST NOT map `danger` to `destructive`. All five are Gaps because live web/src/index.css has no status tokens (record below). Do not copy the Phase 15 domain-status → role mapping table (D-17-43). Note web/src/components/shared/status-badge.tsx still uses raw Tailwind palettes with `dark:` variants as known historical debt; this plan MUST NOT modify it (D-17-44).

    overlay (D-17-19): one short rule — Hourglass overlays shadcn style `base-mira` / baseColor `olive` (web/components.json) and MUST use those primitives; MUST NOT invent a parallel visual system or restyle the kit in this file; cite web/components.json + web/src/index.css; no component APIs.

    Light/dark (D-17-23): one short MUST — semantic roles are theme-invariant; UI MUST NOT branch on light/dark except for the theme toggle; per-theme values stay in web/src/index.css; no duplicate per-mode role lists.

    do/don't (D-17-22): a Don't-only digest of the foundation `MUST NOT`s already stated above. No Do column, no examples. Every Don't MUST already appear as a foundation `MUST NOT`.

    pointers (D-17-18): repo-root citations only — `web/src/index.css` and `web/components.json`. No markdown links to CSS/config.

    not-in-this-file (D-17-16): a short fenced note listing what is explicitly out of scope here: no component APIs, no chrome/layout, no workflow/copy, no token tables, no screenshots.

    15-UI-SPEC note (D-17-11): state that `.planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md` is untouched historical input and not authority; if it conflicts with LANGUAGE.md on type/color/density/motion/status vocabulary, LANGUAGE.md wins. Within this note also list the Phase 15 frozen components as inputs per D-17-45: StatusBadge present at web/src/components/shared/status-badge.tsx (raw Tailwind, not role tokens), while PageHeader, FilterBar, DataTable, EmptyState, ConfirmDialog are absent from the live tree — list them with these live gaps rather than silently reusing, and do not restore or rewrite them in Phase 17. Note also that entries-table.tsx and entries-filters.tsx are present but are NOT the frozen set (D-17-45).

    Gaps (D-17-07, D-17-08, D-17-27, D-17-34, D-17-38, D-17-42): one line per role/rule, form `name — no <kind> token in index.css.`, no owner phase, no proposed token name, no speculative `--status-*`/`--duration-*` identifiers. Exact lines, each on its own line:
    - `ui — no type token in index.css.`
    - `density — no density token in index.css.`
    - `duration — no motion token in index.css.`
    - `easing — no motion token in index.css.`
    - `neutral — no status token in index.css.`
    - `info — no status token in index.css.`
    - `success — no status token in index.css.`
    - `warning — no status token in index.css.`
    - `danger — no status token in index.css.`
  </action>
  <verify>
    <automated>test -f docs/design/LANGUAGE.md && grep -q "2026-08-26 · Phase 17 · Added foundations" docs/design/LANGUAGE.md && echo OK</automated>
  </verify>
  <done>LANGUAGE.md exists with all five foundations, overlay, light/dark MUST, do/don't digest, pointers, not-in-this-file fence, 15-UI-SPEC note with frozen-component input list, and the nine exact Gap lines, in the D-17-20 section order.</done>
  <acceptance_criteria>
    - File docs/design/LANGUAGE.md exists.
    - Section order matches D-17-20 (Title, Purpose, changelog, foundations, overlay, do/don't, pointers, not-in-this-file, 15-UI-SPEC note, Gaps) — verified by grep of each `## ` heading in order.
    - changelog contains exactly `2026-08-26 · Phase 17 · Added foundations` (D-17-21).
    - Color inventory lists all roles from D-17-31 including `destructive` with no invented `destructive-foreground` (D-17-32).
    - Status roles list exactly `neutral`, `info`, `success`, `warning`, `danger` (D-17-40).
    - The nine Gap lines appear verbatim, one per line (D-17-08/27/34/38/42).
    - 15-UI-SPEC note lists StatusBadge present + PageHeader/FilterBar/DataTable/EmptyState/ConfirmDialog absent (D-17-45).
    - No raw `oklch(` or `hex` color table and no px spacing table appears in the file (D-17-04/03) — verified by grep returning zero matches for `oklch(` and for a hex `#` token table.
    - Light/dark MUST present and states semantic roles theme-invariant, UI MUST NOT branch except theme toggle (D-17-23).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Conformance audit of LANGUAGE.md against locked decisions</name>
  <files>docs/design/LANGUAGE.md</files>
  <read_first>
    - docs/design/LANGUAGE.md
    - .planning/phases/17-design-language-contract/17-CONTEXT.md
  </read_first>
  <action>
    Audit the just-written `docs/design/LANGUAGE.md` against the locked decisions and correct any deviation; this plan edits only LANGUAGE.md. Checks (do not modify web/src or any other file per D-17-46):
    - Foundations-only scope (D-17-03): no component API catalog, no workflow/copy, no page layouts, no screenshots, no worked examples, no token tables, no oklch/hex, no component anatomy. Remove any such content.
    - No duplicated value tables (D-17-04): grep for `oklch(` and for hex `#` tokens; if any value table slipped in, replace with a repo-root CSS pointer.
    - do/don't is Don't-only (D-17-22): every line under do/don't MUST already appear as a foundation `MUST NOT`; remove any Do line, example, or Don't that was not already a foundation `MUST NOT`.
    - Authority (D-17-09): the file states LANGUAGE.md wins on the five vocabularies and later docs may only add surface/layout/copy/composition.
    - Each foundation names its meaning in English and points at web/src/index.css / web/components.json by repo-root citation (D-17-18); no markdown link to CSS/config.
    - All 9 Gap lines present verbatim and only in the Gaps section; no proposed token names (D-17-08).
    - Overlay cites base-mira/olive and says MUST use those primitives, MUST NOT invent a parallel system (D-17-19).
    If a check fails, edit LANGUAGE.md to satisfy it and re-run the verification grep.
  </action>
  <verify>
    <automated>grep -c "oklch(" docs/design/LANGUAGE.md | grep -q "^0$" && grep -q "LANGUAGE.md wins" docs/design/LANGUAGE.md && grep -c "no .* token in index.css." docs/design/LANGUAGE.md | grep -q "9" && echo OK</automated>
  </verify>
  <done>LANGUAGE.md passes the conformance audit: no value tables, do/don't is Don't-only and fully mirrored from foundation MUST NOTs, authority and overlay statements present, all 9 Gap lines present, only LANGUAGE.md was edited.</done>
  <acceptance_criteria>
    - `grep -c "oklch(" docs/design/LANGUAGE.md` returns 0 (D-17-04).
    - A statement that LANGUAGE.md wins on the five vocabularies is present (D-17-09).
    - Exactly 9 Gap lines (count of `no .* token in index.css.`) are present (D-17-08/42).
    - No file other than docs/design/LANGUAGE.md was modified (D-17-46).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc author → repo | Advisory markdown authored into docs/design; no untrusted input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | docs/design/LANGUAGE.md authority drift | low | accept | Advisory documentation only; no runtime consumer. If a later phase contradicts it, the authority stack (D-17-09) and amend rule (D-17-10) resolve drift through a GSD docs/planning amendment, not code. |
| T-17-02 | Information Disclosure | AGENTS.md / docs pointers | low | accept | No secrets, credentials, or untrusted data referenced; pointers are repo-root paths to CSS/config only. |
</threat_model>

<verification>
- `test -f docs/design/LANGUAGE.md` is true.
- Section-order grep confirms D-17-20 order.
- `grep -c "oklch("` returns 0.
- 9 Gap lines present verbatim.
- No web/src or components.json modification occurred (D-17-46).
</verification>

<success_criteria>
- docs/design/LANGUAGE.md exists as the source-of-truth design-language contract covering type, color, density, motion, status (DL-01, SC1/SC2).
- Phase 15 frozen components are listed as inputs with gaps, not silently reused (SC3).
- No UI implementation, sketches, or route work were produced (SC4).
</success_criteria>

<output>
Create `.planning/phases/17-design-language-contract/17-01-SUMMARY.md` when done
</output>
