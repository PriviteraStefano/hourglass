---
phase: 17-design-language-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - docs/design/INDEX.md
  - AGENTS.md
autonomous: true
requirements:
  - DL-01
estimate:
  tokens: 12000
  raw_tokens: 6000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - docs/design/INDEX.md exists as a simple design-documentation map (D-17-13).
    - AGENTS.md contains the one-sentence design gate immediately after the title, and no other design text was added to AGENTS.md (D-17-17).
    - INDEX.md references LANGUAGE.md (the Phase 17 contract) and reserves CHROME/workflows/COMPOSITION for Phases 18/19/20 (D-17-15, D-17-48).
  artifacts:
    - docs/design/INDEX.md exists.
    - AGENTS.md edited with exactly one inserted sentence.
  key_links:
    - INDEX.md links the live LANGUAGE.md; reserved paths are backtick-quoted with `(reserved — Phase N)` (D-17-14).
    - The AGENTS.md gate points agents at INDEX.md before any web/src UI/token/component/copy/layout change (D-17-17).
---

<objective>
Write `docs/design/INDEX.md` (the design documentation map) and insert the single fixed design gate into `AGENTS.md` (D-17-17). These two files close the DL-01 loop: the contract (LANGUAGE.md, Plan 17-01) is now discoverable through INDEX.md and enforced through the AGENTS.md gate. Per D-17-01 the design docs live under `docs/design/`; per D-17-12 this plan MUST NOT create `docs/design/workflows/`; per D-17-46 this plan MUST NOT modify `web/src/index.css`, `web/components.json`, any `web/src` component, or `15-UI-SPEC.md`; per D-17-47/D-17-48 no implementation/sketches/route work occurs — chrome is Phase 18, workflows Phase 19, composition Phase 20.

Purpose: make the design-language contract the referenced entry point for all later presentation work.
Output: docs/design/INDEX.md (new) and the AGENTS.md gate edit.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-design-language-contract/17-CONTEXT.md
@.planning/phases/17-design-language-contract/17-01-PLAN.md
@docs/design/LANGUAGE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/design/INDEX.md path map</name>
  <files>docs/design/INDEX.md</files>
  <read_first>
    - docs/design/INDEX.md
    - docs/design/LANGUAGE.md
    - .planning/phases/17-design-language-contract/17-CONTEXT.md
  </read_first>
  <action>
    Create `docs/design/INDEX.md` as a simple path map (D-17-13): a Title, one sentence stating this is the design documentation map, then the path list with one-line purposes. No stack, no MUST NOTs, no shadcn, no Gaps, no How-to-use, no AGENTS/CSS/`15-UI-SPEC` pointers (D-17-13).

    List ONLY these four entries (D-17-15):
    - `LANGUAGE.md` as a markdown link — one-line purpose: the design-language contract (type, color, density, motion, status vocabulary); this is the source of truth.
    - `docs/design/CHROME.md` reserved for Phase 18 — write as a backtick path followed by ` (reserved — Phase 18)`; one-line purpose: app-shell / chrome contract (frame, navigation, role-scoped chrome, page anatomy).
    - `docs/design/workflows/` reserved for Phase 19 — backtick path followed by ` (reserved — Phase 19)`; one-line purpose: role/workflow contracts (employee, manager, finance, HR, customer).
    - `docs/design/COMPOSITION.md` reserved for Phase 20 — backtick path followed by ` (reserved — Phase 20)`; one-line purpose: cross-role composition map.

    Existing files are markdown links; reserved paths are backtick paths plus `(reserved — Phase N)`, never fake links (D-17-14). Do not add extra prose beyond the one-sentence intro and the four one-line purposes (D-17-48 discretion note).
  </action>
  <verify>
    <automated>test -f docs/design/INDEX.md && grep -q "design documentation map" docs/design/INDEX.md && echo OK</automated>
  </verify>
  <done>INDEX.md exists listing exactly LANGUAGE.md (link) plus the three reserved paths (CHROME Phase 18, workflows Phase 19, COMPOSITION Phase 20), each with a one-line purpose, no extra prose.</done>
  <acceptance_criteria>
    - File docs/design/INDEX.md exists.
    - Intro sentence contains "design documentation map" (D-17-13).
    - Exactly four path entries present: `LANGUAGE.md` (markdown link), and `docs/design/CHROME.md`, `docs/design/workflows/`, `docs/design/COMPOSITION.md` as backtick paths each followed by `(reserved — Phase 18|19|20)` (D-17-14/15).
    - No `docs/design/workflows/` directory or file was created in this plan (D-17-12).
    - No MUST NOTs, shadcn notes, Gaps, or AGENTS/CSS pointers appear in INDEX.md (D-17-13).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Insert the AGENTS.md design gate after the title</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/17-design-language-contract/17-CONTEXT.md
  </read_first>
  <action>
    Edit `AGENTS.md` by inserting exactly one sentence immediately after the title line `# Hourglass Codebase Guide for AI Agents` (the first line) and immediately before the existing `## OpenWiki` section, with a single blank line separating it from the title above and the `## OpenWiki` heading below. Do not modify any other line. The inserted sentence, verbatim (D-17-17):

    Before any `web/src` change to UI, tokens, components, copy, or layout, open `docs/design/INDEX.md` first. Backend-only work skips this. No other design text belongs in `AGENTS.md`.

    This is the ONLY design text added to AGENTS.md; the existing OpenWiki content and everything after it stays unchanged (D-17-17). Do not add any other design guidance, links, or prose to AGENTS.md. Per D-17-46 this plan does not touch web/src/index.css, web/components.json, any web/src component, or 15-UI-SPEC.md.
  </action>
  <verify>
    <automated>sed -n '1,3p' AGENTS.md | grep -q "open \`docs/design/INDEX.md\` first" && echo OK</automated>
  </verify>
  <done>AGENTS.md line 2 is exactly the design gate sentence, the title (line 1) and `## OpenWiki` (line 3) are unchanged, and no other AGENTS.md line was modified.</done>
  <acceptance_criteria>
    - Line 2 of AGENTS.md contains `open \`docs/design/INDEX.md\` first` (D-17-17).
    - Line 1 is still `# Hourglass Codebase Guide for AI Agents` and line 3 is still `## OpenWiki` (unchanged surrounding content).
    - The gate sentence is present exactly once (no duplicate), and no other design text was added anywhere in AGENTS.md (D-17-17).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc author → repo | Advisory markdown + one-line AGENTS.md gate; no untrusted input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Tampering | AGENTS.md gate wording drift | low | accept | Advisory gate only; exact copy is pinned by D-17-17. If edited later, it must remain the single sentence; the freeze rule prevents extra design text. |
| T-17-04 | Spoofing | INDEX.md link target | low | accept | INDEX.md links only to the in-repo LANGUAGE.md and reserves future paths; no external/untrusted URLs. |
</threat_model>

<verification>
- `test -f docs/design/INDEX.md` is true and lists the four entries.
- `sed -n '2p' AGENTS.md` equals the design gate sentence.
- No web/src, components.json, or 15-UI-SPEC.md was modified (D-17-46).
</verification>

<success_criteria>
- docs/design/INDEX.md exists as the design-documentation map (DL-01 discoverability).
- AGENTS.md carries the single design gate immediately after the title; no other design text added (D-17-17).
- The DL-01 contract is now referenced as source of truth for subsequent presentation work (SC1).
</success_criteria>

<output>
Create `.planning/phases/17-design-language-contract/17-02-SUMMARY.md` when done
</output>
