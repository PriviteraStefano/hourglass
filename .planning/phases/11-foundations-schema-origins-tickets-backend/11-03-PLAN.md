---
phase: 11-foundations-schema-origins-tickets-backend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/services/routing/routing.go
  - internal/core/services/routing/routing_test.go
  - internal/core/services/time_entry/time_entry.go
  - internal/core/services/time_entry/time_entry_test.go
  - cmd/server/main.go
  - cmd/server/main_test.go
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "Manager-stage routing lives in a shared package both entry approval and proposal approval consume (D-G parity)"
    - "The time_entry service compiles and passes its full suite with the extracted code"
    - "BE-014 semantics are preserved verbatim: anchored-WG approver set, ErrActivityNotLoggable for commercial-without-WG, unit-manager upward walk, roleGated terminal case, D-11 skip"
  artifacts:
    - path: "internal/core/services/routing/routing.go"
      provides: "managerResolution + ResolveManagerStage + ResolveUnitManager"
      exports: ["NewService", "ResolveManagerStage", "ResolveUnitManager"]
    - path: "internal/core/services/time_entry/time_entry.go"
      provides: "delegation to routing.Service at Submit/Approve call sites"
      contains: "routing."
  key_links:
    - from: "services/time_entry/time_entry.go"
      to: "services/routing/routing.go"
      via: "ResolveManagerStage call replacing private resolveManagerStage"
      pattern: "routing\\.ResolveManagerStage"
---

<objective>
Extract the BE-014 manager-stage resolution (`managerResolution`, `resolveManagerStage`, `resolveUnitManager`) from the time_entry service into a new shared package `internal/core/services/routing`, verbatim (Pattern 5).

Purpose: FND-02 (proposal approval) must route through the activity's approval routing exactly like entry approval (D-G). A duplicate implementation would let the routing rules drift. The extraction is pure refactor — the time_entry service keeps identical behavior, its unit tests stay green, and the new package's unit tests pin the semantics.
Output: routing package (service + tests), time_entry service refactored to delegate.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-CONTEXT.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-RESEARCH.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract routing to a shared package</name>
  <files>internal/core/services/routing/routing.go (NEW), internal/core/services/time_entry/time_entry.go</files>
  <read_first>
    - internal/core/services/time_entry/time_entry.go (the ENTIRE file — L127-226 is the extraction region; L243 + L282 are the call sites; L355-393 IsWGManager mirrors the wgRepo path and stays)
    - internal/core/ports/working_group_repository.go (ListByOrg signature with activityID scoping)
    - internal/core/ports/activity_repository.go (ResolveCommercialContext)
    - internal/core/ports/unit_repository.go (ListMembers, GetByID)
    - internal/core/domain/activity/activity.go (ErrActivityNotLoggable sentinel — must keep its identity/import path)
    - internal/core/domain/unit/unit.go (ErrUnitNotFound sentinel used by resolveUnitManager)
    - internal/core/domain/time_entry/time_entry.go (types referenced by the moved code, if any)
  </read_first>
  <action>
    Create package `internal/core/services/routing` (no subdirectory collision; the package name is `routing`). Move VERBATIM from time_entry.go: the `managerResolution` struct (L127-137, fields approverIDs []uuid.UUID, roleGated bool, skipToFinance bool with the D-11 comment), `resolveManagerStage` (L139-196), and `resolveUnitManager` (L198-226). Do NOT re-implement, reorder, or rename logic — copy the code as-is, changing only: receiver from `(s *Service)` to `(s *Service)`, constructor `NewService(wgRepo ports.WorkingGroupRepository, activityRepo ports.ActivityRepository, unitRepo ports.UnitRepository) *Service` with a Service struct holding those three repos. Export as methods `ResolveManagerStage(ctx, orgID, activityID, unitID, ownerID uuid.UUID) (*managerResolution, error)` — the struct stays unexported but must be usable by callers through the returned pointer (same as today). Preserve: the anchored-WG path (ListByOrg with &activityID → WG manager + delegates approver set, ownerIsApprover → skipToFinance), the R-2 enforcement returning `activity.ErrActivityNotLoggable` for commercial-without-WG, the unit-manager upward walk (ListMembers role == "manager", GetByID parent walk with ErrUnitNotFound → not found), and the terminal roleGated case. Keep the ADR-BE-014 R-1/R-2 doc comments.

    In time_entry.go: delete the extracted region (managerResolution struct + both private methods); add a `routing *routing.Service` field to the Service struct and a `routingSvc *routing.Service` parameter to NewService (append AFTER the existing params to minimize churn); replace the two call sites (L243 in Submit, L282 in Approve) `s.resolveManagerStage(ctx, e.OrgID, e.ActivityID, e.UnitID, e.UserID)` with `s.routing.ResolveManagerStage(ctx, e.OrgID, e.ActivityID, e.UnitID, e.UserID)`; the skipToFinance/roleGated/approverIDs consumption at the call sites stays identical. Leave `IsWGManager`, `contains`, `strPtr`, `timePtr` untouched. Import `github.com/stefanoprivitera/hourglass/internal/core/services/routing` in time_entry.go. `errors` import may become unused if resolveUnitManager was its only user — check and prune.

    Update `cmd/server/main.go` (and `cmd/server/main_test.go`): construct the routing service once — `routingSvc := routing.NewService(wgRepo, activityRepo, unitRepo)` next to the other service constructions (main.go ~L120-130) — and pass it to the time_entry service constructor (its signature gained the param). The routing service instance is REUSED later by the activity service (plan 05) — build it now, wire only time_entry.
  </action>
  <acceptance_criteria>
    - `internal/core/services/routing/routing.go` exists with exported `NewService`, `ResolveManagerStage`, `ResolveUnitManager` (ResolveUnitManager must be exported for reuse/tests — it is a public primitive of the package)
    - The file contains `activity.ErrActivityNotLoggable` (import preserved from the domain package, not redefined)
    - The file contains the skipToFinance comment mentioning D-11
    - `internal/core/services/time_entry/time_entry.go` no longer defines `resolveManagerStage`/`resolveUnitManager`/`managerResolution` (grep returns zero for `func (s \*Service) resolveManagerStage`)
    - time_entry.go contains `routing.ResolveManagerStage` at both former call sites
    - `cmd/server/main.go` + `cmd/server/main_test.go` construct routing.NewService and pass it to the time_entry service
    - `go build ./...` succeeds
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./internal/core/services/routing/ ./internal/core/services/time_entry/</automated>
  </verify>
  <done>Routing extracted verbatim; time_entry compiles and delegates; no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Routing unit tests + time_entry suite green</name>
  <files>internal/core/services/routing/routing_test.go (NEW), internal/core/services/time_entry/time_entry_test.go</files>
  <read_first>
    - internal/core/services/time_entry/time_entry_test.go (full file — the fixture pattern at L19-46, mock repos from internal/core/services/testdata/, existing routing coverage that must keep passing)
    - internal/core/services/testdata/ (MockWorkingGroupRepo, MockActivityRepo, MockUnitRepo shapes — what the routing test can reuse)
    - internal/core/services/routing/routing.go (the extracted code under test)
  </read_first>
  <action>
    Create `routing_test.go` in the new package using the serviceFixture pattern (testdata mocks, testify): 
    - Test 1 R-1 anchored WG: mock wgRepo.ListByOrg to return one WG with ManagerID + DelegateIDs; assert ResolveManagerStage returns approverIDs = {manager, delegates} and skipToFinance=false when owner is not in the set; assert skipToFinance=true when owner == WG manager.
    - Test 2 R-2 commercial-without-WG: wgRepo returns empty; activityRepo.ResolveCommercialContext returns a contract; assert error is activity.ErrActivityNotLoggable.
    - Test 3 R-2 unit fallback: no WG, no contract; unitRepo.ListMembers returns a membership with Role "manager" for the unit; assert approverIDs = [managerID], skipToFinance=false; assert skipToFinance=true when managerID == ownerID.
    - Test 4 upward walk: unit has no manager, GetByID parent chain leads to a unit with manager; assert resolution walks up.
    - Test 5 terminal: walk exhausts to org root with no manager anywhere; assert roleGated=true.
    - Test 6 ResolveUnitManager not-found: unitRepo.GetByID returns unit.ErrUnitNotFound; assert (uuid.Nil, false, nil).
    Then update time_entry_test.go: its setupService fixture must construct NewService with the new routing param (pass a routing.NewService with the same mock repos the fixture already holds). If the existing time_entry tests exercise routing through Submit/Approve, they must stay green unchanged — the mocks drive identical paths. Run the time_entry suite.
  </action>
  <acceptance_criteria>
    - routing_test.go contains at least the six scenarios above (grep for ErrActivityNotLoggable, roleGated, skipToFinance assertions)
    - time_entry_test.go compiles with the new NewService signature (routing param appended)
    - `go test ./internal/core/services/routing/ ./internal/core/services/time_entry/ -count=1` exits 0
    - Full-suite `go test ./... -count=1` runs at the WAVE GATE (see <verification>), NOT at this task boundary: the postgres package's pre-existing red TestMigration011/012 cycle tests only turn green once 11-01 Task 1 lands in the same wave — a full-suite run mid-wave would false-negative. The two-package command above is the task-level gate.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/core/services/routing/ ./internal/core/services/time_entry/ -count=1</automated>
  </verify>
  <done>Routing package fully unit-tested; time_entry suite green (full-repo check at the wave gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service call sites → routing.Service | Internal Go boundary — approver sets computed for authorization decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Elevation of Privilege | routing.ResolveManagerStage | mitigate | Verbatim extraction preserves the approver-set semantics; unit tests pin roleGated/skipToFinance/approverIDs per BE-014; proposal approval (11-05) consumes the same function so entry and proposal routing cannot drift (D-G) |
| T-11-SC | Tampering | package installs | accept | No package-manager installs this phase — Go-only refactor on the existing dependency set |

**Remaining register (T-11-01, T-11-03..T-11-11) is enforced by plans 01/04/05/06.**
</threat_model>

<verification>
- `go test ./internal/core/services/routing/ ./internal/core/services/time_entry/ -count=1` after each task
- `go test ./... -count=1` (or `make test`) after the wave — valid as the wave gate because wave 1 runs to completion before the gate: 11-01 Task 1 fixes the pre-existing red TestMigration011/012 cycle tests, so the full suite is green only at wave end, not mid-wave (dependency_correctness W-2)
</verification>

<success_criteria>
- routing package exists with verbatim BE-014 semantics
- time_entry service delegates at both call sites; its suite is green
- Full repo test suite green (no drift)
</success_criteria>

## Artifacts this phase produces

- `internal/core/services/routing/routing.go` — NEW package `routing`: type `managerResolution` (unexported: approverIDs []uuid.UUID, roleGated, skipToFinance), `type Service struct{wgRepo, activityRepo, unitRepo}`, funcs `NewService(wgRepo, activityRepo, unitRepo) *Service`, `(*Service).ResolveManagerStage(ctx, orgID, activityID, unitID, ownerID uuid.UUID) (*managerResolution, error)`, `(*Service).ResolveUnitManager(ctx, unitID uuid.UUID) (uuid.UUID, bool, error)`
- `internal/core/services/routing/routing_test.go` — NEW: TestRoutingAnchoredWGApproverSet, TestRoutingCommercialWithoutWGNotLoggable, TestRoutingUnitManagerFallback, TestRoutingUnitManagerUpwardWalk, TestRoutingTerminalRoleGated, TestResolveUnitManagerNotFound
- `internal/core/services/time_entry/time_entry.go` — MODIFIED: removed private resolveManagerStage/resolveUnitManager/managerResolution; Service gains `routing *routing.Service` field + NewService param; call sites delegate
- `internal/core/services/time_entry/time_entry_test.go` — MODIFIED: fixture passes the routing service

<output>
Create `.planning/phases/11-foundations-schema-origins-tickets-backend/11-03-SUMMARY.md` when done
</output>
