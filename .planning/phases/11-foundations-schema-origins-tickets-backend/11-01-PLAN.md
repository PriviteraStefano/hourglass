---
phase: 11-foundations-schema-origins-tickets-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/014_ticket_schema.up.sql
  - migrations/014_ticket_schema.down.sql
  - migrations/015_activity_origins.up.sql
  - migrations/015_activity_origins.down.sql
  - migrations/016_contract_sold_hours.up.sql
  - migrations/016_contract_sold_hours.down.sql
  - migrations/017_audit_logs.up.sql
  - migrations/017_audit_logs.down.sql
  - internal/adapters/secondary/postgres/exported_test_helpers.go
  - internal/adapters/secondary/postgres/activity_ontology_migration_test.go
  - internal/adapters/secondary/postgres/staffing_schema_migration_test.go
  - internal/adapters/secondary/postgres/ontology_extension_migrations_test.go
autonomous: true
requirements: [FND-01, FND-03, TICK-01, TICK-02, TICK-04, TICK-05, FND-04]
must_haves:
  truths:
    - "The 014-017 migrations apply cleanly up, down, and up again (ADR-BE-004 cycle)"
    - "Legacy rows (NULL discriminators) pass every new CHECK — migration cycle tests prove it"
    - "The pre-existing red migration cycle tests (011/012) are green before any new test is written"
    - "New tables are torn down between test packages (teardown list extended)"
  artifacts:
    - path: "migrations/014_ticket_schema.up.sql"
      provides: "tickets + ticket_comments tables (kind/status CHECK vocabulary, dismissed_hours, indexes)"
      contains: "CREATE TABLE tickets"
    - path: "migrations/015_activity_origins.up.sql"
      provides: "origin_type discriminator + 5 nullable ref columns + refs-to-type CHECK + ticket_id FK + index"
      contains: "activities_origin_refs_check"
    - path: "migrations/016_contract_sold_hours.up.sql"
      provides: "contract_type/sold_hours/sold_period + type-consistency CHECK"
      contains: "contracts_sold_check"
    - path: "migrations/017_audit_logs.up.sql"
      provides: "general audit_logs table + (entity_type, entity_id, created_at) index"
      contains: "CREATE TABLE audit_logs"
  key_links:
    - from: "migrations/014_ticket_schema.up.sql"
      to: "migrations/015_activity_origins.up.sql"
      via: "activities.ticket_id FK resolves against tickets (016-before-014 ordering problem solved by numbering tickets 014)"
      pattern: "ticket_id\\s+UUID\\s+REFERENCES tickets\\(id\\)"
    - from: "exported_test_helpers.go"
      to: "tickets/ticket_comments/audit_logs"
      via: "teardown drop list (before activities)"
      pattern: "\"audit_logs\"|\"ticket_comments\"|\"tickets\""
---

<objective>
Migration foundation for the three-plane ontology: the four additive migrations (014 tickets, 015 origins, 016 sold_hours, 017 audit_logs) with up/down pairs, plus the test-infrastructure fix the phase inherits (pre-existing red cycle tests 011/012, teardown list).

Purpose: Every later plan (sold_hours, origins, tickets) compiles and tests against these tables. Without fixing the broken cycle-test seed wiring first, every new migration cycle test would fail identically (research Pitfall 3, verified by execution). This plan also pins the CHECK-constraint semantics (three-valued-logic guard, D-01) so legacy rows stay valid (D-16, Pitfall 1).
Output: 8 migration SQL files, 4 new migration cycle tests, green 011/012 cycle tests, extended teardown list.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-CONTEXT.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-RESEARCH.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-PATTERNS.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pre-existing red migration cycle tests + extend teardown list</name>
  <files>internal/adapters/secondary/postgres/activity_ontology_migration_test.go, internal/adapters/secondary/postgres/staffing_schema_migration_test.go, internal/adapters/secondary/postgres/exported_test_helpers.go</files>
  <read_first>
    - internal/adapters/secondary/postgres/activity_ontology_migration_test.go (failing test, its assertions are the contract)
    - internal/adapters/secondary/postgres/staffing_schema_migration_test.go (failing test + the cycle-test skeleton 014-017 will mirror)
    - internal/adapters/secondary/postgres/exported_test_helpers.go (applyMigrations, TeardownTestSchema, seedOrg/seedUser/seedActivity/seedActivityKind helpers)
    - internal/adapters/secondary/postgres/test_setup.go (SetupPackageContainer)
    - migrations/011_activity_ontology.up.sql + migrations/012_staffing_schema.up.sql (what the migrations change)
    - migrations/000_full_schema.up.sql (pre-011 tables: projects, subprojects, organization_memberships — the seed targets)
  </read_first>
  <action>
    Run `go test ./internal/adapters/secondary/postgres/ -run 'TestMigration011|TestMigration012' -count=1` first to confirm the red state (research Pitfall 3: both fail because the MVP seed data from historical `003_seed.up.sql` is no longer applied — seed data lives in `scripts/seed_demo.sql` which `applyMigrations` never loads).

    Fix the two tests by SELF-SEEDING the pre-state each test's assertions require — do NOT restore `003_seed.up.sql` and do NOT load `scripts/seed_demo.sql` into tests (demo data is not a migration fixture). For each test: read every assertion (counts like 6 memberships / 6 projects / 6 subprojects / 12 time entries, and any FK-existence requirements), then seed exactly those rows inline in the test before applying the migration under test. Use the existing helpers (seedOrg, seedUser, seedActivity, seedActivityKind in exported_test_helpers.go) where the helper covers the table; use direct `pool.Exec` SQL inserts for rows helpers don't cover (e.g., legacy `projects`/`subprojects`/`project_managers` rows for the 011 test, `organization_memberships` rows for the 012 test). Assertions must keep their original expected values — only the seeding strategy changes. Per ADR-BE-004 the migrations themselves stay untouched.

    Then extend `TeardownTestSchema` in exported_test_helpers.go: insert `"audit_logs"`, `"ticket_comments"`, `"tickets"` into the drop list immediately BEFORE `"activities"` (activities gains a FK to tickets in migration 015; teardown drops use CASCADE so order is forgiving, but keep the dependency-ordered shape per research Pitfall 8).
  </action>
  <acceptance_criteria>
    - `go test ./internal/adapters/secondary/postgres/ -run 'TestMigration011_ActivityOntology_UpDownUpCycle|TestMigration012_StaffingSchema_UpDownUpCycle' -count=1` exits 0
    - No file under `migrations/` is modified by this task (git status shows only test-helper changes)
    - `exported_test_helpers.go` contains `"audit_logs"`, `"ticket_comments"`, `"tickets"` entries in the TeardownTestSchema table list before `"activities"`
    - The seeded pre-state in each test is explicit inline SQL/helpers, not a load of `scripts/seed_demo.sql` or `003_seed.up.sql`
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration011_ActivityOntology_UpDownUpCycle|TestMigration012_StaffingSchema_UpDownUpCycle' -count=1</automated>
  </verify>
  <done>The 011 and 012 cycle tests pass against self-seeded pre-state; teardown list extended with the three new tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create migrations 014-017 (up/down pairs)</name>
  <files>migrations/014_ticket_schema.up.sql, migrations/014_ticket_schema.down.sql, migrations/015_activity_origins.up.sql, migrations/015_activity_origins.down.sql, migrations/016_contract_sold_hours.up.sql, migrations/016_contract_sold_hours.down.sql, migrations/017_audit_logs.up.sql, migrations/017_audit_logs.down.sql</files>
  <read_first>
    - migrations/012_staffing_schema.up.sql + migrations/012_staffing_schema.down.sql (ALTER + CREATE TABLE + CHECK + index house style, down-file shape)
    - migrations/011_activity_ontology.up.sql (activities base table: columns, kind FK to activity_kinds catalog, created_by_org_id)
    - migrations/000_full_schema.up.sql (contracts table ~line 125, users, organizations, time_entries.hours DECIMAL(8,2) ~line 301 area)
    - .agents/skills/postgresql-table-design/SKILL.md (DECIMAL precision, FK index guidance, CHECK semantics)
  </read_first>
  <action>
    Create four migrations, numbered to resolve the tickets-before-origins FK ordering (A8): 014 tickets, 015 origins (FK to tickets), 016 sold_hours, 017 audit_logs. Every new column nullable; every multi-column CHECK written as `discriminator IS NULL OR (<per-type rules with explicit IS [NOT] NULL>)` per D-01/Pitfall 1. All per D-16 (additive, no backfill).

    `014_ticket_schema.up.sql` — CREATE TABLE tickets: id UUID PRIMARY KEY DEFAULT gen_random_uuid(); org_id UUID NOT NULL REFERENCES organizations(id); title VARCHAR(255) NOT NULL; description TEXT; kind VARCHAR(50) NOT NULL CHECK (kind IN ('question','bug','change','evolution')); status VARCHAR(50) NOT NULL DEFAULT 'open' CHECK (status IN ('open','triage','planned','in_progress','resolved','closed','dismissed')); requester_id UUID NOT NULL REFERENCES users(id); assignee_id UUID REFERENCES users(id); dismissed_hours DECIMAL(8,2); created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(); updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(). Indexes: idx_tickets_org_id ON tickets(org_id), idx_tickets_status ON tickets(status). CREATE TABLE ticket_comments: id UUID PK, ticket_id UUID NOT NULL REFERENCES tickets(id) ON DELETE CASCADE, author_id UUID NOT NULL REFERENCES users(id), body TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(); index idx_ticket_comments_ticket_id. dismissed_hours implements TICK-04's "dismissed with N h logged" note (OQ3/A4 resolution: column + audit row, D-13 raw Σ value). down.sql: DROP TABLE IF EXISTS ticket_comments; DROP TABLE IF EXISTS tickets; (mirror 012 down style).

    `015_activity_origins.up.sql` — ALTER TABLE activities ADD: origin_type VARCHAR(50); assigned_by UUID REFERENCES users(id); assigned_to UUID REFERENCES users(id); proposed_by UUID REFERENCES users(id); reviewed_by UUID REFERENCES users(id); ticket_id UUID REFERENCES tickets(id). Constraint activities_origin_type_check CHECK (origin_type IN ('manager_assignment','employee_proposal','customer_ticket')). Constraint activities_origin_refs_check CHECK (origin_type IS NULL OR (origin_type = 'manager_assignment' AND assigned_by IS NOT NULL AND assigned_to IS NOT NULL AND proposed_by IS NULL AND reviewed_by IS NULL AND ticket_id IS NULL) OR (origin_type = 'employee_proposal' AND proposed_by IS NOT NULL AND assigned_by IS NULL AND assigned_to IS NULL AND ticket_id IS NULL) OR (origin_type = 'customer_ticket' AND ticket_id IS NOT NULL AND assigned_by IS NULL AND assigned_to IS NULL AND proposed_by IS NULL AND reviewed_by IS NULL)) — reviewed_by deliberately unconstrained per D-02 (only proposed_by required; see research OQ1). CREATE INDEX idx_activities_ticket_id ON activities(ticket_id). down.sql: DROP INDEX IF EXISTS idx_activities_ticket_id; ALTER TABLE activities DROP CONSTRAINT IF EXISTS activities_origin_refs_check, DROP CONSTRAINT IF EXISTS activities_origin_type_check; ALTER TABLE activities DROP COLUMN IF EXISTS ticket_id, DROP COLUMN IF EXISTS reviewed_by, DROP COLUMN IF EXISTS proposed_by, DROP COLUMN IF EXISTS assigned_to, DROP COLUMN IF EXISTS assigned_by, DROP COLUMN IF EXISTS origin_type;

    `016_contract_sold_hours.up.sql` — ALTER TABLE contracts ADD: contract_type VARCHAR(50); sold_hours DECIMAL(8,2) (align with time_entries.hours scale per research); sold_period VARCHAR(10). Constraint contracts_sold_check CHECK (contract_type IS NULL OR (contract_type = 'support' AND sold_hours IS NOT NULL AND sold_period IS NOT NULL) OR (contract_type = 'project' AND sold_period IS NULL)) per D-08/D-09. down.sql: DROP CONSTRAINT IF EXISTS contracts_sold_check; DROP COLUMN sold_period, sold_hours, contract_type.

    `017_audit_logs.up.sql` — CREATE TABLE audit_logs: id UUID PK, org_id UUID NOT NULL REFERENCES organizations(id), entity_type VARCHAR(50) NOT NULL, entity_id UUID NOT NULL, action VARCHAR(50) NOT NULL, actor_id UUID REFERENCES users(id), comment TEXT, payload JSONB, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW() per D-05; CREATE INDEX idx_audit_logs_entity ON audit_logs(entity_type, entity_id, created_at). down.sql: DROP TABLE IF EXISTS audit_logs;
  </action>
  <acceptance_criteria>
    - `migrations/014_ticket_schema.up.sql` contains `CHECK (kind IN ('question','bug','change','evolution'))` and `CHECK (status IN ('open','triage','planned','in_progress','resolved','closed','dismissed'))`
    - `migrations/015_activity_origins.up.sql` contains `origin_type IS NULL` guard and the per-type ref pins (explicit IS NOT NULL / IS NULL on every ref column)
    - `migrations/015_activity_origins.up.sql` contains `REFERENCES tickets(id)` (FK resolution requires 014 applied first)
    - `migrations/016_contract_sold_hours.up.sql` contains `contract_type = 'support' AND sold_hours IS NOT NULL AND sold_period IS NOT NULL`
    - `migrations/017_audit_logs.up.sql` contains `payload JSONB` and `idx_audit_logs_entity`
    - Each `.up.sql` has a matching `.down.sql` that reverses it (no `migrations/` file modified outside the 8 new files)
  </acceptance_criteria>
  <verify>
    <automated>ls migrations/014_ticket_schema.up.sql migrations/014_ticket_schema.down.sql migrations/015_activity_origins.up.sql migrations/015_activity_origins.down.sql migrations/016_contract_sold_hours.up.sql migrations/016_contract_sold_hours.down.sql migrations/017_audit_logs.up.sql migrations/017_audit_logs.down.sql && grep -c 'origin_type IS NULL' migrations/015_activity_origins.up.sql</automated>
  </verify>
  <done>Eight migration files exist with the exact CHECK vocabulary and nullable-column shapes; ordering resolves the tickets FK.</done>
</task>

<task type="auto">
  <name>Task 3: Write migration cycle tests 014-017</name>
  <files>internal/adapters/secondary/postgres/ontology_extension_migrations_test.go</files>
  <read_first>
    - internal/adapters/secondary/postgres/staffing_schema_migration_test.go (cycle-test skeleton: applyMigrations, readMigration, assertTableExists, assertConstraintExists, assertColumnNotNull, assertFkAction)
    - internal/adapters/secondary/postgres/activity_ontology_migration_test.go (fixed in Task 1 — same helper usage)
    - internal/adapters/secondary/postgres/exported_test_helpers.go (seed helpers for org/user/kind pre-state)
    - migrations/014_ticket_schema.up.sql, migrations/015_activity_origins.up.sql, migrations/016_contract_sold_hours.up.sql, migrations/017_audit_logs.up.sql (the shapes under test)
  </read_first>
  <action>
    Create `ontology_extension_migrations_test.go` in the postgres package with four cycle tests mirroring the 012 skeleton: TestMigration014_TicketSchema_UpDownUpCycle, TestMigration015_ActivityOrigins_UpDownUpCycle, TestMigration016_ContractSoldHours_UpDownUpCycle, TestMigration017_AuditLogs_UpDownUpCycle. Each: apply migrations (applyMigrations), assert schema shape, apply down + up again (cycle), assert still green.

    Functional assertions per test (these are the Pitfall-1 regression guards, not just existence checks):
    - 014: assertTableExists tickets + ticket_comments; assertConstraintExists for kind + status CHECKs; assertFkAction on ticket_comments.ticket_id (CASCADE). Seed one org + one user (helpers), insert a ticket (SQL), insert a comment, assert both readable.
    - 015: assertConstraintExists activities_origin_refs_check + activities_origin_type_check; assertFkAction on activities.ticket_id. Insert THREE rows into activities using seeded pre-state (org, user, an activity_kind from the catalog): (a) a legacy-shaped row with ALL origin columns NULL — MUST succeed (three-valued-logic guard); (b) a valid customer_ticket row (origin_type + ticket_id set, others NULL) — must succeed; (c) a mixed-refs row (origin_type='customer_ticket' AND assigned_by set) — MUST fail with a constraint error (Pitfall 1 warning sign).
    - 016: assertConstraintExists contracts_sold_check. Insert three contracts (org pre-state): (a) contract_type NULL — succeeds; (b) 'support' with sold_hours + sold_period — succeeds; (c) 'support' without sold_period — fails; (d) 'project' with sold_period — fails.
    - 017: assertTableExists audit_logs; assertConstraintExists is N/A (no CHECKs) — instead insert one audit_logs row (org + user pre-state) and assert it reads back with its payload JSONB intact.
  </action>
  <acceptance_criteria>
    - File contains four tests named TestMigration014_TicketSchema_UpDownUpCycle, TestMigration015_ActivityOrigins_UpDownUpCycle, TestMigration016_ContractSoldHours_UpDownUpCycle, TestMigration017_AuditLogs_UpDownUpCycle
    - The 015 test asserts BOTH a legacy-NULL insert succeeds AND a mixed-refs insert fails (grep for the constraint-violation assertion)
    - The 016 test asserts the support-without-period insert fails
    - `go test ./internal/adapters/secondary/postgres/ -count=1` exits 0 (whole package: fixed 011/012 + new 014-017 + all repo tests)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -count=1</automated>
  </verify>
  <done>All four cycle tests pass; the whole postgres package suite is green including the previously red 011/012 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrations → running DB | Untrusted? No — migrations are repo-controlled DDL; but CHECK semantics must not corrupt legacy data (D-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Integrity | migrations/015 CHECK shape | mitigate | `origin_type IS NULL OR (...)` three-valued-logic guard (Pitfall 1); cycle test inserts legacy-shaped all-NULL row and asserts it passes (Task 3) |
| T-11-09 | Tampering | SQL in cycle tests | mitigate | parameterized queries or literal strings only from repo-controlled test files; no user input reaches these |
| T-11-SC | Tampering | package installs | accept | No package-manager installs this phase — pure Go on the existing go.mod dependency set (verified `go mod verify`); nothing to vet |

**Threats T-11-01..T-11-08 (role gates, immutability, audit) are enforced in code plans 04/05/06; this plan only shapes the schema they rely on (CHECK vocabulary, append-only tables with no update/delete paths).**
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -count=1` (full postgres package, includes fixed 011/012 + new 014-017 cycle tests)
- `make test` after the wave (per VALIDATION.md sampling: full suite after every wave)
</verification>

<success_criteria>
- 014-017 migrations exist with up/down pairs and pass up→down→up cycles (SC-9)
- Legacy rows pass every new CHECK (proven by functional test assertions)
- 011/012 cycle tests green — pre-existing debt closed before new tests landed
- Teardown list covers the three new tables
</success_criteria>

## Artifacts this phase produces

- `migrations/014_ticket_schema.up.sql` / `.down.sql` — tables `tickets` (id, org_id, title, description, kind, status, requester_id, assignee_id, dismissed_hours DECIMAL(8,2), created_at, updated_at), `ticket_comments` (id, ticket_id, author_id, body, created_at); indexes idx_tickets_org_id, idx_tickets_status, idx_ticket_comments_ticket_id
- `migrations/015_activity_origins.up.sql` / `.down.sql` — `activities` columns origin_type VARCHAR(50), assigned_by/assigned_to/proposed_by/reviewed_by/ticket_id UUID; constraints `activities_origin_type_check`, `activities_origin_refs_check`; index idx_activities_ticket_id
- `migrations/016_contract_sold_hours.up.sql` / `.down.sql` — `contracts` columns contract_type VARCHAR(50), sold_hours DECIMAL(8,2), sold_period VARCHAR(10); constraint `contracts_sold_check`
- `migrations/017_audit_logs.up.sql` / `.down.sql` — table `audit_logs` (id, org_id, entity_type, entity_id, action, actor_id, comment, payload JSONB, created_at); index idx_audit_logs_entity
- `internal/adapters/secondary/postgres/ontology_extension_migrations_test.go` — test funcs TestMigration014_TicketSchema_UpDownUpCycle, TestMigration015_ActivityOrigins_UpDownUpCycle, TestMigration016_ContractSoldHours_UpDownUpCycle, TestMigration017_AuditLogs_UpDownUpCycle
- `exported_test_helpers.go` — teardown list entries `"audit_logs"`, `"ticket_comments"`, `"tickets"`
- Fixed: `activity_ontology_migration_test.go`, `staffing_schema_migration_test.go` (self-seeded pre-state, assertions unchanged)

<output>
Create `.planning/phases/11-foundations-schema-origins-tickets-backend/11-01-SUMMARY.md` when done
</output>
