---
phase: 11-foundations-schema-origins-tickets-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md
  - hourglass-vault/decisions/project/ADR-P-013 — Origins.md
  - hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md
  - hourglass-vault/decisions/project/_index.md
  - hourglass-vault/decisions/backend/_index.md
autonomous: true
requirements: [FND-01, FND-02, FND-04, TICK-01, TICK-02, TICK-03, TICK-04, TICK-05]
must_haves:
  truths:
    - "ADR-P-003 is revised to the v0.2 lifecycle (triage, reopen, dismissal guard) while preserving the hard boundary list"
    - "ADR-P-013 records the origin-axis decisions (D-01..D-04, D-12) incl. reviewed_by resolution and the FND-04 Phase-13 fallback"
    - "ADR-BE-016 records the schema encoding (migrations 014-017, CHECK vocabulary, three-layer ticket model, in-transaction audit writes)"
    - "Both _index.md files list the new/changed ADRs"
  artifacts:
    - path: "hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md"
      provides: "revised ticket lifecycle + hard boundary list"
      contains: "dismissed"
    - path: "hourglass-vault/decisions/project/ADR-P-013 — Origins.md"
      provides: "origin axis decision record"
      contains: "employee_proposal"
    - path: "hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md"
      provides: "BE encoding ADR (schema + audit semantics)"
      contains: "audit_logs"
  key_links:
    - from: "ADR-P-013"
      to: "ADR-P-003 rev"
      via: "ticket_id origin ref requires the tickets entity (P-003)"
      pattern: "customer_ticket"
---

<objective>
Record the phase's decision documents in the vault: revise ADR-P-003 (tickets lifecycle per D-A), draft ADR-P-013 (origin axis per D-D/D-01..D-04/D-12), draft the BE encoding ADR ADR-BE-016 (schema 014-017 + audit semantics), and update both decision indexes.

Purpose: Milestone convention — "Each backend phase drafts its ADR + BE encoding ADR". The ADRs pin the semantic resolutions this plan set makes (reviewed_by NULL at creation, terminal-activity definition, in-transaction audit writes, dismissal note column) so Phase 12/13 consumers and future readers have the record of truth outside the research note.
Output: 3 ADR files (1 revised, 2 new) + 2 index updates.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-CONTEXT.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-RESEARCH.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise ADR-P-003 (tickets lifecycle)</name>
  <files>hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md, hourglass-vault/decisions/project/_index.md</files>
  <read_first>
    - hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md (current Proposed draft — revise in place)
    - hourglass-vault/decisions/project/_index.md (entry format to update)
    - hourglass-vault/research/2026-08-01 — Origins, Tickets & Coverage — Ontology Extension Research.md (Parts 12-15; decisions D-A..D-AA — record of truth)
  </read_first>
  <action>
    Revise the existing ADR-P-003 (status stays Proposed; add a "Revised 2026-08-03 (Phase 11)" section noting the delta). Replace the lifecycle sketch with the v0.2 state machine per D-A/D-14: statuses `open → triage → planned → in_progress → resolved → closed` plus `reopen (resolved → in_progress)` and `dismissed` from `open|triage` guarded by the dismissal guard (D-M, TICK-04); kinds are a closed set `question · bug · change · evolution` (TICK-01, D-H); tickets are internal-only in v0.2 (D-E) with backend permission gates per D-15 (create → any employee, update/comment → owner/assignee/manager+, triage/dismiss → manager+finance, view → all internal members; customer role rejected — tickets are not customer-facing per the out-of-scope table). The chain is ticket → activity → entries; tickets never reference entries (revised P-003, TICK-03). KEEP the hard boundary list verbatim: tickets are demand tracking, NOT task execution — no kanban, no sub-task trees, no comment threads as conversation (resolution note only). Note the dismissal note semantics (TICK-04: "dismissed with N h logged" via `dismissed_hours` column + audit row). Update the project _index.md entry for ADR-P-003 (revised date + one-line summary change).
  </action>
  <acceptance_criteria>
    - The ADR-P-003 file contains all seven statuses (`open`, `triage`, `planned`, `in_progress`, `resolved`, `closed`, `dismissed`) and the reopen edge `resolved → in_progress`
    - The file contains the closed kind set (`question`, `bug`, `change`, `evolution`)
    - The hard boundary list (no kanban / no sub-task trees / no comment-thread-as-conversation) is still present — grep each term
    - The file mentions `dismissed_hours` and the dismissal guard
    - `_index.md` entry for ADR-P-003 reflects the revision (date + summary)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'dismissed' "hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md" && grep -c 'kanban' "hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md"</automated>
  </verify>
  <done>ADR-P-003 reflects the v0.2 lifecycle, permission gates, and preserved hard boundaries; index updated.</done>
</task>

<task type="auto">
  <name>Task 2: Draft ADR-P-013 (origin axis)</name>
  <files>hourglass-vault/decisions/project/ADR-P-013 — Origins.md, hourglass-vault/decisions/project/_index.md</files>
  <read_first>
    - hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md (house ADR format + cross-reference to three-plane model)
    - hourglass-vault/research/2026-08-01 — Origins, Tickets & Coverage — Ontology Extension Research.md (D-D origin = type + ref set; R4 resolution §"R4 resolution: origin refs storage" ~line 522; D-G proposal routing)
    - hourglass-vault/decisions/project/_index.md
  </read_first>
  <action>
    Draft ADR-P-013 "Origins" (status Proposed) recording: the three origin types (manager_assignment → assigned_by/assigned_to, employee_proposal → proposed_by/reviewed_by, customer_ticket → ticket_id, per D-D/FND-01); storage decision — discriminator + nullable columns, EAV and JSONB rejected by R4 (D-01); same-org validation at service level with DB FKs where possible (D-02); immutability — refs set once at creation, service rejects any update carrying origin fields with ErrOriginImmutable, no DB trigger (D-03); creation gates per origin type (D-04: manager_assignment + customer_ticket → manager/finance role, employee_proposal → any employee with proposed_by = self); proposal approval = activity created with origin employee_proposal + is_active=false, approval flips is_active, approval + lifecycle events live in audit_logs never in origin refs (D-12, D-G); **reviewed_by resolution (OQ1)**: Phase 11 leaves reviewed_by NULL at creation — the CHECK requires only proposed_by (D-02) — and the actual approver is recorded in the proposal_approved audit row; a future phase may pin reviewed_by at creation-time; **FND-04**: refs stored directly on activities, the first-direction-record fallback is an additive Phase-13 read-path enhancement — no migration, the read model must not be painted into a corner. Cross-reference ADR-P-003 (customer_ticket refs) and ADR-P-012 (three-plane model). Add the ADR-P-013 entry to the project _index.md.
  </action>
  <acceptance_criteria>
    - File `hourglass-vault/decisions/project/ADR-P-013 — Origins.md` exists
    - Contains `manager_assignment`, `employee_proposal`, `customer_ticket`
    - Contains `ErrOriginImmutable` (the D-03 sentinel name)
    - Contains the reviewed_by Phase-11 resolution (NULL at creation, approver in audit)
    - Contains the FND-04 Phase-13 fallback statement
    - `_index.md` lists ADR-P-013
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ErrOriginImmutable' "hourglass-vault/decisions/project/ADR-P-013 — Origins.md" && grep -c 'reviewed_by' "hourglass-vault/decisions/project/ADR-P-013 — Origins.md"</automated>
  </verify>
  <done>ADR-P-013 records the origin-axis decisions incl. reviewed_by and FND-04 resolutions; index updated.</done>
</task>

<task type="auto">
  <name>Task 3: Draft ADR-BE-016 (schema encoding)</name>
  <files>hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md, hourglass-vault/decisions/backend/_index.md</files>
  <read_first>
    - hourglass-vault/decisions/backend/ADR-BE-012 — Audit Log Writes.md (the ADR this phase extends from approval-table-only to the general audit_logs table, D-05)
    - hourglass-vault/decisions/backend/ADR-BE-004 — Database Migrations.md (append-only rule, cycle tests)
    - hourglass-vault/decisions/backend/_index.md
    - migrations/014_ticket_schema.up.sql, migrations/015_activity_origins.up.sql, migrations/016_contract_sold_hours.up.sql, migrations/017_audit_logs.up.sql (created in 11-01 — the encoded shapes)
  </read_first>
  <action>
    Draft ADR-BE-016 (status Proposed), the BE encoding ADR for migrations 014-017: encode each migration's shape (tickets + ticket_comments with kind/status CHECK vocabulary; activities origin columns + refs-to-type CHECK; contracts contract_type/sold_hours/sold_period + contracts_sold_check; general audit_logs table) and the house rule that multi-column CHECKs carry the `discriminator IS NULL OR (...)` three-valued-logic guard (Pitfall 1, D-16 legacy safety). Record the three-layer ticket model (state / comments / audit — D-06): comments are first-class in ticket_comments, audit_logs tracks every event including comments; no UPDATE/DELETE paths exist on any of the three (TICK-05). Record the audit-write decision (OQ4/A3): ticket audit rows are written SYNCHRONOUSLY inside the same transaction as the state change (triage, transitions, dismissal, comments, create) — the ADR-BE-012 fire-and-forget pattern stays for entry approvals but is NOT used for tickets (Pitfall 2); the deferred outbox upgrade remains possible because the table shape is consumer-neutral. Record the semantic resolutions: terminal activity = no non-terminal time entries (status IN draft/submitted/pending_manager/pending_finance, is_deleted=false) on the linked-activity subtree via recursive CTE (OQ2); dismissal guard raw Σ per D-13 with the stable port signature `LoggedHours(ctx, ticketID)` kept for the Phase-12 net-of-compensations swap; transition matrix edges (A7) with dismissed reachable from open|triage; pre-triage fast path allowed (OQ5: POST /activities with origin customer_ticket while the ticket is open/triage, manager+ gated). Also record the sold_hours semantics (D-07..D-09: project → lifetime total, support → per-period, NULL contract_type = legacy project) and that BE-012's scope note now covers the general table. Add the ADR-BE-016 entry to the backend _index.md.
  </action>
  <acceptance_criteria>
    - File `hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md` exists
    - Contains `audit_logs`, `ticket_comments`, and the three-layer split statement (state / comments / audit)
    - Contains the synchronous in-transaction audit-write decision (grep `synchronously` or `in-transaction`)
    - Contains the terminal-activity definition (grep `terminal`)
    - Contains `LoggedHours` (the Phase-12-stable guard signature)
    - `_index.md` lists ADR-BE-016
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'LoggedHours' "hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md" && grep -c 'in-transaction' "hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md"</automated>
  </verify>
  <done>ADR-BE-016 records schema + audit semantics + semantic resolutions; backend index updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision records → future implementers | Docs are consumed as constraints by Phases 12/13; inaccuracies propagate as wrong behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Repudiation | ADR-BE-016 audit-write section | mitigate | ADR explicitly records synchronous in-transaction ticket audit writes (Pitfall 2) so a later implementer cannot regress to fire-and-forget; grep-verified in acceptance criteria |
| T-11-SC | Tampering | package installs | accept | No package-manager installs this phase — docs-only plan |

**No code is written in this plan; the remaining threat register (T-11-01..T-11-07, T-11-09..T-11-11) is enforced by plans 01/04/05/06.**
</threat_model>

<verification>
- grep assertions in each task's acceptance criteria against the vault files
- No Go code or migrations modified (git status shows vault-only changes)
</verification>

<success_criteria>
- ADR-P-003 revised with v0.2 lifecycle + preserved hard boundaries
- ADR-P-013 and ADR-BE-016 drafted with all semantic resolutions recorded
- Both _index.md files current
</success_criteria>

## Artifacts this phase produces

- `hourglass-vault/decisions/project/ADR-P-003 — Tickets as the Second Capture Layer.md` — revised (v0.2 lifecycle: seven statuses + reopen + guarded dismissal; closed kind set; internal-only + D-15 permission gates; preserved hard boundary list)
- `hourglass-vault/decisions/project/ADR-P-013 — Origins.md` — NEW (origin types, storage decision, same-org validation, immutability + ErrOriginImmutable, creation gates, proposal approval model, reviewed_by resolution, FND-04 fallback)
- `hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md` — NEW (migrations 014-017 shapes, CHECK guard rule, three-layer ticket model, in-transaction audit writes, terminal-activity definition, dismissal-guard signature, transition matrix, fast path, sold_hours semantics)
- `hourglass-vault/decisions/project/_index.md`, `hourglass-vault/decisions/backend/_index.md` — updated entries

<output>
Create `.planning/phases/11-foundations-schema-origins-tickets-backend/11-02-SUMMARY.md` when done
</output>
