---
phase: 11-foundations-schema-origins-tickets-backend
plan: 06
type: execute
wave: 4
depends_on: ["11-01", "11-05"]
files_modified:
  - internal/core/ports/ticket_repository.go
  - internal/adapters/secondary/postgres/ticket_repository.go
  - internal/core/services/ticket/ticket.go
  - internal/core/services/ticket/ticket_test.go
  - internal/core/services/ticket/ticket_integration_test.go
  - internal/adapters/primary/http/ticket_handler.go
  - cmd/server/main.go
  - internal/core/services/testdata/mock_ticket_repo.go
  - internal/adapters/secondary/postgres/ticket_repository_test.go
autonomous: true
requirements: [TICK-01, TICK-02, TICK-03, TICK-04, TICK-05]
must_haves:
  truths:
    - "Any employee can create a ticket with a closed-set kind; customer role is rejected (TICK-01, D-15)"
    - "The lifecycle works: open → triage → planned → in_progress → resolved → closed, plus reopen resolved → in_progress and guarded dismissal; invalid edges return errors (TICK-02, D-14)"
    - "resolved is blocked while any linked activity has non-terminal time entries on its subtree (TICK-02, OQ2 definition)"
    - "Triage atomically reclassifies kind + creates 1..N customer_ticket-origin activities + flips ticket to planned — all-or-nothing (TICK-03, D-10)"
    - "dismissal is blocked while linked activities carry logged hours (submitted+approved, not deleted); the dismissed ticket carries the hours note (TICK-04, D-13)"
    - "Ticket history is append-only: comments/transitions/resolution notes land in audit_logs in the same transaction; no update/delete endpoints exist (TICK-05)"
  artifacts:
    - path: "internal/core/services/ticket/ticket.go"
      provides: "ticket service: Create/List/Get/UpdateDetails/Transition/Dismiss/Triage/AddComment/ListHistory"
      contains: "ErrDismissalBlocked"
    - path: "internal/adapters/primary/http/ticket_handler.go"
      provides: "HTTP adapter for the ticket service"
      contains: "Triage"
    - path: "internal/adapters/secondary/postgres/ticket_repository.go"
      provides: "tx-backed mutators + LoggedHours + HasNonTerminalActivities + ListHistory"
      contains: "BeginTx"
    - path: "internal/core/ports/ticket_repository.go"
      provides: "full TicketRepository interface (extended from 11-05)"
      contains: "LoggedHours"
  key_links:
    - from: "services/ticket/ticket.go"
      to: "domain/ticket/ticket.go"
      via: "CanTransition matrix gates every Transition/Dismiss call"
      pattern: "CanTransition"
    - from: "postgres/ticket_repository.go"
      to: "time_entries"
      via: "LoggedHours Σ (submitted+approved, is_deleted=false, linked via ticket_id + origin_type)"
      pattern: "COALESCE\\(SUM\\(hours\\),0\\)"
    - from: "services/ticket/ticket.go"
      to: "cmd/server/main.go"
      via: "routes POST /tickets/{id}/triage, /dismiss, /transition, /comments, GET /tickets/{id}/history"
      pattern: "ticketHandler"
---

<objective>
The ticket lifecycle service lands (TICK-01..05): create with closed-set kinds, the state machine with reopen + guarded dismissal, atomic triage converting a ticket into 1..N activities, comments, and the immutable history stream — all permission-gated per D-15 and audit-written in-transaction per ADR-BE-016.

Purpose: Tickets are the second capture layer (revised P-003). Every state change is an event; TICK-05 makes the stream real by writing audit rows in the same transaction as the state change (Pitfall 2). The dismissal guard and resolved-block read hours from time_entries (server-side Σ, never client-supplied — T-11-07).
Output: ticket service + HTTP handler + tx-backed repository + full test coverage.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-CONTEXT.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-RESEARCH.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-PATTERNS.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ticket service state machine + permissions + audit-in-tx (create/list/get/update/transition)</name>
  <files>internal/core/services/ticket/ticket.go (NEW), internal/core/services/ticket/ticket_test.go (NEW), internal/core/ports/ticket_repository.go, internal/adapters/secondary/postgres/ticket_repository.go, internal/core/services/testdata/mock_ticket_repo.go, internal/adapters/secondary/postgres/ticket_repository_test.go</files>
  <read_first>
    - internal/core/domain/ticket/ticket.go (entity, constants, CanTransition matrix, sentinels — created in 11-05)
    - internal/core/domain/audit/audit.go (AuditLog entity — created in 11-05)
    - internal/core/ports/ticket_repository.go (current minimal interface to EXTEND — created in 11-05)
    - internal/adapters/secondary/postgres/ticket_repository.go (current Get — created in 11-05; extend in place)
    - internal/core/services/time_entry/time_entry.go (transition-method skeleton L228-262 Submit + L264-320 Approve: load → permission → state → mutate → persist; audit-in-tx per ADR-BE-016 instead of pool-level writes)
    - internal/adapters/secondary/postgres/refresh_token_repo.go L88-136 (BeginTx/defer Rollback/Commit precedent)
    - internal/adapters/secondary/postgres/time_entry_repository.go L303-343 (audit INSERT shape — general table differs)
    - internal/models/models.go (RoleEmployee/RoleManager/RoleFinance/RoleCustomer)
    - internal/core/services/testdata/mock_ticket_repo.go (created in 11-05 — extend to full interface)
    - migrations/014_ticket_schema.up.sql + migrations/017_audit_logs.up.sql (column names)
  </read_first>
  <action>
    Extend `ports.TicketRepository` to the full interface: Get (existing); Create(ctx, orgID uuid.UUID, t *ticket.Ticket, audit *audit.AuditLog) (*ticket.Ticket, error); ListByOrg(ctx, orgID uuid.UUID, status, kind string) ([]ticket.Ticket, error); UpdateDetails(ctx, orgID, ticketID uuid.UUID, title, description *string, assigneeID *uuid.UUID, audit *audit.AuditLog) (*ticket.Ticket, error); UpdateState(ctx, orgID, ticketID uuid.UUID, to string, note *string, audit *audit.AuditLog) (*ticket.Ticket, error); Dismiss(ctx, orgID, ticketID uuid.UUID, hours float64, audit *audit.AuditLog) (*ticket.Ticket, error); Triage(ctx, orgID, ticketID uuid.UUID, kind *string, plans []*activitydomain.CreateActivityRequest, audits []*audit.AuditLog) (*ticket.Ticket, []*activitydomain.ActivityResponse, error); AddComment(ctx, orgID, ticketID uuid.UUID, c *ticket.TicketComment, audit *audit.AuditLog) (*ticket.TicketComment, error); ListComments(ctx, orgID, ticketID uuid.UUID) ([]ticket.TicketComment, error); ListHistory(ctx, orgID, ticketID uuid.UUID) ([]audit.AuditLog, error); LoggedHours(ctx, ticketID uuid.UUID) (float64, error); HasNonTerminalActivities(ctx, ticketID uuid.UUID) (bool, error).

    Repository mutators (postgres/ticket_repository.go): every mutator opens `pool.BeginTx` (or `pool.Begin`), performs the state write AND the audit INSERT in the same tx, commits; `defer tx.Rollback`; audit row = `INSERT INTO audit_logs (id, org_id, entity_type, entity_id, action, actor_id, comment, payload, created_at) VALUES (...)`, entity_type always `'ticket'`, payload from `json.Marshal(audit.Payload)`, actor_id/comment nullable. Implement now: Create (insert ticket with requester_id + status open + audit 'created'), ListByOrg (org-scoped, optional status/kind filters, ORDER BY created_at DESC), UpdateDetails (title/description/assignee_id SET branches + updated_at + audit 'updated' with payload of changed fields), UpdateState (status SET + updated_at + audit 'status_changed' with payload {from, to, note}). Dismiss/Triage/AddComment/ListComments/ListHistory/LoggedHours/HasNonTerminalActivities are Task 2 (leave unimplemented only if the interface extension breaks the build — instead implement ALL methods in this task with Dismiss/Triage/AddComment/ListComments/ListHistory/LoggedHours/HasNonTerminalActivities as Task 2's scope: to keep the build green, the interface extension and the full implementation must land together; if you prefer, split the interface into two files — acceptable, but the compile-time assertion `var _ ports.TicketRepository` must hold at the end of Task 1 with at least stubbed-return implementations for Task 2 methods that you finish in Task 2; prefer implementing all in Task 1 where cheap, LoggedHours + HasNonTerminalActivities + ListHistory are single queries and cheap).

    Service `internal/core/services/ticket/ticket.go` (NEW package `ticket`): Service struct {repo ports.TicketRepository, activityRepo ports.ActivityRepository, contractRepo ports.ContractRepository, orgRepo ports.OrganizationRepository}; NewService(ticketRepo, activityRepo, contractRepo, orgRepo). Methods:
    - Create(ctx, orgID, actorID uuid.UUID, role string, req *CreateTicketRequest{Title, Description string, Kind string, AssigneeID *uuid.UUID}) (*ticket.Ticket, error) — gate role != RoleCustomer else ErrForbidden (T-11-04, D-15 create → any employee); title non-empty + kind in the four-kind set else ErrInvalidRequest (TICK-01); assignee (if set) must be an org member via orgRepo.GetMembership else ErrInvalidRequest (same-org); call repo.Create with audit {action: "created", payload: {kind}}.
    - List(ctx, orgID uuid.UUID, role, status, kind string) — gate role != RoleCustomer; validate status/kind params against the vocabulary else ErrInvalidRequest; repo.ListByOrg.
    - Get(ctx, orgID uuid.UUID, role string, ticketID uuid.UUID) — gate role != RoleCustomer; repo.Get + repo.ListComments; return both (detail).
    - UpdateDetails(ctx, orgID, actorID uuid.UUID, role string, ticketID uuid.UUID, title, description *string, assigneeID *uuid.UUID) — gate: role ∈ {manager, finance} OR ticket owner (RequesterID == actorID) OR assignee (IsAssignee) else ErrForbidden (D-15 update → owner/assignee/manager+); repo.UpdateDetails with audit {action: "updated"}.
    - Transition(ctx, orgID, actorID uuid.UUID, role string, ticketID uuid.UUID, to string, note *string) (*ticket.Ticket, error) — gate owner/assignee/manager+ (T-11-05); load ticket; `ticket.CanTransition(current, to)` else ErrInvalidTransition (TICK-02); if to == "resolved": `repo.HasNonTerminalActivities(ticketID)` true → ErrActivityNotTerminal (OQ2 terminal definition); repo.UpdateState with audit {action: "status_changed", payload: {from, to, note}}.

    Mock: extend `testdata/mock_ticket_repo.go` to the full interface (record calls, return configured values).

    Tests (ticket_test.go unit): create gating (customer → ErrForbidden, invalid kind → ErrInvalidRequest); transition matrix edges incl. invalid edge → ErrInvalidTransition and reopen resolved→in_progress allowed; resolved blocked on non-terminal (mock HasNonTerminalActivities true → ErrActivityNotTerminal); permission gates (employee non-owner non-assignee transition → ErrForbidden; assignee allowed); audit rows passed to repo contain the right action (assert on mock capture). Repo tests (ticket_repository_test.go): Create round-trip + audit row exists in audit_logs with entity_type 'ticket' (integration); UpdateState persists + audit row written in same tx (integration, assert count).
  </action>
  <acceptance_criteria>
    - ports/ticket_repository.go contains all 12 methods (the 11 extended here + existing Get from 11-05)
    - services/ticket/ticket.go contains Create/List/Get/UpdateDetails/Transition + the D-15 gates (grep `ErrForbidden` and `CanTransition` and `HasNonTerminalActivities`)
    - ticket_repository.go mutators use BeginTx and write audit rows in-tx (grep `BeginTx` and `INSERT INTO audit_logs`)
    - `go test ./internal/core/services/ticket/ -run 'TestTicketCreate|TestTicketLifecycle' -count=1` exits 0
    - `go build ./...` succeeds
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/core/services/ticket/ -run 'TestTicketCreate|TestTicketLifecycle' -count=1</automated>
  </verify>
  <done>State machine + gates work end-to-end; every state write commits its audit row atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic triage + dismissal guard + comments + history</name>
  <files>internal/core/services/ticket/ticket.go, internal/adapters/secondary/postgres/ticket_repository.go, internal/core/services/ticket/ticket_test.go, internal/core/services/ticket/ticket_integration_test.go (NEW), internal/adapters/secondary/postgres/ticket_repository_test.go</files>
  <read_first>
    - internal/core/services/ticket/ticket.go (current state after Task 1)
    - internal/core/services/activity/activity.go (activity validation rules triage must mirror: KindExists, parent same-org via Get, contract same-org — Pitfall 7)
    - internal/core/ports/activity_repository.go (KindExists, Get signatures)
    - internal/core/ports/contract_repository.go (Get signature)
    - internal/core/domain/activity/activity.go (CreateActivityRequest fields triage plans use)
    - internal/adapters/secondary/postgres/activity_repository_test.go (seed helpers for integration tests)
  </read_first>
  <action>
    Service additions (services/ticket/ticket.go):
    - Dismiss(ctx, orgID, actorID uuid.UUID, role string, ticketID uuid.UUID) (*ticket.Ticket, error) — gate role ∈ {RoleManager, RoleFinance} else ErrForbidden (T-11-05, D-11); load ticket; `ticket.CanTransition(current, "dismissed")` else ErrInvalidTransition (allowed from open|triage per the pinned matrix); `hours, err := repo.LoggedHours(ticketID)`; if hours > 0 → ErrDismissalBlocked (T-11-07, TICK-04, D-13 raw Σ); repo.Dismiss(ticketID, hours, audit {action: "dismissed", payload: {hours: hours}}) — sets status dismissed + dismissed_hours column; the note "dismissed with N h logged" renders from DismissedHours on read (TICK-04).
    - Triage(ctx, orgID, actorID uuid.UUID, role string, ticketID uuid.UUID, kind *string, plans []*TriageActivityPlan) (*ticket.Ticket, []*activitydomain.ActivityResponse, error) — gate role ∈ {RoleManager, RoleFinance} (T-11-05, D-11); load ticket; CanTransition(current, "planned") else ErrInvalidTransition; kind (if provided) ∈ four-kind set else ErrInvalidRequest; len(plans) >= 1 else ErrInvalidRequest. Structural plan checks in the service (pure string/pointer validation, NO DB reads): name non-empty; kind non-empty; governance model valid. The DB-read validations (kind in org catalog, parent same-org, contract same-org) are AUTHORITATIVE inside the REPO's tx — see repository additions below (Pitfall 7: do not split validation out of the tx; no TOCTOU window). The service MAY additionally fast-fail before the repo call via pool-level reads (activityRepo.KindExists, activityRepo.Get, contractRepo.Get) — optional UX only, never the correctness guarantee; the in-tx checks are authoritative and DB FK/CHECK constraints backstop. Then repo.Triage(ctx, orgID, ticketID, kind, convertedPlans, []*audit.AuditLog{{action: "triaged"}, {action: "activities_created"}}) — atomic: in-tx existence checks per plan → kind update (if provided) + status='planned' + 1..N activity inserts with origin_type='customer_ticket' + ticket_id + is_active=true + both audit rows (D-10, TICK-03, T-11-06). Note (D-A / TICK-02): "one demand thread per real-world request" is a triage-time process — the triage operator selects the plans for one request; no automated duplicate detection exists in v0.2 (research defines no mechanism; do not add one this phase).
    - AddComment(ctx, orgID, actorID uuid.UUID, role string, ticketID uuid.UUID, body string) (*ticket.TicketComment, error) — gate owner/assignee/manager+ (D-15); body non-empty else ErrInvalidRequest; repo.AddComment with audit {action: "comment_added"} — comment row (first-class, D-06) + audit row in the same tx (TICK-05).
    - ListHistory(ctx, orgID uuid.UUID, role string, ticketID uuid.UUID) ([]audit.AuditLog, error) — gate role != RoleCustomer; repo.ListHistory.

    Repository additions (postgres/ticket_repository.go): Dismiss (in-tx: status='dismissed' + dismissed_hours + updated_at + audit row); Triage (in-tx: FIRST validate every plan against the open tx — `SELECT EXISTS (SELECT 1 FROM activity_kinds WHERE id = $1 AND org_id = $2)` per plan kind, `SELECT EXISTS (SELECT 1 FROM activities WHERE id = $1 AND org_id = $2)` per parent, `SELECT EXISTS (SELECT 1 FROM contracts WHERE id = $1 AND org_id = $2)` per contract; any miss → return ticket.ErrInvalidRequest and roll back — this is the AUTHORITATIVE check per Pitfall 7, with no pool-level read and no TOCTOU window (the DB FK/CHECK constraints backstop as a third line); then UPDATE tickets SET kind/status='planned'/updated_at + INSERT INTO activities for each plan with ALL activity columns incl. created_by_org_id=orgID, is_active=true, origin columns + ticket_id + both audit rows; returns updated ticket + created activities — prefer RETURNING ids, fall back to in-tx Get per id); AddComment (in-tx: INSERT ticket_comments + audit row); ListComments (org-scoped via ticket join); ListHistory (`SELECT ... FROM audit_logs WHERE entity_type='ticket' AND entity_id=$1 AND org_id=$2 ORDER BY created_at` — scan into []audit.AuditLog with payload JSONB unmarshal); LoggedHours (exact query per D-13: `SELECT COALESCE(SUM(hours),0) FROM time_entries WHERE is_deleted = false AND status IN ('submitted','approved') AND activity_id IN (SELECT id FROM activities WHERE ticket_id = $1 AND origin_type = 'customer_ticket')` — stable signature for the Phase-12 computation swap); HasNonTerminalActivities (recursive CTE: `WITH RECURSIVE subtree AS (SELECT id FROM activities WHERE ticket_id = $1 AND origin_type = 'customer_ticket' UNION ALL SELECT a.id FROM activities a JOIN subtree s ON a.parent_id = s.id) SELECT EXISTS (SELECT 1 FROM time_entries te WHERE te.is_deleted = false AND te.status IN ('draft','submitted','pending_manager','pending_finance') AND te.activity_id IN (SELECT id FROM subtree))`).

    Tests: unit (ticket_test.go) — dismissal blocked when LoggedHours > 0 → ErrDismissalBlocked; dismissal succeeds with 0 hours and repo.Dismiss receives hours=0; triage pre-validation rejects unknown activity kind / cross-org parent / cross-org contract; comment gating. Integration (ticket_integration_test.go NEW + ticket_repository_test.go) — TestTicketTriage: seed org/users/kind, create ticket + a linked activity with a submitted time entry, assert triage tx atomicity (one activity plan failing pre-validation → no writes), assert activities carry origin customer_ticket + ticket_id, ticket status planned, two audit rows; TestDismissalGuard: linked activity with submitted entry (not deleted) → Dismiss → ErrDismissalBlocked; with only draft entries → dismissal allowed (raw Σ excludes draft); TestTicketAudit: comments/transitions produce rows in audit_logs, ListHistory returns them ordered, and no repo method updates or deletes existing audit rows (grep-level: no UPDATE/DELETE on audit_logs or ticket_comments in the repo).
  </action>
  <acceptance_criteria>
    - services/ticket/ticket.go contains Dismiss/Triage/AddComment/ListHistory with the D-11/D-15 gates (grep `ErrDismissalBlocked`, `LoggedHours`, `activities_created`)
    - ticket_repository.go contains the exact LoggedHours query (statuses submitted+approved, is_deleted=false, origin_type='customer_ticket' filter), the recursive HasNonTerminalActivities CTE, AND the in-tx plan validation (SELECT EXISTS on activity_kinds/activities/contracts inside the BeginTx block, mapping misses to ticket.ErrInvalidRequest — Pitfall 7, pattern_compliance W-6)
    - `go test ./internal/core/services/ticket/ ./internal/adapters/secondary/postgres/ -run 'TestDismissalGuard|TestTicketTriage|TestTicketAudit' -count=1` exits 0
    - grep across internal/adapters/secondary/postgres/ticket_repository.go shows no `UPDATE audit_logs` and no `DELETE FROM audit_logs`/`DELETE FROM ticket_comments`
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/core/services/ticket/ ./internal/adapters/secondary/postgres/ -run 'TestDismissalGuard|TestTicketTriage|TestTicketAudit' -count=1</automated>
  </verify>
  <done>Triage is atomic; dismissal guard + note work; comments and history are append-only and fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Ticket HTTP handler + route registration + API contract test</name>
  <files>internal/adapters/primary/http/ticket_handler.go (NEW), cmd/server/main.go, internal/adapters/primary/http/ticket_handler_test.go (NEW)</files>
  <read_first>
    - internal/adapters/primary/http/activity_handler.go (handler struct + sentinel-switch + DTO shape to mirror, ~357 lines)
    - internal/adapters/primary/http/time_entry.go (action-endpoint shape: POST /time-entries/{id}/submit)
    - cmd/server/main.go (route registration ~L184-190 + service construction ~L120-130, current state after 11-05)
    - internal/middleware/middleware.go (GetUserID/GetOrganizationID/GetRole)
    - pkg/api/response.go (RespondWithJSON/RespondWithError)
    - internal/core/services/ticket/ticket.go (service methods + request types from Tasks 1-2)
  </read_first>
  <action>
    Create `internal/adapters/primary/http/ticket_handler.go`: `TicketHandler{service *ticketsvc.Service}` + NewTicketHandler. DTOs with string IDs parsed at the boundary (activity_handler pattern): CreateTicketRequest{Title, Description, Kind string, AssigneeID string}; UpdateTicketRequest{Title *string, Description *string, AssigneeID *string}; TransitionRequest{Status string, Note *string}; TriageRequest{Kind *string, Activities []TriageActivityPlanDTO{Name, Kind, ParentID, ContractID, GovernanceModel string, Description string, Billable *bool, IsShared *bool}}; CommentRequest{Body string}. Methods: Create, List (optional status/kind query params validated), Get (returns ticket + comments), Update, Triage, Dismiss, Transition, AddComment, History. Every method: decode → uuid.Parse → middleware claims → service call → sentinel switch → pkg/api envelope. Sentinel map: ErrTicketNotFound → 404; ErrInvalidRequest/ErrInvalidTransition → 400; ErrForbidden → 403; ErrDismissalBlocked/ErrActivityNotTerminal → 409 Conflict (guard-style, mirrors ErrHasChildren → 409); default → 500.

    Register routes in `cmd/server/main.go` next to the time-entry action routes (~L203-211), all wrapped in middleware.Auth(authService, ...): `POST /tickets` (Create), `GET /tickets` (List), `GET /tickets/{id}` (Get), `PUT /tickets/{id}` (Update), `POST /tickets/{id}/triage` (Triage), `POST /tickets/{id}/dismiss` (Dismiss), `POST /tickets/{id}/transition` (Transition), `POST /tickets/{id}/comments` (AddComment), `GET /tickets/{id}/history` (History). Construct ticketService with the repos from 11-05 + activityRepo + contractRepo + orgRepo (already constructed).

    API contract test (`ticket_handler_test.go`, integration against the test container — mirror how existing handler tests drive the server or handler directly): TestTicketAPI — employee creates a ticket (201, status open); manager transitions open→triage→planned→in_progress→resolved with all linked activities terminal; reopen resolved→in_progress allowed; employee (non-owner) transition → 403; customer role create → 403; dismiss with logged hours → 409; dismiss with 0 hours → 200 + dismissed_hours 0 + history contains 'dismissed'; comments POST → 200 + 'comment_added' in history; GET /tickets/{id}/history returns ordered audit stream. Assert NO `DELETE /tickets` route exists in the mux (TICK-05 — no delete/update endpoints for history/comments: assert the mux only serves the 9 registered ticket routes).
  </action>
  <acceptance_criteria>
    - ticket_handler.go defines all 9 handler methods (grep `func (h \*TicketHandler)`)
    - main.go registers the 9 routes (grep `POST /tickets` — 5 POST routes: /tickets, /tickets/{id}/triage, /tickets/{id}/dismiss, /tickets/{id}/transition, /tickets/{id}/comments + 3 GET: /tickets, /tickets/{id}, /tickets/{id}/history + 1 PUT: /tickets/{id})
    - `go test ./internal/adapters/primary/http/ -run TestTicket -count=1` exits 0
    - `go build ./... && go test ./... -count=1` (or `make test`) exits 0 — full suite green
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run TestTicket -count=1</automated>
  </verify>
  <done>All ticket endpoints live, gated, and contract-tested; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ticket API | untrusted status/kind/comment payloads cross here (role gates + state machine must hold) |
| service → time_entries (read) | dismissal-guard Σ and terminal check read hours — server-side only (T-11-07) |
| service → audit_logs (in-tx writes) | the immutable event stream (T-11-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Elevation of Privilege | ticket Service.Create | mitigate | D-15 gate: any employee may create; customer role rejected with ErrForbidden; kind constrained to the closed set (TICK-01) |
| T-11-05 | Elevation of Privilege | ticket Service.Transition/Dismiss/Triage | mitigate | D-15 owner/assignee/manager+ gates on transition + update + comment; D-11 manager|finance gate on triage + dismiss; state machine edges enforced via domain CanTransition (D-14); actor captured in every audit row |
| T-11-06 | Elevation of Privilege | ticket Service.Triage activity plans | mitigate | Authoritative in-tx validation of every plan (kind in org catalog, parent + contract same-org via SELECT EXISTS against the open tx, mapped to ticket.ErrInvalidRequest) — Pitfall 7, no TOCTOU window; service pool-level reads are optional fast-fail only; DB FK/CHECK constraints backstop; activities inserted with origin customer_ticket inside the same tx |
| T-11-07 | Tampering | ticket Service.Dismiss | mitigate | Hours Σ computed server-side from time_entries (statuses submitted+approved, is_deleted=false, linked via ticket_id) — client never supplies the number; guard blocks Σ > 0 (D-13) |
| T-11-08 | Repudiation | ticket transitions/comments/audit | mitigate | Audit rows written in the SAME tx as the state change (Pitfall 2, ADR-BE-016); no UPDATE/DELETE paths on audit_logs or ticket_comments (TICK-05); history endpoint is read-only |
| T-11-09 | Tampering | all new SQL | mitigate | Parameterized pgx queries only (house ADR-BE-003); recursive CTE + SUM queries use bind params |
| T-11-SC | Tampering | package installs | accept | No package-manager installs this phase — Go-only on the existing dependency set |

**Remaining register (T-11-01..T-11-03, T-11-10, T-11-11) is enforced by plans 01/03/04/05.**
</threat_model>

<verification>
- Per task: the task's automated commands
- Per wave: `make test` (full suite) + `go vet ./...`
- Phase gate: `make test` green before SUMMARY handoff
</verification>

<success_criteria>
- TICK-01: any-employee create with closed-set kinds; TICK-02: full lifecycle + reopen + resolved-block; TICK-03: atomic triage with validated 1..N activities; TICK-04: dismissal guard with note; TICK-05: append-only history with in-tx audit writes — all endpoint-level and unit-tested
</success_criteria>

## Artifacts this phase produces

- `internal/core/ports/ticket_repository.go` — EXTENDED to 12 methods: Get (from 11-05), Create, ListByOrg, UpdateDetails, UpdateState, Dismiss, Triage, AddComment, ListComments, ListHistory, LoggedHours, HasNonTerminalActivities
- `internal/adapters/secondary/postgres/ticket_repository.go` — EXTENDED: tx-backed mutators (BeginTx + audit INSERT in same tx), ListByOrg (status/kind filters), ListComments, ListHistory (payload JSONB unmarshal), `LoggedHours(ctx, ticketID) (float64, error)` (Phase-12-stable), `HasNonTerminalActivities(ctx, ticketID) (bool, error)` (recursive CTE)
- `internal/core/services/ticket/ticket.go` — NEW package `ticket` (service): struct `Service{repo, activityRepo, contractRepo, orgRepo}`; `NewService(ticketRepo, activityRepo, contractRepo, orgRepo)`; request types `CreateTicketRequest{Title, Description, Kind, AssigneeID}`, `UpdateTicketRequest`, `TriageActivityPlan`, `TriageRequest{Kind *string, Plans}`; methods `Create`, `List`, `Get`, `UpdateDetails`, `Transition`, `Dismiss`, `Triage`, `AddComment`, `ListHistory`
- `internal/adapters/primary/http/ticket_handler.go` — NEW: `TicketHandler{service}` + `NewTicketHandler`; DTOs CreateTicketRequest/UpdateTicketRequest/TransitionRequest/TriageRequest/CommentRequest; methods Create/List/Get/Update/Triage/Dismiss/Transition/AddComment/History; 409 mapping for ErrDismissalBlocked/ErrActivityNotTerminal
- `cmd/server/main.go` — MODIFIED: 9 routes under /tickets + ticketService construction
- Tests: `ticket_test.go` (TestTicketCreate, TestTicketLifecycle, TestDismissalGuard, TestTicketAudit, triage pre-validation), `ticket_integration_test.go` (TestTicketTriage, TestDismissalGuard, TestTicketAudit), `ticket_repository_test.go` (round-trips, audit counts, tx assertions), `ticket_handler_test.go` (TestTicketAPI — full API contract incl. route-absence assertion), `testdata/mock_ticket_repo.go` extended

<output>
Create `.planning/phases/11-foundations-schema-origins-tickets-backend/11-06-SUMMARY.md` when done
</output>
