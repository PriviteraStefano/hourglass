---
phase: 11-foundations-schema-origins-tickets-backend
plan: 08
type: execute
wave: 2
depends_on: [11-07]
files_modified:
  - internal/core/domain/ticket/ticket.go
  - internal/adapters/secondary/postgres/ticket_repository.go
  - internal/adapters/secondary/postgres/ticket_repository_test.go
  - internal/adapters/primary/http/ticket_handler_test.go
  - internal/core/services/ticket/ticket.go
  - internal/core/services/ticket/ticket_test.go
autonomous: false
gap_closure: true
requirements: [TICK-04]
estimate:
  tokens: 45000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TICK-04 (note rendered): every read of a dismissed ticket — GET /tickets, GET /tickets/{id}, and the POST /tickets/{id}/dismiss response — carries dismissed_note = 'dismissed with N h logged' where N is the dismissed_hours value (server-derived, IN-02 closed)"
    - "WR-04: a ticket title longer than 255 chars (the VARCHAR(255) column limit) is rejected with ErrInvalidRequest → 400 on create and update — never a 500"
    - "IN-01: an update that sets an empty title is rejected with ErrInvalidRequest → 400 — a ticket can no longer be renamed to ''"
  artifacts:
    - internal/core/domain/ticket/ticket.go (DismissedNote derived field)
    - internal/adapters/secondary/postgres/ticket_repository.go (scanTicketRow populates the note)
    - internal/core/services/ticket/ticket.go (title length/emptiness validation)
  key_links:
    - "scanTicketRow → every JSON ticket response (repo Get/List/returned-ticket paths all scan through scanTicketRow)"
    - "Service.Create/UpdateDetails validation → ErrInvalidRequest → handler 400 mapping (already wired)"
---

<objective>
Close the IN-02 gap (11-VERIFICATION.md gap 2 secondary): the "dismissed with N h logged" note is rendered server-side as a derived field on every ticket read, so TICK-04's note claim (D-13: the note carries the raw Σ number) is true at the API boundary — no longer only in doc comments. Two ticket-input quality warnings ride along in the same subsystem (per the gap-closure brief, low-priority items that fit naturally): WR-04 (title > 255 chars currently surfaces as a 500 from the VARCHAR(255) column) and IN-01 (UpdateDetails accepts an empty title). No scope expansion beyond the ticket subsystem.

Purpose: The phase's own acceptance text and ADR-P-003 promise a visible dismissal note; the reviewer (11-REVIEW.md) flagged it as a claim only comments make. Rendering it at read time makes TICK-04 observable end-to-end.

Output: `DismissedNote` derived field on the Ticket domain populated by scanTicketRow; title validation in the ticket service; tests at repo + handler-contract + service level.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-VERIFICATION.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-06-SUMMARY.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-REVIEW.md

@internal/core/domain/ticket/ticket.go
@internal/adapters/secondary/postgres/ticket_repository.go
@internal/adapters/primary/http/ticket_handler.go
@internal/adapters/primary/http/ticket_handler_test.go
@internal/core/services/ticket/ticket.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 'dismissed with N h logged' note rendered on every read — derived field + scan wiring + contract assertion</name>
  <files>internal/core/domain/ticket/ticket.go, internal/adapters/secondary/postgres/ticket_repository.go, internal/adapters/secondary/postgres/ticket_repository_test.go, internal/adapters/primary/http/ticket_handler_test.go</files>
  <read_first>
    - internal/core/domain/ticket/ticket.go — the `Ticket` struct (lines 15-27): `Status string` + `DismissedHours *float64` already exist; the new `DismissedNote` field joins here
    - internal/adapters/secondary/postgres/ticket_repository.go — `scanTicketRow` (lines 42-54) is the single funnel every ticket read passes through; `ticketColumns` (lines 38-39) must NOT change (the note is derived, not a column)
    - internal/adapters/secondary/postgres/ticket_repository_test.go — `TestTicketRepository_Get` (lines 21-57) is the template for the note read-back test (raw INSERT + repo.Get)
    - internal/adapters/primary/http/ticket_handler_test.go — the TestTicketAPI contract: locate the dismiss-0-hours scenario (dismiss returns 200 with dismissed_hours) to extend with the note assertion
  </read_first>
  <behavior>
    - Test 1 (repo): a dismissed ticket with dismissed_hours = 5 reads back with DismissedNote == "dismissed with 5 h logged"; a non-dismissed ticket and a dismissed ticket with NULL dismissed_hours read back with DismissedNote == nil.
    - Test 2 (handler contract): after POST /tickets/{id}/dismiss (0 hours), the response body and the subsequent GET /tickets/{id} body both contain "dismissed_note":"dismissed with 0 h logged".
  </behavior>
  <action>
    1. In internal/core/domain/ticket/ticket.go, add to the Ticket struct: `DismissedNote *string \`json:"dismissed_note,omitempty"\`` with a comment stating it is DERIVED on read (status == 'dismissed' and DismissedHours != nil) and never persisted (no column, no migration — the note renders from the existing dismissed_hours column per OQ3/A4 and D-13).
    2. In internal/adapters/secondary/postgres/ticket_repository.go `scanTicketRow` (after the existing scan + nil normalization): if t.Status == ticketdomain.StatusDismissed && dismissedHours != nil, set the note via `fmt.Sprintf("dismissed with %s h logged", strconv.FormatFloat(*dismissedHours, 'f', -1, 64))` (FormatFloat with precision -1 trims trailing zeros: 5.00 → "5", 7.50 → "7.5"). Import strconv. Do NOT touch ticketColumns — the note is derived at scan time, never selected.
    3. Repo test: add TestTicketRepository_DismissedNote in ticket_repository_test.go — seed a dismissed ticket with dismissed_hours = 5 (raw INSERT, mirroring TestTicketRepository_Get's shape) → repo.Get → assert DismissedNote == "dismissed with 5 h logged"; seed a dismissed ticket with dismissed_hours NULL → DismissedNote nil; seed a 'planned' ticket → DismissedNote nil.
    4. Handler contract: extend the TestTicketAPI dismiss scenario in ticket_handler_test.go — assert the dismiss response body and a follow-up GET /tickets/{id} both carry `"dismissed_note":"dismissed with 0 h logged"`.
    5. The service is untouched by this task — the note rides the repo's returned ticket into the handler response automatically (Dismiss and Get both end in r.Get → scanTicketRow).
  </action>
  <verify>
    <automated>
      go test ./internal/adapters/secondary/postgres/ -run 'TestTicketRepository_DismissedNote' -count=1 && go test ./internal/adapters/primary/http/ -run TestTicket -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `internal/core/domain/ticket/ticket.go` Ticket struct has `DismissedNote *string` tagged `json:"dismissed_note,omitempty"` with a comment stating it is derived, never persisted
    - `scanTicketRow` sets DismissedNote only when Status == 'dismissed' and DismissedHours != nil, formatted exactly "dismissed with {N} h logged" with N trimmed of trailing zeros; `ticketColumns` unchanged
    - `go test ./internal/adapters/secondary/postgres/ -run 'TestTicketRepository_DismissedNote' -count=1` exits 0
    - `go test ./internal/adapters/primary/http/ -run TestTicket -count=1` exits 0, and the dismiss scenario asserts the note in both the dismiss response and the follow-up GET
    - `go test ./internal/adapters/secondary/postgres/ -run TestTicket -count=1` exits 0 (no regressions)
  </acceptance_criteria>
  <done>A dismissed ticket's JSON carries the note on every read path, and the API contract test pins it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Title input validation rides along — 256-char titles rejected (WR-04), empty-title updates rejected (IN-01)</name>
  <files>internal/core/services/ticket/ticket.go, internal/core/services/ticket/ticket_test.go</files>
  <read_first>
    - internal/core/services/ticket/ticket.go — `Create` (lines 78-112, the existing `req.Title == ""` check at line 82) and `UpdateDetails` (lines 150-183, the `title != nil` branch that currently accepts any string)
    - internal/core/services/ticket/ticket_test.go — TestTicketCreate (line 75) and TestTicketUpdateDetails (line 300): the unit-test shape (mock repo) to extend
    - migrations/014_ticket_schema.up.sql — the `title VARCHAR(255) NOT NULL` column the validation mirrors (oversized titles currently 500 from the column)
  </read_first>
  <behavior>
    - Test 1: Create with a 256-char title → ErrInvalidRequest; Create with a 255-char title → success (mock repo receives the ticket).
    - Test 2: UpdateDetails with title pointer to "" → ErrInvalidRequest; UpdateDetails with a 256-char title → ErrInvalidRequest; UpdateDetails with a 255-char title → success (mock receives the update).
  </behavior>
  <action>
    Write the failing unit tests in ticket_test.go first (256-char strings via strings.Repeat("a", 256); confirm they pass through today — they fail to assert ErrInvalidRequest on the current code), then add validation in services/ticket/ticket.go:
    1. In `Create`, extend the existing title check (line 82) to `req.Title == "" || len(req.Title) > 255` → ticketdomain.ErrInvalidRequest (255 = the VARCHAR(255) column limit in migration 014 — a longer title would otherwise surface as a 500 from the DB).
    2. In `UpdateDetails`, in the `title != nil` branch: `*title == "" || len(*title) > 255` → ticketdomain.ErrInvalidRequest (closes both WR-04 and IN-01; the payload map and repo call must not execute for rejected titles).
    3. No handler change needed — ErrInvalidRequest already maps to 400 in TicketHandler.writeError.
    Keep the change to these two validations only; do not touch description (TEXT, unlimited) or any other field.
  </action>
  <verify>
    <automated>
      go test ./internal/core/services/ticket/ -run 'TestTicketCreate|TestTicketUpdateDetails' -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - services/ticket/ticket.go `Create` rejects `len(req.Title) > 255` with ticketdomain.ErrInvalidRequest; `UpdateDetails` rejects `*title == ""` and `len(*title) > 255` with ticketdomain.ErrInvalidRequest
    - `go test ./internal/core/services/ticket/ -run 'TestTicketCreate|TestTicketUpdateDetails' -count=1` exits 0
    - `go test ./internal/core/services/ticket/ -count=1` exits 0 (no regressions)
    - `go test ./internal/adapters/primary/http/ -run TestTicket -count=1` exits 0 (contract unaffected)
  </acceptance_criteria>
  <done>Oversized and empty titles are rejected with 400 at the service boundary; no 500 path remains for title input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: confirm the dismissal note renders server-side on live API reads (VERIFICATION.md human item 2)</name>
  <what-built>Server-derived dismissal note (IN-02): the Ticket read model now carries dismissed_note "dismissed with N h logged" wherever a dismissed ticket is returned; plus title-length/emptiness validation (WR-04/IN-01 ride-alongs).</what-built>
  <how-to-verify>
    Per 11-VERIFICATION.md "Human Verification Required" item 2 — the note-rendering decision, now implemented as server-derived:
    1. Start Postgres (`docker compose up -d`) and the backend (`go run ./cmd/server`).
    2. As a manager: create a ticket (POST /tickets with kind 'bug'), transition it to 'triage', then dismiss it (POST /tickets/{id}/dismiss) — expect 200.
    3. Read it back: GET /tickets/{id} (and GET /tickets) — expect the JSON to carry "dismissed_note": "dismissed with 0 h logged" alongside "dismissed_hours": 0.
    4. Sanity: POST /tickets with a 300-char title → expect 400 invalid request (not 500).
    Expected: the note is visible to the user on the API — the TICK-04 claim holds server-side; oversized titles never 500.
  </how-to-verify>
  <resume-signal>Type "approved" if the note renders on read and oversized titles answer 400, or describe what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ticket API | untrusted title/description strings cross here (length validation before persistence) |
| repo scan → JSON response | derived note rendering (read-only, no trust impact) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-16 | Denial of Service | ticket Service.Create/UpdateDetails | medium | mitigate | Reject `len(title) > 255` with ErrInvalidRequest before any SQL (WR-04) — eliminates the oversized-input 500 path and cap-length abuse of the VARCHAR(255) column |
| T-11-17 | Spoofing | ticket Service.UpdateDetails | low | mitigate | Reject empty-title updates with ErrInvalidRequest (IN-01) — tickets can no longer be renamed to '' (integrity of the read model) |
| T-11-SC | Tampering | package installs | low | accept | No package-manager installs in this plan — Go-only on the existing dependency set |

**Remaining register (T-11-01..T-11-15) enforced by plans 01-07; this register covers the ride-along delta only.**
</threat_model>

<verification>
- Per task: the task's automated commands
- Per plan: `go test ./internal/adapters/secondary/postgres/ -run TestTicket -count=1` + `go test ./internal/adapters/primary/http/ -run TestTicket -count=1` + `go test ./internal/core/services/ticket/ -count=1` + `go build ./...` all green
- Human gate: the checkpoint:human-verify above (blocking)
</verification>

<success_criteria>
- VERIFICATION.md gap 2 fully closes: TICK-04's note is server-rendered (IN-02) on top of the concurrency-safe guard from plan 11-07
- WR-04 and IN-01 ride-alongs landed within the ticket subsystem only; no other subsystem touched
- Full phase suite (`make test`) green
</success_criteria>

## Artifacts this phase produces

- `internal/core/domain/ticket/ticket.go` — MODIFIED: `Ticket` struct gains `DismissedNote *string` tagged `json:"dismissed_note,omitempty"` (derived on read, never persisted — no column, no migration)
- `internal/adapters/secondary/postgres/ticket_repository.go` — MODIFIED: `scanTicketRow` populates DismissedNote as "dismissed with {N} h logged" (strconv.FormatFloat, precision -1) when Status == 'dismissed' && DismissedHours != nil; `ticketColumns` unchanged
- `internal/adapters/secondary/postgres/ticket_repository_test.go` — NEW: `TestTicketRepository_DismissedNote` (note present with 5h, nil when dismissed with NULL hours, nil when non-dismissed)
- `internal/adapters/primary/http/ticket_handler_test.go` — MODIFIED: TestTicketAPI dismiss scenario asserts `"dismissed_note":"dismissed with 0 h logged"` in the dismiss response and the follow-up GET
- `internal/core/services/ticket/ticket.go` — MODIFIED: `Create` rejects `len(title) > 255`; `UpdateDetails` rejects `*title == ""` and `len(*title) > 255` — all via ticketdomain.ErrInvalidRequest
- `internal/core/services/ticket/ticket_test.go` — MODIFIED: TestTicketCreate/TestTicketUpdateDetails extended with 255/256-char and empty-title cases
