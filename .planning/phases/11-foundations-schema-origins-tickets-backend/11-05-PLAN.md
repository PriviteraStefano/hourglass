---
phase: 11-foundations-schema-origins-tickets-backend
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - internal/core/domain/activity/activity.go
  - internal/core/services/activity/activity.go
  - internal/adapters/secondary/postgres/activity_repository.go
  - internal/adapters/primary/http/activity_handler.go
  - internal/core/domain/ticket/ticket.go
  - internal/core/domain/audit/audit.go
  - internal/core/ports/ticket_repository.go
  - internal/core/ports/audit_log_repository.go
  - internal/adapters/secondary/postgres/ticket_repository.go
  - internal/adapters/secondary/postgres/audit_log_repository.go
  - cmd/server/main.go
  - internal/core/services/testdata/mock_ticket_repo.go
  - internal/core/services/testdata/mock_audit_log_repo.go
  - internal/core/services/activity/activity_test.go
  - internal/adapters/secondary/postgres/activity_repository_test.go
  - internal/adapters/secondary/postgres/ticket_repository_test.go
autonomous: true
requirements: [FND-01, FND-02, FND-04, TICK-01, TICK-05]
must_haves:
  truths:
    - "An activity created via POST /activities carries an origin (type + reference set) matching D-01; refs are set once and immutable (FND-01, D-03)"
    - "Origin creation is role-gated per type: manager_assignment and customer_ticket need manager/finance; employee_proposal requires proposed_by = self (D-04)"
    - "Employee proposals are created is_active=false and can be approved by the routing-resolved approver, flipping is_active and writing an audit row (FND-02, D-12)"
    - "Origin refs are readable from activity responses (stored directly, FND-04); the Phase-13 fallback is documented, not built"
    - "The ticket domain (entity, kind/status vocabulary, transition matrix) and the general audit_logs repository exist for the ticket service plan"
    - "Ticket audit rows are written synchronously (never fire-and-forget) — the TICK-05 guarantee is real"
  artifacts:
    - path: "internal/core/domain/activity/activity.go"
      provides: "OriginType + 5 ref fields on Activity/ActivityResponse/requests"
      contains: "OriginType"
    - path: "internal/core/services/activity/activity.go"
      provides: "Create role gates + same-org validation + immutability guard + ApproveProposal"
      contains: "ErrOriginImmutable"
    - path: "internal/core/domain/ticket/ticket.go"
      provides: "Ticket entity, kind/status constants, transition matrix, sentinels"
      contains: "StatusDismissed"
    - path: "internal/core/domain/audit/audit.go"
      provides: "general AuditLog entity"
      contains: "EntityType"
    - path: "internal/adapters/secondary/postgres/audit_log_repository.go"
      provides: "Create (synchronous, parameterized)"
      contains: "INSERT INTO audit_logs"
  key_links:
    - from: "services/activity/activity.go"
      to: "services/routing"
      via: "ApproveProposal resolves approvers via routing.ResolveManagerStage"
      pattern: "ResolveManagerStage"
    - from: "services/activity/activity.go"
      to: "postgres/ticket_repository.go"
      via: "customer_ticket origin validation calls ticketRepo.Get (same-org)"
      pattern: "ticketRepo\\.Get"
---

<objective>
The origin axis lands (FND-01/02/04) and the ticket/audit foundation is laid: activity origin fields + role gates + same-org validation + immutability, employee proposal approval via the shared routing, the ticket domain (entity + vocabulary + transition matrix), and the general audit_logs repository with synchronous writes.

Purpose: Per D-D, activities carry an origin set once at creation. Per D-12, proposals are `is_active=false` activities whose approval routes via BE-014 and lands in audit — never in origin refs. This plan creates the ticket domain + audit infrastructure that plan 06 (ticket lifecycle service) builds on, and resolves the semantic decisions pinned in ADR-P-013/ADR-BE-016 (reviewed_by NULL at creation, fast path allowed, in-transaction audit writes).
Output: origin-capable activity domain/service/repo/handler, ticket + audit domain packages, base ticket repository, audit_logs repository, approve-proposal endpoint, wiring.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-CONTEXT.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-RESEARCH.md
@.planning/phases/11-foundations-schema-origins-tickets-backend/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ticket + audit domain foundation, base ticket repo, audit_logs repo</name>
  <files>internal/core/domain/ticket/ticket.go (NEW), internal/core/domain/audit/audit.go (NEW), internal/core/ports/ticket_repository.go (NEW), internal/core/ports/audit_log_repository.go (NEW), internal/adapters/secondary/postgres/ticket_repository.go (NEW), internal/adapters/secondary/postgres/audit_log_repository.go (NEW), internal/core/services/testdata/mock_ticket_repo.go (NEW), internal/core/services/testdata/mock_audit_log_repo.go (NEW), internal/adapters/secondary/postgres/ticket_repository_test.go (NEW)</files>
  <read_first>
    - internal/core/domain/time_entry/time_entry.go (full file — the entity + constants + sentinel shape to mirror for ticket.go, ~100 lines)
    - internal/adapters/secondary/postgres/time_entry_repository.go L303-343 (AuditLogRepository Create precedent — but note the general table shape below)
    - internal/adapters/secondary/postgres/activity_repository.go (repo struct/compile-time assertion ~L19-28, scan pattern L594-614)
    - migrations/014_ticket_schema.up.sql + migrations/017_audit_logs.up.sql (exact column names — created in 11-01, wave 1)
    - internal/core/services/testdata/ (existing mock files — shape to copy for the two new mocks)
  </read_first>
  <action>
    `internal/core/domain/ticket/ticket.go` (NEW package `ticket`): entity `Ticket` with fields ID, OrgID, Title, Description, Kind, Status (strings), RequesterID uuid.UUID, AssigneeID *uuid.UUID, DismissedHours *float64, CreatedAt/UpdatedAt time.Time (json tags matching the migration columns). Constants: KindQuestion/KindBug/KindChange/KindEvolution = "question"/"bug"/"change"/"evolution"; StatusOpen/StatusTriage/StatusPlanned/StatusInProgress/StatusResolved/StatusClosed/StatusDismissed. `TicketComment` struct (ID, TicketID, AuthorID, Body, CreatedAt). Sentinels (errors.New, house style): ErrTicketNotFound, ErrInvalidRequest, ErrForbidden, ErrInvalidTransition, ErrDismissalBlocked, ErrActivityNotTerminal. Domain predicates + transition matrix per A7 (locked): `CanTransition(from, to string) bool` — edges: open→triage; triage→planned; triage→dismissed; planned→in_progress; in_progress→resolved; resolved→closed; resolved→in_progress (reopen); open→dismissed (superset of A7, pinned in ADR-BE-016); nothing else. `IsTerminalStatus(s)` — closed + dismissed true. `(t *Ticket) IsOwner(userID)`, `(t *Ticket) IsAssignee(userID)` predicates. Map sentinel errors to JSON names: ErrNotFound etc.

    `internal/core/domain/audit/audit.go` (NEW package `audit`): `AuditLog` struct: ID uuid.UUID, OrgID uuid.UUID, EntityType string, EntityID uuid.UUID, Action string, ActorID *uuid.UUID, Comment string, Payload map[string]any, CreatedAt time.Time (json tags: entity_type, entity_id, actor_id omitempty, payload omitempty). Mirror the existing time_entry.AuditLog json style (time_entry.go L77-88) but generalized per D-05.

    `internal/core/ports/ticket_repository.go` (NEW): minimal interface for THIS plan — `TicketRepository { Get(ctx context.Context, orgID, ticketID uuid.UUID) (*ticket.Ticket, error) }`. Plan 06 extends this interface with the lifecycle methods (state mutators, triage, comments, history, LoggedHours, HasNonTerminalActivities) — do NOT add stubs now.

    `internal/core/ports/audit_log_repository.go` (NEW): `AuditLogRepository { Create(ctx context.Context, log *audit.AuditLog) error }` — synchronous (caller awaits; error propagates), NOT the fire-and-forget goroutine pattern (T-11-08, Pitfall 2; entry-side behavior untouched).

    `internal/adapters/secondary/postgres/ticket_repository.go` (NEW): struct + `var _ ports.TicketRepository = (*TicketRepository)(nil)` + NewTicketRepository(pool). Get: `SELECT id, org_id, title, description, kind, status, requester_id, assignee_id, dismissed_hours, created_at, updated_at FROM tickets WHERE id = $1 AND org_id = $2`; pgx.ErrNoRows → ticket.ErrTicketNotFound; scan with nullable locals for assignee_id + dismissed_hours (activity_repository scan pattern); wrapPGError for other errors.

    `internal/adapters/secondary/postgres/audit_log_repository.go` (NEW): NewAuditLogRepository(pool); Create marshals log.Payload to JSON (`json.Marshal`, error → fmt.Errorf wrap), generates `id := uuid.New()`, `INSERT INTO audit_logs (id, org_id, entity_type, entity_id, action, actor_id, comment, payload, created_at) VALUES ($1..$9)` with actor_id/comment as nullable params, wrapPGError.

    `internal/core/services/testdata/mock_ticket_repo.go` + `mock_audit_log_repo.go` (NEW): copy an existing mock's shape (e.g. MockActivityRepo); MockTicketRepo.Get returns configured ticket/error; MockAuditLogRepo.Create records calls.

    `internal/adapters/secondary/postgres/ticket_repository_test.go` (NEW): integration — seed org + users, insert a ticket row via SQL, assert Get returns it with nullable fields handled (assignee NULL); Get with wrong org → ErrTicketNotFound; audit repo Create round-trips (insert + SELECT payload JSONB back, assert Payload map equality) — matches VALIDATION.md task 11-05-01 test map (TestTicketAudit foundation).
  </action>
  <acceptance_criteria>
    - ticket.go contains all seven status constants + CanTransition with the pinned edges (open→dismissed present) + IsTerminalStatus
    - audit.go contains AuditLog with EntityType/EntityID/Action/Payload fields
    - audit_log_repository.go Create is synchronous (no `go func` / context.Background() detached write) and inserts into audit_logs
    - testdata contains MockTicketRepo + MockAuditLogRepo
    - `go test ./internal/adapters/secondary/postgres/ -run TestTicket -count=1` exits 0 (Get + audit round-trip)
    - `go test ./internal/core/services/activity/ -count=1` still exits 0 (mocks compile into the fixture)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestTicket' -count=1 && go build ./internal/...</automated>
  </verify>
  <done>Ticket domain (vocabulary + matrix) and general audit infrastructure exist; base ticket repo + audit repo tested.</done>
</task>

<task type="auto">
  <name>Task 2: Origin fields end-to-end (domain, repo, service, handler) with role gates + immutability</name>
  <files>internal/core/domain/activity/activity.go, internal/adapters/secondary/postgres/activity_repository.go, internal/core/services/activity/activity.go, internal/adapters/primary/http/activity_handler.go, internal/core/services/activity/activity_test.go, internal/adapters/secondary/postgres/activity_repository_test.go</files>
  <read_first>
    - internal/core/domain/activity/activity.go (Activity struct ~L41-57, CreateActivityRequest ~L96-106, UpdateActivityRequest, ActivityResponse, existing sentinels ~L18-31)
    - internal/adapters/secondary/postgres/activity_repository.go (baseActivityQuery ~L32-45, Create INSERT ~L121-135 with hardcoded `true` for is_active, scanActivity/scanActivityResponse ~L594-638 with nullable *uuid.UUID locals, wrapPGError)
    - internal/core/services/activity/activity.go (Create ~L64-90, Update ~L94-111 with finance gate + owner check, DI struct ~L25-33)
    - internal/adapters/primary/http/activity_handler.go (CreateActivityRequest DTO ~L36-46, Create ~L98-164 uuid.Parse pattern, Update DTO + handler)
    - internal/core/ports/organization_repository.go (GetMembership — the same-org user check, D-02)
    - internal/models/models.go (RoleManager, RoleFinance, RoleEmployee constants)
    - internal/core/domain/ticket/ticket.go (StatusOpen/StatusTriage constants — created in Task 1)
    - internal/core/ports/ticket_repository.go + internal/core/services/testdata/mock_ticket_repo.go + mock_audit_log_repo.go (created in Task 1)
    - migrations/015_activity_origins.up.sql (column names the SQL must match)
    - internal/core/services/activity/activity_test.go + internal/adapters/secondary/postgres/activity_repository_test.go (fixture + seed patterns to extend)
  </read_first>
  <action>
    Domain (`activity.go`): add to `Activity`, `ActivityResponse`, `CreateActivityRequest`, `UpdateActivityRequest`: `OriginType *string` json `origin_type,omitempty`, `AssignedBy/AssignedTo/ProposedBy/ReviewedBy/TicketID *uuid.UUID` json `assigned_by,omitempty` etc. Add `IsActive *bool` json `is_active,omitempty` to CreateActivityRequest (the repo currently hardcodes is_active=true; proposals need false per D-12). Add sentinel `ErrOriginImmutable = errors.New("origin refs are immutable after creation")` (D-03). Add constants `OriginTypeManagerAssignment = "manager_assignment"`, `OriginTypeEmployeeProposal = "employee_proposal"`, `OriginTypeCustomerTicket = "customer_ticket"` mirroring the DB CHECK.

    Repository: extend `baseActivityQuery` SELECT + `scanActivity`/`scanActivityResponse` with the six origin columns as nullable locals (`var ot *string; var ab, at, pb, rb, tid *uuid.UUID`). Extend Create INSERT with the origin columns + `is_active` reading `COALESCE(req.IsActive, true)` (nil → true, legacy behavior unchanged).

    Service (`activity.go`): extend the Service struct + NewService with `orgRepo ports.OrganizationRepository`, `ticketRepo ports.TicketRepository`, `auditRepo ports.AuditLogRepository`, `routing *routing.Service` (append params — do not drop unitRepo). Change Create signature to `Create(ctx, role string, orgID, userID uuid.UUID, req *activitydomain.CreateActivityRequest)`. Keep the existing base validations; add origin validation when `req.OriginType != nil`:
    - `manager_assignment` (T-11-01): role ∈ {models.RoleManager, models.RoleFinance} else ErrForbidden; assigned_by + assigned_to required else ErrInvalidRequest; both must be org members — `orgRepo.GetMembership(ctx, user, orgID)` must return a membership (nil/error → ErrInvalidRequest "origin ref must be an org member") (D-02).
    - `employee_proposal` (T-11-01): proposed_by required else ErrInvalidRequest; `*req.ProposedBy != userID` → ErrForbidden (spoofing guard, D-04); force `req.IsActive = ptr(false)` (D-12); also reject `req.IsActive` explicitly false WITHOUT employee_proposal origin → ErrInvalidRequest.
    - `customer_ticket` (T-11-06): role ∈ {RoleManager, RoleFinance} else ErrForbidden; ticket_id required else ErrInvalidRequest; `ticketRepo.Get(ctx, orgID, *req.TicketID)` must succeed else ErrInvalidRequest ("ticket not found or not in this org") (D-02 same-org); after Get succeeds, apply the ticket-state precondition (OQ5 fast path, ADR-BE-016): if `t.Status` is neither `ticket.StatusOpen` nor `ticket.StatusTriage` (constants from Task 1's ticket.go) → ErrInvalidRequest ("customer_ticket origin requires the ticket to be open or triage") — blocks linking activities to planned/in_progress/resolved/closed/dismissed tickets.
    - `reviewed_by` non-nil on ANY create → ErrInvalidRequest (Phase 11 resolution: reviewed_by stays NULL; the approver lives in the audit row — ADR-P-013).
    - unknown origin_type string → ErrInvalidRequest (closed set).
    Update: after the existing finance gate + owner check, add the D-03 immutability guard (T-11-10): if any of req.OriginType/AssignedBy/AssignedTo/ProposedBy/ReviewedBy/TicketID is non-nil → return activitydomain.ErrOriginImmutable.

    Handler (`activity_handler.go`): CreateActivityRequest DTO gains `OriginType string` + the five refs as `string` + `IsActive *bool`; Create parses UUIDs (`uuid.Parse` → nil on empty string), reads `role := middleware.GetRole(r.Context())`, `userID := middleware.GetUserID(r.Context())`, passes to the service. UpdateActivityRequest DTO gains the six origin fields as strings (parsed to pointers) so the service immutability guard can reject them. Map ErrOriginImmutable → 409 Conflict, ErrForbidden → 403, ErrInvalidRequest → 400 (mirror the existing sentinel switch).

    Tests: activity service unit tests (testdata mocks from Task 1 — MockTicketRepo with Get, MockAuditLogRepo with Create; org membership via MockOrgRepo or the existing mock): role gates per origin type; proposed_by != actor → ErrForbidden; customer_ticket with unknown ticket → ErrInvalidRequest; customer_ticket on a ticket whose Status is "planned" or "dismissed" → ErrInvalidRequest (state precondition per OQ5/ADR-BE-016); customer_ticket on an open or triage ticket → passes the state check (role + same-org gates still apply); Update carrying origin fields → ErrOriginImmutable; proposal create forces is_active=false. Repository integration tests: create manager-assignment activity, read back origin fields; update name → origin unchanged (immutability through the repo).
  </action>
  <acceptance_criteria>
    - activity.go contains `ErrOriginImmutable` and the three OriginType constants
    - activity_repository.go INSERT uses COALESCE(req.IsActive, true) semantics for is_active
    - services/activity/activity.go Create has the role + userID params and gates per origin type (grep `OriginTypeManagerAssignment`, `ErrOriginImmutable`, `GetMembership`)
    - customer_ticket origin on a planned/dismissed ticket → ErrInvalidRequest (ticket-state precondition after ticketRepo.Get, per OQ5/ADR-BE-016; unit test asserts it)
    - `go test ./internal/core/services/activity/ ./internal/adapters/secondary/postgres/ -run TestActivityOrigin -count=1` exits 0 (new test funcs prefixed TestActivityOrigin)
    - `go build ./internal/...` succeeds (interface/DI changes compile; full `go build ./...` incl. cmd/server is NOT expected until Task 3 lands the main.go wiring — the NewService signature change breaks the main.go call site until then, task_completeness W-5)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/core/services/activity/ ./internal/adapters/secondary/postgres/ -run TestActivityOrigin -count=1</automated>
  </verify>
  <done>Origins are settable once with role gates + same-org validation; the customer_ticket gate rejects tickets not in open|triage state with ErrInvalidRequest; updates carrying origin fields are rejected with ErrOriginImmutable; refs readable via GET /activities.</done>
</task>

<task type="auto">
  <name>Task 3: Proposal approval (ApproveProposal) + main.go wiring</name>
  <files>internal/core/services/activity/activity.go, internal/adapters/primary/http/activity_handler.go, cmd/server/main.go, internal/core/services/activity/activity_test.go</files>
  <read_first>
    - internal/core/services/activity/activity.go (current state after Task 2 — DI, Create, Update)
    - internal/core/services/routing/routing.go (ResolveManagerStage signature + managerResolution fields, created in 11-03)
    - internal/core/ports/unit_repository.go (ListMembershipsForUser — proposer's primary unit lookup)
    - internal/core/domain/unit/unit.go (UnitMember.IsPrimary field name)
    - cmd/server/main.go (service wiring ~L120-130, activity routes ~L184-190, wgRepo/unitRepo/activityRepo instances available for routing.NewService)
    - internal/adapters/primary/http/activity_handler.go (current state after Task 2 — handler struct + sentinel switch)
    - internal/core/services/activity/activity_test.go (fixture + mocks after Task 2)
  </read_first>
  <action>
    Service `ApproveProposal(ctx, role string, orgID, actorID, activityID uuid.UUID) (*activitydomain.ActivityResponse, error)` (D-12, T-11-02):
    1. existing, err := activityRepo.Get(ctx, orgID, activityID) — propagate ErrActivityNotFound.
    2. If existing.OriginType == nil || *existing.OriginType != activitydomain.OriginTypeEmployeeProposal → ErrInvalidRequest ("only employee proposals are approvable").
    3. If existing.IsActive → ErrInvalidRequest ("already approved").
    4. If actorID == *existing.ProposedBy → ErrForbidden (no self-approval).
    5. Resolve the proposer's primary unit: unitRepo.ListMembershipsForUser(ctx, *existing.ProposedBy); find the membership with IsPrimary true → its UnitID; none → uuid.Nil.
    6. res, err := s.routing.ResolveManagerStage(ctx, orgID, activityID, unitID, *existing.ProposedBy). Propagate errors, including activity.ErrActivityNotLoggable (commercial proposal without anchored WG — map to ErrForbidden in the handler).
    7. Gate: if res.roleGated → require role ∈ {RoleManager, RoleFinance} else ErrForbidden; if res.skipToFinance → ErrForbidden (the only approver would be the proposer — routing cannot approve; documented in ADR-P-013); else require contains(res.approverIDs, actorID) else ErrForbidden.
    8. Persist: activityRepo.Update(ctx, orgID, activityID, &UpdateActivityRequest{IsActive: ptr(true)}) — the REPO method directly, bypassing the service Update gate (the approver check above is the gate).
    9. Audit: s.auditRepo.Create(ctx, &audit.AuditLog{OrgID: orgID, EntityType: "activity", EntityID: activityID, Action: "proposal_approved", ActorID: &actorID, Payload: map[string]any{"approver": actorID.String()}, CreatedAt: time.Now()}) — synchronous; error propagates (T-11-08).
    10. Return the updated activity.

    Handler: `ApproveProposal(w, r)` — method `POST /activities/{id}/approve-proposal`; parse id, role/userID from middleware; sentinel map: ErrActivityNotFound/ErrInvalidRequest → 400, ErrForbidden → 403, activity.ErrActivityNotLoggable → 400.

    main.go: construct `routingSvc := routing.NewService(wgRepo, activityRepo, unitRepo)` (already built in 11-03 — reuse), `auditRepo := postgres.NewAuditLogRepository(pool)`, `ticketRepo := postgres.NewTicketRepository(pool)`; pass orgRepo/ticketRepo/auditRepo/routingSvc into the activity service constructor; register `mux.HandleFunc("POST /activities/{id}/approve-proposal", middleware.Auth(authService, activityHandler.ApproveProposal))` next to the other activity routes (~L184-190).

    Tests: unit — approver in set → is_active flipped + audit row recorded with action proposal_approved; actor not in set → ErrForbidden; actor == proposer → ErrForbidden; wrong origin → ErrInvalidRequest; roleGated resolution → manager role passes, employee role fails; skipToFinance → ErrForbidden. Integration (activity repo): ApproveProposal end-to-end via a proposal created through Task 2's create path.
  </action>
  <acceptance_criteria>
    - activity.go defines ApproveProposal with the gate sequence (grep `ResolveManagerStage` + `proposal_approved` in activity.go)
    - activity_handler.go exposes ApproveProposal mapped to the sentinel switch
    - main.go registers `POST /activities/{id}/approve-proposal` and wires routingSvc/auditRepo/ticketRepo into the activity service
    - `go test ./internal/core/services/activity/ -run TestProposal -count=1` exits 0 (new funcs prefixed TestProposal)
    - `go build ./... && go test ./internal/core/services/... ./internal/adapters/secondary/postgres/ -count=1` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/core/services/activity/ -run 'TestProposal|TestActivityOrigin' -count=1</automated>
  </verify>
  <done>Proposals route through BE-014 machinery, flip is_active on approval, write a synchronous audit row; endpoint registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /activities | untrusted origin payload crosses here (role gates + same-org refs must hold) |
| client → POST /activities/{id}/approve-proposal | unauthorized approval attempt crosses here (approver set gate) |
| service → audit_logs | event stream writes (must be synchronous, T-11-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | activity Service.Create origin gates | mitigate | Per-type role gates (manager_assignment/customer_ticket → manager|finance; employee_proposal → proposed_by == actor enforced with ErrForbidden) + same-org membership validation via orgRepo.GetMembership (D-02/D-04) |
| T-11-02 | Elevation of Privilege | activity Service.ApproveProposal | mitigate | Approver set from routing.ResolveManagerStage; self-approval rejected; skipToFinance (proposer-only) rejected; roleGated requires manager|finance |
| T-11-06 | Elevation of Privilege | customer_ticket origin refs | mitigate | ticketRepo.Get org-scoped — cross-org ticket_id fails with ErrInvalidRequest; ticket-state precondition after Get (Status ∈ {open, triage} else ErrInvalidRequest) blocks linking activities to planned/in_progress/resolved/closed/dismissed tickets (OQ5 fast path, ADR-BE-016); DB FK backstops (015) |
| T-11-10 | Tampering | activity Service.Update | mitigate | Any origin field in an update request → ErrOriginImmutable (D-03); repo UPDATE never touches origin columns (columns absent from SET clause) |
| T-11-08 | Repudiation | audit_log_repository.Create | mitigate | Synchronous writes with propagated errors (no detached goroutine); append-only — no update/delete methods exist; proposal approval writes action proposal_approved with actor captured |
| T-11-09 | Tampering | all new SQL | mitigate | Parameterized pgx queries only; wrapPGError maps constraint violations (house ADR-BE-003) |
| T-11-SC | Tampering | package installs | accept | No package-manager installs this phase — Go-only on the existing dependency set |

**Remaining register (T-11-03, T-11-04, T-11-05, T-11-07, T-11-11) is enforced by plans 01/04/06.**
</threat_model>

<verification>
- Per task: the task's automated commands
- Per wave: `make test` (full suite — must stay green after each plan)
- `go vet ./...` before wave close
- **Scope monitor (W-3):** this plan touches 16 files, but 9 are small new scaffolds (domain/ports/mocks/repos). If Task 1 balloons past ~30% context, split it at the clean seam: (a) `internal/core/domain/ticket`, `internal/core/domain/audit`, the two ports files, the two testdata mocks; then (b) the two postgres repos + `ticket_repository_test.go` — ports/mocks are consumed by Task 2, repo tests are leaf work. Task 2 and Task 3 are coherent verticals and stay whole.
</verification>

<success_criteria>
- POST /activities accepts origin payloads with D-04 role gates and D-02 same-org validation; origin refs immutable (D-03)
- Proposals created is_active=false; POST /activities/{id}/approve-proposal flips is_active + writes audit (FND-02)
- Activity responses expose stored origin refs (FND-04 read path; fallback reserved for Phase 13)
- ticket + audit domain packages, base ticket repo, audit repo, and mocks exist and are tested
</success_criteria>

## Artifacts this phase produces

- `internal/core/domain/activity/activity.go` — MODIFIED: `OriginType *string`, `AssignedBy/AssignedTo/ProposedBy/ReviewedBy/TicketID *uuid.UUID` on Activity/ActivityResponse/both requests; `IsActive *bool` on CreateActivityRequest; sentinel `ErrOriginImmutable`; constants `OriginTypeManagerAssignment/OriginTypeEmployeeProposal/OriginTypeCustomerTicket`
- `internal/core/services/activity/activity.go` — MODIFIED: DI + NewService gain orgRepo/ticketRepo/auditRepo/routing; `Create(ctx, role string, orgID, userID uuid.UUID, req)` with per-type gates + same-org validation + reviewed_by rejection; Update immutability guard; NEW method `ApproveProposal(ctx, role string, orgID, actorID, activityID uuid.UUID) (*activitydomain.ActivityResponse, error)`
- `internal/adapters/secondary/postgres/activity_repository.go` — MODIFIED: origin columns in query/scan; Create INSERT with origin columns + COALESCE(req.IsActive, true)
- `internal/adapters/primary/http/activity_handler.go` — MODIFIED: origin payload + role/userID pass-through in Create/Update; NEW handler method `ApproveProposal`
- `internal/core/domain/ticket/ticket.go` — NEW: `Ticket`, `TicketComment` structs; kind/status constants (7 statuses); `CanTransition(from, to string) bool` (matrix incl. open→dismissed); `IsTerminalStatus(s) bool`; `(t *Ticket) IsOwner/IsAssignee`; sentinels ErrTicketNotFound/ErrInvalidRequest/ErrForbidden/ErrInvalidTransition/ErrDismissalBlocked/ErrActivityNotTerminal
- `internal/core/domain/audit/audit.go` — NEW: `AuditLog` (ID, OrgID, EntityType, EntityID, Action, ActorID *uuid.UUID, Comment, Payload map[string]any, CreatedAt)
- `internal/core/ports/ticket_repository.go` — NEW: minimal `TicketRepository { Get }` (extended by plan 06)
- `internal/core/ports/audit_log_repository.go` — NEW: `AuditLogRepository { Create }`
- `internal/adapters/secondary/postgres/ticket_repository.go` — NEW: `TicketRepository` with org-scoped `Get`
- `internal/adapters/secondary/postgres/audit_log_repository.go` — NEW: `AuditLogRepository.Create` (synchronous INSERT INTO audit_logs)
- `internal/core/services/testdata/mock_ticket_repo.go`, `mock_audit_log_repo.go` — NEW mocks
- `cmd/server/main.go` — MODIFIED: routingSvc/auditRepo/ticketRepo construction; activity service DI; route `POST /activities/{id}/approve-proposal`
- Tests: TestActivityOrigin* (unit + repo), TestProposal* (unit + integration), TestTicket* (repo Get + audit round-trip)

<output>
Create `.planning/phases/11-foundations-schema-origins-tickets-backend/11-05-SUMMARY.md` when done
</output>

