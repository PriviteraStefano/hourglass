---
phase: 12-coverage-backend-the-allocation-loop
plan: 07
type: execute
wave: 4
depends_on:
  - 05
  - 06
files_modified:
  - internal/adapters/primary/http/coverage_handler.go
  - internal/adapters/primary/http/coverage_handler_test.go
  - cmd/server/main.go
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04
  - COV-05

estimate:
  tokens: 25000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The full coverage API surface is registered: PUT /time-entries/{id}/allocations (replace-set, D-07), GET /time-entries/{id}/allocations, GET /coverage/proposals/{entry_id}, GET /coverage/to-cover (D-06), GET /coverage/buckets/{contract_id}/balance (D-02), POST /coverage/close (D-12), GET /coverage/snapshots/{close_id}, GET /coverage/allocations/{entry_id}/history — all behind middleware.Auth (COV-01..05)"
    - "Handler writeError maps sentinels: ErrEntryNotCoverable → 404, ErrAllocationSumMismatch/ErrInvalidRequest → 400, ErrForbidden → 403, ErrPeriodAlreadyClosed → 409, ErrNotFound → 404, default → 500 (pkg/api envelope shape)"
    - "Write endpoints (PUT allocations, POST close) require a manager; the D-08 gate runs in the service — handler tests prove the permission matrix: approver/role-gated manager 200, entry owner/employee/finance/customer 403 (COV-03, D-08)"
    - "POST /coverage/close takes {period_start, period_end} (org from claims) and returns the frozen PeriodClose incl. rows in one call (D-12, OQ4); close body date parse errors → 400"
    - "cmd/server/main.go wires coverageRepo → coveragesvc.NewService(coverageRepo, activityRepo, contractRepo, unitRepo, timeEntryRepo, routingSvc) → NewCoverageHandler and registers the eight routes using the Go 1.22 method pattern"
    - "Full suite green: make test"
  artifacts:
    - internal/adapters/primary/http/coverage_handler.go
    - internal/adapters/primary/http/coverage_handler_test.go
    - cmd/server/main.go (coverage service + handler wiring + routes)
  key_links:
    - "The handler stays thin: role/user/org come from middleware claims; all invariant logic is already in the service (12-05) — no re-validation at the boundary"
    - "Route path params ({id}, {entry_id}, {contract_id}, {close_id}) parsed with r.PathValue; entry_id and contract_id are UUIDs — parse errors → 400"
    - "Close uses org from middleware.GetOrganizationID — the body carries only the period; org never comes from the client"
  prohibitions:
    - "Never register incremental allocation CRUD routes (no PUT /allocations/{id}, no DELETE) — replace-set is the only write shape (D-07)"
    - "Never expose snapshot/audit UPDATE or DELETE routes — append-only (COV-04)"
    - "Never add a finance confirm step route (D-L one-step)"
    - "Never let the close body carry org_id — org comes from claims"
  flagged_assumptions:
    - "All reads are manager+finance (role from middleware.GetRole); employee reads of own coverage are Phase 17 (SURF-03) — read-model endpoints are not employee-facing this phase"
    - "GET /time-entries/{id}/allocations is included as the read-back endpoint (research 'optional; queue may embed' — kept for Phase 17)"
---

<objective>
The HTTP surface of the coverage plane: thin handlers for all eight routes behind auth, the sentinel→status map, and the cmd/server wiring that brings the service, repo, and routing together — proven by handler integration tests with the full permission matrix.

Purpose: This is where the phase becomes observable at the API boundary and where D-08's permission semantics are finally exercised end-to-end (claims → gate → status codes).
Output: coverage_handler.go + coverage_handler_test.go + main.go wiring; full suite green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md
@internal/adapters/primary/http/ticket_handler.go
@internal/core/services/coverage/coverage.go
@cmd/server/main.go
@internal/middleware/middleware.go
@pkg/api/response.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: coverage_handler.go — eight routes, DTOs, sentinel map + permission matrix tests</name>
  <files>
    internal/adapters/primary/http/coverage_handler.go
    internal/adapters/primary/http/coverage_handler_test.go
  </files>
  <read_first>
    internal/adapters/primary/http/ticket_handler.go (handler shape, DTO parse, writeError switch 347-364, parseOptionalUUID)
    internal/core/services/coverage/coverage.go (service methods + sentinels from 12-05)
    internal/core/domain/coverage/errors.go (sentinels + JSONNames)
    pkg/api/response.go (envelope)
    internal/middleware/middleware.go (GetOrganizationID/GetUserID/GetRole)
    internal/adapters/primary/http/ticket_handler_test.go (integration test conventions, permission matrix)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (handler analog, lines 213-273)
  </read_first>
  <action>
    Create internal/adapters/primary/http/coverage_handler.go: struct CoverageHandler{service *coveragesvc.Service}, NewCoverageHandler, and eight methods reading claims via middleware (orgID := GetOrganizationID(ctx), userID := GetUserID(ctx), role := GetRole(ctx)):
    - PutAllocations(w, r): path {id} → entryID; body DTO ReplaceAllocationsRequest{Allocations []AllocationRequest} with AllocationRequest{EntryType string, SourceType string, ContractID *string, UnitID *string, Hours float64, Reason *string, Justification *string} (string ids parsed at the boundary via parseOptionalUUID, parse errors → 400); map to []*coverage.CoverageAllocation (EntryID from path); call service.ReplaceAllocations; respond 200 with the stored set.
    - GetAllocations(w, r): GET /time-entries/{id}/allocations → service read (ListByEntry) with read-gate.
    - GetProposal(w, r): GET /coverage/proposals/{entry_id} → service.Propose → 200 proposal + current allocations.
    - GetToCoverQueue(w, r): GET /coverage/to-cover → service.ToCoverQueue → 200 queue rows.
    - GetBucketBalance(w, r): GET /coverage/buckets/{contract_id}/balance → service.BucketBalance → 200 {balance: float64}.
    - PostClose(w, r): body ClosePeriodRequest{PeriodStart string, PeriodEnd string} (dates as "2006-01-02" strings); parse errors → 400 "invalid period_start"/"invalid period_end"; call service.ClosePeriod(orgID, start, end, userID, role); 201 with the PeriodClose incl. rows (OQ4).
    - GetSnapshot(w, r): GET /coverage/snapshots/{close_id} → 200.
    - GetHistory(w, r): GET /coverage/allocations/{entry_id}/history → 200 audit stream.
    - writeError(w, err) switch: ErrEntryNotCoverable/ErrNotFound → 404, ErrAllocationSumMismatch/ErrInvalidRequest → 400, ErrForbidden → 403, ErrPeriodAlreadyClosed → 409, default → 500 (ticket_handler writeError shape; use api.RespondWithError).
    Handler tests (coverage_handler_test.go, testcontainers + real service stack mirroring ticket_handler_test conventions): permission matrix — manager (approver resolution) PUT allocations 200; entry owner PUT 403; finance PUT 403; employee PUT 403; finance GET to-cover 200; customer 403; envelope shape ({data:{...}}); sentinel mapping — PUT with Σ mismatch 400, close of overlapping period 409, proposal for a nonexistent entry 404; close date parse error 400; close returns rows.
  </action>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run 'TestCoverageHandler' -count=1</automated>
  </verify>
  <done>Every route works at the API boundary with the right status codes; the permission matrix holds; the envelope is consistent.</done>
  <acceptance_criteria>
    - coverage_handler.go defines CoverageHandler with the eight methods and writeError; body/query/path parsing uses middleware claims + r.PathValue + parseOptionalUUID
    - writeError maps all five sentinel outcomes (404/400/403/409/500)
    - ClosePeriodRequest parses "2006-01-02" dates; parse failure → 400
    - coverage_handler_test.go covers: manager 200, owner/finance/employee/customer 403 on PUT, finance 200 on GET to-cover, Σ mismatch 400, overlapping close 409, nonexistent entry proposal 404, date parse 400
    - `go test ./internal/adapters/primary/http/ -run 'TestCoverageHandler' -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: cmd/server/main.go wiring — service, handler, eight routes + full suite green</name>
  <files>
    cmd/server/main.go
  </files>
  <read_first>
    cmd/server/main.go (ticket wiring lines 144-153; route registration 220-228 — the Go 1.22 pattern)
    internal/core/services/coverage/coverage.go (NewService signature)
    internal/adapters/primary/http/coverage_handler.go (handler methods)
  </read_first>
  <action>
    In cmd/server/main.go, after the ticket wiring block: `coverageRepo := postgres.NewCoverageRepository(pool)`; `coverageService := coveragesvc.NewService(coverageRepo, activityRepo, contractRepo, unitRepo, timeEntryRepo, routingSvc)` (import the new packages — coveragesvc alias `github.com/stefanoprivitera/hourglass/internal/core/services/coverage` and the http coverage package); `coverageHandler := http.NewCoverageHandler(coverageService)`. Register exactly eight routes with middleware.Auth, following the existing pattern:
    `mux.HandleFunc("PUT /time-entries/{id}/allocations", middleware.Auth(authService, coverageHandler.PutAllocations))`
    `mux.HandleFunc("GET /time-entries/{id}/allocations", middleware.Auth(authService, coverageHandler.GetAllocations))`
    `mux.HandleFunc("GET /coverage/proposals/{entry_id}", middleware.Auth(authService, coverageHandler.GetProposal))`
    `mux.HandleFunc("GET /coverage/to-cover", middleware.Auth(authService, coverageHandler.GetToCoverQueue))`
    `mux.HandleFunc("GET /coverage/buckets/{contract_id}/balance", middleware.Auth(authService, coverageHandler.GetBucketBalance))`
    `mux.HandleFunc("POST /coverage/close", middleware.Auth(authService, coverageHandler.PostClose))`
    `mux.HandleFunc("GET /coverage/snapshots/{close_id}", middleware.Auth(authService, coverageHandler.GetSnapshot))`
    `mux.HandleFunc("GET /coverage/allocations/{entry_id}/history", middleware.Auth(authService, coverageHandler.GetHistory))`
    Reuse the already-constructed routingSvc (STATE.md: routing.Service is constructed once and shared — do NOT build a second instance). Confirm timeEntryRepo (hexTE repo) is already available in scope; if the variable name differs, use the existing entry repo instance for the service.
    Then run the full suite.
  </action>
  <verify>
    <automated>go build ./... && make test</automated>
  </verify>
  <done>Server compiles with the coverage stack wired; the full suite is green.</done>
  <acceptance_criteria>
    - cmd/server/main.go contains the coverageService construction with the shared routingSvc and the eight mux.HandleFunc registrations matching the paths above
    - `go build ./...` exits 0
    - `make test` exits 0 (full suite including all 12-01..12-07 tests)
    - No second routing.Service instance is created
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/adapters/primary/http/coverage_handler.go` — `CoverageHandler{service}` + `NewCoverageHandler`; methods `PutAllocations`, `GetAllocations`, `GetProposal`, `GetToCoverQueue`, `GetBucketBalance`, `PostClose`, `GetSnapshot`, `GetHistory`; DTOs `ReplaceAllocationsRequest`/`AllocationRequest` (string ids) + `ClosePeriodRequest{PeriodStart, PeriodEnd string}`; `writeError` sentinel map
- `internal/adapters/primary/http/coverage_handler_test.go` — permission-matrix + sentinel-mapping integration tests (`TestCoverageHandler*`)
- `cmd/server/main.go` — `coverageRepo`/`coverageService`/`coverageHandler` construction + eight registered routes under middleware.Auth

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP handler | untrusted bodies, path params, claims cross here |
| handler → service | only validated DTOs pass the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-18 | Elevation of privilege | PUT allocations / POST close | high | mitigate | All writes behind middleware.Auth + the service D-08 gate; handler tests assert the full permission matrix (approver 200, owner/employee/finance/customer 403) |
| T-12-19 | Spoofing | close body org | high | mitigate | org_id never accepted from the client — middleware.GetOrganizationID claims only; date parse errors → 400 (Task 1) |
| T-12-20 | Information disclosure | queue/proposals/balance/history reads | medium | mitigate | Read endpoints call the service read-gate (manager+finance, 12-05); org-scoped repo queries (12-06); finance 200 / customer 403 asserted in tests |
| T-12-21 | Tampering | route surface | medium | mitigate | Only the eight pinned routes exist — no incremental CRUD, no snapshot/audit mutation routes (D-07/COV-04) |

No high-severity unmitigated threats: T-12-18/19 mitigated by the tasks above; T-12-20's gate was implemented in 12-05 and is exercised here.
</threat_model>

<verification>
- `go test ./internal/adapters/primary/http/ -run 'TestCoverageHandler' -count=1` green
- `go build ./...` green
- `make test` green (full suite — the phase gate)
</verification>

<success_criteria>
- All eight endpoints respond with the envelope and correct status codes per the permission matrix
- The coverage stack is live in the server binary with a single shared routing service
- Full suite green before /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/12-coverage-backend-the-allocation-loop/12-07-SUMMARY.md` when done
</output>
