---
phase: 12-coverage-backend-the-allocation-loop
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - internal/core/domain/activity/activity.go
  - internal/core/ports/activity_repository.go
  - internal/adapters/secondary/postgres/activity_repository.go
  - internal/core/services/activity/activity.go
  - internal/adapters/primary/http/activity_handler.go
  - internal/core/services/testdata/mocks.go
  - internal/adapters/secondary/postgres/activity_beneficiary_unit_test.go
autonomous: true
requirements:
  - COV-05

estimate:
  tokens: 25000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Activity create/update accepts a nullable beneficiary_unit_id; the value persists and round-trips through Get/List (COV-05)"
    - "GetAncestry's explicit column list carries beneficiary_unit_id in the seed, recursive, and final SELECTs; scanActivity maps it (Pitfall 5 closed)"
    - "ResolveBeneficiaryUnit(activityID) returns the nearest ancestor's beneficiary_unit_id walking upward while NULL — the CTE mirrors ResolveCommercialContext (COV-05, D-04 absorption default)"
    - "ResolveFundingContext(activityID) returns the nearest ancestor contract_id plus contract_type and sold_hours via the contracts JOIN — the D-04 decision input"
    - "Activity service rejects beneficiary_unit_id that is not same-org (unitRepo.GetByID + OrgID compare); handler parses it as *string → *uuid.UUID at the boundary"
  artifacts:
    - internal/core/domain/activity/activity.go (BeneficiaryUnitID + request fields)
    - internal/core/ports/activity_repository.go (ResolveBeneficiaryUnit + ResolveFundingContext + FundingContext)
    - internal/adapters/secondary/postgres/activity_repository.go (5 touch points + 2 CTE resolvers)
    - internal/core/services/activity/activity.go (same-org validation)
    - internal/adapters/primary/http/activity_handler.go (DTO parse)
    - internal/core/services/testdata/mocks.go (MockActivityRepo field)
    - internal/adapters/secondary/postgres/activity_beneficiary_unit_test.go
  key_links:
    - "baseActivityQuery + GetAncestry + scanActivity all gain the column in the same change — a half-wired column silently breaks ResolveBeneficiaryUnit (Pitfall 5)"
    - "Coverage service (plan 12-05) consumes ResolveBeneficiaryUnit + ResolveFundingContext via the port — signatures are fixed by this plan"
    - "beneficiary_unit_id is EDITABLE on Update (unlike origin refs) — service Update validates it but must not treat it as an origin field"
  prohibitions:
    - "Never branch the coverage proposal rule on billability — this plan only provides chain data (ResolveFundingContext/ResolveBeneficiaryUnit); the D-04 decision function itself lands in plan 12-05"
    - "Never make beneficiary_unit_id immutable or force it on create — it is nullable and editable per COV-05"
  flagged_assumptions:
    - "Unit same-org validation uses unitRepo.GetByID(*id → string) then compares u.OrgID == orgID (unitRepo.GetByID is not org-scoped, verified) — the same pattern the expense service applies"
    - "FundingContext is defined in domain/activity (chain data belongs to the activity domain); ResolveFundingContext returns the nearest ancestor contract even when the walk must also resolve the beneficiary unit — two separate resolvers, each with its own walk guard"
---

<objective>
The phase tracer: beneficiary unit on the activity stack, end-to-end — migration 018 column through domain, port, repository (read + write + both CTE resolvers), service validation, and handler boundary, proven by integration tests.

Purpose: This is the thinnest vertical slice that touches every layer this phase modifies and de-risks the phase's sharpest DB trap (Pitfall 5: GetAncestry's explicit column list must gain the new column or the absorption proposal path silently returns nil). It also delivers the two resolvers the coverage service (plan 12-05) builds on — ResolveBeneficiaryUnit (absorption default, COV-05) and ResolveFundingContext (contract type/value for D-04).
Output: beneficiary_unit_id read/write through the activity stack + two CTE resolvers + tests.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md
@internal/core/domain/activity/activity.go
@internal/core/ports/activity_repository.go
@internal/adapters/secondary/postgres/activity_repository.go
@internal/core/services/activity/activity.go
@internal/adapters/primary/http/activity_handler.go
</context>

<tasks>

<task type="tracer">
  <name>End-to-end beneficiary unit — column field, port resolvers, repo read/write + CTE walks</name>
  <files>
    internal/core/domain/activity/activity.go
    internal/core/ports/activity_repository.go
    internal/adapters/secondary/postgres/activity_repository.go
    internal/adapters/secondary/postgres/activity_beneficiary_unit_test.go
  </files>
  <read_first>
    internal/core/domain/activity/activity.go (ContractID field line ~64; CreateActivityRequest ~126; UpdateActivityRequest ~148; origin axis block)
    internal/core/ports/activity_repository.go (ResolveCommercialContext decl line 28)
    internal/adapters/secondary/postgres/activity_repository.go (baseActivityQuery 33-36, GetAncestry 194-231, ResolveCommercialContext 236-264, Update 428-503, scanActivity 605-634)
    internal/adapters/secondary/postgres/ticket_repository_test.go (integration test conventions)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (activity MODIFY analog section, lines 421-451)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Pattern 2, lines 242-261; Common Operation 2, lines 448-466; Pitfall 5)
  </read_first>
  <action>
    Domain (internal/core/domain/activity/activity.go): add field `BeneficiaryUnitID *uuid.UUID` with json tag `beneficiary_unit_id,omitempty` immediately after ContractID (nullable = internal work inheritance, mirrors contract_id); add pointer fields of the same name to CreateActivityRequest and UpdateActivityRequest.

    Port (internal/core/ports/activity_repository.go): add two methods with doc comments — `ResolveBeneficiaryUnit(ctx context.Context, activityID uuid.UUID) (*uuid.UUID, error)` (nearest ancestor with beneficiary_unit_id set; COV-05) and `ResolveFundingContext(ctx context.Context, activityID uuid.UUID) (*activitydomain.FundingContext, error)` (nearest ancestor contract_id + contract_type + sold_hours; D-04 input). Define `FundingContext` in domain/activity with fields `ContractID *uuid.UUID`, `ContractType *string`, `SoldHours *float64`.

    Repository (internal/adapters/secondary/postgres/activity_repository.go) — five touch points, one commit:
    1. baseActivityQuery SELECT: add `a.beneficiary_unit_id` after `a.contract_id`.
    2. GetAncestry: add `beneficiary_unit_id` to the seed SELECT, the recursive SELECT, and the final SELECT of the explicit column list (lines 194-231).
    3. scanActivity (+ scanActivityResponse): add `var beneficiaryUnitID *uuid.UUID`, scan slot, and `a.BeneficiaryUnitID = beneficiaryUnitID` — exact origin-columns pattern.
    4. Update dynamic SET builder: add a branch mirroring the req.ContractID branch — `if req.BeneficiaryUnitID != nil { sets = append(sets, "beneficiary_unit_id = $N"); args = append(args, *req.BeneficiaryUnitID) }` (beneficiary unit is editable, unlike origin refs).
    5. Implement ResolveBeneficiaryUnit: copy ResolveCommercialContext's WITH RECURSIVE chain CTE shape, but walk `beneficiary_unit_id` with the guard `WHERE c.beneficiary_unit_id IS NULL` and return the first non-null value, falling back to the final SELECT; nil when the chain has none. Implement ResolveFundingContext: same CTE shape walking contract_id with `WHERE c.contract_id IS NULL`, LEFT JOIN contracts ct ON ct.id = c.contract_id, selecting `ct.contract_type, ct.sold_hours` alongside the resolved contract_id (Common Operation 2 shape).

    Integration test (internal/adapters/secondary/postgres/activity_beneficiary_unit_test.go, testcontainers + applyMigrations + TeardownTestSchema + seedOrg/seedActivityKind/seedUnit/seedActivity — self-seed inline, never seed_demo.sql):
    - Create activity with BeneficiaryUnitID set via repo Create → Get returns it.
    - Create parent with beneficiary_unit_id, child without → ResolveBeneficiaryUnit(childID) returns the parent's unit (inheritance).
    - Activity with no beneficiary anywhere → ResolveBeneficiaryUnit returns nil.
    - ResolveFundingContext returns contract_id + contract_type + sold_hours for an activity under a seeded contract (seed a contract row via direct SQL INSERT matching contracts columns from 000/016) and returns all-NULL fields when the chain has no contract.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestActivityBeneficiaryUnit|TestResolveBeneficiaryUnit|TestResolveFundingContext' -count=1</automated>
  </verify>
  <done>The one beneficiary-unit path works end-to-end through the DB layer: create → read → update → resolve (both CTE resolvers) — committed with its tests.</done>
  <acceptance_criteria>
    - activity.go contains `BeneficiaryUnitID *uuid.UUID` and both request structs carry the pointer field with `json:"beneficiary_unit_id,omitempty"`
    - ports/activity_repository.go declares ResolveBeneficiaryUnit and ResolveFundingContext; domain/activity defines FundingContext with ContractID/ContractType/SoldHours
    - postgres/activity_repository.go: baseActivityQuery SELECT includes `a.beneficiary_unit_id`; GetAncestry includes it in all three SELECTs; scanActivity assigns it; Update builds the beneficiary_unit_id SET branch
    - `go test ./internal/adapters/secondary/postgres/ -run 'TestActivityBeneficiaryUnit|TestResolveBeneficiaryUnit|TestResolveFundingContext' -count=1` exits 0
    - `go build ./...` compiles
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Expansion: service same-org validation + handler DTO parse + mock field + tests</name>
  <files>
    internal/core/services/activity/activity.go
    internal/adapters/primary/http/activity_handler.go
    internal/core/services/testdata/mocks.go
  </files>
  <read_first>
    internal/core/services/activity/activity.go (Create contract check lines 106-110; validateOrigin; Update 210-230 — hasOriginFields must stay origin-only)
    internal/adapters/primary/http/activity_handler.go (UpdateActivityRequest DTO 57-77, CreateActivityRequest 36-55, ContractID parse blocks 175-182 and 315-322)
    internal/core/services/testdata/mocks.go (MockActivityRepo ~480)
    internal/core/domain/unit/unit.go (OrgID field — the same-org compare target)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (activity service/handler MODIFY sections, lines 453-472)
  </read_first>
  <action>
    Service (internal/core/services/activity/activity.go): in Create, after the ContractID check, add the same shape for BeneficiaryUnitID — `if req.BeneficiaryUnitID != nil { u, err := s.unitRepo.GetByID(ctx, req.BeneficiaryUnitID.String()); if err != nil || u.OrgID != orgID { return nil, err } }` (GetByID returns the unit or an error; a unit from another org must be rejected — the fetch error path is fine to surface as the sentinel the caller maps). In Update, add the identical check before s.activityRepo.Update. Do NOT touch hasOriginFields (beneficiary unit is not an origin ref — it stays editable).

    Handler (internal/adapters/primary/http/activity_handler.go): add `BeneficiaryUnitID *string` with json tag `beneficiary_unit_id,omitempty` to CreateActivityRequest and UpdateActivityRequest DTOs; in the Create and Update parse blocks, mirror the req.ContractID parse: when non-nil, uuid.Parse and on error respond 400 "invalid beneficiary_unit_id"; set svcReq.BeneficiaryUnitID to the parsed pointer.

    Mock (internal/core/services/testdata/mocks.go): add `BeneficiaryUnitID *uuid.UUID` to MockActivityRepo's stored activity map entries/creation helper and honor it in Create/Update handlers so service tests can round-trip it (follow the existing MockActivityRepo field pattern).
  </action>
  <verify>
    <automated>go test ./internal/core/services/activity/ ./internal/adapters/primary/http/ -count=1</automated>
  </verify>
  <done>The beneficiary unit is validated same-org at both service entry points and accepted through the HTTP boundary; existing activity suites stay green.</done>
  <acceptance_criteria>
    - services/activity/activity.go Create and Update both contain a BeneficiaryUnitID fetch-and-compare block using s.unitRepo.GetByID with an OrgID equality check
    - http/activity_handler.go DTOs contain `BeneficiaryUnitID *string` and both parse blocks return 400 "invalid beneficiary_unit_id" on parse failure
    - testdata/mocks.go MockActivityRepo handles BeneficiaryUnitID on create/update
    - `go test ./internal/core/services/activity/ ./internal/adapters/primary/http/ -count=1` exits 0
    - hasOriginFields in services/activity/activity.go is unchanged (no beneficiary_unit_id added to it)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/core/domain/activity/activity.go` — field `Activity.BeneficiaryUnitID *uuid.UUID` + `CreateActivityRequest.BeneficiaryUnitID` + `UpdateActivityRequest.BeneficiaryUnitID` (pointer, omitempty); struct `FundingContext{ContractID *uuid.UUID, ContractType *string, SoldHours *float64}`
- `internal/core/ports/activity_repository.go` — methods `ResolveBeneficiaryUnit(ctx, activityID) (*uuid.UUID, error)` and `ResolveFundingContext(ctx, activityID) (*activitydomain.FundingContext, error)`
- `internal/adapters/secondary/postgres/activity_repository.go` — beneficiary_unit_id in baseActivityQuery/GetAncestry/scanActivity/Update; `ResolveBeneficiaryUnit` + `ResolveFundingContext` CTE implementations
- `internal/core/services/activity/activity.go` — same-org BeneficiaryUnitID validation in Create + Update
- `internal/adapters/primary/http/activity_handler.go` — `BeneficiaryUnitID *string` on both DTOs + parse blocks
- `internal/core/services/testdata/mocks.go` — MockActivityRepo BeneficiaryUnitID handling
- `internal/adapters/secondary/postgres/activity_beneficiary_unit_test.go` — integration tests (create/get/update round-trip, inheritance, nil-when-none, ResolveFundingContext)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → activity API | untrusted beneficiary_unit_id crosses here |
| service → unit repo | same-org check depends on the fetch+compare |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Spoofing | activity Create/Update beneficiary_unit_id | high | mitigate | Same-org fetch-and-compare on both paths (unitRepo.GetByID then u.OrgID == orgID) — a cross-org unit id is rejected (Task 2) |
| T-12-07 | Tampering | GetAncestry column list | high | mitigate | The column lands in all three SELECTs + scanActivity in one commit with an integration test asserting inheritance (Task 1 — Pitfall 5 closed) |

No high-severity unmitigated threats: both T-12-06 and T-12-07 are mitigated by the tasks above.
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestActivityBeneficiaryUnit|TestResolveBeneficiaryUnit|TestResolveFundingContext' -count=1` exits 0
- `go test ./internal/core/services/activity/ ./internal/adapters/primary/http/ -count=1` exits 0
- `go build ./...` compiles
</verification>

<success_criteria>
- beneficiary_unit_id round-trips through the full activity stack and is inherited downward via the CTE
- Both resolvers the coverage service needs are live with proven semantics
- Existing activity tests unaffected (additive change)
</success_criteria>

<output>
Create `.planning/phases/12-coverage-backend-the-allocation-loop/12-03-SUMMARY.md` when done
</output>
