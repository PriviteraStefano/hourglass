---
phase: 12-coverage-backend-the-allocation-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/018_activity_beneficiary_unit.up.sql
  - migrations/018_activity_beneficiary_unit.down.sql
  - migrations/019_coverage_allocations.up.sql
  - migrations/019_coverage_allocations.down.sql
  - migrations/020_coverage_snapshots.up.sql
  - migrations/020_coverage_snapshots.down.sql
  - internal/adapters/secondary/postgres/exported_test_helpers.go
  - internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-04
  - COV-05

estimate:
  tokens: 20000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Migration 018 adds activities.beneficiary_unit_id (UUID, nullable, REFERENCES units(id)) + idx_activities_beneficiary_unit_id; down drops both; legacy rows pass untouched (no 3VL guard needed — single nullable column, mirrors contract_id in 011) (COV-05)"
    - "Migration 019 creates coverage_allocations with the tagged-union ledger shape (D-01): id, org_id, entry_type, entry_id, source_type, contract_id, unit_id, hours DECIMAL(8,2) NOT NULL CHECK (hours > 0), reason, justification, created_at, updated_at; constraint coverage_allocations_source_check follows the 3VL house rule (source_type IS NULL OR ...); mandatory-field CHECKs coverage_allocations_reason_check / coverage_allocations_justification_check; closed vocabularies CHECK source_type IN ('contract','absorption','transfer'), reason IN ('WarrantyBug','UnderEstimate','Goodwill'), entry_type IN ('time') (COV-01, COV-02, COV-05, D-01, D-11)"
    - "Migration 020 creates coverage_period_closes (id, org_id, period_start, period_end, closed_by, closed_at) + coverage_snapshot_rows (id, close_id ON DELETE CASCADE, entry_id, employee_id, entry_date, activity_id, source_type, contract_id, unit_id, hours, reason, justification) with idx_coverage_snapshot_rows_close/entry; entry-level rows only, no aggregates (COV-04, D-11)"
    - "TeardownTestSchema drops coverage_snapshot_rows, coverage_period_closes, coverage_allocations before time_entries/activities (dependency order, Pitfall 8)"
    - "Cycle tests TestMigration018/019/020 pass up → down → up with functional 3VL assertions: absorption-without-unit rejected (23514), transfer-without-justification rejected, all-NULL legacy row passes (COV-02, Pitfall 2)"
  artifacts:
    - migrations/018_activity_beneficiary_unit.up.sql
    - migrations/018_activity_beneficiary_unit.down.sql
    - migrations/019_coverage_allocations.up.sql
    - migrations/019_coverage_allocations.down.sql
    - migrations/020_coverage_snapshots.up.sql
    - migrations/020_coverage_snapshots.down.sql
    - internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go
  key_links:
    - "019 coverage_allocations.entry_id has NO FK (polymorphic entry per D-K) — service validates; entry_type CHECK ('time') + service D-K branch are the costed belt-and-braces pair (COV-05)"
    - "019 CHECKs guard NULLs: 3VL (source_type IS NULL OR ...) so legacy/mixed refs never silently pass (Pitfall 2, ADR-BE-016)"
    - "020 close_id → coverage_period_closes ON DELETE CASCADE; append-only: no UPDATE/DELETE paths exist anywhere (COV-04)"
  prohibitions:
    - "Never create proposal tables or stored balance counters in migrations 018-020 — balances and proposals are derived on read (D-02, D-I); the ledger stores only confirmed allocations"
    - "Never add an is_locked / closed flag column to coverage_allocations — period close is a frozen snapshot, not a lock (D-F, D-10)"
    - "Never reuse financial_cutoff_periods as the close trigger — the coverage close is a separate mechanism (D-12); the existing table stays facts-only"
  flagged_assumptions:
    - "Zero-value contract predicate pinned: contract_type='project' AND sold_hours IS NOT DISTINCT FROM 0 → service-request draw (A3, OQ3 recommendation) — schema-side there is no discriminator, the predicate lives in code and ADR-BE-017"
    - "source_type vocabulary = 3 row-level values ('contract'/'absorption'/'transfer'); the five funding sources are derived from the referenced contract (A1, OQ1 recommendation) — CHECK enforces the 3 values"
    - "entry_type has a schema CHECK ('time') AND a service branch (A4/D-K) — both documented as the costed 'one extra validation branch' in ADR-BE-017"
---

<objective>
Schema foundation for the coverage plane: three append-only migrations (018 beneficiary unit, 019 allocation ledger, 020 period-close snapshots) with up/down pairs per ADR-BE-004, the shared test teardown list updated, and self-seeding cycle tests proving every constraint.

Purpose: Every later coverage task (repo, service, handler) compiles and tests against a verified schema. The 3VL-guarded tagged-union CHECK (D-01) is the riskiest schema claim in the phase — the cycle tests are the end-to-end proof that runs before any code touches the tables.
Output: migrations 018/019/020 (.up/.down), updated `TeardownTestSchema`, `coverage_ontology_migrations_test.go` with TestMigration018/019/020.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md
@migrations/015_activity_origins.up.sql
@migrations/016_contract_sold_hours.up.sql
@migrations/017_audit_logs.up.sql
@internal/adapters/secondary/postgres/exported_test_helpers.go
@internal/adapters/secondary/postgres/ontology_extension_migrations_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migrations 018-020 (up/down pairs) for beneficiary unit, allocation ledger, snapshots</name>
  <files>
    migrations/018_activity_beneficiary_unit.up.sql
    migrations/018_activity_beneficiary_unit.down.sql
    migrations/019_coverage_allocations.up.sql
    migrations/019_coverage_allocations.down.sql
    migrations/020_coverage_snapshots.up.sql
    migrations/020_coverage_snapshots.down.sql
  </files>
  <read_first>
    migrations/015_activity_origins.up.sql (tagged-union + 3VL CHECK house rule)
    migrations/016_contract_sold_hours.up.sql (sold_hours column + contracts_sold_check precedent)
    migrations/017_audit_logs.up.sql (append-only table + index shape)
    migrations/000_full_schema.up.sql line 270-286 (time_entries: hours DECIMAL(8,2) CHECK > 0, entry_date TIMESTAMPTZ)
    migrations/011_activity_ontology.up.sql (activities table shape; time_entries.activity_id rewrite)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Pattern 1 lines 206-237, Pattern 5 lines 279-305 — recommended SQL shapes to copy)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (migration analog sections)
  </read_first>
  <action>
    Create six files with the exact shapes from RESEARCH.md Pattern 1 (019) and Pattern 5 (020), following the numbering rule of ADR-BE-004 (next free numbers are 018, 019, 020 — the 007 gap is frozen, do not renumber).

    Migration 018 up: ALTER TABLE activities ADD COLUMN beneficiary_unit_id UUID REFERENCES units(id) — nullable, no backfill, no 3VL CHECK (single nullable column, mirrors contract_id per 011); CREATE INDEX idx_activities_beneficiary_unit_id ON activities(beneficiary_unit_id). Down: drop the index first, then ALTER TABLE activities DROP COLUMN IF EXISTS beneficiary_unit_id (016 down shape).

    Migration 019 up: CREATE TABLE coverage_allocations with columns id UUID PRIMARY KEY DEFAULT gen_random_uuid(), org_id UUID NOT NULL REFERENCES organizations(id), entry_type VARCHAR(50) NOT NULL, entry_id UUID NOT NULL (NO FK — polymorphic, D-K), source_type VARCHAR(50) NOT NULL, contract_id UUID REFERENCES contracts(id), unit_id UUID REFERENCES units(id), hours DECIMAL(8,2) NOT NULL CHECK (hours > 0) — identical to time_entries.hours, reason VARCHAR(50), justification TEXT, created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(). Then four ALTER TABLE ADD CONSTRAINT statements with exactly these names and shapes: coverage_allocations_source_check — 3VL guard `CHECK (source_type IS NULL OR (source_type = 'contract' AND contract_id IS NOT NULL AND unit_id IS NULL) OR (source_type = 'absorption' AND unit_id IS NOT NULL AND contract_id IS NULL) OR (source_type = 'transfer' AND contract_id IS NOT NULL AND unit_id IS NULL))`; coverage_allocations_source_type_check — `CHECK (source_type IN ('contract','absorption','transfer'))`; coverage_allocations_reason_check — `CHECK (source_type <> 'absorption' OR reason IS NOT NULL)`; coverage_allocations_justification_check — `CHECK (source_type <> 'transfer' OR justification IS NOT NULL)`; coverage_allocations_reason_vocab_check — `CHECK (reason IS NULL OR reason IN ('WarrantyBug','UnderEstimate','Goodwill'))` per COV-02 (exactly these three, the Part-5 fourth value is superseded); coverage_allocations_entry_type_check — `CHECK (entry_type IN ('time'))` per D-K. Indexes: idx_coverage_allocations_org (org_id), idx_coverage_allocations_entry (entry_type, entry_id), idx_coverage_allocations_contract (contract_id), idx_coverage_allocations_unit (unit_id). Down: DROP TABLE IF EXISTS coverage_allocations CASCADE.

    Migration 020 up: CREATE TABLE coverage_period_closes (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), org_id UUID NOT NULL REFERENCES organizations(id), period_start DATE NOT NULL, period_end DATE NOT NULL, closed_by UUID NOT NULL REFERENCES users(id), closed_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); CREATE TABLE coverage_snapshot_rows (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), close_id UUID NOT NULL REFERENCES coverage_period_closes(id) ON DELETE CASCADE, entry_id UUID NOT NULL, employee_id UUID NOT NULL, entry_date DATE NOT NULL, activity_id UUID NOT NULL, source_type VARCHAR(50) NOT NULL, contract_id UUID, unit_id UUID, hours DECIMAL(8,2) NOT NULL CHECK (hours > 0), reason VARCHAR(50), justification TEXT); indexes idx_coverage_snapshot_rows_close (close_id) and idx_coverage_snapshot_rows_entry (entry_id). Entry-level rows only — no aggregate columns (D-11). No UNIQUE(org_id, period_start, period_end) constraint — duplicate-close rejection is a repo-level in-tx check returning 409 (A6), not a DB unique error. Down: DROP TABLE IF EXISTS coverage_snapshot_rows CASCADE; then DROP TABLE IF EXISTS coverage_period_closes CASCADE.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration018|TestMigration019|TestMigration020' -count=1</automated>
  </verify>
  <done>All six migration files exist with the constraint names above; both cycle tests pass in the same task (tests may not exist yet on first run — see Task 2 for the test scaffold; if missing, the automated check targets Task 2's tests after they land).</done>
  <acceptance_criteria>
    - migrations/018_activity_beneficiary_unit.up.sql contains `beneficiary_unit_id UUID REFERENCES units(id)` and `idx_activities_beneficiary_unit_id`
    - migrations/018_activity_beneficiary_unit.down.sql drops idx_activities_beneficiary_unit_id then beneficiary_unit_id (IF EXISTS)
    - migrations/019_coverage_allocations.up.sql contains table coverage_allocations with all six constraint names: coverage_allocations_source_check (3VL), coverage_allocations_source_type_check, coverage_allocations_reason_check, coverage_allocations_justification_check, coverage_allocations_reason_vocab_check, coverage_allocations_entry_type_check
    - migrations/019_coverage_allocations.up.sql has hours DECIMAL(8,2) NOT NULL CHECK (hours > 0) and entry_id with no REFERENCES clause
    - migrations/020_coverage_snapshots.up.sql creates coverage_period_closes and coverage_snapshot_rows with close_id ON DELETE CASCADE and idx_coverage_snapshot_rows_close/entry; no aggregate columns (no totals/billing columns)
    - migrations/019/020 down files use DROP TABLE IF EXISTS ... CASCADE
    - No file in migrations/ is renumbered; numbering continues at 018
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Teardown list update + cycle tests TestMigration018/019/020 (self-seed, 3VL + mandatory-field assertions)</name>
  <files>
    internal/adapters/secondary/postgres/exported_test_helpers.go
    internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/exported_test_helpers.go (TeardownTestSchema lines 79-124; seed helpers 138-196)
    internal/adapters/secondary/postgres/ontology_extension_migrations_test.go (TestMigration014..017 self-seed cycle shape, lines 26-84)
    internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go (being created)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Common Operation 4, lines 487-508; Pitfall 8)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (exported_test_helpers analog; cycle-test skeleton lines 474-498)
  </read_first>
  <action>
    In exported_test_helpers.go, extend the TeardownTestSchema table list with exactly three entries before "time_entries" (dependency order — snapshot rows → period closes → allocations, all before time_entries/activities): "coverage_snapshot_rows", "coverage_period_closes", "coverage_allocations".

    Create coverage_ontology_migrations_test.go with three tests following the TestMigration014 self-seed shape (applyMigrations for pre-state + readMigration + seedOrg/seedActivityKind/seedUnit/seedActivity + inline SQL):
    - TestMigration018_ActivityBeneficiaryUnit_UpDownUpCycle: apply 019+020 as pre-state, seed org + kind + unit + activity; UP → assert column exists (information_schema), UPDATE the seeded activity setting beneficiary_unit_id to the seeded unit and read it back; assert a NULL default row passes (legacy valid); DOWN → column and index gone (pg_constraint/information_schema count); UP again → green.
    - TestMigration019_CoverageAllocations_UpDownUpCycle: UP → assert the six constraints exist by name; functional 3VL assertions: INSERT absorption-without-unit must fail with pgErr.Code "23514" and constraint name coverage_allocations_source_check; INSERT transfer-without-justification fails with coverage_allocations_justification_check; INSERT all-NULL source_type row passes (legacy guard); INSERT source_type='contract' with both contract_id and unit_id set fails (source_check); INSERT hours=0 fails (hours CHECK); DOWN → constraints gone; UP again → green.
    - TestMigration020_CoverageSnapshots_UpDownUpCycle: UP → both tables exist; assert idx_coverage_snapshot_rows_close/entry; assert ON DELETE CASCADE fires (insert close + row, delete close, row count 0); DOWN → both tables gone; UP again → green.
    Self-seed pre-state inline per the Phase 11 house rule (003_seed is retired; never load scripts/seed_demo.sql in tests).
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration018|TestMigration019|TestMigration020' -count=1</automated>
  </verify>
  <done>All three cycle tests pass; teardown list contains the three coverage tables in dependency order before time_entries.</done>
  <acceptance_criteria>
    - exported_test_helpers.go TeardownTestSchema list contains "coverage_snapshot_rows", "coverage_period_closes", "coverage_allocations" all appearing before "time_entries"
    - coverage_ontology_migrations_test.go exists with funcs TestMigration018_ActivityBeneficiaryUnit_UpDownUpCycle, TestMigration019_CoverageAllocations_UpDownUpCycle, TestMigration020_CoverageSnapshots_UpDownUpCycle
    - TestMigration019 asserts pgErr.Code "23514" for absorption-without-unit and transfer-without-justification, and a passing all-NULL source_type row
    - go test ./internal/adapters/secondary/postgres/ -run 'TestMigration018|TestMigration019|TestMigration020' -count=1 exits 0
    - No test loads scripts/seed_demo.sql
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `migrations/018_activity_beneficiary_unit.up.sql` / `.down.sql` — `activities.beneficiary_unit_id UUID REFERENCES units(id)`, `idx_activities_beneficiary_unit_id`
- `migrations/019_coverage_allocations.up.sql` / `.down.sql` — table `coverage_allocations` (id, org_id, entry_type, entry_id, source_type, contract_id, unit_id, hours, reason, justification, created_at, updated_at) + constraints `coverage_allocations_source_check` (3VL), `coverage_allocations_source_type_check`, `coverage_allocations_reason_check`, `coverage_allocations_justification_check`, `coverage_allocations_reason_vocab_check`, `coverage_allocations_entry_type_check` + indexes `idx_coverage_allocations_org/entry/contract/unit`
- `migrations/020_coverage_snapshots.up.sql` / `.down.sql` — tables `coverage_period_closes` (id, org_id, period_start, period_end, closed_by, closed_at) + `coverage_snapshot_rows` (id, close_id, entry_id, employee_id, entry_date, activity_id, source_type, contract_id, unit_id, hours, reason, justification) + indexes `idx_coverage_snapshot_rows_close/entry`
- `internal/adapters/secondary/postgres/coverage_ontology_migrations_test.go` — `TestMigration018_ActivityBeneficiaryUnit_UpDownUpCycle`, `TestMigration019_CoverageAllocations_UpDownUpCycle`, `TestMigration020_CoverageSnapshots_UpDownUpCycle`
- `exported_test_helpers.go` teardown entries: `coverage_snapshot_rows`, `coverage_period_closes`, `coverage_allocations`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL → PostgreSQL | Untrusted?? — migrations are repo-owned append-only SQL; the risk is constraint correctness, not injection (parameterized migration runner) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | coverage_allocations CHECKs | high | mitigate | 3VL house-rule guard (`source_type IS NULL OR (...)`) so NULL cannot bypass ref-set pinning; mandatory-field CHECKs use `source_type <> 'absorption' OR reason IS NOT NULL` form (never NULL-satisfiable); asserted in TestMigration019 (Pitfall 2) |
| T-12-02 | Tampering | coverage_allocations vocabulary | medium | mitigate | Closed vocabularies enforced by CHECK: source_type IN (3 values), reason IN (3 values), entry_type IN ('time') — D-K branch costed |
| T-12-03 | Tampering | snapshot tables | medium | mitigate | Append-only by construction: 020 has no UPDATE/DELETE surface anywhere (no endpoints, no repo methods planned); ON DELETE CASCADE only from close header |

No high-severity unmitigated threats: T-12-01 is mitigated by Task 2's cycle tests asserting the constraint behavior.
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestMigration018|TestMigration019|TestMigration020' -count=1` exits 0
- `go build ./...` compiles (test package wired)
- Full suite green at wave merge: `make test`
</verification>

<success_criteria>
- All three migrations apply, cycle, and down cleanly (ADR-BE-004)
- The 3VL guard + mandatory-field CHECKs are proven by failing-insert assertions, not just by DDL text
- Teardown drops coverage tables in dependency order (Pitfall 8 closed)
</success_criteria>

<output>
Create `.planning/phases/12-coverage-backend-the-allocation-loop/12-01-SUMMARY.md` when done
</output>
