---
phase: 12-coverage-backend-the-allocation-loop
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/domain/coverage/coverage.go
  - internal/core/domain/coverage/errors.go
  - internal/core/ports/coverage_repository.go
  - internal/core/services/testdata/mock_coverage_repo.go
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04

estimate:
  tokens: 14000
  raw_tokens: 8000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "domain/coverage exports the entity and read models: CoverageAllocation (ID, OrgID, EntryType, EntryID, SourceType, ContractID *uuid.UUID, UnitID *uuid.UUID, Hours float64, Reason *string, Justification *string, CreatedAt, UpdatedAt), CoverageProposal, ToCoverQueueRow, PeriodClose, SnapshotRow (COV-01/02/04, D-01/D-11)"
    - "Closed vocabularies exported as constants: SourceTypeContract/SourceTypeAbsorption/SourceTypeTransfer, AbsorptionReasonWarrantyBug/UnderEstimate/Goodwill, EntryTypeTime, AuditActionAllocationsSet/CoverageClosed (COV-02, A2, A7)"
    - "Sentinels + JSONNames: ErrEntryNotCoverable, ErrAllocationSumMismatch, ErrPeriodAlreadyClosed, ErrForbidden, ErrInvalidRequest, ErrNotFound (house style, ticket domain analog)"
    - "ports.CoverageRepository declares exactly: ReplaceAllocations, ListByEntry, ToCoverQueue, BucketBalance, ClosePeriod, GetSnapshot, ListHistory — replace-set only, no incremental CRUD (D-07), append-only (COV-04)"
    - "MockCoverageRepo implements the port with mutex-guarded state + per-method Fn overrides + Audits capture (mock_ticket_repo pattern)"
  artifacts:
    - internal/core/domain/coverage/coverage.go
    - internal/core/domain/coverage/errors.go
    - internal/core/ports/coverage_repository.go
    - internal/core/services/testdata/mock_coverage_repo.go
  key_links:
    - "Port signatures are the contract plans 12-05 (service) and 12-06 (repo) both compile against — they must match exactly"
    - "The ToCoverQueueRow shape carries entry/employee/activity ids and covered/uncovered hours; proposal enrichment happens in the service (12-05), not the repo (D-06)"
    - "audit.AuditLog is the shared audit type from domain/audit — coverage mutators hand audit rows to the repo exactly like tickets"
  prohibitions:
    - "Never add incremental CRUD methods (CreateAllocation/UpdateAllocation/DeleteAllocation) to the port — the replace-set is the only write shape (D-07)"
    - "Never add a stored-balance or stored-proposal type — balances derive on read (D-02), proposals compute on read (D-I); only confirmed allocations and snapshots persist"
  flagged_assumptions:
    - "A1 pinned: source_type vocabulary = 3 values (contract/absorption/transfer); five funding sources derived from the referenced contract — constants mirror the schema CHECK exactly"
    - "A2 pinned: absorption reasons are exactly WarrantyBug/UnderEstimate/Goodwill (the Part-5 'plain internal' is superseded by COV-02)"
    - "A7 pinned: audit entity_type='coverage_allocation', actions 'allocations-set'/'coverage-closed' — constants exported so the repo and service can never drift"
---

<objective>
The coverage domain types, error vocabulary, repository port, and test mock — the shared contracts every other coverage plan compiles against.

Purpose: With the domain and port pinned here, the service (12-05) and the repo (12-06) can be built in parallel without signature drift; the mock makes service tests hermetic.
Output: domain/coverage package, ports.CoverageRepository, testdata.MockCoverageRepo.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md
@internal/core/domain/ticket/ticket.go
@internal/core/ports/ticket_repository.go
@internal/core/services/testdata/mock_ticket_repo.go
@internal/core/domain/audit/audit.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: coverage domain package — entity, read models, constants, sentinels</name>
  <files>
    internal/core/domain/coverage/coverage.go
    internal/core/domain/coverage/errors.go
  </files>
  <read_first>
    internal/core/domain/ticket/ticket.go (entity + closed-vocab constants + sentinels + JSONNames pattern, lines 3-8, 45-63, 65-83)
    internal/core/domain/audit/audit.go (AuditLog type — referenced, not redefined)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (coverage model analog, lines 95-140)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Pattern 1 vocabulary, lines 198-240; A7)
  </read_first>
  <action>
    Create internal/core/domain/coverage/coverage.go, package coverage:
    - Constants (closed vocabulary, mirror ticket.go style): `EntryTypeTime = "time"`, `SourceTypeContract = "contract"`, `SourceTypeAbsorption = "absorption"`, `SourceTypeTransfer = "transfer"`, `AbsorptionReasonWarrantyBug = "WarrantyBug"`, `AbsorptionReasonUnderEstimate = "UnderEstimate"`, `AbsorptionReasonGoodwill = "Goodwill"`, `AuditActionAllocationsSet = "allocations-set"`, `AuditActionCoverageClosed = "coverage-closed"`, `AuditEntityCoverageAllocation = "coverage_allocation"`.
    - Structs with json tags and *uuid.UUID nullable refs (ticket.go shape): CoverageAllocation{ID uuid.UUID, OrgID uuid.UUID, EntryType string, EntryID uuid.UUID, SourceType string, ContractID *uuid.UUID, UnitID *uuid.UUID, Hours float64, Reason *string, Justification *string, CreatedAt time.Time, UpdatedAt time.Time}; CoverageProposal{EntryID uuid.UUID, SourceType string, ContractID *uuid.UUID, UnitID *uuid.UUID, Hours float64, Reason *string, Justification *string, Flagged bool, FlagReason string} — Flagged/FlagReason carry the D-06 "no eligible source" state; ToCoverQueueRow{EntryID uuid.UUID, EmployeeID uuid.UUID, EmployeeName string, EntryDate time.Time, ActivityID uuid.UUID, ActivityName string, Hours float64, CoveredHours float64, UncoveredHours float64, Proposal *CoverageProposal}; PeriodClose{ID uuid.UUID, OrgID uuid.UUID, PeriodStart time.Time, PeriodEnd time.Time, ClosedBy uuid.UUID, ClosedAt time.Time, Rows []SnapshotRow}; SnapshotRow{ID uuid.UUID, CloseID uuid.UUID, EntryID uuid.UUID, EmployeeID uuid.UUID, EntryDate time.Time, ActivityID uuid.UUID, SourceType string, ContractID *uuid.UUID, UnitID *uuid.UUID, Hours float64, Reason *string, Justification *string}.
    - PeriodClose time fields are DATE-semantics (compare via .Format("2006-01-02") or truncated days — note in a doc comment; entry_date is TIMESTAMPTZ in the DB, snapshot rows store the date part).
    Create internal/core/domain/coverage/errors.go: sentinels via errors.New — ErrEntryNotCoverable ("entry not coverable"), ErrAllocationSumMismatch ("allocation sum does not match entry hours"), ErrPeriodAlreadyClosed ("period already closed"), ErrForbidden ("forbidden"), ErrInvalidRequest ("invalid request"), ErrNotFound ("not found") — plus JSONNames map mirroring ticket.go mapping each sentinel to a stable JSON-safe name.
  </action>
  <verify>
    <automated>go build ./internal/core/domain/coverage/ && go vet ./internal/core/domain/coverage/</automated>
  </verify>
  <done>The domain package compiles with the full vocabulary and read models; constants match the schema vocabulary of plan 12-01 exactly.</done>
  <acceptance_criteria>
    - coverage.go exports all nine constants with the exact values above
    - coverage.go defines CoverageAllocation, CoverageProposal, ToCoverQueueRow, PeriodClose, SnapshotRow with the field sets above (uuid.UUID ids, *uuid.UUID refs, json tags)
    - errors.go defines the six sentinels + JSONNames with an entry per sentinel
    - `go build ./internal/core/domain/coverage/` and `go vet ./internal/core/domain/coverage/` exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ports.CoverageRepository + testdata.MockCoverageRepo</name>
  <files>
    internal/core/ports/coverage_repository.go
    internal/core/services/testdata/mock_coverage_repo.go
  </files>
  <read_first>
    internal/core/ports/ticket_repository.go (interface style + doc comments stating the in-tx audit contract)
    internal/core/domain/coverage/coverage.go (types just created)
    internal/core/domain/audit/audit.go
    internal/core/services/testdata/mock_ticket_repo.go (mutex map + Fn overrides + Audits capture, lines 22-51)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (port + mock analogs, lines 142-159, 361-377)
  </read_first>
  <action>
    Create internal/core/ports/coverage_repository.go: interface CoverageRepository with doc comments stating the in-tx audit contract (every mutator writes its audit_logs row in the same transaction; append-only by construction — mirror ticket port doc style). Methods, exactly:
    - ReplaceAllocations(ctx context.Context, orgID, entryID uuid.UUID, allocs []*coverage.CoverageAllocation, auditLog *audit.AuditLog) ([]*coverage.CoverageAllocation, error) — the atomic replace-set (D-07); in-tx Σ re-validation under the FOR UPDATE entry lock is the repo's contract
    - ListByEntry(ctx context.Context, orgID, entryID uuid.UUID) ([]*coverage.CoverageAllocation, error)
    - ToCoverQueue(ctx context.Context, orgID uuid.UUID) ([]coverage.ToCoverQueueRow, error) — raw uncovered rows; proposal enrichment is service-side (D-06)
    - BucketBalance(ctx context.Context, orgID, contractID uuid.UUID) (float64, error) — sold_hours − Σ drawn (D-02); negative allowed (D-03)
    - ClosePeriod(ctx context.Context, orgID uuid.UUID, periodStart, periodEnd time.Time, closedBy uuid.UUID, auditLog *audit.AuditLog) (*coverage.PeriodClose, error) — frozen entry-level snapshot (D-10/D-11); duplicate period → coverage.ErrPeriodAlreadyClosed (A6)
    - GetSnapshot(ctx context.Context, orgID, closeID uuid.UUID) (*coverage.PeriodClose, error)
    - ListHistory(ctx context.Context, orgID, entryID uuid.UUID) ([]audit.AuditLog, error) — audit stream for entity_type='coverage_allocation' (A7)
    Create internal/core/services/testdata/mock_coverage_repo.go: MockCoverageRepo implementing the port — sync.Mutex, `Allocations map[uuid.UUID][]*coverage.CoverageAllocation` keyed by entry id, `Snapshots map[uuid.UUID]*coverage.PeriodClose`, `Audits []*audit.AuditLog` (every audit row handed to a mutator, mirroring MockTicketRepo), per-method `Fn func(...) (...)` override knobs (ReplaceAllocationsFn, ListByEntryFn, ToCoverQueueFn, BucketBalanceFn, ClosePeriodFn, GetSnapshotFn, ListHistoryFn) plus error-return knobs (ErrOnReplace etc.), compile-time assertion `var _ ports.CoverageRepository = (*MockCoverageRepo)(nil)`. Default behaviors: ReplaceAllocations stores the set and records the audit; BucketBalance returns a settable balance field; ClosePeriod returns a stored snapshot or ErrPeriodAlreadyClosed when the mock is primed so; keep default semantics minimal and deterministic for service tests.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/core/services/testdata/ -count=1</automated>
  </verify>
  <done>The port compiles and the mock satisfies it; the service unit tests (12-05) can drive every method deterministically.</done>
  <acceptance_criteria>
    - ports/coverage_repository.go declares exactly the seven methods with the signatures above
    - mock_coverage_repo.go contains `var _ ports.CoverageRepository = (*MockCoverageRepo)(nil)` and implements all seven methods
    - MockCoverageRepo records every audit log passed to ReplaceAllocations/ClosePeriod into Audits
    - `go build ./...` and `go test ./internal/core/services/testdata/ -count=1` exit 0
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/core/domain/coverage/coverage.go` — constants EntryTypeTime, SourceTypeContract/Absorption/Transfer, AbsorptionReasonWarrantyBug/UnderEstimate/Goodwill, AuditActionAllocationsSet/CoverageClosed, AuditEntityCoverageAllocation; structs CoverageAllocation, CoverageProposal, ToCoverQueueRow, PeriodClose, SnapshotRow
- `internal/core/domain/coverage/errors.go` — sentinels ErrEntryNotCoverable, ErrAllocationSumMismatch, ErrPeriodAlreadyClosed, ErrForbidden, ErrInvalidRequest, ErrNotFound + JSONNames
- `internal/core/ports/coverage_repository.go` — interface CoverageRepository with 7 methods: ReplaceAllocations, ListByEntry, ToCoverQueue, BucketBalance, ClosePeriod, GetSnapshot, ListHistory
- `internal/core/services/testdata/mock_coverage_repo.go` — MockCoverageRepo (Allocations map, Snapshots map, Audits capture, per-method Fn/error knobs)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| port interface → repo/service | signature contract — a drift here breaks both sides silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08 | Tampering | port surface | medium | mitigate | Replace-set is the only write method (D-07); no incremental CRUD can enter the system through the port — compile-time assertion on the mock keeps the contract honest |

No high-severity threats in scope (types/contracts only).
</threat_model>

<verification>
- `go build ./...` green
- `go vet ./internal/core/domain/coverage/ ./internal/core/ports/` green
</verification>

<success_criteria>
- Domain vocabulary matches the 019 schema CHECKs exactly (single source of truth, no drift)
- Port and mock are the compile-time anchor for plans 12-05 and 12-06
</success_criteria>

<output>
Create `.planning/phases/12-coverage-backend-the-allocation-loop/12-04-SUMMARY.md` when done
</output>
