---
phase: 12-coverage-backend-the-allocation-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md"
  - "hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md"
  - hourglass-vault/decisions/backend/_index.md
  - hourglass-vault/decisions/project/_index.md
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04
  - COV-05

estimate:
  tokens: 10000
  raw_tokens: 6000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "ADR-P-012 header status flips from Proposed to Accepted with an acceptance note dated this phase (COV-01..05, D-01..D-12)"
    - "ADR-BE-017 — Coverage Encoding exists and documents: migrations 018-020 shapes, tagged-union source_type with the 3VL guard (D-01), derived bucket balance (D-02/D-03), chain-driven proposal decision function (D-04) with the ticket-kind extension seam (D-05), replace-set write with in-tx Σ validation under FOR UPDATE (D-07), manager gate via routing.ResolveManagerStage (D-08), no correction handling (D-09), frozen entry-level snapshots via POST /coverage/close (D-10/D-11/D-12), the D-K polymorphic validation cost ('one extra validation branch') stated honestly, and the pinned open-question resolutions: zero-value predicate contract_type='project' AND sold_hours IS NOT DISTINCT FROM 0 (A3), raw bucket balance without period scaling (A8), duplicate close rejected with 409 (A6), audit entity vocabulary entity_type='coverage_allocation' + actions allocations-set/coverage-closed (A7)"
    - "vault _index.md files list ADR-BE-017 with status Accepted and ADR-P-012 with status Accepted"
  artifacts:
    - "hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md"
    - "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md" (status flipped)
    - hourglass-vault/decisions/backend/_index.md
    - hourglass-vault/decisions/project/_index.md
  key_links:
    - "ADR-P-012's 'Implemented by:' header line points at ADR-BE-017 (it currently says ADR-BE-0xx) — the link must resolve after this plan"
    - "ADR-BE-017 cites ADR-BE-016 (3VL house rule), ADR-BE-014 (routing gate), and the research note parts 5-8/12/13 — the decision chain is traceable"
  prohibitions:
    - "Never record decisions that contradict locked D-01..D-12 — the ADR operationalizes them, it does not re-open them"
    - "Never omit the D-K polymorphic validation cost from ADR-BE-017 — ROADMAP requires it to be costed honestly"
  flagged_assumptions:
    - "The next free BE ADR number is 017 (verified: ADR-BE-016 exists, 017 does not) — pinned, do not skip numbers"
    - "A6 duplicate-close semantics recorded as reject-with-409 (not overwrite, not append) — the ADR is the record of truth for the repo behavior"
    - "A8 raw bucket balance (sold_hours − Σ drawn) recorded as the v0.2 semantic; period-scaling deferred to the Phase 17 bucket surface"
---

<objective>
Decision records for the coverage plane: ADR-P-012 moves from Proposed to Accepted, and the new backend encoding ADR (ADR-BE-017) is drafted, operationalizing D-01..D-12 and pinning every planner-resolved open question (zero-value predicate, raw bucket balance, close idempotency, audit vocabulary, D-K cost).

Purpose: The vault is the record of truth the executor agents and Phase 17 surfaces later consult. Every semantic the code encodes (3-value source_type, service-request predicate, 409 duplicate close) must be decided in writing before or while the code lands.
Output: ADR-BE-017 (new), ADR-P-012 status → Accepted, both vault indexes updated.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-CONTEXT.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md
@hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md
@hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md
@hourglass-vault/decisions/backend/_index.md
@hourglass-vault/decisions/project/_index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip ADR-P-012 to Accepted + draft ADR-BE-017 (coverage encoding)</name>
  <files>
    "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md"
    "hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md"
  </files>
  <read_first>
    "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md" (header format; the 'Implemented by' line currently names ADR-BE-0xx)
    "hourglass-vault/decisions/backend/ADR-BE-016 — Origins Tickets & Audit Schema Encoding.md" (structure/status conventions to mirror)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Patterns 1-6, Open Questions 1-7, Assumptions A1-A10, Pitfalls 1-9)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-CONTEXT.md (D-01..D-12, discretion areas)
  </read_first>
  <action>
    In ADR-P-012: change the header line `**Status:** Proposed` to `**Status:** Accepted`; append an Acceptance section dated 2026-08-07 recording that Phase 12 operationalizes D-1..D-6 via ADR-BE-017 and that the snapshot-not-lock choice (Q10 amendment) is implemented as the frozen period-close snapshot (D-10/D-11/D-12); update the `Implemented by:` header reference from ADR-BE-0xx to ADR-BE-017.

    Create "hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md" following ADR-BE-016's structure (title, Status: Accepted, Date, Operationalizes/Basis/Extends header, Context, Decision, Costs, Verification/Implementation sections). Document, each as a section with concrete schema/code identifiers:
    1. Schema 018-020: the exact table/column/constraint names from plan 12-01 (beneficiary_unit_id column, coverage_allocations with all six constraints, coverage_period_closes + coverage_snapshot_rows).
    2. Tagged-union source_type (D-01): 3 row-level values contract/absorption/transfer; the five funding sources derived from the referenced contract's contract_type/sold_hours (A1).
    3. Derived bucket balance (D-02/D-03): sold_hours − Σ allocations drawn (scoped by contract_id, any source_type); overdraw allowed; raw formula per A8, period-scaling deferred to Phase 17.
    4. Proposal decision function (D-04/D-05): chain-driven order — contract found: type 'support' → bucket draw, type 'project': sold_hours IS NOT DISTINCT FROM 0 → service-request draw (A3), else budget draw; no contract: beneficiary unit → absorption; neither → no-source flag (D-06). One reserved extension seam for ticket-kind rules (D-05) — no kind→source matrix implemented.
    5. Replace-set write (D-07): PUT /time-entries/{id}/allocations, full-set atomic replace, Σ = entry hours validated in-tx under the FOR UPDATE entry-row lock (CR-01 lesson), cents arithmetic; refs validated at the service — contract refs must resolve via contractRepo.Get AND be org-visible (res.CreatedByOrgID == orgID || (res.IsShared && res.IsAdopted)), unit refs via GetByID + OrgID compare (Get is not org-scoped).
    6. Manager gate (D-08): routing.ResolveManagerStage on (OrgID, ActivityID, UnitID, UserID); structural self-barrier; finance read-only; no correction handling (D-09) — approved 'time' entries only.
    7. Period-close snapshots (D-10/D-11/D-12): POST /coverage/close (org from claims, body period_start/period_end DATE), entry-level frozen rows, inclusive date range on entry_date::date, duplicate close → 409 (A6), financial_cutoff_periods stays facts-only (D-12/OQ7), close response returns the snapshot rows (OQ4).
    8. Audit vocabulary (A7): entity_type='coverage_allocation' (per entry) with actions 'allocations-set' and 'coverage-closed'; payload = full allocation set JSON / close summary; in-tx synchronous writes (BE-016).
    9. D-K polymorphic validation cost, stated honestly: the cost is one service branch rejecting entry_type != 'time' plus the entry_type CHECK — name both, estimate the branch as a single comparison, and note COV-06 (expense) will need an ALTER of the CHECK + a service rule change, not a redesign.
    10. Resolution record for Open Questions 1-7 (A1/A3/A4/A6/A8/A10 + proposal exposure: per-entry endpoint and queue-with-proposals both, sharing the decision function).
  </action>
  <verify>
    <automated>test -f "hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md" && grep -c "Accepted" "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md" && grep -c "ADR-BE-017" "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md"</automated>
  </verify>
  <done>ADR-P-012 says Accepted and links ADR-BE-017; ADR-BE-017 exists with all ten sections and the pinned OQ resolutions.</done>
  <acceptance_criteria>
    - "hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md" contains `**Status:** Accepted` and `ADR-BE-017`
    - "hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md" exists and contains all of: `coverage_allocations`, `coverage_allocations_source_check`, `source_type IS NULL OR`, `allocations-set`, `coverage-closed`, `IS NOT DISTINCT FROM 0`, `FOR UPDATE`, `ResolveManagerStage`, `financial_cutoff_periods`, `entry_type`
    - ADR-BE-017 contains a section or paragraph explicitly costing the D-K branch (mentions `entry_type != 'time'` or equivalent service check)
    - ADR-BE-017 records the duplicate-close semantics as 409 and the raw bucket balance formula
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update both vault indexes for ADR-BE-017 + ADR-P-012 status</name>
  <files>
    hourglass-vault/decisions/backend/_index.md
    hourglass-vault/decisions/project/_index.md
  </files>
  <read_first>
    hourglass-vault/decisions/backend/_index.md (table row format: | [[ADR-BE-0NN — Title]] | summary | scope | Status |)
    hourglass-vault/decisions/project/_index.md (same conventions)
  </read_first>
  <action>
    In hourglass-vault/decisions/backend/_index.md add one row to the table: `| [[ADR-BE-017 — Coverage Encoding]] | Allocation ledger (migrations 018-020): tagged-union source_type with 3VL CHECK guard, chain-driven proposals on read, replace-set with in-tx Σ validation, manager gate via BE-014, frozen period-close snapshots, D-K polymorphic validation cost | COV-01..05, D-01..D-12 | Accepted |` following the existing column layout exactly.
    In hourglass-vault/decisions/project/_index.md update the ADR-P-012 row status from Proposed to Accepted (mirror the exact row that already exists; only the status cell changes).
    Do not restructure either index; append/update cells only.
  </action>
  <verify>
    <automated>grep -c "ADR-BE-017" hourglass-vault/decisions/backend/_index.md && grep -c "Accepted" hourglass-vault/decisions/project/_index.md</automated>
  </verify>
  <done>Both indexes reflect the new ADR and the flipped status; no other rows changed.</done>
  <acceptance_criteria>
    - hourglass-vault/decisions/backend/_index.md contains a row naming `ADR-BE-017 — Coverage Encoding` with status `Accepted`
    - hourglass-vault/decisions/project/_index.md lists ADR-P-012 with status `Accepted`
    - git diff on both index files shows only the intended row additions/cell updates (no table reflow beyond the new row)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `hourglass-vault/decisions/backend/ADR-BE-017 — Coverage Encoding.md` — new ADR (Status: Accepted) documenting schema 018-020, tagged-union source_type (D-01), derived balance (D-02/D-03), chain-driven proposal function + extension seam (D-04/D-05), replace-set write (D-07), manager gate (D-08), no-corrections (D-09), frozen snapshots (D-10/D-11/D-12), audit vocabulary (A7), D-K cost, OQ1-7 resolutions
- `hourglass-vault/decisions/project/ADR-P-012 — Facts vs Decisions: The Coverage-Allocation Ledger.md` — Status: Proposed → Accepted + Acceptance section + `Implemented by:` link to ADR-BE-017
- `hourglass-vault/decisions/backend/_index.md` — new ADR-BE-017 row
- `hourglass-vault/decisions/project/_index.md` — ADR-P-012 status cell update

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault docs → executor agents | Decision records are read as truth by later tasks; a wrong pin propagates into code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Repudiation | ADR-BE-017 decision record | medium | mitigate | Every planner-resolved open question (A1/A3/A6/A8/A10, OQ1-7) recorded with an explicit resolution — no ambiguity left for Phase 17 to re-litigate |
| T-12-05 | Tampering | ADR-P-012 status flip | low | accept | Vault is git-tracked; the flip is append-only per index rules (status cell + Acceptance section, no content rewrite) |

No high-severity threats in scope (documentation plan).
</threat_model>

<verification>
- Both ADR files + both indexes committed (commit_docs workflow) and present on disk
- Grep markers from Task 1/2 acceptance criteria all match
</verification>

<success_criteria>
- ADR-P-012 accepted with a dated Acceptance section
- ADR-BE-017 pins every semantic the code will encode; D-K branch costed honestly
- Vault indexes consistent (one new row, one status cell)
</success_criteria>

<output>
Create `.planning/phases/12-coverage-backend-the-allocation-loop/12-02-SUMMARY.md` when done
</output>
