---
phase: 12-coverage-backend-the-allocation-loop
plan: 05
type: execute
wave: 3
depends_on:
  - 03
  - 04
files_modified:
  - internal/core/services/coverage/coverage.go
  - internal/core/services/coverage/coverage_test.go
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04

estimate:
  tokens: 28000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The D-04 decision function maps (entry chain) → proposal exactly: contract with contract_type='support' → contract draw (bucket); 'project' with sold_hours IS NOT DISTINCT FROM 0 → contract draw (service request); 'project' with sold_hours > 0 → contract draw (budget); no contract + beneficiary unit → absorption with the unit; neither → no-source flag 'no eligible source — needs a unit or contract' (COV-02, COV-03, D-04, D-06, A3)"
    - "Proposal computed on read only — the service never persists proposals (D-I); one reserved extension seam for ticket-kind rules, no kind→source matrix implemented (D-05)"
    - "ReplaceAllocations validates: entry approved + not deleted + entry_type == 'time' (D-K, D-09), Σ request hours == entry hours compared in cents (COV-01, D-07), every contract_id ref resolves via contractRepo.Get AND the returned contract is org-visible — res.CreatedByOrgID == orgID || (res.IsShared && res.IsAdopted) — else ErrInvalidRequest (Get filters only by c.id; orgID feeds the is_adopted subquery), every unit_id ref resolves same-org via unitRepo.GetByID + OrgID compare; every row is validated before any DB write: hours > 0, source_type ∈ {contract,absorption,transfer}, absorption reason ∈ {WarrantyBug,UnderEstimate,Goodwill}, exactly one pinned ref per source_type (contract XOR unit) — violations map to coverage.ErrInvalidRequest (→ 400) because 23514 check_violation is unmapped in wrapPGError (would surface as 500); the 019 CHECKs stay DB-backed belt-and-braces"
    - "D-08 gate (mandatory, Pattern 6 — allowed = actor ∈ ApproverIDs OR (res.RoleGated && role == 'manager')): the entry owner is structurally forbidden; routing.ResolveManagerStage(orgID, activityID, unitID, userID) is the only resolution; ErrActivityNotLoggable → forbidden; when the resolution returns RoleGated=true (org root without a unit manager) the org role claim MUST be 'manager' (role from middleware.GetRole) — finance/employee/customer are rejected on every path, no optional role pass (COV-03, D-08, A9)"
    - "Every write carries an audit.AuditLog{EntityType: 'coverage_allocation', EntityID: entryID, Action: 'allocations-set', ActorID: writer, Payload: full allocation set JSON} handed to the repo — the repo writes it in-tx (COV-03, BE-016, A7)"
    - "ClosePeriod orchestration: the service generates closeID := uuid.New() and calls repo.ClosePeriod(ctx, orgID, periodStart, periodEnd, closeID, userID, audit) with the coverage-closed audit log (single-tx, BE-016); the repo's in-tx overlap re-check is authoritative → coverage.ErrPeriodAlreadyClosed mapped to 409 (COV-04, A6); the close response returns the frozen PeriodClose incl. rows (OQ4)"
    - "ToCoverQueue service method enriches repo rows with per-entry proposals (or the no-source flag) via ResolveFundingContext/ResolveBeneficiaryUnit; BucketBalance passes through the repo formula sold_hours − Σ drawn, negatives returned as-is (COV-01/02, D-02, D-03, D-06)"
  artifacts:
    - internal/core/services/coverage/coverage.go (Service + NewService + all methods + pure DefaultSource decision helper)
    - internal/core/services/coverage/coverage_test.go
  key_links:
    - "The pure decision helper is the D-04 heart — unit-tested table-driven without repos; the service methods wrap it with entry fetch + gate + audit construction"
    - "routing.ResolveManagerStage is the shared BE-014 instance (constructed once in cmd/server wiring) — the service receives *routing.Service, never re-implements resolution"
    - "Entry data for the gate comes from timeEntryRepo.GetByID — the service checks e.OrgID == orgID after fetch (GetByID is not org-scoped)"
  prohibitions:
    - "Never validate Σ only at the pool level — pool checks are fast-fail UX; the repo re-validates inside the mutator tx under FOR UPDATE (CR-01, Pitfall 1)"
    - "Never branch the proposal rule on billability (D-04: single chain-driven function; ResolveBillability stays a report signal)"
    - "Never write audit rows outside the mutator tx or fire-and-forget (BE-016)"
    - "Never add a second confirm step or finance gate on writes (D-L one-step)"
    - "Never reject overdraw on bucket balance — balance is report-visible only (D-03)"
  flagged_assumptions:
    - "A3 pinned: zero-value predicate = contract_type='project' AND sold_hours IS NOT DISTINCT FROM 0 (NULL and explicit 0 both → service-request draw)"
    - "A10 pinned: the proposal is a single allocation of the full entry hours to the default source; splits are manager edits at confirm time"
    - "A9 pinned: the entry owner can never allocate their own coverage (structural self-barrier mirroring Approve); the RoleGated branch additionally requires the org role claim 'manager' — no optional role path on any write"
    - "A8 pinned: bucket balance is raw sold_hours − Σ drawn (no period scaling) — documented in ADR-BE-017"
    - "Contract ref org-visibility (pinned, corrects the earlier 'same-org by port contract' assumption): contractRepo.Get is NOT org-scoped (verified contract_repository.go:103-113 — WHERE c.id = $2, orgID feeds only the is_adopted subquery); the service requires res.CreatedByOrgID == orgID || (res.IsShared && res.IsAdopted) on the returned contract — the exact predicate BucketBalance's pre-check uses in 12-06, so shared+adopted contracts resolve consistently across both plans"
---

<objective>
The coverage service: the D-04 chain-driven proposal decision function, the replace-set write with Σ validation, per-row vocabulary/ref-pinning validation, and the mandatory D-08 manager gate (ApproverIDs OR RoleGated && role == "manager"), the to-cover queue enrichment, bucket balance pass-through, and the close orchestration — all unit-tested against the mock.

Purpose: This is where the phase's business semantics live (proposal rule, invariant, permission). The repo (12-06) stays a storage mechanism; every invariant a user can observe is decided here.
Output: internal/core/services/coverage/coverage.go + coverage_test.go.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md
@.planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md
@internal/core/services/ticket/ticket.go
@internal/core/services/time_entry/time_entry.go
@internal/core/services/routing/routing.go
@internal/core/domain/coverage/coverage.go
@internal/core/ports/coverage_repository.go
@internal/core/ports/activity_repository.go
@internal/core/ports/time_entry_repository.go
@internal/core/ports/contract_repository.go
@internal/core/ports/unit_repository.go
@internal/core/services/testdata/mock_coverage_repo.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-04 decision function (pure) + proposal/queue/balance service methods</name>
  <files>
    internal/core/services/coverage/coverage.go
    internal/core/services/coverage/coverage_test.go
  </files>
  <read_first>
    internal/core/services/ticket/ticket.go (Service struct + ctor + audit-row construction, lines 28-42, 105-113)
    internal/core/ports/activity_repository.go (ResolveFundingContext + ResolveBeneficiaryUnit from plan 12-03)
    internal/core/domain/activity/activity.go (FundingContext)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Pattern 2 lines 242-261, Pattern 6 lines 308-312, Assumptions A3/A10)
    .planning/phases/12-coverage-backend-the-allocation-loop/12-PATTERNS.md (service analog, lines 161-211)
  </read_first>
  <behavior>
    - Test 1: chain {contract: type=project, sold=40} → proposal {source_type=contract, contract_id set, unit nil, not flagged}
    - Test 2: chain {contract: type=support, sold=8} → proposal {source_type=contract, contract_id set} (bucket draw)
    - Test 3: chain {contract: type=project, sold=0} → proposal {source_type=contract} (service-request draw per A3)
    - Test 4: chain {contract: type=project, sold=nil} → proposal {source_type=contract} (NULL treated as zero-value per A3)
    - Test 5: chain {no contract, beneficiary set} → proposal {source_type=absorption, unit_id set}
    - Test 6: chain {no contract, no beneficiary} → flagged proposal with FlagReason "no eligible source — needs a unit or contract" (D-06)
  </behavior>
  <action>
    In internal/core/services/coverage/coverage.go (package coveragesvc): define the Service struct holding repo ports.CoverageRepository, activityRepo ports.ActivityRepository, contractRepo ports.ContractRepository, unitRepo ports.UnitRepository, entryRepo ports.TimeEntryRepository, routing *routing.Service — and NewService(···) wiring them.

    Implement a PURE helper (no repos, no ctx): `func DefaultSource(chain *activitydomain.FundingContext) (sourceType string, contractID, unitID *uuid.UUID, flagged bool, flagReason string)` implementing exactly the D-04 order — contract found: contract_type 'support' → contract draw; 'project' with SoldHours nil-or-0 (IS NOT DISTINCT FROM 0) → contract draw (service request); else → contract draw (budget); no contract: BeneficiaryUnitID set → absorption draw with that unit; neither → flagged=true, flagReason "no eligible source — needs a unit or contract". Add a doc comment reserving the D-05 extension seam (a single switch point where ticket-kind rules will plug in — no matrix implemented).

    Service methods (reads are manager+finance; role param from middleware.GetRole, role "finance" read-allowed):
    - Propose(ctx, orgID, entryID, role, userID): fetch entry via entryRepo.GetByID (reject if e.OrgID != orgID → ErrEntryNotCoverable), read-gate (manager or finance), fetch ResolveFundingContext; when ContractID nil also ResolveBeneficiaryUnit; build coverage.CoverageProposal via DefaultSource with Hours = entry.Hours; return proposal + current allocations from repo.ListByEntry.
    - ToCoverQueue(ctx, orgID, role, userID): read-gate (manager or finance), repo.ToCoverQueue, then enrich each row: DefaultSource over the chain (ResolveFundingContext/ResolveBeneficiaryUnit per row) attaching Proposal (hours = uncovered hours) or the flagged no-source proposal (D-06).
    - BucketBalance(ctx, orgID, contractID, role, userID): read-gate (manager or finance), repo.BucketBalance passthrough — return the raw signed value (negative allowed, D-03).
    - GetSnapshot(ctx, orgID, closeID, role, userID) and ListHistory(ctx, orgID, entryID, role, userID): read-gate + repo passthrough.

    Unit tests (coverage_test.go, table-driven with testdata.MockCoverageRepo + a stub routing resolution): the six DefaultSource cases above; Propose returns the current allocations and the computed proposal; ToCoverQueue attaches proposals and flags no-source rows; BucketBalance returns negative balances unchanged; read-gate rejects employee with coverage.ErrForbidden.
  </action>
  <verify>
    <automated>go test ./internal/core/services/coverage/ -run 'TestDefaultSource|TestService_Propose|TestService_ToCoverQueue|TestService_BucketBalance' -count=1</automated>
  </verify>
  <done>The decision function and read paths are green: proposals match the D-04 matrix on all six cases; no-source entries are flagged, never dropped.</done>
  <acceptance_criteria>
    - coverage.go contains `func DefaultSource(chain *activitydomain.FundingContext) (string, *uuid.UUID, *uuid.UUID, bool, string)` and a doc comment naming the D-05 extension seam
    - coverage.go defines Service with the six deps and NewService; Propose/ToCoverQueue/BucketBalance/GetSnapshot/ListHistory methods exist with read-gate checks (role manager|finance, else coverage.ErrForbidden)
    - coverage_test.go covers all six DefaultSource cases incl. sold=0 and sold=nil → contract draw, no-contract+unit → absorption, neither → flagged
    - `go test ./internal/core/services/coverage/ -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ReplaceAllocations (Σ + gate + refs + audit) and ClosePeriod orchestration</name>
  <files>
    internal/core/services/coverage/coverage.go
    internal/core/services/coverage/coverage_test.go
  </files>
  <read_first>
    internal/core/services/time_entry/time_entry.go (Approve gate lines 171-193 verbatim pattern; contains helper 302-309)
    internal/core/services/routing/routing.go (ResolveManagerStage 57-104; ApproverIDs/RoleGated fields)
    internal/core/services/ticket/ticket.go (audit-row construction 105-113)
    internal/core/domain/coverage/errors.go
    .planning/phases/12-coverage-backend-the-allocation-loop/12-RESEARCH.md (Pattern 3 lines 263-270, Pattern 6 lines 308-312, Pitfall 1)
  </read_first>
  <action>
    ReplaceAllocations(ctx, orgID, entryID uuid.UUID, req []*coverage.CoverageAllocation, userID, role string) ([]*coverage.CoverageAllocation, error):
    1. Entry fetch: entryRepo.GetByID(entryID); if e == nil or e.OrgID != orgID → coverage.ErrEntryNotCoverable; if e.Status != "approved" or e.IsDeleted → coverage.ErrEntryNotCoverable (D-09: approved only).
    2. D-K branch: for every allocation, entryType must be coverage.EntryTypeTime → else coverage.ErrInvalidRequest (the costed validation branch).
    3. Σ fast-fail: sum req hours in cents (int(math.Round(h*100))), compare with int(math.Round(e.Hours*100)); mismatch → coverage.ErrAllocationSumMismatch; empty request → same sentinel (Σ never equals hours>0).
     4. D-08 gate (mandatory, no optional role path — Pattern 6: allowed = actor ∈ ApproverIDs OR (res.RoleGated && role == "manager")): if e.UserID == userID → coverage.ErrForbidden (structural self-barrier, A9); res, err := s.routing.ResolveManagerStage(ctx, e.OrgID, e.ActivityID, e.UnitID, e.UserID); ErrActivityNotLoggable → coverage.ErrForbidden; if !res.RoleGated && !contains(res.ApproverIDs, userID) → coverage.ErrForbidden; and critically: if res.RoleGated && role != "manager" → coverage.ErrForbidden — when the resolution returns RoleGated=true (org root without a unit manager, routing.go terminal state) the org role claim (from middleware.GetRole) MUST be exactly "manager"; an employee/finance/customer claim never passes, mirroring Approve's `case role == "manager"` role switch in time_entry.go. The ApproverIDs path is unchanged from Approve (WG manager/delegate or unit manager may write in any role claim, exactly as they may approve).
     5. Per-row validation (schema vocabulary + ref pinning — every row checked BEFORE the repo call; violations → coverage.ErrInvalidRequest → 400, never a 500, because check_violation 23514 is unmapped in wrapPGError, verified postgres.go:16-33): for each allocation — hours must be > 0 (else ErrInvalidRequest); sourceType must be one of the 12-04 constants coverage.SourceTypeContract / SourceTypeAbsorption / SourceTypeTransfer (else ErrInvalidRequest); if sourceType == absorption the Reason must be non-nil AND one of coverage.AbsorptionReasonWarrantyBug / AbsorptionReasonUnderEstimate / AbsorptionReasonGoodwill (else ErrInvalidRequest); exactly one pinned ref per source_type — 'contract' → ContractID != nil && UnitID == nil, 'absorption' → UnitID != nil && ContractID == nil, 'transfer' → ContractID != nil && UnitID == nil (else ErrInvalidRequest). These invariants remain DB-backed by the 019 CHECKs — the service checks are the 400 path, the schema CHECKs are the last line of defense.
     6. Ref validation: for each allocation — source_type 'contract' → s.contractRepo.Get(ctx, orgID, *ContractID) must succeed AND the resolved contract must be org-visible: res.CreatedByOrgID == orgID || (res.IsShared && res.IsAdopted) → else coverage.ErrInvalidRequest. contractRepo.Get is NOT org-scoped (it filters only by c.id; orgID feeds the is_adopted subquery — verified contract_repository.go:103-113), so the CreatedByOrgID/IsShared/IsAdopted check in the service is the actual visibility gate. source_type 'absorption' → s.unitRepo.GetByID(UnitID.String()) must succeed and u.OrgID == orgID; source_type 'transfer' → contractRepo.Get + the same org-visibility check on the target contract + justification non-empty → else ErrInvalidRequest. reason mandatory for absorption, justification for transfer → ErrInvalidRequest.
     7. Audit: build audit.AuditLog{OrgID, EntityType: coverage.AuditEntityCoverageAllocation, EntityID: entryID, Action: coverage.AuditActionAllocationsSet, ActorID: &userID, Payload: map[string]any with the full allocation set (hours per source)}, then s.repo.ReplaceAllocations(ctx, orgID, entryID, allocs, log). The repo performs the authoritative in-tx Σ + status re-check under the FOR UPDATE lock (CR-01) — the service checks are fast-fail.
    ClosePeriod(ctx, orgID uuid.UUID, periodStart, periodEnd time.Time, userID uuid.UUID, role string) (*coverage.PeriodClose, error): write gate mandatory — the period is org-scoped with no entry chain to resolve, so gate purely on the role claim: role != "manager" → coverage.ErrForbidden (finance/employee/customer never close; no optional/approver-resolution branch). Single-tx contract (BE-016): the service generates closeID := uuid.New(), builds the audit log (Action: coverage.AuditActionCoverageClosed, EntityID: closeID, payload: period + row count), and calls s.repo.ClosePeriod(ctx, orgID, periodStart, periodEnd, closeID, userID, audit) — the repo inserts the header with id=closeID and the audit row in the same transaction, then returns the PeriodClose incl. rows (OQ4). Overlap → coverage.ErrPeriodAlreadyClosed from the repo (in-tx authoritative; A6).
    Unit tests (coverage_test.go): gate matrix — owner rejected, approver (in ApproverIDs) accepted, non-approver rejected, finance rejected, and the mandatory RoleGated branch: RoleGated=true + role="employee" → coverage.ErrForbidden, RoleGated=true + role="manager" → accepted (stub routing resolution returns managerResolution{RoleGated: true}); Σ mismatch and empty-set → ErrAllocationSumMismatch; entry_type 'expense' → ErrInvalidRequest; malformed-row matrix — hours=0 → ErrInvalidRequest, sourceType outside the 12-04 vocabulary → ErrInvalidRequest, absorption with reason outside {WarrantyBug,UnderEstimate,Goodwill} → ErrInvalidRequest, contract row with both refs pinned / no ref pinned → ErrInvalidRequest, absorption row with contract_id set → ErrInvalidRequest; contract ref visibility matrix — same-org contract (CreatedByOrgID == orgID) accepted, shared+adopted contract accepted, cross-org non-shared contract → ErrInvalidRequest, shared not-adopted → ErrInvalidRequest; absorption without reason / transfer without justification → ErrInvalidRequest; audit log handed to the repo has Action allocations-set and a payload with all allocations; ClosePeriod: employee → ErrForbidden, manager passes a coverage-closed audit and returns the mock snapshot; overlapping close propagates ErrPeriodAlreadyClosed.
  </action>
  <verify>
    <automated>go test ./internal/core/services/coverage/ -run 'TestService_ReplaceAllocations|TestService_ClosePeriod' -count=1</automated>
  </verify>
  <done>Write paths enforce the invariant (Σ, D-K, per-row vocabulary/ref pinning, refs), the mandatory D-08 gate (ApproverIDs OR RoleGated && role == "manager"), and the audit contract; close orchestrates the frozen snapshot with 409 semantics and a manager-only gate.</done>
  <acceptance_criteria>
    - coverage.go ReplaceAllocations performs the seven steps above (entry fetch+org check → D-K branch → cents Σ fast-fail → mandatory D-08 gate with the RoleGated && role == "manager" branch → per-row vocabulary/ref-pinning validation mapped to coverage.ErrInvalidRequest → ref validation with the contract org-visibility check res.CreatedByOrgID == orgID || (res.IsShared && res.IsAdopted) → audit + repo call)
    - Empty request and Σ mismatch both return coverage.ErrAllocationSumMismatch; non-time entry_type returns coverage.ErrInvalidRequest; owner/employee/finance writes AND RoleGated=true + non-manager role claims return coverage.ErrForbidden (no optional gate path exists)
    - Malformed allocation rows — hours <= 0, source_type outside {contract,absorption,transfer}, absorption reason outside {WarrantyBug,UnderEstimate,Goodwill}, refs not matching the source_type pinning (contract XOR unit) — return coverage.ErrInvalidRequest before any repo call, so 019 CHECKs (23514) are never reached from the service path
    - ClosePeriod requires role == "manager" (else coverage.ErrForbidden), generates the close id, passes Action coverage-closed audit, and returns the PeriodClose with rows
    - coverage_test.go asserts every gate/sentinel case above incl. RoleGated+employee → ErrForbidden; `go test ./internal/core/services/coverage/ -count=1` exits 0
    - `go build ./...` compiles
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `internal/core/services/coverage/coverage.go` — `Service` struct + `NewService(repo, activityRepo, contractRepo, unitRepo, entryRepo, routing)`; pure `DefaultSource(chain *activitydomain.FundingContext) (sourceType string, contractID, unitID *uuid.UUID, flagged bool, flagReason string)` (D-04 + D-05 seam); methods `Propose`, `ReplaceAllocations`, `ToCoverQueue`, `BucketBalance`, `ClosePeriod`, `GetSnapshot`, `ListHistory` (role-gated)
- `internal/core/services/coverage/coverage_test.go` — table-driven unit tests (DefaultSource 6 cases, gate matrix, sentinels, audit capture)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → coverage service | untrusted allocation sets, entry ids, refs cross here |
| service → routing | permission resolution output gates writes |
| service → repos | refs validated org-visible (contracts: CreatedByOrgID == orgID || IsShared && IsAdopted; units: OrgID compare) before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Elevation of privilege | ReplaceAllocations write gate | high | mitigate | D-08 gate via routing.ResolveManagerStage (ApproverIDs/RoleGated) + structural self-barrier (owner never writes own coverage); the RoleGated branch (org root without unit manager) additionally REQUIRES role == "manager" (ApproverIDs path unchanged, mirroring Approve's role switch); finance/employee/customer rejected on every path — gate matrix incl. RoleGated+employee unit-tested (Task 2 step 4) |
| T-12-10 | Spoofing | contract_id/unit_id refs | high | mitigate | Existence + org-visibility checks on every ref before the repo call: contractRepo.Get succeeds AND res.CreatedByOrgID == orgID || (res.IsShared && res.IsAdopted) — Get itself is not org-scoped (c.id only, contract_repository.go:103-113), the visibility check is the service's gate; unitRepo.GetByID + OrgID compare — Task 2 step 6 |
| T-12-11 | Tampering | Σ invariant (fast-fail path) | high | mitigate | Service fast-fail is UX only; the repo re-validates Σ + status in-tx under the FOR UPDATE entry lock (CR-01, plan 12-06 Task 1) — Pitfall 1 closed at the boundary where it matters |
| T-12-12 | Tampering | non-time entry_type | medium | mitigate | D-K branch rejects entry_type != 'time' at the service before any repo call (Task 2 step 2) |
| T-12-13 | Repudiation | audit trail | high | mitigate | Every mutator builds the audit row (entity_type coverage_allocation, payload full set) and hands it to the repo for in-tx write — never fire-and-forget (BE-016) |
| T-12-14 | Tampering | malformed allocation rows reaching 019 CHECKs | medium | mitigate | Per-row validation (hours > 0, source_type/reason vocabulary, ref pinning) maps every violation to coverage.ErrInvalidRequest → 400 before any repo call; check_violation 23514 is unmapped in wrapPGError (verified postgres.go:16-33) so the 500 path is unreachable from the service (Task 2 step 5); 019 CHECKs remain the DB-backed last line of defense |

No high-severity unmitigated threats: T-12-09/10/11/13 all mitigated by the tasks above (T-12-11's authoritative half lands in 12-06 Task 1, which 12-07 blocks on).
</threat_model>

<verification>
- `go test ./internal/core/services/coverage/ -count=1` green (full package)
- `go build ./...` green
- Per-wave: `make test`
</verification>

<success_criteria>
- Proposal matrix matches D-04 on all six cases including the A3 zero-value predicate
- Replace-set write path enforces Σ, D-K, org-visible refs, per-row vocabulary/ref-pinning (ErrInvalidRequest → 400, never 500), and the mandatory D-08 gate; audit contract asserted via mock capture
- No-source entries surface in the queue with the exact flag reason (D-06)
</success_criteria>

<output>
Create `.planning/phases/12-coverage-backend-the-allocation-loop/12-05-SUMMARY.md` when done
</output>
