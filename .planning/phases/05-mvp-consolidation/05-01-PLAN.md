---
phase: 05-mvp-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/002_seed_tcg.deprecated.surql
  - schema/003_seed_demo.surql
autonomous: true
requirements:
  - D-01
  - D-04
  - D-05
  - D-06
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-15
user_setup: []

must_haves:
  truths:
    - "Old seed file is renamed to .deprecated and no longer loaded by cmd/schema"
    - "New seed file exists at schema/003_seed_demo.surql"
    - "Organization 'Tech Consulting Group' exists in seed data with fresh UUID"
    - "6 users exist: 2 managers, 1 finance, 3 employees — each with bcrypt-hashed password_hash"
    - "3 departments (engineering, consulting, operations) exist as units with sub-units (8 total units)"
    - "1 customer (NovaTech Industries) exists"
    - "6 org memberships map each user to the org with role"
    - "6 unit memberships map each user to a primary unit"
    - "3 contracts exist (2 billable linked to NovaTech, 1 internal)"
    - "6 projects exist (2 per contract) with both schema (project_type) and Go (type) field names"
    - "6 subprojects exist (1 per project)"
    - "6 working groups exist (1 per subproject) with managers and unit assignments"
    - "6 WG members exist (1 per WG)"
  artifacts:
    - path: schema/003_seed_demo.surql
      provides: "Complete MVP demo seed data (foundation entities)"
      min_lines: 300
    - path: schema/002_seed_tcg.deprecated.surql
      provides: "Old seed file renamed for deprecation"
      min_lines: 1
  key_links:
    - from: schema/002_seed_tcg.deprecated.surql
      to: schema/003_seed_demo.surql
      via: "Rename operation — old seed is deprecated, new seed replaces it"
      pattern: "002_seed_tcg.deprecated"
    - from: schema/003_seed_demo.surql
      to: cmd/schema/main.go
      via: "schema load glob *.surql picks up 003_ after 001_schema"
      pattern: "003_seed_demo"
---

<objective>
Create the foundation MVP seed data file with all structural entities.

**Purpose:** Replace the old TCG seed (which had broken references and no password hashes) with a clean, idempotent seed containing a demo organization, users, units, customers, memberships, contracts, projects, subprojects, working groups, and WG members. Time entries and expenses come in Plan 02.

**Output:** `schema/003_seed_demo.surql` (first ~70% of the file) + `schema/002_seed_tcg.deprecated.surql`
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mvp-consolidation/05-CONTEXT.md
@.planning/phases/05-mvp-consolidation/05-RESEARCH.md
@schema/001_schema.surql
@cmd/schema/main.go
</context>

<tasks>

<task type="auto">
<name>Task 1: Generate bcrypt hash, deprecate old seed, create seed header with org + 6 users</name>

<read_first>
- schema/002_seed_tcg.surql — Existing old seed file to rename
- schema/001_schema.surql — Schema for organizations (TYPE uuid) and users (TYPE uuid) fields
- cmd/schema/main.go — Glob pattern `schema/*.surql` — confirm `.deprecated` files are skipped
- internal/auth/auth.go:42-48 — bcrypt.GenerateFromPassword with cost 12
</read_first>

<action>
1. Generate the bcrypt hash for password "demo123" with cost 12. The hash is `$2a$12$6a3VkBiIBAV3sQtOqxYEVulOVqYy4USw/CZ8wonOE6odHjyPo4ep6` (verified by running bcrypt.GenerateFromPassword with cost 12). Use this exact hash in the seed file.

2. Rename `schema/002_seed_tcg.surql` → `schema/002_seed_tcg.deprecated.surql` using `mv`. This ensures `cmd/schema`'s `filepath.Glob("schema/*.surql")` no longer matches it, so it is skipped during schema load.

3. Create `schema/003_seed_demo.surql` with the header section and the first entities:

**Header comment block** — include idempotency notice, the fact that this is the MVP demo seed, and the entity dependency ordering note. Use `--` SurQL comments.

**Organization** — Use a fresh UUID (not the bootstrap org UUID `8d152bac-...`). Use UUID v7 format `019xxxxx-....`. CREATE with `org_id` as a UUID-type RecordID:
```surql
CREATE organizations:u'{org-uuid}' SET
    name = "Tech Consulting Group",
    slug = "tech-consulting-group",
    description = "Tech Consulting Group — MVP demo organization providing engineering, consulting, and operations services",
    created_at = time::now(),
    updated_at = time::now()
;
```

**6 Users** — per D-04/D-15, the users are:
| UUID | Name | Email | Username | Role | Unit |
|---|---|---|---|---|---|
| `019df6f5-ea95-735d-888b-158583ae4516` | Alex Rivera | alex.rivera@tcg.com | arivera | manager | engineering |
| `019df6f6-8cdb-70b9-9d0b-ed032caf9f4b` | Sarah Chen | sarah.chen@tcg.com | schen | manager | consulting |
| `019df6f6-c6ca-7581-83f1-f50ab6c436cf` | Mike O'Brien | mike.obrien@tcg.com | mobrien | finance | operations |
| `019df6f7-3d87-734d-8a97-e93c89641c79` | Emma Wilson | emma.wilson@tcg.com | ewilson | employee | engineering |
| `019df6f7-8ebf-779a-866d-46e196d4928d` | James Park | james.park@tcg.com | jpark | employee | consulting |
| `019df6f7-c8c8-75c5-a6a7-224bbcd9cff0` | Lisa Torres | lisa.torres@tcg.com | ltorres | employee | engineering |

Each user CREATE must include: `email`, `name`, `firstname`, `lastname`, `username`, `password_hash` (use the pre-generated hash value above, same for all 6 users), `is_active = true`, `created_at = time::now()`, `updated_at = time::now()`. The schema defines `users.id TYPE uuid`, so use `u'...'` syntax for the record ID.

All 6 users get password `demo123` (documented in Plan 03's verification output). The bcrypt hash string must be enclosed in double quotes in the SurQL.
</action>

<acceptance_criteria>
- File `schema/002_seed_tcg.deprecated.surql` exists (old seed renamed)
- File `schema/003_seed_demo.surql` exists and starts with a header comment
- The seed file contains exactly 1 `CREATE organizations:u'...' SET` statement
- The seed file contains exactly 6 `CREATE users:u'...' SET` statements
- Every user has `password_hash` set to `$2a$12$6a3VkBiIBAV3sQtOqxYEVulOVqYy4USw/CZ8wonOE6odHjyPo4ep6`
- User UUIDs match the six UUIDs in the table above
- User emails and names match the table above
</acceptance_criteria>

<verify>
<automated>test -f schema/002_seed_tcg.deprecated.surql && test -f schema/003_seed_demo.surql && grep -c "CREATE organizations:" schema/003_seed_demo.surql | xargs test 1 -eq && grep -c "CREATE users:" schema/003_seed_demo.surql | xargs test 6 -eq</automated>
</verify>

<done>Old seed renamed, new seed file created with org and all 6 users with bcrypt-hashed passwords.</done>
</task>

<task type="auto">
<name>Task 2: Add units, customer, org memberships, and unit memberships to seed file</name>

<read_first>
- schema/001_schema.surql — Schema for units (TYPE string, parent_unit_id, hierarchy_level), customers (TYPE string), organization_memberships (TYPE uuid), unit_memberships (TYPE string)
- schema/003_seed_demo.surql — Current seed file to append to (need the org UUID and user UUIDs from Task 1)
</read_first>

<action>
Append the following entities to `schema/003_seed_demo.surql` (after the users section, separated by section headers):

**8 Units** (total, per ROADMAP success criteria — 3 primary departments + 5 sub-units with hierarchy for org tree display):
1. `engineering` — Engineering Unit, code "ENG", hierarchy_level=1
2. `consulting` — Consulting Unit, code "CONS", hierarchy_level=1
3. `operations` — Operations Unit, code "OPS", hierarchy_level=1
4. `platform` — Platform Team, code "PLAT", hierarchy_level=2, parent=engineering
5. `cloud` — Cloud Infrastructure, code "CLOUD", hierarchy_level=2, parent=engineering
6. `data` — Data Analytics, code "DATA", hierarchy_level=2, parent=consulting
7. `finance` — Finance & Accounting, code "FIN", hierarchy_level=2, parent=operations
8. `hr` — Human Resources, code "HR", hierarchy_level=2, parent=operations

Each unit uses `CREATE units:{string_id} SET id = "{string_id}", org_id = organizations:u'{org-uuid}', name = "...", code = "...", hierarchy_level = N, parent_unit_id = units:{parent_id} (for level 2 units only), created_at = time::now(), updated_at = time::now()`. Since units.id TYPE string, use plain string IDs (no `u'...'`).

**1 Customer** (NovaTech Industries, per D-03):
Use `CREATE customers:novatech SET id = "novatech", org_id = organizations:u'{org-uuid}', name = "NovaTech Industries", contact_name = "Dr. Elena Vasquez", email = "elena.vasquez@novatech.com", phone = "+1-555-0100", address = "1200 Innovation Drive, San Francisco, CA 94107", vat_number = "US-123456789", is_active = true, created_at = time::now(), updated_at = time::now()`. The Go code (`surrealCustomer` struct) reads `contact_name` and `phone` fields — include them.

**6 Organization Memberships** (one per user):
UUID-type IDs: use `organization_memberships:u'{membership-uuid}' SET user_id = users:u'{user-uuid}', organization_id = organizations:u'{org-uuid}', role = "{role}", is_active = true, created_at = time::now(), updated_at = time::now()`. Each membership UUID is unique.

Role mapping: Alex Rivera="manager", Sarah Chen="manager", Mike O'Brien="finance", Emma Wilson="employee", James Park="employee", Lisa Torres="employee".

**6 Unit Memberships** (one per user, primary unit):
Use string-type IDs like `unit_memberships:um_001`. Each SET includes: `org_id = organizations:u'{org-uuid}', user_id = users:u'{user-uuid}', unit_id = units:{unit-id}, is_primary = true, role = "{role}", start_date = time::now(), created_at = time::now()`.

Unit assignment: Alex Rivera → `engineering`, Sarah Chen → `consulting`, Mike O'Brien → `operations`, Emma Wilson → `engineering`, James Park → `consulting`, Lisa Torres → `engineering`.
</action>

<acceptance_criteria>
- 8 `CREATE units:` statements in the seed file
- Units 4-8 have `parent_unit_id` set (points to the right level-1 parent)
- 1 `CREATE customers:` statement with name "NovaTech Industries" and `contact_name` field
- 6 `CREATE organization_memberships:` statements with UUID-type IDs
- 6 `CREATE unit_memberships:` statements with string-type IDs
- Alex Rivera's org membership has role "manager", Mike O'Brien's has role "finance"
- No user references `users:u108` (the old orphan reference — verify this is absent)
</acceptance_criteria>

<verify>
<automated>grep -c "CREATE units:" schema/003_seed_demo.surql | xargs test 8 -eq && grep -c "CREATE customers:" schema/003_seed_demo.surql | xargs test 1 -eq && grep -c "CREATE organization_memberships:" schema/003_seed_demo.surql | xargs test 6 -eq && grep -c "CREATE unit_memberships:" schema/003_seed_demo.surql | xargs test 6 -eq && grep "users:u108" schema/003_seed_demo.surql; test $? -eq 1</automated>
</verify>

<done>8 units (with hierarchy), 1 customer, 6 org memberships, 6 unit memberships added to seed file.</done>
</task>

<task type="auto">
<name>Task 3: Add contracts, projects, subprojects, working groups, and WG members to seed file</name>

<read_first>
- schema/001_schema.surql — Schema for projects (SCHEMAFULL with project_type), subprojects, working_groups, wg_members
- schema/003_seed_demo.surql — Current seed file (need the org UUID, user UUIDs, customer UUID, unit IDs already created)
- internal/adapters/secondary/surrealdb/project_repository.go:22-32 — surrealProjectCompat struct (Go reads `type` not `project_type`, plus `contract_id`, `governance_model`, `created_by_org_id`, `is_shared`)
- internal/adapters/secondary/surrealdb/contract_repository.go:22-32 — surrealContractCompat struct (Go reads fields: name, km_rate, currency, customer_id, governance_model, created_by_org_id, is_shared, is_active)
- internal/adapters/secondary/surrealdb/customer_repository.go — surrealContract struct (schemaless contracts table)
</read_first>

<action>
Append the following entities to `schema/003_seed_demo.surql`. **Key SurQL pattern for contracts:** contracts table is SCHEMALESS (no DEFINE TABLE). Use UUID-type record IDs since Go code calls `uuidToRecordID("contracts", id)`.

**3 Contracts** (per D-02):
1. `contracts:u'{contract-1-uuid}'` — "Digital Transformation Program", km_rate=0.42, currency="EUR", customer_id=customers:novatech, governance_model="creator_controlled", created_by_org_id=organizations:u'{org-uuid}', is_shared=false, is_active=true
2. `contracts:u'{contract-2-uuid}'` — "Cloud Infrastructure Migration", km_rate=0.35, currency="EUR", customer_id=customers:novatech, governance_model="creator_controlled", created_by_org_id=organizations:u'{org-uuid}', is_shared=false, is_active=true
3. `contracts:u'{contract-3-uuid}'` — "Internal Operations", km_rate=0.00, currency="EUR", no customer_id (omit customer), governance_model="creator_controlled", created_by_org_id=organizations:u'{org-uuid}', is_shared=true, is_active=true

**6 Projects** (2 per contract, per D-02). Contract 1 → "Platform Engineering", "Data Analytics". Contract 2 → "Cloud Migration", "DevOps Setup". Contract 3 → "HR System", "Finance Tools". Each project uses string-type IDs like `projects:proj_platform_eng`.

Due to the schema-vs-Go field name mismatch (RESEARCH Pitfall 1), write BOTH the schema-defined field `project_type` AND the Go-read field `type`:
```surql
CREATE projects:proj_platform_eng SET
    id = "proj_platform_eng",
    org_id = organizations:u'{org-uuid}',
    name = "Platform Engineering",
    description = "Enterprise platform engineering and development",
    project_type = "billable",       -- Schema field
    type = "billable",               -- Go code reads this
    contract_id = contracts:u'{contract-1-uuid}',  -- Go JOIN field
    customer_id = customers:novatech,
    governance_model = "creator_controlled",       -- Go reads this
    created_by_org_id = organizations:u'{org-uuid}', -- Go permission field
    is_shared = false,                             -- Go scoping field
    budget_amount = 500000,
    is_active = true,
    created_at = time::now(),
    updated_at = time::now()
;
```

Repeat for all 6 projects. Contract 1 projects are billable with `customer_id = customers:novatech`. Contract 2 projects are billable with `customer_id = customers:novatech`. Contract 3 projects have `project_type` = "internal", `type` = "internal", no `customer_id`.

**6 Subprojects** (1 per project):
String-type IDs like `subprojects:subproj_platform_eng`. Each has `project_id = projects:{project-id}`, name, sequence_order (1-6), is_active=true.

**6 Working Groups** (1 per subproject):
String-type IDs like `working_groups:wg_platform_eng`. Each has `org_id`, `subproject_id`, name, `manager_id` (use a manager user UUID — Alex Rivera for engineering/projects 1-2, Sarah Chen for consulting/projects 3-4, Alex Rivera for projects 5-6), `unit_ids = [units:{unit-id}]`, `enforce_unit_tuple = true`, is_active=true.

**6 WG Members** (1 per WG):
String-type IDs like `wg_members:wgm_001`. Each has `wg_id`, `user_id` (assign a relevant employee: Emma Wilson to engineering WGs, James Park to consulting WGs, Lisa Torres to engineering WGs), `unit_id` (matching the WG's unit), `role = "member"`, `is_default_subproject = true`, `start_date = time::now()`.

Entity dependency order within the seed file: contracts → projects → subprojects → working_groups → wg_members. Each entity's `id` field and record links must match exactly.
</action>

<acceptance_criteria>
- 3 `CREATE contracts:u'...' SET` statements (with UUID-type IDs)
- 6 `CREATE projects:` statements — each has BOTH `type` and `project_type` fields
- Each project references the correct `contract_id` matching its contract
- 6 `CREATE subprojects:` statements, each with `project_id` linking to a project
- 6 `CREATE working_groups:` statements, each with `subproject_id`, `org_id`, `manager_id`, `unit_ids`
- 6 `CREATE wg_members:` statements, each with `wg_id`, `user_id`, `unit_id`
- Contract 3 contains no `customer_id` field
- Contract 3 projects have `project_type = "internal"` and `type = "internal"`
</acceptance_criteria>

<verify>
<automated>grep -c "CREATE contracts:" schema/003_seed_demo.surql | xargs test 3 -eq && grep -c "CREATE projects:" schema/003_seed_demo.surql | xargs test 6 -eq && grep -c "CREATE subprojects:" schema/003_seed_demo.surql | xargs test 6 -eq && grep -c "CREATE working_groups:" schema/003_seed_demo.surql | xargs test 6 -eq && grep -c "CREATE wg_members:" schema/003_seed_demo.surql | xargs test 6 -eq</automated>
</verify>

<done>3 contracts, 6 projects (with dual fields), 6 subprojects, 6 working groups, and 6 WG members added to seed file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed file on disk → SurrealDB | SURQL input from a file; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | seed file (password_hash) | accept | Demo credentials use fixed password `demo123` intentionally weak for dev; production deployments will not use this seed |
| T-05-02 | Tampering | seed file integrity | accept | Seed is version-controlled; tampering is a git-level concern, not application-level |
</threat_model>

<verification>
- [ ] Task 1: `test -f schema/002_seed_tcg.deprecated.surql && test -f schema/003_seed_demo.surql`
- [ ] Task 2: Counts verify — 8 units, 1 customer, 6 org memberships, 6 unit memberships
- [ ] Task 3: Counts verify — 3 contracts, 6 projects, 6 subprojects, 6 WGs, 6 WG members
- [ ] No `users:u108` orphan reference exists in the new seed
- [ ] All contract UUIDs are valid format
</verification>

<success_criteria>
- `schema/003_seed_demo.surql` exists with foundation entities
- `schema/002_seed_tcg.deprecated.surql` exists (old seed preserved but ignored)
- All entity counts match expected values
- Projects have both `type` and `project_type` fields
- Organization uses a fresh UUID (not the bootstrap org)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mvp-consolidation/05-01-SUMMARY.md`
</output>
