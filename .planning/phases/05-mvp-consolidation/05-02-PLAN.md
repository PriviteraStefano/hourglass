---
phase: 05-mvp-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - schema/003_seed_demo.surql
autonomous: true
requirements:
  - D-01
  - D-16
  - D-17
user_setup: []

must_haves:
  truths:
    - "Time entries exist (3-5 per employee = 9-15 total) showing past week work"
    - "Expenses exist (1-2 per employee = 3-6 total)"
    - "Time entries link to correct user via user_id record link"
    - "Time entries have all 6 required record links: org_id, user_id, project_id, subproject_id, wg_id, unit_id"
    - "Expenses link to correct user and have valid category values"
    - "Schema loads without errors when time_entries and expenses are added"
  artifacts:
    - path: schema/003_seed_demo.surql
      provides: "Complete MVP demo seed data including time entries and expenses"
      min_lines: 450
  key_links:
    - from: schema/003_seed_demo.surql
      to: schema/001_schema.surql
      via: "time_entries and expenses fields must match SCHEMAFULL definition"
      pattern: "time_entries:|expenses:"
---

<objective>
Append sample time entries (3-5 per employee) and expenses (1-2 per employee) to the seed file, completing the demo dataset.

**Purpose:** Make the time tracking and expense pages immediately demonstrable by seeding real-looking entries from the past week. Time entries are in "submitted" status (showing approval workflow). Expenses include "mileage" and "meal" categories.

**Output:** Completed `schema/003_seed_demo.surql` with all entities
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@schema/001_schema.surql
@schema/003_seed_demo.surql
@internal/adapters/secondary/surrealdb/models.go
</context>

<tasks>

<task type="auto">
<name>Task 1: Append time entries (3-5 per employee) and expenses (1-2 per employee) to seed file</name>

<read_first>
- schema/003_seed_demo.surql — Current seed file. Read to extract the exact record IDs (UUIDs) for: org, all 6 users, all 6 projects, all 6 subprojects, all 6 working groups, all 8 units. These IDs are REQUIRED to create correct record links in time entries and expenses.
- schema/001_schema.surql — Schema for time_entries (6 mandatory record links + hours > 0 + description + entry_date + status) and expenses (org_id, user_id, project_id optional, unit_id, category, amount, currency, expense_date, status)
</read_first>

<action>
Read `schema/003_seed_demo.surql` to extract ALL record IDs created by Plan 01. Then append time entries and expenses to the same file. The file is now complete — this is the final append.

**Entity dependency note:** Since the seed file was written in Plan 01 with specific IDs for org, users, units, projects, subprojects, and WGs, you must use those EXACT IDs. Read the file to extract them.

**Time entries** — Per D-16, 3-5 per employee (Emma Wilson, James Park, Lisa Torres). Target 4 per employee = 12 total. Each time entry needs all 6 required record link fields (per `time_entries` SCHEMAFULL):
- `org_id` → organizations:u'{org-uuid}'
- `user_id` → users:u'{user-uuid}' (employee who logged the time)
- `project_id` → projects:{project-id} (relevant project)
- `subproject_id` → subprojects:{subproject-id} (matching subproject)
- `wg_id` → working_groups:{wg-id} (matching WG)
- `unit_id` → units:{unit-id} (employee's unit)

Use string-type IDs like `time_entries:te_001`. Each sets: `hours` (between 4.0 and 8.0), `description` (realistic work descriptions), `entry_date` (past week dates in `<datetime>"YYYY-MM-DDTHH:MM:SSZ"` format), `status = "submitted"`, `is_deleted = false`, `created_at = time::now()`, `updated_at = time::now()`.

Spread entries across the past 5 business days. Vary descriptions: "Frontend dashboard development", "API endpoint implementation", "Code review and QA testing", "Sprint planning and team sync", "Database optimization work", "Client requirements analysis", "Integration testing", "Documentation updates", etc.

Assignment guide:
- **Emma Wilson** (engineering): projects 1-2 (Platform Engineering, Data Analytics), unit=engineering
- **James Park** (consulting): projects 3-4 (Cloud Migration, DevOps Setup), unit=consulting
- **Lisa Torres** (engineering): projects 1, 2, 5 (Platform Engineering, Data Analytics, HR System), unit=engineering

**Expenses** — Per D-17, 1-2 per employee = 3-6 total. Target 2 per employee = 6 expenses.

Use string-type IDs like `expenses:exp_001`. Each sets:
- `org_id`, `user_id`, `unit_id` (same as time entries)
- `project_id` → optional, set where relevant
- `category` → "mileage" or "meal" (valid values per schema: 'mileage', 'meal', 'accommodation', 'other')
- `amount` → realistic amounts (15.50, 45.00, 120.00, etc.)
- `currency = "EUR"`
- `description` → realistic descriptions
- `expense_date` → past week formatted as `<datetime>"..."` 
- `status = "submitted"`, `is_deleted = false`

Sample expense ideas:
- Emma: "Client site visit mileage" (45km × 0.42 = 18.90 EUR, category mileage), "Team lunch meeting" (32.50 EUR, category meal)
- James: "Travel to client office" (120km × 0.42 = 50.40 EUR, category mileage), "Business dinner" (68.00 EUR, category meal)
- Lisa: "Conference parking" (15.00 EUR, category mileage), "Office supplies" (45.00 EUR, category other)
</action>

<acceptance_criteria>
- At least 9 time entries (3 per employee × 3 employees) and at most 15 (5 per employee × 3 employees)
- Each time entry has all 6 required record links: org_id, user_id, project_id, subproject_id, wg_id, unit_id
- Each time entry has `hours > 0` (float), `description` (non-empty string), `entry_date` (datetime), `status` (string)
- Time entry IDs are string type and unique (e.g., `te_001` through `te_012`)
- At least 3 expenses (1 per employee) and at most 6 (2 per employee)
- Each expense has valid `category` from the allowed list: mileage, meal, accommodation, other
- Each expense has `amount > 0`
- All `project_id` record links point to existing project IDs from the seed
- All `user_id` record links point to one of the 3 employee UUIDs
- File parses as valid SurQL (each statement ends with `;`)
</acceptance_criteria>

<verify>
<automated>COUNT_TE=$(grep -c "CREATE time_entries:" schema/003_seed_demo.surql); COUNT_EXP=$(grep -c "CREATE expenses:" schema/003_seed_demo.surql); test "$COUNT_TE" -ge 9 -a "$COUNT_TE" -le 15 && test "$COUNT_EXP" -ge 3 -a "$COUNT_EXP" -le 6 && echo "PASS: ${COUNT_TE} time entries, ${COUNT_EXP} expenses"</automated>
</verify>

<done>Sample time entries (9-15) and expenses (3-6) appended to seed file, completing the full seed dataset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed file → SurrealDB | SurQL loaded from file, no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | seed file | accept | Time entry and expense data are sample demo data only, no real PII |
</threat_model>

<verification>
- [ ] Time entry count between 9-15
- [ ] Expense count between 3-6
- [ ] Each time entry has all 6 record link fields
- [ ] Each expense has valid `category` value
- [ ] IDs referenced in record links exist in earlier seed sections
- [ ] **Run** `go run ./cmd/schema` to verify seed file loads without errors
</verification>

<success_criteria>
- Complete `003_seed_demo.surql` with all entities: org, 6 users, 8 units, 1 customer, 6 org memberships, 6 unit memberships, 3 contracts, 6 projects, 6 subprojects, 6 working groups, 6 WG members, 9-15 time entries, 3-6 expenses
- `go run ./cmd/schema` succeeds with no errors
- Schema loading log shows `001_schema.surql` and `003_seed_demo.surql` applied
</success_criteria>

<output>
Run `go run ./cmd/schema` to verify the complete seed file loads. The schema load should show:
```
Applying schema: 001_schema.surql
Applied: 001_schema.surql
Applying schema: 003_seed_demo.surql
Applied: 003_seed_demo.surql
Schema migration completed successfully
```

After completion, create `.planning/phases/05-mvp-consolidation/05-02-SUMMARY.md`
</output>
