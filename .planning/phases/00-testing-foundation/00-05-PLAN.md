---
phase: 00-testing-foundation
plan: 05
type: execute
wave: 5
depends_on: [00-04]
files_modified:
  - internal/core/services/auth/auth.go
  - internal/adapters/primary/http/auth.go
  - internal/core/services/unit/unit.go
  - internal/adapters/primary/http/unit.go
  - internal/core/services/organization/organization.go
  - internal/adapters/primary/http/organization.go
  - internal/core/services/time_entry/time_entry.go
  - internal/adapters/primary/http/time_entry.go
  - (others as needed)
autonomous: false
requirements: [TEST-05]
must_haves:
  truths:
    - "All bugs discovered during test rewrites are addressed"
    - "No t.Skip() remains in the test suite for unaddressed bugs"
    - "Full test suite passes: go test -v ./internal/..."
    - "Human-in-the-middle review completed with sign-off"
    - "All addressed bugs have test verification"
  artifacts:
    - path: "internal/adapters/primary/http/handler_test_helper.go"
      provides: "Updated if needed"
    - path: "internal/adapters/primary/http/*_test.go"
      provides: "All tests green"
  key_links:
    - from: "bug log"
      to: "fixed files"
      via: "per-bug commits"
      pattern: "fix:"
---

<objective>
Batch-fix all bugs discovered during the PostgreSQL test rewrite.

Purpose: During Plans 03 and 04, complex bugs may be discovered that can't be fixed inline (per D-14). This dedicated plan triages each documented bug, fixes it with test verification, and runs a human-in-the-middle review pass (D-15) before declaring the test suite clean.

Output: All documented bugs fixed, human review completed, full test suite green with no skipped tests.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md
@.planning/phases/00-testing-foundation/00-VALIDATION.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Triage and fix all documented bugs from test rewrite</name>
  <files>
    internal/core/services/auth/auth.go
    internal/adapters/primary/http/auth.go
    internal/core/services/unit/unit.go
    internal/adapters/primary/http/unit.go
    internal/core/services/organization/organization.go
    internal/adapters/primary/http/organization.go
    internal/core/services/time_entry/time_entry.go
    internal/adapters/primary/http/time_entry.go
    (others as needed)
  </files>
  <read_first>
    # Read the bug log created during Plans 03-04 (if any t.Skip calls or documented issues)
    # If no bug log exists, run the full suite and check for any remaining failures:
    @internal/adapters/primary/http/*_test.go
    @internal/core/services/*/*_integration_test.go
  </read_first>
  <action>
    **Phase A: Gather documented bugs**

    Run `grep -rn 't.Skip.*Plan 05' ./internal/...` to find all tests skipped for Plan 05. Also grep for `TODO` and `FIXME` comments related to bugs.

    If there are no skipped tests, verify the full suite is green with:
    ```bash
    go test -v -count=1 -timeout 600s ./internal/...
    ```

    If the suite is already green, this plan is complete. Move to Task 2 (human review).

    **Phase B: Fix found bugs**

    For each documented bug:

    1. Read the source code and understand the root cause (use the existing CONCERNS.md analysis as reference).
    2. Write or update a test that reproduces the bug in the appropriate `*_integration_test.go` file.
    3. Fix the production code.
    4. Run the specific test to verify it passes.
    5. Commit with message format: `fix(00-05): {description} — closes bug #N`

    **Common bug patterns to watch for** (from CONCERNS.md that may not have been caught in earlier plans):
    - Time entry DB status CHECK constraint mismatch (CONCERNS.md line 106-118): The schema only allows 'draft', 'submitted', 'approved' but code uses 'pending_manager', 'pending_finance', 'rejected'.
    - Bogus error on Register with bad OrgID (CONCERNS.md line 120-128): uuid.Parse silently ignores errors.
    - Unhandled JSON decode error in TimeEntry Reject handler (CONCERNS.md line 130-138).
    - In-memory only rate limiting map growth (CONCERNS.md line 221-229).

    **For each fix:**
    - Use `tdd="true"` — write the test first, then fix, then verify
    - Add a `<behavior>` block in the task action for the specific fix
    - If the bug is truly out of scope for Phase 0 (e.g., requires new schema migration), document it and move it to the Known Issues section for future milestones.
  </action>
  <acceptance_criteria>
    <checks>
      - All documented bugs from Plans 03-04 have either been fixed with test verification or documented as deferred
      - No `t.Skip('Plan 05')` remains in any test file
      - `go test -v -count=1 -timeout 600s ./internal/...` is green
      - Each fix has a corresponding test (in an _integration_test.go or _test.go file)
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 -timeout 600s ./internal/...</automated>
  </verify>
  <done>
    - All written bugs fixed with test verification
    - No t.Skip('Plan 05') left in codebase
    - Full suite green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-in-the-middle review pass</name>
  <files>
    (nothing modified — review only)
  </files>
  <read_first>
    (none)
  </read_first>
  <action>
    Per D-15, this checkpoint provides a human review opportunity for the bug-fixing buffer.

    The executor presents a summary of:
    1. All bugs that were fixed (with before/after behavior)
    2. All tests that were written or updated to verify fixes
    3. Any bugs that were deferred (with rationale)
    4. Current test suite status

    Run the full test suite to demonstrate green status before the review.
  </action>
  <what-built>
    All bug fixes from the PostgreSQL test rewrite. The full suite has been run and is green.
  </what-built>
  <how-to-verify>
    1. Review the commit log: `git log --oneline -20` — verify fix commits are well-described
    2. Run the full test suite locally: `go test -v -count=1 -timeout 600s ./internal/...`
    3. Verify there are no `t.Skip()` calls left for Plan 05: `grep -rn 'Plan 05' ./internal/...`
    4. Review the diff for any concerning patterns: `git diff --stat`
  </how-to-verify>
  <resume-signal>
    Type "approved" to sign off on all bug fixes, or describe specific concerns to address.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Address review feedback and final green suite</name>
  <files>
    (various — per review feedback)
  </files>
  <read_first>
    (none — read review comments first)
  </read_first>
  <action>
    Address any feedback from the human review (Task 2). 

    If the review is approved without changes:
    1. Run the final full suite: `go test -v -count=1 -timeout 600s ./...`
    2. Verify the smoke test still passes: `go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...`
    3. Confirm no `t.Skip('Plan 05')` or other blockers remain

    If the review identified issues:
    1. Fix each issue with test-driven approach
    2. Re-run the full suite
    3. Present for follow-up if needed
  </action>
  <acceptance_criteria>
    <checks>
      - `go test -v -count=1 -timeout 600s ./internal/...` green
      - `go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...` green
      - No skipped tests for Plan 05
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 -timeout 600s ./internal/...</automated>
  </verify>
  <done>
    - All review feedback addressed
    - Full test suite green
    - Ready for E2E verification (Plan 06)
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| Bug fix commits | git commits | various production files | All subsequent phases |
| (Updated tests) | test files | various `*_test.go` files | CI pipeline |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bug fix code → test | All fixes verified by testcontainer-backed tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-05-01 | Tampering | Bug fix changes | mitigate | All fixes are test-driven (tdd=true). Each fix must have a passing test before commit. |
| T-00-05-02 | Spoofing | Human review bypass | mitigate | This plan has `autonomous: false` — the human-in-the-middle checkpoint (Task 2, gate: blocking) prevents auto-advance. |
</threat_model>

<verification>
1. `grep -rn 't.Skip.*Plan 05' ./internal/...` returns nothing
2. `go test -v -count=1 -timeout 600s ./internal/...` exits 0
3. `go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...` exits 0
4. Human review sign-off recorded
</verification>

<success_criteria>
- All documented bugs fixed with test verification
- Human-in-the-middle review completed
- No t.Skip remains for unaddressed bugs
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/00-testing-foundation/00-05-SUMMARY.md` when done
</output>
