---
phase: 00-testing-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 00-01
files_modified:
  - internal/adapters/primary/http/contract_test.go
  - internal/adapters/primary/http/customer_test.go
  - internal/adapters/primary/http/project_test.go
  - internal/adapters/primary/http/organization_test.go
  - internal/adapters/primary/http/unit_test.go
  - internal/adapters/primary/http/working_group_test.go
  - internal/adapters/primary/http/invitation_test.go
  - internal/adapters/primary/http/password_reset_test.go
  - internal/adapters/primary/http/export_test.go
  - internal/adapters/primary/http/time_entry_test.go (extend existing)
autonomous: true
requirements: []
must_haves:
  truths:
    - "Contract handler tests cover create, list, get, update, delete with auth context"
    - "Customer handler tests cover CRUD with auth + validation error responses"
    - "Project handler tests cover CRUD with auth context and contract association"
    - "Organization handler tests cover create org, list orgs, get org, update settings"
    - "Unit handler tests cover CRUD with org-scoped queries"
    - "Working Group handler tests cover CRUD with org-scoped queries"
    - "Invitation handler tests cover create, accept, reject, list"
    - "Password Reset handler tests cover request reset and reset password"
    - "Export handler tests cover export generation"
    - "Time Entry handler tests are extended beyond the current nil-service skeleton"
    - "All handler tests use the existing httptest.Server + GetTestDBWithNamespace pattern OR nil-service pattern for pure HTTP validation"
  artifacts:
    - path: "internal/adapters/primary/http/contract_test.go"
      provides: "Contract handler integration tests"
      min_lines: 80
    - path: "internal/adapters/primary/http/customer_test.go"
      provides: "Customer handler integration tests"
      min_lines: 80
    - path: "internal/adapters/primary/http/project_test.go"
      provides: "Project handler integration tests"
      min_lines: 80
    - path: "internal/adapters/primary/http/time_entry_test.go"
      provides: "Extended time entry handler tests"
      min_lines: 120
  key_links:
    - from: "internal/adapters/primary/http/*_test.go"
      to: "internal/adapters/primary/http/*.go"
      via: "New*Handler call"
      pattern: "New\\w+Handler"
    - from: "internal/adapters/primary/http/*_test.go"
      to: "internal/middleware/middleware.go"
      via: "middleware.SetUserID / middleware.SetRole"
      pattern: "middleware\\.Set"
---

<objective>
Plan 05: Backend handler integration tests for all HTTP handler domains. Extends existing auth_test.go and time_entry_test.go patterns to cover all 10 handler files.

Purpose: Every handler gets HTTP-level tests following the existing pattern (D-08, all three layers). Tests verify request parsing, auth context injection, validation error responses, and integration with services. Some handler tests use nil-service pattern for pure HTTP validation (following time_entry_test.go pattern), while complex handlers use the full GetTestDBWithNamespace setup (following auth_test.go pattern).

Output: 10 new/extended handler test files
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md (# Existing Test Inventory, Architecture Patterns)

# Reference handler test patterns
@internal/adapters/primary/http/auth_test.go (lines 1-100: testServer setup, uniqueID/email/orgName helpers, post/get helpers)
@internal/adapters/primary/http/time_entry_test.go (pattern: nil-service handler, middleware context injection, httptest.NewRequest/Recorder)

# Source handler files (interfaces under test)
@internal/adapters/primary/http/contract.go
@internal/adapters/primary/http/customer.go
@internal/adapters/primary/http/project.go
@internal/adapters/primary/http/organization.go
@internal/adapters/primary/http/unit.go
@internal/adapters/primary/http/working_group.go
@internal/adapters/primary/http/invitation.go
@internal/adapters/primary/http/password_reset.go
@internal/adapters/primary/http/export.go
@internal/adapters/primary/http/time_entry.go

# Domain models for request/response structures
@internal/models/models.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract, Customer, and Project handler tests</name>
  <files>
    internal/adapters/primary/http/contract_test.go
    internal/adapters/primary/http/customer_test.go
    internal/adapters/primary/http/project_test.go
  </files>
  <read_first>
    @internal/adapters/primary/http/contract.go
    @internal/adapters/primary/http/customer.go
    @internal/adapters/primary/http/project.go
    @internal/adapters/primary/http/time_entry_test.go
    @internal/middleware/middleware.go
  </read_first>
  <action>
    Follow the time_entry_test.go pattern (nil-service handler + middleware context injection) for these handler tests. All three handlers accept services via constructor — passing nil is fine for testing HTTP validation only (as D-16 says, business rules are tested at service layer, not handler).

    Package: `package http` (same-package test, Go convention).

    Import: `testing`, `net/http`, `net/http/httptest`, `strings`, `github.com/google/uuid`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`, `github.com/stefanoprivitera/hourglass/internal/middleware`.

    Create `internal/adapters/primary/http/contract_test.go`:

    Handler: `h := NewContractHandler(nil)` (nil service → tests HTTP validation only).

    Table-driven tests using `t.Run`:

    1. `TestContractHandler_Create_InvalidBody` — malformed JSON, expect 400
    2. `TestContractHandler_Create_MissingName` — valid JSON but empty name, expect 400
    3. `TestContractHandler_Create_MissingAuth` — no middleware context, expect 401 or error
    4. `TestContractHandler_List_NoAuth` — missing user context, expect 401
    5. `TestContractHandler_Get_NotFound` — valid UUID but no entry (nil service returns nil), expect 404
    6. `TestContractHandler_Update_InvalidBody` — malformed JSON, expect 400
    7. `TestContractHandler_Delete_NoAuth` — expect 401

    Create `internal/adapters/primary/http/customer_test.go`:

    Handler: `h := NewCustomerHandler(nil)`.

    Same pattern: Create (invalid body, missing name, missing auth), List (no auth), Get (not found), Update (invalid body), Delete (no auth).

    Create `internal/adapters/primary/http/project_test.go`:

    Handler: `h := NewProjectHandler(nil)`.

    Same pattern: Create (invalid body, missing name/type, missing auth), List (no auth), Get (not found), Update, Delete.

    For all three, set auth context using middleware.SetUserID, middleware.SetOrganizationID, middleware.SetRole in each test where auth is expected.
  </action>
  <acceptance_criteria>
    - `go test ./internal/adapters/primary/http/... -count=1 -run "Contract" -v` passes
    - `go test ./internal/adapters/primary/http/... -count=1 -run "Customer" -v` passes
    - `go test ./internal/adapters/primary/http/... -count=1 -run "Project" -v` passes
    - Each handler has tests for: invalid body (400), missing auth context (4xx), not found (404 for Get)
    - test files don't panic on nil service (nil-safe handler calls)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/adapters/primary/http/... -count=1 -run "Contract|Customer|Project" -v 2>&1 | tail -20</automated>
  </verify>
  <done>Contract, Customer, Project handler tests pass: invalid body, missing auth, not-found cases all green.</done>
</task>

<task type="auto">
  <name>Task 2: Organization, Unit, Working Group handler tests</name>
  <files>
    internal/adapters/primary/http/organization_test.go
    internal/adapters/primary/http/unit_test.go
    internal/adapters/primary/http/working_group_test.go
  </files>
  <read_first>
    @internal/adapters/primary/http/organization.go
    @internal/adapters/primary/http/unit.go
    @internal/adapters/primary/http/working_group.go
  </read_first>
  <action>
    Same nil-service pattern as Task 1.

    Create `internal/adapters/primary/http/organization_test.go`:

    Handler: `h := NewOrganizationHandler(nil)`.

    Tests:
    1. `TestOrganizationHandler_Create_InvalidBody` — malformed JSON. Expect 400
    2. `TestOrganizationHandler_Create_MissingName` — empty name. Expect 400
    3. `TestOrganizationHandler_Create_MissingAuth` — no middleware. Expect 401/error
    4. `TestOrganizationHandler_List_NoAuth` — expect 401
    5. `TestOrganizationHandler_Get_NotFound` — nil service returns nil. Expect 404
    6. `TestOrganizationHandler_UpdateSettings_InvalidBody` — malformed JSON. Expect 400

    Create `internal/adapters/primary/http/unit_test.go`:

    Handler: `h := NewUnitHandler(nil)`.

    Tests: Create (invalid body, missing name, missing auth), List (no auth), Get (not found), Update, Delete.

    Create `internal/adapters/primary/http/working_group_test.go`:

    Handler: `h := NewWorkingGroupHandler(nil)`.

    Tests: Create (invalid body, missing name, missing auth), List (no auth), Get (not found), Update, Delete.
  </action>
  <acceptance_criteria>
    - `go test ./internal/adapters/primary/http/... -count=1 -run "Organization|Unit|WorkingGroup" -v` passes
    - Tests cover: invalid body, missing required fields, missing auth context (401/error), not found (404)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/adapters/primary/http/... -count=1 -run "Organization|Unit|WorkingGroup" -v 2>&1 | tail -15</automated>
  </verify>
  <done>Organization, Unit, Working Group handler tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Invitation, Password Reset, Export, and extended Time Entry handler tests</name>
  <files>
    internal/adapters/primary/http/invitation_test.go
    internal/adapters/primary/http/password_reset_test.go
    internal/adapters/primary/http/export_test.go
    internal/adapters/primary/http/time_entry_test.go (extend)
  </files>
  <read_first>
    @internal/adapters/primary/http/invitation.go
    @internal/adapters/primary/http/password_reset.go
    @internal/adapters/primary/http/export.go
    @internal/adapters/primary/http/time_entry.go
  </read_first>
  <action>
    Nil-service pattern for all — same as Tasks 1-2.

    Create `internal/adapters/primary/http/invitation_test.go`:

    Handler: `h := NewInvitationHandler(nil)`.

    Tests:
    1. `TestInvitationHandler_Create_InvalidBody` — malformed JSON. Expect 400
    2. `TestInvitationHandler_Create_MissingEmail` — valid JSON, empty email. Expect 400
    3. `TestInvitationHandler_Create_MissingAuth` — no auth context. Expect 401
    4. `TestInvitationHandler_Accept_InvalidBody` — malformed. Expect 400
    5. `TestInvitationHandler_Reject_InvalidBody` — malformed. Expect 400
    6. `TestInvitationHandler_List_NoAuth` — expect 401

    Create `internal/adapters/primary/http/password_reset_test.go`:

    Handler: `h := NewPasswordResetHandler(nil)`.

    Tests:
    1. `TestPasswordResetHandler_Request_InvalidBody` — malformed JSON. Expect 400
    2. `TestPasswordResetHandler_Request_MissingEmail` — empty email. Expect 400
    3. `TestPasswordResetHandler_Reset_InvalidBody` — malformed JSON. Expect 400
    4. `TestPasswordResetHandler_Reset_MissingToken` — valid JSON, empty token. Expect 400
    5. `TestPasswordResetHandler_Reset_WeakPassword` — password too short. Expect 400

    Create `internal/adapters/primary/http/export_test.go`:

    Handler: `h := NewExportHandler(nil)`.

    Tests:
    1. `TestExportHandler_Export_InvalidBody` — malformed JSON. Expect 400
    2. `TestExportHandler_Export_MissingAuth` — no auth context. Expect 401
    3. `TestExportHandler_ListExports_NoAuth` — expect 401

    Extend `internal/adapters/primary/http/time_entry_test.go` (edit existing file):

    The existing file has 3 tests. Add the following table-driven tests (append after existing tests):

    1. `TestTimeEntryHandler_List_NoAuth` — no middleware context. Expect 401
    2. `TestTimeEntryHandler_Get_InvalidID` — non-UUID string in path. Expect 400
    3. `TestTimeEntryHandler_Get_NotFound` — valid UUID but no entry (nil service returns nil). Expect 404
    4. `TestTimeEntryHandler_Update_InvalidBody` — malformed JSON. Expect 400
    5. `TestTimeEntryHandler_Delete_NoAuth` — expect 401
    6. `TestTimeEntryHandler_SubmitMonth_InvalidBody` — malformed JSON. Expect 400
    7. `TestTimeEntryHandler_BatchApprove_InvalidBody` — malformed JSON. Expect 400
    8. `TestTimeEntryHandler_Approve_EmployeeForbidden` — (already exists, keep as-is)
    9. `TestTimeEntryHandler_Reject_NoAuth` — no auth. Expect 401

    Preserve the 3 existing tests — add new tests after them.
  </action>
  <acceptance_criteria>
    - `go test ./internal/adapters/primary/http/... -count=1 -run "Invitation|PasswordReset|Export|TimeEntry" -v` passes
    - Existing 3 time_entry_test.go tests are preserved (not modified)
    - New time_entry_test.go tests add at least 7 new test functions
    - Invitation, PasswordReset, Export handlers each have 3+ test cases
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/adapters/primary/http/... -count=1 -run "Invitation|PasswordReset|Export|TimeEntry" -v 2>&1 | tail -25</automated>
  </verify>
  <done>Invitation, PasswordReset, Export, and Time Entry handler tests pass. All existing tests preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→HTTP handler →service | Handler tests validate auth context injection (middleware.SetUserID, SetRole). Tests prove unauthenticated requests are rejected at HTTP boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-09 | S | All handler tests | mitigate | Tests verify missing auth context returns 4xx for every handler |
| T-00-10 | I | Input validation handlers | mitigate | Tests verify malformed JSON and missing required fields return 400 |
</threat_model>

<verification>
- `go test ./internal/adapters/primary/http/... -count=1 -v` passes all handler tests (existing + new)
- Each new handler has at least: invalid body (400), missing auth (4xx), not found (404) test cases
- All nil-service handlers test correctly (no panics on nil service)
</verification>

<success_criteria>
- All 10 handler files have test coverage
- auth_test.go (24 tests) and time_entry_test.go (3→10+ tests) are extended where needed
- Missing auth context returns 401 for all protected handlers
- Malformed/missing request body returns 400 for all create/update handlers
</success_criteria>

<output>
After completion, create `.planning/phases/00-testing-foundation/00-05-SUMMARY.md`
</output>
