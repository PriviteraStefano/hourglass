---
phase: 00-testing-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - internal/core/services/contract/contract_test.go
  - internal/core/services/customer/customer_test.go
  - internal/core/services/project/project_test.go
  - internal/core/services/unit/unit_test.go
  - internal/core/services/working_group/working_group_test.go
  - internal/core/services/invitation/invitation_test.go
  - internal/core/services/password_reset/password_reset_test.go
  - internal/core/services/export/export_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Contract service tests cover CRUD, governance model validation, customer assignment"
    - "Customer service tests cover CRUD, organization-scoped queries, activation/deactivation"
    - "Project service tests cover CRUD, contract association, type validation, billable/internal"
    - "Unit service tests cover CRUD, org-scoped queries"
    - "Working Group service tests cover CRUD, org-scoped queries"
    - "Invitation service tests cover create invite, accept/reject, role assignment"
    - "Password Reset service tests cover request reset, reset with token, expired token handling"
    - "Export service tests cover export generation, format selection"
    - "All service tests use table-driven pattern (D-17) with mock repos from testdata package"
  artifacts:
    - path: "internal/core/services/contract/contract_test.go"
      provides: "Contract service table-driven tests"
      min_lines: 80
    - path: "internal/core/services/customer/customer_test.go"
      provides: "Customer service table-driven tests"
      min_lines: 80
    - path: "internal/core/services/project/project_test.go"
      provides: "Project service table-driven tests"
      min_lines: 80
    - path: "internal/core/services/unit/unit_test.go"
      provides: "Unit service table-driven tests"
      min_lines: 60
    - path: "internal/core/services/working_group/working_group_test.go"
      provides: "Working group service table-driven tests"
      min_lines: 60
    - path: "internal/core/services/invitation/invitation_test.go"
      provides: "Invitation service table-driven tests"
      min_lines: 60
    - path: "internal/core/services/password_reset/password_reset_test.go"
      provides: "Password reset service table-driven tests"
      min_lines: 60
    - path: "internal/core/services/export/export_test.go"
      provides: "Export service table-driven tests"
      min_lines: 40
  key_links:
    - from: "internal/core/services/*/*_test.go"
      to: "internal/core/services/testdata/mocks.go"
      via: "import testdata"
      pattern: "testdata\\.Mock"
    - from: "internal/core/services/*/*_test.go"
      to: "internal/core/services/testdata/factories.go"
      via: "testdata entity factories"
      pattern: "testdata\\.New"
---

<objective>
Plan 04: Backend service-layer tests for the remaining 8 service packages — Contract, Customer, Project, Unit, WorkingGroup, Invitation, PasswordReset, Export.

Purpose: Cover all domains equally per D-06/D-07. These 8 service packages currently have zero test coverage. Each gets table-driven tests (D-17) with mock repositories from the shared testdata package (Plan 01). Tests are fast — no DB dependency, no HTTP — pure business logic validation.

Output: 8 new test files covering all remaining service packages
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md (Service-Layer Testing section)

# Source files under test
@internal/core/services/contract/contract.go
@internal/core/services/customer/customer.go
@internal/core/services/project/project.go
@internal/core/services/unit/unit.go
@internal/core/services/working_group/working_group.go
@internal/core/services/invitation/invitation.go
@internal/core/services/password_reset/password_reset.go
@internal/core/services/export/export.go

# Port interfaces for mock implementations
@internal/core/ports/contract_repository.go
@internal/core/ports/customer_repository.go
@internal/core/ports/project_repository.go
@internal/core/ports/unit_repository.go
@internal/core/ports/working_group_repository.go
@internal/core/ports/invitation_repository.go
@internal/core/ports/password_reset_repository.go
@internal/core/ports/export_repository.go

# Domain models (for entity factories and validation constants)
@internal/core/domain/contract/contract.go
@internal/core/domain/customer/customer.go
@internal/core/domain/project/project.go
@internal/core/domain/unit/unit.go
@internal/core/domain/working_group/working_group.go
@internal/core/domain/invitation/invitation.go
@internal/core/domain/password_reset/password_reset.go

# Test infrastructure (created in Plan 01)
@internal/core/services/testdata/factories.go
@internal/core/services/testdata/mocks.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract and Customer service tests</name>
  <files>
    internal/core/services/contract/contract_test.go
    internal/core/services/customer/customer_test.go
  </files>
  <read_first>
    @internal/core/services/contract/contract.go
    @internal/core/services/customer/customer.go
    @internal/core/ports/contract_repository.go
    @internal/core/ports/customer_repository.go
    @internal/core/domain/contract/contract.go
    @internal/core/domain/customer/customer.go
  </read_first>
  <action>
    Create `internal/core/services/contract/contract_test.go` (package contract):

    Import `testing`, `context`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`, `github.com/google/uuid`, the project's contract domain, ports, and testdata packages.

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockContractRepo)` — creates service with MockContractRepo.

    Table-driven tests:

    1. `TestService_Create`:
       - `"valid contract"` — valid name, governance model. Assert: contract.ID != nil, Status is active/draft
       - `"with customer"` — set CustomerID. Assert: CustomerID matches
       - `"missing name"` — empty name. Assert: validation error
       - `"invalid governance model"` — random string. Assert: validation error

    2. `TestService_List`:
       - `"by org"` — pre-seed contracts. Assert: correct count
       - `"org with no contracts"` — pre-seed for different org. Assert: empty list

    3. `TestService_Get`:
       - `"existing"` — pre-seed. Assert: correct ID
       - `"not found"` — random ID. Assert: error

    4. `TestService_Update`:
       - `"update name and km_rate"` — Assert: fields updated
       - `"update customer"` — change CustomerID. Assert: new customer

    5. `TestService_Delete`:
       - `"soft delete"` — Assert: IsActive = false, not removed from map
       - `"not found"` — Assert: error

    Create `internal/core/services/customer/customer_test.go` (package customer):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockCustomerRepo)` — creates service with MockCustomerRepo.

    Table-driven tests:

    1. `TestService_Create`:
       - `"valid customer"` — company name, contact info. Assert: customer.ID != nil
       - `"missing company name"` — empty company name. Assert: validation error
       - `"org scoped"` — OrgID matches input

    2. `TestService_List`:
       - `"by org"` — pre-seed 3 customers. Assert: count == 3
       - `"by org empty"` — different org. Assert: empty list

    3. `TestService_Get`:
       - `"existing"` — Assert: correct data
       - `"not found"` — Assert: error

    4. `TestService_Update`:
       - `"update contact info"` — change email, phone. Assert: fields updated

    5. `TestService_Deactivate`:
       - `"set inactive"` — Assert: IsActive = false
  </action>
  <acceptance_criteria>
    - `go test ./internal/core/services/contract/... ./internal/core/services/customer/... -count=1 -v` passes
    - Contract tests cover create (with/without customer, validation), list, get, update, delete
    - Customer tests cover create (valid + missing name), list, get, update, deactivate
    - Both use testify assertions and table-driven subtests
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/core/services/contract/... ./internal/core/services/customer/... -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <done>Contract and Customer service tests pass: CRUD + validation cases all green.</done>
</task>

<task type="auto">
  <name>Task 2: Project, Unit, and Working Group service tests</name>
  <files>
    internal/core/services/project/project_test.go
    internal/core/services/unit/unit_test.go
    internal/core/services/working_group/working_group_test.go
  </files>
  <read_first>
    @internal/core/services/project/project.go
    @internal/core/services/unit/unit.go
    @internal/core/services/working_group/working_group.go
    @internal/core/ports/project_repository.go
    @internal/core/ports/unit_repository.go
    @internal/core/ports/working_group_repository.go
    @internal/core/domain/project/project.go
    @internal/core/domain/unit/unit.go
    @internal/core/domain/working_group/working_group.go
  </read_first>
  <action>
    Create `internal/core/services/project/project_test.go` (package project):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockProjectRepo)`.

    Table-driven tests:

    1. `TestService_Create`:
       - `"valid billable project"` — Assert: Status active, Type billable
       - `"valid internal project"` — Assert: Type internal
       - `"with contract"` — set ContractID. Assert: associated
       - `"missing name"` — Assert: validation error
       - `"invalid type"` — random string type. Assert: validation error

    2. `TestService_List`:
       - `"by org"` — pre-seed. Assert: correct count
       - `"filter by contract"` — Assert: filtered

    3. `TestService_Get`:
       - `"existing"` — Assert: returns project
       - `"not found"` — Assert: error

    4. `TestService_Update`:
       - `"update fields"` — change name, type. Assert: fields changed

    5. `TestService_Deactivate`:
       - `"soft delete"` — IsActive = false

    Create `internal/core/services/unit/unit_test.go` (package unit):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockUnitRepo)`.

    Table-driven tests:

    1. `TestService_Create`:
       - `"valid unit"` — name, org. Assert: created
       - `"missing name"` — Assert: validation error

    2. `TestService_List`:
       - `"by org"` — pre-seed. Assert: correct list
       - `"empty"` — Assert: empty

    3. `TestService_Get` / `TestService_Update` / `TestService_Delete`:
       - `"CRUD"` — standard patterns as above

    Create `internal/core/services/working_group/working_group_test.go` (package working_group):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockWorkingGroupRepo)`.

    Same CRUD pattern as unit_test.go: create (valid + missing name), list, get, update, delete.
  </action>
  <acceptance_criteria>
    - `go test ./internal/core/services/project/... ./internal/core/services/unit/... ./internal/core/services/working_group/... -count=1 -v` passes
    - Project tests cover billable/internal types, contract association, validation
    - Unit tests cover CRUD with org-scoped list
    - Working Group tests cover CRUD with org-scoped list
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/core/services/project/... ./internal/core/services/unit/... ./internal/core/services/working_group/... -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <done>Project, Unit, and Working Group service tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Invitation, Password Reset, and Export service tests</name>
  <files>
    internal/core/services/invitation/invitation_test.go
    internal/core/services/password_reset/password_reset_test.go
    internal/core/services/export/export_test.go
  </files>
  <read_first>
    @internal/core/services/invitation/invitation.go
    @internal/core/services/password_reset/password_reset.go
    @internal/core/services/export/export.go
    @internal/core/ports/invitation_repository.go
    @internal/core/ports/password_reset_repository.go
    @internal/core/ports/export_repository.go
    @internal/core/domain/invitation/invitation.go
    @internal/core/domain/password_reset/password_reset.go
  </read_first>
  <action>
    Create `internal/core/services/invitation/invitation_test.go` (package invitation):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockInvitationRepo)`.

    Table-driven tests:

    1. `TestService_Create`:
       - `"valid invitation"` — email, role, org. Assert: Status pending, token generated
       - `"duplicate email for org"` — existing pending invitation. Assert: error
       - `"invalid role"` — bad role string. Assert: validation error

    2. `TestService_Accept`:
       - `"valid accept"` — invitation exists and is pending. Assert: Status accepted
       - `"already accepted"` — Assert: error
       - `"expired"` — past expiry. Assert: error
       - `"not found"` — Assert: error

    3. `TestService_Reject`:
       - `"reject pending"` — Assert: Status rejected
       - `"already rejected"` — Assert: error

    4. `TestService_ListByOrg`:
       - `"returns invitations"` — pre-seed. Assert: correct count
       - `"empty"` — Assert: empty

    Create `internal/core/services/password_reset/password_reset_test.go` (package password_reset):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockPasswordResetRepo, *testdata.MockUserRepo)`.

    Table-driven tests:

    1. `TestService_RequestReset`:
       - `"valid email"` — user exists. Assert: token created
       - `"nonexistent email"` — Assert: returns success (don't reveal email existence) OR error per existing logic

    2. `TestService_ResetPassword`:
       - `"valid token"` — unused, unexpired token. Assert: password updated, token marked used
       - `"expired token"` — past expiry. Assert: error
       - `"already used token"` — used=true. Assert: error
       - `"invalid token"` — not found. Assert: error
       - `"weak new password"` — Assert: validation error

    Create `internal/core/services/export/export_test.go` (package export):

    Helper: `func setupService(t *testing.T) (*Service, *testdata.MockExportRepo)`.

    Table-driven tests:

    1. `TestService_Export`:
       - `"export time entries"` — valid request. Assert: data returned, no error
       - `"export with format selection"` — CSV/JSON format. Assert: format matches

    2. `TestService_ListExports`:
       - `"by org"` — pre-seed. Assert: correct list
  </action>
  <acceptance_criteria>
    - `go test ./internal/core/services/invitation/... ./internal/core/services/password_reset/... ./internal/core/services/export/... -count=1 -v` passes
    - Invitation tests cover create (valid + duplicate), accept, reject, list
    - Password Reset tests cover request (valid + nonexistent email), reset (valid + expired + used + invalid token)
    - Export tests cover export generation and listing
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/core/services/invitation/... ./internal/core/services/password_reset/... ./internal/core/services/export/... -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <done>Invitation, Password Reset, and Export service tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| N/A | Phase 0 — test-only code. No production code modified. All tests use mock repositories. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07 | T | Invitation tests | accept | Test-only; validates existing invitation logic |
| T-00-08 | T | Password reset tests | accept | Test-only; validates existing password reset logic |
</threat_model>

<verification>
- `go test ./internal/core/services/contract/... -count=1 -v` passes
- `go test ./internal/core/services/customer/... -count=1 -v` passes
- `go test ./internal/core/services/project/... -count=1 -v` passes
- `go test ./internal/core/services/unit/... -count=1 -v` passes
- `go test ./internal/core/services/working_group/... -count=1 -v` passes
- `go test ./internal/core/services/invitation/... -count=1 -v` passes
- `go test ./internal/core/services/password_reset/... -count=1 -v` passes
- `go test ./internal/core/services/export/... -count=1 -v` passes
</verification>

<success_criteria>
- All 8 remaining service packages have table-driven tests
- Tests cover CRUD, validation, error handling per domain
- All tests pass with mock repos (no DB dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/00-testing-foundation/00-04-SUMMARY.md`
</output>
