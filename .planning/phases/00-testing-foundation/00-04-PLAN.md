---
phase: 00-testing-foundation
plan: 04
type: execute
wave: 4
depends_on: [00-03]
files_modified:
  - internal/adapters/primary/http/auth_test.go
  - internal/adapters/primary/http/unit_test.go
  - internal/adapters/primary/http/organization_test.go
  - internal/adapters/primary/http/project_test.go
  - internal/adapters/primary/http/contract_test.go
  - internal/adapters/primary/http/customer_test.go
  - internal/adapters/primary/http/time_entry_test.go
  - internal/adapters/primary/http/password_reset_test.go
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "Handler tests use testcontainers-backed PostgreSQL instead of mocks"
    - "Each handler test file has TestMain with container lifecycle"
    - "Full HTTP request/response cycle is tested against real DB"
    - "Auth handler tests cover login, register, refresh, profile, memberships"
    - "Error scenarios return proper HTTP status codes"
    - "Existing smoke test still passes"
  artifacts:
    - path: "internal/adapters/primary/http/auth_test.go"
      provides: "Rewritten auth handler integration tests"
      min_lines: 200
    - path: "internal/adapters/primary/http/unit_test.go"
      provides: "Rewritten unit handler integration tests"
      min_lines: 100
    - path: "internal/adapters/primary/http/organization_test.go"
      provides: "Rewritten org handler integration tests"
      min_lines: 100
  key_links:
    - from: "*_test.go (handlers)"
      to: "postgres.SetupPackageContainer"
      via: "TestMain -> container startup"
      pattern: "SetupPackageContainer"
    - from: "*_test.go (handlers)"
      to: "postgres.SetupTestSchema"
      via: "per-test schema setup"
      pattern: "SetupTestSchema"
---

<objective>
Rewrite HTTP handler integration tests to use testcontainers-backed PostgreSQL. Per D-12, new test files use `_integration_test.go` suffix. Per D-13, minor bugs discovered during rewriting are fixed inline.

Purpose: The current handler tests either use mocks (not testing real SQL/DB behavior) or use TestPool with DATABASE_URL dependency. After this plan, all handler tests run against testcontainers-managed PostgreSQL, catching SQL errors, constraint violations, and full HTTP request/response cycle issues before they reach production.

Output: Rewritten `*_test.go` files in `internal/adapters/primary/http/` with testcontainers-backed TestMain, real route wiring via `httptest.NewServer`, and per-test schema lifecycle.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md
@.planning/phases/00-testing-foundation/00-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@cmd/server/main_test.go
@internal/adapters/primary/http/auth.go
@internal/adapters/primary/http/auth_integration_test.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite auth + password_reset handler tests for testcontainers</name>
  <files>
    internal/adapters/primary/http/auth_test.go
    internal/adapters/primary/http/password_reset_test.go
  </files>
  <read_first>
    @cmd/server/main_test.go (for the route-wiring pattern with httptest.NewServer)
    @internal/adapters/primary/http/auth_integration_test.go (for existing auth integration test pattern)
    @internal/adapters/primary/http/auth_test.go (current state)
    @internal/adapters/primary/http/password_reset_test.go (current state)
  </read_first>
  <action>
    Rewrite `auth_test.go` and `password_reset_test.go` to use testcontainers-backed PostgreSQL.

    **Pattern** (consistent with main_test.go and auth_integration_test.go):

    Each file gets:
    1. Package-level `var testPool *pgxpool.Pool` 
    2. `TestMain(m *testing.M)` that calls `postgres.SetupPackageContainer(m)` then `m.Run()` then `os.Exit`
    3. A `setupHandlerTest(t)` function that:
       - Calls `postgres.SetupTestSchema(t, testPool)` + registers TeardownTestSchema cleanup
       - Creates REAL repositories, services, and handlers (same wiring as main.go)
       - Registers routes on `http.ServeMux`
       - Starts `httptest.NewServer`
       - Returns fixture with client (with cookie jar), server URL, pool

    **auth_test.go** should cover:
    - Login with valid credentials → 200 + auth_token cookie
    - Login with wrong password → 401
    - Register with new org → 201 + user data
    - Register with existing email → 409
    - Refresh with valid token → 200 + new token
    - Refresh with invalid token → 401
    - GetProfile (authenticated) → 200 with role + org_id
    - GetProfile (unauthenticated) → 401
    - GetMemberships → 200 with memberships list
    - GetMemberships (no org) → 200 with empty list (not panic)
    - Bootstrap → 200 (first user) then 409 (already exists)
    - SwitchOrganization → 200 with new token
    - Logout → 200, clears cookies
    - Password reset request → 200, no code in body
    - Password reset verify → 200 with success
    - Password reset verify with wrong code → 401

    **password_reset_test.go** should cover:
    - Request reset with valid email → 200
    - Request reset with unknown email → 404
    - Verify with valid code → 200
    - Verify with expired code → 410
    - Verify with invalid code → 401

    Keep the existing `*_test.go` file names (not `_integration_test.go` per D-12). The integration test suffix rule (D-12) applies to NEW files. Since these are REWRITES of existing files, keep the original filename.

    Wire ALL services needed for the test (not just auth). Use the full main.go wiring pattern from `cmd/server/main_test.go` lines 40-208 to ensure route registration matches production.
  </action>
  <acceptance_criteria>
    <checks>
      - auth_test.go and password_reset_test.go use testcontainers (postgres.SetupPackageContainer)
      - Each file has TestMain
      - Each uses httptest.NewServer + full route wiring
      - Auth tests cover 13+ scenarios including error paths
      - Password reset tests cover 5+ scenarios
      - `go test -v -count=1 -timeout 180s -run 'TestAuth' ./internal/adapters/primary/http/...` passes
      - `go test -v -count=1 -timeout 180s -run 'TestPasswordReset' ./internal/adapters/primary/http/...` passes
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 -timeout 180s -run 'TestAuth|TestPasswordReset' ./internal/adapters/primary/http/...</automated>
  </verify>
  <done>
    - Auth handler tests rewritten with testcontainers
    - Password reset handler tests rewritten with testcontainers
    - 18+ test scenarios passing
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite remaining handler tests for testcontainers</name>
  <files>
    internal/adapters/primary/http/unit_test.go
    internal/adapters/primary/http/organization_test.go
    internal/adapters/primary/http/project_test.go
    internal/adapters/primary/http/contract_test.go
    internal/adapters/primary/http/customer_test.go
    internal/adapters/primary/http/time_entry_test.go
  </files>
  <read_first>
    @internal/adapters/primary/http/unit.go
    @internal/adapters/primary/http/organization.go
    @internal/adapters/primary/http/project.go
    @internal/adapters/primary/http/contract.go
    @internal/adapters/primary/http/customer.go
    @internal/adapters/primary/http/time_entry.go
    @internal/adapters/primary/http/unit_test.go
    @internal/adapters/primary/http/organization_test.go
    @internal/adapters/primary/http/project_test.go
    @internal/adapters/primary/http/contract_test.go
    @internal/adapters/primary/http/customer_test.go
    @internal/adapters/primary/http/time_entry_test.go
  </read_first>
  <action>
    Rewrite each handler test file to use testcontainers. Each follows the same pattern as Task 1:

    1. Package-level `var testPool *pgxpool.Pool`
    2. `TestMain(m *testing.M)` with `postgres.SetupPackageContainer(m)`
    3. `setupHandlerTest(t)` that creates a test server with REAL repos + full route wiring

    **Important**: Since all handler test files share the SAME package (`http`), they share the SAME testcontainers container (via sync.Once in SetupPackageContainer). The TestMain in each file will trigger SetupPackageContainer — the second+ calls will reuse the existing container. However, only ONE TestMain per package is allowed in Go. This is a constraint.

    **Consolidation requirement — remove Plan 01's TestMain**: Plan 01 (Task 3) will create `auth_integration_test.go` with its own TestMain. When executing THIS plan (Plan 04), you MUST remove that TestMain from `auth_integration_test.go` — it will be replaced by the single TestMain in `handler_test_helper.go`. Only the `handler_test_helper.go` TestMain survives. All test files (including auth) use `newHandlerFixture(t)` instead of their own TestMain.

    **Solution**: Create a shared `handler_test_setup.go` file in the `http` package:
    - Contains `var handlerTestPool *pgxpool.Pool`
    - Contains `func HandlerTestMain(m *testing.M)` — calls SetupPackageContainer, stores pool, runs tests
    - Contains `func newHandlerFixture(t *testing.T) *handlerFixture` — sets up schema, creates server

    Each `*_test.go` file REMOVES its TestMain and uses `newHandlerFixture(t)` instead.

    Wait — this is important. Go allows only ONE TestMain per package. The current handler tests might already have one TestMain each, or none. We need to consolidate.

    **Consolidation plan**:
    1. Create `internal/adapters/primary/http/handler_test_helper.go` (NEW):
       - Package-level `var handlerTestPool *pgxpool.Pool`
       - `func TestMain(m *testing.M)` — the single TestMain for the http package
       - `func newHandlerFixture(t *testing.T) *handlerFixture` — creates full server wiring
       - `type handlerFixture struct { client *http.Client; server *httptest.Server; pool *pgxpool.Pool; mux *http.ServeMux }`

    2. Each `*_test.go` file:
       - Calls `fixture := newHandlerFixture(t)` at the start of each test
       - Uses `fixture.client` to make HTTP requests
       - Uses `fixture.server.URL` as base URL
       - Closes server via `t.Cleanup(fixture.server.Close)`

    3. If any test file currently has conflicting TestMain functions, remove them.

    **Test coverage for each handler:**
    - **unit_test.go**: Create unit, List, GetByID, Update, Delete, ListMembers, AddMember, RemoveMember, GetTree, GetDescendants. Test UUID validation error (400 vs 500).
    - **organization_test.go**: Create org, Get, ListMembers (with guard), UpdateSettings, Invite. Test nil orgID guard.
    - **project_test.go**: Create project (billable + internal), List, Get, Adopt, ListManagers, AddManager, RemoveManager.
    - **contract_test.go**: Create contract with customer, List, Get, Update, Delete.
    - **customer_test.go**: Create customer, ListByOrg, GetByID, Update, Deactivate.
    - **time_entry_test.go**: Create time entry, List, GetByID, Update, Delete, Submit, Approve, Reject.

    For each handler, test at least 2 error scenarios (bad request, not found, unauthorized).
  </action>
  <acceptance_criteria>
    <checks>
      - `handler_test_helper.go` created with TestMain + newHandlerFixture
      - All `*_test.go` files use newHandlerFixture
      - No conflicting TestMain functions
      - `go test -v -count=1 -timeout 300s -run 'TestUnit|TestOrganization|TestProject|TestContract|TestCustomer|TestTimeEntry' ./internal/adapters/primary/http/...` passes
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 -timeout 300s -run 'TestUnit|TestOrganization|TestProject|TestContract|TestCustomer|TestTimeEntry' ./internal/adapters/primary/http/...</automated>
  </verify>
  <done>
    - handler_test_helper.go created with shared TestMain and fixture
    - All 6 handler test files rewritten with testcontainers
    - Each handler tests 5+ scenarios including error paths
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full handler test suite run, fix inline issues</name>
  <files>
    (various - fixes applied inline per D-13)
  </files>
  <read_first>
    (none - run tests first, inspect failures)
  </read_first>
  <action>
    Run the full handler test suite:

    ```bash
    go test -v -count=1 -timeout 300s ./internal/adapters/primary/http/... 2>&1
    ```

    1. Verify ALL tests pass. This includes:
       - The new auth_integration_test.go from Plan 01
       - The rewritten handler test files from this plan
       - Any other existing tests in the package

    2. If any failure occurs:
       - Fix minor issues inline per D-13 (wrong import, missing route, wrong expected status)
       - For complex bugs: use `t.Skip("reason — see Plan 05 #issue-N")` and document in the Plan 05 issue log
       - The bug log can be a comment block at the top of the test file or a dedicated PLAN-05-ISSUES.md file

    3. Verify that the old smoke test still passes:
       ```bash
       go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...
       ```

    4. Run the full Go test suite to check for regressions:
       ```bash
       go test -v -count=1 -timeout 600s ./... 2>&1 | tail -50
       ```
       (This may take a while — use `timeout 600` to cap it)

    Document ALL skipped tests and their reasons — this feeds directly into Plan 05.
  </action>
  <acceptance_criteria>
    <checks>
      - `go test -v -count=1 -timeout 300s ./internal/adapters/primary/http/...` exits 0
      - `go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...` exits 0
      - `go test -v -count=1 -timeout 600s ./internal/...` exits 0
      - Any skipped tests documented for Plan 05
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 -timeout 300s ./internal/adapters/primary/http/...</automated>
  </verify>
  <done>
    - Full handler test suite is green
    - Full backend test suite is green
    - Smoke test still passes
    - Any complex bugs documented for Plan 05
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `handlerTestPool *pgxpool.Pool` | package var | `internal/adapters/primary/http/handler_test_helper.go` | All handler test files |
| `TestMain(m *testing.M)` | test entry | `internal/adapters/primary/http/handler_test_helper.go` | Single TestMain for http package |
| `newHandlerFixture(t) *handlerFixture` | test helper | `internal/adapters/primary/http/handler_test_helper.go` | All handler test files |
| `handlerFixture` | struct type | `internal/adapters/primary/http/handler_test_helper.go` | Test functions |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client (httptest) → handler | Simulated HTTP requests from test to handler |
| Handler → service → database | Full request processing pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04-01 | Tampering | handler_test_helper.go | mitigate | Shared TestMain with sync.Once container. Schema isolation per test via SetupTestSchema/TeardownTestSchema. Sequential execution (no t.Parallel). |
| T-00-04-02 | DoS | Full test suite | accept | Estimated 5min for full suite vs 2min target. This is a one-time cost during test migration. |
</threat_model>

<verification>
1. `handler_test_helper.go` created with shared TestMain
2. All handler tests passing with testcontainers
3. Full test suite green: `go test -v -count=1 -timeout 600s ./internal/...`
4. Smoke test still passes
</verification>

<success_criteria>
- All handler test files rewritten to use testcontainers-backed PostgreSQL
- Shared TestMain + fixture in handler_test_helper.go
- Auth handler: 13+ scenarios (login, register, refresh, profile, memberships, etc.)
- Other handlers: 5+ scenarios each with error paths
- Full backend test suite green
- All skipped tests documented for Plan 05
</success_criteria>

<output>
Create `.planning/phases/00-testing-foundation/00-04-SUMMARY.md` when done
</output>
