---
phase: 00-testing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/quick-scan.sh
  - .planning/phases/00-testing-foundation/BUGS.md
  - go.mod
  - go.sum
  - internal/core/services/testdata/factories.go
  - internal/core/services/testdata/mocks.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Quick-scan probe script exists and exercises all API endpoints"
    - "BUGS.md exists at the correct path with documented findings"
    - "testify is a direct Go dependency (importable in _test.go files)"
    - "Testdata factory functions exist for all entity types"
    - "Mock repository implementations exist for all port interfaces"
  artifacts:
    - path: "scripts/quick-scan.sh"
      provides: "Lightweight curl-based API endpoint walkthrough"
      min_lines: 80
    - path: ".planning/phases/00-testing-foundation/BUGS.md"
      provides: "Bug log with severity, location, description"
      contains: "severity"
    - path: "internal/core/services/testdata/factories.go"
      provides: "Factory functions for all domain entities"
      exports: ["NewTimeEntry", "NewUser", "NewOrganization", "NewContract", "NewCustomer", "NewProject", "NewUnit", "NewWorkingGroup", "NewInvitation", "NewExpense", "UniqueEmail", "UniqueOrgName"]
    - path: "internal/core/services/testdata/mocks.go"
      provides: "In-memory mock implementations of all port interfaces"
      exports: ["*Mock*Repository"]
    - path: "go.mod"
      provides: "testify as direct dependency"
      contains: "github.com/stretchr/testify"
  key_links:
    - from: "scripts/quick-scan.sh"
      to: "localhost:8080"
      via: "curl"
      pattern: "curl.*localhost:8080"
    - from: "internal/core/services/testdata/factories.go"
      to: "internal/core/services/*/*_test.go"
      via: "import testdata"
      pattern: "testdata\\."
    - from: "internal/core/services/testdata/mocks.go"
      to: "internal/core/ports/*.go"
      via: "implements ports.*Repository"
      pattern: "ports\\.\\w+Repository"
---

<objective>
Plan 01: Infrastructure and shared test dependencies — quick-scan probe, testify dependency, BUGS.md scaffold, and the shared testdata package with factory functions and mock repository implementations.

Purpose: Create all shared infrastructure that downstream test-writing plans depend on. Quick-scan catches obvious API issues before deep test writing begins. Testdata package eliminates duplicated entity-creation patterns across test files. Mock repositories enable fast service-layer tests with zero DB dependency.

Output: Quick-scan bash script, BUGS.md, testify as direct dep, testdata/factories.go, testdata/mocks.go
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md

# Port interfaces — these define the signatures mock repos must implement
@internal/core/ports/time_entry_repository.go
@internal/core/ports/user_repository.go
@internal/core/ports/organization_repository.go
@internal/core/ports/contract_repository.go
@internal/core/ports/customer_repository.go
@internal/core/ports/project_repository.go
@internal/core/ports/unit_repository.go
@internal/core/ports/working_group_repository.go
@internal/core/ports/invitation_repository.go
@internal/core/ports/password_reset_repository.go
@internal/core/ports/export_repository.go
@internal/core/ports/refresh_token_repo.go
@internal/core/ports/token_service.go
@internal/core/ports/password_hasher.go
@internal/core/ports/organization_management_repository.go

# Existing test helpers — migrate from inline to shared
@internal/adapters/primary/http/auth_test.go (lines 22-32: uniqueID, uniqueEmail, uniqueOrgName)

# Service constructors — need the constructor signatures for mock injection
@internal/core/services/auth/auth.go
@internal/core/services/time_entry/time_entry.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quick-scan probe script and BUGS.md scaffold</name>
  <files>
    scripts/quick-scan.sh
    .planning/phases/00-testing-foundation/BUGS.md
  </files>
  <read_first>
    @internal/adapters/primary/http/auth.go
    @internal/adapters/primary/http/time_entry.go
    @cmd/server/main.go
  </read_first>
  <action>
    Create `scripts/quick-scan.sh`: A lightweight bash script that exercises all API endpoints via curl. Per D-02, this runs before structured test writing. The script must:
    - Set SURREALDB_URL default (ws://localhost:8000/rpc), SURREALDB_USER (root), SURREALDB_PASS (root)
    - Start with `#!/bin/bash` and `set -euo pipefail`
    - First register a test user via POST /auth/register (capture cookies)
    - Login via POST /auth/login (capture auth_token and refresh_token cookies)
    - GET /auth/me — verify authenticated profile access
    - POST /auth/refresh — verify token refresh
    - Using the auth cookie, exercise each registered route from cmd/server/main.go in order:
      - GET /time-entries (list, expect 200 or empty array)
      - POST /time-entries (create with valid body, expect 201)
      - POST /projects (create, expect 201)
      - GET /projects (list, expect 200)
      - POST /contracts (create, expect 201)
      - GET /contracts (list, expect 200)
      - POST /customers (create, expect 201)
      - GET /customers (list, expect 200)
      - POST /org-hierarchy/units, /org-hierarchy/working-groups
      - GET /org-hierarchy/units, /org-hierarchy/working-groups
    - Test adversarial/break-it cases per D-03:
      - POST with empty JSON body -> expects 400
      - GET /auth/me without cookie -> expects 401
      - POST /auth/register with missing fields -> expects 400
      - POST /time-entries with missing project_id -> expects 400
    - On each non-2xx response, print a WARNING line
    - After all probes, print summary: "PASS: X endpoints OK, FAIL: Y endpoints failed, WARN: Z warnings"
    - The script MUST NOT exit on first failure — run all probes and report
    - Use `trap` to print a summary even if script errors mid-way

    Create `.planning/phases/00-testing-foundation/BUGS.md`: The bug accumulation log per D-04. Frontmatter with date and phase. Table format:
    | # | Severity | Location | Description | Found By | Status |
    |---|----------|----------|-------------|----------|--------|
    Severity values: critical, high, medium, low. Status values: open, fixed (set by wave 2).
    Pre-populate with header row and example row. Add date-range footer.
  </action>
  <acceptance_criteria>
    - `scripts/quick-scan.sh` exists, is executable (chmod +x), starts with `#!/bin/bash`
    - `bash scripts/quick-scan.sh` runs to completion (passes or reports failures) — does NOT hang
    - `bash scripts/quick-scan.sh` outputs summary line with PASS/FAIL/WARN counts
    - `.planning/phases/00-testing-foundation/BUGS.md` exists with correct table header (Severity, Location, Description, Found By, Status columns)
    - BUGS.md has at least one example row
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && ls -la scripts/quick-scan.sh && head -3 scripts/quick-scan.sh && head -10 .planning/phases/00-testing-foundation/BUGS.md</automated>
  </verify>
  <done>Quick-scan script exists and is executable. BUGS.md exists with table structure. Both verified on filesystem.</done>
</task>

<task type="auto">
  <name>Task 2: Make testify a direct Go dependency</name>
  <files>go.mod</files>
  <read_first></read_first>
  <action>
    Run `go get github.com/stretchr/testify@v1.11.1` from the project root to add testify as a direct dependency. This makes it importable in all `_test.go` files across the codebase (currently an indirect dependency only, used internally by `surrealdb.go`). After the get, verify with `go mod tidy`.
    The testify package provides: `assert.Equal`, `assert.NoError`, `require.NoError`, `assert.NotNil`, `assert.True`/`assert.False`.
  </action>
  <acceptance_criteria>
    - `go.mod` contains a `require github.com/stretchr/testify v1.11.1` line (direct dep, not indirect via `// indirect`)
    - `go.sum` is updated
    - `go build ./...` compiles without errors
    - `go test -count=0 ./...` discovers test files without compilation errors (doesn't run, just verifies compilation)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && grep -q "stretchr/testify" go.mod && go build ./... && go test -count=0 ./...</automated>
  </verify>
  <done>testify v1.11.1 is a direct Go dependency. go build and go test -count=0 pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create shared testdata package with factories and mock repositories</name>
  <files>
    internal/core/services/testdata/factories.go
    internal/core/services/testdata/mocks.go
    internal/core/services/testdata/mocks_test.go
  </files>
  <read_first>
    @internal/core/domain/time_entry/time_entry.go
    @internal/core/domain/project/project.go
    @internal/core/domain/contract/contract.go
    @internal/core/domain/customer/customer.go
    @internal/core/domain/user/user.go
    @internal/core/domain/organization/organization.go
    @internal/core/domain/unit/unit.go
    @internal/core/domain/working_group/working_group.go
    @internal/core/domain/invitation/invitation.go
    @internal/core/domain/password_reset/password_reset.go
    @internal/core/ports/time_entry_repository.go
    @internal/core/ports/user_repository.go
    @internal/core/ports/organization_repository.go
    @internal/core/ports/contract_repository.go
    @internal/core/ports/customer_repository.go
    @internal/core/ports/project_repository.go
    @internal/core/ports/unit_repository.go
    @internal/core/ports/working_group_repository.go
    @internal/core/ports/invitation_repository.go
    @internal/core/ports/password_reset_repository.go
    @internal/core/ports/export_repository.go
    @internal/core/ports/refresh_token_repo.go
    @internal/core/ports/token_service.go
    @internal/core/ports/password_hasher.go
    @internal/core/ports/organization_management_repository.go
  </read_first>
  <action>
    Create `internal/core/services/testdata/factories.go` (package testdata):

    Import the standard library, `github.com/google/uuid`, and each domain package (timeentry, project, contract, customer, user, organization, unit, workinggroup, invitation, passwordreset, expense).
    Note: expense domain package is empty — the Expense struct lives in `internal/models`. Use the models import for expense entities.

    Factory functions with override pattern `func New<T>(overrides ...func(*<T>)) <T>`:

    Entity factories required:
    1. `NewUser(overrides ...func(*user.User)) user.User` — sets ID, Email (via UniqueEmail), Name, IsActive=true
    2. `NewOrganization(overrides ...func(*organization.Organization)) organization.Organization` — sets ID, Name, Slug, CreatedAt
    3. `NewOrganizationMembership(overrides ...func(*organization.Membership)) organization.Membership` — sets ID, UserID, OrgID, Role=employee, IsActive=true
    4. `NewTimeEntry(overrides ...func(*timeentry.TimeEntry)) timeentry.TimeEntry` — sets ID, UserID, OrgID, Status=StatusDraft, Hours=8.0, EntryDate, CreatedAt, UpdatedAt
    5. `NewTimeEntryItem(overrides ...func(*timeentry.TimeEntryItem)) timeentry.TimeEntryItem` — sets ID, TimeEntryID, ProjectID, Hours=4.0
    6. `NewContract(overrides ...func(*contract.Contract)) contract.Contract` — sets ID, Name, OrgID, Status, CreatedAt
    7. `NewCustomer(overrides ...func(*customer.Customer)) customer.Customer` — sets ID, OrgID, CompanyName, Email, IsActive=true
    8. `NewProject(overrides ...func(*project.Project)) project.Project` — sets ID, Name, OrgID, ContractID, Type=project.TypeBillable, Status, IsActive=true
    9. `NewUnit(overrides ...func(*unit.Unit)) unit.Unit` — sets ID, OrgID, Name, CreatedAt
    10. `NewWorkingGroup(overrides ...func(*workinggroup.WorkingGroup)) workinggroup.WorkingGroup` — sets ID, OrgID, Name, CreatedAt
    11. `NewInvitation(overrides ...func(*invitation.Invitation)) invitation.Invitation` — sets ID, OrgID, Email, Role=employee, Status=pending
    12. `NewPasswordReset(overrides ...func(*passwordreset.PasswordReset)) passwordreset.PasswordReset` — sets ID, UserID, Token, ExpiresAt, Used=false
    13. `NewExpense(overrides ...func(*models.Expense)) models.Expense` — sets ID, UserID, OrgID, Status=models.StatusDraft, Date, CreatedAt, UpdatedAt

    Helper functions:
    - `UniqueID() string` — returns uuid.New().String()
    - `UniqueEmail() string` — returns uuid.New().String() + "@test.com"
    - `UniqueOrgName() string` — returns "Org_" + uuid.New().String()

    Create `internal/core/services/testdata/mocks.go`:

    For each port interface listed in read_first, create a mock struct with an in-memory map and a `mu sync.Mutex` for thread safety. Use a single file with all mocks.

    Required mock structs:
    1. `MockTimeEntryRepo` — implements ports.TimeEntryRepository + ports.AuditLogRepository
       - Fields: mu sync.Mutex, Entries map[uuid.UUID]*timeentry.TimeEntry, AuditLogs []*timeentry.AuditLog
       - Methods: List(ctx, orgID, filters), GetByID(ctx, id), Create(ctx, e), Update(ctx, e), Delete(ctx, id), IsPeriodLocked(ctx, orgID, projectID, entryDate), ListPending(ctx, orgID, role, userID), Create(ctx, log) (for AuditLogRepository)
    2. `MockUserRepo` — implements ports.UserRepository
       - Fields: mu sync.Mutex, Users map[uuid.UUID]*user.User
    3. `MockOrgRepo` — implements ports.OrganizationRepository + ports.OrganizationManagementRepository
       - Fields: mu sync.Mutex, Orgs map[uuid.UUID]*organization.Organization, Memberships map[uuid.UUID]*organization.Membership
    4. `MockContractRepo` — implements ports.ContractRepository
       - Fields: mu sync.Mutex, Contracts map[uuid.UUID]*contract.Contract
    5. `MockCustomerRepo` — implements ports.CustomerRepository
       - Fields: mu sync.Mutex, Customers map[uuid.UUID]*customer.Customer
    6. `MockProjectRepo` — implements ports.ProjectRepository
       - Fields: mu sync.Mutex, Projects map[uuid.UUID]*project.Project
    7. `MockUnitRepo` — implements ports.UnitRepository
       - Fields: mu sync.Mutex, Units map[uuid.UUID]*unit.Unit
    8. `MockWorkingGroupRepo` — implements ports.WorkingGroupRepository
       - Fields: mu sync.Mutex, Groups map[uuid.UUID]*workinggroup.WorkingGroup
    9. `MockInvitationRepo` — implements ports.InvitationRepository
       - Fields: mu sync.Mutex, Invitations map[uuid.UUID]*invitation.Invitation
    10. `MockPasswordResetRepo` — implements ports.PasswordResetRepository
        - Fields: mu sync.Mutex, Resets map[uuid.UUID]*passwordreset.PasswordReset
    11. `MockExportRepo` — implements ports.ExportRepository
        - Fields: mu sync.Mutex
    12. `MockRefreshTokenRepo` — implements ports.RefreshTokenRepository
        - Fields: mu sync.Mutex, Tokens map[uuid.UUID]*models.RefreshToken

    For the non-entity mock services (token_service.go, password_hasher.go):
    13. `MockTokenService` — implements ports.TokenService
        - Fields: mu sync.Mutex, CreateToken returns ("mock-token", nil), ValidateToken returns (&auth.Claims{UserID: uuid.New(), OrgID: uuid.New(), Role: "employee"}, nil)
    14. `MockPasswordHasher` — implements ports.PasswordHasher
        - Fields: mu sync.Mutex, Hash returns ("hashed:"+password, nil), Verify returns (true, nil)

    Each mock method must:
    - Lock/unlock the mutex
    - Check if the entity exists in the map before returning
    - Return ports.ErrNotFound (or equivalent sentinel) when entity not found
    - Accept context.Context as first parameter
    - Use testify-style patterns but DO NOT import testify in mocks.go (tests import it)

    Create `internal/core/services/testdata/mocks_test.go`:
    A minimal test that verifies each mock can be instantiated:
    ```go
    func TestMocks_Instantiate(t *testing.T) {
        assert.NotNil(t, &MockTimeEntryRepo{})
        assert.NotNil(t, &MockUserRepo{})
        // ... repeat for all mock types
    }
    ```
    This uses testify's `assert` from the new direct dependency.
  </action>
  <acceptance_criteria>
    - `internal/core/services/testdata/factories.go` compiles without errors (`go build ./internal/core/services/testdata/...`)
    - `internal/core/services/testdata/mocks.go` compiles without errors
    - `internal/core/services/testdata/mocks_test.go` passes: `go test ./internal/core/services/testdata/... -count=1 -v`
    - Each factory function returns a non-zero entity (e.g., `newID := NewUser().ID; newID != uuid.Nil`)
    - Each mock struct implements its port interface (checked via compilation assignment: `var _ ports.TimeEntryRepository = (*MockTimeEntryRepo)(nil)`)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go build ./internal/core/services/testdata/... && go test ./internal/core/services/testdata/... -count=1 -v</automated>
  </verify>
  <done>Testdata package compiles and tests pass. All factory functions produce valid entities. All mock structs implement their port interfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| N/A | Phase 0 establishes testing infrastructure. No new production code written. Quick-scan and BUGS.md operate in test/dev context. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | T | quick-scan.sh | accept | Script runs locally against dev server only; no secrets in script |
| T-00-02 | E | testify dependency | accept | testify is a widely-used test library, no new attack surface |
</threat_model>

<verification>
- `go build ./...` compiles without errors
- `go test ./internal/core/services/testdata/... -count=1 -v` passes
- `bash scripts/quick-scan.sh` runs to completion
- `go.mod` has testify as direct dep
</verification>

<success_criteria>
- Quick-scan script exercises all API endpoints and produces PASS/FAIL/WARN summary
- BUGS.md exists with proper table structure ready for bug accumulation
- testify available as direct Go dependency
- Testdata factories produce valid entities for all 13 entity types
- All 16 port interface mock implementations compile and pass instantiation test
</success_criteria>

<output>
After completion, create `.planning/phases/00-testing-foundation/00-01-SUMMARY.md`
</output>
