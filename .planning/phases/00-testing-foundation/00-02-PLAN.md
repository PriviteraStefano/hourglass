---
phase: 00-testing-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/adapters/secondary/postgres/test_setup.go
  - internal/adapters/secondary/postgres/exported_test_helpers.go
  - cmd/server/main_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Tests start a PostgreSQL container via Docker automatically"
    - "No DATABASE_URL env var needed to run integration tests"
    - "Existing smoke test passes with container-backed DB"
    - "TestPool helper is replaced by container-backed pool"
    - "Existing SetupTestSchema/TeardownTestSchema continue working unchanged"
    - "Seed helper functions (seedOrg, seedUser, etc.) continue working unchanged"
  artifacts:
    - path: "internal/adapters/secondary/postgres/test_setup.go"
      provides: "Package-level testcontainers container lifecycle"
      min_lines: 70
    - path: "internal/adapters/secondary/postgres/exported_test_helpers.go"
      provides: "Updated TestPool using container-backed pool"
      contains: "SetupPackageContainer"
    - path: "cmd/server/main_test.go"
      provides: "Smoke test using new container pool"
      contains: "postgres.SetupPackageContainer"
  key_links:
    - from: "exported_test_helpers.go"
      to: "test_setup.go"
      via: "SetupPackageContainer call"
      pattern: "SetupPackageContainer"
    - from: "main_test.go"
      to: "exported_test_helpers.go"
      via: "testcontainers pool"
      pattern: "SetupPackageContainer"
    - from: "go.mod"
      to: "testcontainers-go"
      via: "module dependency"
      pattern: "testcontainers"
---

<objective>
Set up testcontainers-go as the integration test database infrastructure. Per D-07, testcontainers-go replaces TestPool entirely. Per D-08, use hybrid container strategy (one per package, per-function schema). Per D-09, ALL tests will use this infrastructure.

Purpose: Replace the current TestPool pattern (which requires DATABASE_URL/env-dependent setup) with a fully self-contained testcontainers-managed PostgreSQL container per package. This is the foundation for all subsequent test rewrites and auth bug verification.

Output: Created test_setup.go with SetupPackageContainer, updated TestPool in exported_test_helpers.go, updated smoke test in main_test.go, added testcontainers-go dependencies to go.mod.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md
@.planning/phases/00-testing-foundation/00-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@internal/adapters/secondary/postgres/exported_test_helpers.go
@cmd/server/main_test.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add testcontainers-go dependency and create test_setup.go</name>
  <files>
    go.mod
    go.sum
    internal/adapters/secondary/postgres/test_setup.go
  </files>
  <read_first>
    @internal/adapters/secondary/postgres/exported_test_helpers.go
    @.planning/phases/00-testing-foundation/00-RESEARCH.md
  </read_first>
  <action>
    Add testcontainers-go as a Go module dependency:

    ```
    go get github.com/testcontainers/testcontainers-go@latest
    go get github.com/testcontainers/testcontainers-go/modules/postgres@latest
    ```

    Then create `internal/adapters/secondary/postgres/test_setup.go`:

    1. Package: `postgres` (same package as exported_test_helpers.go)
    2. Exports:
       - `SetupPackageContainer(t testing.TB) *pgxpool.Pool` — uses `sync.Once` to start ONE PostgreSQL 16-alpine container per package. Uses `github.com/testcontainers/testcontainers-go/modules/postgres` with `postgres.Run()`, `postgres.WithDatabase("hourglass_test"`, `postgres.WithUsername("hourglass")`, `postgres.WithPassword("hourglass")`, `postgres.BasicWaitStrategies()`. Registers `t.Cleanup()` that closes the pool and terminates the container via `testcontainers.TerminateContainer()`.
       - `PackageTestPool() *pgxpool.Pool` — returns the cached pool (panics if SetupPackageContainer hasn't been called; callers should always use SetupPackageContainer which returns the pool).

    3. Package-level vars:
       - `packagePool *pgxpool.Pool` — cached pool
       - `packageContainer *postgres.PostgresContainer` — for cleanup
       - `poolOnce sync.Once` — ensures one container per package

    4. If Docker is not available (container fails to start), call `t.Skip("Docker not available, skipping integration test")` instead of `t.Fatalf`.

    Follow existing import conventions in exported_test_helpers.go: three groups (stdlib, third-party, internal). Use `github.com/jackc/pgx/v5/pgxpool` for the pool.

    DO NOT include `TestMain` — that belongs in each package's `_integration_test.go` file, not in the shared helper.

    DO NOT change SetupTestSchema, TeardownTestSchema, or any seed function.
  </action>
  <acceptance_criteria>
    <checks>
      - `go build ./internal/adapters/secondary/postgres/...` compiles without errors
      - `test_setup.go` contains `func SetupPackageContainer` and the postgres.Run pattern
      - File is in package `postgres`
      - `go list -m github.com/testcontainers/testcontainers-go` shows a resolved version
      - `go list -m github.com/testcontainers/testcontainers-go/modules/postgres` shows a resolved version
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/adapters/secondary/postgres/...</automated>
  </verify>
  <done>
    - go.mod has testcontainers-go and modules/postgres dependencies
    - test_setup.go compiles into the postgres package
    - SetupPackageContainer is exported
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update exported_test_helpers.go — replace TestPool with container-backed helper</name>
  <files>
    internal/adapters/secondary/postgres/exported_test_helpers.go
  </files>
  <read_first>
    @internal/adapters/secondary/postgres/exported_test_helpers.go
    @internal/adapters/secondary/postgres/test_setup.go
  </read_first>
  <action>
    In `exported_test_helpers.go`:

    1. Replace the existing `TestPool(t)` function. The new version:
       - Calls `SetupPackageContainer(t)` to get a container-backed pool
       - Returns the pool (no env check, no t.Skip for DATABASE_URL)
       - Keeps `t.Helper()` call
       - Does NOT call `db.NewPool()` anymore

    2. Remove the `"os"` import if it's no longer used elsewhere in the file (check if SetupTestSchema or TeardownTestSchema use os — yes, `os.ReadFile` is in SetupTestSchema). Keep `"os"` if still used by other functions.

    3. Remove the `"github.com/stefanoprivitera/hourglass/internal/db"` import if only TestPool used it.

    4. Do NOT modify SetupTestSchema, TeardownTestSchema, or any seed function.

    After the change:
    - `TestPool(t)` starts a testcontainers PostgreSQL container on first call (via sync.Once)
    - All existing test code that calls `postgres.TestPool(t)` now uses the container
    - No env var needed
  </action>
  <acceptance_criteria>
    <checks>
      - TestPool function no longer reads DATABASE_URL
      - TestPool calls SetupPackageContainer internally
      - File compiles: `go build ./internal/adapters/secondary/postgres/...`
      - SetupTestSchema and TeardownTestSchema and all seed functions are unchanged
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/adapters/secondary/postgres/...</automated>
  </verify>
  <done>
    - TestPool uses SetupPackageContainer internally
    - No DATABASE_URL dependency in TestPool
    - Existing schema lifecycle functions unchanged
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update smoke test and verify testcontainers works end-to-end</name>
  <files>
    cmd/server/main_test.go
  </files>
  <read_first>
    @cmd/server/main_test.go
  </read_first>
  <action>
    In `cmd/server/main_test.go`:

    1. The existing `TestSmoke` already calls `postgres.TestPool(t)` at line 36. Since TestPool now returns a container-backed pool, no changes are needed to the pool call itself.

    2. Verify the existing smoke test structure works with the new container-backed pool. The test wires the full server (identical to main.go's setup) and tests health/register/login/units endpoints.

    3. Add a quick Docker-availability check comment to explain the test behavior: "This test starts a PostgreSQL container via testcontainers-go. Docker must be running."

    4. Update the TestSmoke doc comment at line 33-34: replace "automatically skipped if DATABASE_URL is not set" with "automatically skipped if Docker is not available (testcontainers self-detects)".

    5. Run `go test -v -run TestSmoke ./cmd/server/...` with timeout:
       - First run: container starts (~15s), migrations apply, tests execute
       - The test should pass (200 on health, 201 on register, 200 on login, 200 on units)
       - Second run: container is reused (sync.Once), faster execution
  </action>
  <acceptance_criteria>
    <checks>
      - `go test -v -run TestSmoke ./cmd/server/...` passes within 120s
      - Smoke test verifies: health (200), register (201), login (200 + auth_token cookie), units (200 + data field)
      - Test does not read DATABASE_URL env var
    </checks>
  </acceptance_criteria>
  <verify>
    <automated>go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...</automated>
  </verify>
  <done>
    - Smoke test passes with container-backed PostgreSQL
    - No DATABASE_URL env var required
    - Full server wiring verified end-to-end
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `SetupPackageContainer(t testing.TB) *pgxpool.Pool` | function | `internal/adapters/secondary/postgres/test_setup.go` | All future `_integration_test.go` files |
| `PackageTestPool() *pgxpool.Pool` | function | `internal/adapters/secondary/postgres/test_setup.go` | Packages needing pool reference |
| Updated `TestPool(t) *pgxpool.Pool` | function | `internal/adapters/secondary/postgres/exported_test_helpers.go` | Existing smoke test, repository tests |
| `packagePool *pgxpool.Pool` | package var | `internal/adapters/secondary/postgres/test_setup.go` | Internal caching |
| `packageContainer *postgres.PostgresContainer` | package var | `internal/adapters/secondary/postgres/test_setup.go` | Container cleanup |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host filesystem → Docker container | Migration SQL files read from host and applied to test container |
| Docker daemon → test process | Container lifecycle management via testcontainers SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Tampering | go module installs | mitigate | Dependencies are well-known: testcontainers-go (4+ yrs, 150M+ downloads). Both packages are [OK] per Package Legitimacy Audit in 00-RESEARCH.md. No blocking checkpoint needed. |
| T-00-02-02 | DoS | SetupPackageContainer | mitigate | sync.Once ensures single container per package. t.Cleanup guarantees termination. Test timeout (120s) prevents hung test suites. |
| T-00-02-03 | Information Disclosure | SetupTestSchema | accept | Migration SQL files are already in the repository. Container is ephemeral and destroyed after tests. No PII or secrets in test data. |
| T-00-02-SC | Tampering | go module installs | mitigate | testcontainers-go v1.35+ — [ASSUMED] per Package Legitimacy Audit; no blocking human checkpoint needed because both packages are [OK] with long-standing reputation. |
</threat_model>

<verification>
1. `go build ./internal/adapters/secondary/postgres/...` succeeds
2. `go test -v -count=1 -timeout 120s -run 'TestSmoke' ./cmd/server/...` passes
3. No DATABASE_URL env var is referenced by the new code path
4. Second run of TestSmoke reuses the container (check via test logs)
</verification>

<success_criteria>
- Testcontainers-go v1.35+ installed as dependency
- test_setup.go created with SetupPackageContainer using sync.Once
- TestPool in exported_test_helpers.go delegates to SetupPackageContainer
- Smoke test passes with container-backed PostgreSQL
- No test requires DATABASE_URL env var
- All existing schema lifecycle and seed functions unchanged
</success_criteria>

<output>
Create `.planning/phases/00-testing-foundation/00-02-SUMMARY.md` when done
</output>
