---
phase: 00-testing-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/bun.lock
  - web/vitest.config.ts
  - web/src/lib/__tests__/setup.ts
  - web/tsconfig.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vitest is installed and configurable via `bun run test`"
    - "Vitest has jsdom environment for React component testing"
    - "MSW is installed for API mocking in Vitest tests"
    - "React Testing Library + jest-dom matchers are available"
    - "A shared test setup file initializes MSW server globally"
    - "tsconfig.json includes vitest type references"
  artifacts:
    - path: "web/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment, path aliases, and setup file"
      min_lines: 15
    - path: "web/src/lib/__tests__/setup.ts"
      provides: "Global MSW server setup with beforeAll/afterAll/afterEach"
      min_lines: 15
    - path: "web/tsconfig.json"
      provides: "Vitest types reference in compilerOptions.types"
      contains: "vitest"
  key_links:
    - from: "web/vitest.config.ts"
      to: "web/package.json"
      via: "scripts.test"
      pattern: "\"test\":"
    - from: "web/src/lib/__tests__/setup.ts"
      to: "web/vitest.config.ts"
      via: "setupFiles"
      pattern: "setupFiles"
---

<objective>
Plan 02: Frontend testing infrastructure — install Vitest, React Testing Library, MSW, jsdom; create vitest.config.ts and shared test setup file.

Purpose: Establish the frontend unit/integration testing framework per D-11/D-12. All downstream frontend test plans depend on this infrastructure being in place. MSW enables testing the auto-refresh-on-401 logic in api.ts.

Output: vitest.config.ts, setup.ts, package.json updated, tsconfig.json updated
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md

# Existing frontend config
@web/package.json
@web/tsconfig.json
@web/vite.config.ts
@web/src/lib/api.ts
@web/src/lib/query-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and frontend testing dependencies</name>
  <files>
    web/package.json
    web/bun.lock
  </files>
  <read_first>
    @web/package.json
  </read_first>
  <action>
    From `web/` directory, install all required frontend testing dependencies as dev dependencies using bun:

    ```
    bun add -d vitest@^4.1.6
    bun add -d @testing-library/react@^16.3.2
    bun add -d @testing-library/jest-dom@^6.9.1
    bun add -d jsdom@^29.1.1
    bun add -d msw@^2.14.6
    ```

    Also add a test script to package.json if not present: `"test": "vitest run"`.
    And a watch variant: `"test:watch": "vitest"`.

    After installation, verify MSW version exports `setupServer` from `msw/node` (confirming node environment compatibility).

    Update `web/tsconfig.json` to add `"types": ["vitest/globals"]` to `compilerOptions` so that `describe`, `it`, `expect`, `beforeAll`, `afterAll` etc. are available without imports.
  </action>
  <acceptance_criteria>
    - `web/package.json` has `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`, `msw` in devDependencies
    - `web/package.json` has `"test": "vitest run"` in scripts
    - `web/package.json` has `"test:watch": "vitest"` in scripts
    - `web/tsconfig.json` has `"types":` including `"vitest/globals"`
    - `bun install` runs without errors after package.json changes
    - `bun run test` exits 0 (even if no tests found yet)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && cat package.json | node -e "const p=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(p);console.log('vitest:',!!j.devDependencies.vitest,'rtl:',!!j.devDependencies['@testing-library/react'],'msw:',!!j.devDependencies.msw,'scripts.test:',j.scripts.test)" && bun run test 2>&1 | head -5</automated>
  </verify>
  <done>Vitest, RTL, jsdom, MSW installed as dev deps. Test scripts in package.json. tsconfig updated with vitest types. `bun run test` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create Vitest configuration and MSW test setup</name>
  <files>
    web/vitest.config.ts
    web/src/lib/__tests__/setup.ts
  </files>
  <read_first>
    @web/vite.config.ts
    @web/src/lib/api.ts
  </read_first>
  <action>
    Create `web/vitest.config.ts`:
    - Use `defineConfig` from `vitest/config` (not vite)
    - Set `test.environment: 'jsdom'`
    - Set `test.globals: true` to make `describe`/`it`/`expect` globally available
    - Set `test.setupFiles: ['./src/lib/__tests__/setup.ts']`
    - Set `test.restoreMocks: true`
    - Copy `resolve.alias` from `web/vite.config.ts` to enable `@/` path imports
    - Do NOT use `/// <reference types="vitest" />` — use vitest/config's defineConfig

    Create `web/src/lib/__tests__/setup.ts`:
    ```typescript
    import '@testing-library/jest-dom/vitest'
    import { cleanup } from '@testing-library/react'
    import { afterEach } from 'vitest'
    
    afterEach(() => {
      cleanup()
    })
    ```
    This file:
    - Imports jest-dom matchers for Vitest (toBeVisible, toHaveTextContent, etc.)
    - Runs cleanup after each test to unmount React components
    - MSW server setup will be added per-test-file (not globally) for flexibility
  </action>
  <acceptance_criteria>
    - `web/vitest.config.ts` exists with `environment: 'jsdom'`, `globals: true`, `setupFiles` pointing to setup.ts, `restoreMocks: true`, and `resolve.alias` mapping `@/` → `./src/`
    - `web/src/lib/__tests__/setup.ts` exists importing `@testing-library/jest-dom/vitest` and calling `cleanup` in `afterEach`
    - `bun run test` exits 0 (discovers vitest.config.ts and runs with jsdom environment)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run test 2>&1 | head -10</automated>
  </verify>
  <done>vitest.config.ts configures jsdom, globals, aliases. setup.ts provides jest-dom matchers + cleanup. `bun run test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| N/A | Phase 0 testing infrastructure. No production code modified. New devDependencies only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | T | MSW in devDeps | accept | MSW intercepts fetch in test environment only; not included in production bundle |
| T-00-04 | T | jsdom surface area | accept | jsdom is a test-only dependency; not exposed in production |
</threat_model>

<verification>
- `bun run test` exits 0 (Vitest discovers config, runs with jsdom, finds zero test files)
- `web/package.json` has all required devDependencies
- `web/tsconfig.json` has vitest types
- `web/vitest.config.ts` has correct configuration
- `web/src/lib/__tests__/setup.ts` exists and is loadable by Vitest
</verification>

<success_criteria>
- Vitest configuration complete and verified: `bun run test` works
- MSW available for API mocking
- React Testing Library + jest-dom available for component assertions
- jsdom environment configured for React component testing
- Shared setup file provides cleanup and jest-dom matchers
</success_criteria>

<output>
After completion, create `.planning/phases/00-testing-foundation/00-02-SUMMARY.md`
</output>
