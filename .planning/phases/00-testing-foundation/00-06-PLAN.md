---
phase: 00-testing-foundation
plan: 06
type: execute
wave: 3
depends_on:
  - 00-01
files_modified:
  - internal/adapters/secondary/surrealdb/time_entry_repository_test.go
  - internal/adapters/secondary/surrealdb/contract_repository_test.go
  - internal/adapters/secondary/surrealdb/customer_repository_test.go
  - internal/adapters/secondary/surrealdb/project_repository_test.go
  - internal/adapters/secondary/surrealdb/working_group_repository_test.go
  - internal/adapters/secondary/surrealdb/unit_repository_test.go
  - internal/adapters/secondary/surrealdb/invitation_repository_test.go
  - internal/adapters/secondary/surrealdb/password_reset_repository_test.go
autonomous: false
requirements: []
must_haves:
  truths:
    - "Time Entry repository tests cover Create, GetByID, List, Update, Delete operations via SurrealDB"
    - "Contract, Customer, Project repository tests cover CRUD via SurrealDB"
    - "Working Group and Unit repository tests cover CRUD via SurrealDB"
    - "Invitation repository tests cover Create, FindByEmail, Update status"
    - "Password Reset repository tests cover Create, FindByToken, MarkUsed"
    - "All new repository tests use GetTestDBWithNamespace (not GetDB singleton) per D-09/CONTEXT.md guidance"
    - "All repository tests skip gracefully when SURREALDB_URL is unset"
  artifacts:
    - path: "internal/adapters/secondary/surrealdb/time_entry_repository_test.go"
      provides: "Time entry CRUD operations against SurrealDB"
      min_lines: 80
    - path: "internal/adapters/secondary/surrealdb/contract_repository_test.go"
      provides: "Contract CRUD against SurrealDB"
      min_lines: 60
    - path: "internal/adapters/secondary/surrealdb/customer_repository_test.go"
      provides: "Customer CRUD against SurrealDB"
      min_lines: 60
    - path: "internal/adapters/secondary/surrealdb/project_repository_test.go"
      provides: "Project CRUD against SurrealDB"
      min_lines: 60
  key_links:
    - from: "internal/adapters/secondary/surrealdb/*_test.go"
      to: "internal/adapters/secondary/surrealdb/helpers.go"
      via: "GetTestDBWithNamespace"
      pattern: "GetTestDBWithNamespace"
    - from: "internal/adapters/secondary/surrealdb/*_test.go"
      to: "internal/adapters/secondary/surrealdb/*.go"
      via: "New*Repository"
      pattern: "New\\w+Repository"
---

<objective>
Plan 06: Backend repository integration tests — all untested SurrealDB repositories get CRUD coverage following the existing user_repository_test.go pattern.

Purpose: Complete the final backend layer per D-08 (all three layers: handler + service + repository). New tests use GetTestDBWithNamespace for isolation per anti-pitfall guidance from research. Tests skip gracefully when SurrealDB is unavailable (env guard pattern).

Note: This plan has autonomous: false because repository tests require a running SurrealDB instance via `docker-compose up surrealdb`. The executor should verify SurrealDB is running before attempting test execution.

Output: 8 new repository test files
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-testing-foundation/00-CONTEXT.md
@.planning/phases/00-testing-foundation/00-RESEARCH.md (# Existing Test Inventory, Architecture Patterns)

# Reference patterns
@internal/adapters/secondary/surrealdb/user_repository_test.go
@internal/adapters/secondary/surrealdb/organization_repo_test.go
@internal/adapters/secondary/surrealdb/helpers.go (GetTestDBWithNamespace)

# Source repository files under test
@internal/adapters/secondary/surrealdb/time_entry_repository.go
@internal/adapters/secondary/surrealdb/contract_repository.go
@internal/adapters/secondary/surrealdb/customer_repository.go
@internal/adapters/secondary/surrealdb/project_repository.go
@internal/adapters/secondary/surrealdb/working_group_repository.go
@internal/adapters/secondary/surrealdb/unit_repository.go
@internal/adapters/secondary/surrealdb/invitation_repository.go
@internal/adapters/secondary/surrealdb/password_reset_repository.go

# Domain models for entity construction
@internal/core/domain/time_entry/time_entry.go
@internal/core/domain/contract/contract.go
@internal/core/domain/customer/customer.go
@internal/core/domain/project/project.go
@internal/core/domain/unit/unit.go
@internal/core/domain/working_group/working_group.go
@internal/core/domain/invitation/invitation.go
@internal/core/domain/password_reset/password_reset.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Time Entry repository tests</name>
  <files>internal/adapters/secondary/surrealdb/time_entry_repository_test.go</files>
  <read_first>
    @internal/adapters/secondary/surrealdb/time_entry_repository.go
    @internal/adapters/secondary/surrealdb/helpers.go
    @internal/adapters/secondary/surrealdb/user_repository_test.go
    @internal/core/domain/time_entry/time_entry.go
  </read_first>
  <action>
    Create `internal/adapters/secondary/surrealdb/time_entry_repository_test.go` (package surrealdb):

    Package: `package surrealdb` (same-package test).

    Import: `testing`, `context`, `time`, `github.com/google/uuid`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`, plus domain and models imports needed for entity construction.

    Helper setup:
    ```go
    func setupTimeEntryRepo(t *testing.T) (*TimeEntryRepository, func()) {
        ns := "test_timeentry_" + uuid.New().String()
        db, err := GetTestDBWithNamespace(ns, ns)
        if err != nil {
            t.Skipf("SurrealDB not available: %v", err)
        }
        repo := NewTimeEntryRepository(db)
        return repo, func() { db.Close(context.Background()) }
    }
    ```
    This follows the user_repository_test.go pattern (env guard via t.Skip, unique namespace per test, t.Cleanup).

    Create a helper to seed a user (TimeEntry needs UserID, OrgID — create a minimal user in SurrealDB):

    Table-driven tests using `t.Run`:

    1. `TestTimeEntryRepo_Create`:
       - `"valid entry"` — Create entry with valid fields. Assert: no error, ID set, Status draft
       - `"with items"` — entry with 2 TimeEntryItems. Assert: items stored

    2. `TestTimeEntryRepo_GetByID`:
       - `"existing"` — Create then GetByID. Assert: fields match
       - `"not found"` — random UUID. Assert: error

    3. `TestTimeEntryRepo_List`:
       - `"by org"` — Create 3 entries for same org. Assert: List returns 3
       - `"filters by user"` — List with user filter. Assert: correct subset

    4. `TestTimeEntryRepo_Update`:
       - `"update hours and description"` — Assert: changes persist

    5. `TestTimeEntryRepo_Delete`:
       - `"soft delete"` — Assert: deleted flag set

    6. `TestTimeEntryRepo_IsPeriodLocked`:
       - `"not locked"` — Assert: false
  </action>
  <acceptance_criteria>
    - Time entry repo test compiles: `go test -c -o /dev/null ./internal/adapters/secondary/surrealdb/ -run TimeEntry`
    - Test skips with `t.Skip` when SurrealDB is unavailable
    - Test passes when SurrealDB is running (`docker-compose up` active)
    - Uses `GetTestDBWithNamespace` (not GetDB singleton)
    - Covers Create, GetByID, List, Update, Delete operations
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "TimeEntry" -v 2>&1 | tail -15</automated>
  </verify>
  <done>Time Entry repository tests compile and pass (or skip gracefully when SurrealDB unavailable).</done>
</task>

<task type="auto">
  <name>Task 2: Contract, Customer, and Project repository tests</name>
  <files>
    internal/adapters/secondary/surrealdb/contract_repository_test.go
    internal/adapters/secondary/surrealdb/customer_repository_test.go
    internal/adapters/secondary/surrealdb/project_repository_test.go
  </files>
  <read_first>
    @internal/adapters/secondary/surrealdb/contract_repository.go
    @internal/adapters/secondary/surrealdb/customer_repository.go
    @internal/adapters/secondary/surrealdb/project_repository.go
    @internal/adapters/secondary/surrealdb/helpers.go
    @internal/adapters/secondary/surrealdb/organization_repo_test.go
  </read_first>
  <action>
    Create three test files following the same pattern as the user/org repo tests:

    Each file:
    - Package `surrealdb`
    - Setup function with `GetTestDBWithNamespace` + `t.Skip` env guard
    - Unique namespace per test (`uuid.New().String()` prefix)
    - `t.Cleanup(db.Close)` for teardown
    - testify assertions

    **Contract repository test** (`contract_repository_test.go`):
    1. `TestContractRepo_Create` — valid contract, verify ID and fields
    2. `TestContractRepo_GetByID` — existing + not found
    3. `TestContractRepo_List` — by org
    4. `TestContractRepo_Update` — change name/fields
    5. `TestContractRepo_Delete` — soft delete

    **Customer repository test** (`customer_repository_test.go`):
    1. `TestCustomerRepo_Create` — valid customer with company name
    2. `TestCustomerRepo_GetByID` — existing + not found
    3. `TestCustomerRepo_ListByOrg` — by org
    4. `TestCustomerRepo_Update` — modify contact info
    5. `TestCustomerRepo_Delete` — set inactive

    **Project repository test** (`project_repository_test.go`):
    1. `TestProjectRepo_Create` — valid billable project
    2. `TestProjectRepo_GetByID` — existing + not found
    3. `TestProjectRepo_List` — by org + by contract
    4. `TestProjectRepo_Update` — change name/type
    5. `TestProjectRepo_Delete` — set inactive

    Seed prerequisite data (org for org_id foreign key refs) using direct SurrealDB queries within the test setup if needed.
  </action>
  <acceptance_criteria>
    - All three test files compile: `go test -c -o /dev/null ./internal/adapters/secondary/surrealdb/ -run "Contract|Customer|Project"`
    - Tests skip gracefully when SurrealDB unavailable
    - Tests use `GetTestDBWithNamespace`
    - Each covers Create, GetByID, List (by org), Update, Delete
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "Contract|Customer|Project" -v 2>&1 | tail -20</automated>
  </verify>
  <done>Contract, Customer, and Project repository tests compile and pass (or skip when SurrealDB unavailable).</done>
</task>

<task type="auto">
  <name>Task 3: Working Group, Unit, Invitation, and Password Reset repository tests</name>
  <files>
    internal/adapters/secondary/surrealdb/working_group_repository_test.go
    internal/adapters/secondary/surrealdb/unit_repository_test.go
    internal/adapters/secondary/surrealdb/invitation_repository_test.go
    internal/adapters/secondary/surrealdb/password_reset_repository_test.go
  </files>
  <read_first>
    @internal/adapters/secondary/surrealdb/working_group_repository.go
    @internal/adapters/secondary/surrealdb/unit_repository.go
    @internal/adapters/secondary/surrealdb/invitation_repository.go
    @internal/adapters/secondary/surrealdb/password_reset_repository.go
    @internal/adapters/secondary/surrealdb/refresh_token_repo_test.go
  </read_first>
  <action>
    Create four test files. Each follows the same pattern as Task 2: GetTestDBWithNamespace, unique namespace, t.Skip env guard, t.Cleanup, testify assertions.

    **Working Group repository test** (`working_group_repository_test.go`):
    1. `TestWorkingGroupRepo_Create` — valid group, verify fields
    2. `TestWorkingGroupRepo_GetByID` — existing + not found
    3. `TestWorkingGroupRepo_List` — by org
    4. `TestWorkingGroupRepo_Update` — rename
    5. `TestWorkingGroupRepo_Delete` — set inactive/remove

    **Unit repository test** (`unit_repository_test.go`):
    1. `TestUnitRepo_Create` — valid unit
    2. `TestUnitRepo_GetByID` — existing + not found
    3. `TestUnitRepo_List` — by org
    4. `TestUnitRepo_Update`
    5. `TestUnitRepo_Delete`

    **Invitation repository test** (`invitation_repository_test.go`):
    (Different CRUD pattern — invitations have status states)
    1. `TestInvitationRepo_Create` — email, role, org
    2. `TestInvitationRepo_FindByEmail` — existing + not found
    3. `TestInvitationRepo_ListByOrg` — pre-seed, verify list
    4. `TestInvitationRepo_UpdateStatus` — accept/reject status changes

    **Password Reset repository test** (`password_reset_repository_test.go`):
    1. `TestPasswordResetRepo_Create` — userID, token, expiry
    2. `TestPasswordResetRepo_FindByToken` — existing + not found
    3. `TestPasswordResetRepo_MarkUsed` — verify used flag set
    4. `TestPasswordResetRepo_FindByUser` — list by user
  </action>
  <acceptance_criteria>
    - All four test files compile
    - Tests skip gracefully when SurrealDB unavailable
    - Tests use `GetTestDBWithNamespace`
    - WorkingGroup and Unit cover CRUD + org list
    - Invitation covers create, find by email, list, status update
    - Password Reset covers create, find by token, mark used
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "WorkingGroup|Unit|Invitation|PasswordReset" -v 2>&1 | tail -20</automated>
  </verify>
  <done>Working Group, Unit, Invitation, Password Reset repository tests compile and pass (or skip when SurrealDB unavailable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application → SurrealDB | Repository tests use isolated namespaces per test run. Data is cleaned up by namespace teardown. No cross-test contamination. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-11 | T | SurrealDB test namespace isolation | mitigate | Each test creates unique namespace via GetTestDBWithNamespace + uuid prefix |
| T-00-12 | E | Skipped tests when SurrealDB unavailable | accept | t.Skip gracefully handles missing SurrealDB — tests don't fail in CI without DB |
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "TimeEntry"` passes (or skips)
- `go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "Contract"` passes (or skips)
- `go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "Customer"` passes (or skips)
- `go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "Project"` passes (or skips)
- `go test ./internal/adapters/secondary/surrealdb/ -count=1 -run "WorkingGroup|Unit|Invitation|PasswordReset"` passes (or skips)
</verification>

<success_criteria>
- All 8 untested repository files have test coverage
- All tests use GetTestDBWithNamespace (not GetDB singleton)
- All tests skip gracefully when SurrealDB is unavailable
- Each repository has tests covering its primary CRUD operations
</success_criteria>

<output>
After completion, create `.planning/phases/00-testing-foundation/00-06-SUMMARY.md`
</output>
