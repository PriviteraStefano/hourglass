---
phase: 07-exports
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx
  - web/src/routes/_authenticated/expenses/-components/expenses-page.tsx
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
must_haves:
  truths:
    - "Time entries page shows [List] [Calendar] [Export] tabs at the top"
    - "Expenses page shows [List] [Calendar] [Export] tabs at the top"
    - "Export tab on time entries page renders ExportForm with type='timesheets'"
    - "Export tab on expenses page renders ExportForm with type='expenses'"
    - "Switching between tabs preserves form state within the Export tab"
    - "Default tab is 'list' on both pages"
    - "Existing list and calendar functionality unchanged"
  artifacts:
    - path: "web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx"
      provides: "Tabbed layout with List/Calendar/Export tabs, Export tab renders ExportForm"
      contains: "ExportForm"
    - path: "web/src/routes/_authenticated/expenses/-components/expenses-page.tsx"
      provides: "Tabbed layout with List/Calendar/Export tabs, Export tab renders ExportForm"
      contains: "ExportForm"
  key_links:
    - from: "time-entries-page.tsx"
      to: "export-form.tsx"
      via: "<ExportForm type=\"timesheets\" />"
      pattern: "ExportForm"
    - from: "expenses-page.tsx"
      to: "export-form.tsx"
      via: "<ExportForm type=\"expenses\" />"
      pattern: "ExportForm"
---

<objective>
Add Export tabs to the time entries and expenses pages so users can export data directly from their current context without navigating to a separate page.

**Purpose:** Make exports accessible from within the time entries and expenses pages via a familiar tab interface, consistent with the existing page layout patterns.

**Output:** Tabbed layouts on both pages with [List] [Calendar] [Export] tabs.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-exports/07-CONTEXT.md
@.planning/phases/07-exports/07-UI-SPEC.md
@.planning/phases/07-exports/07-PATTERNS.md
@web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx
@web/src/routes/_authenticated/expenses/-components/expenses-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Export tab to time entries page with [List] [Calendar] [Export] tabs</name>

  <files>
    web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx
  </files>

  <read_first>
    1. web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx — Current 11-line file rendering MiniCalendar + EntryDetail in flex
    2. .planning/phases/07-exports/07-PATTERNS.md (lines 601-645) — New tabbed layout pattern for time-entries page
    3. .planning/phases/07-exports/07-UI-SPEC.md — Tab interaction contract
  </read_first>

  <action>
    Rewrite `web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx`:

    Keep all existing imports and add:
    - `import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'`
    - `import { ExportForm } from '@/components/exports/export-form'`

    Replace the single `flex` div with a `Tabs` component:

    ```tsx
    <Tabs defaultValue="list" className="p-2">
      <TabsList>
        <TabsTrigger value="list">List</TabsTrigger>
        <TabsTrigger value="calendar">Calendar</TabsTrigger>
        <TabsTrigger value="export">Export</TabsTrigger>
      </TabsList>
      <TabsContent value="list">
        {/* Existing list view content — consult current page to see if there's a List view or just Calendar */}
      </TabsContent>
      <TabsContent value="calendar">
        <div className="flex">
          <MiniCalendar/>
          <EntryDetail/>
        </div>
      </TabsContent>
      <TabsContent value="export">
        <ExportForm type="timesheets" />
      </TabsContent>
    </Tabs>
    ```

    Key details:
    - Default tab is "list" (per UI-SPEC: "Default: 'List' tab active")
    - ExportTab: renders `<ExportForm type="timesheets" />` (no user/project filters for in-page tab)
    - The current `flex p-2` content goes into the "calendar" tab content
    - Include a "list" tab as a placeholder for a future list view if it doesn't exist yet — if there's no separate list component yet, the list tab can render a message or be omitted. Check the current component structure.
    - Per D-21: Export replaces the content area entirely (no slide-in panel)
    - Per UI-SPEC: switching away from Export tab restores previous view; no state persistence needed
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `bun run build` passes (TypeScript compiles without errors)
    - `web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx` imports `Tabs`, `TabsContent`, `TabsList`, `TabsTrigger` from `@/components/ui/tabs`
    - `web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx` imports `ExportForm` from `@/components/exports/export-form`
    - Component renders `<Tabs defaultValue="list">` with 3 triggers: List, Calendar, Export
    - Export tab content is `<ExportForm type="timesheets" />`
    - Calendar tab renders `<MiniCalendar/>` and `<EntryDetail/>`
    - Existing MiniCalendar and EntryDetail imports are preserved
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Export tab to expenses page with [List] [Calendar] [Export] tabs</name>

  <files>
    web/src/routes/_authenticated/expenses/-components/expenses-page.tsx
  </files>

  <read_first>
    1. web/src/routes/_authenticated/expenses/-components/expenses-page.tsx — Current 11-line file rendering ExpenseCalendar + ExpenseDetail in flex
    2. .planning/phases/07-exports/07-PATTERNS.md (lines 649-691) — New tabbed layout for expenses page
    3. .planning/phases/07-exports/07-UI-SPEC.md — Tab interaction contract (same as time-entries)
  </read_first>

  <action>
    Rewrite `web/src/routes/_authenticated/expenses/-components/expenses-page.tsx`:

    Follow the same pattern as Task 1:

    - Add imports for `Tabs, TabsContent, TabsList, TabsTrigger` from `@/components/ui/tabs`
    - Add import for `ExportForm` from `@/components/exports/export-form`
    - Wrap existing content in Tabs component:
      ```tsx
      <Tabs defaultValue="list" className="p-2">
        <TabsList>
          <TabsTrigger value="list">List</TabsTrigger>
          <TabsTrigger value="calendar">Calendar</TabsTrigger>
          <TabsTrigger value="export">Export</TabsTrigger>
        </TabsList>
        <TabsContent value="list">{/* list placeholder or empty */}</TabsContent>
        <TabsContent value="calendar">
          <div className="flex">
            <ExpenseCalendar/>
            <ExpenseDetail/>
          </div>
        </TabsContent>
        <TabsContent value="export">
          <ExportForm type="expenses" />
        </TabsContent>
      </Tabs>
      ```
    - Default tab is "list"
    - Export tab renders `<ExportForm type="expenses" />` (per D-15, UI-SPEC)
    - Calendar tab renders existing `<ExpenseCalendar/>` and `<ExpenseDetail/>` in flex layout
    - List tab shows empty state or placeholder for future list view
    - Preserve all existing imports (ExpenseCalendar, ExpenseDetail)
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `bun run build` passes (TypeScript compiles without errors)
    - `web/src/routes/_authenticated/expenses/-components/expenses-page.tsx` imports `Tabs`, `TabsContent`, `TabsList`, `TabsTrigger` from `@/components/ui/tabs`
    - `web/src/routes/_authenticated/expenses/-components/expenses-page.tsx` imports `ExportForm` from `@/components/exports/export-form`
    - Component renders `<Tabs defaultValue="list">` with 3 triggers: List, Calendar, Export
    - Export tab content is `<ExportForm type="expenses" />`
    - Calendar tab renders `<ExpenseCalendar/>` and `<ExpenseDetail/>`
    - Existing ExpenseCalendar and ExpenseDetail imports are preserved
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts This Phase Produces

### Modified Files
- `web/src/routes/_authenticated/time-entries/-components/time-entries-page.tsx`:
  - Wrapped in `Tabs` component with `TabsList` (List, Calendar, Export triggers)
  - Existing MiniCalendar + EntryDetail moved to Calendar tab content
  - Export tab: renders `<ExportForm type="timesheets" />`
  - List tab: placeholder for future list view

- `web/src/routes/_authenticated/expenses/-components/expenses-page.tsx`:
  - Wrapped in `Tabs` component with `TabsList` (List, Calendar, Export triggers)
  - Existing ExpenseCalendar + ExpenseDetail moved to Calendar tab content
  - Export tab: renders `<ExportForm type="expenses" />`
  - List tab: placeholder for future list view
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI component | Tab navigation — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Denial of Service | Export tab UI | accept | No new DoS vector — ExportForm already handles 1-year range enforcement and 60s timeout. Tab switching is client-side only. |
</threat_model>

<verification>
1. `cd web && bun run build` passes (TypeScript compiles)
2. `bun run lint` passes
3. Manual: Navigate to /time-entries, verify List/Calendar/Export tabs render
4. Manual: Navigate to /expenses, verify List/Calendar/Export tabs render
5. Manual: Switch between tabs on both pages, verify content changes correctly
6. Manual: Export tab on time entries shows ExportForm with type=timesheets
7. Manual: Export tab on expenses shows ExportForm with type=expenses
</verification>

<success_criteria>
- Time entries page: 3 visible tabs (List, Calendar, Export), Export tab renders ExportForm with timesheets type
- Expenses page: 3 visible tabs (List, Calendar, Export), Export tab renders ExportForm with expenses type
- Switching tabs shows correct content (Calendar shows calendar+detail, Export shows form)
- Default tab is List on both pages
- Both files pass TypeScript compilation with no errors
- All existing functionality (calendar, detail view) preserved under Calendar tab
</success_criteria>

<output>
Create `.planning/phases/07-exports/07-03-SUMMARY.md` when done
</output>
