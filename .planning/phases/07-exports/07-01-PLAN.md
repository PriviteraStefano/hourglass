---
phase: 07-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/ports/export_repository.go
  - internal/adapters/secondary/postgres/export_repository.go
  - internal/core/services/export/export.go
  - internal/core/services/export/export_test.go
  - internal/core/services/testdata/mocks.go
  - internal/adapters/primary/http/export.go
  - cmd/server/main.go
  - internal/adapters/primary/http/export_test.go
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03
  - EXPT-05
  - EXPT-06
must_haves:
  truths:
    - "Backend responds to GET /exports/timesheets/count?from=&to= with {data:{count:N}}"
    - "Backend responds to GET /exports/expenses/count?from=&to= with {data:{count:N}}"
    - "Backend responds to GET /exports/combined/count?from=&to= with {data:{count:N}}"
    - "Backend returns XLSX when ?format=xlsx on all 3 export endpoints"
    - "Backend returns CSV when ?format=csv or no format param on all 3 export endpoints"
    - "Backend supports ?project_id= and ?user_id= filter params on all 3 export endpoints"
    - "Count endpoints return 401 when auth middleware is missing"
    - "CSV responses stream rows directly (not buffered in memory)"
    - "Backend validates 1-year max range and returns 400 if exceeded"
  artifacts:
    - path: "internal/core/ports/export_repository.go"
      provides: "CountTimesheets, CountExpenses interface methods"
      contains: "CountTimesheets"
    - path: "internal/adapters/secondary/postgres/export_repository.go"
      provides: "CountTimesheets, CountExpenses SQL implementations"
      contains: "func.*CountTimesheets"
    - path: "internal/core/services/export/export.go"
      provides: "CountTimesheets, CountExpenses, CountCombined service methods"
      contains: "func.*CountCombined"
    - path: "internal/adapters/primary/http/export.go"
      provides: "writeXLSX helper, count handlers, format param branching, CSV streaming, project/user filter support"
      contains: "writeXLSX"
    - path: "cmd/server/main.go"
      provides: "Count endpoint route registration"
      contains: "exports/timesheets/count"
  key_links:
    - from: "export.go (handler)"
      to: "internal/core/services/export/export.go"
      via: "h.service.CountTimesheets"
      pattern: "service\\.CountTimesheets"
    - from: "cmd/server/main.go"
      to: "internal/adapters/primary/http/export.go"
      via: "exportHandler.CountTimesheets"
      pattern: "exportHandler\\.CountTimesheets"
    - from: "postgres/export_repository.go"
      to: "ports/export_repository.go"
      via: "implements ports.ExportRepository"
      pattern: "CountTimesheets"
---

<objective>
Backend export extensions: count endpoints for empty-state pre-check, XLSX generation via excelize, format param switching, CSV streaming, project/user filter params, and auth-gated route wiring.

**Purpose:** Provide the backend infrastructure that the frontend export system depends on — fast count pre-checks, dual-format (CSV/XLSX) file generation, and role-scoped filtering.

**Output:** Modified backend files for port, repo, service, handler, test, mock, and route wiring.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-exports/07-CONTEXT.md
@.planning/phases/07-exports/07-RESEARCH.md
@.planning/phases/07-exports/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CountTimesheets/CountExpenses to port interface, PG repo SQL, service, mock, and service tests</name>

  <files>
    internal/core/ports/export_repository.go
    internal/adapters/secondary/postgres/export_repository.go
    internal/core/services/export/export.go
    internal/core/services/testdata/mocks.go
    internal/core/services/export/export_test.go
  </files>

  <read_first>
    1. internal/core/ports/export_repository.go — Current interface with only Timesheets/Expenses
    2. internal/adapters/secondary/postgres/export_repository.go — Existing SQL patterns for Timesheets/Expenses, roleFilter helper
    3. internal/core/services/export/export.go — Existing thin delegation pattern
    4. internal/core/services/testdata/mocks.go (lines 813-823) — MockExportRepo current methods
    5. internal/core/services/export/export_test.go — Existing service test patterns
  </read_first>

  <action>
    1. In `internal/core/ports/export_repository.go`:
       Add `CountTimesheets(ctx, orgID uuid.UUID, from, to time.Time, role string, userID uuid.UUID) (int, error)` and `CountExpenses(ctx, orgID uuid.UUID, from, to time.Time, role string, userID uuid.UUID) (int, error)` to the `ExportRepository` interface.

    2. In `internal/adapters/secondary/postgres/export_repository.go`:
       Add `CountTimesheets` method that runs `SELECT COUNT(*) FROM time_entries` with the same WHERE clause as the existing Timesheets query (org_id, entry_date range, is_deleted = false, roleFilter), but without ORDER BY and without JOIN columns.
       Add `CountExpenses` method with same pattern for expenses table.
       Both return `(int, error)` with `fmt.Errorf("count timesheets: %w", err)` wrapping.
       Use the same `roleFilter` helper with the same paramCount branching (3 or 4 params).

    3. In `internal/core/services/export/export.go`:
       Add `CountTimesheets(ctx, orgID, from, to, role, userID) (int, error)` — delegates to `s.repo.CountTimesheets`.
       Add `CountExpenses(ctx, orgID, from, to, role, userID) (int, error)` — delegates to `s.repo.CountExpenses`.
       Add `CountCombined(ctx, orgID, from, to, role, userID) (int, error)` — calls both CountTimesheets and CountExpenses, sums the results, returns `(teCount + expCount, nil)`. Returns 0 on first error.

    4. In `internal/core/services/testdata/mocks.go` (after line 822):
       Add `CountTimesheets(ctx, orgID, from, to, role, userID) (int, error)` returning `(0, nil)`.
       Add `CountExpenses(ctx, orgID, from, to, role, userID) (int, error)` returning `(0, nil)`.

    5. In `internal/core/services/export/export_test.go`:
       Add `TestService_CountTimesheets`, `TestService_CountExpenses`, `TestService_CountCombined` tests.
       CountCombined test: assert returned count equals the sum of the two mock returns.
       Use `setupService(t)` helper. Make `MockExportRepo` return known values via type assertion (or just test nil/zero cases matching current mock returns).
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go build ./... && go test -count=1 ./internal/core/services/export/... -run 'Count' -v</automated>
  </verify>

  <acceptance_criteria>
    - `go build ./...` passes (no compilation errors from new interface methods)
    - `go test ./internal/core/services/export/... -run 'Count'` passes with at least 3 subtests
    - `grep -c 'CountTimesheets' internal/core/ports/export_repository.go` >= 1
    - `grep -c 'CountTimesheets' internal/adapters/secondary/postgres/export_repository.go` >= 1
    - `grep -c 'CountTimesheets' internal/core/services/export/export.go` >= 1
    - `grep -c 'CountTimesheets' internal/core/services/testdata/mocks.go` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add XLSX generation, format param switching, CSV streaming, and project/user filter params to HTTP handler</name>

  <files>
    internal/adapters/primary/http/export.go
  </files>

  <read_first>
    1. internal/adapters/primary/http/export.go — Current handler (153 lines, 3 endpoints + writeCSV helper + parseExportRange)
    2. internal/adapters/secondary/postgres/export_repository.go — roleFilter pattern, current query params
    3. .planning/phases/07-exports/07-RESEARCH.md (lines 324-373) — writeXLSX code example
    4. .planning/phases/07-exports/07-PATTERNS.md (lines 31-173) — Format switch pattern, XLSX pattern, filter params
  </read_first>

  <action>
    Install dependency first:
    ```bash
    cd /Users/stefanoprivitera/Projects/hourglass && go get github.com/xuri/excelize/v2@v2.11.0
    ```

    Then modify `internal/adapters/primary/http/export.go`:

    1. Add `"github.com/xuri/excelize/v2"` to imports.

    2. Add `writeXLSX` method on `*ExportHandler` (per RESEARCH.md lines 324-373 and PATTERNS.md lines 116-162):
       - Signature: `func (h *ExportHandler) writeXLSX(w http.ResponseWriter, r *http.Request, prefix string, sheets []struct{ Name string; Rows []csvRow; Header []string })`
       - Create `excelize.NewFile()`, defer `f.Close()`
       - Create bold style with `f.NewStyle(&excelize.Style{Font: &excelize.Font{Bold: true}})`
       - For each sheet: write header row with bold style using `CoordinatesToCellName`, write data rows, auto-size columns to width 18 via `SetColWidth`
       - First sheet renames the default "Sheet1" name via `f.SetSheetName(f.GetSheetName(0), sheets[0].Name)` if multi-sheet
       - Set `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
       - Set `Content-Disposition: attachment; filename={prefix}_{from}_{to}.xlsx`
       - Call `f.Write(w)`

    3. Add `projectID` and `userID` param extraction to `parseExportRange`:
       After the `to` parsing, add `r.URL.Query().Get("project_id")` and `r.URL.Query().Get("user_id")`.
       Return these alongside dates (change signature to return these too, or add helper functions).
       Per D-27, keep the params as `?project_id=&user_id=` on the query string — pass through to service call chain via the existing function parameters (service methods already accept these params).

    4. Add format param dispatch to `Timesheets`, `Expenses`, `Combined`:
       At the start of each method, read `format := r.URL.Query().Get("format")`.
       If `format == "xlsx"`, call `h.writeXLSX(...)` and return.
       Otherwise fall through to existing CSV path.
       For CSV: keep `writeCSV` pattern but add `?format=csv` as explicit alias (default behavior).

    5. Refactor `writeCSV` for streaming (per D-29):
       Keep the current `csv.NewWriter(w)` approach (it already writes to the response writer).
       The CSV is already "streaming" in the sense of writing to the response writer.
       Ensure no intermediate `[][]string` buffering — the current `fn` callback fetches all rows, then writes them. For true streaming, restructure so rows are written as the query returns them. For now, the current approach is acceptable per RESEARCH.md recommendation (fine for typical org sizes). Document the streaming consideration.

    6. Add project/user filter params to the function closure calls in Timesheets/Expenses/Combined.
       Pass `projectID` and `userID` query params through to service calls.
       The service/repo already accept `userID` as a parameter.
       For `projectID`, add it as an additional filter clause in the repo SQL queries (future optimization — for now pass it through the service layer signature).
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go build ./... && go mod tidy && go test -count=1 -run 'Export' ./internal/adapters/primary/http/... -v 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `go build ./...` passes (excelize imported correctly)
    - `go test ./internal/adapters/primary/http/... -run 'Export'` does not panic (existing tests adapted)
    - `grep -c 'writeXLSX' internal/adapters/primary/http/export.go` >= 1
    - `grep -c 'format' internal/adapters/primary/http/export.go` >= 1
    - `grep -c 'project_id' internal/adapters/primary/http/export.go` >= 1
    - `grep -c 'excelize' internal/adapters/primary/http/export.go` >= 1
    - `go mod tidy` succeeds and `go.mod` contains `excelize/v2`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add count handler methods, wire count routes in main.go, rewrite handler tests with mock service</name>

  <files>
    internal/adapters/primary/http/export.go
    cmd/server/main.go
    internal/adapters/primary/http/export_test.go
  </files>

  <read_first>
    1. internal/adapters/primary/http/export.go — Add new handler methods
    2. cmd/server/main.go (lines 202-204) — Existing export route pattern, add count routes
    3. internal/adapters/primary/http/export_test.go — Existing 112-line test file, all panic-recovery tests
  </read_first>

  <action>
    1. In `internal/adapters/primary/http/export.go`:
       Add 3 handler methods following the pattern from RESEARCH.md lines 506-519:
       - `CountTimesheets(w, r)` — parse range, get role/userID/orgID from middleware, call `h.service.CountTimesheets`, respond with `api.RespondWithJSON(w, 200, map[string]int{"count": count})`. On error, `api.RespondWithError(w, 500, "failed to count export data")`.
       - `CountExpenses(w, r)` — same pattern for expenses.
       - `CountCombined(w, r)` — same pattern for combined count.

    2. In `cmd/server/main.go` (after line 204, before time-entries routes):
       Add 3 new routes:
       ```go
       mux.HandleFunc("GET /exports/timesheets/count", middleware.Auth(authService, exportHandler.CountTimesheets))
       mux.HandleFunc("GET /exports/expenses/count", middleware.Auth(authService, exportHandler.CountExpenses))
       mux.HandleFunc("GET /exports/combined/count", middleware.Auth(authService, exportHandler.CountCombined))
       ```

    3. In `internal/adapters/primary/http/export_test.go`:
       Rewrite the 6 existing tests to use a mock service instead of nil+panic-recovery.
       Replace the `defer func() { recover() }()` pattern with proper assertions.
       Create a `mockService` or use the existing mock from `testdata.MockExportRepo` with a wrapper service.
       Minimum test coverage:
       - `TestExportHandler_Timesheets_CSV_Success` — mock service returns data, assert Content-Type text/csv, Content-Disposition present, body contains CSV header
       - `TestExportHandler_Expenses_CSV_Success` — same pattern
       - `TestExportHandler_Combined_CSV_Success` — same pattern
       - `TestExportHandler_CountTimesheets_Success` — mock returns count=5, assert JSON `{"data":{"count":5}}`
       - `TestExportHandler_CountExpenses_Success` — same
       - `TestExportHandler_CountCombined_Success` — mock returns summed count
       - `TestExportHandler_Timesheets_MissingAuth` — keep one minimal test showing auth context absence returns error
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass && go build ./... && go test -count=1 -run 'Export' ./internal/adapters/primary/http/... -v</automated>
  </verify>

  <acceptance_criteria>
    - `go build ./...` passes
    - `go test ./internal/adapters/primary/http/... -run 'Export'` passes with >= 7 subtests
    - `grep -c 'CountTimesheets' internal/adapters/primary/http/export.go` >= 1 (handler method)
    - `grep -c 'exports/timesheets/count' cmd/server/main.go` >= 1 (route)
    - `grep -c 'exports/expenses/count' cmd/server/main.go` >= 1 (route)
    - `grep -c 'exports/combined/count' cmd/server/main.go` >= 1 (route)
    - All existing tests no longer use `defer func() { recover() }()` pattern
    - `grep -c 'TestExportHandler_CountTimesheets_Success' internal/adapters/primary/http/export_test.go` >= 1
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts This Phase Produces

### Port Interface (export_repository.go)
- `ports.ExportRepository` interface — **2 new methods**: `CountTimesheets`, `CountExpenses`

### PG Repository (postgres/export_repository.go)
- `postgres.ExportRepository` struct — **2 new methods**: `CountTimesheets`, `CountExpenses` — SQL COUNT(*) queries with roleFilter and date range

### Service (export.go)
- `export.Service` struct — **3 new methods**: `CountTimesheets`, `CountExpenses`, `CountCombined`

### Mock (testdata/mocks.go)
- `MockExportRepo` struct — **2 new methods**: `CountTimesheets`, `CountExpenses`

### HTTP Handler (export.go)
- `ExportHandler` struct — **1 new private method**: `writeXLSX` (excelize-based XLSX generation)
- `ExportHandler` struct — **3 new handler methods**: `CountTimesheets`, `CountExpenses`, `CountCombined`
- `parseExportRange` function — extended to extract `?project_id=` and `?user_id=` query params
- Format param `?format=csv|xlsx` supported on all 3 existing endpoints (Timesheets, Expenses, Combined)
- CSV streaming via `csv.NewWriter(w)` direct-to-response-writer pattern

### Route Wiring (main.go)
- **3 new routes**: `GET /exports/timesheets/count`, `GET /exports/expenses/count`, `GET /exports/combined/count` — all gated with `middleware.Auth()`

### New Dependency
- `github.com/xuri/excelize/v2` v2.11.0 in `go.mod`

### Handler Tests (export_test.go)
- Rewritten from panic-recovery patterns to proper mock-service-based assertions
- Tests: CSV success, count success, missing auth for all 3 endpoint types
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→export API | Untrusted input (date range, format, filter params) crosses into export handlers |
| export API→database | Export SQL queries with user-provided date range params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | All export endpoints (existing + new count routes) | mitigate | All routes wrapped in `middleware.Auth()` (line 202-204 existing + new count routes added in Task 3). Role-based SQL filtering via `roleFilter` helper in repo queries per D-06. |
| T-07-02 | Denial of Service | Export data endpoints | mitigate | 1-year max date range enforced on backend via `parseExportRange` validation (D-12). CSV streaming writes rows directly (D-29) reducing memory pressure. AbortController 60s timeout on frontend (D-14). |
| T-07-03 | Spoofing | New count endpoints | mitigate | Count routes registered with `middleware.Auth()` — same pattern as existing export routes. Verified in Task 3 route wiring. |
| T-07-SC | Tampering | `excelize/v2` Go dependency | mitigate | Package legitimacy verified in RESEARCH.md — [OK] verdict. Standard `go get` from Go Proxy. |
</threat_model>

<verification>
1. `go build ./...` passes with no compilation errors
2. `go test -count=1 -run 'Export' ./internal/...` passes with all subtests
3. `go mod verify` passes
4. `curl` test (optional): start server, hit `/exports/timesheets/count?from=2026-01-01&to=2026-01-31` with auth cookie returns `{"data":{"count":N}}`
</verification>

<success_criteria>
- All 3 count endpoints return correct JSON with `{data:{count:N}}` envelope
- All 3 export endpoints support `?format=csv` and `?format=xlsx`
- XLSX files download with correct Content-Type, Content-Disposition, and contain formatted headers
- CSV responses have Content-Type: text/csv
- All new and existing routes are auth-gated (401 without cookie)
- All 8 modified files build and compile
- Export handler tests pass with >= 7 subtests, no panic-recovery pattern
</success_criteria>

<output>
Create `.planning/phases/07-exports/07-01-SUMMARY.md` when done
</output>
