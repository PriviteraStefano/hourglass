---
phase: 07-exports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/use-download.ts
  - web/src/api/exports.ts
  - web/src/components/exports/export-form.tsx
  - web/src/routes/_authenticated/exports/-components/exports-page.tsx
  - web/src/components/layout/sidebar.tsx
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03
  - EXPT-04
  - EXPT-05
must_haves:
  truths:
    - "User can open the combined export page at /exports with date range picker, format selector, and Download button"
    - "User sees preset period buttons (This Month, Last Month, This Quarter, This Year) that auto-fill the date range"
    - "User sees a segmented control toggle between CSV and XLSX format"
    - "Clicking Download triggers count pre-check first; if count=0 shows toast 'No data to export' and stops"
    - "If count>0, download fires via fetch+blob with 60s timeout, shows spinner on button, toast promise for feedback"
    - "Date range defaults to current month on page load"
    - "Sidebar shows Exports nav item in Tracking section with Download icon"
    - "Client-side validation prevents submitting date range > 1 year or from >= to"
    - "Filename comes from Content-Disposition header (not frontend-constructed)"
    - "Error responses show toast 'Export failed. Please try again.'"
  artifacts:
    - path: "web/src/lib/use-download.ts"
      provides: "useDownload() hook with download(path, opts) function and isPending state"
      min_lines: 60
    - path: "web/src/api/exports.ts"
      provides: "ExportApis.exportCountQueryOpts and getExportUrl helper"
      min_lines: 25
    - path: "web/src/components/exports/export-form.tsx"
      provides: "Shared ExportForm component with date picker, format selector, preset periods, download button"
      min_lines: 100
    - path: "web/src/routes/_authenticated/exports/-components/exports-page.tsx"
      provides: "Combined export page using ExportForm type='combined' with user/project filters"
      min_lines: 30
    - path: "web/src/components/layout/sidebar.tsx"
      provides: "Exports nav item moved to Tracking section"
      contains: "Exports"
  key_links:
    - from: "export-form.tsx"
      to: "use-download.ts"
      via: "import { useDownload } from '@/lib/use-download'"
      pattern: "useDownload"
    - from: "export-form.tsx"
      to: "api/exports.ts"
      via: "import { exportCountQueryOpts } from '@/api/exports'"
      pattern: "exportCountQueryOpts"
    - from: "exports-page.tsx"
      to: "export-form.tsx"
      via: "<ExportForm type=\"combined\" />"
      pattern: "ExportForm"
---

<objective>
Frontend core: shared download hook, export API module, shared ExportForm component with date range picker and format selector, combined export page rewrite, and sidebar navigation item.

**Purpose:** Build the reusable frontend infrastructure that both in-page export tabs (Plan 03) and the standalone exports page use.

**Output:** Hook in `web/src/lib/`, API module in `web/src/api/`, shared component in `web/src/components/exports/`, rewritten page component, modified sidebar.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-exports/07-CONTEXT.md
@.planning/phases/07-exports/07-RESEARCH.md
@.planning/phases/07-exports/07-PATTERNS.md
@.planning/phases/07-exports/07-UI-SPEC.md
@web/src/lib/api.ts
@web/src/api/time-entries.ts
@web/src/routes/_authenticated/exports/-components/exports-page.tsx
@web/src/components/layout/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useDownload hook and rewrite api/exports.ts with count query options</name>

  <files>
    web/src/lib/use-download.ts
    web/src/api/exports.ts
  </files>

  <read_first>
    1. web/src/lib/api.ts — Existing fetch+auth retry pattern (for credentials/include, error parsing)
    2. web/src/api/time-entries.ts — queryOptions pattern (timeEntriesForMonthQueryOpts, queryKey)
    3. web/src/api/exports.ts — Current 6-line file (getExportUrl helper)
    4. .planning/phases/07-exports/07-RESEARCH.md (lines 422-488) — useDownload hook example
    5. .planning/phases/07-exports/07-RESEARCH.md (lines 492-503) — count query options example
    6. .planning/phases/07-exports/07-PATTERNS.md (lines 379-445) — Full useDownload hook implementation pattern
    7. .planning/phases/07-exports/07-PATTERNS.md (lines 449-483) — API module pattern
  </read_first>

  <action>
    Create `web/src/lib/use-download.ts`:
    - Export `function useDownload()` returning `{ download, isPending }`
    - `isPending` is a `boolean` state variable (useState)
    - `download` is a `useCallback` accepting `(path: string, options?: { timeout?: number })`
    - Inside download:
      - Set `isPending(true)`, create `AbortController` with 60s default timeout (per D-14)
      - Fetch with `credentials: 'include'` and abort signal (per D-01, D-10)
      - Check `Content-Type` header:
        - If `text/csv` or `application/vnd.openxmlformats...` → success path: `response.blob()`, parse `Content-Disposition` for filename via regex `/filename=(.+)/`, create blob URL, programmatic `<a>` click, revoke URL (per D-01, D-03, D-04)
        - If `application/json` or `!response.ok` → parse JSON error body, throw `new Error(body.error || 'Export failed. Please try again.')` (per D-31)
      - Wrap in try/catch: catch shows `toast.error('Export failed. Please try again.')` (per D-31)
      - Finally: `setIsPending(false)`, clear timeout
    - Do NOT include auth retry logic (the backend returns 401, and the useDownload hook for file downloads just shows the error toast; auth retry happens at middleware/api.ts level per D-10)

    Rewrite `web/src/api/exports.ts`:
    - Import `{ queryOptions }` from `@tanstack/react-query`
    - Import `{ api }` from `@/lib/api.ts`
    - Export `exportCountQueryOpts(type: 'timesheets' | 'expenses' | 'combined', from: string, to: string, projectId?: string, userId?: string)` returning `queryOptions({ queryKey: ['exports', 'count', type, from, to], queryFn: () => api<{ count: number }>(\`/exports/${type}/count?from=${from}&to=${to}\`) })` (per D-28)
    - Export `getExportUrl(type: 'timesheets' | 'expenses' | 'combined', from: string, to: string, format: 'csv' | 'xlsx', projectId?: string, userId?: string)` returning the full URL with query params (per D-17, D-27)
    - Export namespace `ExportApis` with `{ exportCountQueryOpts, getExportUrl }` (following TimeEntriesApis pattern)
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `bun run build` passes (TypeScript compiles without errors)
    - `web/src/lib/use-download.ts` exists with exported `useDownload` function
    - `web/src/lib/use-download.ts` contains `AbortController`, `credentials: 'include'`, `URL.createObjectURL`
    - `web/src/api/exports.ts` exports `exportCountQueryOpts`, `getExportUrl`, and `ExportApis`
    - `ExportApis.exportCountQueryOpts` returns queryOptions with correct queryKey ['exports', 'count', ...]
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create shared ExportForm component with date range picker, format selector, preset buttons, and download button</name>

  <files>
    web/src/components/exports/export-form.tsx
  </files>

  <read_first>
    1. .planning/phases/07-exports/07-UI-SPEC.md — Complete interaction contract, copywriting, states summary
    2. .planning/phases/07-exports/07-RESEARCH.md (lines 377-419) — react-day-picker range mode + presets example
    3. .planning/phases/07-exports/07-RESEARCH.md (lines 422-488) — useDownload hook example
    4. .planning/phases/07-exports/07-PATTERNS.md (lines 486-549) — ExportForm props, state, presets, segmented control, validation
    5. web/src/components/ui/calendar.tsx — Existing calendar component (react-day-picker wrapper)
    6. web/src/routes/_authenticated/exports/-components/exports-page.tsx — Current form layout (reference for form patterns)
  </read_first>

  <action>
    Create `web/src/components/exports/export-form.tsx`:

    **Props interface:**
    ```typescript
    interface ExportFormProps {
      type: 'timesheets' | 'expenses' | 'combined'
      showUserFilter?: boolean
      showProjectFilter?: boolean
    }
    ```

    **State:**
    - `range: DateRange | undefined` — default to `{ from: startOfMonth(new Date()), to: endOfMonth(new Date()) }` (per D-13, D-26)
    - `format: 'csv' | 'xlsx'` — default `'csv'` (per D-17)
    - Use `useDownload()` hook for `{ download, isPending }`

    **Layout (per UI-SPEC spacing scale):**
    - Wrapped in `Card` component with `max-w-lg` and standard padding
    - Card has title and description (per UI-SPEC copywriting)
    - Inside CardContent: `space-y-6` layout

    **Date range section:**
    - Preset buttons row: "This Month", "Last Month", "This Quarter", "This Year" using date-fns (per D-25, UI-SPEC presets)
      - `startOfMonth`, `endOfMonth`, `subMonths`, `startOfQuarter`, `endOfQuarter`, `startOfYear`, `endOfYear`
      - Buttons use `variant="outline" size="sm"`
      - Active preset highlighted with `variant="secondary"` when currently selected
    - Calendar popover: `Popover` containing `DayPicker` with `mode="range"` (per D-24)
      - Trigger button shows formatted range or "Select date range" placeholder (per UI-SPEC copy)
      - Format: `format(range.from, "dd MMM yyyy") - format(range.to, "dd MMM yyyy")`

    **Format selector section (per D-33):**
    - Segmented control: button group `flex rounded-lg border p-1`
    - Two buttons: "CSV" and "XLSX"
    - Active button: `variant="default"`, inactive: `variant="ghost"`
    - Uses `format` state to toggle

    **Filter section (when showUserFilter or showProjectFilter is true):**
    - For combined export: show user filter Combobox (for managers/finance) and project filter Combobox
    - Use existing `Combobox` component from `@/components/ui/combobox.tsx`

    **Download button (per D-06, D-08):**
    - Full-width Button with `Download` lucide icon
    - Label: "Download" when idle, "Downloading..." when `isPending` (per UI-SPEC copy)
    - Disabled when `isPending`, or when `range` is invalid

    **Client-side validation (per D-11, D-12, D-14):**
    - `const isValid = range?.from && range?.to && range.from < range.to && (range.to.getTime() - range.from.getTime()) <= 365 * 24 * 60 * 60 * 1000`
    - Show inline validation message: "Date range must be within 1 year" or "Start date must be before end date" (per UI-SPEC copy)

    **Download handler logic (per D-30, UI-SPEC download flow):**
    1. If `!isValid` → show validation toast, return
    2. Call `exportCountQueryOpts(type, fromISO, toISO)` count query first
    3. If count === 0 → `toast('No data to export')`, return (per D-30)
    4. If count > 0 → call `download(url)` where url is from `getExportUrl(type, fromISO, toISO, format)`
    5. Toast promise wrapping: loading "Generating export...", success "Download started", error "Export failed. Please try again." (per D-05, UI-SPEC copy)

    **Mobile layout (per D-32):**
    - Form stacks `flex-col` on mobile
    - Calendar popover works as-is (already a popover)
    - Preset buttons use `overflow-x-auto` on small screens
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `bun run build` passes (TypeScript compiles without errors)
    - `web/src/components/exports/export-form.tsx` exists
    - Component imports from: `react-day-picker`, `date-fns`, `lucide-react`, `@/lib/use-download`, `@/api/exports`, `@/components/ui/button`, `@/components/ui/popover`, `@/components/ui/card`
    - Component uses `useDownload()` hook
    - Component has preset buttons for This Month, Last Month, This Quarter, This Year
    - Component has segmented control for CSV/XLSX format
    - Component validates date range (from < to, max 1 year)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite combined exports page and move sidebar nav item to Tracking section</name>

  <files>
    web/src/routes/_authenticated/exports/-components/exports-page.tsx
    web/src/components/layout/sidebar.tsx
  </files>

  <read_first>
    1. web/src/routes/_authenticated/exports/-components/exports-page.tsx — Current 116-line page with Select dropdown, date inputs, direct `<a>` download
    2. web/src/routes/_authenticated/exports/index.tsx — Current route file (verify it stays unchanged)
    3. web/src/components/layout/sidebar.tsx — navItems array (line 37-41) and settingsItems (line 50-53)
    4. .planning/phases/07-exports/07-PATTERNS.md (lines 552-596) — exports-page.tsx rewrite pattern
    5. .planning/phases/07-exports/07-PATTERNS.md (lines 695-717) — sidebar move pattern
  </read_first>

  <action>
    1. Rewrite `web/src/routes/_authenticated/exports/-components/exports-page.tsx`:
       - Replace the entire content with the new combined export page
       - Import `ExportForm` from `@/components/exports/export-form`
       - Import `FileDown` from `lucide-react`
       - Layout: `div className="p-6 space-y-6"` with heading section (FileDown icon + h1 "Exports" + subtitle) (per UI-SPEC copy)
       - Subtitle: "Download CSV or Excel reports for timesheets, expenses, or combined data" (per UI-SPEC copy)
       - Render `<ExportForm type="combined" showUserFilter={true} showProjectFilter={true} />`
       - Remove all old state and handlers (exportType, from, to, handleDownload, getExportUrl import, Select, Input, etc.)

    2. In `web/src/components/layout/sidebar.tsx` (per D-23, PATTERNS.md lines 695-717):
       - Move `{ label: 'Exports', href: '/exports', icon: DownloadIcon }` from `settingsItems` array to `navItems` array
       - Place it after the Approvals entry in `navItems` (index 3 in the Tracking section)
       - Order: Time → Expenses → Approvals → Exports
       - Remove it from `settingsItems` array
       - `settingsItems` should now only contain: `{ label: 'Settings', href: '/settings', icon: SettingsIcon, disabled: true }`
       - Keep `DownloadIcon` import (already imported at line 13)
  </action>

  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `bun run build` passes
    - `web/src/routes/_authenticated/exports/-components/exports-page.tsx` renders `<ExportForm type="combined" showUserFilter={true} showProjectFilter={true} />`
    - `web/src/components/layout/sidebar.tsx` has Exports in `navItems` array (after Approvals, before closing bracket)
    - `web/src/components/layout/sidebar.tsx` does NOT have Exports in `settingsItems` array
    - Sidebar Tracking section nav order: Time, Expenses, Approvals, Exports
    - `grep -c 'Exports' web/src/components/layout/sidebar.tsx` == 2 (one in navItems, one in the SidebarMenuItem render)
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts This Phase Produces

### New Files
- `web/src/lib/use-download.ts` — `useDownload()` hook (fetch+blob, AbortController 60s timeout, Content-Type error detection, Content-Disposition filename parsing, isPending state)
- `web/src/components/exports/export-form.tsx` — Shared `ExportForm` component:
  - Props: `{ type: 'timesheets' | 'expenses' | 'combined', showUserFilter?: boolean, showProjectFilter?: boolean }`
  - States: default (current month pre-filled) / validating / checking count / downloading / error
  - Preset period buttons: This Month, Last Month, This Quarter, This Year
  - Date range picker: react-day-picker range mode in Popover
  - Format selector: segmented control [CSV] [XLSX]
  - Download button: triggers count pre-check → fetch+blob download
  - Client-side validation: from < to, max 1 year range
  - toast.promise() for feedback
- `web/src/api/exports.ts` — Rewritten:
  - `exportCountQueryOpts(type, from, to)` — queryOptions for count endpoint
  - `getExportUrl(type, from, to, format, projectId?, userId?)` — URL construction with query params
  - `ExportApis` namespace

### Modified Files
- `web/src/routes/_authenticated/exports/-components/exports-page.tsx` — Rewritten to use `<ExportForm type="combined" showUserFilter={true} showProjectFilter={true} />`
- `web/src/components/layout/sidebar.tsx` — Exports moved from `settingsItems` to `navItems` (Tracking section, after Approvals)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→export API | User-triggered download requests with date range / format params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Export data | accept | Mitigated by backend middleware.Auth() and role-based SQL filtering (07-01). Frontend only triggers download to authenticated endpoints via credentials: 'include'. |
| T-07-02 | Denial of Service | Download fetch | mitigate | AbortController 60s timeout on all download fetches (D-14). Client-side 1-year max range validation blocks oversized requests. Count pre-check prevents unnecessary large downloads when count=0. |
| T-07-03 | Spoofing | Export endpoints | accept | Auth enforcement is backend-side (middleware.Auth()). Frontend always sends credentials via `credentials: 'include'` (D-10). If user lacks auth, backend returns 401 and frontend shows generic error toast. |
</threat_model>

<verification>
1. `cd web && bun run build` passes (TypeScript compiles)
2. `bun run lint` passes (no lint errors)
3. Manual: `/exports` page loads with ExportForm showing date picker, preset buttons, segmented control, download button
4. Manual: Sidebar shows Exports in Tracking section
</verification>

<success_criteria>
- `/exports` page renders without crashing: card with title, date range picker, preset buttons, format selector, Download button
- Download button triggers count pre-check on click
- If count=0, toast "No data to export" shown, no download fired
- If count>0, loading toast → download success → blob downloaded with correct filename
- Invalid date range (>1 year or from>=to) shows inline validation
- Sidebar Exports nav item appears after Approvals in Tracking section
- Format segmented control toggles between CSV and XLSX
- All 5 modified/created files pass TypeScript compilation
</success_criteria>

<output>
Create `.planning/phases/07-exports/07-02-SUMMARY.md` when done
</output>
