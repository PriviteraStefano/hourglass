---
phase: 02-org-hierarchy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/_authenticated/org-hierarchy/-context/org-hierarchy-context.tsx
  - web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx
  - web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx
autonomous: true
requirements: [ORG-04]

must_haves:
  truths:
    - "ReparentConfirmDialog uses reparentUnitMutationOpts instead of updateUnitMutationOpts"
    - "ReparentConfirmDialog sends only { parent_unit_id } on confirm, not full unit body"
    - "pendingEdgeConnect is removed from Zustand store state, actions, and initial state"
    - "org-hierarchy-page.tsx onConnect no longer references setPendingEdgeConnect"
  artifacts:
    - path: "web/src/routes/_authenticated/org-hierarchy/-context/org-hierarchy-context.tsx"
      provides: "Clean store without pendingEdgeConnect"
      missing: ["pendingEdgeConnect"]
    - path: "web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx"
      provides: "Dedicated reparent mutation with clean contract"
      imports: ["reparentUnitMutationOpts"]
  key_links:
    - from: "ReparentConfirmDialog"
      to: "reparentUnitMutationOpts"
      via: "useMutation(reparentUnitMutationOpts)"
      pattern: "reparentUnitMutationOpts"
---

<objective>
Clean up the reparent flow: switch `ReparentConfirmDialog` from the heavyweight `updateUnitMutationOpts` (which sends full unit body) to the dedicated `reparentUnitMutationOpts` (which sends only `{parent_unit_id}`), and remove the unused `pendingEdgeConnect` state from the Zustand store and its single consumer reference.

Purpose: Cleaner API contract for reparent (D-08), remove dead code (D-09). The `reparentUnitMutationOpts` already exists at `web/src/api/units.ts:79` — just needs wiring.
Output: 3 modified frontend files.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-CONTEXT.md
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-RESEARCH.md (D-08, D-09 sections)
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-PATTERNS.md (pages 882-1015)
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-UI-SPEC.md (Interaction Contract 3 and 4)
@/Users/stefanoprivitera/Projects/hourglass/web/src/routes/_authenticated/org-hierarchy/-context/org-hierarchy-context.tsx
@/Users/stefanoprivitera/Projects/hourglass/web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx
@/Users/stefanoprivitera/Projects/hourglass/web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx
@/Users/stefanoprivitera/Projects/hourglass/web/src/api/units.ts (line 79 — existing reparentUnitMutationOpts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove pendingEdgeConnect from Zustand store</name>
  <files>
    web/src/routes/_authenticated/org-hierarchy/-context/org-hierarchy-context.tsx
  </files>
  <read_first>
    web/src/routes/_authenticated/org-hierarchy/-context/org-hierarchy-context.tsx — full file, 106 lines
  </read_first>
  <action>
    Remove `pendingEdgeConnect` from all four locations in this file:

    1. **`OrgHierarchyState` interface** (line 18): Remove `pendingEdgeConnect: { source: string; target: string } | null`

    2. **`OrgHierarchyActions` interface** (line 32): Remove `setPendingEdgeConnect: (connect: { source: string; target: string } | null) => void`

    3. **`createOrgHierarchyStore` initial state** (line 54): Remove `pendingEdgeConnect: null,`

    4. **`createOrgHierarchyStore` setter** (line 71): Remove `setPendingEdgeConnect: (connect) => set({pendingEdgeConnect: connect}),`

    Leave all other state/actions unchanged. The remaining state is: viewMode, collapsedIds, searchQuery, selectedUnit, formOpen, formMode, editingUnit, deleteOpen, reparentTarget, draggingUnit. The remaining actions are: setViewMode, toggleCollapsed, setSearchQuery, setSelectedUnit, setFormOpen, setFormMode, setEditingUnit, setDeleteOpen, setReparentTarget, setDraggingUnit, addUnit, editUnit, deleteUnit, reparentUnit.

    (Per D-09, PATTERNS.md lines 678-718, UI-SPEC Interaction Contract 4)
  </action>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pendingEdgeConnect` is not present in `OrgHierarchyState` interface
    - `setPendingEdgeConnect` is not present in `OrgHierarchyActions` interface
    - Initial state has no `pendingEdgeConnect: null`
    - Store setter has no `setPendingEdgeConnect` action
    - `bun run build` passes with no type errors (typecheck)
  </acceptance_criteria>
  <done>
    Zustand store cleaned: pendingEdgeConnect and setPendingEdgeConnect removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Switch ReparentConfirmDialog to reparentUnitMutationOpts + clean up page consumer</name>
  <files>
    web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx
    web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx
  </files>
  <read_first>
    web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx — full file, 78 lines
    web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx — onConnect callback (lines 232-242), store destructuring (line 132)
  </read_first>
  <action>
    **A) Update `reparent-confirm-dialog.tsx`:**

    1. **Change imports:**
       - Replace `import {updateUnitMutationOpts} from "@/api/units.ts"` with `import {reparentUnitMutationOpts} from "@/api/units.ts"`
    
    2. **Remove `pendingEdgeConnect` state reads:**
       - Remove `const pendingEdgeConnect = useOrgHierarchyStore(s => s.pendingEdgeConnect)` (line 19)
       - Remove `const setPendingEdgeConnect = useOrgHierarchyStore(s => s.setPendingEdgeConnect)` (line 22)
    
    3. **Swap mutation:**
       - Replace `const {mutateAsync: updateUnit} = useMutation(updateUnitMutationOpts)` with `const {mutateAsync: reparentUnit} = useMutation(reparentUnitMutationOpts)`
    
    4. **Update `onOpenChange`:**
       - Remove `setPendingEdgeConnect(null)` call from the close handler (line 34)
       - Only keep `setDraggingUnit(null)` and `setReparentTarget(null)`
    
    5. **Update `handleConfirm`:**
       - Replace `await updateUnit({id: draggingUnit.id, body: {...full unit body...}})` with `await reparentUnit({id: draggingUnit.id, parent_unit_id: reparentTarget.id})`
       - Remove the full body object `{name, code, description, parent_unit_id}` — the dedicated mutation only needs `parent_unit_id`
    
    6. **Remove the unused `reparentOpen` variable** (line 24) since it's calculated but used only once (the AlertDialog's `open` prop already receives the expression `draggingUnit !== null && reparentTarget !== null` directly? Wait, line 24 assigns `const reparentOpen = ...` and line 58 uses `reparentOpen`. Keep `reparentOpen` — it's still used. Just remove the state references.

    (Per D-08, PATTERNS.md lines 882-971, UI-SPEC Interaction Contract 3)

    **B) Update `org-hierarchy-page.tsx`:**

    1. Remove the `setPendingEdgeConnect` destructure on line 132:
       - Remove `const setPendingEdgeConnect = useOrgHierarchyStore(s => s.setPendingEdgeConnect)`
    
    2. Update the `onConnect` callback (lines 232-242):
       - Remove the `setPendingEdgeConnect({ source: params.source, target: params.target })` call inside the callback
       - Keep only `reparentUnit(sourceUnit, targetUnit)` call
       - Remove `setPendingEdgeConnect` from the dependency array

    (Per D-09, PATTERNS.md lines 975-1013)
  </action>
  <verify>
    <automated>cd /Users/stefanoprivitera/Projects/hourglass/web && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `reparent-confirm-dialog.tsx` imports `reparentUnitMutationOpts` not `updateUnitMutationOpts`
    - `reparent-confirm-dialog.tsx` has no references to `pendingEdgeConnect` or `setPendingEdgeConnect`
    - `handleConfirm` sends `{id, parent_unit_id}` only (not full body)
    - `org-hierarchy-page.tsx` no longer destructures `setPendingEdgeConnect` from store
    - `onConnect` callback no longer calls `setPendingEdgeConnect`
    - `bun run build` passes with no type errors
  </acceptance_criteria>
  <done>
    Reparent dialog uses clean mutation; pendingEdgeConnect fully removed from all consumers
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
**No new symbols created. Removed symbols:**
- `OrgHierarchyState.pendingEdgeConnect` (removed from interface)
- `OrgHierarchyActions.setPendingEdgeConnect` (removed from interface)
- `setPendingEdgeConnect` action in `createOrgHierarchyStore` (removed)
- `pendingEdgeConnect: null` initial state (removed)

**Changed wiring:**
- `ReparentConfirmDialog` now calls `reparentUnitMutationOpts` instead of `updateUnitMutationOpts`
- `OrgHierarchyPage.onConnect` no longer calls `setPendingEdgeConnect`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | All mutations go through the existing `api<T>()` helper which includes cookie-based JWT auth with refresh-on-401. No new trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installations needed for this phase |
</threat_model>

<verification>
- `bun run build` passes with 0 type errors
- `bun run lint` passes (if lint script exists)
</verification>

<success_criteria>
- Frontend builds successfully (`bun run build`)
- Reparent dialog sends `{ parent_unit_id }` only (verified by reading dialog code)
- No references to `pendingEdgeConnect` exist in the codebase (grep for zero results)
</success_criteria>

<output>
Create `.planning/phases/02-org-hierarchy/02-02-SUMMARY.md` when done
</output>
