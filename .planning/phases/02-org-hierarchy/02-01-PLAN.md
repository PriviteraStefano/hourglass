---
phase: 02-org-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/domain/unit/unit.go
  - internal/core/ports/unit_repository.go
  - internal/core/services/unit/unit.go
  - internal/adapters/secondary/postgres/unit_repository.go
  - internal/adapters/secondary/postgres/unit_member_repository.go
  - internal/adapters/primary/http/unit.go
  - cmd/server/main.go
  - internal/core/services/unit/unit_integration_test.go
  - internal/adapters/secondary/postgres/unit_member_repository_test.go
autonomous: true
requirements: [ORG-03, ORG-05]

must_haves:
  truths:
    - "Backend rejects deletion of root unit (hierarchy_level === 0) with 400-level error"
    - "Backend rejects deletion of unit with children with 400-level error"
    - "Backend rejects deletion of unit with members with 400-level error"
    - "PUT /units/{id}/members/{membershipId} updates is_primary and enforces one-primary-per-user"
    - "GET /units/members/batch returns members for multiple unit IDs in one query"
  artifacts:
    - path: "internal/core/domain/unit/unit.go"
      provides: "ErrCannotDeleteRootUnit and ErrCannotDeleteWithChildren sentinel errors"
      contains: "ErrCannotDeleteRootUnit"
    - path: "internal/core/ports/unit_repository.go"
      provides: "UpdateMember, HasChildren, ListMembersByUnitIDs, ListMembershipsForUser port methods"
      contains: "UpdateMember"
    - path: "internal/core/services/unit/unit.go"
      provides: "UpdateMember service method with one-primary-per-user; updated Delete with root+children+members checks"
      contains: "UpdateMember(ctx context.Context, unitID, membershipID string"
    - path: "internal/adapters/secondary/postgres/unit_member_repository.go"
      provides: "Update and ListByUnitIDs repository implementations"
      contains: "func (r *UnitMemberRepository) Update"
    - path: "internal/adapters/primary/http/unit.go"
      provides: "UpdateMember and ListMembersBatch handlers with sentinel error mapping"
      contains: "UpdateUnitMemberRequest"
    - path: "cmd/server/main.go"
      provides: "Route registrations for PUT and batch GET endpoints"
      contains: "unitHandler.UpdateMember"
  key_links:
    - from: "Service.UpdateMember"
      to: "repo.ListMembers + repo.ListMembershipsForUser + repo.UpdateMember"
      via: "one-primary-per-user enforcement"
      pattern: "ListMembershipsForUser.*UpdateMember"
    - from: "Service.Delete"
      to: "repo.GetByID + repo.HasChildren + repo.HasMembers"
      via: "root-unit + children + members guards"
      pattern: "HasChildren.*HasMembers"
    - from: "Handler.UpdateMember"
      to: "Service.UpdateMember"
      via: "JSON decode + path params"
      pattern: "h.service.UpdateMember"
    - from: "Handler.Delete"
      to: "ErrCannotDeleteRootUnit + ErrCannotDeleteWithChildren sentinels"
      via: "error mapping to HTTP 400"
      pattern: "ErrCannotDeleteRootUnit"
---

<objective>
Backend foundation: delete protection enforcement (root/children/members), new `PUT /units/{id}/members/{membershipId}` endpoint for primary unit designation, batch members endpoint `GET /units/members/batch`, and all supporting port/repository/service/handler layers plus integration tests.

Purpose: Enforce ORG-05 delete constraints at the service layer and ship the backend half of ORG-03 primary unit designation.
Output: 9 modified files (2 error sentinels, 4 port methods, 2 service methods, 2 handler methods, 3 repo methods, 2 routes, integration tests).
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-CONTEXT.md
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-RESEARCH.md
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-PATTERNS.md
@/Users/stefanoprivitera/Projects/hourglass/.planning/phases/02-org-hierarchy/02-VALIDATION.md
@/Users/stefanoprivitera/Projects/hourglass/internal/core/domain/unit/unit.go
@/Users/stefanoprivitera/Projects/hourglass/internal/core/ports/unit_repository.go
@/Users/stefanoprivitera/Projects/hourglass/internal/core/services/unit/unit.go
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/secondary/postgres/unit_repository.go
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/secondary/postgres/unit_member_repository.go
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/primary/http/unit.go
@/Users/stefanoprivitera/Projects/hourglass/cmd/server/main.go
@/Users/stefanoprivitera/Projects/hourglass/internal/core/services/unit/unit_integration_test.go
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/secondary/postgres/unit_member_repository_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain sentinel errors + Port interface + Service methods</name>
  <files>
    internal/core/domain/unit/unit.go
    internal/core/ports/unit_repository.go
    internal/core/services/unit/unit.go
  </files>
  <read_first>
    internal/core/domain/unit/unit.go — current sentinel errors block (lines 10-16)
    internal/core/ports/unit_repository.go — current interface methods (lines 10-22)
    internal/core/services/unit/unit.go — current Delete method (lines 124-133), AddMember (lines 169-181)
  </read_first>
  <action>
    **A) Add sentinel errors to `internal/core/domain/unit/unit.go`:**
    After `ErrMemberNotFound` (line 15), insert two new errors:
    - `ErrCannotDeleteRootUnit = errors.New("cannot delete root unit")`
    - `ErrCannotDeleteWithChildren = errors.New("cannot delete unit with child units")`
    (Per D-05, D-06)

    **B) Add port methods to `internal/core/ports/unit_repository.go`:**
    Add these four method signatures after `GetMemberCountsByOrg` (line 21):
    - `UpdateMember(ctx context.Context, m *unit.UnitMember) (*unit.UnitMember, error)`
    - `HasChildren(ctx context.Context, id string) (bool, error)`
    - `ListMembersByUnitIDs(ctx context.Context, orgID uuid.UUID, unitIDs []string) ([]unit.UnitMember, error)`
    - `ListMembershipsForUser(ctx context.Context, userID uuid.UUID) ([]unit.UnitMember, error)`
    (Per PATTERNS.md lines 77-89)

    **C) Update `Service.Delete` in `internal/core/services/unit/unit.go`**
    Replace the current Delete method (lines 124-133) to:
    1. Call `s.repo.GetByID(ctx, id)` first to get the unit
    2. Check `u.HierarchyLevel == 0` → return `unit.ErrCannotDeleteRootUnit`
    3. Call `s.repo.HasChildren(ctx, id)` → if true, return `unit.ErrCannotDeleteWithChildren`
    4. Keep the existing `HasMembers` check
    5. Fall through to `s.repo.Delete(ctx, id)`
    (Per D-05, PATTERNS.md lines 178-207)

    **D) Add `UpdateMember` service method to `internal/core/services/unit/unit.go`:**
    After `RemoveMember` (line 185), add:
    ```go
    func (s *Service) UpdateMember(ctx context.Context, unitID, membershipID string, isPrimary bool, endDate *time.Time) (*unit.UnitMember, error) {
        members, err := s.repo.ListMembers(ctx, unitID)
        if err != nil { return nil, err }
        var targetMember *unit.UnitMember
        for _, m := range members {
            if m.ID == membershipID { targetMember = &m; break }
        }
        if targetMember == nil { return nil, unit.ErrMemberNotFound }
        if isPrimary {
            allMemberships, err := s.repo.ListMembershipsForUser(ctx, targetMember.UserID)
            if err != nil { return nil, err }
            for _, m := range allMemberships {
                if m.IsPrimary && m.ID != membershipID {
                    m.IsPrimary = false
                    if _, err := s.repo.UpdateMember(ctx, &m); err != nil { return nil, err }
                }
            }
        }
        targetMember.IsPrimary = isPrimary
        targetMember.EndDate = endDate
        return s.repo.UpdateMember(ctx, targetMember)
    }
    ```
    (Per D-02, D-03 — PATTERNS.md lines 137-175)

    **E) Add `ListMembersByUnitIDs` service method:**
    After `UpdateMember`, add:
    ```go
    func (s *Service) ListMembersByUnitIDs(ctx context.Context, orgID uuid.UUID, unitIDs []string) ([]unit.UnitMember, error) {
        return s.repo.ListMembersByUnitIDs(ctx, orgID, unitIDs)
    }
    ```
    (Per D-13)

    Ensure import for `"time"` is added at the top of the service file (currently only imports context, uuid, domain unit, ports).
  </action>

  <verify>
    <automated>go build ./internal/core/services/unit/...</automated>
  </verify>

  <acceptance_criteria>
    - `ErrCannotDeleteRootUnit` and `ErrCannotDeleteWithChildren` are declared in domain/unit.go
    - `UnitRepository` interface in ports includes all four new method signatures
    - `Service.Delete` calls `GetByID`, checks `HierarchyLevel`, calls `HasChildren`, then `HasMembers`
    - `Service.UpdateMember` exists and calls `ListMembers`/`ListMembershipsForUser`/`UpdateMember`
    - `Service.ListMembersByUnitIDs` exists and delegates to repo
    - `go build ./internal/core/services/unit/...` succeeds with no errors
  </acceptance_criteria>

  <done>
    Sentinel errors, port interface, and service methods all compile
  </done>
</task>

<task type="auto">
  <name>Task 2: Repository implementations (HasChildren, Update, ListByUnitIDs, ListMembershipsForUser)</name>
  <files>
    internal/adapters/secondary/postgres/unit_repository.go
    internal/adapters/secondary/postgres/unit_member_repository.go
  </files>
  <read_first>
    internal/adapters/secondary/postgres/unit_repository.go — HasMembers method (lines 179-191) as pattern for HasChildren; delegation methods (lines 193-206)
    internal/adapters/secondary/postgres/unit_member_repository.go — Add method (lines 58-86) for UPDATE RETURNING scan pattern; ListByUnit (lines 24-55) for rows-iteration scan pattern
  </read_first>
  <action>
    **A) Add `HasChildren` to `unit_repository.go`:**
    After `HasMembers` (line 191), add a method `HasChildren` that uses the same `EXISTS` pattern:
    - Parse `id` to `uuid.UUID`
    - Query: `SELECT EXISTS(SELECT 1 FROM units WHERE parent_unit_id = $1)`
    - Return `bool, error`
    (Per PATTERNS.md lines 234-248)

    **B) Add delegation methods to `unit_repository.go`:**
    After `RemoveMember` delegation (line 206), add three new delegation methods that forward to `r.members`:
    - `UpdateMember(ctx, m)` → `r.members.Update(ctx, m)`
    - `ListMembersByUnitIDs(ctx, orgID, unitIDs)` → `r.members.ListByUnitIDs(ctx, orgID, unitIDs)`
    - `ListMembershipsForUser(ctx, userID)` → `r.members.ListMembershipsForUser(ctx, userID)`
    (Per PATTERNS.md lines 252-265)

    **C) Add `Update` to `unit_member_repository.go`:**
    After `Remove` (line 96), add method:
    - Parse `m.ID` to `uuid.UUID`
    - UPDATE `unit_memberships` SET `is_primary = $2, end_date = $3` WHERE `id = $1`
    - RETURNING all columns (same scan columns as `Add` method: id, org_id, user_id, unit_id, is_primary, role, start_date, end_date, created_at)
    - Use `scanUnitMember` for the scan
    - On `pgx.ErrNoRows` → return `nil, unit.ErrMemberNotFound`
    (Per PATTERNS.md lines 305-333)
    **IMPORTANT:** The RETURNING clause does NOT include `user_name`/`user_email` (those come from JOIN). The UPDATE RETURNING will only return the raw unit_memberships columns. So use inline scan (not `scanUnitMember` which expects user_name/user_email). Follow the `Add` method's scan pattern which scans individual fields directly.

    **D) Add `ListByUnitIDs` to `unit_member_repository.go`:**
    After `Update`, add method:
    - Parse each `unitIDs` string to `uuid.UUID`, skip invalid parses
    - If `len(ids) == 0`, return `[]unit.UnitMember{}`
    - Query: `SELECT um.id, um.org_id, um.user_id, um.unit_id, um.is_primary, um.role, um.start_date, um.end_date, um.created_at, COALESCE(u.firstname || ' ' || u.lastname, '') AS user_name, COALESCE(u.email, '') AS user_email FROM unit_memberships um LEFT JOIN users u ON um.user_id = u.id WHERE um.unit_id = ANY($1) AND um.org_id = $2 ORDER BY um.unit_id, um.created_at DESC`
    - Use `scanUnitMember` for each row
    (Per PATTERNS.md lines 337-380)
    **IMPORTANT:** Add `"strings"` to the import list.

    **E) Add `ListMembershipsForUser` to `unit_member_repository.go`:**
    After `ListByUnitIDs`, add method:
    - Parse `userID` to `uuid.UUID`
    - Query: `SELECT all columns with user join (same as ListByUnit) WHERE um.user_id = $1 ORDER BY um.created_at DESC`
    - Use `scanUnitMember` for each row
    - Return `[]unit.UnitMember`
    This returns ALL memberships for a user across all units (needed by D-03 primary enforcement).
  </action>

  <verify>
    <automated>go build ./internal/adapters/secondary/postgres/...</automated>
  </verify>

  <acceptance_criteria>
    - `HasChildren` uses `SELECT EXISTS(SELECT 1 FROM units WHERE parent_unit_id = $1)` same pattern as `HasMembers`
    - `Update` in `UnitMemberRepository` does UPDATE RETURNING with proper scan
    - `ListByUnitIDs` uses `WHERE um.unit_id = ANY($1)` pattern and `scanUnitMember`
    - `ListMembershipsForUser` uses `WHERE um.user_id = $1` pattern
    - Delegation methods `UpdateMember`, `ListMembersByUnitIDs`, `ListMembershipsForUser` exist in `UnitRepository`
    - `go build ./internal/adapters/secondary/postgres/...` succeeds
  </acceptance_criteria>

  <done>
    Repository methods compile. `go build` passes for postgres adapter package.
  </done>
</task>

<task type="auto">
  <name>Task 3: HTTP handlers + route registration + integration tests</name>
  <files>
    internal/adapters/primary/http/unit.go
    cmd/server/main.go
    internal/core/services/unit/unit_integration_test.go
    internal/adapters/secondary/postgres/unit_member_repository_test.go
  </files>
  <read_first>
    internal/adapters/primary/http/unit.go — AddMember handler (lines 232-271) and RemoveMember handler (lines 273-289) for patterns; Delete handler (lines 146-170) for sentinel mapping
    cmd/server/main.go — existing unit route registrations (lines 140-149)
    internal/core/services/unit/unit_integration_test.go — existing subtest patterns (CreateAndGetByID, Delete, etc.)
    internal/adapters/secondary/postgres/unit_member_repository_test.go — existing test patterns using TestPool, seedOrg, seedUser
  </read_first>
  <action>
    **A) Add `UpdateUnitMemberRequest` type to HTTP handler file:**
    In `internal/adapters/primary/http/unit.go`, after `AddUnitMemberRequest` struct (line 207), add:
    ```go
    type UpdateUnitMemberRequest struct {
        IsPrimary bool       `json:"is_primary"`
        EndDate   *time.Time `json:"end_date,omitempty"`
    }
    ```
    Add `"time"` to the imports.

    **B) Add `UpdateMember` handler to `unit.go`:**
    After `RemoveMember` handler (line 289), add handler that:
    - Reads `unitID` from `r.PathValue("id")` and `membershipID` from `r.PathValue("membership_id")`
    - Validates both are non-empty
    - Decodes `UpdateUnitMemberRequest` from JSON body
    - Calls `h.service.UpdateMember(ctx, unitID, membershipID, req.IsPrimary, req.EndDate)`
    - Maps `unit.ErrMemberNotFound` → `http.StatusNotFound`
    - Returns 200 with the updated member on success
    (Per D-02, PATTERNS.md lines 500-530)

    **C) Add `ListMembersBatch` handler to `unit.go`:**
    After `UpdateMember` handler, add handler that:
    - Reads `unit_ids` from `r.URL.Query().Get("unit_ids")`
    - Returns 400 if empty
    - Splits by comma: `strings.Split(unitIDsParam, ",")`
    - Gets org ID via `middleware.GetOrganizationID(ctx)`
    - Calls `h.service.ListMembersByUnitIDs(ctx, orgID, unitIDs)`
    - Returns 200 with members on success
    (Per D-13, PATTERNS.md lines 532-553)
    Add `"strings"` to the imports.

    **D) Update `Delete` handler error mapping:**
    In the existing `Delete` handler, add sentinel checks after the `ErrCannotDeleteWithMembers` check (line 160):
    ```go
    if err == unit.ErrCannotDeleteRootUnit {
        api.RespondWithError(w, http.StatusBadRequest, "cannot delete root unit")
        return
    }
    if err == unit.ErrCannotDeleteWithChildren {
        api.RespondWithError(w, http.StatusBadRequest, "cannot delete unit with child units")
        return
    }
    ```
    (Per D-06, PATTERNS.md lines 556-566)

    **E) Register two new routes in `cmd/server/main.go`:**
    After the existing unit routes (after line 149), add:
    ```go
    mux.HandleFunc("PUT /units/{id}/members/{membership_id}", middleware.Auth(authService, unitHandler.UpdateMember))
    mux.HandleFunc("GET /units/members/batch", middleware.Auth(authService, unitHandler.ListMembersBatch))
    ```
    (Per PATTERNS.md lines 592-594)

    **F) Add integration test subtests to `unit_integration_test.go`:**
    Inside `TestUnitIntegration`, add these subtests:
    
    i. `UpdateMember` — Create org + user + unit + add member. Then call `svc.UpdateMember` with `isPrimary: true`. Verify returned member has `IsPrimary: true`. Add a second membership for the same user (different unit), then set that to primary. Verify the first membership's `IsPrimary` is now false.

    ii. `Delete_RootUnit` — Create a root unit (hierarchy_level 0). Call `svc.Delete`. Assert error is `unit.ErrCannotDeleteRootUnit`.

    iii. `Delete_HasChildren` — Create a parent unit + child unit. Call `svc.Delete` on parent. Assert error is `unit.ErrCannotDeleteWithChildren`. (Per D-05)

    iv. `Delete_HasMembers` — Already may exist. Create a unit + add a member. Call `svc.Delete`. Assert error is `unit.ErrCannotDeleteWithMembers`.

    Each subtest uses `realRepoFixture(t, pool)` and seeds an org via `pool.Exec`. For member subtests, seed a user too (use UUID from uuid.New() — the user FK only needs to exist in the users table or use `pool.Exec` to insert a minimal user row with UUID, firstname, lastname, email, password_hash).

    **G) Add repository-level tests to `unit_member_repository_test.go`:**
    Add two new test functions following the existing pattern:

    i. `TestUnitMemberRepository_Update` — Create org, user, unit, add member. Call `r.members.Update(ctx, &m)` with `IsPrimary: true`. Verify returned member has IsPrimary=true.

    ii. `TestUnitMemberRepository_ListByUnitIDs` — Create org, user, unit. Add member. Call `r.members.ListByUnitIDs(ctx, orgID, []string{unitID})`. Verify 1 member returned.

    Use `TestPool(t)` + `SetupTestSchema` + `TeardownTestSchema` pattern matching existing tests.
  </action>

  <verify>
    <automated>go test -count=1 -timeout 120s ./internal/core/services/unit/... 2>&1 | grep -E '^(---|ok |FAIL)'</automated>
  </verify>

  <acceptance_criteria>
    - `UpdateMember` handler exists and decodes `UpdateUnitMemberRequest`
    - `ListMembersBatch` handler exists and splits `unit_ids` query param
    - `Delete` handler returns "cannot delete root unit" on `ErrCannotDeleteRootUnit`
    - `Delete` handler returns "cannot delete unit with child units" on `ErrCannotDeleteWithChildren`
    - Two new routes registered in main.go: PUT and GET /units/members/batch
    - 4 integration subtests pass: UpdateMember, Delete_RootUnit, Delete_HasChildren, Delete_HasMembers
    - 2 repository test functions pass: Update, ListByUnitIDs
    - All tests pass: `go test -count=1 -timeout 120s ./internal/core/services/unit/...` ok
  </acceptance_criteria>

  <done>
    Full backend implementation: handlers, routes, and integration tests all passing
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
**Go errors:**
- `unit.ErrCannotDeleteRootUnit` — sentinel error
- `unit.ErrCannotDeleteWithChildren` — sentinel error

**Port interface methods:**
- `ports.UnitRepository.UpdateMember(ctx, *unit.UnitMember) (*unit.UnitMember, error)`
- `ports.UnitRepository.HasChildren(ctx, string) (bool, error)`
- `ports.UnitRepository.ListMembersByUnitIDs(ctx, uuid.UUID, []string) ([]unit.UnitMember, error)`
- `ports.UnitRepository.ListMembershipsForUser(ctx, uuid.UUID) ([]unit.UnitMember, error)`

**Service methods:**
- `unit.Service.UpdateMember(ctx, string, string, bool, *time.Time) (*unit.UnitMember, error)`
- `unit.Service.ListMembersByUnitIDs(ctx, uuid.UUID, []string) ([]unit.UnitMember, error)`

**Repository implementations:**
- `UnitRepository.HasChildren(ctx, string) (bool, error)` — EXISTS query
- `UnitRepository.UpdateMember(ctx, *UnitMember) (*UnitMember, error)` — delegation
- `UnitRepository.ListMembersByUnitIDs(ctx, uuid.UUID, []string) ([]UnitMember, error)` — delegation
- `UnitRepository.ListMembershipsForUser(ctx, uuid.UUID) ([]UnitMember, error)` — delegation
- `UnitMemberRepository.Update(ctx, *UnitMember) (*UnitMember, error)` — UPDATE RETURNING
- `UnitMemberRepository.ListByUnitIDs(ctx, uuid.UUID, []string) ([]UnitMember, error)` — ANY($1)
- `UnitMemberRepository.ListMembershipsForUser(ctx, uuid.UUID) ([]UnitMember, error)` — WHERE user_id

**HTTP handler methods:**
- `UnitHandler.UpdateMember(w, r)` — PUT /units/{id}/members/{membershipId}
- `UnitHandler.ListMembersBatch(w, r)` — GET /units/members/batch

**HTTP request types:**
- `UpdateUnitMemberRequest{IsPrimary bool, EndDate *time.Time}`

**Routes:**
- `PUT /units/{id}/members/{membership_id}` (Auth-protected)
- `GET /units/members/batch` (Auth-protected, query: ?unit_ids=...)

**Test subtests (in unit_integration_test.go):**
- `TestUnitIntegration/UpdateMember`
- `TestUnitIntegration/Delete_RootUnit`
- `TestUnitIntegration/Delete_HasChildren`
- `TestUnitIntegration/Delete_HasMembers`

**Test functions (in unit_member_repository_test.go):**
- `TestUnitMemberRepository_Update`
- `TestUnitMemberRepository_ListByUnitIDs`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API | All new endpoints are JWT-auth-protected; request body and path params are untrusted |
| API → PostgreSQL | All queries use parameterized pgx (no SQL injection); UUID parsing guards reject invalid IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `PUT /units/{id}/members/{membershipId}` | mitigate | Return "member not found" on `ErrMemberNotFound` (standard pattern); return generic "failed to update member" for internal errors — never leak member details in error messages |
| T-02-02 | Tampering | `Service.Delete` / `Service.UpdateMember` | mitigate | All operations extract org ID from JWT claims via `middleware.GetOrganizationID(ctx)` — never from request body or path params; org-scoped data access via UnitRepository.ListByOrg |
| T-02-03 | Spoofing | `PUT /units/{id}/members/{membershipId}` | mitigate | JWT HttpOnly cookie (`auth_token`) validated by `middleware.Auth` before any handler runs; no bearer token or session ID in request body |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installations needed for this phase |
</threat_model>

<verification>
- `go build ./...` compiles without errors
- `go test -count=1 -timeout 120s ./internal/core/services/unit/...` — all subtests pass including 4 new ones
- `go test -count=1 -timeout 120s ./internal/adapters/secondary/postgres/...` — all tests pass including 2 new ones
- Sentinel errors (ErrCannotDeleteRootUnit, ErrCannotDeleteWithChildren) are referenced in handler error mapping
- New routes registered in main.go
</verification>

<success_criteria>
- All backend code compiles (`go build ./...`)
- All unit + integration tests pass (`go test -count=1 -timeout 300s ./...`)
- 4 integration subtests validate delete protection (root, children, members, UpdateMember primary enforcement)
- 2 repo test functions validate Update and ListByUnitIDs
- Handlers correctly map sentinel errors to HTTP status codes
</success_criteria>

<output>
Create `.planning/phases/02-org-hierarchy/02-01-SUMMARY.md` when done
</output>
