---
phase: 15-ux-foundation-design-tokens-shared-components
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - web/package.json
  - web/bun.lock
  - web/src/components/shared/data-table.tsx
  - web/src/components/shared/filter-bar.tsx
  - web/src/components/shared/__tests__/data-table.test.tsx
  - web/src/components/shared/__tests__/filter-bar.test.tsx
autonomous: false
requirements: [UXFD-01]
user_setup: []
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "@tanstack/react-table is installed pinned at ^8.21.3 (v8 line — NOT the 3-day-old v9 breaking major) after a blocking human-verify checkpoint clears the SUS package verdict"
    - "DataTable<T> renders a sortable, client-paginated table on the v8 API (useReactTable + getCoreRowModel/getSortedRowModel/getPaginationRowModel + flexRender) wrapped in ui/table primitives inside a rounded-lg border overflow-x-auto container"
    - "Every column header renders a sort button with the exact a11y contract: aria-label 'Sort by {column label}' → 'Sorted by {column label} ({direction})' when active, with aria-sort on the header cell (UI-SPEC; e.g. 'Sort by Date' / 'Sorted by Date (ascending)')"
    - "The pagination footer renders 'Showing {first}–{last} of {total}' + prev/next icon buttons labeled 'Previous page'/'Next page' + a page-size select (10/20/50, default 10)"
    - "DataTable state coverage: isLoading → 5 skeleton rows (ui/skeleton); zero rows → EmptyState with the defaults 'No matching records' / 'Try adjusting your search or filters.' (consumer-overridable); missing cell values render em-dash —; cells truncate with a title attribute; onRowClick adds hover bg + cursor-pointer"
    - "FilterBar renders search input first (grows, min-w-40) then select/date-range controls, ml-auto spacer, the '{N} active' count chip (bg-muted text-muted-foreground rounded-full px-2 py-0.5 text-xs), and a 'Reset' ghost button visible only when ≥1 filter is active; it is stateless — values/onChange come from the consumer"
    - "FilterBar's active count excludes the search term (values.search); select filters hold string values; date-range filters hold {from?, to?} date strings rendered via a calendar popover with 'dd MMM' labels"
  artifacts:
    - path: "web/package.json"
      provides: "@tanstack/react-table pinned ^8.21.3"
      contains: "@tanstack/react-table"
    - path: "web/src/components/shared/data-table.tsx"
      provides: "Frozen sortable/paginated table"
      exports: ["DataTable"]
    - path: "web/src/components/shared/filter-bar.tsx"
      provides: "Frozen search/filter/reset bar"
      exports: ["FilterBar", "Filter"]
    - path: "web/src/components/shared/__tests__/data-table.test.tsx"
      provides: "Sorting/pagination/a11y/empty/loading tests"
    - path: "web/src/components/shared/__tests__/filter-bar.test.tsx"
      provides: "Active-count/reset visibility tests"
  key_links:
    - from: "web/src/components/shared/data-table.tsx"
      to: "web/src/components/shared/empty-state.tsx"
      via: "the empty state renders EmptyState with the default copy (plan 01 artifact — the reason this plan depends on 15-01)"
      pattern: "EmptyState"
    - from: "web/src/components/shared/data-table.tsx"
      to: "@tanstack/react-table"
      via: "v8 imports: useReactTable, flexRender, getCoreRowModel, getSortedRowModel, getPaginationRowModel — the pinned ^8.21.3 API (research Pitfall 1)"
      pattern: "useReactTable"
    - from: "web/src/components/shared/filter-bar.tsx"
      to: "web/src/components/ui/calendar.tsx"
      via: "date-range filters use DayPicker mode='range' numberOfMonths={2} in a Popover (entries-filters.tsx DateRangeFilter pattern)"
      pattern: "mode=\"range\""
  prohibitions:
    - "grep -nE 'useTable\\(|table\\.FlexRender' web/src/components/shared/data-table.tsx == 0 — the v9 API must not leak in (v8 is pinned; Pitfall 1)"
    - "grep -nE 'useQuery|useCustomersStore|useOrgHierarchyStore|invalidateQueries' web/src/components/shared/data-table.tsx web/src/components/shared/filter-bar.tsx == 0 — frozen components stay presentational (research Pitfall 4)"
    - "web/src/components/shared/entries-table.tsx and entries-filters.tsx remain untouched this phase (D-15-12 — Phase 21 migrates them)"
  backstop:
    - statement: "DataTable cell content truncates with ellipsis + title attribute at container width; the pagination footer text never overflows its container (UI-SPEC long-text backstop row)"
      verification: backstop
---

<objective>
Land the two list-surface frozen components (UXFD-01): DataTable on @tanstack/react-table and the stateless FilterBar, both under `web/src/components/shared/` with full state-coverage unit tests.

Purpose: every list surface from Phase 16 on (allocation screen, tickets, scheduler queue) and the Phase 21 time-entries migration consume these two components — sorting, pagination, a11y contracts, empty/loading/partial states are frozen here exactly per the approved UI-SPEC contracts.

Output: the pinned @tanstack/react-table dependency (checkpoint-gated, SUS verdict), DataTable with sortable headers + client pagination + skeleton/empty/em-dash states, FilterBar with select/date-range filters + active-count chip + reset, and their unit tests. The hand-rolled entries-table/entries-filters stay untouched (D-15-12).
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md
@web/src/components/shared/entries-table.tsx
@web/src/components/shared/entries-filters.tsx
@web/src/components/ui/table.tsx
@web/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human verification of the @tanstack/react-table package (SUS verdict)</name>
  <what-built>
    Nothing is built — this is the package legitimacy gate required by the research audit. @tanstack/react-table carries a SUS verdict (reason: "too-new" — v9.1.2 was published 2026-08-09, 3 days ago; the v8 line is 16 months stable). The plan pins ^8.21.3 (v8) to avoid the breaking v9 major; the DataTable public contract is identical under both, so the pin is safe.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@tanstack/react-table in a browser.
    2. Confirm the publisher is the TanStack org (same org as the project's existing @tanstack/react-query and @tanstack/react-router), the package has a real GitHub repo, and there is no postinstall script (npm view @tanstack/react-table@8.21.3 scripts).
    3. Confirm the version to install is 8.21.3 (the v8 maintenance line), NOT 9.1.2.
    4. Type "approved" to clear the gate, or describe any concern.
  </how-to-verify>
  <resume-signal>approved — or describe the concern</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install @tanstack/react-table ^8.21.3 + DataTable component</name>
  <files>
    web/package.json
    web/bun.lock
    web/src/components/shared/data-table.tsx
    web/src/components/shared/__tests__/data-table.test.tsx
  </files>
  <read_first>
    - web/src/components/shared/entries-table.tsx — the generic presentational shell being generalized (props pattern, ui/table composition, pagination idiom)
    - web/src/components/ui/table.tsx — the table primitives (data-slot, Table/TableHeader/TableRow/TableHead/TableBody/TableCell)
    - web/src/components/shared/empty-state.tsx — from plan 01; the empty-state render target
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md — DataTable contract (lines 159-169) + a11y label strings
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — data-table assignment (lines 115-154, incl. the v8 core API kept verbatim)
    - web/src/components/shared/__tests__/entries-table.test.tsx — the test convention (sort/pagination/aria-label idiom)
  </read_first>
  <behavior>
    - Test 1: clicking a header sort button toggles asc → desc → none and the aria-label flips to 'Sorted by {label} ({direction})' with aria-sort on the header cell
    - Test 2: the pagination footer shows 'Showing {first}–{last} of {total}' and the icon buttons carry 'Previous page'/'Next page' labels; page-size select offers 10/20/50
    - Test 3: isLoading renders 5 skeleton rows; zero rows renders the EmptyState defaults
    - Test 4: missing cell values render an em-dash; onRowClick rows get cursor-pointer + hover behavior
  </behavior>
  <action>
    RED phase: write web/src/components/shared/__tests__/data-table.test.tsx covering the four behaviors (columns: e.g. name + date; assert via getByRole("button", { name: "Sort by Date" }), within(footer) for the showing-label and labels). Run it — MUST fail (data-table.tsx does not exist; @tanstack/react-table not yet installed). Commit: test(15-02): add failing data-table tests.

    GREEN phase:
    1. cd web && bun add @tanstack/react-table@^8.21.3 — pin the caret range ^8.21.3; do NOT install latest (v9.1.2 breaking major, 3 days old). Verify package.json gained "@tanstack/react-table": "^8.21.3".
    2. Create web/src/components/shared/data-table.tsx per the UI-SPEC contract: props {columns: ColumnDef<T>[]; data: T[]; isLoading?: boolean; empty?: ReactNode; initialSorting?: SortingState; pageSize?: number (default 10); onRowClick?: (row: T) => void}. Use the v8 API ONLY — useReactTable with state {sorting, pagination}, onSortingChange/onPaginationChange, getCoreRowModel(), getSortedRowModel(), getPaginationRowModel(); header cells render a sort button per column with lucide ArrowUpDownIcon/ArrowUpIcon/ArrowDownIcon reflecting header.column.getIsSorted(); a11y labels exactly 'Sort by {label}' / 'Sorted by {label} ({direction})' + aria-sort on the th. Cells: default truncate with a title attribute; missing values render em-dash —. Pagination footer: 'Showing {first}–{last} of {total}' (derived from table.getState().pagination + getRowModel().rows.length + getRowCount()), prev/next icon buttons (ChevronLeftIcon/ChevronRightIcon, size-8, aria-label 'Previous page'/'Next page', disabled at bounds), page-size select 10/20/50 via table.setPageSize. isLoading → 5 Skeleton rows (ui/skeleton). empty → EmptyState with 'No matching records' / 'Try adjusting your search or filters.' (overridden by the empty prop). onRowClick → row hover bg + cursor-pointer. Wrap in div rounded-lg border overflow-x-auto. No data-fetching imports (no useQuery/zustand — presentational-only). JSDoc citing (D-15-05).
    Commit: feat(15-02): implement DataTable.
  </action>
  <verify>
    <automated>bun run test -- src/components/shared/__tests__/data-table.test.tsx</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - data-table.test.tsx passes; git log shows test(15-02) before feat(15-02)
    - package.json contains "@tanstack/react-table": "^8.21.3" and no 9.x entry; bun.lock updated
    - data-table.tsx imports only the v8 API (useReactTable, flexRender) — no useTable( or table.FlexRender usage
    - The a11y label strings match the UI-SPEC exactly (asserted in tests via getByRole)
    - data-table.tsx contains no useQuery/zustand imports (grep gate in must_haves.prohibitions)
  </acceptance_criteria>
  <done>
    DataTable renders sortable, client-paginated tables with the full state coverage (loading/empty/partial/overflow) and the pinned a11y contracts; the v8 dependency is committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FilterBar component</name>
  <files>
    web/src/components/shared/filter-bar.tsx
    web/src/components/shared/__tests__/filter-bar.test.tsx
  </files>
  <read_first>
    - web/src/components/shared/entries-filters.tsx — the role model: URL-agnostic/presentational filter controls (search field, StatusFilterSelect, DateRangeFilter patterns)
    - web/src/routes/_authenticated/customers/-components/customers-page.tsx (lines 40-70) — the InputGroup search-field pattern
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md — FilterBar contract (lines 171-179) + copy strings ('{N} active', 'Reset')
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — filter-bar assignment (lines 156-173)
  </read_first>
  <behavior>
    - Test 1: the search input renders first with the placeholder; typing calls onChange with the search key
    - Test 2: the active-count chip shows '{N} active' derived from non-empty values EXCLUDING the search term; with zero active filters the chip and Reset are hidden
    - Test 3: 'Reset' appears only when ≥1 filter is active and calls onReset
    - Test 4: a select filter renders its options; a date-range filter renders a calendar popover with 'dd MMM' labels
  </behavior>
  <action>
    RED phase: write web/src/components/shared/__tests__/filter-bar.test.tsx covering the four behaviors. Run it — MUST fail (filter-bar.tsx does not exist). Commit: test(15-02): add failing filter-bar tests.

    GREEN phase: create web/src/components/shared/filter-bar.tsx per the UI-SPEC contract — props {filters: Filter[]; values: Record<string, string | {from?: string; to?: string} | undefined>; onChange: (id: string, value: string | {from?: string; to?: string} | undefined) => void; onReset: () => void; searchPlaceholder?: string}; export type Filter = {id: string; label: string; kind: "select" | "date-range"; options?: {value: string; label: string}[]}. Conventions: the search input binds to values.search and is EXCLUDED from the active count; select filters hold string values via ui/select single-select; date-range filters hold {from?, to?} strings via a Popover + ui/calendar DayPicker mode="range" numberOfMonths={2} with a 'dd MMM' range label and a Clear button (entries-filters.tsx DateRangeFilter pattern). Layout: flex flex-wrap items-center gap-2 — search input first (grows, min-w-40) with SearchIcon, then the filter controls, then ml-auto, then the count chip (bg-muted text-muted-foreground rounded-full px-2 py-0.5 text-xs, '{N} active'), then a 'Reset' ghost button visible only when ≥1 filter is active. Stateless — the only local state allowed is the date popover open flag. JSDoc citing (D-15-08). Commit: feat(15-02): implement FilterBar.
  </action>
  <verify>
    <automated>bun run test -- src/components/shared/__tests__/filter-bar.test.tsx</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - filter-bar.test.tsx passes; git log shows test(15-02) before feat(15-02)
    - filter-bar.tsx renders search first, chip + Reset per the visibility rules (chip hidden at 0 active; Reset hidden at 0 active)
    - the count chip excludes the search term by construction (tested)
    - filter-bar.tsx contains no useQuery/zustand imports and no useSearchParams (presentational-only)
  </acceptance_criteria>
  <done>
    FilterBar renders search + select/date-range filters with the active-count chip and conditional Reset, fully stateless per the frozen contract.
  </done>
</task>

</tasks>

## Flagged Assumptions

| # | Req | Probe | Assumption |
|---|-----|-------|------------|
| FA-3 | UXFD-01 | ordering: "When elements compare equal, is output order specified and stable?" | Sort ties preserve the original data order — TanStack v8's default sort is stable and no custom sortFn is added in the frozen DataTable. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project | New dependency installation (checkpoint-gated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-SC | Tampering | npm install @tanstack/react-table | high | mitigate | Package legitimacy audit flags SUS (3-day-old v9 publish); install is gated behind the blocking human-verify checkpoint (Task 1) and pinned to the battle-tested ^8.21.3 v8 line; no postinstall script (verified in research); verification URL npmjs.com/package/@tanstack/react-table |
| T-15-03 | — (a11y misuse) | DataTable sort/page controls | low | mitigate | Icon-only controls carry the exact UI-SPEC aria-labels ('Sort by {label}', 'Previous page'/'Next page') + aria-sort on active headers; enforced by unit-test getByRole assertions and oxlint jsx-a11y (research Security Domain) |

## Security Notes

UI-only phase: no server surface touched. The a11y contract is the security-relevant item for the table — unlabeled controls are the known misuse vector (research Security Domain); the v9-dependency drift is contained by the pin + prohibition grep.
</threat_model>

<verification>
- After Task 2/3 commits: `bun run test -- src/components/shared/__tests__/data-table.test.tsx` / `filter-bar.test.tsx` + `bun run typecheck`
- After each code-modifying task: `python3 -c "from graphify.watch import _rebuild_code; from pathlib import Path; _rebuild_code(Path('.'))"` (AGENTS.md graphify step — keeps the knowledge graph current)
- Wave gate: `bun run test` + `bun run lint` + `bun run build`
- Phase gate: full suite green + `bun run fmt:check` + human visual check (plan 04 Task 2)
</verification>

<success_criteria>
- The checkpoint cleared the SUS verdict; package.json pins ^8.21.3 with bun.lock updated
- DataTable and FilterBar pass their unit suites with RED-before-GREEN commits
- v9 API and data-lifecycle imports absent from both components (grep gates)
- entries-table.tsx / entries-filters.tsx untouched (git diff empty for those paths)
</success_criteria>

## Artifacts this phase produces

- `web/package.json` + `web/bun.lock` — dependency @tanstack/react-table ^8.21.3
- `web/src/components/shared/data-table.tsx` — export `DataTable<T>`; internal props interface `DataTableProps<T>`
- `web/src/components/shared/filter-bar.tsx` — exports `FilterBar`, type `Filter` (`{id, label, kind: "select" | "date-range", options?}`)
- `web/src/components/shared/__tests__/data-table.test.tsx`, `filter-bar.test.tsx`

<output>
Create `.planning/phases/15-ux-foundation-design-tokens-shared-components/15-02-SUMMARY.md` when done
</output>
