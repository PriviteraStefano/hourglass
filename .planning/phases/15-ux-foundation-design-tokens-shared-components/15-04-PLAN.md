---
phase: 15-ux-foundation-design-tokens-shared-components
plan: 04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - .planning/sketches/SKETCH-LOOP-CONTRACT.md
autonomous: false
requirements: [UXFD-02]
estimate:
  tokens: 2500
  raw_tokens: 2500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - ".planning/sketches/SKETCH-LOOP-CONTRACT.md exists and pins: every surface/polish phase (16-26) runs gsd-sketch first; 2-3 variants per round shown to the user; user agrees on one variant; plans are UI-only; ≤3 sketch rounds max (D-15-11, the ONLY hard rule); the sketch MANIFEST is updated per sketch; --wrap-up produces sketch-findings skills and is NOT a sketch round (research A6)"
    - "The contract references the sketch mechanics: variants saved under .planning/sketches/NNN-descriptive-name/ with a per-sketch README marking the winner in frontmatter (winner: 'B'); .planning/sketches/MANIFEST.md carries the design direction, reference points, and sketch table with winners; commit convention docs(sketch-NNN): [winner] ..."
    - "15-UI-SPEC.md exists in this phase directory with status: approved — verified, not regenerated (D-15-10, research A5)"
    - "Sidebar collapsed-mode hover/navigation passes a human visual check (quick task 260801-got closure; code fix 54f465a landed 2026-08-01) and the new status badges render correctly in dark mode (human smoke)"
  artifacts:
    - path: ".planning/sketches/SKETCH-LOOP-CONTRACT.md"
      provides: "Sketch-loop process contract for phases 16-26"
      contains: "2–3"
    - path: ".planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md"
      provides: "The approved design-system contract (presence verification only)"
  key_links:
    - from: ".planning/sketches/SKETCH-LOOP-CONTRACT.md"
      to: ".config/opencode/skills/gsd-sketch/SKILL.md"
      via: "the contract's mechanics source — 2-3 variants, MANIFEST updates, --wrap-up → sketch-findings"
      pattern: "gsd-sketch"
    - from: "web/src/components/shared/status-badge.tsx"
      to: "web/src/index.css"
      via: "dark-mode smoke check exercises the runtime token resolution (bg-(--status-{role})/10 under .dark)"
      pattern: "--status-"
---

<objective>
Establish the sketch-driven loop contract (UXFD-02 / D-15-09) as the standalone document every surface/polish phase (16-26) inherits, and close ROADMAP SC4: the sidebar collapsed-mode quick task (code fix already landed — only the human visual verification remains, plus a dark-mode badge smoke over the plan-01 tokens).

Purpose: downstream phases read `.planning/sketches/SKETCH-LOOP-CONTRACT.md`, not CONTEXT.md, for the loop rules — the 3-round cap is the only hard rule (D-15-11), so the contract must be unambiguous about what counts as a round. The human verification tasks are the phase's evidence that the frozen tokens render correctly in both themes and the sidebar dead-zone fix holds.

Output: the contract document in `.planning/sketches/` (the directory is created this phase), a presence check of the approved UI-SPEC (D-15-10 — no regeneration), and one human session covering the sidebar collapsed-mode check + the dark-mode badge smoke.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-CONTEXT.md
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md
@.config/opencode/skills/gsd-sketch/SKILL.md
@.planning/quick/260801-got-investigate-sidebar-collapsed-mode-hover/260801-got-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SKETCH-LOOP-CONTRACT.md + verify UI-SPEC presence</name>
  <files>
    .planning/sketches/SKETCH-LOOP-CONTRACT.md
  </files>
  <read_first>
    - .config/opencode/skills/gsd-sketch/SKILL.md — the gsd-sketch mechanics the contract pins (2-3 variants, README + winner frontmatter, MANIFEST, --wrap-up)
    - ~/.config/opencode/gsd-core/workflows/sketch.md — the sketch workflow the contract distills (winner frontmatter at README top, MANIFEST table, wrap-up workflow)
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-CONTEXT.md — D-15-09/D-15-11 (locked contract scope)
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — the sketch-contract assignment (lines 290-297, pinned mechanics)
  </read_first>
  <action>
    Create the directory .planning/sketches/ and write .planning/sketches/SKETCH-LOOP-CONTRACT.md (D-15-09) — a standalone process contract for phases 16-26 that pins, in explicit numbered rules:
    1. Every surface/polish phase (16-26) runs gsd-sketch FIRST, before its UI plan is written.
    2. Each sketch round shows 2-3 variants; the user agrees on exactly one; variants live under .planning/sketches/NNN-descriptive-name/ with a per-sketch README.md whose frontmatter marks the winner (winner: "B").
    3. .planning/sketches/MANIFEST.md is created/updated per sketch with the design direction, reference points, and the sketch table including winners.
    4. Plans that follow an agreed sketch are UI-only (no new API endpoints, no backend changes).
    5. The 3-round cap is the ONLY hard rule (D-15-11): a phase runs at most 3 sketch-option rounds; phases decide their own round count (1-2 for polish, 2-3 for new surfaces like the scheduler). The wrap-up step (--wrap-up producing sketch-findings-* skills under .opencode/skills/) is NOT a sketch round and does not consume the cap (research A6).
    6. Commit convention for sketch approvals: docs(sketch-NNN): [winner] ...
    7. Downstream phases inherit these rules from this file — CONTEXT.md is not the source of truth for the loop.
    Also verify (file-presence check, no regeneration per D-15-10/A5): .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md exists with frontmatter status: approved. Record the verification result in the plan's SUMMARY.
    Commit: docs(15-04): add sketch loop contract.
  </action>
  <verify>
    <automated>test -f .planning/sketches/SKETCH-LOOP-CONTRACT.md && grep -q "3-round" .planning/sketches/SKETCH-LOOP-CONTRACT.md && grep -q "2-3 variants" .planning/sketches/SKETCH-LOOP-CONTRACT.md</automated>
    <automated>grep -E "^status: approved" .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/sketches/SKETCH-LOOP-CONTRACT.md exists and contains all seven pinned rules: the 2-3 variants rule, the winner-in-frontmatter convention, the MANIFEST update requirement, the UI-only plan rule, the 3-round cap with the wrap-up exclusion, the commit convention, and the inheritance note
    - The document states that wrap-up is NOT a round (the only hard rule is the 3-round cap — D-15-11)
    - 15-UI-SPEC.md status: approved verified (no regeneration — git diff empty for that file)
  </acceptance_criteria>
  <done>
    The sketch-loop contract is committed at .planning/sketches/SKETCH-LOOP-CONTRACT.md; the approved UI-SPEC presence is verified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — sidebar collapsed mode + dark-mode badge smoke</name>
  <what-built>
    Two things are verified, not built: (1) the sidebar collapsed-mode dead-zone fix (commit 54f465a, 2026-08-01 — pointer-events-none on SidebarGroupLabel; ROADMAP SC4 closure) and (2) the plan-01 semantic status tokens rendering through the rewritten StatusBadge in dark mode.
  </what-built>
  <how-to-verify>
    1. Start the app: bun run dev in web/ (backend on :8080 optional — log in as any seeded user; the authenticated shell renders the sidebar).
    2. Collapse the sidebar to icon-only mode. Hover over each nav icon (Today, Working Groups, Customers, Approvals, etc.): the icon highlight/tooltip must respond immediately and clicking must navigate — the dead zone over the previous group's last button is gone (the pre-fix symptom: hovering the last icon of a group did nothing).
    3. Open the time-entries page (shows EntryStatus badges) and toggle dark mode (devtools: add class="dark" to <html> or use the theme toggle). Each badge must render with theme-correct colors: subtle variant = tinted background + strong text readable on --base-950; warning badges amber (pending_manager AND pending_finance — both amber per D-15-02); approved green; rejected red.
    4. Type "approved" to close the task, or describe any issue found (visual regressions, dead zones, unreadable badges).
  </how-to-verify>
  <resume-signal>approved — or describe the issue</resume-signal>
</task>

</tasks>

## Flagged Assumptions

| # | Req | Probe | Assumption |
|---|-----|-------|------------|
| FA-4 | UXFD-02 | boundary: "What happens exactly at each min/max/threshold — and one step either side?" | The cap is rounds ≤ 3: a phase may run 1, 2, or 3 sketch-option rounds; a 4th round is refused. There is no minimum (D-15-11 — phase plans decide, 1-2 for polish, 2-3 for new surfaces). |
| FA-5 | UXFD-02 | adjacency: "When two things are exactly equal or just touch, do they merge, collide, or separate?" | When two variants tie in the user's preference, the user picks exactly ONE winner — variants never merge; the winner is marked in the sketch README frontmatter (sketch.md convention). |
| FA-6 | UXFD-02 | empty: "What is the result for empty, single-element, or null input?" | A phase with no sketches is not allowed: every surface/polish phase 16-26 MUST run at least one sketch round (contract rule 1). Even a phase with obvious design answers shows 2-3 variants first. |
| FA-7 | UXFD-02 | ordering: "When elements compare equal, is output order specified and stable?" | Variants are labeled A/B/C in display order (alphabetical); the MANIFEST lists sketches in creation order; the winner marker references the display label. |
| FA-8 | UXFD-02 | precision: "Where can precision loss, overflow, or rounding/tie-breaking occur — and what is the exact contract?" | No numeric computation exists in the contract; round counting is integer-exact and the cap comparison is rounds ≤ 3. Tie-breaking (FA-5) is user-picked, not computed. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | No runtime code in this plan — a process doc + two human verification tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering | SKETCH-LOOP-CONTRACT.md | low | accept | A planning artifact, not code — a malformed contract causes process drift, not a security breach; the 3-round cap is enforced by the user in the loop, not by a runtime check |
| T-15-SC | Tampering | npm installs | high | mitigate | No packages installed by this plan — the phase's only new dependency is gated in plan 02 (Task 1 checkpoint) |

## Security Notes

No runtime surface: the threat model covers process integrity only. The dark-mode smoke verifies token rendering, not security properties.
</threat_model>

<verification>
- After Task 1: the file-presence + content greps
- Phase gate: full suite green (all plans) + `bun run fmt:check` + this plan's human session
</verification>

<success_criteria>
- SKETCH-LOOP-CONTRACT.md committed with all seven pinned rules; UI-SPEC presence verified without regeneration
- The human session approved the sidebar collapsed-mode behavior and the dark-mode badge rendering (or produced actionable findings)
- UXFD-02 evidence complete: the loop contract exists and every phase 16-26 inherits it
</success_criteria>

## Artifacts this phase produces

- `.planning/sketches/SKETCH-LOOP-CONTRACT.md` — the process contract document (no runtime symbols)
- Directory `.planning/sketches/` created (MANIFEST.md is created by the first sketching phase per the contract)

<output>
Create `.planning/phases/15-ux-foundation-design-tokens-shared-components/15-04-SUMMARY.md` when done
</output>
