---
phase: 15-ux-foundation-design-tokens-shared-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/index.css
  - web/src/types/models.ts
  - web/src/components/shared/status-badge.tsx
  - web/src/components/shared/empty-state.tsx
  - web/src/components/ui/empty.tsx
  - web/src/components/shared/page-header.tsx
  - web/src/components/shared/__tests__/status-badge.test.tsx
  - web/src/components/shared/__tests__/entries-table.test.tsx
  - web/src/components/shared/__tests__/empty-state.test.tsx
  - web/src/components/shared/__tests__/page-header.test.tsx
autonomous: true
requirements: [UXFD-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The 5-role status palette (neutral/info/success/warning/danger) exists in web/src/index.css as :root + .dark CSS variable pairs (--status-{role} + --status-{role}-foreground) registered through @theme inline with exactly 10 --color-status-* keys (D-15-01, UI-SPEC Color table values verbatim; danger = var(--destructive) in both themes); the D-15-04 direction-warning vocabulary (away/partial → warning, over-capacity/invalid → danger) resolves through these tokens with no new variables"
    - "STATUS_ROLE_MAP maps every status in the D-15-03 table to a role; pending_manager AND pending_finance are BOTH warning; approved is success (D-15-02 meaning-first); unknown status → neutral role + humanized label — never blank, never crash (UI-consideration empty/error covered); the D-15-04 warning keys (away, partial, over-capacity, invalid) resolve to their roles so Phase 19 warning labels derive roles without a second map"
    - "StatusBadge renders 4 variants (subtle default / solid / outline / dot) whose classes reference role tokens only; base carries text-xs font-semibold (2-weight typography contract); dot variant = leading size-2 rounded-full role dot + text-foreground label"
    - "The 7 existing StatusBadge consumer sites and the time-entries re-export file (8 files total, measured live: expense-detail, expense-row, expenses-list, today-page, time-entries-list, entry-row, approvals-page + time-entries/-components/status-badge.tsx) compile with zero edits — StatusBadgeProps export shape (status: EntryStatus; className?) preserved; EntryStatus labels identical (Draft, Submitted, Pending Manager, Pending Finance, Approved, Rejected)"
    - "TicketStatus / DirectionStatus / AbsenceStatus unions exist in web/src/types/models.ts mirroring the backend vocabularies (D-15-13) and flow through the types barrel"
    - "EmptyState wraps ui/empty.tsx primitives with default Inbox icon + title/description/action slots; ui/empty.tsx EmptyTitle renders 600-weight after the remap (typography contract)"
    - "PageHeader renders title (truncate), description (muted), right-aligned actions slot, optional breadcrumb slot, and the status summary strip (chips = StatusBadge dot variant + count)"
  artifacts:
    - path: "web/src/index.css"
      provides: "Semantic status role tokens + @theme inline registration"
      contains: "--status-neutral"
    - path: "web/src/components/shared/status-badge.tsx"
      provides: "Generic role-mapped StatusBadge, 4 variants"
      exports: ["StatusBadge", "StatusBadgeProps", "STATUS_ROLE_MAP", "StatusRole"]
    - path: "web/src/types/models.ts"
      provides: "TicketStatus / DirectionStatus / AbsenceStatus unions"
      contains: "TicketStatus"
    - path: "web/src/components/shared/empty-state.tsx"
      provides: "EmptyState wrapper over ui/empty.tsx"
      exports: ["EmptyState"]
    - path: "web/src/components/ui/empty.tsx"
      provides: "EmptyTitle weight remap (500 → 600)"
      contains: "font-semibold"
    - path: "web/src/components/shared/page-header.tsx"
      provides: "Frozen page header with summary strip"
      exports: ["PageHeader"]
    - path: "web/src/components/shared/__tests__/status-badge.test.tsx"
      provides: "Role mapping + variant + fallback tests"
    - path: "web/src/components/shared/__tests__/empty-state.test.tsx"
      provides: "Wrapper slot tests"
    - path: "web/src/components/shared/__tests__/page-header.test.tsx"
      provides: "Summary strip + slot tests"
  key_links:
    - from: "web/src/components/shared/status-badge.tsx"
      to: "web/src/index.css"
      via: "variant classes reference the runtime vars --status-{role} (bg-(--status-{role})/10 etc.) — dark mode resolves because @theme inline registers runtime vars, not baked values"
      pattern: "--status-"
    - from: "web/src/routes/_authenticated/time-entries/-components/status-badge.tsx"
      to: "web/src/components/shared/status-badge.tsx"
      via: "re-exports StatusBadge + StatusBadgeProps — must compile with zero edits"
      pattern: "re-export"
    - from: "web/src/components/shared/page-header.tsx"
      to: "web/src/components/shared/status-badge.tsx"
      via: "summary chips render the StatusBadge dot variant"
      pattern: "StatusBadge"
    - from: "web/src/components/shared/empty-state.tsx"
      to: "web/src/components/ui/empty.tsx"
      via: "wrapper composes Empty/EmptyMedia/EmptyTitle/EmptyContent/EmptyDescription primitives"
      pattern: "EmptyMedia"
  prohibitions:
    - "grep -rnE 'bg-(yellow|purple|green|blue|red|emerald|amber)-[0-9]' web/src/components/shared/ == 0 — no raw Tailwind palette color classes in the frozen set (ROADMAP SC1)"
    - "grep -rn 'font-medium' web/src/components/shared/ web/src/components/ui/empty.tsx == 0 — the 500-weight token must not survive the frozen type system (2-weight contract, research Pitfall 5)"
    - "No plain @theme block may be added to index.css — the registration must be @theme inline so utilities resolve the runtime variables (research Pitfall 2)"
    - "No dangerouslySetInnerHTML anywhere in the frozen set (T-15-01)"
  backstop:
    - statement: "StatusBadge labels and PageHeader titles never overflow their containers in either theme — longest badge label (Pending finance, 15 chars) fits at text-xs; PageHeader title truncates with a title attribute"
      verification: backstop
---

<objective>
Freeze the design-system base layer (UXFD-01): the semantic status/state token palette lands in `web/src/index.css` and the three token-consuming shared components (StatusBadge rewrite, EmptyState wrapper, PageHeader) land in `web/src/components/shared/`, plus the three new status vocabulary unions in `web/src/types/models.ts` (D-15-13).

Purpose: this is the tracer-first plan of Phase 15 — the thinnest end-to-end slice of the design freeze (token layer → component layer → type layer → existing consumers) proven before any other frozen component is built. The UI-SPEC (`15-UI-SPEC.md`, approved) pins every token value and component API contract; this plan implements it, never re-designs it.

Output: 5-role token palette with dark-mode pairs, rewritten generic StatusBadge with the locked status→role mapping, EmptyState wrapper + EmptyTitle weight remap, PageHeader with status summary strip, three type unions, and their unit tests. All 7 existing StatusBadge consumer sites + the time-entries re-export file (8 files total) must compile with zero edits.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-CONTEXT.md
@web/src/index.css
@web/src/components/shared/status-badge.tsx
@web/src/types/models.ts
@web/src/components/ui/empty.tsx
@web/src/routes/_authenticated/time-entries/-components/status-badge.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Semantic role tokens + generic StatusBadge rewrite — tracer slice (token → badge → consumers)</name>
  <files>
    web/src/index.css
    web/src/types/models.ts
    web/src/components/shared/status-badge.tsx
    web/src/components/shared/__tests__/status-badge.test.tsx
    web/src/components/shared/__tests__/entries-table.test.tsx
  </files>
  <read_first>
    - web/src/index.css — the token machinery being extended: `:root` (lines 33-97, `--destructive` at line 79), `.dark` (lines 99-131, `--destructive` at line 114), `@theme inline` (lines 133-195, `--color-destructive` at line 178)
    - web/src/components/shared/status-badge.tsx — the current file whose hardcoded per-status map is being replaced (lines 9-40)
    - web/src/types/models.ts — EntryStatus union (lines 3-9), the shape the three new unions mirror
    - web/src/routes/_authenticated/time-entries/-components/status-badge.tsx — the re-export file that must keep compiling
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md — Color table (lines 106-136) + StatusBadge API contract (lines 144-158): the source of truth
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — StatusBadge assignment (lines 76-113) + Pattern 1 token shape
    - web/src/components/shared/__tests__/entries-table.test.tsx — the existing StatusBadge describe block (lines 117-143): its it.each label assertions and /bg-/ presence check survive the rewrite, but the "gives each workflow state a distinct color class" test asserts `new Set(colors).size).toBe(cases.length)` (6 distinct classes) — that assertion breaks under D-15-02 (pending_manager and pending_finance share the warning class → 5 distinct) and MUST be updated in the same GREEN change
  </read_first>
  <behavior>
    - Test 1: STATUS_ROLE_MAP resolves every status in the D-15-03 table to the locked role — draft→neutral, submitted→info, pending_manager→warning, pending_finance→warning, approved→success, rejected→danger; open/planned→neutral, triage→warning, in_progress→info, resolved/closed→success, dismissed→danger; declared→neutral, confirmed→success, withdrawn→danger; active/claimed→info, superseded→neutral, lapsed→warning, cancelled→danger, done→success; creator_controlled/unanimous/majority→neutral
    - Test 2: labels humanize snake_case → Title Case; the six EntryStatus labels are unchanged ("Draft", "Submitted", "Pending Manager", "Pending Finance", "Approved", "Rejected")
    - Test 3: unknown status renders neutral role + humanized label — no crash, no blank
    - Test 4: each of the 4 variants renders its role-token class recipe (subtle/solid/outline/dot); the dot variant renders a leading size-2 rounded-full dot element
    - Test 5: the mapping prop overrides a per-status role
  </behavior>
  <action>
    RED phase: write web/src/components/shared/__tests__/status-badge.test.tsx FIRST, covering the five behaviors above (role-mapping table per D-15-03, label humanization, unknown fallback, variant class recipes, mapping override) using the entries-table.test.tsx idiom (render/screen/getByRole/it.each). Run it — it MUST fail (STATUS_ROLE_MAP and the variants do not exist yet). Commit: test(15-01): add failing status-badge tests.

    GREEN phase — implement in this order:
    1. web/src/index.css: add the 10 status tokens to `:root` right after the `--destructive` line (79) and the dark pairs to `.dark` right after its `--destructive` line (114). Values verbatim from the UI-SPEC Color table: light — neutral var(--base-600) / info oklch(0.546 0.245 262.881) / success oklch(0.596 0.145 163.225) / warning oklch(0.769 0.188 70.08) / danger var(--destructive); dark — neutral var(--base-400) / info oklch(0.707 0.165 254.624) / success oklch(0.765 0.177 163.223) / warning oklch(0.828 0.189 84.429) / danger var(--destructive). Foregrounds: white for neutral/info/success/danger, black for warning (both themes). Then register exactly 10 keys inside the existing @theme inline block next to `--color-destructive`: --color-status-{role} and --color-status-{role}-foreground for all five roles, each mapping to its runtime var. The block must stay `@theme inline` — never plain `@theme`.
    2. web/src/types/models.ts: add TicketStatus ("open" | "triage" | "planned" | "in_progress" | "resolved" | "closed" | "dismissed"), DirectionStatus ("draft" | "active" | "superseded" | "cancelled"), AbsenceStatus ("declared" | "confirmed" | "rejected" | "withdrawn") directly after EntryStatus, with JSDoc mirroring the file's "Mirrors the backend..." style (D-15-13). web/src/types/index.ts already re-exports models — verify only, no edit.
    3. web/src/components/shared/status-badge.tsx rewrite: keep the exported StatusBadgeProps interface exactly (status: EntryStatus; className?: string) so the time-entries re-export and all 7 consumer sites compile with zero edits (research Pitfall 3). Add exports: StatusRole type ("neutral" | "info" | "success" | "warning" | "danger"), STATUS_ROLE_MAP (Record<string, StatusRole> — the full D-15-03 vocabulary including the three governance values, PLUS the D-15-04 direction-warning keys: away and partial → warning, over-capacity and invalid → danger), and the generic StatusBadge<S extends string> with props {status: S; variant?: "subtle" | "solid" | "outline" | "dot"; label?: string; mapping?: Partial<Record<S, StatusRole>>; className?: string}. Variant recipes with the role substituted at runtime: subtle "border-transparent bg-(--status-{role})/10 text-(--status-{role})", solid "border-transparent bg-(--status-{role}) text-(--status-{role}-foreground)", outline "border-(--status-{role})/40 bg-transparent text-(--status-{role})", dot "border-transparent bg-transparent text-foreground" plus a leading size-2 rounded-full bg-(--status-{role}) dot span; base "inline-flex items-center gap-1 rounded-full border px-2 py-0.5 text-xs font-semibold whitespace-nowrap" (font-semibold per the 2-weight contract — the old 500-weight token dies here). If the parenthesized custom-property alpha syntax fails to compile, fall back to the registered utility form bg-status-{role}/10 (research A2 — identical visual). Label humanization: snake_case → Title Case; unknown status → neutral role + verbatim humanized label; the label prop overrides. Status strings render as React text children only — no dangerouslySetInnerHTML (T-15-01). JSDoc citing (D-15-03), (D-15-06), (D-15-13).
    4. web/src/components/shared/__tests__/entries-table.test.tsx: update the StatusBadge describe block (lines 117-143) to the D-15-02 reality — the six EntryStatus values now render 5 distinct class strings, not 6. Keep the it.each label assertions (labels are unchanged: Draft/Submitted/Pending Manager/Pending Finance/Approved/Rejected) and the `expect(badge.className).toMatch(/bg-/)` presence assertion (the subtle variant class still starts with bg-), but reword the now-false "All six states get distinct styling classes" comment (line 131) to say classes derive from role tokens. In the "gives each workflow state a distinct color class" test, change the `expect(new Set(colors).size).toBe(cases.length)` assertion to `toBe(5)` and add a specific D-15-02 assertion that the pending_manager and pending_finance rendered class strings are identical — collect the classes keyed by status (e.g., build a Record<string, string> from the cases array) and assert the pending_manager entry equals the pending_finance entry. This update is part of the GREEN change and lands in the same feat commit — the plan's wave gate (`bun run test`) and the phase gate (full suite green) both depend on it.
    Commit: feat(15-01): implement role tokens + generic StatusBadge — when the test suite passes AND bun run build proves the 7 consumer sites + re-export compile unchanged.
  </action>
  <verify>
    <automated>bun run test -- src/components/shared/__tests__/status-badge.test.tsx</automated>
    <automated>bun run test -- src/components/shared/__tests__/entries-table.test.tsx</automated>
    <automated>bun run build</automated>
    <automated>[ "$(grep -v '^#' web/src/index.css | grep -c -- '--status-neutral:')" -eq 2 ]</automated>
    <automated>[ "$(grep -v '^#' web/src/index.css | grep -c -- '--color-status-')" -eq 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - status-badge.test.tsx passes; git log shows the RED commit test(15-01) before the GREEN commit feat(15-01)
    - entries-table.test.tsx StatusBadge block updated for D-15-02: `bun run test -- src/components/shared/__tests__/entries-table.test.tsx` exits 0 — 5 distinct class strings with the pending_manager and pending_finance entries asserting identical
    - bun run build (tsc -b + vite build) passes with zero edits to the 7 consumer sites or the time-entries re-export file
    - index.css defines --status-neutral through --status-danger plus foreground pairs in BOTH :root and .dark, and exactly 10 --color-status-* keys inside the @theme inline block
    - status-badge.tsx contains no per-status Tailwind palette class map; colors come exclusively from the role tokens
    - STATUS_ROLE_MAP assigns warning to both pending_manager and pending_finance (D-15-02) and success only to approved/confirmed/resolved/closed/done
    - models.ts exports the three new unions and bun run typecheck passes
  </acceptance_criteria>
  <done>
    The tracer slice works end-to-end: any status from the five vocabularies renders through the role tokens in all four variants; all existing consumers compile unchanged; the token + type + badge layer is committed with RED→GREEN commits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmptyState wrapper + EmptyTitle weight remap</name>
  <files>
    web/src/components/ui/empty.tsx
    web/src/components/shared/empty-state.tsx
    web/src/components/shared/__tests__/empty-state.test.tsx
  </files>
  <read_first>
    - web/src/components/ui/empty.tsx — the wrapper target; EmptyTitle base class at line 63
    - web/src/components/shared/__tests__/entries-table.test.tsx — the in-repo test convention (vitest globals, @testing-library/react)
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md — EmptyState contract (lines 189-195) + Typography section (lines 51-89)
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — empty-state assignment (lines 194-210, incl. the approvals-page composition example)
  </read_first>
  <behavior>
    - Test 1: EmptyState renders the default Inbox icon inside the EmptyMedia icon variant when no icon prop is given
    - Test 2: title / description / action props render in their slots; action is a ReactNode rendered below the description
    - Test 3: the EmptyTitle element carries the 600-weight class (not the 500-weight token)
  </behavior>
  <!-- planner-discipline-allow: font-medium -->
  <action>
    RED phase: write web/src/components/shared/__tests__/empty-state.test.tsx covering the three behaviors above. Run it — MUST fail (empty-state.tsx does not exist). Commit: test(15-01): add failing empty-state tests.

    GREEN phase:
    1. web/src/components/ui/empty.tsx: remap the EmptyTitle base class at line 63 — the exact current string "font-heading text-sm font-medium tracking-tight" becomes "font-heading text-sm font-semibold tracking-tight" (2-weight contract; the 500-weight token must not survive; the today-page and approvals-page empty states change appearance — intended, per research Pitfall 5).
    2. web/src/components/shared/empty-state.tsx: thin presentational wrapper over the ui/empty primitives per the UI-SPEC contract — props {icon?: LucideIcon; title: string; description?: string; action?: ReactNode}; default icon Inbox (lucide-react, Icon suffix convention); compose Empty > EmptyHeader > (EmptyMedia variant="icon" + EmptyTitle) > EmptyContent > EmptyDescription; render action below EmptyContent; never invent page copy (props only); JSDoc citing (D-15-08).
    Commit: feat(15-01): implement EmptyState wrapper.
  </action>
  <verify>
    <automated>bun run test -- src/components/shared/__tests__/empty-state.test.tsx</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - empty-state.test.tsx passes; git log shows test(15-01) before feat(15-01) for this task's files
    - ui/empty.tsx EmptyTitle carries the 600-weight class and the 500-weight token is gone from the file (grep -n 'font-medium' web/src/components/ui/empty.tsx returns nothing)
    - empty-state.tsx imports Empty/EmptyHeader/EmptyMedia/EmptyTitle/EmptyContent/EmptyDescription from the ui alias and InboxIcon as the default icon
    - the 500-weight token appears nowhere under web/src/components/shared/ (grep -rn 'font-medium' web/src/components/shared/ returns nothing)
  </acceptance_criteria>
  <done>
    EmptyState renders with the default Inbox icon and title/description/action slots; the EmptyTitle primitive renders 600-weight; the frozen set is free of the 500-weight token.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PageHeader with status summary strip</name>
  <files>
    web/src/components/shared/page-header.tsx
    web/src/components/shared/__tests__/page-header.test.tsx
  </files>
  <read_first>
    - web/src/routes/_authenticated/customers/-components/customers-page.tsx (lines 40-70) — the canonical in-page header block this component freezes
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md — PageHeader contract (lines 181-187) + Typography section
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — page-header assignment (lines 177-190: title/actions-slot pattern, summary chips, breadcrumb slot)
    - web/src/components/shared/status-badge.tsx — the just-rewritten component the summary chips consume (dot variant)
  </read_first>
  <behavior>
    - Test 1: the title renders with truncate; the description renders muted
    - Test 2: the actions slot is right-aligned (ml-auto)
    - Test 3: summary chips render as StatusBadge dot variant with their count ("Pending 4")
    - Test 4: the breadcrumb slot renders above the title when provided, nothing when absent
  </behavior>
  <action>
    RED phase: write web/src/components/shared/__tests__/page-header.test.tsx covering the four behaviors. Run it — MUST fail (page-header.tsx does not exist). Commit: test(15-01): add failing page-header tests.

    GREEN phase: create web/src/components/shared/page-header.tsx per the UI-SPEC contract — props {title: string; description?: string; actions?: ReactNode; summary?: {label: string; count?: number; tone?: StatusRole}[]; breadcrumb?: ReactNode}. Title = text-xl font-semibold truncate with a title attribute (Heading 20px/600); description = text-sm text-muted-foreground (Label 14px/400); actions slot ml-auto; breadcrumb slot above the title; summary strip flex flex-wrap gap-2 beside the title block — each chip renders StatusBadge with status={chip.label}, variant="dot", mapping={{[chip.label]: chip.tone}} (neutral fallback when tone is absent) plus the count in a muted span ("Pending 4"); no count renders when count is undefined. Presentational-only — no data fetching, no navigation; JSDoc citing (D-15-08). Commit: feat(15-01): implement PageHeader.
  </action>
  <verify>
    <automated>bun run test -- src/components/shared/__tests__/page-header.test.tsx</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - page-header.test.tsx passes; git log shows test(15-01) before feat(15-01) for this task's files
    - page-header.tsx imports StatusBadge from the shared alias and renders summary chips through it with variant="dot"
    - bun run lint passes (a11y rules)
    - the summary strip, breadcrumb slot, and actions slot are all optional props — PageHeader renders with title alone
  </acceptance_criteria>
  <done>
    PageHeader renders title/description/actions/breadcrumb/status-summary-strip per the pinned contract, and its chips consume the frozen StatusBadge dot variant.
  </done>
</task>

</tasks>

## Flagged Assumptions

Specless-probe fallback items (8 total across the phase; 3 assigned here — the remaining 5 live in plans 02 and 04). Surfaced explicitly per the fallback contract — never silently dropped.

| # | Req | Probe | Assumption |
|---|-----|-------|------------|
| FA-1 | UXFD-01 | adjacency: "When two things are exactly equal or just touch, do they merge, collide, or separate?" | Class conflicts in badge/page-header composition merge via the existing cn() tailwind-merge (last-wins) — the in-repo pattern from lib/utils.ts; no custom merge logic is added. |
| FA-2 | UXFD-01 | empty: "What is the result for empty, single-element, or null input?" | A null/unknown status renders neutral + humanized label (never blank, never crash) — this assumption is converted into the must_haves.truths above; the DataTable/FilterBar empty behaviors are covered by plan 02's truths. |
| FA-UI | UXFD-01 | UI-SPEC unresolved (not part of the 8 probes) | Direction warning tokens (away/partial/over-capacity/invalid) are token-seeded only this phase (D-15-04) — their render behavior has no UI this phase and is re-probed in Phase 19's UI-SPEC. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client strings | Server-emitted status labels and (later) direction warning messages rendered by frozen components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | StatusBadge (and all frozen components rendering server strings) | low | mitigate | Status strings render as React text children — auto-escaped; no dangerouslySetInnerHTML anywhere in the frozen set; icons come from lucide-react components only (research Security Domain; enforced by the prohibitions grep gate) |
| T-15-SC | Tampering | npm installs | high | mitigate | Only new dependency is @tanstack/react-table (SUS verdict) — install gated behind a blocking human-verify checkpoint in plan 02 (T-15-SC row there); this plan adds no packages |

## Security Notes

UI-only phase: no auth/session/access-control surface touched. ASVS L1 applies only to the client-side rendering pattern above; the destructive-write invariant stays server-authoritative (D-13-10) — covered in plan 03.
</threat_model>

<verification>
- After Task 1: `bun run test -- src/components/shared/__tests__/status-badge.test.tsx` + `bun run build` + token greps (10 keys, both themes)
- After each task commit: `bun run typecheck`
- After each code-modifying task: `python3 -c "from graphify.watch import _rebuild_code; from pathlib import Path; _rebuild_code(Path('.'))"` (AGENTS.md graphify step — keeps the knowledge graph current)
- Wave gate (after this plan): `bun run test` + `bun run lint` + `bun run build`
- Phase gate: full suite green + `bun run fmt:check` + human visual check (dark-mode badge smoke — plan 04 Task 2)
</verification>

<success_criteria>
- All 9 files exist in their final shape; unit suites for the three components pass with RED-before-GREEN commits
- The 7 consumer sites + time-entries re-export compile with zero edits (bun run build)
- The 500-weight token and raw palette color classes are absent from web/src/components/shared/ and ui/empty.tsx (grep gates)
- The 10 @theme inline keys resolve against the runtime pairs (dark mode correctness — human smoke in plan 04)
</success_criteria>

## Artifacts this phase produces

- `web/src/index.css` — 20 CSS variable declarations (10 light + 10 dark pairs: --status-neutral/-foreground … --status-danger/-foreground) + 10 @theme inline keys (--color-status-{role}[-foreground])
- `web/src/types/models.ts` — type unions `TicketStatus`, `DirectionStatus`, `AbsenceStatus`
- `web/src/components/shared/status-badge.tsx` — exports `StatusBadge<S>`, `StatusBadgeProps` (kept), `STATUS_ROLE_MAP`, `StatusRole`; variants subtle/solid/outline/dot
- `web/src/components/shared/empty-state.tsx` — export `EmptyState`
- `web/src/components/ui/empty.tsx` — modified EmptyTitle base class
- `web/src/components/shared/page-header.tsx` — export `PageHeader`; prop types `PageHeaderSummaryItem` (label/count/tone)
- `web/src/components/shared/__tests__/status-badge.test.tsx`, `empty-state.test.tsx`, `page-header.test.tsx`

<output>
Create `.planning/phases/15-ux-foundation-design-tokens-shared-components/15-01-SUMMARY.md` when done
</output>
