---
phase: 15-ux-foundation-design-tokens-shared-components
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/shared/confirm-dialog.tsx
  - web/src/components/shared/__tests__/confirm-dialog.test.tsx
  - web/src/routes/_authenticated/customers/-components/customers-page.tsx
  - web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx
  - web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx
  - web/src/routes/_authenticated/org-hierarchy/-components/dialogs/delete-confirm-dialog.tsx
  - web/src/routes/_authenticated/working-groups/-components/working-groups-page.tsx
  - web/src/routes/_authenticated/working-groups/-components/delete-working-group-dialog.tsx
autonomous: true
requirements: [UXFD-01]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ConfirmDialog is a controlled, presentational destructive-confirmation dialog on ui/alert-dialog + ui/textarea with props {open, onOpenChange, title, description?, confirmLabel, variant? ('destructive'|'default'), requiredReason?, reasonLabel?, reasonPlaceholder?, isSubmitting?, error?, onConfirm(reason?)}"
    - "With requiredReason, the reason textarea shows with its label, the confirm button is disabled until the reason is non-empty, and the validation copy is exactly 'A reason is required.' — mirroring the server-side 400 invariant (D-13-10/D-13-16); the local reason state resets when the dialog closes"
    - "isSubmitting disables the confirm button + spinner; the error prop renders inline muted-danger text 'Could not complete the action. Try again.' by default"
    - "The customers and org-hierarchy delete flows render ConfirmDialog from their pages with their existing zustand store wiring (deleteOpen/setDeleteOpen + selectedCustomer/selectedUnit) and mutations (CustomersApis.deleteCustomerMutationOpts with the 409 toast; deleteUnitMutationOpts with invalidate ['units']) — the two per-page dialog files are deleted"
    - "The working-groups delete flow renders ConfirmDialog from working-groups-page.tsx with the page's existing wg/onClose state wiring and WorkingGroupsApis.deleteWorkingGroupMutationOpts — the per-page dialog file is deleted"
    - "None of the three absorbing sites pass requiredReason (CustomerHandler.Delete, UnitHandler.Delete, WorkingGroupHandler.Delete take no reason — verified in backend code); reparent-confirm-dialog.tsx is untouched (D-15-12)"
  artifacts:
    - path: "web/src/components/shared/confirm-dialog.tsx"
      provides: "Frozen destructive confirmation dialog with required-reason gate"
      exports: ["ConfirmDialog"]
    - path: "web/src/components/shared/__tests__/confirm-dialog.test.tsx"
      provides: "Required-reason / error / isSubmitting tests"
    - path: "web/src/routes/_authenticated/customers/-components/customers-page.tsx"
      provides: "Customers delete flow on ConfirmDialog"
    - path: "web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx"
      provides: "Org-hierarchy delete flow on ConfirmDialog"
    - path: "web/src/routes/_authenticated/working-groups/-components/working-groups-page.tsx"
      provides: "Working-groups delete flow on ConfirmDialog"
  key_links:
    - from: "web/src/components/shared/confirm-dialog.tsx"
      to: "web/src/components/ui/alert-dialog.tsx"
      via: "composes AlertDialog/AlertDialogContent/AlertDialogHeader/AlertDialogFooter/AlertDialogCancel with the destructive confirm button"
      pattern: "AlertDialog"
    - from: "web/src/routes/_authenticated/customers/-components/customers-page.tsx"
      to: "web/src/components/shared/confirm-dialog.tsx"
      via: "renders ConfirmDialog with open/onOpenChange from the customers zustand store"
      pattern: "ConfirmDialog"
    - from: "web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx"
      to: "web/src/components/shared/confirm-dialog.tsx"
      via: "renders ConfirmDialog with open/onOpenChange from the org-hierarchy zustand store"
      pattern: "ConfirmDialog"
    - from: "web/src/routes/_authenticated/working-groups/-components/working-groups-page.tsx"
      to: "web/src/components/shared/confirm-dialog.tsx"
      via: "renders ConfirmDialog driven by the page's wg state + onClose"
      pattern: "ConfirmDialog"
  prohibitions:
    - "grep -nE 'useCustomersStore|useOrgHierarchyStore|useQuery|useMutation|sonner|Apis' web/src/components/shared/confirm-dialog.tsx == 0 — the shared component stays presentational; site wiring never leaks in (research Pitfall 4)"
    - "git diff --quiet HEAD -- web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx — reparent dialog untouched (D-15-12)"
  backstop:
    - statement: "ConfirmDialog content never overflows the alert-dialog viewport — long reasons/descriptions scroll or clip within the dialog bounds (UI-SPEC backstop row)"
      verification: backstop
---

<objective>
Freeze the destructive-action pattern (UXFD-01 / D-15-07): the shared ConfirmDialog lands in `web/src/components/shared/` with the required-reason gate, and the three per-page delete-confirm dialogs (customers, org-hierarchy, working-groups) are consolidated onto it NOW — their state wiring stays at the sites.

Purpose: every future destructive action (absence reject, claim unclaim, Phase 16-26) uses this one frozen pattern with the client-side reason gate mirroring the server 400 invariant (D-13-10/D-13-16). The consolidation is the first in-repo proof that the frozen component works at real consumer sites.

Output: ConfirmDialog + its unit tests, and three consumer sites rendering it with their existing state/mutation wiring preserved; three per-page dialog files deleted; reparent-confirm-dialog.tsx untouched.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md
@.planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md
@web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx
@web/src/routes/_authenticated/org-hierarchy/-components/dialogs/delete-confirm-dialog.tsx
@web/src/routes/_authenticated/working-groups/-components/delete-working-group-dialog.tsx
@web/src/components/ui/alert-dialog.tsx
@web/src/components/ui/textarea.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfirmDialog component with required-reason gate</name>
  <files>
    web/src/components/shared/confirm-dialog.tsx
    web/src/components/shared/__tests__/confirm-dialog.test.tsx
  </files>
  <read_first>
    - web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx — the canonical AlertDialog chrome being frozen (lines 42-63)
    - web/src/components/ui/alert-dialog.tsx — AlertDialogAction renders a Button with variant/size props (lines 144-168)
    - web/src/components/approval/approval-buttons.tsx (lines 78-110) — the in-repo textarea + disabled-confirm idiom the required-reason gate freezes
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-UI-SPEC.md — ConfirmDialog contract (lines 197-206) + copy strings
    - .planning/phases/15-ux-foundation-design-tokens-shared-components/15-PATTERNS.md — confirm-dialog assignment (lines 220-255)
  </read_first>
  <behavior>
    - Test 1: with requiredReason, the confirm button is disabled until the reason textarea is non-empty; the validation copy 'A reason is required.' appears when touched-and-empty
    - Test 2: onConfirm receives the entered reason string; without requiredReason, onConfirm is called with no reason
    - Test 3: isSubmitting disables the confirm button and shows a spinner
    - Test 4: the error prop renders the default copy 'Could not complete the action. Try again.'
    - Test 5: the local reason state resets when the dialog closes (no stale reason on reopen)
  </behavior>
  <action>
    RED phase: write web/src/components/shared/__tests__/confirm-dialog.test.tsx covering the five behaviors (render with open=true, fireEvent.change on the textarea, fireEvent.click on the confirm button). Run it — MUST fail (confirm-dialog.tsx does not exist). Commit: test(15-03): add failing confirm-dialog tests.

    GREEN phase: create web/src/components/shared/confirm-dialog.tsx per the UI-SPEC contract — props {open: boolean; onOpenChange: (open: boolean) => void; title: string; description?: string; confirmLabel: string; variant?: "destructive" | "default" (default "destructive"); requiredReason?: boolean; reasonLabel?: string; reasonPlaceholder?: string; isSubmitting?: boolean; error?: string; onConfirm: (reason?: string) => void}. Compose ui/alert-dialog: AlertDialogContent > AlertDialogHeader (AlertDialogTitle + AlertDialogDescription) > optional reason field (Label + Textarea, shown only when requiredReason) > AlertDialogFooter (AlertDialogCancel 'Cancel' + confirm Button via AlertDialogAction with variant="destructive" for the destructive variant). Gate: reasonInvalid = requiredReason && reason.trim() === ""; confirm disabled={reasonInvalid || isSubmitting}; validation copy exactly 'A reason is required.' (rendered when touched and empty — copy per UI-SPEC); when isSubmitting show a spinner (lucide Loader2Icon animate-spin) in the confirm button; error renders inline muted-danger text, default 'Could not complete the action. Try again.' when the error prop is truthy without a custom message (custom error prop value wins). Reset the local reason state on close (in the onOpenChange false branch). Controlled/presentational — no store imports, no query hooks, no sonner (prohibition grep); JSDoc citing (D-15-07) and the D-13-10 server-invariant mirror. Commit: feat(15-03): implement ConfirmDialog.
  </action>
  <verify>
    <automated>bun run test -- src/components/shared/__tests__/confirm-dialog.test.tsx</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - confirm-dialog.test.tsx passes; git log shows test(15-03) before feat(15-03)
    - confirm-dialog.tsx imports only ui primitives + React + lucide — no useCustomersStore/useOrgHierarchyStore/useQuery/useMutation/sonner/Apis imports (grep gate in must_haves.prohibitions)
    - The three copy strings ('A reason is required.', 'Could not complete the action. Try again.', 'Cancel') match the UI-SPEC verbatim
    - The reason textarea renders only when requiredReason is true
  </acceptance_criteria>
  <done>
    ConfirmDialog enforces the required-reason gate client-side, handles submitting/error states, and resets its reason state between opens — fully controlled and presentational.
  </done>
</task>

<task type="auto">
  <name>Task 2: Absorb the customers + org-hierarchy delete dialogs</name>
  <files>
    web/src/routes/_authenticated/customers/-components/customers-page.tsx
    web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx
    web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx
    web/src/routes/_authenticated/org-hierarchy/-components/dialogs/delete-confirm-dialog.tsx
  </files>
  <read_first>
    - web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx — the source dialog: store selectors (selectedCustomer, deleteOpen, setDeleteOpen, setSelectedCustomer), deleteCustomer mutation with the 409 toast, handleConfirm
    - web/src/routes/_authenticated/customers/-components/customers-page.tsx — renders <DeleteConfirmDialog /> at line 88; import at line 25
    - web/src/routes/_authenticated/org-hierarchy/-components/dialogs/delete-confirm-dialog.tsx — the source dialog: store selectors (selectedUnit, deleteOpen, setDeleteOpen, setSelectedUnit), deleteUnitMutationOpts + invalidate ['units']
    - web/src/routes/_authenticated/org-hierarchy/-components/org-hierarchy-page.tsx — renders <DeleteConfirmDialog /> at line 335; import at line 27
  </read_first>
  <action>
    For each of the two sites, MOVE the dialog's logic into the page and render the frozen ConfirmDialog:
    1. customers-page.tsx: replace the DeleteConfirmDialog import with import { ConfirmDialog } from "@/components/shared/confirm-dialog"; move the deleteCustomer mutation (CustomersApis.deleteCustomerMutationOpts + the 409 toast.error 'Cannot delete customer with linked contracts' branch) into the page component; keep the store selectors (deleteOpen/setDeleteOpen/selectedCustomer/setSelectedCustomer) as the open-state wiring; render <ConfirmDialog open={deleteOpen && !!selectedCustomer} onOpenChange={setDeleteOpen} title="Delete customer?" description={selectedCustomer ? 'Are you sure you want to delete ' + selectedCustomer.company_name + '? This action cannot be undone.' : undefined} confirmLabel="Delete" isSubmitting={deleteCustomer.isPending} onConfirm={handleDeleteConfirm} /> where handleDeleteConfirm mirrors the old handleConfirm (mutateAsync(selectedCustomer.id) → setDeleteOpen(false) + setSelectedCustomer(null)); the null-guard stays STRUCTURAL: both open and description are gated on !!selectedCustomer, so the dialog cannot open — and no expression dereferences selectedCustomer — when the store pairing invariant breaks (this preserves the old dialog's `if (!selectedCustomer) return null` early-return semantics at the call site).
    2. org-hierarchy-page.tsx: the same move — deleteUnit mutation (deleteUnitMutationOpts + toast.success 'Unit deleted successfully' + invalidateQueries ['units'] + the failure toast), store selectors for open state, ConfirmDialog with title "Delete unit?" and the entity name in the description — with the same structural guard: open={deleteOpen && !!selectedUnit} and description={selectedUnit ? 'Are you sure you want to delete ' + selectedUnit.name + '? This action cannot be undone.' : undefined}.
    3. None of these sites pass requiredReason — the customers and unit DELETE endpoints take no reason (verified: CustomerHandler.Delete, UnitHandler.Delete have no reason input; the D-13-10 400 invariant does not apply to these two endpoints).
    4. Delete the two per-page dialog files: customers/-components/dialogs/delete-confirm-dialog.tsx and org-hierarchy/-components/dialogs/delete-confirm-dialog.tsx. Do NOT touch org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx (D-15-12 — not a delete pattern).
    Commit: refactor(15-03): consolidate customers + org-hierarchy delete dialogs onto ConfirmDialog.
  </action>
  <verify>
    <automated>bun run build</automated>
    <automated>bun run lint</automated>
    <automated>test ! -f web/src/routes/_authenticated/customers/-components/dialogs/delete-confirm-dialog.tsx && test ! -f web/src/routes/_authenticated/org-hierarchy/-components/dialogs/delete-confirm-dialog.tsx</automated>
    <automated>git diff --quiet HEAD -- web/src/routes/_authenticated/org-hierarchy/-components/dialogs/reparent-confirm-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bun run build and bun run lint pass; the two page suites still pass (bun run test -- src/routes/_authenticated/customers/-components/__tests__/ src/routes/_authenticated/org-hierarchy/-components/__tests__/ where they exist)
    - Both pages import ConfirmDialog from the shared alias and no longer import the deleted dialogs
    - The 409 toast behavior for customers and the units invalidate behavior are preserved (source assertions: customers-page.tsx still contains 'Cannot delete customer with linked contracts'; org-hierarchy-page.tsx still contains queryKey: ['units'])
    - The two per-page dialog files no longer exist; reparent-confirm-dialog.tsx has no diff
  </acceptance_criteria>
  <done>
    Customers and org-hierarchy delete flows render the frozen ConfirmDialog with their store wiring intact; the per-page dialogs are gone and the reparent dialog is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Absorb the working-groups delete dialog</name>
  <files>
    web/src/routes/_authenticated/working-groups/-components/working-groups-page.tsx
    web/src/routes/_authenticated/working-groups/-components/delete-working-group-dialog.tsx
  </files>
  <read_first>
    - web/src/routes/_authenticated/working-groups/-components/delete-working-group-dialog.tsx — the source dialog: props {wg: WorkingGroup | null; onClose: () => void}; deleteWg mutation (WorkingGroupsApis.deleteWorkingGroupMutationOpts + error toast); handleConfirm
    - web/src/routes/_authenticated/working-groups/-components/working-groups-page.tsx — renders <DeleteWorkingGroupDialog wg={...} onClose={...} /> at line 94; import at line 12
  </read_first>
  <action>
    Move the dialog's logic into working-groups-page.tsx: replace the DeleteWorkingGroupDialog import with import { ConfirmDialog } from "@/components/shared/confirm-dialog"; move the deleteWg mutation (WorkingGroupsApis.deleteWorkingGroupMutationOpts + the onError toast) into the page — name it deleteWgMutation in the page, NOT deleteWg, because the page already declares the state pair const [deleteWg, setDeleteWg] = useState&lt;WorkingGroup | null&gt;(null) (duplicate identifier would fail bun run build); render <ConfirmDialog open={!!deleteWg} onOpenChange={(open) => !open && setDeleteWg(null)} title="Delete working group?" description={deleteWg ? 'Are you sure you want to delete ' + deleteWg.name + '? This action cannot be undone.' : undefined} confirmLabel="Delete" isSubmitting={deleteWgMutation.isPending} onConfirm={async () => { if (!deleteWg) return; await deleteWgMutation.mutateAsync(deleteWg.id); setDeleteWg(null); }} />; the null-guard stays STRUCTURAL, mirroring Task 2: open is gated on !!deleteWg and no expression dereferences deleteWg when it is null — description and onConfirm both null-check before touching .name/.id — so the dialog cannot open with a null entity and no unguarded dereference survives (this preserves the old dialog's `if (!wg) return null` early-return semantics at the call site; with tsconfig strict:true, bun run build fails on the unguarded form). No requiredReason — WorkingGroupHandler.Delete takes no reason. Delete the per-page dialog file web/src/routes/_authenticated/working-groups/-components/delete-working-group-dialog.tsx. Commit: refactor(15-03): consolidate working-groups delete dialog onto ConfirmDialog.
  </action>
  <verify>
    <automated>bun run build</automated>
    <automated>bun run lint</automated>
    <automated>test ! -f web/src/routes/_authenticated/working-groups/-components/delete-working-group-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bun run build and bun run lint pass; the working-groups page suite still passes where it exists
    - bun run build passes under tsconfig strict:true — no null-dereference errors: the ConfirmDialog render never dereferences deleteWg.name/deleteWg.id unguarded (open, description, and onConfirm are all null-gated)
    - working-groups-page.tsx imports ConfirmDialog from the shared alias and no longer imports the deleted dialog
    - The deleteWg mutation error toast is preserved in the page (source assertion: working-groups-page.tsx still contains 'Failed to delete working group' or the verbatim onError handler from the source dialog)
    - The per-page dialog file no longer exists
  </acceptance_criteria>
  <done>
    The working-groups delete flow renders ConfirmDialog with its props wiring intact; the per-page dialog is gone.
  </done>
</task>

</tasks>

## Flagged Assumptions

No specless-probe items are assigned to this plan — the ConfirmDialog empty/partial/error states are pinned by the UI-SPEC copywriting contract and enforced in must_haves.truths above.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client dialog → server | Destructive writes confirmed client-side; enforcement stays server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Tampering | ConfirmDialog | low | mitigate | Client-side required-reason gate mirrors the server 400 invariant (D-13-10/D-13-16) — it only prevents accidental reason-less submits; the server remains the enforcement point. The frozen component is presentational (no store/API imports — prohibition grep) so no data flows through it that the server does not re-validate |

## Security Notes

ASVS V5 (input validation) applies only as the client-side UX mirror; the three consolidated sites (customers/unit/WG deletes) take no server-side reason today — verified in backend handlers — so they pass no requiredReason. Future destructive endpoints with a 400-on-reasonless contract (absence reject, claim unclaim) pass requiredReason at their sites.
</threat_model>

<verification>
- After each task: `bun run typecheck`; after Task 1: `bun run test -- src/components/shared/__tests__/confirm-dialog.test.tsx`
- After Tasks 2/3: `bun run build` + `bun run lint` + the file-absence greps
- After each code-modifying task: `python3 -c "from graphify.watch import _rebuild_code; from pathlib import Path; _rebuild_code(Path('.'))"` (AGENTS.md graphify step — keeps the knowledge graph current)
- Wave gate: `bun run test` + `bun run lint` + `bun run build`
- Phase gate: full suite green + `bun run fmt:check` + human visual check (plan 04 Task 2)
</verification>

<success_criteria>
- ConfirmDialog passes its unit suite with RED-before-GREEN commits
- All three delete flows render through ConfirmDialog; three per-page dialog files deleted; reparent dialog untouched
- The prohibition greps are clean (presentational-only component, untouched reparent dialog)
- bun run build/lint/test green
</success_criteria>

## Artifacts this phase produces

- `web/src/components/shared/confirm-dialog.tsx` — export `ConfirmDialog` (props: open, onOpenChange, title, description?, confirmLabel, variant?, requiredReason?, reasonLabel?, reasonPlaceholder?, isSubmitting?, error?, onConfirm(reason?))
- `web/src/components/shared/__tests__/confirm-dialog.test.tsx`
- Modified: customers-page.tsx, org-hierarchy-page.tsx, working-groups-page.tsx (ConfirmDialog consumption + moved mutations)
- Deleted: customers delete-confirm-dialog.tsx, org-hierarchy delete-confirm-dialog.tsx, working-groups delete-working-group-dialog.tsx

<output>
Create `.planning/phases/15-ux-foundation-design-tokens-shared-components/15-03-SUMMARY.md` when done
</output>
