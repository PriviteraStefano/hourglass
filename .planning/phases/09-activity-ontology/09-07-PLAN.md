---
phase: 09-activity-ontology
plan: 09-07
title: "Gap fix: working_group integration test re-seed onto activities"
type: execute
wave: 3
depends_on: []
files_modified:
  - internal/core/services/working_group/working_group_integration_test.go
autonomous: true
requirements: [P-007-D5, BE-014-R5]
gap_closure: true
must_haves:
  truths:
    - "go test ./... is fully green (VERIFICATION truth #14 turns green — 19/19 packages)"
    - "TestWorkingGroupIntegration seeds via activity_kinds + activities with zero references to the dropped projects/subprojects tables"
    - "All 4 WG integration subtests (CreateAndGetByID, ListByOrg, GetNotFound, Delete) pass unchanged in behavior"
  artifacts:
    - path: "internal/core/services/working_group/working_group_integration_test.go"
      provides: "Re-seeded seedWGData helper (org + manager user + activity_kinds row + one activities row)"
      contains: "INSERT INTO activities"
  key_links:
    - from: "seedWGData"
      to: "working_groups.activity_id"
      via: "CreateWorkingGroupRequest.SubprojectID is written into the activity_id column by WorkingGroupRepository.Create"
      pattern: "SubprojectID"
---

# Plan 09-07 — Gap fix: working_group integration test re-seed (GAP 2)

<objective>
Close VERIFICATION GAP 2: `seedWGData` in `working_group_integration_test.go` INSERTs into `projects`/`subprojects` — both dropped by migration 011 — so the package fails with SQLSTATE 42P01 and `go test ./...` is red. The fix is the small, self-contained re-seed documented in `deferred-items.md` item 2: seed an `activity_kinds` row plus one `activities` row, and point the WG at the activity id. The WG service-side call is unchanged because the legacy `SubprojectID` field still maps to `activities.activity_id` at the repo layer (D-5).

Purpose: Make VERIFICATION truth #14 green (full suite 19/19).
Output: Re-seeded `seedWGData` helper in the WG integration test. No production code changes.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-activity-ontology/09-VERIFICATION.md (GAP 2 truth + artifacts)
@.planning/phases/09-activity-ontology/deferred-items.md (item 2 — the suggested re-seed)
@/Users/stefanoprivitera/Projects/hourglass/internal/core/services/working_group/working_group_integration_test.go
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/secondary/postgres/working_group_repository.go (Create — SubprojectID → activity_id column)
@/Users/stefanoprivitera/Projects/hourglass/migrations/011_activity_ontology.up.sql (activities NOT NULL column set)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-seed seedWGData with activity_kinds + activities</name>
  <files>internal/core/services/working_group/working_group_integration_test.go</files>
  <read_first>
  - `internal/core/services/working_group/working_group_integration_test.go` — seedWGData (lines 27-60) and all four subtests that consume its return tuple (lines 62-130)
  - `internal/adapters/secondary/postgres/working_group_repository.go` — Create (lines 83-97): `wg.SubprojectID` is written into the `activity_id` column, and the RETURNING scan reads it back into `SubprojectID` (scanWorkingGroup line 197)
  - `internal/core/services/working_group/working_group.go` — Service.Create (lines 28-45): passthrough, no parent validation
  - `migrations/011_activity_ontology.up.sql` — activities table (lines 46-64): NOT NULL columns are id, org_id, name, kind, created_by_org_id; parent_id/contract_id/billable nullable; governance_model/is_shared/is_active/created_at/updated_at have defaults
  - `internal/adapters/secondary/postgres/exported_test_helpers.go` — seedActivityKind/seedActivity (lines 156-182) as the in-repo seeding pattern reference
  </read_first>
  <action>
  Rewrite `seedWGData` in `working_group_integration_test.go`:
  - KEEP unchanged: the org INSERT and the manager-user INSERT (both tables still exist).
  - REMOVE: the `projects` INSERT (lines 46-51) and the `subprojects` INSERT (lines 53-57) — those tables are gone post-011.
  - ADD an activity_kinds row for the new org: `INSERT INTO activity_kinds (org_id, name, is_seed) VALUES ($1, 'engagement', TRUE) ON CONFLICT (org_id, name) DO NOTHING` — required because `activities.kind` is an FK on (org_id, name) to the catalog.
  - ADD one activities row: `INSERT INTO activities (id, org_id, parent_id, name, kind, created_by_org_id) VALUES ($1, $2, NULL, $3, 'engagement', $2)` — parent_id NULL (root activity), kind 'engagement' (catalog FK satisfied by the kind row above), contract_id NULL (internal activity, D-3); governance_model, is_shared, is_active, created_at, updated_at use their column defaults.
  - Rename the local variables `projectID`/`subprojectID` to `activityID` throughout the helper, update the return to `(orgID, managerID, activityID)`, and update the doc comment to "seedWGData creates org, manager user, and one root activity (kind 'engagement') — working_groups anchor to it via activity_id (ADR-P-007 D-5)".
  - The call sites (4 subtests) keep passing the returned id into `CreateWorkingGroupRequest.SubprojectID` — the field name is legacy but the repo maps it into the `activity_id` column, so the service-side call is unchanged. Update the local variable name in each subtest from `subprojectID` to `activityID` where the tuple is destructured, keeping every assertion (e.g. `assert.Equal(t, subprojectID, wg.SubprojectID)`) semantically identical.
  - Do NOT touch production code, the working_group domain type, or the `SubprojectID` field (its rename is Phase 10 scope).
  </action>
  <verify>
  <automated>go test ./internal/core/services/working_group/ -run TestWorkingGroupIntegration -v</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'INSERT INTO projects\|INSERT INTO subprojects' internal/core/services/working_group/working_group_integration_test.go` → no matches
  - `go test ./internal/core/services/working_group/ -run TestWorkingGroupIntegration -v` → all 4 subtests PASS (no 42P01)
  - `go test ./internal/core/services/... -count=1` → all service packages PASS
  - No file outside `working_group_integration_test.go` is modified (`git diff --stat` shows exactly one file)
  </acceptance_criteria>
  <done>WG integration test green; no dropped-table references remain; single-file diff</done>
</task>

</tasks>

<verification>
- `go test ./internal/core/services/working_group/ -run TestWorkingGroupIntegration -v` — 4/4 subtests PASS
- `go test ./internal/core/services/... -count=1` — green
- Phase gate (run after all three wave-3 plans land): `go test ./... -count=1` → 19/19 packages PASS (truth #14)
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → test database | seedWGData writes rows into the testcontainers schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Tampering | seedWGData re-seed | accept | Test-only fixture change with zero runtime surface — no production code, no secrets, no network exposure; the FK to `activity_kinds` is satisfied by the seeded catalog row, and the NOT NULL activity columns are fully populated |
| T-09-07-02 | Spoofing | tests referencing production paths | accept | No production symbols are modified; the repo mapping (`SubprojectID` → `activity_id` column) is exercised exactly as in production |

No high-severity threats — this plan is test-only.
</threat_model>

<success_criteria>
- VERIFICATION truth #14 green: `go test ./...` fully green (19/19)
- The WG service tests exercise the real activity-backed schema rather than a stale fixture
- Zero production code changed
</success_criteria>

<output>
Create `.planning/phases/09-activity-ontology/09-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `seedWGData` helper in `internal/core/services/working_group/working_group_integration_test.go` — rewritten: org + manager user + `activity_kinds('engagement')` row + one root `activities` row; returns `(orgID, managerID, activityID)`
- Updated local variable names in the 4 subtests (`subprojectID` → `activityID`) with assertions unchanged
- No production code, no new symbols outside the test file
