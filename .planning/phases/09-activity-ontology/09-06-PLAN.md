---
phase: 09-activity-ontology
plan: 09-06
title: "Gap fix: subproject-derived activities kind='phase' (forward migration 013)"
type: execute
wave: 3
depends_on: []
files_modified:
  - migrations/013_activity_kind_phase_fix.up.sql
  - migrations/013_activity_kind_phase_fix.down.sql
  - internal/adapters/secondary/postgres/activity_ontology_migration_test.go
  - internal/adapters/secondary/postgres/staffing_schema_migration_test.go
autonomous: true
requirements: [P-007-D2, P-007-D6]
gap_closure: true
must_haves:
  truths:
    - "After 013 applies, the 6 subproject-derived activities carry kind='phase' (VERIFICATION truth #10 turns green)"
    - "Migration 011 remains immutable — the fix is a forward migration per ADR-BE-004"
    - "up→down→up cycle including 013 stays clean (VERIFICATION truth #8 stays green)"
    - "The migration test asserts the corrected distribution: engagement 6, phase 6, task 0, internal 1 (13 total)"
  artifacts:
    - path: "migrations/013_activity_kind_phase_fix.up.sql"
      provides: "Forward label fix: kind='task' → 'phase' for subproject-derived rows"
      contains: "UPDATE activities SET kind = 'phase' WHERE kind = 'task' AND parent_id IS NOT NULL"
    - path: "migrations/013_activity_kind_phase_fix.down.sql"
      provides: "Reverse label fix (phase → task) for the same row set"
      contains: "UPDATE activities SET kind = 'task' WHERE kind = 'phase' AND parent_id IS NOT NULL"
    - path: "internal/adapters/secondary/postgres/activity_ontology_migration_test.go"
      provides: "Migration cycle test asserting the corrected kind distribution"
      exports: ["TestMigration011_ActivityOntology_UpDownUpCycle", "applyMigrations"]
  key_links:
    - from: "activity_ontology_migration_test.go"
      to: "013_activity_kind_phase_fix.up.sql"
      via: "explicit up013 exec after up011 in both up passes; down013 before down011"
      pattern: "readMigration\\(t, \"013_activity_kind_phase_fix"
---

# Plan 09-06 — Gap fix: kind='phase' forward migration (GAP 1)

<objective>
Close VERIFICATION GAP 1: SPEC requirement #4 (data migration) + acceptance bullet + the interview log locked `kind = 'phase'` for subproject-derived activities, but migration 011 line 115 hardcodes `'task'`. Because 011 is applied history (immutable per ADR-BE-004), the fix lands as a NEW forward migration numbered 013 (next free after 012) plus the migration-test surgery that makes the corrected distribution the asserted truth.

Purpose: Make VERIFICATION truth #10 green without violating the append-only migration rule.
Output: `migrations/013_activity_kind_phase_fix.{up,down}.sql`, updated `applyMigrations` helper (variadic skip), updated kind-distribution assertions in the 011 cycle test.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-activity-ontology/09-VERIFICATION.md (GAP 1 truth + artifacts)
@.planning/phases/09-activity-ontology/09-SPEC.md (acceptance #4 and #6 — locked kind='phase')
@/Users/stefanoprivitera/Projects/hourglass/hourglass-vault/decisions/backend/ADR-BE-004 — Database Migrations.md (append-only, immutable applied history)
@/Users/stefanoprivitera/Projects/hourglass/migrations/011_activity_ontology.up.sql (line 115 hardcode — do NOT edit)
@/Users/stefanoprivitera/Projects/hourglass/migrations/011_activity_ontology.down.sql
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/secondary/postgres/activity_ontology_migration_test.go
@/Users/stefanoprivitera/Projects/hourglass/internal/adapters/secondary/postgres/staffing_schema_migration_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write forward migration 013 (up + down)</name>
  <files>migrations/013_activity_kind_phase_fix.up.sql, migrations/013_activity_kind_phase_fix.down.sql</files>
  <read_first>
  - `migrations/011_activity_ontology.up.sql` — sections 6 (lines 109-119: subproject INSERT hardcodes kind='task') and 5 (projects → 'engagement'); the activities column set the UPDATE must respect
  - `migrations/011_activity_ontology.down.sql` — the reverse migration 013's down must interleave with (down013 runs before down011)
  - `hourglass-vault/decisions/backend/ADR-BE-004 — Database Migrations.md` — append-only rule; every up has a genuinely-reversing down
  - `hourglass-vault/decisions/project/ADR-P-007 — Activity Ontology.md` — D-2 (kind is a free catalog label, no kind↔depth constraint) and D-6 (big-bang, pre-deploy MVP seed scope)
  - `.planning/phases/09-activity-ontology/09-SPEC.md` — acceptance #4 ("subprojects → activities with kind='phase'") and #6
  </read_first>
  <action>
  Create `migrations/013_activity_kind_phase_fix.up.sql`:
  - Header comment (multi-line `--`) documenting: this is a forward label fix per SPEC acceptance #6 — subproject-derived activities must carry `kind='phase'`, but 011 line 115 hardcoded `'task'`; 011 is applied history and immutable per ADR-BE-004, so the correction lands as a new migration; the predicate targets ONLY subproject-derived rows (all subproject children have a parent; the only other 'task'-kind rows would be user-created, and none exist in the pre-deploy MVP seed scope); expected post-fix distribution for the MVP seed: engagement 6, phase 6, task 0, internal 1 (13 activities total).
  - Body: exactly one statement — `UPDATE activities SET kind = 'phase' WHERE kind = 'task' AND parent_id IS NOT NULL;`
  - Do NOT touch migration 011 or 012 or 000_full_schema — they are immutable.

  Create `migrations/013_activity_kind_phase_fix.down.sql`:
  - Header comment documenting: reverses the label fix for the same row set (parented activities carrying 'phase' → 'task'); best-effort per ADR-BE-004 — a user-created 'phase'-kind child created between up and down is also reverted (acceptable: pre-deploy seed scope, D-6).
  - Body: exactly one statement — `UPDATE activities SET kind = 'task' WHERE kind = 'phase' AND parent_id IS NOT NULL;`
  </action>
  <verify>
  <automated>grep -c "UPDATE activities SET kind = 'phase' WHERE kind = 'task' AND parent_id IS NOT NULL" migrations/013_activity_kind_phase_fix.up.sql; grep -c "UPDATE activities SET kind = 'task' WHERE kind = 'phase' AND parent_id IS NOT NULL" migrations/013_activity_kind_phase_fix.down.sql</automated>
  </verify>
  <acceptance_criteria>
  - Both files exist with exactly the predicates above (grep returns 1 for each)
  - 011/012/000_full_schema are byte-identical to HEAD (no edits — `git diff --stat migrations/011_activity_ontology.up.sql` empty)
  - Both headers explain the immutability rationale and the down's best-effort lossiness
  </acceptance_criteria>
  <done>013 up/down exist, predicates exact, 011/012 untouched</done>
</task>

<task type="auto">
  <name>Task 2: Update migration tests for 013 (applyMigrations skip + cycle assertions)</name>
  <files>internal/adapters/secondary/postgres/activity_ontology_migration_test.go, internal/adapters/secondary/postgres/staffing_schema_migration_test.go</files>
  <read_first>
  - `internal/adapters/secondary/postgres/activity_ontology_migration_test.go` — applyMigrations (lines 24-46), pre-state call (line 105), kind assertions (lines 142-147), down section (lines 167-208), final up pass (lines 210-217)
  - `internal/adapters/secondary/postgres/staffing_schema_migration_test.go` — its applyMigrations call site (line 31)
  - `migrations/013_activity_kind_phase_fix.up.sql` / `.down.sql` (written in Task 1)
  </read_first>
  <action>
  Update `activity_ontology_migration_test.go`:
  - Change the `applyMigrations` signature from `(t *testing.T, pool *pgxpool.Pool, skip string, withSeed bool)` to variadic-last: `(t *testing.T, pool *pgxpool.Pool, withSeed bool, skip ...string)`; inside the loop, skip when `slices.Contains(skip, base)` (import `slices`). Both existing call sites change to the new argument order.
  - In `TestMigration011_ActivityOntology_UpDownUpCycle`: read `up013 := readMigration(t, "013_activity_kind_phase_fix.up.sql")` and `down013 := readMigration(t, "013_activity_kind_phase_fix.down.sql")` next to the 011 reads.
  - Pre-state call (line 105) becomes: `applyMigrations(t, pool, true, "011_activity_ontology.up.sql", "013_activity_kind_phase_fix.up.sql")` — 013 MUST be skipped in pre-state because it UPDATEs the `activities` table that only exists after 011; applied before 011 it fails with SQLSTATE 42P01.
  - After the first `pool.Exec(ctx, up011)`: execute `pool.Exec(ctx, up013)`; then change the line-146 assertion from `kind = 'task'` count 6 to `kind = 'phase'` count 6 and ADD `kind = 'task'` count 0; update the comment at lines 142-143 to "6 projects (engagement) + 6 subprojects (phase after 013) + 1 internal fallback".
  - Before the `pool.Exec(ctx, down011)` down pass: execute `pool.Exec(ctx, down013)` first, and optionally assert `kind = 'task'` count 6 is restored (proves 013 down reverses exactly).
  - In the final "UP again (cycle)" pass: after `pool.Exec(ctx, up011)`, execute `pool.Exec(ctx, up013)` and assert `kind = 'phase'` count 6 and `kind = 'task'` count 0 again.
  - Keep the total-count and zero-orphan assertions unchanged (13 activities, 0 orphaned entries).

  Update `staffing_schema_migration_test.go`:
  - Its call site (line 31) becomes `applyMigrations(t, pool, true, "012_staffing_schema.up.sql")` (new variadic signature; 013 applies in sorted order AFTER 011 in this test, so it is not skipped — the flip is a no-op for its assertions).
  </action>
  <verify>
  <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration011_ActivityOntology_UpDownUpCycle|TestMigration012_StaffingSchema_UpDownUpCycle' -v</automated>
  </verify>
  <acceptance_criteria>
  - Both migration cycle tests pass: pre-state applies without 42P01; up011→up013 → down013→down011 → up011→up013 cycle is clean
  - The corrected distribution (phase 6 / task 0) is asserted in all three UP states
  - down013 provably reverses (phase → task) before down011 runs
  - `go test ./internal/adapters/secondary/postgres/ -count=1` (whole package) passes
  </acceptance_criteria>
  <done>Migration cycle tests green with 013 in the cycle; assertions reflect phase=6, task=0</done>
</task>

</tasks>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -count=1` — whole postgres package green (activity repo, WG repo, migration cycles)
- Phase gate (meaningful once plans 09-06+09-07+09-08 have all landed, since they run in parallel wave 3): `go test ./... -count=1` → 19/19 packages PASS
- `git diff --stat -- migrations/011_activity_ontology.up.sql migrations/012_staffing_schema.up.sql` — empty (immutability preserved)
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrations/ → database | Migration SQL executes with full schema privileges; a bad UPDATE corrupts seed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering / Data integrity | 013 up UPDATE predicate | mitigate | Predicate is scoped to `kind='task' AND parent_id IS NOT NULL` — root rows and non-task kinds are untouched; the cycle test asserts the exact distribution (engagement 6 / phase 6 / task 0 / internal 1) before and after, and down013 reverses the same row set before down011 runs |
| T-09-06-02 | Tampering | migrations/011, 012 files | mitigate | Applied history is immutable per ADR-BE-004 — verification greps git diff for 011/012 and fails if they changed; 013's header documents the immutability rule |
| T-09-06-03 | Spoofing | applyMigrations helper | accept | Test-only helper; signature change is additive (variadic skip) and both call sites are updated in the same task |

No high-severity threats.
</threat_model>

<success_criteria>
- VERIFICATION truth #10 green: subproject-derived activities carry `kind='phase'` after 013
- Truth #8 stays green: up→down→up cycle clean with 013 interleaved
- Truth #14 remains achievable: no migration-test regression; postgres package green
- 011/012 byte-identical (immutable)
</success_criteria>

<output>
Create `.planning/phases/09-activity-ontology/09-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `migrations/013_activity_kind_phase_fix.up.sql` — forward UPDATE: `kind='task' AND parent_id IS NOT NULL` → `'phase'` (subproject-derived rows only)
- `migrations/013_activity_kind_phase_fix.down.sql` — reverse UPDATE: `kind='phase' AND parent_id IS NOT NULL` → `'task'`
- `applyMigrations` helper — signature changed to `(t, pool, withSeed bool, skip ...string)` (variadic skip, `slices.Contains`)
- `TestMigration011_ActivityOntology_UpDownUpCycle` — now interleaves 013 up/down into the cycle; asserts `kind='phase'` count 6 and `kind='task'` count 0
- `TestMigration012_StaffingSchema_UpDownUpCycle` — call site updated to the new variadic signature (no assertion changes)
