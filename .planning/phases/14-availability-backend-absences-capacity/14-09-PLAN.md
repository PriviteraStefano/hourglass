---
phase: 14-availability-backend-absences-capacity
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/services/availability/availability.go
  - internal/core/services/availability/availability_test.go
  - internal/adapters/secondary/postgres/availability_repository.go
  - internal/adapters/secondary/postgres/availability_repository_test.go
  - internal/core/domain/availability/availability.go
  - internal/core/domain/availability/availability_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: true
estimate:
  tokens: 16000
  raw_tokens: 13000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "HR medical curation preserves certificate_ref: absent fields keep the current values (D-14-05/D-14-11) — a date-only medical edit must NOT null certificate_ref"
    - "Employee can declare a legal whole-cent partial-day window (0.29h, 2.30h) via the API (AVAIL-01) — valid DECIMAL(4,2) values must not 400"
    - "Service.UpdateMedical hands the repo a window whose CertificateRef equals the current row value on a date-only edit; an empty-string certificate_ref is rejected with ErrCertificateRequired before the repo call (Declare parity)"
    - "The repo refuses to persist a NULL or empty certificate_ref on a medical window in-tx (ErrCertificateRequired, no UPDATE, no audit row) — the D-14-05 invariant is structural at the DB boundary"
    - "WindowHoursValid(0.29) == true, WindowHoursValid(1.15) == true, WindowHoursValid(2.30) == true while WindowHoursValid(4.005) == false and WindowHoursValid(99.995) == false"
  artifacts:
    - internal/core/services/availability/availability.go (UpdateMedical copy + empty-ref rejection)
    - internal/adapters/secondary/postgres/availability_repository.go (in-tx certificate_ref guard)
    - internal/core/domain/availability/availability.go (WindowHoursValid epsilon)
  key_links:
    - "UpdateMedical service branch ↔ the repo UPDATE: the service always passes the current ref so the unconditional $3 write can never NULL it"
    - "Empty-string rejection ↔ Declare's existing certificate_ref fast-fail (availability.go:106) — one invariant on both paths (D-14-05)"
    - "WindowHoursValid ↔ the declare service fast-fail: the shared exported helper (14-03) fixes both the domain and the API path"
flagged_assumptions:
  - statement: "AVAIL-01 adjacency probe (specless fallback, covered by executed plans): exactly-touching date ranges do NOT collide — inclusive-intersection overlap semantics with the adjacency-200 edge, pinned by 14-03 (D-14-14) and VERIFIED (truth #4). Surfaced here, not re-planned."
    status: covered
    flagged: true
  - statement: "AVAIL-01 empty/null-input probe (specless fallback, covered by executed plans): empty body / missing dates / zero hours 400 via the 14-03 declare validation fast-fails (VERIFIED truth #4); invalid employees excluded by the 14-07 D-14-22 validity split (VERIFIED truth #7 battery). Surfaced here, not re-planned."
    status: covered
    flagged: true
  - statement: "AVAIL-01 ordering probe (specless fallback, covered by executed plans): equal-key window ordering is stable — deterministic (user_id, starts_on, created_at) + id tiebreaker in the 14-07 Windows read (VERIFIED truth #8). Surfaced here, not re-planned."
    status: covered
    flagged: true
  - statement: "AVAIL-02 unclassified probe (specless fallback, covered by executed plans): 'Manager/HR can confirm' is pinned by locked D-14-03 as manager-confirms + HR-curates; the hr-not-manager confirm 403 is asserted in the 14-05 service and 14-08 handler batteries (VERIFIED truth #5). Surfaced here, not re-planned."
    status: covered
    flagged: true
---

<objective>
Close the two lifecycle data-integrity gaps from 14-VERIFICATION.md: **CR-01 (critical, data loss)** — a date-only HR medical edit must preserve the current `certificate_ref` (absent fields keep current values, D-14-05/D-14-11) and an empty-string ref must be rejected on the curation path; **WR-01** — `WindowHoursValid` must accept legal whole-cent partial-day hours (0.29h, 2.30h) that fail float equality.

Purpose: CR-01 is the phase goal's core HR-curation path silently corrupting medical records (SQL NULL persisted on every date-only correction, and the `{before, after}` audit records the loss); WR-01 rejects valid DECIMAL(4,2) declares with 400 (AVAIL-01 edge). Both fixes are small, well-specified, and regression-tested at the layer that owns the invariant.

Output: service + repo + domain fixes with RED→GREEN regression tests; every existing battery stays green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-availability-backend-absences-capacity/14-VERIFICATION.md
@.planning/phases/14-availability-backend-absences-capacity/14-REVIEW.md
@.planning/phases/14-availability-backend-absences-capacity/14-05-SUMMARY.md
@.planning/phases/14-availability-backend-absences-capacity/14-07-SUMMARY.md
@internal/core/services/availability/availability.go
@internal/adapters/secondary/postgres/availability_repository.go
@internal/core/domain/availability/availability.go
@internal/core/services/testdata/mock_availability_repo.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service UpdateMedical — copy the current CertificateRef + reject empty refs (CR-01 service half)</name>
  <files>internal/core/services/availability/availability.go, internal/core/services/availability/availability_test.go</files>
  <read_first>internal/core/services/availability/availability.go (UpdateMedical 302-355 — the defect: `updated := &Window{StartsOn, EndsOn}` at 331-334 skips CertificateRef; the empty-string gap at 342-344; the Declare fast-fail at 106 that rejects empty refs), internal/core/services/availability/availability_test.go (TestService_HrGates 410-474 — the existing gate battery + the seedWindow/ptrStr/day fixtures), internal/core/services/testdata/mock_availability_repo.go (UpdateMedical 174-196 — the mock does not overwrite cur.CertificateRef when the passed window's ref is nil, so the assertion must capture the window argument via UpdateMedicalFn)</read_first>
  <action>
    RED: add a new test function TestService_UpdateMedical_PreservesCertificateRef to availability_test.go with two subtests:
    (a) "date-only edit preserves certificate_ref" — seed a medical window via f.seedWindow(orgID, employeeID, availabilitydomain.KindMedical, availabilitydomain.StatusConfirmed); read the seedWindow fixture first — if it does not set a CertificateRef, set the WindowMap entry's ref directly (e.g. w.CertificateRef = ptrStr("INPS-11111") and re-store the entry) so the service's getWindow resolves a ref-bearing window; override f.availRepo.UpdateMedicalFn with a closure that captures the *Window argument and returns it; call f.svc.UpdateMedical(ctx, orgID, hrID, string(models.RoleHR), w.ID, &UpdateMedicalRequest{StartsOn: day(2026, 8, 11), EndsOn: day(2026, 8, 13)}) — NO CertificateRef in the request; assert the captured window's CertificateRef is non-nil and equals "INPS-11111" and its StartsOn/EndsOn equal the request values.
    (b) "empty-string certificate_ref is rejected" — same seed; req.CertificateRef = ptrStr(""); call f.svc.UpdateMedical; assert errors.Is(err, availabilitydomain.ErrCertificateRequired) and that UpdateMedicalFn was never invoked (flag set inside the closure) and f.availRepo.Audits is empty.
    Run — RED: (a) fails because the service passes a window with a nil CertificateRef (capture shows nil); (b) fails because today an empty ref is assigned, not rejected.
    GREEN: in UpdateMedical (availability.go 331-344): initialize the updated window with CertificateRef: w.CertificateRef (absent field keeps the current value — D-14-05/D-14-11); in the `if req.CertificateRef != nil` branch, before assigning, reject an empty string with `return nil, availabilitydomain.ErrCertificateRequired` (Declare parity with availability.go:106). No other behavior change — the date-range branch (335-341) stays untouched.
  </action>
  <behavior>
    - Test 1: date-only UpdateMedicalRequest (no CertificateRef) → the window handed to the repo carries the current seeded ref verbatim.
    - Test 2: UpdateMedicalRequest{CertificateRef: ptrStr("")} → ErrCertificateRequired, zero repo calls, zero audit rows.
  </behavior>
  <verify>
    <automated>go test ./internal/core/services/availability/ -run 'TestService_UpdateMedical' -count=1 && go test ./internal/core/services/availability/ -count=1</automated>
  </verify>
  <acceptance_criteria>TestService_UpdateMedical_PreservesCertificateRef passes; the existing TestService_HrGates battery (incl. the dates+cert edit at 446-460) still passes; the captured window in subtest (a) carries the seeded ref; subtest (b) asserts ErrCertificateRequired with the repo never called.</acceptance_criteria>
  <done>The service boundary preserves certificate_ref on date-only edits and rejects empty refs with the 400-path sentinel — CR-01's service half closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repo UpdateMedical — in-tx certificate_ref guard (CR-01 repo half, belt-and-braces)</name>
  <files>internal/adapters/secondary/postgres/availability_repository.go, internal/adapters/secondary/postgres/availability_repository_test.go</files>
  <read_first>internal/adapters/secondary/postgres/availability_repository.go (UpdateMedical 391-430 — the unconditional `UPDATE ... SET starts_on = $1, ends_on = $2, certificate_ref = $3` at 421-424 that writes NULL when the window's ref is nil), internal/adapters/secondary/postgres/availability_repository_test.go (TestAvailabilityRepository_UpdateMedical 544-610 — existing medical/non-medical/missing-id subtests + the seedAvailabilityWindowWithCert and windowAudit helper shapes)</read_first>
  <action>
    RED: extend TestAvailabilityRepository_UpdateMedical with two subtests:
    (a) "date-only edit preserves certificate_ref through the UPDATE" — seed a medical window with cert "INPS-11111" (seedAvailabilityWindowWithCert with the &cert arg, "confirmed" status); updated := &availabilitydomain.Window{StartsOn: day(2026, 9, 2), EndsOn: day(2026, 9, 4), CertificateRef: &cert} (the fixed-service contract: the passed window always carries the current value); repo.UpdateMedical with windowAudit(orgID, windowID, userID, AuditActionEdited, nil, now); assert the returned window's CertificateRef equals "INPS-11111" AND re-read the row with `SELECT certificate_ref FROM availability_windows WHERE id = $1` asserting it still equals "INPS-11111" (this is the exact row the unconditional $3 write used to NULL).
    (b) "nil certificate_ref on a medical window is refused in-tx" — same seed; updated WITHOUT CertificateRef; expect errors.Is(err, availabilitydomain.ErrCertificateRequired); then assert the row's certificate_ref is unchanged (still "INPS-11111") and no 'edited' audit row exists for the window (`SELECT count(*) FROM audit_logs WHERE entity_type = 'availability_window' AND entity_id = $1 AND action = 'edited'` == 0).
    Run — (a) is green today (the service-side Task 1 fix makes this contract hold), (b) is RED (today the NULL write succeeds silently).
    GREEN: in UpdateMedical, immediately after the in-tx medical-only kind check (availability_repository.go 416-419), add the D-14-05 guard: if w.CertificateRef == nil || *w.CertificateRef == "" → return nil, availabilitydomain.ErrCertificateRequired (before the UPDATE and before the audit insert — a refused edit leaves no trace). Extend the method doc comment (391-395) with one line: the certificate_ref invariant is enforced in-tx (D-14-05), so a caller can never NULL the medical ref.
  </action>
  <behavior>
    - Test 1: date-only medical edit round-trips the current certificate_ref through the SQL UPDATE.
    - Test 2: nil certificate_ref on a medical window → ErrCertificateRequired, row unchanged, no audit row.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1</automated>
  </verify>
  <acceptance_criteria>Both new subtests pass; the pre-existing UpdateMedical subtests (medical edit with new cert, non-medical in-tx check, missing id) still pass; the DB row assertion in (a) proves the ref survives the UPDATE; (b) proves the guard fires before any write.</acceptance_criteria>
  <done>The repo enforces the certificate_ref invariant in-tx — CR-01's repo half closed; the medical record can no longer be NULLed by any caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Domain WindowHoursValid — epsilon tolerance for legal cent values (WR-01)</name>
  <files>internal/core/domain/availability/availability.go, internal/core/domain/availability/availability_test.go</files>
  <read_first>internal/core/domain/availability/availability.go (202-209 — `math.Round(h*100) == h*100` rejects 0.29/1.15/2.30 whose binary scaling is inexact), internal/core/domain/availability/availability_test.go (TestWindowHoursValid 91-101 — the valid/invalid lists)</read_first>
  <action>
    RED: in TestWindowHoursValid, add 0.29, 1.15, 2.30 to the valid list (legal DECIMAL(4,2) cent values — the exact values the review verified as rejected); keep the invalid list unchanged (0, -1, -0.5, 100, 99.995, 4.005, 100.00). Run — RED: the three cent values fail today on float equality.
    GREEN: replace the equality in WindowHoursValid (availability.go 208) with the tolerance form: `return h > 0 && math.Abs(math.Round(h*100)-h*100) < 1e-9 && h <= 99.99` — same shape as the review's supplied fix; update the doc comment (202-206) to state the epsilon tolerance instead of exact equality. The ceiling (99.99) and positivity bounds are unchanged, so 4.005/99.995 stay invalid.
  </action>
  <behavior>
    - Test 1: WindowHoursValid(0.29) == true, WindowHoursValid(1.15) == true, WindowHoursValid(2.30) == true.
    - Test 2: WindowHoursValid(4.005) == false, WindowHoursValid(99.995) == false, WindowHoursValid(100) == false, WindowHoursValid(0) == false.
  </behavior>
  <verify>
    <automated>go test ./internal/core/domain/availability/ -count=1 && go test ./internal/core/services/availability/ -count=1</automated>
  </verify>
  <acceptance_criteria>TestWindowHoursValid passes with the three legal cent values in the valid list; the sub-cent ceiling cases stay invalid; the services package stays green — the declare fast-fail (which consumes the same exported helper) still 400s 4.005 and now accepts 2.30.</acceptance_criteria>
  <done>Legal whole-cent partial-day hours pass WindowHoursValid — WR-01 closed at the single source of truth shared by the declare path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | HR medical-edit requests cross here — absent fields must mean "keep current value", never "clear" (D-14-05/D-14-11) |
| service→repo | the update window crosses here — the service must hand the repo a ref-bearing window on date-only edits |
| handler→service→repo | declare hours cross here — the domain helper gates legal DECIMAL(4,2) values before PG |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-26 | Tampering | UpdateMedical date-only edit nulls certificate_ref | critical | mitigate | Service copies w.CertificateRef into the update (Task 1); repo in-tx guard refuses nil/empty refs (Task 2); regression tests at both layers prove the row keeps the ref |
| T-14g-27 | Tampering | empty-string certificate_ref bypass on the curation path | high | mitigate | ErrCertificateRequired in UpdateMedical (Task 1, Declare parity at availability.go:106) + repo guard (Task 2) — the D-14-05 invariant holds on every path |
| T-14g-28 | Integrity | float-precision equality rejects legal cent hours (0.29/2.30) | medium | mitigate | epsilon tolerance in WindowHoursValid (Task 3) + domain regression; the 99.99 ceiling is untouched |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/core/services/availability/ -count=1` — service preserve/empty-ref regressions + the full authority battery
- `go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1` — repo guard + round-trip subtests
- `go test ./internal/core/domain/availability/ -count=1` — WindowHoursValid epsilon cases
- `go build ./...` — repo-wide compile
</verification>

<success_criteria>
- Date-only HR medical edits keep the current certificate_ref at the service boundary, through the SQL UPDATE, and in the persisted row
- Empty-string certificate_ref is rejected on the curation path with ErrCertificateRequired (400-path) at both service and repo layers
- WindowHoursValid accepts 0.29/1.15/2.30 and still rejects sub-cent values; the declare API accepts legal partial-day hours
- All existing batteries green (no regression on the 14-03 declare, 14-05 lifecycle, 14-07 read-model suites)
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified files:**
- `internal/core/services/availability/availability.go` — UpdateMedical: copies `w.CertificateRef` into the update window; rejects empty-string refs with `ErrCertificateRequired`
- `internal/core/services/availability/availability_test.go` — +`TestService_UpdateMedical_PreservesCertificateRef` (capture-based subtests: date-only preserves, empty-ref rejected)
- `internal/adapters/secondary/postgres/availability_repository.go` — UpdateMedical: in-tx nil/empty certificate_ref guard returning `ErrCertificateRequired` before UPDATE/audit; doc comment extended
- `internal/adapters/secondary/postgres/availability_repository_test.go` — +2 subtests on `TestAvailabilityRepository_UpdateMedical` (date-only round-trip via `SELECT certificate_ref`, nil-ref refusal with unchanged row + zero audit rows)
- `internal/core/domain/availability/availability.go` — `WindowHoursValid`: epsilon tolerance `< 1e-9` replacing exact float equality; doc comment updated
- `internal/core/domain/availability/availability_test.go` — `TestWindowHoursValid` valid list gains 0.29, 1.15, 2.30

No new exported symbols; no schema/migration changes.
