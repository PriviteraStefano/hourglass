---
phase: 14-availability-backend-absences-capacity
plan: 07
type: execute
wave: 4
depends_on: [14-03, 14-05, 14-06]
files_modified:
  - internal/adapters/secondary/postgres/availability_read_models.go
  - internal/adapters/secondary/postgres/availability_repository_test.go
  - internal/core/services/availability/read_models.go
  - internal/core/services/availability/read_models_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: false
estimate:
  tokens: 22000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GET /availability/windows read-model is org-wide: any authenticated org member sees any employee's windows — kind, dates, status including declared (D-14-24); deterministic ordering (user_id, starts_on, created_at — the ordering edge: equal keys are stable); filters user_id/kind/status/period with limit/offset pagination."
    - "Medical privacy filtering (D-14-24) is server-side: certificate_ref + attachment metadata appear ONLY for hr and the window owner's resolved unit manager; other members see the medical kind label but never the ref or documents (the privacy carve-out)."
    - "Capacity: GET /availability/capacity with scope=activity|wg|unit|org (+ scope_id) and required period_start/period_end bounds (parsePeriod, malformed → 400); per-employee weekly hours per the resolved schedule, minus confirmed-only absences (partial-day subtracts hours, full-day zeroes the day, floored at 0 — D-14-23), plus workload = Σ submitted+approved time entries on the activity subtree grouped per employee (D-14-19, literal status pin); declared windows surface as an advisory field, never subtracted (D-14-21)."
    - "Employment validity filters capacity: employees outside valid_from/valid_until are EXCLUDED from capacity responses entirely (D-14-22 — parity with D-13-31)."
    - "Scope resolution: activity → employees with entries on the subtree (ActivityWorkloadEmployees); wg → members; unit → unit + descendant members; org → org members (D-14-20)."
    - "The schedule resolution level (override|type|org_default|fallback) is documented in the capacity response (D-14-18)."
  artifacts:
    - internal/adapters/secondary/postgres/availability_read_models.go (Windows + Capacity + workload CTE)
    - internal/core/services/availability/read_models.go (scope resolution, validity split, field filtering, advisory assembly)
  key_links:
    - "Capacity absence subtraction ↔ confirmed-only closure (14-04) — same fact set, same predicate"
    - "Capacity base hours ↔ ResolveSchedule (14-06) — the service passes per-employee per-day schedule hours to the repo"
    - "Windows read filtering ↔ ResolveUnitManager (14-05) — the unit-manager visibility scope"
flagged_assumptions:
  - statement: "Workload literal pin (research OQ1/A2, D-14-19): Σ counts ONLY statuses ('submitted','approved') — entries in pending_manager/pending_finance do NOT count. Surfaced as a flagged assumption; the integration battery asserts the literal set."
    status: unverified
    flagged: true
---

<objective>
The read-models: the org-wide windows read with D-14-24 medical field filtering, and the capacity read-model — weekly hours per employee per resolved schedule, minus confirmed absences (partial/full semantics), plus workload from submitted+approved entries on the activity subtree, per scope (activity/wg/unit/org).

Purpose: supports AVAIL-04 (Phase 16 UI capacity grid) and completes the D-14-21 confirmed-only fact set (the same semantics the 14-04 closure applied to the direction path). The repo does the set-based math (generate_series + recursive CTE, the Coverage shape); the service owns scope resolution, the validity split, schedule expansion, and the privacy filtering.

Output: repo Windows + Capacity queries, service scope/filter/assembly, read-model + service batteries, all green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@internal/adapters/secondary/postgres/direction_repository.go
@internal/core/services/direction/direction.go
@internal/adapters/secondary/postgres/availability_repository.go
@internal/core/services/availability/availability.go
@internal/core/ports/unit_repository.go
@internal/core/ports/working_group_repository.go
@internal/core/ports/organization_management_repository.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repo Windows read + Capacity math (generate_series + workload CTE)</name>
  <files>internal/adapters/secondary/postgres/availability_read_models.go, internal/adapters/secondary/postgres/availability_repository_test.go</files>
  <read_first>internal/adapters/secondary/postgres/direction_repository.go (Coverage read-model 744-803 — unnest × generate_series, partial_abs/full_abs joins, GREATEST floor, normalizeDay 723-725, roundCents 792-796; terminalActivitySubtree CTE 547-566 — the workload shape to re-anchor), internal/core/services/availability/read_models.go does NOT exist yet — the repo port contract is in internal/core/ports/availability_repository.go (14-02): Capacity(ctx, orgID, employeeIDs, schedules []availability.EmployeeSchedule, periodStart, periodEnd)</read_first>
  <action>
    RED: repo batteries (availability_repository_test.go): Windows — seeded windows returned org-wide ordered by user_id, starts_on, created_at; filters user_id/kind/status/period apply; normalizeDay UTC-midnight on scanned DATEs (the Phase 13 lesson). Capacity — per-employee rows: schedule hours minus confirmed partial (hours subtracted) and full (day zeroed) absences, GREATEST floor at 0; declared windows NOT subtracted; workload Σ per employee from seeded submitted+approved entries on the subtree (seeded draft/pending entries excluded — the A2 literal pin); employees with no windows/workload still appear with their schedule hours. Run — RED (read-model methods absent).
    GREEN: availability_read_models.go — methods on *AvailabilityRepository:
    1. Windows(ctx, orgID, filter WindowsFilter) — org-wide SELECT with optional user_id/kind/status/date-range predicates, ORDER BY user_id, starts_on, created_at, LIMIT/OFFSET; normalizeDay on scanned dates.
    2. Capacity(ctx, orgID, employeeIDs, schedules, periodStart, periodEnd) — unnest($2::uuid[]) × generate_series($3::date,$4::date) day expansion; per-day base hours from the passed schedules table (map employee → day-key → hours; the service expands the schedule, D-14-17 weekly/monthly derivation); LEFT JOIN partial_abs (confirmed + hours IS NOT NULL — subtract SUM(aw.hours) per day) and full_abs (confirmed + hours IS NULL — zero the day), both over generate_series per window (the Coverage shape with status = 'confirmed' per D-14-21); capacity = GREATEST(base − COALESCE(partial,0), 0) when not full-absent; ONE WITH RECURSIVE subtree CTE for the workload (status IN ('submitted','approved'), is_deleted = false, grouped per user — research Pitfall 3: never a correlated per-row CTE), LEFT JOINed per employee; declared-windows advisory subquery (status = 'declared', aggregated per employee — not subtracted); roundCents on rendered values. Returns per-employee aggregated rows (capacity, absence hours, workload, declared hours) + per-day detail where the service needs it — keep the aggregate shape per D-14-20 weekly basis.
  </action>
  <behavior>
    - Test 1: windows read org-wide, ordered, filtered; dates normalize to UTC midnight.
    - Test 2: capacity subtracts confirmed partial hours / zeroes full days; floor at 0; declared not subtracted.
    - Test 3: workload Σ = submitted+approved only (pending/draft/rejected excluded); subtree recursion depth ≥ 2 covered by a seeded nested activity.
    - Test 4: employee with no absences/workload still appears with full schedule hours.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1</automated>
  </verify>
  <acceptance_criteria>Windows + Capacity batteries green; confirmed-only subtraction, advisory, workload literal set, and floor-at-0 all asserted; one subtree CTE (no per-row recursion).</acceptance_criteria>
  <done>Repo read-models shipped: org-wide windows read + per-employee capacity math with confirmed-only semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Service — scope resolution, validity split, schedule expansion, field filtering</name>
  <files>internal/core/services/availability/read_models.go</files>
  <read_first>internal/core/services/direction/direction.go (scope resolution 666-726 — wgRepo.ListMembers / unitRepo.GetDescendants+ListMembers / orgRepo.ListMembers; membershipValid 777-791 — the D-14-22 filter, boundaries inclusive, nil membership invalid), internal/core/ports/availability_repository.go (14-02 — Capacity signature + ActivityWorkloadEmployees), internal/core/services/availability/contract_types.go (14-06 — ResolveSchedule)</read_first>
  <action>
    Implement the service read-model assembly in read_models.go (methods on the availability Service) — no tdd tag: the RED→GREEN loop is deferred to Task 3 (this task's output is the code those batteries target), so the verify here is compile-only, never a vacuous test run:
    1. ListWindows(ctx, orgID, actorID, role, filter) — repo.Windows, then D-14-24 field filtering: for each window, certificate_ref + attachment metadata (repo.AttachmentsByWindowIDs for the privileged viewer only) are attached when role == models.RoleHR OR the actor is the resolved unit manager of the window's owner (unitRepo.ListMembershipsForUser + routing.ResolveUnitManager — the same authority resolution as 14-05); otherwise those fields are dropped server-side (never client-side). The medical KIND label stays in every response.
    2. Capacity(ctx, orgID, actorRole, scope, scopeID, periodStart, periodEnd) — resolve the employee universe per D-14-20: employee/activity/wg/unit/org (activity → repo.ActivityWorkloadEmployees; wg → wgRepo.ListMembers; unit → unitRepo.GetDescendants + ListMembersByUnitIDs; org → orgRepo.ListMembers); apply the D-14-22 validity split (mirror membershipValid — boundaries inclusive, nil membership excluded — with an attribution comment, D-G parity: do not fork silently); per employee expand the schedule into per-day hours (ResolveSchedule → week matrix repeated per week / month-cadence derived hours ÷ working days, D-14-17); call repo.Capacity; assemble the response: per-employee rows (employee_id, schedule_source level, period_capacity_hours, confirmed_absence_hours, workload_hours, available_hours) + totals + declared_windows advisory list (never subtracted) + scope + period metadata.
    3. Attachment(ctx, orgID, actorID, role, windowID) — the privileged single-document read for the 14-08 GET route (hr or resolved unit manager of the owner; ErrForbidden otherwise).
  </action>
  <verify>
    <automated>go build ./internal/core/services/availability/</automated>
  </verify>
  <acceptance_criteria>Service read-models compile against the port contract; scope universes + validity split + filtering + schedule expansion implemented; unit-tested in Task 3.</acceptance_criteria>
  <done>Service read-model assembly shipped: scope→universe, validity split, schedule expansion, server-side privacy filtering, advisory assembly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service read-model batteries — filtering matrix, scopes, expansion</name>
  <files>internal/core/services/availability/read_models_test.go</files>
  <read_first>internal/core/services/testdata/mock_availability_repo.go (14-02 — SetWindows/Capacity stub patterns per mock_direction_repo.go), internal/core/services/availability/read_models.go (Task 2 output)</read_first>
  <action>
    RED→GREEN (tests first, fix gaps in read_models.go):
    1. Field-filtering matrix: hr sees certificate_ref + attachment metadata; the window owner's resolved unit manager sees them; a manager/employee who is neither → fields absent, kind label present (D-14-24). ResolveUnitManager via the routing fixture (mock unit repo seeding the employee's unit membership).
    2. Scope resolution: wg scope → members only; unit scope → unit + descendant members; org scope → all members; activity scope → ActivityWorkloadEmployees result; unknown scope → ErrInvalidRequest (400-path, the direction handler precedent).
    3. Validity split: employees whose valid_from/valid_until covers the period stay; fully-outside employees are excluded from the repo call (mock call-count assertion — D-14-22); nil membership excluded.
    4. Schedule expansion: week cadence matrix repeats per week; month cadence with matrix = fixed weekday hours; month cadence without matrix = hours_per_period ÷ working days (Mon–Fri calendar, D-14-17); override merge (Mon 6h over an 8h type); fallback 8×5.
    5. Advisory: declared windows appear in the advisory list with kind/dates/hours; capacity rows unchanged (D-14-21).
  </action>
  <behavior>
    - Test 1: filtering matrix — hr + unit manager see refs/docs; others see kind only.
    - Test 2: scope universes per scope type; unknown → ErrInvalidRequest.
    - Test 3: validity-excluded employees never reach the repo (mock call-count); boundaries inclusive.
    - Test 4: schedule expansion per cadence/override/fallback (table-driven).
    - Test 5: declared advisory present, not subtracted.
  </behavior>
  <verify>
    <automated>go test ./internal/core/services/availability/ -run TestReadModels -count=1</automated>
  </verify>
  <acceptance_criteria>All five batteries green against the mock; filtering and scope behaviors proven without a DB.</acceptance_criteria>
  <done>Read-model service behavior fully unit-tested: privacy matrix, scope universes, validity split, schedule expansion, advisory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB→service | window rows (incl. medical certificate_ref) cross here — filtering must happen server-side, never in the client |
| service→routing | the unit-manager visibility scope resolves here — a wrong resolution leaks medical data |
| client→API | scope/period query params cross here — malformed input must 400, never 500 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-05 | Information Disclosure | medical certificate_ref + docs org-wide | critical | mitigate | Server-side field filtering at the service layer (Task 2) + the filtering-matrix battery (Task 3) — D-14-24; never client-side |
| T-14g-21 | Tampering | capacity subtracting declared (unconfirmed) absences | high | mitigate | Confirmed-only predicates in the repo math (Task 1) + advisory-only declared subquery (D-14-21) |
| T-14g-22 | Tampering | invalid employees counted (validity) | high | mitigate | Service validity split before the repo call (Task 2, D-14-22) + call-count battery (Task 3) |
| T-14g-23 | Denial of Service | capacity query blowup (employees × days × subtree) | medium | mitigate | One subtree CTE per scope, grouped, joined (Task 1, Pitfall 3) + required bounded period (parsePeriod, 14-08) |
| T-14g-24 | Tampering | malformed scope/period → 500 | medium | mitigate | Scope vocabulary validation + period bounds parse → 400 (Task 3; handler in 14-08) |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1` — Windows/Capacity batteries green
- `go test ./internal/core/services/availability/ -run TestReadModels -count=1` — service batteries green
- `go build ./...` — repo-wide compile
</verification>

<success_criteria>
- Org-wide windows read with server-side medical filtering and deterministic ordering
- Capacity read-model: schedule basis, confirmed-only subtraction, declared advisory, validity exclusion, workload CTE — per scope
- Resolution level documented in the response
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `internal/adapters/secondary/postgres/availability_read_models.go` — Windows (filters + ordering + normalizeDay), Capacity (generate_series day expansion + confirmed-only partial/full joins + GREATEST floor + workload subtree CTE + declared advisory subquery), roundCents rendering
- `internal/core/services/availability/read_models.go` — ListWindows (D-14-24 field filtering), Capacity (scope→universe + validity split + schedule expansion + assembly), Attachment (privileged single-doc read)
- `internal/core/services/availability/read_models_test.go` — filtering matrix, scope universes, validity split, schedule expansion, advisory batteries

**Modified files:**
- `internal/adapters/secondary/postgres/availability_repository_test.go` — Windows/Capacity integration batteries
