---
phase: 14-availability-backend-absences-capacity
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/core/services/availability/contract_types.go
  - internal/core/services/availability/contract_types_test.go
  - internal/adapters/primary/http/contract_types_handler_test.go
  - internal/core/ports/errors.go
  - internal/adapters/secondary/postgres/postgres.go
  - internal/adapters/secondary/postgres/postgres_test.go
  - internal/adapters/primary/http/availability_handler.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: true
estimate:
  tokens: 14000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Never 500 for client input: the full /availability surface maps client errors to 4xx (T-14g-03/18/25)"
    - "POST /availability/contract-types with hours_per_period 1000 → 400 (not 500): validateContractType rejects values above the DECIMAL(5,2) ceiling 999.99 with ErrInvalidRequest; the boundary value 999.99 is accepted"
    - "SQLSTATE 22003 (numeric_value_out_of_range) maps to a 4xx sentinel in wrapPGError (ports.ErrInvalidRequest), and the availability handler's writeError maps that sentinel to 400 — belt-and-braces for any numeric-overflow path on the surface"
  artifacts:
    - internal/core/services/availability/contract_types.go (validateContractType ceiling)
    - internal/core/ports/errors.go (ports.ErrInvalidRequest)
    - internal/adapters/secondary/postgres/postgres.go (wrapPGError 22003 case)
    - internal/adapters/primary/http/availability_handler.go (writeError ports.ErrInvalidRequest → 400)
  key_links:
    - "validateContractType ↔ migration 024 DECIMAL(5,2): the service ceiling mirrors the column's max exactly (999.99), so client input 400s before PG can overflow"
    - "wrapPGError 22003 ↔ the availability handler writeError: the sentinel produced by the adapter must land on the 400 branch"
---

<objective>
Close **WR-03** from 14-VERIFICATION.md: never 500 for client input on the /availability surface. Today `validateContractType` checks only `HoursPerPeriod > 0` while the column is `DECIMAL(5,2)` (max 999.99), so `hours_per_period: 1000` fails in PG with numeric overflow (SQLSTATE 22003), which `wrapPGError` does not map → 500 on pure client input.

Purpose: eliminate the last known 500 path on the surface (T-14g-03/18/25) with the service-side ceiling the review specifies, and make the overflow state unreachable-as-500 generically via a 22003 mapping at the adapter boundary.

Output: ceiling validation + regression cases at the service and HTTP layers, plus the ports sentinel + adapter mapping + handler case with unit tests.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-availability-backend-absences-capacity/14-VERIFICATION.md
@.planning/phases/14-availability-backend-absences-capacity/14-REVIEW.md
@.planning/phases/14-availability-backend-absences-capacity/14-06-SUMMARY.md
@internal/core/services/availability/contract_types.go
@internal/core/services/availability/contract_types_test.go
@internal/adapters/primary/http/contract_types_handler_test.go
@internal/core/ports/errors.go
@internal/adapters/secondary/postgres/postgres.go
@internal/adapters/primary/http/availability_handler.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validateContractType 999.99 ceiling + service/HTTP regressions (WR-03 first half)</name>
  <files>internal/core/services/availability/contract_types.go, internal/core/services/availability/contract_types_test.go, internal/adapters/primary/http/contract_types_handler_test.go</files>
  <read_first>internal/core/services/availability/contract_types.go (validateContractType 37-57 — only the `HoursPerPeriod <= 0` check today; ContractTypeRequest 26-35), internal/core/services/availability/contract_types_test.go (TestContractTypes_Validation 77-110 — the table-driven invalid cases, shape at 86-95), internal/adapters/primary/http/contract_types_handler_test.go (TestContractTypesHandler 124-190 — the CRUD battery with validBody, loginUser/switchToOrg/doJSON fixture discipline), migrations/024_work_schedules.up.sql (the DECIMAL(5,2) hours_per_period column)</read_first>
  <action>
    RED: (a) service — add to the TestContractTypes_Validation table: {"hours above the DECIMAL(5,2) ceiling", &ContractTypeRequest{Name: "x", Cadence: availabilitydomain.CadenceWeek, HoursPerPeriod: 1000, DayHours: map[string]float64{"1": 8}}, availabilitydomain.ErrInvalidRequest}; (b) HTTP — add a subtest to TestContractTypesHandler: login as hr (loginUser + switchToOrg discipline), POST /availability/contract-types with a body identical to validBody but hours_per_period set to 1000, assert the status is 400. Run both — RED: (a) fails because validation accepts 1000 today; (b) fails today with 500 (PG numeric overflow 22003, unwrapped).
    GREEN: in validateContractType (contract_types.go 50-52), after the `HoursPerPeriod <= 0` check, add the ceiling: `if req.HoursPerPeriod > 999.99 { return availabilitydomain.ErrInvalidRequest }` (the migration 024 DECIMAL(5,2) max — the WindowHoursValid 99.99-ceiling precedent applied to the period hours). Also add a boundary-acceptance case to the validation table: {"ceiling boundary accepted", &ContractTypeRequest{Name: "x", Cadence: availabilitydomain.CadenceWeek, HoursPerPeriod: 999.99, DayHours: map[string]float64{"1": 8}}, nil} — nil expectation in the table's success path (read the table's expected-error shape first and follow it).
  </action>
  <behavior>
    - Test 1: validateContractType rejects HoursPerPeriod 1000 with ErrInvalidRequest; accepts 999.99 on an otherwise-valid type.
    - Test 2: POST /availability/contract-types with hours_per_period 1000 → HTTP 400, never 500.
  </behavior>
  <verify>
    <automated>go test ./internal/core/services/availability/ -run TestContractTypes -count=1 && go test ./internal/adapters/primary/http/ -run 'TestContractTypesHandler' -count=1</automated>
  </verify>
  <acceptance_criteria>The two new validation-table cases pass; the HTTP subtest asserts 400; the existing validation cases (zero/negative hours, day-key matrix, empty name) and the full CRUD battery still pass.</acceptance_criteria>
  <done>Client input above the DECIMAL(5,2) ceiling 400s at the service boundary — the known 500 path is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wrapPGError 22003 mapping + writeError case (WR-03 second half, belt-and-braces)</name>
  <files>internal/core/ports/errors.go, internal/adapters/secondary/postgres/postgres.go, internal/adapters/secondary/postgres/postgres_test.go, internal/adapters/primary/http/availability_handler.go</files>
  <read_first>internal/core/ports/errors.go (the ports sentinel family: ErrNotFound/ErrConflict/ErrForeignKey), internal/adapters/secondary/postgres/postgres.go (wrapPGError 16-33 — the 23505/23503 switch, no 22003 case), internal/adapters/primary/http/availability_handler.go (writeError 73-98 — the 400 branch maps availabilitydomain sentinels only; the handler package does not import ports today)</read_first>
  <action>
    RED: create internal/adapters/secondary/postgres/postgres_test.go (package postgres — pure unit test, no TestPool, no testcontainer): TestWrapPGError_22003 — construct &pgconn.PgError{Code: "22003", Message: "numeric value out of range"}; wrapped := wrapPGError(fmt.Errorf("exec: %w", pgErr), "op"); assert errors.Is(wrapped, ports.ErrInvalidRequest). Add a companion assertion in the same test that an unknown code (e.g. 23514) still passes through without mapping to ports.ErrInvalidRequest (the 23514 passthrough precedent, contract_repository_test.go 418-421). Run — RED: today 22003 passes through unmapped.
    GREEN: (a) internal/core/ports/errors.go — add `ErrInvalidRequest = errors.New("invalid request")` to the sentinel var block (additive; the availability handler will map it to 400); (b) postgres.go wrapPGError — add `case "22003": return fmt.Errorf("%s: %w", op, ports.ErrInvalidRequest)` to the switch and extend the doc comment (12-15) with the 22003 → ports.ErrInvalidRequest line; (c) internal/adapters/primary/http/availability_handler.go writeError — add `errors.Is(err, ports.ErrInvalidRequest)` to the 400 case (lines 81-88) and add the ports import to the file's import block; (d) update the writeError doc comment (73-75) to mention the ports sentinel.
    NOTE (flagged, recorded in the summary): this deliberately extends wrapPGError beyond the Phase 13 13-09 scope note ("wrapPGError untouched — the 22003 path was unreachable for client input there"). The verifier mandates the mapping; the effect on unrelated repos (time entries, coverage) is strictly 500→400 on numeric overflow — the house rule's direction — and no existing test asserts a 500 on 22003.
  </action>
  <behavior>
    - Test 1: wrapPGError maps SQLSTATE 22003 to ports.ErrInvalidRequest.
    - Test 2: an unknown SQLSTATE (23514) still passes through unwrapped.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestWrapPGError -count=1 && go test ./internal/adapters/primary/http/ -run 'TestContractTypesHandler|TestAvailabilityHandler' -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>TestWrapPGError_22003 passes without spinning a testcontainer; the handler batteries stay green with the new writeError case; go build clean.</acceptance_criteria>
  <done>Numeric overflow on the /availability surface can no longer surface as 500: the adapter maps 22003 to a 4xx sentinel and the handler maps that sentinel to 400 — WR-03 fully closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | contract-type create/update payloads cross here — hours_per_period is client input that must 400, never 500 (T-14g-18) |
| postgres adapter→service | SQLSTATE 22003 crosses here — an unmapped overflow bubbles up as an internal 500 |
| handler→writeError | the sentinel map is the last line: every mapped client-error sentinel lands on 4xx |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-30 | Denial of Service | hours_per_period ≥ 1000 → PG 22003 → 500 | high | mitigate | validateContractType 999.99 ceiling (Task 1) + HTTP 400 regression — client input 400s before the DB is reached |
| T-14g-31 | Availability | other numeric-overflow paths on the /availability surface | medium | mitigate | wrapPGError 22003 → ports.ErrInvalidRequest + writeError 400 case (Task 2) — the surface can never 500 on numeric overflow |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/core/services/availability/ -run TestContractTypes -count=1` — ceiling table cases
- `go test ./internal/adapters/primary/http/ -run 'TestContractTypesHandler|TestAvailabilityHandler' -count=1` — HTTP 400 regression + full surface battery
- `go test ./internal/adapters/secondary/postgres/ -run TestWrapPGError -count=1` — adapter mapping unit test
- `go build ./...` — repo-wide compile
</verification>

<success_criteria>
- hours_per_period 1000 → 400 at both the service and HTTP layers; 999.99 → accepted
- SQLSTATE 22003 maps to ports.ErrInvalidRequest in wrapPGError; the availability writeError maps it to 400
- The contract-type CRUD battery, the availability handler battery, and the postgres suite stay green
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-11-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified files:**
- `internal/core/services/availability/contract_types.go` — validateContractType: `HoursPerPeriod > 999.99` → `ErrInvalidRequest` ceiling
- `internal/core/services/availability/contract_types_test.go` — TestContractTypes_Validation gains the 1000-reject and 999.99-boundary cases
- `internal/adapters/primary/http/contract_types_handler_test.go` — +subtest: POST /availability/contract-types hours_per_period 1000 → 400
- `internal/core/ports/errors.go` — +`ErrInvalidRequest` sentinel (additive)
- `internal/adapters/secondary/postgres/postgres.go` — wrapPGError +`case "22003"` → ports.ErrInvalidRequest; doc comment extended
- `internal/adapters/secondary/postgres/postgres_test.go` — NEW: `TestWrapPGError_22003` (pure unit, no container: 22003 mapped, unknown code passthrough)
- `internal/adapters/primary/http/availability_handler.go` — writeError 400 branch +`ports.ErrInvalidRequest` case; ports import added

No schema/migration changes.
