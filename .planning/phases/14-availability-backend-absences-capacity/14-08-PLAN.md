---
phase: 14-availability-backend-absences-capacity
plan: 08
type: execute
wave: 5
depends_on: [14-03, 14-05, 14-06, 14-07]
files_modified:
  - internal/adapters/primary/http/availability_handler.go
  - internal/adapters/primary/http/availability_handler_test.go
  - internal/adapters/primary/http/handler_test_helper.go
  - cmd/server/main.go
  - cmd/server/main_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: false
estimate:
  tokens: 24000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The full /availability REST surface is live and auth-gated (D-14-25/26): POST /availability/windows (14-03), POST .../{id}/withdraw (owner, declared-only), POST .../{id}/confirm + POST .../{id}/reject (resolved unit manager, reason required), PUT .../{id} (HR medical edit), POST .../{id}/certificate (hr, medical only, multipart 5 MB cap + PDF/JPEG/PNG allowlist), GET /availability/windows (org-wide, D-14-24 filtered), GET /availability/windows/{id}/certificate (hr + owner's unit manager only), GET /availability/capacity (scope params + required period bounds), contract-types CRUD (14-06)."
    - "Permission matrix over HTTP: non-owner withdraw 403; non-manager confirm/reject 403 (incl. hr-not-manager — D-14-03); self-confirm 200 (D-14-04); non-hr medical edit/certificate attach 403; reject without reason 400; terminal-state ops 409; cross-org ids 404 (no existence oracle); unauthenticated 401; malformed ids/periods 400 — never 500 for client input."
    - "Certificate download (GET .../certificate) returns the stored bytes with the stored content-type only to hr + the owner's resolved unit manager; others 403/404 (T-14g-10)."
    - "GET /availability/capacity serves scope=activity|wg|unit|org with scope_id + required period_start/period_end (parsePeriod), returning per-employee rows + totals + declared advisory + schedule resolution level (D-14-20/21/18)."
    - "cmd/server/main.go registers every route with middleware.Auth; handler_test_helper.go mirrors the wiring; the full suite is green (make test exit 0) with the D-13-29 closure (14-04) and all availability packages passing."
  artifacts:
    - internal/adapters/primary/http/availability_handler.go (full route set + sentinel map)
    - internal/adapters/primary/http/availability_handler_test.go (permission-matrix + read battery)
    - cmd/server/main.go + handler_test_helper.go (wiring parity)
  key_links:
    - "Handler methods ↔ service methods (14-03/14-05/14-07) — thin adapters only, no logic"
    - "Fixture ↔ main.go — the battery exercises the real stack (13-08 precedent: fixture wiring mirrors main.go, compile-forced)"
    - "GET certificate ↔ AttachCertificate storage (14-05) + D-14-24 visibility (14-07)"
flagged_assumptions:
  - statement: "Unclassified edge (AVAIL-02, specless probe): 'Manager/HR can confirm' — the HTTP battery asserts hr-not-manager confirm → 403 (D-14-03 reading), manager + self-confirm → 200 (D-14-01/04). Surfaced, never auto-resolved."
    status: unverified
    flagged: true
---

<objective>
The complete HTTP surface: the remaining /availability routes (withdraw/confirm/reject/HR-edit/certificate-attach, windows read, certificate download, capacity), production wiring in cmd/server/main.go, the fixture mirror, and the full integration battery (permission matrix, sentinel map, D-14-24 filtering e2e, upload gates).

Purpose: makes the phase consumable — Phase 16 (AVAIL-03/04/05 UI) and Phase 19 (scheduler warnings) call these endpoints. The battery is the end-to-end proof of the locked gate decisions (D-14-03/04/26), the privacy carve-out (D-14-24), and the upload contract (D-14-07).

Output: every route live + wired, the integration battery green, full suite green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@.planning/phases/14-availability-backend-absences-capacity/14-VALIDATION.md
@internal/adapters/primary/http/direction_handler.go
@internal/adapters/primary/http/direction_handler_test.go
@internal/adapters/primary/http/expense.go
@internal/adapters/primary/http/handler_test_helper.go
@cmd/server/main.go
@cmd/server/main_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lifecycle + certificate routes — handler methods, fixture routes, permission battery</name>
  <files>internal/adapters/primary/http/availability_handler.go, internal/adapters/primary/http/availability_handler_test.go, internal/adapters/primary/http/handler_test_helper.go</files>
  <read_first>internal/adapters/primary/http/direction_handler.go (claims + writeError 72-93, 304-323 — the thin-adapter skeleton), internal/adapters/primary/http/expense.go (upload gate 492-511 — MaxBytesReader 5 MB + ParseMultipartForm + FormFile + extension allowlist — the MIME/size pattern ONLY, never the disk storage at 513-537 — locked D-14-07), internal/adapters/primary/http/handler_test_helper.go (route registration block — add the availability routes here FIRST so the battery runs against the fixture), internal/adapters/primary/http/direction_handler_test.go (battery shape: registerAndLogin, seed chains, cookie discipline)</read_first>
  <action>
    RED: extend availability_handler_test.go with the lifecycle + upload battery (fixture + registerAndLogin; seed the unit chain so the employee's unit manager is resolvable): withdraw by owner 200 / non-owner 403 / confirmed-window 409; confirm by resolved unit manager 200 + audit / non-manager 403 / hr-not-manager 403 (D-14-03) / self-confirm 200 (D-14-04) / confirmed-window 409; reject without reason 400 / with reason 200 + rejection_reason persisted; HR medical edit 200 / non-medical 400 / non-hr 403; certificate attach (hr, medical, small PNG) 200 / non-hr 403 / non-medical 400 / wrong extension 400 / oversized body 400 (the MaxBytesReader cap); GET .../certificate by hr + unit manager 200 with content-type / other member 403; cross-org ids 404; unauthenticated 401. Run — RED (routes absent from the fixture: 404s; handler methods absent).
    GREEN: availability_handler.go — add the thin handler methods (Withdraw/Confirm/Reject/UpdateMedical/AttachCertificate/GetCertificate): ctx claims (orgID/userID/role), uuid.Parse PathValue ids (400 on malformed), JSON/multipart decode, service call, api envelope; AttachCertificate per the expense.go gate (r.Body = http.MaxBytesReader(w, r.Body, 5<<20); ParseMultipartForm; FormFile("file"); extension allowlist .pdf/.jpg/.jpeg/.png → 400; content-type + size read from the part header — bytes go to the service for BYTEA storage, never to disk); writeError gains the remaining sentinel cases (ErrNotMedical/ErrRejectReasonRequired/ErrCertificateRequired → 400; ErrOverlap/ErrInvalidTransition → 409). Register the six routes in handler_test_helper.go (fixture) so the battery runs against the real stack. Run — GREEN.
  </action>
  <behavior>
    - Test 1: withdraw owner 200 + audit; non-owner 403; confirmed → 409.
    - Test 2: confirm manager 200; non-manager 403; hr-not-manager 403; self-confirm 200; confirmed → 409.
    - Test 3: reject no reason 400; with reason 200 + persisted reason; rejected re-confirm → 409.
    - Test 4: HR edit 200; non-medical 400; non-hr 403; attach PNG 200; non-hr 403; wrong extension 400; oversized 400; GET certificate hr/manager 200, other 403.
    - Test 5: cross-org ids → 404; unauthenticated → 401.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run TestAvailabilityHandler -count=1</automated>
  </verify>
  <acceptance_criteria>Battery green through the fixture; the D-14-03/04/26 gate matrix and the D-14-07 upload contract proven over HTTP.</acceptance_criteria>
  <done>Lifecycle + certificate routes live and battery-proven: gates, reason requirements, terminal 409s, upload allowlist + cap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read routes — windows list filtering e2e, capacity endpoint, certificate download</name>
  <files>internal/adapters/primary/http/availability_handler.go, internal/adapters/primary/http/availability_handler_test.go, internal/adapters/primary/http/handler_test_helper.go</files>
  <read_first>internal/adapters/primary/http/direction_handler.go (parsePeriod 283-298 — reuse verbatim for period_start/period_end; parseOptionalQueryUUID 268-278 for optional filters), internal/adapters/primary/http/availability_handler.go (Task 1 output — writeError + claims shape), internal/core/services/availability/read_models.go (14-07 — ListWindows/Capacity/Attachment service methods the handlers wrap)</read_first>
  <action>
    RED: extend availability_handler_test.go with the read battery: GET /availability/windows as an org member → 200 with windows (kind/dates/status incl. declared); medical window's certificate_ref + attachment metadata visible to hr and to the owner's resolved unit manager, ABSENT for a plain employee/manager (D-14-24 e2e — server-side, assert the JSON lacks the fields); filters (user_id/kind/status/period) + limit/offset applied; GET /availability/capacity with scope=activity (seeded subtree with submitted+approved entries on a descendant), scope=wg, scope=unit, scope=org → per-employee rows + totals + declared advisory + schedule resolution level; missing/unknown scope → 400; missing/malformed period → 400; GET /availability/windows/{id}/certificate — hr + unit manager 200 with bytes, employee 403, no attachment 404. Run — RED (routes absent from the fixture; ListWindows/Capacity/GetCertificate handler methods absent).
    GREEN: availability_handler.go — add ListWindows (query-param filters + limit/offset parse → service), Capacity (scope + scope_id + parsePeriod for period_start/period_end — the direction_handler.go:283-298 shape verbatim → service), GetCertificate (privileged single-doc read → bytes with the stored content-type header, 404 when no attachment). Register the three routes in handler_test_helper.go. Run — GREEN.
  </action>
  <behavior>
    - Test 1: windows read org-wide; filters + pagination; deterministic ordering.
    - Test 2: D-14-24 filtering e2e — hr + unit manager see certificate_ref/attachment metadata; other members do not (medical kind label present in all cases).
    - Test 3: capacity per scope (activity subtree workload / wg / unit / org) with declared advisory + resolution level; unknown scope 400; malformed period 400.
    - Test 4: certificate download — hr/unit-manager 200 + content-type, employee 403, missing 404.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run TestAvailabilityHandler -count=1</automated>
  </verify>
  <acceptance_criteria>Read battery green through the fixture; the D-14-24 filtering e2e proves server-side privacy; capacity scope/period contract proven.</acceptance_criteria>
  <done>Read routes live and battery-proven: windows list with privacy filtering, capacity per scope, certificate download gated.</done>
</task>

<task type="auto">
  <name>Task 3: Production wiring — cmd/server/main.go parity + full-suite green</name>
  <files>cmd/server/main.go, cmd/server/main_test.go, internal/adapters/primary/http/handler_test_helper.go</files>
  <read_first>cmd/server/main.go (direction wiring + route block 177-187, 284-290 — the block this phase mirrors; orgsvc + availability service constructors), cmd/server/main_test.go (the NewService call sites — the 14-06 constructor change is compile-forced here), internal/adapters/primary/http/handler_test_helper.go (fixture availability block from Tasks 1-2 + 14-03)</read_first>
  <action>
    Wire the full surface in cmd/server/main.go: the availability service + handler block (the 14-03 wiring shape) plus the remaining routes — POST /availability/windows/{id}/withdraw, POST /availability/windows/{id}/confirm, POST /availability/windows/{id}/reject, PUT /availability/windows/{id}, POST /availability/windows/{id}/certificate, GET /availability/windows, GET /availability/windows/{id}/certificate, GET /availability/capacity — each wrapped in middleware.Auth (D-14-26). Verify parity with the contract-types routes (14-06) already sitting in the same block — DO NOT re-register them: Go 1.22 ServeMux panics on duplicate pattern registration (the same fixture/most-specific-wins discipline as the Pitfall 6 org-settings note). Verify the cmd/server/main_test.go orgsvc constructor call sites already match the new signature (updated in 14-06 Task 3) — do not change them here. Confirm the fixture (handler_test_helper.go) already mirrors every route (Task 1/2 did the availability routes; 14-06 did contract-types — verify parity with main.go).
    Close the phase: run the full suite; fix any cross-package fallout (e.g. the D-13-29 closure interplay with the 14-04 direction suites); commit.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && make test</automated>
  </verify>
  <acceptance_criteria>make test green (all packages, incl. direction + availability + cmd/server); main.go and the fixture register the identical route set; no orphan routes in either.</acceptance_criteria>
  <done>The /availability surface is production-wired, fixture-parity holds, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | all /availability requests cross here — the permission matrix (owner/manager/hr gates), sentinel mapping, and upload gates are enforced at this boundary |
| handler→service | multipart bytes cross here — MIME/size enforcement at the handler, storage in the DB (never disk) |
| API→client | medical fields cross here — the D-14-24 server-side filtering is the only privacy control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-04 | Elevation of Privilege | confirm/reject by non-manager (incl. hr-not-manager) | high | mitigate | Permission battery: non-manager 403, hr-not-manager 403, self-confirm 200 (Task 1, D-14-01/03/04) |
| T-14g-13 | Elevation of Privilege | HR edit / certificate attach by non-hr | high | mitigate | models.RoleHR gates + battery 403 cases (Task 1, D-14-26) |
| T-14g-14 | Denial of Service | oversized / wrong-MIME certificate upload | medium | mitigate | MaxBytesReader 5 MB + PDF/JPEG/PNG allowlist (Task 1, D-14-07 — expense.go gate, never disk storage) |
| T-14g-05 | Information Disclosure | medical certificate_ref/docs org-wide | critical | mitigate | D-14-24 filtering e2e in the read battery (Task 2) — server-side, JSON field absence asserted |
| T-14g-10 | Information Disclosure | certificate document download exfiltration | high | mitigate | GET .../certificate gated to hr + the owner's resolved unit manager (Task 2) |
| T-14g-25 | Tampering | unauthenticated / malformed input → 500 | medium | mitigate | middleware.Auth on every route + sentinel map + parsePeriod/parseUUID 400s (Tasks 1/2/3) |
| T-14g-06 | Tampering | reject-without-reason bypass | high | mitigate | Handler passes through the service fast-fail; battery asserts 400 (Task 1, D-14-09) |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit); no install step |
</threat_model>

<verification>
- `go test ./internal/adapters/primary/http/ -run TestAvailabilityHandler -count=1` — full battery green (Task 1 lifecycle + Task 2 read)
- `go build ./... && go vet ./...` — clean
- `make test` — full suite green (24+ packages; direction closure from 14-04 stays green)
</verification>

<success_criteria>
- All nine /availability routes + contract-types routes live, auth-gated, and fixture-mirrored
- Permission matrix, privacy filtering, upload gates, and capacity contract proven over HTTP
- Full suite green; the phase is consumable by Phase 16/19
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified files:**
- `internal/adapters/primary/http/availability_handler.go` — +Withdraw/Confirm/Reject/UpdateMedical/AttachCertificate/GetCertificate/ListWindows/Capacity, writeError sentinel map completed
- `internal/adapters/primary/http/availability_handler_test.go` — full permission-matrix + read + upload battery (D-14-24 filtering e2e, D-14-03/04 gate cases)
- `internal/adapters/primary/http/handler_test_helper.go` — fixture parity with main.go (all routes)
- `cmd/server/main.go` — full /availability route block + orgsvc constructor call site
- `cmd/server/main_test.go` — constructor call sites updated

