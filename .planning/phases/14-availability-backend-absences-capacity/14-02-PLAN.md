---
phase: 14-availability-backend-absences-capacity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md
  - hourglass-vault/decisions/backend/ADR-BE-019 — Availability Schema Encoding.md
  - internal/core/domain/availability/availability.go
  - internal/core/domain/availability/errors.go
  - internal/core/ports/availability_repository.go
  - internal/core/services/testdata/mock_availability_repo.go
  - internal/models/models.go
  - internal/models/models_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: false
estimate:
  tokens: 18000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The availability domain pins the closed window vocabulary and matrix (declared → confirmed|rejected|withdrawn; rejected/withdrawn terminal; medical never enters the matrix — auto-confirmed at declare, D-14-02/08/09/10) and the audit vocabulary entity_type 'availability_window' with actions declared/confirmed/rejected/withdrawn/edited/certificate_attached — no 'cancelled'/'superseded' anywhere (D-14-12, research Pitfall 2)."
    - "Domain sentinels cover every client-input failure class (invalid kind, invalid hours with the DECIMAL(4,2) 99.99 ceiling, reject reason required, certificate required for medical, overlap, invalid transition, not medical) with JSONNames mapping (research Pitfall 6)."
    - "models.RoleHR = 'hr' exists and models.Role.IsValid() accepts it; models_test.go validCases updated (D-14-26, research Pitfall 8)."
    - "ports.AvailabilityRepository declares the full mutator surface (every mutator takes its *audit.AuditLog and writes it in the same transaction — BE-012/BE-016 doc contract), the read-models (Windows/Capacity/Attachment), and contract-type CRUD; testdata.MockAvailabilityRepo implements it with the compile-time assertion var _ ports.AvailabilityRepository (contract all later plans compile against)."
    - "ADR-P-008 revision records the D-1a simplification (unit-manager-only confirmation, WG line dropped — D-14-01) and the D-5 boundary change (DB-backed medical certificate attachments, GDPR special-category flag — D-14-06/07); ADR-BE-019 pins the window status vocab + matrix, the rejection_reason 2VL CHECK, the audit vocabulary, the contract_types + override model, the attachment table, and the overlap-guard encoding (D-14-15, no btree_gist)."
  artifacts:
    - internal/core/domain/availability/availability.go + errors.go
    - internal/core/ports/availability_repository.go
    - internal/core/services/testdata/mock_availability_repo.go
    - hourglass-vault/decisions/backend/ADR-BE-019 — Availability Schema Encoding.md
  key_links:
    - "Domain status constants ↔ 023 CHECK vocabulary (14-01) — must match exactly"
    - "Port mutator signatures ↔ repo mutator implementations (14-03/14-05) — audit rows in-tx by contract"
    - "models.RoleHR ↔ hr gates in the service plans (14-05/14-06/14-08)"
flagged_assumptions:
  - statement: "Prohibition recall (research Pitfall 2, D-14-12): no 'cancelled' or 'superseded' literal in the availability domain package — windows withdraw, they don't cancel; the audit action vocabulary is exactly declared/confirmed/rejected/withdrawn/edited/certificate_attached."
    status: unverified
    flagged: true
  - statement: "Prohibition recall (D-14-15, first-extension-free house style): no btree_gist / EXCLUDE constraint anywhere — the overlap guard is an in-tx SELECT ... FOR UPDATE (14-03)."
    status: unverified
    flagged: true
  - statement: "Prohibition recall (ADR-BE-012/BE-016, D-14-12): every window event audit row is written synchronously inside the mutator transaction — never fire-and-forget (the ticket/coverage/direction precedent)."
    status: unverified
    flagged: true
  - statement: "Checker INFO (iteration 1) consciously declined: Task 3 (RoleHR + IsValid + validCases) is not tdd-tagged — tests ship in-task and the task's port/mock portion has no RED-able test; tagging tdd would muddy the tag for one third of the task. Revisit only if the port/mock part is ever split out."
    status: accepted
    flagged: false
---

<objective>
The compile-time contract layer: ADR revision + BE encoding ADR, the availability domain package (entity, vocabularies, matrix, sentinels, audit constants), the ports.AvailabilityRepository interface, the testdata mock, and the first backend consumer of the `hr` role (models.RoleHR).

Purpose: Every later plan (repo mutators, service, handlers) compiles against the pinned contracts produced here. The domain is the single source of truth for the D-14-08 vocabulary, the D-14-12 audit vocabulary, and the transition matrix — the DB CHECK (14-01) and every caller read from it, so vocabulary drift (research Pitfall 2) is structurally impossible. The ADRs record the two ADR-P-008 revisions (D-14-01 routing simplification, D-14-06/07 document-storage boundary change) per the milestone convention "Each backend phase drafts its ADR + BE encoding ADR".

Output: two vault ADR documents, the availability domain package with unit tests, ports.AvailabilityRepository, testdata.MockAvailabilityRepo, models.RoleHR + IsValid case.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@internal/core/domain/direction/direction.go
@internal/core/domain/ticket/ticket.go
@internal/core/ports/direction_repository.go
@internal/core/services/testdata/mock_direction_repo.go
@internal/models/models.go
@hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md
@hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-P-008 revision + ADR-BE-019 encoding ADR</name>
  <files>hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md, hourglass-vault/decisions/backend/ADR-BE-019 — Availability Schema Encoding.md</files>
  <read_first>hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md (the D-1a and D-5 sections being revised), hourglass-vault/decisions/backend/ADR-BE-018 — Direction & Org Settings Encoding.md (vault template for the BE ADR: vocabularies, mechanisms, assumption pins)</read_first>
  <action>
    Revise ADR-P-008 in place (Edit, scoped — never rewrite the whole file): D-1a — replace the two-line confirmation (unit manager + WG manager) with unit-manager-only confirmation, recording D-14-01 (WG line dropped; costly reversibility noted); keep D-4 HR-curator-never-approver verbatim (D-14-03) and add the self-confirm rule (D-14-04) and the certificate_ref-required-at-declare rule (D-14-05). D-5 — revise the boundary: medical certificate documents ARE stored in Hourglass via a DB-backed attachment table (D-14-06/07), carrying an explicit GDPR special-category data flag and the one-way reversibility note (undoing = dropping the attachment table + storage path). Update the ADR status/date header.
    Draft ADR-BE-019 mirroring ADR-BE-018's structure with the 14-PATTERNS "No Analog Found" pins: window status vocabulary + transition matrix (declared → confirmed|rejected|withdrawn; rejected/withdrawn terminal; medical auto-confirms at declare), rejection_reason 2VL CHECK (constraint name availability_windows_reject_reason_check), audit vocabulary (entity_type 'availability_window'; actions declared/confirmed/rejected/withdrawn/edited/certificate_attached; reject/withdraw payloads {reason}; HR edits {before, after}), overlap-guard encoding (in-tx SELECT ... FOR UPDATE, active-only statuses, inclusive date-range intersection — no btree_gist, no EXCLUDE), contract_types + membership override model (cadence week|month, hours_per_period > 0, day_hours JSONB validated code-side, override merges over the type matrix), org default schedule via the org_settings key default_contract_type_id, attachment table + BYTEA + 5 MB cap + PDF/JPEG/PNG allowlist, role gates (hr first backend consumer), and the assumption pins: workload literal statuses ('submitted','approved'), capacity response shape, D-13-29 closure confirmed-only.
  </action>
  <verify>
    <automated>test -f "hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md" && test -f "hourglass-vault/decisions/backend/ADR-BE-019 — Availability Schema Encoding.md" && grep -c "availability_windows_reject_reason_check" "hourglass-vault/decisions/backend/ADR-BE-019 — Availability Schema Encoding.md"</automated>
  </verify>
  <acceptance_criteria>Both ADR files on disk; ADR-P-008 records the D-1a simplification and the D-5 document-storage boundary change; ADR-BE-019 pins the vocabularies, the 2VL constraint name, the overlap encoding, and the assumption pins.</acceptance_criteria>
  <done>ADR-P-008 revised (D-14-01/04/05/06/07 recorded) and ADR-BE-019 drafted with the full schema-encoding pins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Availability domain package — entity, vocabularies, matrix, sentinels, audit constants</name>
  <files>internal/core/domain/availability/availability.go, internal/core/domain/availability/errors.go</files>
  <read_first>internal/core/domain/direction/direction.go (entity shape 19-36, matrix 47-70, audit block 155-169), internal/core/domain/ticket/ticket.go (sentinels + JSONNames 65-83), 14-PATTERNS.md (domain sections with the exact struct/constants to mirror)</read_first>
  <action>
    RED: Write availability domain unit tests (same package) first — matrix probes, vocabulary probes, schedule validation probes — and run them (fail: package files absent).
    GREEN: Create the domain package per 14-PATTERNS. availability.go: Window entity (ID, OrgID, UserID, Kind, StartsOn, EndsOn, Hours *float64, CertificateRef *string, Note *string, Status, RejectionReason *string, CreatedBy, CreatedAt, UpdatedAt) with json tags (NULL hours = full day); kind constants holiday/permit/medical/unavailable mirroring the 012 CHECK; status constants StatusDeclared/StatusConfirmed/StatusRejected/StatusWithdrawn with transitionMatrix (declared → confirmed|rejected|withdrawn) + CanTransition + IsTerminalStatus (rejected/withdrawn) — D-14-08/09/10; a doc comment stating medical skips the matrix (auto-confirmed at declare, D-14-02). Audit constants block: AuditEntityWindow = "availability_window" + AuditActionDeclared/Confirmed/Rejected/Withdrawn/Edited/CertificateAttached (D-14-12) — the exact 14-PATTERNS vocabulary, nothing else. Schedule domain: ContractType struct (ID, OrgID, Name, Cadence, HoursPerPeriod, DayHours map[string]float64), CadenceWeek/CadenceMonth constants, Attachment struct (ID, OrgID, WindowID, ContentType, SizeBytes, Storage []byte, CreatedBy, CreatedAt), EmployeeSchedule + ScheduleResolution (per-day hours + source level "override"|"type"|"org_default"|"fallback" — D-14-18 resolution level). Validation helpers: windowHoursValid (hours > 0, whole-cent via math.Round(h*100) == h*100, ceiling 99.99 — the DECIMAL(4,2) shape, research Pitfall 6) and DayHours validation (keys "1".."7" ISO weekday, values 0 < h <= 24, non-empty for week cadence — D-14-16/17). errors.go: ErrWindowNotFound, ErrInvalidRequest, ErrForbidden, ErrInvalidTransition, ErrOverlap, ErrRejectReasonRequired, ErrInvalidHours, ErrInvalidKind, ErrCertificateRequired, ErrNotMedical, ErrContractTypeNotFound, ErrContractTypeInUse + JSONNames (house style).
  </action>
  <behavior>
    - Test 1 (matrix): CanTransition(declared, confirmed|rejected|withdrawn) all true; CanTransition(confirmed, anything) false; CanTransition(rejected/withdrawn, anything) false; IsTerminalStatus(rejected|withdrawn) true, IsTerminalStatus(declared|confirmed) false.
    - Test 2 (vocabulary): kind constants are the closed 012 set; audit constants spell exactly availability_window + the six actions; no test references 'cancelled' or 'superseded'.
    - Test 3 (hours): 0, negative, > 99.99, and sub-cent values (e.g. 4.005) all fail windowHoursValid; 4.5 and 99.99 pass.
    - Test 4 (day-hours): keys outside "1".."7" rejected; hours <= 0 or > 24 rejected; empty map rejected for week cadence, allowed for month cadence (dynamic, D-14-17).
  </behavior>
  <verify>
    <automated>go test ./internal/core/domain/availability/ -count=1</automated>
  </verify>
  <acceptance_criteria>Domain unit tests green; vocabularies match the 023 CHECK (14-01) and the ADR-BE-019 pins exactly; no 'cancelled'/'superseded' literal in the package.</acceptance_criteria>
  <done>Domain package compiled + unit-tested: entity, matrix, sentinels, audit constants, schedule validation — the single source of truth later plans compile against.</done>
</task>

<task type="auto">
  <name>Task 3: ports.AvailabilityRepository + testdata mock + models.RoleHR</name>
  <files>internal/core/ports/availability_repository.go, internal/core/services/testdata/mock_availability_repo.go, internal/models/models.go, internal/models/models_test.go</files>
  <read_first>internal/core/ports/direction_repository.go (doc-header style 12-32, mutator signatures taking *audit.AuditLog), internal/core/services/testdata/mock_direction_repo.go (mock shape: mutex + maps + Fn overrides + Audits capture + compile-time assertion), internal/models/models.go (role constants 11-25)</read_first>
  <action>
    Create ports/availability_repository.go per 14-PATTERNS: doc header stating the write shape ("every mutator takes its audit row(s) and writes them IN THE SAME TRANSACTION — BE-012/BE-016; pool-level service checks are fast-fail UX only, the repo re-validates under FOR UPDATE"). Methods: Declare(ctx, orgID, w *availability.Window, audit *audit.AuditLog) (*availability.Window, error) with a doc comment pinning the overlap guard (lock the user's ACTIVE declared+confirmed overlapping windows FOR UPDATE → ErrOverlap; medical → status confirmed immediately with the second audit row, D-14-02); Confirm/Reject (reason)/Withdraw/UpdateMedical/AttachCertificate — every mutator takes its audit row; read-models: Windows(ctx, orgID, filter WindowsFilter) ([]availability.Window, error), Capacity(ctx, orgID, employeeIDs []uuid.UUID, schedules []availability.EmployeeSchedule, periodStart, periodEnd time.Time) ([]availability.CapacityRow, error), Attachment(ctx, orgID, windowID) and AttachmentsByWindowIDs(ctx, orgID, windowIDs []uuid.UUID) ([]availability.Attachment, error) for the D-14-24 privileged reads, ActivityWorkloadEmployees(ctx, orgID, activityID) for the activity capacity scope (D-14-20); contract-type CRUD: ListContractTypes/CreateContractType/UpdateContractType/DeleteContractType (audit rows; DeleteContractType doc notes the FK-in-use → ErrContractTypeInUse mapping, D-14-28).
    Create testdata/mock_availability_repo.go mirroring mock_direction_repo.go: maps for windows/contract types/attachments, per-method Fn overrides, Audits capture slice, and var _ ports.AvailabilityRepository = (*MockAvailabilityRepo)(nil) — the compile-time assertion makes the mock the contract check for later plans.
    models.go: add RoleHR Role = "hr" to the constants block + case RoleHR: return true in IsValid() (D-14-26, research Pitfall 8 — JWT claims carry the role verbatim, no auth change). models_test.go: add models.RoleHR to validCases.
  </action>
  <verify>
    <automated>go test ./internal/models/ -count=1 && go vet ./internal/core/ports/ ./internal/core/services/testdata/ ./internal/models/</automated>
  </verify>
  <acceptance_criteria>Port + mock compile with the full-interface assertion; RoleHR accepted by IsValid with a test case; no "hr" string literal in service code paths (the constant is the only vocabulary).</acceptance_criteria>
  <done>Port, mock, and RoleHR land together — the contract surface every later plan compiles against is complete and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain→ports | sentinel errors and vocabularies cross into service/repo code — drift here silently changes HTTP status codes and DB CHECK vocabulary |
| auth claims→service | the membership role string crosses the JWT boundary — RoleHR must be a compile-time constant, not a literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Spoofing | models.Role / JWT role claim | high | mitigate | models.RoleHR constant + IsValid() case + models_test.go validCases (Task 3, research Pitfall 8) — the DB CHECK vocabulary (012) and the Go constant can never drift |
| T-14-08 | Tampering | availability domain vocabulary | high | mitigate | Single domain constants block (statuses + audit actions) with unit tests; the 023 CHECK (14-01) and ADR-BE-019 pin the same strings (Task 2, Pitfall 2) |
| T-14-11 | Tampering | window hours validation | medium | mitigate | windowHoursValid with the 99.99 DECIMAL(4,2) ceiling + whole-cent check in the domain (Task 2, Pitfall 6) — client input 400s before it can reach PG 22003 |
| T-14-04 | Elevation of Privilege | port surface misuse | medium | mitigate | Port doc contract pins org-scoped signatures + in-tx audit; repo implementations enforce org_id + FOR UPDATE in 14-03/14-05 |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit); no install step exists |
</threat_model>

<verification>
- `go test ./internal/core/domain/availability/ ./internal/models/ -count=1` — domain + models green
- `go vet ./internal/core/ports/ ./internal/core/services/testdata/` — port + mock compile with the full-interface assertion
- `go build ./...` — repo-wide build (port/mock changes are additive)
</verification>

<success_criteria>
- ADR-P-008 revision + ADR-BE-019 drafted and recorded in the vault
- Availability domain package green with the pinned matrix/vocabularies/audit constants
- ports.AvailabilityRepository + MockAvailabilityRepo compile with the var _ assertion
- models.RoleHR live with IsValid + test — first backend consumer of the hr role
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `internal/core/domain/availability/availability.go` — Window entity, kind/status constants, transitionMatrix + CanTransition/IsTerminalStatus, audit constants (AuditEntityWindow + six actions), ContractType/Attachment/EmployeeSchedule/ScheduleResolution types, windowHoursValid + DayHours validation helpers
- `internal/core/domain/availability/errors.go` — 11 sentinels + JSONNames
- `internal/core/ports/availability_repository.go` — full mutator (audit-in-tx) + read-model + contract-type CRUD surface
- `internal/core/services/testdata/mock_availability_repo.go` — MockAvailabilityRepo with Fn overrides + Audits capture + compile-time assertion
- `hourglass-vault/decisions/backend/ADR-BE-019 — Availability Schema Encoding.md`

**Modified files:**
- `hourglass-vault/decisions/project/ADR-P-008 — Availability & Employment Validity.md` — D-1a simplification + D-5 document-storage boundary change
- `internal/models/models.go` / `internal/models/models_test.go` — RoleHR + IsValid + validCases
