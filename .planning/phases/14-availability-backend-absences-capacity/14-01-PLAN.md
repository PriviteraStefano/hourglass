---
phase: 14-availability-backend-absences-capacity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/023_availability_status.up.sql
  - migrations/023_availability_status.down.sql
  - migrations/024_work_schedules.up.sql
  - migrations/024_work_schedules.down.sql
  - migrations/025_certificate_attachments.up.sql
  - migrations/025_certificate_attachments.down.sql
  - internal/adapters/secondary/postgres/exported_test_helpers.go
  - internal/adapters/secondary/postgres/availability_ontology_migrations_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: false
estimate:
  tokens: 16000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Migration 023 extends the availability_windows status CHECK to ('declared','confirmed','rejected','withdrawn') and adds rejection_reason with the never-NULL-satisfiable 2VL CHECK (status <> 'rejected' OR rejection_reason IS NOT NULL); a rejected row with NULL reason is rejected by the DB with SQLSTATE 23514 + constraint name availability_windows_reject_reason_check (D-14-08/09, research Pitfall 1)."
    - "Migration 024 creates contract_types (org_id, name, cadence week|month CHECK, hours_per_period > 0 CHECK, nullable day_hours JSONB) and adds contract_type_id (FK) + day_hours_override (JSONB) to organization_memberships — no is_default flag: the org default schedule is an org_settings key (D-14-16/17/18, research OQ4)."
    - "Migration 025 creates certificate_attachments (id, org_id FK, entity_type CHECK = 'availability_window', entity_id FK availability_windows, content_type, size_bytes, storage BYTEA, created_by, created_at) with an (entity_id) index (D-14-07)."
    - "All three migrations are append-only numbered from 023 per ADR-BE-004, have up/down pairs, and pass up/down/up cycle tests with functional CHECK probes asserted by constraint name (23514) — the direction_ontology_migrations_test.go template."
    - "Teardown list in exported_test_helpers.go updated with FK-safe drop order: certificate_attachments BEFORE availability_windows, contract_types AFTER organization_memberships; seedAvailabilityWindowWithCert (the certificate_ref-aware helper) + seedContractType helpers exist (research Pitfall 7). The Phase 13 seedAvailabilityWindow at direction_repository_test.go:121 stays untouched — same package, same identifier would be a compile error (checker BLOCKER)."
  artifacts:
    - migrations/023_availability_status.up.sql / .down.sql
    - migrations/024_work_schedules.up.sql / .down.sql
    - migrations/025_certificate_attachments.up.sql / .down.sql
    - internal/adapters/secondary/postgres/availability_ontology_migrations_test.go
    - internal/adapters/secondary/postgres/exported_test_helpers.go (modified)
  key_links:
    - "availability_windows status CHECK vocabulary ↔ domain status constants (14-02) — must match exactly, no vocabulary drift (research Pitfall 2)"
    - "organization_memberships.contract_type_id FK ↔ schedule fallback resolution reads (14-06)"
    - "certificate_attachments.entity_id FK ↔ availability_windows.id ↔ attach mutator (14-05) + privileged read (14-08)"
flagged_assumptions:
  - statement: "Prohibition recall (ADR-BE-004, research Pitfall 7, 14-CONTEXT canonical refs): append-only migration numbering continues from 023 (latest is 022_org_settings — Phase 11 A8 lesson); no migration below 023, no collision with a taken number."
    status: unverified
    flagged: true
  - statement: "Prohibition recall (research Pitfall 2, 14-PATTERNS anti-pattern 2): the 023 vocabulary must never borrow 'cancelled'/'superseded' — windows withdraw, they don't cancel; the CHECK carries exactly ('declared','confirmed','rejected','withdrawn')."
    status: unverified
    flagged: true
  - statement: "Prohibition recall (research Pitfall 1, 14-PATTERNS anti-pattern 1): the reject-reason CHECK uses the never-NULL-satisfiable 2VL form and is asserted with 23514 + constraint name — a NULL-returning CHECK silently accepts rows."
    status: unverified
    flagged: true
---

<objective>
Schema for the absence lifecycle, work schedules, and medical certificate storage — migrations 023/024/025 + teardown + cycle tests.

Purpose: The shipped `availability_windows` schema (migration 012) has a two-value status CHECK (`declared`, `confirmed`) that cannot express rejects/withdrawals; capacity needs a work-schedule model (contract_types template + per-employee override on organization_memberships); medical certificate documents need a DB-backed attachment table (D-14-07). This plan lands all three append-only migrations with up/down pairs and cycle tests, plus the test-infrastructure edits (teardown drop order + seed helpers) that every later plan's testcontainers suites depend on.

Output: migrations 023/024/025 (+down pairs), cycle tests in availability_ontology_migrations_test.go, teardown list + seed helpers in exported_test_helpers.go.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@.planning/phases/14-availability-backend-absences-capacity/14-VALIDATION.md
@migrations/012_staffing_schema.up.sql
@migrations/021_direction_rows.up.sql
@migrations/022_org_settings.up.sql
@internal/adapters/secondary/postgres/direction_ontology_migrations_test.go
@internal/adapters/secondary/postgres/exported_test_helpers.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 023 — status vocabulary extension + rejection_reason (TDD cycle test)</name>
  <files>migrations/023_availability_status.up.sql, migrations/023_availability_status.down.sql, internal/adapters/secondary/postgres/availability_ontology_migrations_test.go</files>
  <read_first>migrations/012_staffing_schema.up.sql (lines 46-50 — CHECK drop+recreate), migrations/021_direction_rows.up.sql (lines 48-61 — 2VL never-NULL-satisfiable reason CHECK + named constraint convention), internal/adapters/secondary/postgres/direction_ontology_migrations_test.go (TestMigration021/022 template, 47-240)</read_first>
  <action>
    RED: In availability_ontology_migrations_test.go write TestMigration023_AvailabilityStatus_UpDownUpCycle following the TestMigration021/022 template (TestPool/TeardownTestSchema/readMigration/applyMigrations/seedOrg/seedUser helpers; assertConstraintExists). Functional probes: after UP, INSERT a window with status 'withdrawn' succeeds; INSERT status 'rejected' with NULL rejection_reason fails with SQLSTATE 23514 and constraint name availability_windows_reject_reason_check (the direction_ontology_migrations_test.go 146-158 shape); INSERT 'rejected' with a reason succeeds. DOWN probe: the original two-value CHECK is restored under the same constraint name (status 'rejected' rejected with 23514 + availability_windows_status_check). Final UP again: green. Run the test — RED because the migration files do not exist yet.
    GREEN: Write 023 up/down per D-14-08/09 and research Common Operation 1. Up: ALTER TABLE availability_windows DROP CONSTRAINT IF EXISTS availability_windows_status_check, then ADD CONSTRAINT availability_windows_status_check CHECK (status IN ('declared','confirmed','rejected','withdrawn')) — PostgreSQL cannot alter CHECKs in place (012:46-50); ADD COLUMN rejection_reason TEXT; ADD CONSTRAINT availability_windows_reject_reason_check CHECK (status <> 'rejected' OR rejection_reason IS NOT NULL) — the never-NULL-satisfiable 2VL form (021:57-61), a rejected row with NULL reason is FALSE OR FALSE. Header comment: continue-from-max 023+ note (Phase 11 A8 lesson, ADR-BE-004), the explicit IS [NOT] NULL / 2VL rationale, and the vocabulary note that 'rejected'/'withdrawn' extend — never 'cancelled'/'superseded' (research Pitfall 2). Down: drop availability_windows_reject_reason_check, drop the rejection_reason column, drop the extended status check, restore the ORIGINAL two-value CHECK with the SAME constraint name availability_windows_status_check. Run the cycle test — GREEN.
  </action>
  <behavior>
    - Test 1 (up probes): status='withdrawn' INSERT succeeds; status='rejected' with NULL rejection_reason → 23514 + constraint name availability_windows_reject_reason_check; status='rejected' with reason succeeds; status='confirmed' still valid.
    - Test 2 (down probe): after DOWN, INSERT status='rejected' fails with 23514 + availability_windows_status_check (original two-value vocab restored); rejection_reason column gone.
    - Test 3 (up/down/up cycle): reapplying UP restores the extended vocabulary and the 2VL constraint.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestMigration023 -count=1</automated>
  </verify>
  <acceptance_criteria>Cycle test green; both 23514 probes assert the exact constraint names; DOWN restores the original two-value CHECK; existing 011/012 cycle tests still green.</acceptance_criteria>
  <done>023 up/down pairs exist and pass the up/down/up cycle with the 2VL reject-reason constraint proven by 23514 + constraint name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 024 — contract_types + membership override (TDD cycle test)</name>
  <files>migrations/024_work_schedules.up.sql, migrations/024_work_schedules.down.sql, internal/adapters/secondary/postgres/availability_ontology_migrations_test.go</files>
  <read_first>migrations/022_org_settings.up.sql (small additive table + memberships ALTER), migrations/021_direction_rows.up.sql (named CHECK conventions, lines 48-61), internal/adapters/secondary/postgres/direction_ontology_migrations_test.go (022 cycle test lines 190-240 — column nullability + CHECK probes)</read_first>
  <action>
    RED: Write TestMigration024_WorkSchedules_UpDownUpCycle. After UP: contract_types exists with named CHECK constraints; INSERT cadence 'day' → 23514 + contract_types_cadence_check; INSERT hours_per_period 0 → 23514 + contract_types_hours_period_check; day_hours nullable (INSERT with NULL day_hours succeeds); organization_memberships.contract_type_id + day_hours_override columns exist and are nullable; assertConstraintExists proves the FK contract_types → organizations and the memberships FK → contract_types. DOWN: memberships columns gone first, then the table (assertTableExists false). UP again: green. Run — RED (files absent).
    GREEN: Write 024 up/down per D-14-16/17 and 14-PATTERNS migration section. Up: CREATE TABLE contract_types (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), org_id UUID NOT NULL REFERENCES organizations(id), name VARCHAR(100) NOT NULL, cadence VARCHAR(10) NOT NULL, hours_per_period DECIMAL(5,2) NOT NULL, day_hours JSONB, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()) with NAMED constraints contract_types_cadence_check CHECK (cadence IN ('week','month')) and contract_types_hours_period_check CHECK (hours_per_period > 0) — named per the 021 named-constraint lesson (STATE.md Phase 13 note); day_hours JSONB nullable and validated code-side per the 022 "CHECK on JSONB is infeasible" convention (research OQ5). ALTER TABLE organization_memberships ADD COLUMN contract_type_id UUID REFERENCES contract_types(id), ADD COLUMN day_hours_override JSONB (D-14-16). NO is_default flag — the org default schedule is the org_settings key default_contract_type_id (D-14-18, research OQ4). Header comment distinguishes contract_types (work-schedule template) from the contracts.contract_type column (funding-shape label, migration 016) — research assumption A1. Down: ALTER organization_memberships DROP COLUMN contract_type_id, DROP COLUMN day_hours_override, then DROP TABLE contract_types (022 down precedent: drop column then table). Run — GREEN.
  </action>
  <behavior>
    - Test 1: cadence CHECK rejects 'day' with 23514 + contract_types_cadence_check; hours_per_period 0 rejected with 23514 + contract_types_hours_period_check; NULL day_hours accepted.
    - Test 2: membership columns contract_type_id + day_hours_override exist, nullable, with FK to contract_types.
    - Test 3: down/down column-then-table order + up/down/up cycle green.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestMigration024 -count=1</automated>
  </verify>
  <acceptance_criteria>Cycle test green; named CHECK constraints proven with 23514 + names; membership columns + FKs asserted; no is_default flag anywhere in the DDL.</acceptance_criteria>
  <done>024 up/down pairs exist and pass the up/down/up cycle; contract_types + membership override columns are schema-ready for the schedule resolution plan (14-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migration 025 — certificate attachments + teardown order + seed helpers</name>
  <files>migrations/025_certificate_attachments.up.sql, migrations/025_certificate_attachments.down.sql, internal/adapters/secondary/postgres/availability_ontology_migrations_test.go, internal/adapters/secondary/postgres/exported_test_helpers.go</files>
  <read_first>migrations/017_audit_logs.up.sql (FK-table form + index), internal/adapters/secondary/postgres/exported_test_helpers.go (teardown list 81-128, seedDirectionRow 253-274), internal/adapters/secondary/postgres/direction_ontology_migrations_test.go (assertConstraintExists/assertTableExists helpers)</read_first>
  <action>
    RED: Write TestMigration025_CertificateAttachments_UpDownUpCycle. After UP: table exists; entity_type CHECK named certificate_attachments_entity_type_check (INSERT entity_type 'expense' → 23514); FK entity_id → availability_windows asserted; index on (entity_id) asserted; storage BYTEA NOT NULL. DOWN: table gone. UP again: green. Run — RED (files absent).
    GREEN: Write 025 up/down per D-14-07 and 14-PATTERNS (no BYTEA precedent in the repo — 017 FK-table form + standard PG BYTEA; size cap enforced at the handler, not in SQL). Up: CREATE TABLE certificate_attachments (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), org_id UUID NOT NULL REFERENCES organizations(id), entity_type VARCHAR(32) NOT NULL, entity_id UUID NOT NULL REFERENCES availability_windows(id), content_type VARCHAR(100) NOT NULL, size_bytes BIGINT NOT NULL, storage BYTEA NOT NULL, created_by UUID NOT NULL REFERENCES users(id), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()) with named constraint certificate_attachments_entity_type_check CHECK (entity_type = 'availability_window') + CREATE INDEX idx_certificate_attachments_entity ON certificate_attachments(entity_id). Down: DROP TABLE certificate_attachments.
    Teardown + seed helpers (same task — test infra): in exported_test_helpers.go add certificate_attachments BEFORE availability_windows and contract_types AFTER organization_memberships in the TeardownTestSchema list (FK dependency order — research Pitfall 7); add seedAvailabilityWindowWithCert (org/user/kind/starts_on/ends_on/hours/certificate_ref/status params) and seedContractType helpers following the seedDirectionRow shape (253-274). The new helper MUST be named seedAvailabilityWindowWithCert, NOT seedAvailabilityWindow: direction_repository_test.go:121 (package postgres) already defines func seedAvailabilityWindow(t, pool, orgID, userID, kind, startsOn, endsOn, hours *float64, status, now) with no certificate_ref — adding a same-name helper in the same package is a compile error that breaks the ENTIRE postgres test package (14-01 verify, every later repo suite, make test). Leave the Phase 13 helper and its call sites in direction_repository_test.go untouched. Run — GREEN.
  </action>
  <behavior>
    - Test 1: entity_type CHECK rejects 'expense' with 23514 + certificate_attachments_entity_type_check; FK + (entity_id) index present; storage column NOT NULL BYTEA.
    - Test 2: DOWN drops the table; up/down/up cycle green.
    - Test 3: teardown drops certificate_attachments before availability_windows and contract_types after organization_memberships without error (exercised implicitly by every later suite).
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestMigration025 -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>Cycle test green; teardown list updated in FK-safe order; seedAvailabilityWindowWithCert + seedContractType compile and are usable by later plans' repo tests (no same-name collision with the Phase 13 seedAvailabilityWindow).</acceptance_criteria>
  <done>025 up/down pairs exist and pass the cycle; teardown + seed helpers ready for the mutator and read-model plans (14-05/14-06/14-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | untrusted declare payloads (kind, dates, hours) cross here; the DB CHECK vocabulary is the last line of shape enforcement |
| DB→API | CHECK constraint rejects (23514) surface through wrapPGError — must map to 400/409, never 500 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Tampering | availability_windows.status CHECK (023) | high | mitigate | Drop+recreate CHECK with the full D-14-08 vocabulary; cycle test proves 'withdrawn'/'rejected' acceptance and the 2VL reject-reason rejection by constraint name (Task 1) |
| T-14-01 | Tampering | availability_windows_reject_reason_check | high | mitigate | Never-NULL-satisfiable 2VL form (021:57-61 precedent) + 23514 + constraint-name assertion (Task 1) — a NULL-returning CHECK silently accepts rows |
| T-14-07 | Elevation of Privilege | contract_types org scoping (024) | medium | mitigate | org_id FK NOT NULL on the table; membership FK scoped reads come in 14-06/14-07 |
| T-14-09 | Tampering | certificate_attachments entity_type (025) | medium | mitigate | Named CHECK entity_type = 'availability_window' (D-K-style closed scope) + FK to availability_windows + cycle probe |
| T-14-04 | Elevation of Privilege | cross-org FK chains | medium | mitigate | All new FKs are org_id-referencing organizations(id); cross-org access control lands in the service plans (14-05..14-08) |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase — research Package Legitimacy Audit verified all toolchain deps in go.mod (14-RESEARCH.md); no install step exists |
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestMigration02[3-5]' -count=1` — the three new cycle tests green
- `go test ./internal/adapters/secondary/postgres/ -count=1` — full postgres package green (existing 011/012 migration cycle tests unaffected)
- `go build ./...` — repo compiles (seed helpers referenced by later plans are additive only)
</verification>

<success_criteria>
- Migrations 023/024/025 exist with up/down pairs, numbered append-only from 023, passing up/down/up cycle tests with named-constraint probes
- availability_windows carries the full D-14-08 status vocabulary + rejection_reason with the 2VL guard
- contract_types + organization_memberships.contract_type_id/day_hours_override are schema-ready; no is_default flag (org default = org_settings key, D-14-18)
- certificate_attachments is schema-ready for BYTEA documents (D-14-07)
- Teardown list + seed helpers ready for every later availability plan
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `migrations/023_availability_status.up.sql` / `.down.sql` — status CHECK extended to ('declared','confirmed','rejected','withdrawn'), `rejection_reason TEXT`, named 2VL CHECK `availability_windows_reject_reason_check`
- `migrations/024_work_schedules.up.sql` / `.down.sql` — `contract_types` table (named cadence/hours CHECKs, nullable `day_hours` JSONB), `organization_memberships.contract_type_id` FK + `day_hours_override` JSONB
- `migrations/025_certificate_attachments.up.sql` / `.down.sql` — `certificate_attachments` (BYTEA storage, entity_type CHECK, FK chain, `(entity_id)` index)
- `internal/adapters/secondary/postgres/availability_ontology_migrations_test.go` — TestMigration023/024/025 cycle tests

**Modified files:**
- `internal/adapters/secondary/postgres/exported_test_helpers.go` — teardown list additions (`certificate_attachments` before `availability_windows`, `contract_types` after `organization_memberships`), new `seedAvailabilityWindowWithCert` / `seedContractType` helpers (named WithCert because the Phase 13 `seedAvailabilityWindow` already exists in direction_repository_test.go — checker BLOCKER)
