---
phase: 14-availability-backend-absences-capacity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/adapters/secondary/postgres/direction_repository.go
  - internal/core/ports/direction_repository.go
  - internal/core/domain/direction/direction.go
  - internal/adapters/secondary/postgres/direction_repository_test.go
  - internal/core/services/direction/direction_test.go
  - internal/adapters/primary/http/direction_handler_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: false
estimate:
  tokens: 11000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phase 13's direction read path (D-13-29 provisional) is closed: AbsenceWindows and the Coverage partial_abs/full_abs subqueries read ONLY status = 'confirmed' windows (D-14-21) — declared windows no longer produce away/partial warnings."
    - "Direction warnings still fire for confirmed windows (full absence → away, partial hours → partial) — the DIR-05 overlay keeps its behavior on the narrowed fact set."
    - "Port doc comments (ports/direction_repository.go Coverage + AbsenceWindows) and the domain AbsenceWindow doc (direction.go) state confirmed-only — no reader can reintroduce the both-statuses reading."
    - "Phase 13 test seeds that relied on declared windows producing warnings flip to confirmed; a declared-window subtest asserts NO warning is produced — the behavioral proof of D-14-21."
    - "The direction packages (repo/service/http) and the full suite stay green after the closure (make test exit 0)."
  artifacts:
    - internal/adapters/secondary/postgres/direction_repository.go (3 predicate flips: lines 767, 775, 816)
    - internal/core/ports/direction_repository.go + internal/core/domain/direction/direction.go (doc updates)
  key_links:
    - "AbsenceWindows/Coverage status predicates ↔ availability capacity semantics (14-07) — both read confirmed-only per D-14-21"
    - "Direction warning overlay ↔ the new availability confirm lifecycle (14-05) — declared windows are advisory only after this closure"
flagged_assumptions:
  - statement: "Prohibition recall (research Pitfall 5, 14-CONTEXT D-14-21): the closure changes shipped Phase 13 behavior — the plan MUST own the Phase 13 test-seed flips (declared → confirmed) or the direction suites go red; a declared-window no-warning subtest is the behavioral proof."
    status: unverified
    flagged: true
---

<objective>
Close Phase 13's D-13-29 deferred item (D-14-21): the direction scheduler read path — AbsenceWindows and the two Coverage absence subqueries — switches from status IN ('declared','confirmed') to confirmed-only, with the Phase 13 tests that seeded declared windows updated and a behavioral no-warning-on-declared proof added.

Purpose: Phase 14 introduces confirm/reject; declared absences are provisional requests, not facts. The direction warning overlay ("away 10–21 Aug") must reflect only confirmed absence or the scheduler warns on requests that may be rejected. This is a behavior change to shipped Phase 13 code, so it gets its own plan owning the code flip AND the test updates (research Pitfall 5).

Output: confirmed-only predicates + docs, Phase 13 test seeds flipped, no-warning-on-declared subtests, direction suites + full suite green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/13-direction-backend-the-plan-plane/13-CONTEXT.md
@.planning/phases/13-direction-backend-the-plan-plane/13-08-SUMMARY.md
@internal/adapters/secondary/postgres/direction_repository.go
@internal/core/ports/direction_repository.go
@internal/core/domain/direction/direction.go
@internal/adapters/secondary/postgres/direction_repository_test.go
@internal/core/services/direction/direction_test.go
@internal/adapters/primary/http/direction_handler_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirmed-only closure — RED subtest, then the code + doc flips</name>
  <files>internal/adapters/secondary/postgres/direction_repository.go, internal/core/ports/direction_repository.go, internal/core/domain/direction/direction.go, internal/core/services/direction/direction_test.go</files>
  <read_first>internal/adapters/secondary/postgres/direction_repository.go (Coverage subqueries 762-777 — the partial_abs/full_abs status predicates at 767/775; AbsenceWindows 812-839 — the predicate at 816), internal/core/ports/direction_repository.go (Coverage doc 93-98, AbsenceWindows doc 98-102), internal/core/domain/direction/direction.go (AbsenceWindow doc 142-153)</read_first>
  <action>
    RED: in services/direction/direction_test.go add the behavioral subtest: seed a DECLARED absence window for the employee and assert the warnings overlay computes NO away/partial warning; a sibling case seeds a CONFIRMED window and asserts the warning still fires. Run — the declared case RED against the shipped both-statuses code (this is the D-14-21 proof).
    GREEN: flip the three query sites in direction_repository.go — line 767 (Coverage partial_abs) and line 775 (Coverage full_abs): AND aw.status IN ('declared','confirmed') → AND aw.status = 'confirmed'; line 816 (AbsenceWindows): same flip. Rewrite the port doc comments (ports/direction_repository.go: Coverage — capacity subtracts confirmed-only absence hours; AbsenceWindows — confirmed-only per D-14-21/D-13-29 closure) and the domain AbsenceWindow doc comment (direction.go:142-153) to confirmed-only. 
    Interim note (plan-sanctioned scoped build gate): the direction repo/service packages compile and the NEW subtests pass, but existing declared-seed tests are expected red until Task 2 flips them — do not chase those failures here.
  </action>
  <behavior>
    - Test 1 (RED→GREEN): declared window → zero warnings; confirmed window → away/partial warning per its hours (D-14-21 closure proof).
    - Test 2: the port docs state confirmed-only (doc change — verified by the executor's doc rewrite + compile).
  </behavior>
  <verify>
    <automated>go build ./internal/adapters/secondary/postgres/ ./internal/core/services/direction/ && go test ./internal/core/services/direction/ -run 'TestWarnings|Absence' -count=1</automated>
  </verify>
  <acceptance_criteria>The new declared-no-warning + confirmed-warns subtests pass; the three predicate sites read status = 'confirmed'; docs updated. (Full direction suites green after Task 2.)</acceptance_criteria>
  <done>AbsenceWindows + Coverage read confirmed-only; the D-14-21 behavioral proof exists and passes; docs pin the closure.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 13 test-seed flips + full-suite green</name>
  <files>internal/adapters/secondary/postgres/direction_repository_test.go, internal/core/services/direction/direction_test.go, internal/adapters/primary/http/direction_handler_test.go</files>
  <read_first>internal/adapters/secondary/postgres/direction_repository_test.go + internal/core/services/direction/direction_test.go + internal/adapters/primary/http/direction_handler_test.go (locate every declared-window seed used to assert warnings/coverage — research Pitfall 5 calls them out explicitly)</read_first>
  <action>
    Flip every declared-window seed in the three Phase 13 test files that drives an away/partial warning or a coverage subtraction assertion: the seedAvailabilityWindow/seed helper call sites change status 'declared' → 'confirmed' (the coverage read-model and the warning overlay consume confirmed-only now). Where a test specifically exercised the BOTH-statuses reading (e.g. a declared window warning on the plan read), convert it to the confirmed seed + keep the declared seed only where it now asserts the absence of a warning (add at least one handler-level declared-no-warning case to direction_handler_test.go if one does not already exist). Do NOT change any availability-independent assertion (mode gates, claims, supersede chains).
    Full verification: the three direction packages (postgres, services, http) green; then the full suite.
  </action>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestMigration|Direction' -count=1 && go test ./internal/core/services/direction/ ./internal/adapters/primary/http/ -run TestDirection -count=1</automated>
  </verify>
  <acceptance_criteria>All three direction suites green; declared-window seeds flipped to confirmed; at least one declared-window no-warning assertion exists at the service and/or handler level.</acceptance_criteria>
  <done>Phase 13 tests reflect the confirmed-only contract; the D-13-29 closure is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| availability_windows table→direction read path | the status predicate crosses here — declared (unconfirmed requests) must never reach the warning overlay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-10 | Tampering | direction warnings on unconfirmed absences | high | mitigate | Confirmed-only predicates at all three query sites (Task 1) + the declared-no-warning behavioral subtest (Task 1/2) — D-14-21 |
| T-14g-11 | Tampering | doc/port drift reintroducing both statuses | medium | mitigate | Port + domain doc rewrites pin confirmed-only (Task 1); the full-interface assertion + doc comments are the reader's contract |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestMigration|Direction' -count=1` — repo suite green after seed flips
- `go test ./internal/core/services/direction/ ./internal/adapters/primary/http/ -run TestDirection -count=1` — service + handler suites green
- `make test` — full suite green (cross-package check: the closure touches only direction packages; availability plans are additive)
</verification>

<success_criteria>
- AbsenceWindows + Coverage read confirmed-only; docs pin the closure
- Phase 13 test seeds flipped; declared-window no-warning proof present at the service level (and handler level where natural)
- All direction suites + full suite green
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified files:**
- `internal/adapters/secondary/postgres/direction_repository.go` — Coverage partial_abs (767) + full_abs (775) + AbsenceWindows (816) predicates flipped to `status = 'confirmed'`
- `internal/core/ports/direction_repository.go` — Coverage + AbsenceWindows doc comments rewritten to confirmed-only (D-14-21)
- `internal/core/domain/direction/direction.go` — AbsenceWindow doc updated
- `internal/adapters/secondary/postgres/direction_repository_test.go` / `internal/core/services/direction/direction_test.go` / `internal/adapters/primary/http/direction_handler_test.go` — declared-window seeds flipped to confirmed; declared no-warning subtests added
