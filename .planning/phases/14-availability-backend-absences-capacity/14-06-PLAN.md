---
phase: 14-availability-backend-absences-capacity
plan: 06
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - internal/adapters/secondary/postgres/contract_type_repository.go
  - internal/adapters/secondary/postgres/contract_type_repository_test.go
  - internal/core/services/availability/contract_types.go
  - internal/core/services/availability/contract_types_test.go
  - internal/adapters/primary/http/contract_types_handler.go
  - internal/adapters/primary/http/contract_types_handler_test.go
  - internal/adapters/primary/http/organization.go
  - internal/core/services/organization/organization.go
  - internal/core/ports/organization_management_repository.go
  - internal/adapters/secondary/postgres/organization_management_repository.go
  - internal/adapters/primary/http/handler_test_helper.go
  - cmd/server/main.go
  - cmd/server/main_test.go
autonomous: true
requirements: [AVAIL-01, AVAIL-02]
gap_closure: false
estimate:
  tokens: 20000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Contract types are hr-owned CRUD: create/edit/delete require models.RoleHR (403 otherwise), managers + employees read-only (D-14-27); deleting a type referenced by any membership is rejected (ErrContractTypeInUse — the FK blocks, D-14-28)."
    - "Contract-type validation is code-side (JSONB is not CHECK-able — the 022 convention): cadence in {week, month}, hours_per_period > 0, day_hours keys are ISO weekday strings '1'..'7' with 0 < hours <= 24, and a week-cadence type requires a non-empty matrix (D-14-16/17)."
    - "The fallback chain resolves per employee: membership contract_type_id + day_hours_override (override merges over the type matrix — override wins per weekday) → org default contract_type via the org_settings key default_contract_type_id → 8h × Mon-Fri; the resolution level is returned in the response (D-14-18)."
    - "The override attaches through the membership endpoint family: PUT /organizations/members/{member_id}/schedule (hr-gated, D-14-29) sets contract_type_id + day_hours_override on organization_memberships with an audit row (entity_type 'organization_membership', action 'schedule_updated', {before, after}) in the same tx."
    - "The org default schedule is set via the existing PUT /organizations/settings with key default_contract_type_id (D-14-18 discretion — org_settings reference, no uniqueness machinery); a stored default that is missing/wrong-org/unparsable surfaces ErrInvalidValue (the ResolvePlanningMode precedent)."
  artifacts:
    - internal/adapters/secondary/postgres/contract_type_repository.go
    - internal/core/services/availability/contract_types.go
    - internal/adapters/primary/http/contract_types_handler.go
  key_links:
    - "ResolveSchedule ↔ capacity read-model (14-07) — the capacity plan consumes the schedule per employee + resolution level"
    - "membership contract_type_id/override ↔ 024 migration columns (14-01)"
    - "org_settings default_contract_type_id ↔ the orgsettings key/value store (Phase 13)"
flagged_assumptions:
  - statement: "Prohibition recall (D-14-29, 14-CONTEXT): the override is attached via the existing membership/org endpoint family (Phase 10 territory) — a new PUT /organizations/members/{member_id}/schedule route in the org handler is the extension; no separate /availability schedule surface for membership writes."
    status: unverified
    flagged: true
  - statement: "Discretion documented: day_hours_override is a partial JSONB map merged over the type matrix (override wins per weekday); an override without a contract_type merges over the 8×5 fallback base — the D-14-16 'most complex case' (different days or different hours on the same day)."
    status: unverified
    flagged: true
---

<objective>
The work-schedule model (D-14-16..18): contract_types CRUD (hr-owned, D-14-27/28), the per-employee fallback-chain resolution (override → contract_type → org default → 8×5), and the membership-override attachment (D-14-29) via the org membership endpoint family.

Purpose: capacity (14-07) is meaningless without a per-employee hours basis; the two-tier contract model (reusable type + per-employee day-hours override) is the user's explicitly named most-complex case. This plan lands the full management surface so the fallback chain — the capacity basis — is complete and hr-operable before the read-model math lands.

Scope note (conscious acceptance, checker WARNING — 13 files, threshold 10): Task 3 spans the contract-types surface AND the membership-schedule endpoint because both share the org handler/service/port/repo chain, the fixture, and main.go wiring — splitting them would duplicate the orgsvc constructor-change ripple and the wiring work across two plans. The plan accepts the single 13-file task (with the constructor fix in one place) instead of a split.

Output: contract-type CRUD (repo + service + handler + routes), ResolveSchedule with resolution levels, membership schedule endpoint, org-default key path, all tests green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@internal/core/services/orgsettings/orgsettings.go
@internal/core/services/organization/organization.go
@internal/adapters/primary/http/organization.go
@internal/adapters/secondary/postgres/org_settings_repository.go
@internal/core/ports/organization_management_repository.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract-type repo CRUD — hr surface + hard delete</name>
  <files>internal/adapters/secondary/postgres/contract_type_repository.go, internal/adapters/secondary/postgres/contract_type_repository_test.go</files>
  <read_first>internal/adapters/secondary/postgres/availability_repository.go (14-03 — the package type + insertAvailabilityAudit helper to reuse), internal/adapters/secondary/postgres/org_settings_repository.go (in-tx audit upsert shape 121-146), internal/adapters/secondary/postgres/exported_test_helpers.go (seedContractType from 14-01)</read_first>
  <action>
    RED: contract_type_repository_test.go batteries (SetupTestSchema + seedContractType): List returns the org's types; Create inserts + audit row (entity_type 'contract_type', action 'created'); Update mutates name/cadence/hours_per_period/day_hours + audit {before, after}; Delete on an unreferenced type succeeds + audit 'deleted'; Delete on a type referenced by a seeded membership → ErrContractTypeInUse (FK violation mapped — the wrapPGError 23503 branch, D-14-28); cross-org id → ErrContractTypeNotFound (no existence oracle). Run — RED.
    GREEN: contract_type_repository.go — methods on the *AvailabilityRepository type (single port surface per 14-02): ListContractTypes (org-scoped SELECT), CreateContractType (INSERT + in-tx audit), UpdateContractType (SELECT FOR UPDATE → no row → ErrContractTypeNotFound → UPDATE + audit {before, after}), DeleteContractType (SELECT FOR UPDATE → DELETE — 23503 FK violation maps to ErrContractTypeInUse via wrapPGError; audit 'deleted' in-tx). The compile-time full-interface assertion in availability_repository.go keeps the port contract enforced.
  </action>
  <behavior>
    - Test 1: create → row + audit 'created'; list returns org-scoped types only.
    - Test 2: update → changed fields + audit {before, after}; missing id → ErrContractTypeNotFound.
    - Test 3: delete unreferenced → gone + audit 'deleted'; delete referenced → ErrContractTypeInUse (FK blocks, D-14-28).
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run 'TestContractType' -count=1</automated>
  </verify>
  <acceptance_criteria>CRUD + hard-delete batteries green; FK-in-use mapped to ErrContractTypeInUse; audit rows in-tx.</acceptance_criteria>
  <done>Contract-type persistence shipped with the hr CRUD surface and FK-safe hard delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service validation + ResolveSchedule fallback chain</name>
  <files>internal/core/services/availability/contract_types.go, internal/core/services/availability/contract_types_test.go</files>
  <read_first>internal/core/services/orgsettings/orgsettings.go (ResolvePlanningMode 62-95 — the fallback-chain shape: membership override → org key → hardcoded default; invalid stored value → ErrInvalidValue), internal/core/services/availability/availability.go (14-03 constructor — the orgSettingsSvc + orgRepo deps), 14-PATTERNS.md (Pattern 4 — resolution level documented in the response)</read_first>
  <action>
    RED: service unit tests (mock-driven — MockAvailabilityRepo contract-type maps + mock org repo + mock settings repo): validation rejections (cadence 'day' → ErrInvalidRequest; hours_per_period 0 → ErrInvalidRequest; day_hours key '0'/'8' → ErrInvalidRequest; hour 25 → ErrInvalidRequest; week cadence with nil matrix → ErrInvalidRequest); ResolveSchedule: membership with override → override-merged matrix + level "override"; membership type only → type matrix + level "type"; no membership config but org_settings default_contract_type_id set → default type + level "org_default"; nothing configured → 8×5 fallback + level "fallback"; stored default id missing/wrong-org/unparsable → ErrInvalidValue; override merges (type matrix Mon-Fri 8h, override {Mon: 6h} → Mon 6h, Tue 8h). Run — RED.
    GREEN: contract_types.go — validation helpers (cadence vocab, hours > 0, day-hours keys "1".."7" with 0 < h <= 24, week-cadence requires non-empty matrix — the domain helpers from 14-02), the CRUD service methods with the hr gate (role != string(models.RoleHR) → ErrForbidden — the orgsettings.go:128 shape) and audit DTOs, and ResolveSchedule(ctx, orgID, employeeID) returning ScheduleResolution{DayHours map[string]float64, Source string, ContractTypeID *uuid.UUID}: membership contract_type_id + day_hours_override (JSONB unmarshal, merge override-wins-per-weekday); else org_settings key default_contract_type_id (string → uuid.Parse; missing/wrong-org → ErrInvalidValue per the ResolvePlanningMode precedent); else the 8×5 fallback. Level constants "override"|"type"|"org_default"|"fallback" exported for the capacity response (D-14-18).
  </action>
  <behavior>
    - Test 1: table-driven validation rejections → ErrInvalidRequest with zero repo calls.
    - Test 2: ResolveSchedule override → merged matrix + "override"; type-only → "type"; org default → "org_default"; none → 8×5 + "fallback".
    - Test 3: invalid stored org default → ErrInvalidValue; hr gate: non-hr create/update/delete → ErrForbidden.
  </behavior>
  <verify>
    <automated>go test ./internal/core/services/availability/ -run 'TestContractType|TestResolveSchedule' -count=1</automated>
  </verify>
  <acceptance_criteria>Validation + fallback-chain unit tests green; resolution levels exported and asserted; invalid stored default surfaces ErrInvalidValue.</acceptance_criteria>
  <done>Schedule resolution shipped — the capacity basis with its full fallback chain and documented resolution levels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Membership schedule endpoint + contract-types routes + wiring</name>
  <files>internal/adapters/primary/http/contract_types_handler.go, internal/adapters/primary/http/contract_types_handler_test.go, internal/adapters/primary/http/organization.go, internal/core/services/organization/organization.go, internal/core/ports/organization_management_repository.go, internal/adapters/secondary/postgres/organization_management_repository.go, internal/adapters/primary/http/handler_test_helper.go, cmd/server/main.go, cmd/server/main_test.go</files>
  <read_first>internal/adapters/primary/http/organization.go (UpdateMemberRoles handler — the org-membership write shape), internal/core/services/organization/organization.go (UpdateMemberRoles service — role gate + repo call), internal/adapters/secondary/postgres/organization_management_repository.go (UpdateMemberRoles SQL), internal/adapters/primary/http/handler_test_helper.go (fixture route block), cmd/server/main.go (route block 232-244)</read_first>
  <action>
    RED: contract_types_handler_test.go integration battery: POST /availability/contract-types (hr) → 200 + audit; POST as manager → 403; GET as manager/employee → 200 (read-only, D-14-27); PUT update → 200; DELETE referenced → 409, unreferenced → 200; PUT /organizations/members/{member_id}/schedule (hr) → 200 + membership updated + audit 'schedule_updated'; as non-hr → 403; invalid contract_type_id (cross-org/missing) → 404/400. Run — RED (routes + handler absent).
    GREEN:
    1. contract_types_handler.go — methods on *AvailabilityHandler (ListContractTypes/CreateContractType/UpdateContractType/DeleteContractType) with the writeError sentinel additions (ErrContractTypeNotFound → 404, ErrContractTypeInUse → 409, ErrInvalidRequest → 400, ErrForbidden → 403) + routes: GET/POST /availability/contract-types, PUT/DELETE /availability/contract-types/{id} (D-14-25 discretion: contract-types CRUD under /availability).
    2. Membership schedule extension (D-14-29): organization.go handler + organization service + organization_management_repository: PUT /organizations/members/{member_id}/schedule — hr-gated (models.RoleHR), request {contract_type_id, day_hours_override}; the service validates the type is same-org (via the availability service's contract-type read or a repo lookup — use the availability service ListContractTypes to avoid a second repo; the service method signature takes role, orgID, memberID, and the availability service for validation), then repo.UpdateMembershipSchedule: UPDATE organization_memberships SET contract_type_id, day_hours_override + in-tx audit (entity_type 'organization_membership', action 'schedule_updated', {before, after})     — mirror the org_settings in-tx audit shape. The orgsvc.NewService constructor gains the audit writer parameter — update ALL THREE call sites in this task: cmd/server/main.go:122, internal/adapters/primary/http/handler_test_helper.go:85, and cmd/server/main_test.go:125 (compile-forced). cmd/server/main_test.go is NOT deferred to 14-08 — updating it here keeps `go test ./cmd/server/` and `make test` green from the end of this wave; 14-08 Task 3 only verifies the call sites.
    3. Wiring: routes in cmd/server/main.go + fixture mirror; org-default path: PUT /organizations/settings key default_contract_type_id is already served by the Phase 13 orgsettings endpoint (no new route — the availability service reads it via orgSettingsSvc in ResolveSchedule).
  </action>
  <behavior>
    - Test 1: contract-types CRUD permission matrix (hr 200 / manager 403 write, read-only OK); delete referenced → 409.
    - Test 2: membership schedule PUT — hr 200 + membership row updated + audit; non-hr 403; cross-org type id → 400/404.
    - Test 3: org default via PUT /organizations/settings default_contract_type_id → ResolveSchedule consumes it (service-level assertion in Task 2 covers the read; here the route exists and stores).
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run 'TestContractType|TestAvailabilityMembership' -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>Integration battery green; membership schedule write audited in-tx; all three orgsvc constructor call sites updated (cmd/server/main.go:122, handler_test_helper.go:85, cmd/server/main_test.go:125) — repo compiles end-to-end and `go test ./cmd/server/` stays green.</acceptance_criteria>
  <done>The schedule management surface is live: contract-types CRUD, membership override write, org-default key — all hr-gated and audited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | contract-type payloads + membership schedule writes cross here — hr gates and code-side JSONB validation are the only guards |
| service→orgsettings | the org default key crosses here — stored corruption must surface as ErrInvalidValue, never silently default |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-16 | Elevation of Privilege | contract-type mutation by non-hr | high | mitigate | models.RoleHR gates on create/update/delete (Task 2/3, D-14-27) + battery |
| T-14g-17 | Elevation of Privilege | membership schedule write by non-hr | high | mitigate | hr gate on PUT /organizations/members/{member_id}/schedule + audit in-tx (Task 3, D-14-29) |
| T-14g-18 | Tampering | invalid schedule data (bad cadence/hours/day keys) | medium | mitigate | Code-side validation (domain helpers) before any write — the 022 JSONB convention (Task 2) |
| T-14g-19 | Tampering | org-default corruption silently defaulting | medium | mitigate | Invalid stored default_contract_type_id → ErrInvalidValue (ResolvePlanningMode precedent, Task 2) |
| T-14g-20 | Tampering | FK-violation on delete → 500 | medium | mitigate | 23503 mapped to ErrContractTypeInUse → 409 (Task 1, D-14-28) |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run 'TestContractType' -count=1` — repo CRUD green
- `go test ./internal/core/services/availability/ -run 'TestContractType|TestResolveSchedule' -count=1` — service green
- `go test ./internal/adapters/primary/http/ -run 'TestContractType|TestAvailabilityMembership' -count=1` — handler battery green
- `go build ./...` — repo-wide compile (constructor changes)
</verification>

<success_criteria>
- Contract-type CRUD live and hr-gated with FK-safe hard delete
- ResolveSchedule fallback chain complete with resolution levels + invalid-default surfacing
- Membership override + org-default paths live and audited
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `internal/adapters/secondary/postgres/contract_type_repository.go` — List/Create/Update/DeleteContractType on *AvailabilityRepository (audit in-tx, 23503 → ErrContractTypeInUse)
- `internal/adapters/secondary/postgres/contract_type_repository_test.go`
- `internal/core/services/availability/contract_types.go` — validation, hr-gated CRUD, ResolveSchedule (override → type → org default → 8×5) with exported level constants
- `internal/core/services/availability/contract_types_test.go`
- `internal/adapters/primary/http/contract_types_handler.go` — contract-type routes on *AvailabilityHandler
- `internal/adapters/primary/http/contract_types_handler_test.go`

**Modified files:**
- `internal/adapters/primary/http/organization.go` + `internal/core/services/organization/organization.go` + `internal/core/ports/organization_management_repository.go` + `internal/adapters/secondary/postgres/organization_management_repository.go` — UpdateMembershipSchedule (hr-gated, audit in-tx)
- `internal/adapters/primary/http/handler_test_helper.go` + `cmd/server/main.go` + `cmd/server/main_test.go` — contract-type + schedule routes, all three orgsvc constructor call sites
