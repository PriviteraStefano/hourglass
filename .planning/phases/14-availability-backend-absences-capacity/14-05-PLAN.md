---
phase: 14-availability-backend-absences-capacity
plan: 05
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - internal/adapters/secondary/postgres/availability_repository.go
  - internal/adapters/secondary/postgres/availability_repository_test.go
  - internal/core/services/availability/availability.go
  - internal/core/services/availability/availability_test.go
autonomous: true
requirements: [AVAIL-02]
gap_closure: false
estimate:
  tokens: 22000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Confirm: only the employee's resolved unit manager (routing.ResolveUnitManager, BE-014 — no WG line, D-14-01) can confirm a declared window → 'confirmed' + audit row; a non-manager gets 403; when the employee IS the resolved unit manager, self-confirmation succeeds (D-14-04); an HR-role actor who is not the resolved unit manager gets 403 (HR curates, never confirms — D-14-03); only 'declared' windows are confirmable — anything else 409 (D-14-08)."
    - "Reject: requires a reason (400 without — D-14-09); 'rejected' is terminal with the reason persisted + audit payload {reason}; rejected windows can never be confirmed, edited, or withdrawn (409 on any transition)."
    - "Withdraw: declared-only, window owner only (403 for others — D-14-10); 'withdrawn' is a terminal status, never a hard delete, audit-logged — history stays intact (BE-012)."
    - "HR medical edit (D-14-11): PUT only on medical windows (non-medical → 400 ErrNotMedical), hr-gated (403 otherwise), updates dates + certificate_ref with audit {before, after} (D-14-12)."
    - "Certificate attach (D-14-06/07): hr-gated POST, medical windows only (400 otherwise), multipart upload capped at 5 MB with the PDF/JPEG/PNG MIME allowlist (400 on oversize/wrong type — the expense.go gate shape, never the disk storage), BYTEA row + audit action certificate_attached in the same tx."
    - "Every mutator re-validates the matrix under FOR UPDATE inside its transaction with a status-precondition UPDATE backstop; a failed audit insert rolls back the state change (CR-01, BE-012/BE-016)."
  artifacts:
    - internal/adapters/secondary/postgres/availability_repository.go (Confirm/Reject/Withdraw/UpdateMedical/AttachCertificate)
    - internal/core/services/availability/availability.go (orchestration + gates)
  key_links:
    - "Confirm/reject authority ↔ routing.ResolveUnitManager (shared instance, no re-implementation — D-G parity)"
    - "Rejection reason ↔ 023 2VL CHECK (14-01) — service fast-fail + DB backstop"
    - "AttachCertificate ↔ 025 table (14-01) + handler MIME/size gate (14-08)"
flagged_assumptions:
  - statement: "Unclassified edge (AVAIL-02, specless probe — surfaced, never auto-resolved): 'Manager/HR can confirm' is pinned by locked D-14-03 as manager-confirms + HR-curates; HR-role actors confirm ONLY when they are the resolved unit manager (D-14-04 self-confirm covers the employee-as-manager case). The HR-never-confirms 403 is asserted in the service tests and the 14-08 handler battery."
    status: unverified
    flagged: true
---

<objective>
The AVAIL-02 lifecycle: confirm/reject/withdraw mutators, the HR medical-edit carve-out, and the certificate-attach path — repo transactions (CR-01 matrix re-check + in-tx audit) plus the service orchestration (unit-manager authority via routing.ResolveUnitManager, self-confirm, terminal-state guards, hr gates).

Purpose: expands the phase tracer's proven transaction shapes into the full lifecycle. The confirm/reject authority is the locked D-14-01..04 decision set; rejected/withdrawn are terminal per D-14-08..10; nobody edits windows except HR-on-medical per D-14-11; medical documents land in the DB-backed attachment table per D-14-06/07 — every event audit-logged in-tx per D-14-12.

Output: five repo mutators + service orchestration + unit tests, all green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@.planning/phases/14-availability-backend-absences-capacity/14-VALIDATION.md
@internal/adapters/secondary/postgres/availability_repository.go
@internal/core/services/availability/availability.go
@internal/adapters/secondary/postgres/direction_repository.go
@internal/core/services/routing/routing.go
@internal/adapters/primary/http/expense.go
@internal/core/services/direction/direction_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repo Confirm/Reject/Withdraw — CR-01 transitions + terminal guards</name>
  <files>internal/adapters/secondary/postgres/availability_repository.go, internal/adapters/secondary/postgres/availability_repository_test.go</files>
  <read_first>internal/adapters/secondary/postgres/direction_repository.go (Activate tx 265-313 + cancelWithGuard 339-400 — the CR-01 skeleton: FOR UPDATE row → matrix re-check → UPDATE with the locked status as WHERE precondition → audit in-tx → re-read → commit), internal/adapters/secondary/postgres/availability_repository.go (Declare + insertAvailabilityAudit from 14-03)</read_first>
  <action>
    RED: repo tests first (availability_repository_test.go, seeded windows via seedAvailabilityWindowWithCert from 14-01): confirm happy path (declared → confirmed + audit row action confirmed); confirm on confirmed/rejected/withdrawn → ErrInvalidTransition (409-path); confirm on missing/cross-org id → ErrWindowNotFound (404-path, no existence oracle); reject without reason → ErrRejectReasonRequired (400-path); reject with reason → status rejected + rejection_reason persisted + audit {reason}; reject on confirmed → ErrInvalidTransition; withdraw on declared → withdrawn + audit (D-14-10); withdraw on confirmed/rejected → ErrInvalidTransition; withdraw by any caller — owner gating is service-side. Run — RED (methods absent).
    GREEN: implement per the Activate skeleton: Confirm — BeginTx, SELECT status FROM availability_windows WHERE id AND org_id FOR UPDATE (ErrNoRows → ErrWindowNotFound), CanTransition(currentStatus, StatusConfirmed) → ErrInvalidTransition, UPDATE ... SET status='confirmed' WHERE id AND org_id AND status = currentStatus (RowsAffected 0 → ErrWindowNotFound backstop), insertAvailabilityAudit in-tx, re-read, commit. Reject — reason fast-fail at the repo boundary first (empty → ErrRejectReasonRequired, the cancelWithGuard 340-344 shape), matrix re-check against StatusRejected, UPDATE SET status='rejected', rejection_reason = reason; audit {reason}. Withdraw — matrix re-check against StatusWithdrawn, UPDATE SET status='withdrawn'; audit (payload {reason} only when the service supplies one — withdraw has no reason per D-14-10, so nil payload). All three: in-tx audit write that rolls back the state change on failure (BE-012).
  </action>
  <behavior>
    - Test 1: confirm declared → confirmed + audit; confirm confirmed/rejected/withdrawn → ErrInvalidTransition; confirm missing id → ErrWindowNotFound.
    - Test 2: reject '' → ErrRejectReasonRequired; reject with reason → rejected + rejection_reason persisted + audit payload {reason}; reject non-declared → ErrInvalidTransition.
    - Test 3: withdraw declared → withdrawn + audit; withdraw confirmed/rejected → ErrInvalidTransition.
    - Test 4: audit failure on any mutator rolls back the status change.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1</automated>
  </verify>
  <acceptance_criteria>All transition batteries green; terminal states (rejected/withdrawn) reject every further transition with ErrInvalidTransition; reason required at the repo boundary; audit-in-tx proven by the rollback battery.</acceptance_criteria>
  <done>Confirm/Reject/Withdraw transactions shipped with matrix re-check, status-precondition backstop, and in-tx audit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repo UpdateMedical + AttachCertificate — HR curation and BYTEA storage</name>
  <files>internal/adapters/secondary/postgres/availability_repository.go, internal/adapters/secondary/postgres/availability_repository_test.go</files>
  <read_first>internal/adapters/secondary/postgres/availability_repository.go (Declare tx + insertAvailabilityAudit from 14-03), migrations/025_certificate_attachments.up.sql (the table shape from 14-01), 14-PATTERNS.md (HR edit + attach section)</read_first>
  <action>
    RED: repo tests — UpdateMedical on a medical window (dates + certificate_ref change) → updated row + audit {before, after} with action edited (D-14-11/12); UpdateMedical on a non-medical window → ErrNotMedical (the repo re-checks kind in-tx — the service fast-fail is UX only); UpdateMedical on missing id → ErrWindowNotFound; AttachCertificate (medical window, storage bytes + content_type + size) → attachment row lands + audit action certificate_attached; AttachCertificate on non-medical → ErrNotMedical; attach twice → a second row lands (append, no uniqueness — document the read path in 14-08). Run — RED.
    GREEN: UpdateMedical — BeginTx, SELECT kind, dates, certificate_ref FROM availability_windows WHERE id AND org_id FOR UPDATE, kind != 'medical' → ErrNotMedical, UPDATE starts_on/ends_on/certificate_ref, audit {before, after} in-tx, re-read, commit. AttachCertificate — BeginTx, SELECT kind FOR UPDATE (ErrNoRows → ErrWindowNotFound, non-medical → ErrNotMedical), INSERT INTO certificate_attachments (id, org_id, entity_type='availability_window', entity_id, content_type, size_bytes, storage, created_by, created_at) — BYTEA bytes passed through pgx (D-14-07), audit action certificate_attached in-tx, commit. Size/MIME enforcement is the handler's job (14-08) — the repo stores what it is given; the port doc notes the 5 MB + allowlist contract.
  </action>
  <behavior>
    - Test 1: medical edit updates dates + certificate_ref, audit {before, after}; non-medical edit → ErrNotMedical; missing id → ErrWindowNotFound.
    - Test 2: certificate attach inserts the BYTEA row (content_type/size/storage round-trip) + audit certificate_attached; non-medical → ErrNotMedical; second attach appends.
    - Test 3: audit failure on either mutator rolls back the write.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1</automated>
  </verify>
  <acceptance_criteria>HR-edit + attach batteries green; the medical-only in-tx check proven; BYTEA round-trip asserted (bytes read back equal bytes written).</acceptance_criteria>
  <done>HR medical curation + certificate attach transactions shipped with in-tx audit and DB-backed storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service orchestration — authority resolution, self-confirm, hr gates</name>
  <files>internal/core/services/availability/availability.go, internal/core/services/availability/availability_test.go</files>
  <read_first>internal/core/services/routing/routing.go (ResolveUnitManager 109-134 — the confirm authority, shared instance), internal/core/services/direction/direction_test.go (fixture: routing.NewService with mocks + unit membership seeding), internal/core/services/testdata/mock_availability_repo.go + mock_direction_repo.go (14-02 shapes), 14-PATTERNS.md (role gates section 135-148)</read_first>
  <action>
    RED: service unit tests (mock-driven, routing.NewService over mock wg/activity/unit repos + orgsettingssvc over the mock settings repo — the direction_test.go fixture shape). Confirm: employee whose resolved unit manager is actor → confirmed (repo called with audit); non-manager → ErrForbidden (repo NOT called); employee IS the resolved unit manager (self-confirm, D-14-04) → allowed; no manager found → ErrForbidden; hr-role actor who is not the resolved manager → ErrForbidden (D-14-03 — HR never confirms). Reject: reason empty → ErrRejectReasonRequired before the repo; authority same as confirm. Withdraw: owner only — non-owner → ErrForbidden; owner → repo called (declared-only is the repo's matrix check). UpdateMedical/AttachCertificate: role != models.RoleHR → ErrForbidden; non-medical → ErrNotMedical (service fast-fail parity with the repo in-tx check). Run — RED.
    GREEN: implement the service methods: authority resolution — resolve the employee's unit memberships via unitRepo.ListMembershipsForUser (the unit_repository.go:33 port method), then routing.ResolveUnitManager per unit; the actor may confirm/reject if they are the resolved manager of ANY of the employee's units (discretion: any-of, documented in the service comment); self-confirm falls out of the same check (D-14-04). Every mutator builds its audit DTO (actor, action, payload {reason} for reject — the direction.go:439 shape) and hands it to the repo for the in-tx write. UpdateMedical/AttachCertificate gates compare role != string(models.RoleHR) → ErrForbidden (the orgsettings.go:128 gate shape — never a literal).
  </action>
  <behavior>
    - Test 1: confirm by resolved unit manager → success + audit capture; non-manager → ErrForbidden; self-confirm (employee == resolved manager) → success; no-manager → ErrForbidden; hr non-manager → ErrForbidden.
    - Test 2: reject '' → ErrRejectReasonRequired pre-repo; reject with reason → rejected + audit {reason}.
    - Test 3: withdraw non-owner → ErrForbidden; owner → success + audit.
    - Test 4: non-hr UpdateMedical/AttachCertificate → ErrForbidden; hr on non-medical → ErrNotMedical; hr on medical → repo called with audit.
  </behavior>
  <verify>
    <automated>go test ./internal/core/services/availability/ -count=1</automated>
  </verify>
  <acceptance_criteria>Service unit tests green; the authority matrix (manager/self/hr/non-manager) fully covered; audit DTOs asserted via the mock's Audits capture; no role string literals.</acceptance_criteria>
  <done>Lifecycle orchestration shipped: unit-manager authority, self-confirm, terminal guards, hr gates — all unit-tested against the mock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | confirm/reject/withdraw/HR-edit/certificate-upload requests cross here — authority and role gates must hold before any repo write |
| service→routing | the unit-tree upward walk resolves the confirm authority — a wrong resolution is an authorization bypass |
| handler→service→repo | certificate upload bytes cross here — MIME/size enforcement at the handler (14-08), storage in the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-04 | Elevation of Privilege | confirm/reject by non-unit-manager (incl. HR) | high | mitigate | routing.ResolveUnitManager authority + no-manager → ErrForbidden + HR-never-confirms (Task 3, D-14-01/03/04) + handler battery in 14-08 |
| T-14g-05 | Tampering | transition on terminal/rejected states | high | mitigate | In-tx matrix re-check under FOR UPDATE + status-precondition UPDATE backstop (Task 1, CR-01) |
| T-14g-12 | Tampering | reject-without-reason | high | mitigate | Service fast-fail + repo boundary fast-fail + 023 2VL CHECK backstop (Tasks 1/3, D-14-09) |
| T-14g-13 | Elevation of Privilege | HR edit / certificate attach by non-hr | high | mitigate | models.RoleHR gates (Task 3, D-14-03/26) + non-medical → ErrNotMedical |
| T-14g-14 | Denial of Service | oversized / wrong-MIME certificate upload | medium | mitigate | Handler MaxBytesReader 5 MB cap + PDF/JPEG/PNG allowlist (14-08 Task 1, expense.go gate shape); repo stores what the port contract allows |
| T-14g-15 | Tampering | audit suppression on lifecycle events | high | mitigate | Every mutator writes its audit row in the same tx; rollback battery (Tasks 1/2, BE-012) |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1` — mutator batteries green
- `go test ./internal/core/services/availability/ -count=1` — orchestration unit tests green
- `go build ./...` — repo-wide compile
</verification>

<success_criteria>
- Confirm/reject/withdraw transactions green with matrix re-check, terminal guards, and in-tx audit
- HR medical edit + certificate attach green with medical-only in-tx checks and BYTEA round-trip
- Service authority matrix (unit manager / self / hr-never-confirms / no-manager) fully unit-tested
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified files:**
- `internal/adapters/secondary/postgres/availability_repository.go` — +Confirm, +Reject (reason fast-fail + rejection_reason SET), +Withdraw, +UpdateMedical ({before, after} audit), +AttachCertificate (BYTEA insert + audit) — all CR-01 txs
- `internal/adapters/secondary/postgres/availability_repository_test.go` — transition/terminal/reason/rollback/medical-edit/attach batteries
- `internal/core/services/availability/availability.go` — +Confirm/Reject/Withdraw/UpdateMedical/AttachCertificate orchestration (ResolveUnitManager authority, self-confirm, role gates, audit DTOs)
- `internal/core/services/availability/availability_test.go` — authority-matrix + gate unit tests
