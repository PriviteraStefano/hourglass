---
phase: 14-availability-backend-absences-capacity
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - internal/adapters/secondary/postgres/availability_repository.go
  - internal/adapters/secondary/postgres/availability_repository_test.go
  - internal/core/services/availability/availability.go
  - internal/core/services/availability/availability_test.go
  - internal/adapters/primary/http/availability_handler.go
  - internal/adapters/primary/http/availability_handler_test.go
  - internal/adapters/primary/http/handler_test_helper.go
  - cmd/server/main.go
autonomous: true
requirements: [AVAIL-01]
gap_closure: false
estimate:
  tokens: 24000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An authenticated org member can declare a holiday/permit/unavailable window via POST /availability/windows and receives 200 with status 'declared' plus an in-tx audit row (entity_type 'availability_window', action 'declared', D-14-12) — the phase tracer proves the path end-to-end through repo (overlap guard + audit in-tx), service (validation + role gates), handler, route, and wiring (AVAIL-01)."
    - "Invalid declare input never 500s: bad kind, ends_on < starts_on, hours > 99.99, sub-cent hours, missing fields → 400 (empty-input edge: absent kind/date payload → 400); medical without certificate_ref → 400 (D-14-05)."
    - "Overlapping declare → 409: the guard counts only declared+confirmed windows of the same user (active-only, kind-blind, D-14-13) with inclusive date-range intersection (adjacency edge: a window ending the day before another starts does NOT overlap; sharing ANY calendar day does, D-14-14); withdrawn/rejected windows never block."
    - "Concurrent overlapping declares: exactly one succeeds, the other gets 409 — the overlap guard runs inside the declare transaction under FOR UPDATE (CR-01, D-14-15)."
    - "Medical declare auto-confirms at declare (status 'confirmed', D-14-02) with certificate_ref required (D-14-05) and declared + confirmed audit rows."
    - "Role gates on declare: employee declares for self; hr declares for anyone; employee declaring for another user → 403 (D-14-26)."
  artifacts:
    - internal/adapters/secondary/postgres/availability_repository.go (Declare)
    - internal/core/services/availability/availability.go (Service + Declare)
    - internal/adapters/primary/http/availability_handler.go (Declare + writeError)
    - cmd/server/main.go + internal/adapters/primary/http/handler_test_helper.go (declare route + fixture)
  key_links:
    - "Repo Declare overlap guard ↔ service fast-fail — the repo check under FOR UPDATE is authoritative (CR-01 closure, D-14-15)"
    - "Domain status constants (14-02) ↔ INSERT status values — medical branch writes 'confirmed' per D-14-02"
    - "Audit rows ↔ audit_logs table (017) via insertAvailabilityAudit in the same tx — a failed audit insert rolls back the window (BE-012)"
flagged_assumptions:
  - statement: "Unclassified edge (AVAIL-02, specless probe): 'Manager/HR can confirm' reads as manager-confirms + HR-curates per locked D-14-03 — an HR-role actor who is not the resolved unit manager gets 403 on confirm. Surfaced here as a flagged assumption; enforced by tests in 14-05/14-08."
    status: unverified
    flagged: true
---

<objective>
The phase tracer: the absence DECLARE path end-to-end — one entry point (POST /availability/windows) through the repo (overlap guard + medical auto-confirm + in-tx audit), the service (validation + role gates), the handler, route registration, and wiring, proven by the first integration battery (AVAIL-01).

Purpose: proves the architecture on the agent's best early-context tokens — the CR-01 overlap transaction, the medical branch, and the audit-in-tx pattern are the correctness heart of the whole phase; if they work here, the remaining lifecycle mutators (14-05) are mechanical expansions of proven shapes. Stubs are allowed only where fill-in needs no architectural change.

Output: the declare vertical + its repo batteries (concurrency race, audit rollback) + service unit tests, all green.
</objective>

<execution_context>
@/Users/stefanoprivitera/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/stefanoprivitera/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-availability-backend-absences-capacity/14-CONTEXT.md
@.planning/phases/14-availability-backend-absences-capacity/14-RESEARCH.md
@.planning/phases/14-availability-backend-absences-capacity/14-PATTERNS.md
@.planning/phases/14-availability-backend-absences-capacity/14-VALIDATION.md
@internal/adapters/secondary/postgres/direction_repository.go
@internal/core/services/direction/direction.go
@internal/adapters/primary/http/direction_handler.go
@internal/core/services/orgsettings/orgsettings.go
@internal/adapters/primary/http/handler_test_helper.go
@cmd/server/main.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (TRACER): Declare vertical — failing battery first, then repo → service → handler → route → wiring</name>
  <files>internal/adapters/secondary/postgres/availability_repository.go, internal/core/services/availability/availability.go, internal/adapters/primary/http/availability_handler.go, internal/adapters/primary/http/availability_handler_test.go, internal/adapters/primary/http/handler_test_helper.go, cmd/server/main.go</files>
  <read_first>internal/adapters/secondary/postgres/direction_repository.go (Create tx 164-256 — the CR-01 skeleton: BeginTx → SELECT FOR UPDATE → re-check → INSERT → in-tx audit → re-read → commit; insertDirectionAudit 119-144), internal/adapters/primary/http/direction_handler.go (claims 72-93, writeError 304-323), internal/adapters/primary/http/handler_test_helper.go (fixture wiring 50-300), cmd/server/main.go (direction wiring 177-187 + route block 284-290), internal/core/services/orgsettings/orgsettings.go (manager-gate shape 126-130)</read_first>
  <action>
    RED — one path, no batching: write the first integration battery in availability_handler_test.go following the direction_handler_test.go shape (newHandlerFixture + registerAndLogin; seed the org/user/unit chain): declare-holiday → 200 status 'declared' + audit row; validation 400s (bad kind, ends_on < starts_on, hours 100.00, hours 4.005, empty payload, missing dates); overlap → 409 (seed a declared window, declare overlapping); medical without certificate_ref → 400, medical with ref → 200 status 'confirmed'. Run — RED (routes not registered: 404, plus the service/repo/handler files do not exist).
    GREEN in dependency order:
    1. Repo Declare (availability_repository.go): BeginTx → the authoritative overlap guard: SELECT aw.id FROM availability_windows aw WHERE aw.org_id = $1 AND aw.user_id = $2 AND aw.status IN ('declared','confirmed') AND aw.starts_on <= $4::date AND aw.ends_on >= $3::date ORDER BY aw.id LIMIT 1 FOR UPDATE — any row → availabilitydomain.ErrOverlap (D-14-13/14/15; the predicate is the shipped AbsenceWindows shape, direction_repository.go:817, inverted boundary-correct: adjacency does not overlap); INSERT the window (kind vocabulary from the domain; medical → status 'confirmed', else 'declared' — D-14-02); insertAvailabilityAudit in-tx (org_settings_repository.go:121-146 shape; entity_type from the domain constant — the declared row always; the confirmed row when medical, D-14-12); in-tx re-read; commit. Compile-time assertion var _ ports.AvailabilityRepository = (*AvailabilityRepository)(nil).
    2. Service (services/availability/availability.go): constructor NewService(availRepo ports.AvailabilityRepository, orgRepo ports.OrganizationRepository, unitRepo ports.UnitRepository, wgRepo ports.WorkingGroupRepository, activityRepo ports.ActivityRepository, orgSettingsSvc *orgsettingssvc.Service, routingSvc *routing.Service) — the shared instances (D-G parity, main.go wiring order). Declare(ctx, orgID, actorID, role, window): validation fast-fail first (kind in the closed set; ends_on >= starts_on; windowHoursValid 99.99 ceiling + whole-cent — research Pitfall 6; medical → certificate_ref required, D-14-05), then the role gate (actorID == window.UserID OR role == string(models.RoleHR) — D-14-26 employee-self-or-hr; else ErrForbidden), then repo.Declare with the audit DTO (ActorID = actor).
    3. Handler (availability_handler.go): Declare (claims → uuid/JSON parse → service → api envelope) + the writeError sentinel map (404/400/403/409/500 — the direction_handler.go:304-323 shape with the availability domain sentinels: ErrWindowNotFound → 404; ErrInvalidRequest/ErrInvalidHours/ErrInvalidKind/ErrCertificateRequired → 400; ErrForbidden → 403; ErrOverlap → 409).
    4. Wiring: cmd/server/main.go (availabilityRepo → availabilityService → availabilityHandler; route POST /availability/windows with middleware.Auth; reuse the shared orgsettings + routing services) + handler_test_helper.go fixture mirror (same shape as the direction block) — the fixture routes are needed for the battery to pass.
    Run the battery — GREEN. Commit the RED test commit first, then the implementation commits (13-08 dependency-order precedent: failing battery 74f2a3e, handler before wiring).
  </action>
  <behavior>
    - Test 1: POST /availability/windows (holiday, valid range) → 200 {status: "declared"}; audit_logs row (entity_type availability_window, action declared) exists for the window.
    - Test 2: kind "sick_day" → 400; ends_on before starts_on → 400; hours 100.00 → 400; hours 4.005 → 400; empty JSON → 400 (never 500).
    - Test 3: overlapping declared window → 409; adjacent window (ends_on = other.starts_on - 1 day) → 200.
    - Test 4: medical without certificate_ref → 400; medical with certificate_ref → 200 status "confirmed" + declared + confirmed audit rows.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/primary/http/ -run TestAvailabilityHandler -count=1</automated>
  </verify>
  <acceptance_criteria>Battery green end-to-end through the fixture; overlap 409 and medical auto-confirm proven over HTTP; the audit rows are observable in the response path's DB state (repo test asserts them in Task 2).</acceptance_criteria>
  <done>The single declare path works end-to-end (repo → service → handler → route → wiring) and is committed; the phase's core transaction shapes are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repo declare batteries — concurrency race, audit rollback, terminal-window exclusion</name>
  <files>internal/adapters/secondary/postgres/availability_repository_test.go, internal/adapters/secondary/postgres/availability_repository.go</files>
  <read_first>internal/adapters/secondary/postgres/ticket_repository_test.go (concurrent battery 414-509: start channel + buffered results + deterministic outcome set), internal/adapters/secondary/postgres/direction_repository_test.go (suite setup: SetupTestSchema + TeardownTestSchema + seedAvailabilityWindowWithCert from 14-01 — the certificate_ref-aware helper; the Phase 13 seedAvailabilityWindow without certificate_ref also remains available in direction_repository_test.go)</read_first>
  <action>
    RED: write the repo batteries (failing against the current repo file — the batteries assert behavior the repo already has; if Task 1 shipped correctly they pass immediately, otherwise they expose the gap — the standard 13-05/12-06 shape).
    GREEN (fix any gaps found): 
    1. Race battery: N goroutines (two) declare overlapping ranges for the same user simultaneously; exactly one succeeds, all others ErrOverlap — the FOR UPDATE serialization (D-14-15, CR-01 closure; the ticket_repository_test.go 414-509 shape).
    2. Audit-rollback battery: force the in-tx audit insert to fail (e.g. nil/invalid audit actor id or a pre-registered conflict) and assert the window row is NOT persisted — failed audit rolls back the state write (BE-012, D-14-12).
    3. Terminal-exclusion battery: seed withdrawn + rejected windows for the user; a new declare over the same range succeeds (D-14-13 active-only) — and a seeded confirmed window still blocks.
    4. Medical branch: repo-level medical declare returns status 'confirmed' with certificate_ref persisted + both audit rows (D-14-02/05/12).
  </action>
  <behavior>
    - Test 1: two concurrent Declare calls over the same range → outcomes set exactly {success, ErrOverlap}.
    - Test 2: audit insert failure → no window row survives (rollback proven).
    - Test 3: withdrawn + rejected windows never block; confirmed + declared always block.
    - Test 4: medical declare → confirmed status, certificate_ref persisted, declared + confirmed audit rows.
  </behavior>
  <verify>
    <automated>go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1</automated>
  </verify>
  <acceptance_criteria>All four batteries green; the race outcome set is deterministic (exactly one winner); rollback proven by absence of the row.</acceptance_criteria>
  <done>Declare correctness proven at the repo layer: race-closed, audit-atomic, active-only overlap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service declare unit tests — role gates, validation fast-fails, medical branch</name>
  <files>internal/core/services/availability/availability_test.go, internal/core/services/availability/availability.go</files>
  <read_first>internal/core/services/direction/direction_test.go (fixture: mocks + routing.NewService(f.wgRepo, f.actRepo, f.unitRepo) + orgsettingssvc.NewService(f.settings, f.orgRepo) — lines 45-55), internal/core/services/testdata/mock_availability_repo.go (14-02)</read_first>
  <action>
    RED: service unit tests with testdata.MockAvailabilityRepo (built per the direction_test.go fixture shape: routing.NewService with mock wg/activity/unit repos, orgsettingssvc.NewService with the mock org-settings repo — the availability service constructor takes the shared services).
    GREEN: fix any gaps in services/availability/availability.go Declare:
    - Gate tests: employee declaring own window → repo called, 200; hr declaring for ANY user → allowed (D-14-26); employee declaring for another user → ErrForbidden (repo NOT called).
    - Validation fast-fails: each invalid input returns its sentinel and never reaches the repo (kind, date range, hours ceiling, sub-cent hours, medical without certificate_ref).
    - Medical branch: service passes status 'confirmed' through to the repo (the repo writes it — the mock captures the window) and builds BOTH audit rows (declared + confirmed) with ActorID = actor (D-14-02/12).
    - Overlap: mock returns ErrOverlap → service propagates it (fast-fail UX parity: the service may pre-check via the mock's GetFn but the repo check stays authoritative — CR-01).
  </action>
  <behavior>
    - Test 1: employee-self declare → success path + audit capture; hr-for-anyone → success; employee-for-other → ErrForbidden with zero repo calls.
    - Test 2: table-driven invalid inputs → correct sentinels, repo never called.
    - Test 3: medical declare → window status "confirmed" + two audit rows (declared, confirmed) both with the actor id.
  </behavior>
  <verify>
    <automated>go test ./internal/core/services/availability/ -count=1</automated>
  </verify>
  <acceptance_criteria>Service unit tests green with the mock; gates and validation proven without a DB; audit DTO shape asserted via the mock's Audits capture.</acceptance_criteria>
  <done>Service declare behavior unit-tested: gates, validation, medical auto-confirm, audit DTOs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | untrusted declare payloads (kind, dates, hours, certificate_ref) cross here; validation + sentinel map are the only gate before the DB |
| service→repo | audit DTOs and the overlap guard cross here; the repo's FOR UPDATE re-check is authoritative (CR-01) |
| DB→API | 23514/constraint violations surface via wrapPGError — must map to 400/409, never 500 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14g-01 | Tampering | Declare overlap guard (TOCTOU) | critical | mitigate | In-tx SELECT ... FOR UPDATE on the user's ACTIVE overlapping windows (Task 1) + the race battery proving exactly-one-winner (Task 2) — D-14-15, CR-01 closure |
| T-14g-02 | Tampering | concurrent overlapping declares | high | mitigate | FOR UPDATE serialization + deterministic outcome-set race battery (Task 2) |
| T-14g-03 | Tampering | invalid declare input → 500 path | high | mitigate | Domain validation before any write (kind/date-range/hours 99.99 ceiling/certificate_ref) + writeError sentinel map — 400, never 500 (Tasks 1/3) |
| T-14g-04 | Elevation of Privilege | employee declaring for another user | high | mitigate | Service role gate: actor == window user OR role == models.RoleHR (Task 1/3, D-14-26) |
| T-14g-08 | Tampering | medical certificate_ref forgery path | medium | mitigate | certificate_ref required at declare for medical (D-14-05) + HR-correction path in 14-05 |
| T-14g-09 | Tampering | audit suppression (declare without audit) | high | mitigate | Audit row written in the same tx; failed audit insert rolls back the window (Task 2 rollback battery, BE-012) |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (research Package Legitimacy Audit); no install step |
</threat_model>

<verification>
- `go test ./internal/adapters/primary/http/ -run TestAvailabilityHandler -count=1` — declare battery green
- `go test ./internal/adapters/secondary/postgres/ -run TestAvailabilityRepository -count=1` — race/rollback/terminal-exclusion batteries green
- `go test ./internal/core/services/availability/ -count=1` — service unit tests green
- `go build ./...` — repo-wide compile (new handler + wiring)
</verification>

<success_criteria>
- POST /availability/windows works end-to-end: holiday → declared, medical → confirmed-with-ref, invalid → 400, overlap → 409, employee-self/hr gates enforced
- The declare transaction is race-closed (exactly one concurrent winner), audit-atomic (rollback on audit failure), and active-only (withdrawn/rejected never block)
- The phase's transaction/service/handler shapes are proven — 14-05 expands them mechanically
</success_criteria>

<output>
Create `.planning/phases/14-availability-backend-absences-capacity/14-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `internal/adapters/secondary/postgres/availability_repository.go` — AvailabilityRepository type + compile-time assertion + Declare (CR-01 overlap-guard tx with in-tx audit) + insertAvailabilityAudit helper
- `internal/core/services/availability/availability.go` — Service with the 7-arg constructor + Declare (validation fast-fails, employee-self-or-hr gate, medical auto-confirm branch, audit DTOs)
- `internal/adapters/primary/http/availability_handler.go` — AvailabilityHandler + Declare + writeError sentinel map
- `internal/adapters/primary/http/availability_handler_test.go` — declare integration battery
- `internal/adapters/secondary/postgres/availability_repository_test.go` — race/rollback/terminal-exclusion/medical batteries
- `internal/core/services/availability/availability_test.go` — service unit tests

**Modified files:**
- `cmd/server/main.go` — availability wiring + POST /availability/windows route
- `internal/adapters/primary/http/handler_test_helper.go` — fixture availability block
